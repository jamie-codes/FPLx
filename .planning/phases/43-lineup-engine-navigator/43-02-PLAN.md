---
phase: 43
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/app/page.tsx
  - src/components/nav/MobileNav.tsx
  - src/components/transfers/TransferPanel.tsx
  - src/app/page.test.tsx
  - src/components/nav/MobileNav.test.tsx
autonomous: true
requirements: [NAV-01]
tags: [react, navigation, refactor, fpl, squad]

must_haves:
  truths:
    - "User clicks Squad in desktop nav and sees a sub-tab row with 'Transfers' and 'Optimiser' buttons"
    - "User clicks Squad in mobile nav and sees a pill row with 'Transfers' and 'Optimiser' pills"
    - "Squad section default sub-tab is Transfers — TransferPanel visible on first Squad click"
    - "Clicking Optimiser sub-tab shows OptimiserPanel (stub from Plan 01) and hides TransferPanel"
    - "Team ID input lives in TransferPanel UI but state is owned by page.tsx (lifted via props)"
    - "Both TransferPanel and OptimiserPanel call useSquad(submittedId) — TanStack Query cache deduplicates"
    - "All existing page.test.tsx tests pass; the Squad/Sub-tab tests are updated for new behaviour"
    - "All existing MobileNav.test.tsx tests pass; NAV-04 test inverted to expect Squad pill row"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Updated SubTab union, Squad SECTIONS entry, sectionMemory init, teamId state, content guards"
      contains: "'transfers'"
    - path: "src/components/nav/MobileNav.tsx"
      provides: "Pill-row guard relaxed — Squad now shows pills when active"
      contains: "subTabs.length"
    - path: "src/components/transfers/TransferPanel.tsx"
      provides: "Controlled component receiving teamId/onTeamIdChange/submittedId/onSubmit as props"
      contains: "interface TransferPanelProps"
    - path: "src/app/page.test.tsx"
      provides: "Updated Squad tests + new Optimiser sub-tab test (NAV-01)"
      contains: "optimiser-panel"
    - path: "src/components/nav/MobileNav.test.tsx"
      provides: "Updated NAV-04 test asserting 2 Squad pills + 3 section buttons = 5 buttons total"
      contains: "Transfers"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/optimiser/OptimiserPanel"
      via: "import { OptimiserPanel } from '@/components/optimiser/OptimiserPanel'"
      pattern: "import.*OptimiserPanel.*optimiser/OptimiserPanel"
    - from: "src/app/page.tsx"
      to: "src/components/transfers/TransferPanel"
      via: "TransferPanel called with teamId/onTeamIdChange/submittedId/onSubmit props"
      pattern: "<TransferPanel\\s+teamId="
    - from: "src/components/nav/MobileNav.tsx"
      to: "src/app/page.tsx"
      via: "imports SECTIONS, Section, SubTab — Squad SECTIONS entry now has 2 subTabs which the relaxed guard surfaces"
      pattern: "subTabs.length"
---

<objective>
Atomic navigation wiring for Phase 43. All changes ship together because the SubTab union
extension (D-06) breaks any partial state — page.tsx, MobileNav.tsx, and both test files
must update in lockstep. Lifts teamId/submittedId state from TransferPanel up to page.tsx so
both Transfers and Optimiser sub-tabs share the squad fetch via TanStack Query cache.

Purpose: Implement NAV-01 (Squad sub-tabs) and the D-11 state lift required for OptimiserPanel
to receive teamId without prop-drilling squadData. Mounts the OptimiserPanel stub from Plan 01
behind the new Optimiser sub-tab.

Output: 3 modified source files + 2 modified test files. Stub OptimiserPanel from Plan 01 is
now reachable via the UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lineup-engine-navigator/43-CONTEXT.md
@.planning/phases/43-lineup-engine-navigator/43-RESEARCH.md
@.planning/phases/43-lineup-engine-navigator/43-PATTERNS.md
@.planning/phases/43-lineup-engine-navigator/43-01-SUMMARY.md
@src/app/page.tsx
@src/components/nav/MobileNav.tsx
@src/components/transfers/TransferPanel.tsx
@src/app/page.test.tsx
@src/components/nav/MobileNav.test.tsx

<interfaces>
<!-- Current contracts the executor must update atomically. -->

From src/app/page.tsx (current — line 23):
  export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'club-form' | 'value-gems' | 'accuracy'
After this plan: append `| 'transfers' | 'optimiser'`.

From src/app/page.tsx (current — lines 48-53): Squad SECTIONS entry currently has empty subTabs and null defaultSubTab. After this plan: subTabs gets two entries; defaultSubTab becomes 'transfers'.

From src/app/page.tsx (current — lines 58-62): sectionMemory init currently has `squad: null`. After this plan: `squad: 'transfers'`.

From src/app/page.tsx (current — line 109-126): desktop sub-tab nav is wrapped in `{activeSection !== 'squad' && (() => { ... })()}`. After this plan: guard removed; render gated by `activeSectionDef.subTabs.length > 0`.

From src/app/page.tsx (current — line 129): `{activeSection === 'squad' && <div className="mb-6 hidden sm:block" />}` — this entire line is REMOVED.

From src/app/page.tsx (current — line 132): `{activeSection === 'squad' && <TransferPanel />}` — this is REPLACED by two sub-tab-gated mounts (TransferPanel + OptimiserPanel).

From src/components/nav/MobileNav.tsx (current — line 18): pill row wrapped in `{activeSection !== 'squad' && (() => { ... })()}`. After this plan: guard removed; rendered when `subTabs.length > 0`.

From src/components/transfers/TransferPanel.tsx (current — lines 19-23): teamId and submittedId useState calls are LIFTED to page.tsx. TransferPanel becomes controlled. `freeTransfers` and `isModalOpen` STAY local (Pitfall 6). useSquad(submittedId) STAYS in TransferPanel.

From src/components/optimiser/OptimiserPanel.tsx (Plan 01 stub):
  interface OptimiserPanelProps { teamId: string }
  export function OptimiserPanel(_props: OptimiserPanelProps): JSX.Element

page.tsx mounts it as `<OptimiserPanel teamId={submittedId ?? ''} />`.

From src/app/page.test.tsx (current — line 16): TransferPanel is mocked as a no-prop stub. After this plan: mock signature updated to accept props; new mock added for OptimiserPanel.

From src/components/nav/MobileNav.test.tsx (current — lines 70-80): NAV-04 test asserts Squad shows 3 buttons (no pill row). After this plan: replaced with assertion that Squad shows 5 buttons (3 sections + 2 pills).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests FIRST — page.test.tsx and MobileNav.test.tsx (RED state for new contract)</name>
  <files>src/app/page.test.tsx, src/components/nav/MobileNav.test.tsx</files>
  <read_first>
    - src/app/page.test.tsx (full — confirm exact line numbers of Squad test at 116-128 and TransferPanel mock at line 16)
    - src/components/nav/MobileNav.test.tsx (full — confirm exact line numbers of NAV-04 test at 70-80)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 524-612 — exact mock additions and test assertions)
    - .planning/phases/43-lineup-engine-navigator/43-RESEARCH.md (lines 362-378 — Pitfall 3 + 4 explain test breakage)
  </read_first>
  <behavior>
    - Tests assert the NEW post-Phase-43 contract.
    - When run BEFORE Tasks 2-4 land, the new tests FAIL (page.tsx still uses old SubTab/SECTIONS).
    - After Tasks 2-4 land, all tests pass.
  </behavior>
  <action>
Edit `src/app/page.test.tsx`:

(a) UPDATE the TransferPanel mock at line 16. Replace the line:

  vi.mock('@/components/transfers/TransferPanel', () => ({ TransferPanel: () => <div data-testid="transfer-panel" /> }))

with:

  vi.mock('@/components/transfers/TransferPanel', () => ({ TransferPanel: (_props: { teamId: string; onTeamIdChange: (id: string) => void; submittedId: string | null; onSubmit: () => void }) => <div data-testid="transfer-panel" /> }))

(b) ADD an OptimiserPanel mock immediately AFTER the TransferPanel mock line (insert between the TransferPanel mock and the ClubFormTable mock):

  vi.mock('@/components/optimiser/OptimiserPanel', () => ({ OptimiserPanel: (_props: { teamId: string }) => <div data-testid="optimiser-panel" /> }))

(c) DELETE the entire test currently named `'Squad section renders only TransferPanel — no sub-tab content visible (CR-01)'` (lines 116-128 inclusive).

(d) INSERT TWO NEW TESTS at the same location (just before the closing `})` of the `describe('Phase 36: page.tsx state', ...)` block):

  it('Squad section default sub-tab is Transfers; TransferPanel visible, OptimiserPanel hidden, sub-tab nav present (D-05, D-07, D-08)', () => {
    const { container } = render(<Home />)
    const squadBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent === 'Squad')
    fireEvent.click(squadBtn!)
    // TransferPanel visible (default sub-tab = 'transfers' per D-07)
    expect(container.querySelector('[data-testid="transfer-panel"]')).not.toBeNull()
    // OptimiserPanel NOT visible until user navigates to Optimiser sub-tab
    expect(container.querySelector('[data-testid="optimiser-panel"]')).toBeNull()
    // Squad sub-tab nav IS now present (D-08 removed the activeSection !== 'squad' guard)
    expect(container.querySelector('nav[aria-label="Squad sub-tabs"]')).not.toBeNull()
    // Sub-tab nav contains both Transfers and Optimiser buttons
    const subTabs = container.querySelector('nav[aria-label="Squad sub-tabs"]')
    const subTabBtns = Array.from(subTabs!.querySelectorAll('button')).map(b => b.textContent)
    expect(subTabBtns).toEqual(['Transfers', 'Optimiser'])
  })

  it('Squad Optimiser sub-tab shows OptimiserPanel and hides TransferPanel (NAV-01, D-09)', () => {
    const { container } = render(<Home />)
    const squadBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent === 'Squad')
    fireEvent.click(squadBtn!)
    const optimiserBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent === 'Optimiser')
    fireEvent.click(optimiserBtn!)
    expect(container.querySelector('[data-testid="optimiser-panel"]')).not.toBeNull()
    expect(container.querySelector('[data-testid="transfer-panel"]')).toBeNull()
  })

DO NOT delete or modify any other tests in page.test.tsx. The Phase 36 default-landing test, sub-tab restoration tests, MobileNav abbreviations test, and the Phase 39 comparison-modal test ALL continue to pass.

Edit `src/components/nav/MobileNav.test.tsx`:

(e) DELETE the entire test currently named `'Squad active: pill row is absent, only 3 section buttons remain in DOM (NAV-04)'` (lines 70-80 inclusive).

(f) INSERT this new test at the same location:

  it('Squad active: pill row shows 2 pills Transfers and Optimiser; total 5 buttons in DOM (NAV-04 / NAV-01)', () => {
    const { container } = render(
      <MobileNav {...makeProps({ activeSection: 'squad' as Section, activeSubTab: 'transfers' as SubTab })} />
    )
    const allButtons = Array.from(container.querySelectorAll('button'))
    // 3 section buttons + 2 Squad pills = 5 total
    expect(allButtons).toHaveLength(5)
    const pillButtons = allButtons.filter(b => ['Transfers', 'Optimiser'].includes(b.textContent ?? ''))
    expect(pillButtons).toHaveLength(2)
    expect(pillButtons[0].textContent).toBe('Transfers')
    expect(pillButtons[1].textContent).toBe('Optimiser')
    // Active sub-tab pill has aria-current; inactive does not
    expect(pillButtons[0].getAttribute('aria-current')).toBe('page')
    expect(pillButtons[1].getAttribute('aria-current')).not.toBe('page')
  })

DO NOT touch any other tests in MobileNav.test.tsx. The other tests (NAV-01 sections, NAV-02 Analyse pills, NAV-03 Plan pills, NAV-05 click handlers, etc.) all continue to pass.

NOTE: After this task, `npx vitest run src/app/page.test.tsx src/components/nav/MobileNav.test.tsx` will exit NON-ZERO because the production code in Tasks 2-4 has not yet shipped. RED state is expected.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && grep -c "vi.mock('@/components/optimiser/OptimiserPanel'" src/app/page.test.tsx && grep -c "Squad Optimiser sub-tab shows OptimiserPanel" src/app/page.test.tsx && grep -c "Squad active: pill row shows 2 pills" src/components/nav/MobileNav.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.mock('@/components/optimiser/OptimiserPanel'" src/app/page.test.tsx` outputs `1`
    - `grep -c "data-testid=\"optimiser-panel\"" src/app/page.test.tsx` outputs `2` or more (mock + assertions)
    - `grep -c "Squad Optimiser sub-tab shows OptimiserPanel" src/app/page.test.tsx` outputs `1`
    - `grep -c "TransferPanel: (_props" src/app/page.test.tsx` outputs `1` (mock signature updated)
    - `grep -c "Squad section renders only TransferPanel" src/app/page.test.tsx` outputs `0` (old test removed)
    - `grep -c "expect(allButtons).toHaveLength(5)" src/components/nav/MobileNav.test.tsx` outputs `1`
    - `grep -c "Squad active: pill row is absent" src/components/nav/MobileNav.test.tsx` outputs `0`
    - `grep -c "Squad active: pill row shows 2 pills" src/components/nav/MobileNav.test.tsx` outputs `1`
    - At end of THIS task: `npx vitest run src/app/page.test.tsx src/components/nav/MobileNav.test.tsx` exits NON-ZERO (RED — implementation not yet shipped). This is expected.
  </acceptance_criteria>
  <done>Test contract updated; new tests fail (RED) until Tasks 2-4 ship.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor TransferPanel to controlled component (D-11 state lift)</name>
  <files>src/components/transfers/TransferPanel.tsx</files>
  <read_first>
    - src/components/transfers/TransferPanel.tsx (full — confirm exact lines: state declarations 19-23, freeTransfers 25, isModalOpen 26, handleSubmit 84-90, form 99, input onChange 110)
    - .planning/phases/43-lineup-engine-navigator/43-CONTEXT.md (D-11 — exact contract for state lift)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 470-521 — exact prop shape and what stays vs lifts)
    - .planning/phases/43-lineup-engine-navigator/43-RESEARCH.md (lines 382-388 — Pitfall 6 — only teamId/submittedId lift)
  </read_first>
  <behavior>
    - TransferPanel still renders identically to today; only state ownership moves.
    - `freeTransfers`, `isModalOpen`, all auth/transfer logic STAYS local (Pitfall 6).
    - The form input is now driven by props; submit is now a parent callback.
    - useSquad(submittedId) STAYS in TransferPanel — it just reads submittedId from props instead of local state.
  </behavior>
  <action>
Edit `src/components/transfers/TransferPanel.tsx`:

(a) ADD a TransferPanelProps interface ABOVE `export function TransferPanel()` (insert after the last import, before line 18):

  // Phase 43 D-11: teamId / submittedId / onSubmit lifted to page.tsx so OptimiserPanel
  // can receive teamId via props and share the useSquad cache. freeTransfers + isModalOpen
  // remain local — they are not used by OptimiserPanel.
  interface TransferPanelProps {
    teamId: string
    onTeamIdChange: (id: string) => void
    submittedId: string | null
    onSubmit: () => void
  }

(b) CHANGE the function signature on line 18 from:

  export function TransferPanel() {

to:

  export function TransferPanel({ teamId, onTeamIdChange, submittedId, onSubmit }: TransferPanelProps) {

(c) DELETE the local state declarations at lines 19-24 (the two useState calls for teamId and submittedId, both with localStorage init):

  // DELETE both of these:
  const [teamId, setTeamId] = useState<string>(() =>
    typeof window !== 'undefined' ? (localStorage.getItem('fpl_team_id') ?? '') : ''
  )
  const [submittedId, setSubmittedId] = useState<string | null>(() =>
    typeof window !== 'undefined' ? localStorage.getItem('fpl_team_id') : null
  )

KEEP the `freeTransfers` (line 25) and `isModalOpen` (line 26) useState calls — they stay local.

(d) DELETE the local handleSubmit function at lines 84-90:

  // DELETE this entire function:
  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault()
    if (teamId.trim()) {
      setSubmittedId(teamId.trim())
      localStorage.setItem('fpl_team_id', teamId.trim())
    }
  }

(e) REPLACE the form's onSubmit prop on line 99. Change:

  <form onSubmit={handleSubmit} className="flex flex-col sm:flex-row gap-2 sm:items-end">

to:

  <form onSubmit={(e) => { e.preventDefault(); onSubmit() }} className="flex flex-col sm:flex-row gap-2 sm:items-end">

(f) REPLACE the input's onChange handler on line 110. Change:

  onChange={e => setTeamId(e.target.value)}

to:

  onChange={e => onTeamIdChange(e.target.value)}

(g) Verify NO references to `setTeamId` or `setSubmittedId` remain in the file after these edits. The `useState` import remains because `freeTransfers` and `isModalOpen` still use it. `useMemo` and `useCallback` are still needed for transfer logic — leave alone.

DO NOT touch any other logic: the auth flow, useMemo blocks (scoredPlayers, transferResult, verdicts, captaincyCandidates, exactSellPrices), useSquad call, useMyTeam call, all JSX rendering of squad/captaincy/transfer suggestions — all of this stays exactly as-is.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && grep -c "interface TransferPanelProps" src/components/transfers/TransferPanel.tsx && grep -c "setTeamId\|setSubmittedId" src/components/transfers/TransferPanel.tsx && grep -c "onTeamIdChange" src/components/transfers/TransferPanel.tsx && grep -c "onSubmit()" src/components/transfers/TransferPanel.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface TransferPanelProps" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "teamId: string" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "onTeamIdChange:" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "submittedId: string | null" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "onSubmit: () => void" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "setTeamId" src/components/transfers/TransferPanel.tsx` outputs `0` (state lifted)
    - `grep -c "setSubmittedId" src/components/transfers/TransferPanel.tsx` outputs `0` (state lifted)
    - `grep -c "const handleSubmit" src/components/transfers/TransferPanel.tsx` outputs `0` (handler lifted)
    - `grep -c "onTeamIdChange(e.target.value)" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "{ e.preventDefault(); onSubmit()" src/components/transfers/TransferPanel.tsx` outputs `1`
    - `grep -c "useSquad(submittedId)" src/components/transfers/TransferPanel.tsx` outputs `1` (useSquad still here, reads from prop)
    - `grep -c "freeTransfers" src/components/transfers/TransferPanel.tsx` outputs `4` or more (still local — used in label, input, transferResult, freeTransfers cap)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TransferPanel is controlled — teamId/submittedId/onSubmit come from props; freeTransfers and modal state remain local; tsc passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Squad sub-tabs in src/app/page.tsx (atomic D-06..D-09 + D-11)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (full — confirm all 6 surgical edit locations: line 23 SubTab union, lines 48-53 SECTIONS Squad entry, lines 58-62 sectionMemory, line 109 sub-tab nav guard, line 129 spacer, line 132 TransferPanel mount)
    - src/components/optimiser/OptimiserPanel.tsx (Plan 01 stub — confirm `interface OptimiserPanelProps { teamId: string }` and `export function OptimiserPanel`)
    - src/components/transfers/TransferPanel.tsx (after Task 2 — confirm new props shape)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 288-415 — page.tsx exact-edit blocks)
    - .planning/phases/43-lineup-engine-navigator/43-RESEARCH.md (lines 386-391 — Pitfall 7: D-06 and D-08 must be atomic)
  </read_first>
  <behavior>
    - Squad section now has a desktop sub-tab nav matching Analyse/Plan visual.
    - Default sub-tab is Transfers; clicking Optimiser swaps content to OptimiserPanel.
    - teamId/submittedId state lives in page.tsx; localStorage init/write moves here.
    - TransferPanel and OptimiserPanel both call useSquad(submittedId) independently — TanStack Query dedupes.
  </behavior>
  <action>
Edit `src/app/page.tsx`:

(a) ADD an import for OptimiserPanel. Insert this line in the imports block, immediately after the `import { AccuracyTab } from '@/components/accuracy/AccuracyTab'` line (line 21):

  import { OptimiserPanel } from '@/components/optimiser/OptimiserPanel'

(b) UPDATE the SubTab union type on line 23. Replace:

  export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'club-form' | 'value-gems' | 'accuracy'

with:

  export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'club-form' | 'value-gems' | 'accuracy' | 'transfers' | 'optimiser'

(c) REPLACE the Squad SECTIONS entry at lines 48-53. Change:

  {
    id: 'squad' as Section,
    label: 'Squad',
    subTabs: [],
    defaultSubTab: null,
  },

to:

  {
    id: 'squad' as Section,
    label: 'Squad',
    subTabs: [
      { id: 'transfers' as SubTab, label: 'Transfers', mobileLabel: 'Transfers' },
      { id: 'optimiser' as SubTab, label: 'Optimiser', mobileLabel: 'Optimiser' },
    ],
    defaultSubTab: 'transfers' as SubTab,
  },

(d) UPDATE sectionMemory init at lines 58-62. Change `squad: null` to `squad: 'transfers'`. The full block becomes:

  const [sectionMemory, setSectionMemory] = useState<Record<Section, SubTab | null>>({
    analyse: 'gems',
    plan: 'planner',
    squad: 'transfers',
  })

(e) ADD teamId / submittedId state and submit handler. Insert these declarations immediately AFTER the existing `const [compareOpen, setCompareOpen] = useState(false)` line (around line 67, before the `const handleCompare = useCallback(...)` block):

  // Phase 43 D-11: teamId / submittedId lifted from TransferPanel so both Transfers
  // and Optimiser sub-tabs share the squad fetch via TanStack Query cache.
  const [teamId, setTeamId] = useState<string>(() =>
    typeof window !== 'undefined' ? (localStorage.getItem('fpl_team_id') ?? '') : ''
  )
  const [submittedId, setSubmittedId] = useState<string | null>(() =>
    typeof window !== 'undefined' ? localStorage.getItem('fpl_team_id') : null
  )
  const handleTeamIdSubmit = useCallback(() => {
    if (teamId.trim()) {
      setSubmittedId(teamId.trim())
      localStorage.setItem('fpl_team_id', teamId.trim())
    }
  }, [teamId])

(f) REPLACE the desktop sub-tab nav block at lines 109-126. Change:

  {/* Sub-tab row — hidden when Squad is active */}
  {activeSection !== 'squad' && (() => {
    const activeSectionDef = SECTIONS.find(s => s.id === activeSection)!
    return (
      <nav aria-label={`${activeSectionDef.label} sub-tabs`} className="hidden sm:flex gap-4 mb-6 border-b border-zinc-200 dark:border-zinc-700">
        {activeSectionDef.subTabs.map((sub) => (
          <button
            key={sub.id}
            className={`pb-2 px-1 text-sm font-medium ${activeSubTab === sub.id ? 'border-b-2 border-zinc-900 dark:border-white text-zinc-900 dark:text-white' : 'text-zinc-500 dark:text-zinc-400 hover:text-zinc-700 dark:hover:text-zinc-200'}`}
            onClick={() => handleSubTabChange(sub.id)}
            aria-current={activeSubTab === sub.id ? 'page' : undefined}
          >
            {sub.label}
          </button>
        ))}
      </nav>
    )
  })()}

with (D-08 — guard removed; gated on subTabs.length):

  {/* Sub-tab row — rendered for any section with subTabs.length > 0 (D-08) */}
  {(() => {
    const activeSectionDef = SECTIONS.find(s => s.id === activeSection)!
    if (activeSectionDef.subTabs.length === 0) return null
    return (
      <nav aria-label={`${activeSectionDef.label} sub-tabs`} className="hidden sm:flex gap-4 mb-6 border-b border-zinc-200 dark:border-zinc-700">
        {activeSectionDef.subTabs.map((sub) => (
          <button
            key={sub.id}
            className={`pb-2 px-1 text-sm font-medium ${activeSubTab === sub.id ? 'border-b-2 border-zinc-900 dark:border-white text-zinc-900 dark:text-white' : 'text-zinc-500 dark:text-zinc-400 hover:text-zinc-700 dark:hover:text-zinc-200'}`}
            onClick={() => handleSubTabChange(sub.id)}
            aria-current={activeSubTab === sub.id ? 'page' : undefined}
          >
            {sub.label}
          </button>
        ))}
      </nav>
    )
  })()}

(g) DELETE the Squad spacer block at line 129 entirely:

  {/* DELETE this entire line and its leading comment (`{/* Spacer when Squad is active... */}`): */}
  {activeSection === 'squad' && <div className="mb-6 hidden sm:block" />}

(h) REPLACE the TransferPanel mount on line 132. Change:

  {activeSection === 'squad' && <TransferPanel />}

with:

  {activeSection === 'squad' && activeSubTab === 'transfers' && (
    <TransferPanel
      teamId={teamId}
      onTeamIdChange={setTeamId}
      submittedId={submittedId}
      onSubmit={handleTeamIdSubmit}
    />
  )}
  {activeSection === 'squad' && activeSubTab === 'optimiser' && (
    <OptimiserPanel teamId={submittedId ?? ''} />
  )}

DO NOT touch the other content guards (lines 133-152: gems, defcon, club-form, set-pieces, insights, accuracy, value-gems, planner). The `activeSection !== 'squad' &&` prefix on those guards STAYS — those guards ensure non-Squad content does not leak into Squad view, which is still correct.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && grep -c "import { OptimiserPanel }" src/app/page.tsx && grep -c "'transfers' | 'optimiser'" src/app/page.tsx && grep -c "squad: 'transfers'" src/app/page.tsx && grep -c "handleTeamIdSubmit" src/app/page.tsx && grep -c "<OptimiserPanel teamId={submittedId" src/app/page.tsx && grep -c "activeSection !== 'squad' && (() =>" src/app/page.tsx && grep -c "activeSection === 'squad' && <div className=\"mb-6" src/app/page.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { OptimiserPanel } from '@/components/optimiser/OptimiserPanel'" src/app/page.tsx` outputs `1`
    - `grep -c "'transfers' | 'optimiser'" src/app/page.tsx` outputs `1` (SubTab union extended)
    - `grep -c "id: 'transfers' as SubTab, label: 'Transfers'" src/app/page.tsx` outputs `1`
    - `grep -c "id: 'optimiser' as SubTab, label: 'Optimiser'" src/app/page.tsx` outputs `1`
    - `grep -c "defaultSubTab: 'transfers' as SubTab" src/app/page.tsx` outputs `1`
    - `grep -c "squad: 'transfers'" src/app/page.tsx` outputs `1` (sectionMemory init)
    - `grep -c "squad: null" src/app/page.tsx` outputs `0` (old init removed)
    - `grep -c "const \[teamId, setTeamId\]" src/app/page.tsx` outputs `1`
    - `grep -c "const \[submittedId, setSubmittedId\]" src/app/page.tsx` outputs `1`
    - `grep -c "handleTeamIdSubmit" src/app/page.tsx` outputs `2` or more (declaration + use site)
    - `grep -c "activeSection !== 'squad' && (() =>" src/app/page.tsx` outputs `0` (D-08 guard removed from sub-tab nav)
    - `grep -c "activeSection === 'squad' && <div className=\"mb-6 hidden sm:block\" />" src/app/page.tsx` outputs `0` (spacer removed)
    - `grep -c "<TransferPanel$" src/app/page.tsx` outputs `0` (no zero-prop TransferPanel mounts left)
    - `grep -cE "<TransferPanel\s" src/app/page.tsx` outputs `1` (props are passed)
    - `grep -c "<OptimiserPanel teamId={submittedId" src/app/page.tsx` outputs `1`
    - `grep -c "activeSubTab === 'transfers'" src/app/page.tsx` outputs `1`
    - `grep -c "activeSubTab === 'optimiser'" src/app/page.tsx` outputs `1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>page.tsx wired atomically: SubTab union, SECTIONS, sectionMemory, teamId state, sub-tab nav guard, spacer removal, content guards. tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Relax MobileNav guard (D-10) and run full test suite to confirm GREEN</name>
  <files>src/components/nav/MobileNav.tsx</files>
  <read_first>
    - src/components/nav/MobileNav.tsx (full — confirm exact line 18 guard wraps the pill row in `activeSection !== 'squad' && (() => {...})()`)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 418-465 — exact MobileNav guard removal)
    - .planning/phases/43-lineup-engine-navigator/43-RESEARCH.md (lines 264-272 — Pattern 7 — guard becomes subTabs.length check)
  </read_first>
  <behavior>
    - MobileNav pill row appears whenever the active section has subTabs (Analyse, Plan, AND Squad after Phase 43).
    - All Phase 36 MobileNav tests still pass; the updated NAV-04 test now passes too.
    - Full vitest suite passes after this task (Tasks 1-4 were RED until this task lands).
  </behavior>
  <action>
Edit `src/components/nav/MobileNav.tsx`:

(a) REPLACE the pill-row block at lines 18-34 (the IIFE wrapped in `activeSection !== 'squad'`). Change:

  {activeSection !== 'squad' && (() => {
    const activeSectionDef = SECTIONS.find(s => s.id === activeSection)!
    return (
      <div className="flex gap-2 px-4 py-2 border-b border-zinc-200 dark:border-zinc-700">
        {activeSectionDef.subTabs.map((sub) => (
          <button
            key={sub.id}
            className={`px-3 py-1 text-xs font-medium rounded-full active:scale-95 transition-transform ${activeSubTab === sub.id ? 'bg-zinc-900 text-white dark:bg-zinc-100 dark:text-zinc-900' : 'text-zinc-500 dark:text-zinc-400 hover:text-zinc-700 dark:hover:text-zinc-200'}`}
            onClick={() => onSubTabChange(sub.id)}
            aria-current={activeSubTab === sub.id ? 'page' : undefined}
          >
            {sub.mobileLabel}
          </button>
        ))}
      </div>
    )
  })()}

with (D-10 — guard removed; gated on subTabs.length):

  {(() => {
    const activeSectionDef = SECTIONS.find(s => s.id === activeSection)!
    if (activeSectionDef.subTabs.length === 0) return null
    return (
      <div className="flex gap-2 px-4 py-2 border-b border-zinc-200 dark:border-zinc-700">
        {activeSectionDef.subTabs.map((sub) => (
          <button
            key={sub.id}
            className={`px-3 py-1 text-xs font-medium rounded-full active:scale-95 transition-transform ${activeSubTab === sub.id ? 'bg-zinc-900 text-white dark:bg-zinc-100 dark:text-zinc-900' : 'text-zinc-500 dark:text-zinc-400 hover:text-zinc-700 dark:hover:text-zinc-200'}`}
            onClick={() => onSubTabChange(sub.id)}
            aria-current={activeSubTab === sub.id ? 'page' : undefined}
          >
            {sub.mobileLabel}
          </button>
        ))}
      </div>
    )
  })()}

DO NOT touch the section button row (lines 35-46) — those buttons render unconditionally for all 3 sections and that behaviour is unchanged.

(b) After the edit lands, run the full vitest suite to confirm GREEN:

  npx vitest run

ALL tests must pass. If any test fails, debug — do NOT modify the test contracts written in Task 1. Common debug points:
- TransferPanel mock signature in page.test.tsx must match real props (Task 1 already updated this)
- OptimiserPanel mock added in page.test.tsx (Task 1 already added this)
- Squad pill assertions in MobileNav.test.tsx (Task 1 already inverted this)
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && grep -c "activeSection !== 'squad' && (() =>" src/components/nav/MobileNav.tsx && grep -c "activeSectionDef.subTabs.length === 0" src/components/nav/MobileNav.tsx && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeSection !== 'squad'" src/components/nav/MobileNav.tsx` outputs `0` (Squad guard removed)
    - `grep -c "activeSectionDef.subTabs.length === 0" src/components/nav/MobileNav.tsx` outputs `1` (new gate)
    - `grep -c "{sub.mobileLabel}" src/components/nav/MobileNav.tsx` outputs `1` (pill label render unchanged)
    - `grep -c "SECTIONS.map((section) =>" src/components/nav/MobileNav.tsx` outputs `1` (section button row untouched)
    - `npx vitest run src/app/page.test.tsx` exits 0 (all page tests GREEN)
    - `npx vitest run src/components/nav/MobileNav.test.tsx` exits 0 (all MobileNav tests GREEN)
    - `npx vitest run` exits 0 (FULL test suite GREEN — no regressions)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>MobileNav guard relaxed; full vitest suite green; Squad sub-tabs reachable on desktop and mobile; OptimiserPanel stub mounts correctly behind Optimiser sub-tab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | `fpl_team_id` persists across sessions. Already an established trust pattern (TransferPanel has used localStorage for teamId since Phase v1.0). The state lift to page.tsx does not change the trust posture — same key, same data, same client-only scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | localStorage `fpl_team_id` (lifted to page.tsx) | accept | A user tampering with their own browser's localStorage only affects their own /api/squad/[teamId] fetch. The teamId is also validated server-side (existing route validates numeric format). No new attack surface introduced by lifting state location. |
| T-43-05 | Spoofing | Squad sub-tab routing | accept | Sub-tab state is purely client-side; no server-side authorisation depends on activeSubTab. Worst-case impact of state corruption is rendering the wrong panel, which is self-correcting on next click. |
| T-43-06 | Information Disclosure | OptimiserPanel mount with teamId | accept | OptimiserPanel stub does not read or render the teamId in this plan — it only receives the prop. Plan 03 will use the prop to call useSquad, which already enforces the correct authentication and validation pattern. No new disclosure pathway. |

No high-severity threats. ASVS L1 categories V2 (auth), V3 (sessions), V4 (access control) covered by existing FPL auth patterns; this plan inherits without modification. V5 (input validation) covered by SquadPickSchema in squad-adapter.ts (unchanged). No new server-side surface.
</threat_model>

<verification>
Run the full test suite and tsc:

  npx vitest run
  npx tsc --noEmit

All tests must pass; tsc must exit clean.

Manual cross-check (recommended but not blocking — Plan 03 will add a checkpoint for the full Optimiser UI):
- Navigate to Squad in the desktop nav. See sub-tab row with "Transfers" and "Optimiser" buttons. Transfers is highlighted by default.
- Click "Optimiser". Sub-tab nav remains visible; content swaps to the OptimiserPanel stub showing "Optimised Lineup" + placeholder text.
- Resize to mobile width. Open Squad in MobileNav. See pill row with "Transfers" and "Optimiser" pills.
</verification>

<success_criteria>
- src/app/page.tsx imports OptimiserPanel; SubTab union includes 'transfers' and 'optimiser'; Squad SECTIONS entry has 2 sub-tabs; sectionMemory squad init = 'transfers'; teamId/submittedId state lives here; sub-tab nav renders for Squad; spacer removed; TransferPanel and OptimiserPanel mounts gated by activeSubTab
- src/components/nav/MobileNav.tsx pill row guard relaxed to `subTabs.length === 0` check
- src/components/transfers/TransferPanel.tsx is a controlled component receiving 4 props
- src/app/page.test.tsx has new Squad and Optimiser sub-tab tests; old single-Squad test removed; both mocks updated
- src/components/nav/MobileNav.test.tsx NAV-04 test inverted (5 buttons total)
- `npx vitest run` exits 0 (full suite passes)
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/43-lineup-engine-navigator/43-02-SUMMARY.md` with:
- Files modified and net lines changed
- Confirmation full test suite is GREEN (not just touched files)
- Any deviations from the plan (none expected)
- Manual cross-check result (desktop nav + mobile nav both show Squad sub-tabs)
</output>
