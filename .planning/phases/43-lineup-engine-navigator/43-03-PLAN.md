---
phase: 43
plan: 03
type: execute
wave: 3
depends_on: [43-01, 43-02]
files_modified:
  - src/components/optimiser/OptimiserPanel.tsx
  - src/components/optimiser/OptimiserPanel.test.tsx
autonomous: false
requirements: [OPT-01, OPT-02, OPT-03, OPT-04, OPT-05]
tags: [react, tailwind, fpl, optimiser, ui, pitch]

must_haves:
  truths:
    - "User sees a green pitch with 11 player circles arranged by formation (GK at bottom, FWD at top)"
    - "User sees the formation label ('Formation: 4-3-3') above the pitch, on the same row as the horizon toggle"
    - "User sees (C) next to the captain's name and (VC) next to the vice-captain's name on the pitch"
    - "User sees a bench row below the pitch with 4 slots; slot 0 (GK) is visually separated from outfield bench"
    - "User can click 1GW / 3GW / 5GW pills to change the horizon; the lineup re-optimises and updates"
    - "When fewer than 11 BGW-eligible players exist, an amber warning banner appears above the pitch and the engine return is null (no pitch shown OR a degraded view per D-16)"
    - "Empty / loading / error / no-data states render the documented copy from 43-UI-SPEC.md"
    - "Wave 0 OptimiserPanel.test.tsx stub from Plan 01 is REPLACED with full RTL tests covering all OPT requirements"
  artifacts:
    - path: "src/components/optimiser/OptimiserPanel.tsx"
      provides: "Full OptimiserPanel UI with pitch layout, horizon toggle, captain/VC labels, bench row, BGW banner, empty/loading/error states"
      min_lines: 150
    - path: "src/components/optimiser/OptimiserPanel.test.tsx"
      provides: "RTL tests for OPT-01 through OPT-05 UI behaviour"
      contains: "describe('Phase 43: OptimiserPanel"
  key_links:
    - from: "src/components/optimiser/OptimiserPanel.tsx"
      to: "src/lib/optimise-lineup.ts"
      via: "calls optimiseLineup(picks, players, horizon) inside a useMemo"
      pattern: "optimiseLineup\\("
    - from: "src/components/optimiser/OptimiserPanel.tsx"
      to: "src/lib/hooks/useSquad.ts"
      via: "useSquad(submittedId) — TanStack Query cache shared with TransferPanel"
      pattern: "useSquad\\("
    - from: "src/components/optimiser/OptimiserPanel.tsx"
      to: "src/components/gem-table/GwToggle.tsx"
      via: "import { GwToggle } from '@/components/gem-table/GwToggle'"
      pattern: "import.*GwToggle.*GwToggle"
---

<objective>
Replace the OptimiserPanel stub from Plan 01 with the full pitch-layout UI. The component
calls optimiseLineup() with the user's squad and renders an FPL-convention pitch (GK bottom,
FWD top) with player circles, captain/VC text labels, a horizon toggle, a bench row, and
state handling for empty/loading/error/BGW conditions.

Purpose: Deliver the visual surface for OPT-01..OPT-05 — the engine ships in Plan 01, the
nav wires in Plan 02, and this plan turns the data into pixels.

Output: 1 fully-implemented component + RTL tests covering all OPT requirements at the UI layer.
A human-verify checkpoint at the end confirms the live pitch renders correctly with a real squad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lineup-engine-navigator/43-CONTEXT.md
@.planning/phases/43-lineup-engine-navigator/43-RESEARCH.md
@.planning/phases/43-lineup-engine-navigator/43-PATTERNS.md
@.planning/phases/43-lineup-engine-navigator/43-UI-SPEC.md
@.planning/phases/43-lineup-engine-navigator/43-01-SUMMARY.md
@.planning/phases/43-lineup-engine-navigator/43-02-SUMMARY.md
@src/lib/optimise-lineup.ts
@src/lib/hooks/useSquad.ts
@src/lib/hooks/usePlayers.ts
@src/components/gem-table/GwToggle.tsx
@src/components/captaincy/CaptainPicksPanel.tsx
@src/components/squad/SquadView.tsx

<interfaces>
<!-- Contracts the executor uses. -->

From src/lib/optimise-lineup.ts (Plan 01 output):
  export type OptimiserHorizon = 1 | 3 | 5  (re-exported from types.ts)
  export interface OptimisedLineup {
    starters: number[]    // 11 element IDs
    bench: number[]       // 4 element IDs; bench[0] = non-starting GK
    captainId: number
    vcId: number
    formation: string     // e.g. '4-3-3'
  }
  export function optimiseLineup(picks: SquadPick[], players: MergedPlayer[], horizon: OptimiserHorizon): OptimisedLineup | null

From src/lib/hooks/useSquad.ts:
  export function useSquad(teamId: string | null): UseQueryResult<SquadPicksResponse | undefined, Error>
  // SquadPicksResponse.picks: SquadPick[] (15 entries)
  // Query key ['squad', teamId] — cache shared with TransferPanel which calls the same hook

From src/lib/hooks/usePlayers.ts:
  export function usePlayers(): UseQueryResult<MergedPlayer[] | undefined, Error>

From src/components/gem-table/GwToggle.tsx:
  interface Props { value: 1 | 3 | 5; onChange: (v: 1 | 3 | 5) => void }
  export function GwToggle({ value, onChange }: Props): JSX.Element
  // The accepted value type matches OptimiserHorizon (1 | 3 | 5) — direct reuse, no wrapper.
  // NOTE: GwToggle's aria-label is "Projected points horizon"; spec calls for "Scoring horizon".
  // This is fine — the existing aria-label is accurate; do not modify GwToggle.

From src/lib/types.ts (existing):
  PositionCode = 1 | 2 | 3 | 4   (1=GK, 2=DEF, 3=MID, 4=FWD)
  MergedPlayer.web_name, .element_type, .xPts_1gw, .xPts_3gw, .xPts_5gw, .xPts_90th_1gw, .status

From OptimiserPanel.tsx (Plan 01 stub — to be REPLACED):
  interface OptimiserPanelProps { teamId: string }
  // teamId comes from page.tsx as `submittedId ?? ''`. When teamId === '', no squad has been submitted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace OptimiserPanel stub with full pitch UI implementation</name>
  <files>src/components/optimiser/OptimiserPanel.tsx</files>
  <read_first>
    - src/components/optimiser/OptimiserPanel.tsx (the Plan 01 stub — confirm current shape before overwriting)
    - src/lib/optimise-lineup.ts (Plan 01 — confirm exact `optimiseLineup` signature, return type, null semantics)
    - src/lib/hooks/useSquad.ts (confirm useQuery return shape: data/isLoading/error)
    - src/lib/hooks/usePlayers.ts (confirm same return shape)
    - src/components/gem-table/GwToggle.tsx (confirm importable as named export, value prop type 1|3|5)
    - src/components/captaincy/CaptainPicksPanel.tsx (analog — copy 'use client' header, loading/error/null patterns from lines 53-70)
    - src/components/squad/SquadView.tsx (lines 161-165 — exact (C) and (VC) Tailwind classes; line 144 — bench detection pattern)
    - .planning/phases/43-lineup-engine-navigator/43-UI-SPEC.md (full — locked Tailwind classes, copy strings, pitch layout spec; this is the design contract)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 171-285 — exact OptimiserPanel structure; lines 706-790 — shared patterns for amber/error/loading)
    - .planning/phases/43-lineup-engine-navigator/43-RESEARCH.md (lines 344-360 — Pitfall 1 critical: undefined != BGW, only === 0)
  </read_first>
  <behavior>
    - When teamId is empty: render empty-state copy "Enter your FPL Team ID to see your optimised lineup."
    - When useSquad isLoading: render "Loading squad..." in zinc loading panel.
    - When useSquad errored: render error panel with `error.message ?? 'Unable to load squad data. Please try again.'`.
    - When squadData/playersData loaded:
      - Compute eligibleCount: number of squad players whose MergedPlayer.xPts_1gw !== 0 (treating undefined as eligible per Pitfall 1).
      - Call optimiseLineup(squadData.picks, players, horizon).
      - If result is null AND eligibleCount < 11: render BGW amber banner with eligibleCount; do not render pitch.
      - If result is null AND eligibleCount >= 11: defensive fallback — render error panel ("Unable to optimise lineup. Please try again.").
      - If result is non-null: render formation/horizon row + pitch + bench. If eligibleCount < 11 (impossible because engine would have returned null) — N/A, but keep the BGW banner check as a guard.
    - Pitch renders 4 rows top-to-bottom: FWD, MID, DEF, GK (FPL convention places GK at the bottom of the pitch visually).
    - Player circles show: web_name (truncated to 7 chars), xPts (1 decimal place), (C) if captainId, (VC) if vcId.
    - Bench row: bench[0] is the GK (with "GK" label above its circle); 1px vertical divider; bench[1..3] outfield in xPts-descending order.
    - Horizon toggle (GwToggle) is right-aligned in a flex row with the formation label on the left.
  </behavior>
  <action>
OVERWRITE the entire file `src/components/optimiser/OptimiserPanel.tsx` with the implementation below. This REPLACES the Plan 01 stub.

  'use client'

  // Phase 43: OptimiserPanel — full pitch UI (Plan 03)
  // Replaces the Plan 01 stub. Renders the optimised lineup on a green pitch with FPL convention
  // (GK at bottom, FWD at top). Calls optimiseLineup() pure engine; reuses GwToggle for horizon.
  // All Tailwind classes, copy strings, and layout structure follow 43-UI-SPEC.md.
  import { useState, useMemo } from 'react'
  import { useSquad } from '@/lib/hooks/useSquad'
  import { usePlayers } from '@/lib/hooks/usePlayers'
  import { GwToggle } from '@/components/gem-table/GwToggle'
  import { optimiseLineup } from '@/lib/optimise-lineup'
  import type { OptimiserHorizon, MergedPlayer } from '@/lib/types'

  interface OptimiserPanelProps {
    // teamId is the SUBMITTED team id (page.tsx passes `submittedId ?? ''`). Empty string means
    // user has not yet submitted a team id — show empty state.
    teamId: string
  }

  // Position codes (mirrors src/lib/optimise-lineup.ts internals)
  const GK = 1
  const DEF = 2
  const MID = 3
  const FWD = 4

  // Field name lookup for horizon-aware xPts display (matches HORIZON_FIELD in the engine)
  const HORIZON_FIELD: Record<OptimiserHorizon, 'xPts_1gw' | 'xPts_3gw' | 'xPts_5gw'> = {
    1: 'xPts_1gw',
    3: 'xPts_3gw',
    5: 'xPts_5gw',
  }

  // Renders one player circle (used for both pitch starters and bench slots).
  // isCaptain / isVc / isBench affect badge labels and opacity.
  function PlayerCircle({
    player,
    horizonField,
    isCaptain = false,
    isVc = false,
    isBench = false,
  }: {
    player: MergedPlayer
    horizonField: 'xPts_1gw' | 'xPts_3gw' | 'xPts_5gw'
    isCaptain?: boolean
    isVc?: boolean
    isBench?: boolean
  }) {
    const xPts = (player[horizonField] as number | undefined) ?? 0
    const truncatedName = player.web_name.slice(0, 7)
    return (
      <div className={`flex flex-col items-center w-16 ${isBench ? 'opacity-75' : ''}`} data-testid={`player-circle-${player.id}`}>
        <div className="w-12 h-12 rounded-full bg-zinc-100 dark:bg-zinc-700 flex items-center justify-center relative">
          <span className="text-[10px] font-semibold text-zinc-900 dark:text-zinc-100 text-center leading-tight px-0.5 truncate max-w-[44px]">
            {truncatedName}
          </span>
        </div>
        <span className="text-[10px] text-green-300 mt-1">{xPts.toFixed(1)}</span>
        {isCaptain && (
          <span className="text-[10px] font-semibold text-amber-400" data-testid={`captain-badge-${player.id}`}>(C)</span>
        )}
        {isVc && (
          <span className="text-[10px] font-semibold text-zinc-400" data-testid={`vc-badge-${player.id}`}>(VC)</span>
        )}
      </div>
    )
  }

  export function OptimiserPanel({ teamId }: OptimiserPanelProps) {
    const [horizon, setHorizon] = useState<OptimiserHorizon>(1)

    // teamId is the submitted id from page.tsx. Pass null to useSquad when empty so the query is disabled.
    const submittedId = teamId.trim() === '' ? null : teamId.trim()
    const { data: squadData, isLoading: squadLoading, error: squadError } = useSquad(submittedId)
    const { data: playersData, isLoading: playersLoading } = usePlayers()

    const isLoading = squadLoading || playersLoading
    const horizonField = HORIZON_FIELD[horizon]

    // Build playerMap and the optimised lineup (memoised so toggling horizon recomputes O(1,365)).
    const { playerMap, lineup, eligibleCount, totalPlayersInSquad } = useMemo(() => {
      if (!squadData || !playersData) {
        return { playerMap: new Map<number, MergedPlayer>(), lineup: null, eligibleCount: 0, totalPlayersInSquad: 0 }
      }
      const map = new Map<number, MergedPlayer>(playersData.map(p => [p.id, p]))
      // BGW-eligible count: same logic as the engine (Pitfall 1 — undefined != BGW; only === 0 excludes).
      const eligible = squadData.picks.filter(pick => {
        const p = map.get(pick.element)
        if (!p) return false
        return p.xPts_1gw !== 0
      }).length
      const result = optimiseLineup(squadData.picks, playersData, horizon)
      return { playerMap: map, lineup: result, eligibleCount: eligible, totalPlayersInSquad: squadData.picks.length }
    }, [squadData, playersData, horizon])

    // Empty state: no team id submitted
    if (submittedId === null) {
      return (
        <section className="mt-6 space-y-3" data-testid="optimiser-panel">
          <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>
          <div className="rounded border border-zinc-200 dark:border-zinc-700 p-6 text-center text-sm text-zinc-500 dark:text-zinc-400">
            Enter your FPL Team ID to see your optimised lineup.
          </div>
        </section>
      )
    }

    // Loading state
    if (isLoading) {
      return (
        <section className="mt-6 space-y-3" data-testid="optimiser-panel">
          <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>
          <div className="rounded border border-zinc-200 dark:border-zinc-700 p-4 text-sm text-zinc-500 dark:text-zinc-400">
            Loading squad...
          </div>
        </section>
      )
    }

    // Error state (squad fetch failed)
    if (squadError) {
      const errorMessage = squadError instanceof Error && squadError.message
        ? squadError.message
        : 'Unable to load squad data. Please try again.'
      return (
        <section className="mt-6 space-y-3" data-testid="optimiser-panel">
          <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>
          <div className="rounded border border-red-300 bg-red-50 dark:bg-red-950 p-4 text-sm text-red-700 dark:text-red-300">
            {errorMessage}
          </div>
        </section>
      )
    }

    // No squad data despite loaded (shouldn't happen post-loading, but defensive)
    if (!squadData || !playersData) {
      return (
        <section className="mt-6 space-y-3" data-testid="optimiser-panel">
          <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>
          <div className="rounded border border-zinc-200 dark:border-zinc-700 p-6 text-center text-sm text-zinc-500 dark:text-zinc-400">
            Load your squad using the Transfers tab, then return here to see your optimised lineup.
          </div>
        </section>
      )
    }

    // BGW critical state: too few eligible starters — engine returned null
    if (lineup === null) {
      return (
        <section className="mt-6 space-y-3" data-testid="optimiser-panel">
          <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>
          {eligibleCount < 11 ? (
            <div
              className="rounded border border-amber-400 bg-amber-50 dark:bg-amber-950 px-3 py-2 text-sm text-amber-800 dark:text-amber-200"
              data-testid="bgw-banner-critical"
            >
              <span className="font-semibold">Warning:</span>{' '}
              fewer than 11 eligible starters — only {eligibleCount} of your {totalPlayersInSquad} players have a fixture this gameweek. Optimised lineup may include bench players.
            </div>
          ) : (
            <div className="rounded border border-red-300 bg-red-50 dark:bg-red-950 p-4 text-sm text-red-700 dark:text-red-300">
              Unable to optimise lineup. Please try again.
            </div>
          )}
        </section>
      )
    }

    // Lineup is non-null: render formation/horizon row + pitch + bench
    const startersByPosition = {
      [GK]: lineup.starters.filter(id => playerMap.get(id)?.element_type === GK),
      [DEF]: lineup.starters.filter(id => playerMap.get(id)?.element_type === DEF),
      [MID]: lineup.starters.filter(id => playerMap.get(id)?.element_type === MID),
      [FWD]: lineup.starters.filter(id => playerMap.get(id)?.element_type === FWD),
    }

    // Bench: bench[0] is the GK; bench[1..3] are outfield in xPts desc (already ordered by engine).
    const benchGkPlayer = playerMap.get(lineup.bench[0])!
    const benchOutfieldPlayers = lineup.bench.slice(1).map(id => playerMap.get(id)!).filter(Boolean)

    return (
      <section className="mt-6 space-y-3" data-testid="optimiser-panel">
        <h2 className="text-base font-semibold text-zinc-900 dark:text-zinc-100">Optimised Lineup</h2>

        {/* BGW soft warning: some BGW exclusions but still ≥ 11 eligible — engine returned a lineup but show a notice */}
        {eligibleCount < totalPlayersInSquad && eligibleCount >= 11 && (
          <div
            className="rounded border border-amber-400 bg-amber-50 dark:bg-amber-950 px-3 py-2 text-sm text-amber-800 dark:text-amber-200"
            data-testid="bgw-banner-soft"
          >
            <span className="font-semibold">Blank gameweek warning:</span>{' '}
            only {eligibleCount} of your {totalPlayersInSquad} players have a fixture this gameweek.
          </div>
        )}

        {/* Formation label + horizon toggle row */}
        <div className="flex items-center justify-between mb-3">
          <span className="text-sm font-semibold text-zinc-700 dark:text-zinc-300" data-testid="formation-label">
            Formation: {lineup.formation}
          </span>
          <GwToggle value={horizon} onChange={setHorizon} />
        </div>

        {/* Pitch — green background, FWD top, GK bottom (FPL convention) */}
        <div className="relative rounded-lg overflow-hidden bg-green-950 p-4 min-h-[480px]" data-testid="pitch">
          {/* FWD row */}
          <div className="flex justify-center gap-3 mb-3">
            {startersByPosition[FWD].map(id => {
              const p = playerMap.get(id)!
              return (
                <PlayerCircle
                  key={id}
                  player={p}
                  horizonField={horizonField}
                  isCaptain={lineup.captainId === id}
                  isVc={lineup.vcId === id}
                />
              )
            })}
          </div>
          {/* MID row */}
          <div className="flex justify-center gap-3 mb-3">
            {startersByPosition[MID].map(id => {
              const p = playerMap.get(id)!
              return (
                <PlayerCircle
                  key={id}
                  player={p}
                  horizonField={horizonField}
                  isCaptain={lineup.captainId === id}
                  isVc={lineup.vcId === id}
                />
              )
            })}
          </div>
          {/* DEF row */}
          <div className="flex justify-center gap-3 mb-3">
            {startersByPosition[DEF].map(id => {
              const p = playerMap.get(id)!
              return (
                <PlayerCircle
                  key={id}
                  player={p}
                  horizonField={horizonField}
                  isCaptain={lineup.captainId === id}
                  isVc={lineup.vcId === id}
                />
              )
            })}
          </div>
          {/* GK row */}
          <div className="flex justify-center gap-3 mb-3">
            {startersByPosition[GK].map(id => {
              const p = playerMap.get(id)!
              return (
                <PlayerCircle
                  key={id}
                  player={p}
                  horizonField={horizonField}
                  isCaptain={lineup.captainId === id}
                  isVc={lineup.vcId === id}
                />
              )
            })}
          </div>

          {/* Bench row */}
          <div className="mt-3 pt-3 border-t border-green-900" data-testid="bench-row">
            <p className="text-xs font-semibold text-green-400 mb-2">Bench</p>
            <div className="flex justify-center gap-3 items-stretch">
              {/* Bench GK slot — slot 0, visually labelled */}
              <div className="flex flex-col items-center" data-testid="bench-gk-slot">
                <span className="text-[10px] text-green-500 mb-1">GK</span>
                <PlayerCircle player={benchGkPlayer} horizonField={horizonField} isBench />
              </div>
              {/* 1px vertical divider between GK and outfield bench */}
              <div className="w-px bg-green-900 self-stretch mx-1" data-testid="bench-divider" />
              {/* Outfield bench slots 1-3 in xPts desc (engine ordering) */}
              {benchOutfieldPlayers.map(p => (
                <div className="flex flex-col items-center" key={p.id} data-testid={`bench-outfield-${p.id}`}>
                  <span className="text-[10px] text-green-500 mb-1">&nbsp;</span>
                  <PlayerCircle player={p} horizonField={horizonField} isBench />
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>
    )
  }

Save the file. Run `npx tsc --noEmit` — must be clean.

NOTE: The aria-label on GwToggle's wrapper is "Projected points horizon" (existing). The UI-SPEC suggests "Scoring horizon" but the spec also says GwToggle is reused directly without modification. The aria-label is acceptable — do NOT modify GwToggle just to match the spec aria-label, that would touch out-of-scope code. Test assertions in Task 2 use the role+text, not the aria-label.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && grep -c "import { optimiseLineup }" src/components/optimiser/OptimiserPanel.tsx && grep -c "import { useSquad }" src/components/optimiser/OptimiserPanel.tsx && grep -c "import { GwToggle }" src/components/optimiser/OptimiserPanel.tsx && grep -c "data-testid=\"pitch\"" src/components/optimiser/OptimiserPanel.tsx && grep -c "data-testid=\"formation-label\"" src/components/optimiser/OptimiserPanel.tsx && grep -c "data-testid=\"bench-row\"" src/components/optimiser/OptimiserPanel.tsx && grep -c "bg-green-950" src/components/optimiser/OptimiserPanel.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use client'" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "import { optimiseLineup } from '@/lib/optimise-lineup'" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "import { useSquad } from '@/lib/hooks/useSquad'" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "import { usePlayers } from '@/lib/hooks/usePlayers'" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "import { GwToggle } from '@/components/gem-table/GwToggle'" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "data-testid=\"optimiser-panel\"" src/components/optimiser/OptimiserPanel.tsx` outputs `5` or more (one per render branch)
    - `grep -c "data-testid=\"pitch\"" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "data-testid=\"formation-label\"" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "data-testid=\"bench-row\"" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "data-testid=\"bench-gk-slot\"" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "bg-green-950" src/components/optimiser/OptimiserPanel.tsx` outputs `1` (pitch background)
    - `grep -c "border-amber-400" src/components/optimiser/OptimiserPanel.tsx` outputs `2` (BGW critical + soft banners)
    - `grep -c "Formation: {lineup.formation}" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "(C)" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "(VC)" src/components/optimiser/OptimiserPanel.tsx` outputs `1`
    - `grep -c "useState<OptimiserHorizon>(1)" src/components/optimiser/OptimiserPanel.tsx` outputs `1` (D-18 local state, default 1GW)
    - `grep -c "useMemo" src/components/optimiser/OptimiserPanel.tsx` outputs `1` (lineup computation memoised)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>OptimiserPanel renders pitch with formation/captain/VC/bench/horizon toggle for valid lineups; states for empty/loading/error/BGW handled; tsc passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace OptimiserPanel.test.tsx stub with full RTL tests covering OPT-01..OPT-05</name>
  <files>src/components/optimiser/OptimiserPanel.test.tsx</files>
  <read_first>
    - src/components/optimiser/OptimiserPanel.test.tsx (Plan 01 stub — confirm current 2 tests; full file replaced here)
    - src/components/optimiser/OptimiserPanel.tsx (after Task 1 — confirm exact data-testids, render branches, and prop shape)
    - src/lib/optimise-lineup.test.ts (Plan 01 — borrow makePlayer / makePick / makeSquad factory functions for shared test fixtures)
    - src/lib/hooks/useSquad.ts (confirm hook export shape so vi.mock matches)
    - src/lib/hooks/usePlayers.ts (confirm hook export shape so vi.mock matches)
    - .planning/phases/43-lineup-engine-navigator/43-PATTERNS.md (lines 678-704 — analog test pattern)
  </read_first>
  <behavior>
    - Tests cover all OPT requirements at the UI level: pitch presence (OPT-01), formation label (OPT-01), horizon toggle re-optimises (OPT-02), captain/VC badges (OPT-03), bench row + GK slot (OPT-04), BGW banner critical (OPT-05).
    - Hooks (useSquad, usePlayers) are mocked. The real optimiseLineup engine runs (no mock) — this is an integration test of Panel + Engine.
    - Empty / loading / error states each have a smoke test.
  </behavior>
  <action>
OVERWRITE the entire file `src/components/optimiser/OptimiserPanel.test.tsx` with the full test suite below. This REPLACES the Plan 01 stub.

  // Phase 43 (OPT-01..OPT-05): OptimiserPanel — full RTL integration tests.
  // Mocks the data hooks; runs the real optimise-lineup engine; asserts UI state for
  // empty / loading / error / BGW / valid-lineup branches.
  // @vitest-environment jsdom
  import { describe, it, expect, vi, beforeEach } from 'vitest'
  import { render, fireEvent } from '@testing-library/react'
  import type { MergedPlayer } from '@/lib/types'
  import type { SquadPick, SquadPicksResponse } from '@/lib/squad-adapter'

  // ---------- Hook mocks (mutable per test) ----------
  const useSquadMock = vi.fn()
  const usePlayersMock = vi.fn()

  vi.mock('@/lib/hooks/useSquad', () => ({
    useSquad: (id: string | null) => useSquadMock(id),
  }))
  vi.mock('@/lib/hooks/usePlayers', () => ({
    usePlayers: () => usePlayersMock(),
  }))

  // Import AFTER mocks
  import { OptimiserPanel } from './OptimiserPanel'

  // ---------- Test fixtures (mirrors src/lib/optimise-lineup.test.ts factories) ----------
  function makePick(element: number, position: number): SquadPick {
    return { element, position, multiplier: 1, is_captain: false, is_vice_captain: false }
  }

  function makePlayer(overrides: Partial<MergedPlayer> & { id: number; element_type: 1 | 2 | 3 | 4 }): MergedPlayer {
    return {
      id: overrides.id,
      web_name: `P${overrides.id}`,
      team: 1,
      team_short_name: 'T1',
      element_type: overrides.element_type,
      now_cost: 50,
      selected_by_percent: '5.0',
      form: '0.0',
      status: 'a',
      minutes: 900,
      starts: 10,
      total_points: 50,
      goals_scored: 2,
      assists: 1,
      expected_goals: 1.5,
      expected_assists: 1.0,
      pts_last3gw: 12,
      pts_last5gw: 20,
      pts_gw_count: 5,
      defensive_contribution: null,
      clearances_blocks_interceptions: null,
      direct_freekicks_order: null,
      penalties_order: null,
      corners_and_indirect_freekicks_order: null,
      penalties_text: '',
      direct_freekicks_text: '',
      corners_and_indirect_freekicks_text: '',
      news: '',
      cost_change_event: 0,
      cost_change_start: 0,
      understat_id: null,
      xg_per90: null,
      xa_per90: null,
      minutes_per90: 80,
      form_pts_per90: 5.0,
      fixtures: [],
      xmins: 80,
      start_prob: 0.9,
      mins_risk: 'nailed',
      xPts_1gw: 5.0,
      xPts_3gw: 14.0,
      xPts_5gw: 22.0,
      xPts_90th_1gw: 7.0,
      ...overrides,
    } as MergedPlayer
  }

  function makeValidSquad(): { picks: SquadPick[]; players: MergedPlayer[]; squadResp: SquadPicksResponse } {
    const elementTypes: (1 | 2 | 3 | 4)[] = [1, 1, 2, 2, 2, 2, 2, 3, 3, 3, 3, 3, 4, 4, 4]
    const picks: SquadPick[] = []
    const players: MergedPlayer[] = []
    for (let i = 0; i < 15; i++) {
      const id = i + 1
      picks.push(makePick(id, i + 1))
      players.push(makePlayer({ id, element_type: elementTypes[i] }))
    }
    const squadResp: SquadPicksResponse = {
      active_chip: null,
      picks,
      entry_history: { event: 30, bank: 0, event_transfers: 0, event_transfers_cost: 0, value: 1000 },
    }
    return { picks, players, squadResp }
  }

  beforeEach(() => {
    useSquadMock.mockReset()
    usePlayersMock.mockReset()
  })

  describe('Phase 43: OptimiserPanel (UI integration)', () => {
    describe('Empty / loading / error states', () => {
      it('renders empty-state copy when teamId is empty (no submission)', () => {
        useSquadMock.mockReturnValue({ data: undefined, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: undefined, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="" />)
        expect(container.textContent).toContain('Enter your FPL Team ID to see your optimised lineup.')
      })

      it('renders Loading copy when squad is loading', () => {
        useSquadMock.mockReturnValue({ data: undefined, isLoading: true, error: null })
        usePlayersMock.mockReturnValue({ data: undefined, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.textContent).toContain('Loading squad...')
      })

      it('renders red error panel with error.message when useSquad errors', () => {
        useSquadMock.mockReturnValue({ data: undefined, isLoading: false, error: new Error('Boom — 404 from FPL') })
        usePlayersMock.mockReturnValue({ data: [], isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.textContent).toContain('Boom — 404 from FPL')
        expect(container.querySelector('.border-red-300')).not.toBeNull()
      })

      it('renders fallback error copy when error has empty message', () => {
        useSquadMock.mockReturnValue({ data: undefined, isLoading: false, error: new Error('') })
        usePlayersMock.mockReturnValue({ data: [], isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.textContent).toContain('Unable to load squad data. Please try again.')
      })
    })

    describe('OPT-01 pitch + formation render', () => {
      it('renders the pitch div, the formation label, and a bench row when lineup is valid', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.querySelector('[data-testid="pitch"]')).not.toBeNull()
        expect(container.querySelector('[data-testid="formation-label"]')).not.toBeNull()
        expect(container.querySelector('[data-testid="formation-label"]')!.textContent).toMatch(/Formation: \d-\d-\d/)
        expect(container.querySelector('[data-testid="bench-row"]')).not.toBeNull()
      })

      it('pitch has bg-green-950 class (green field background)', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        const pitch = container.querySelector('[data-testid="pitch"]')
        expect(pitch?.className).toContain('bg-green-950')
      })

      it('renders 11 player circles on the pitch (excluding the bench)', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        const pitch = container.querySelector('[data-testid="pitch"]')!
        // Pitch contains starters + bench. Bench is wrapped in [data-testid="bench-row"]; subtract it.
        const allCirclesInPanel = container.querySelectorAll('[data-testid^="player-circle-"]')
        const benchCircles = container.querySelectorAll('[data-testid="bench-row"] [data-testid^="player-circle-"]')
        expect(allCirclesInPanel.length - benchCircles.length).toBe(11)
      })
    })

    describe('OPT-02 horizon toggle re-optimises', () => {
      it('clicking 5GW changes the formation/lineup output (horizon switch propagates)', () => {
        // Make horizon 1 lineup distinctly different from horizon 5 lineup.
        // DEF id=3: terrible 1gw, great 5gw. DEF id=7: great 1gw, terrible 5gw.
        const { picks } = makeValidSquad()
        const players: MergedPlayer[] = []
        const elementTypes: (1 | 2 | 3 | 4)[] = [1, 1, 2, 2, 2, 2, 2, 3, 3, 3, 3, 3, 4, 4, 4]
        for (let i = 0; i < 15; i++) {
          const id = i + 1
          let p = makePlayer({ id, element_type: elementTypes[i] })
          if (id === 3) p = { ...p, xPts_1gw: 0.1, xPts_3gw: 100, xPts_5gw: 100 }
          if (id === 7) p = { ...p, xPts_1gw: 9.9, xPts_3gw: 0.1, xPts_5gw: 0.1 }
          players.push(p)
        }
        const squadResp: SquadPicksResponse = {
          active_chip: null,
          picks,
          entry_history: { event: 30, bank: 0, event_transfers: 0, event_transfers_cost: 0, value: 1000 },
        }
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)

        // Default horizon = 1: id=7 (P7) should be a starter; id=3 (P3) should NOT be.
        const initialCircleIds = Array.from(container.querySelectorAll('[data-testid="pitch"] > div [data-testid^="player-circle-"]'))
          .map(n => n.getAttribute('data-testid'))
        expect(initialCircleIds).toContain('player-circle-7')
        expect(initialCircleIds).not.toContain('player-circle-3')

        // Click the 5GW toggle button (GwToggle renders "5 GW" — note the space)
        const fiveGwBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent?.trim() === '5 GW')
        expect(fiveGwBtn).toBeTruthy()
        fireEvent.click(fiveGwBtn!)

        // After toggle: id=3 should now be a starter; id=7 should NOT.
        const after5gwCircleIds = Array.from(container.querySelectorAll('[data-testid="pitch"] > div [data-testid^="player-circle-"]'))
          .map(n => n.getAttribute('data-testid'))
        expect(after5gwCircleIds).toContain('player-circle-3')
        expect(after5gwCircleIds).not.toContain('player-circle-7')
      })
    })

    describe('OPT-03 captain / VC badges', () => {
      it('renders exactly one (C) badge and one (VC) badge on the pitch', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        const captainBadges = container.querySelectorAll('[data-testid^="captain-badge-"]')
        const vcBadges = container.querySelectorAll('[data-testid^="vc-badge-"]')
        expect(captainBadges).toHaveLength(1)
        expect(vcBadges).toHaveLength(1)
      })

      it('captain badge has amber colour class; VC badge has zinc colour class', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        const captainBadge = container.querySelector('[data-testid^="captain-badge-"]')!
        const vcBadge = container.querySelector('[data-testid^="vc-badge-"]')!
        expect(captainBadge.className).toContain('text-amber-400')
        expect(vcBadge.className).toContain('text-zinc-400')
        expect(captainBadge.textContent).toBe('(C)')
        expect(vcBadge.textContent).toBe('(VC)')
      })
    })

    describe('OPT-04 bench row layout', () => {
      it('bench row has GK slot at position 0 with "GK" label and a divider before outfield', () => {
        const { players, squadResp } = makeValidSquad()
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.querySelector('[data-testid="bench-gk-slot"]')).not.toBeNull()
        expect(container.querySelector('[data-testid="bench-gk-slot"]')!.textContent).toContain('GK')
        expect(container.querySelector('[data-testid="bench-divider"]')).not.toBeNull()
        const outfieldBenchSlots = container.querySelectorAll('[data-testid^="bench-outfield-"]')
        expect(outfieldBenchSlots).toHaveLength(3)
      })
    })

    describe('OPT-05 BGW critical banner', () => {
      it('renders amber critical banner when more than 4 players have xPts_1gw === 0 (engine returns null)', () => {
        // 5 BGW players -> 10 eligible -> engine returns null
        const { picks } = makeValidSquad()
        const players: MergedPlayer[] = []
        const elementTypes: (1 | 2 | 3 | 4)[] = [1, 1, 2, 2, 2, 2, 2, 3, 3, 3, 3, 3, 4, 4, 4]
        for (let i = 0; i < 15; i++) {
          const id = i + 1
          const isBgw = [1, 3, 5, 8, 13].includes(id)
          players.push(makePlayer({
            id,
            element_type: elementTypes[i],
            xPts_1gw: isBgw ? 0 : 5.0,
          }))
        }
        const squadResp: SquadPicksResponse = {
          active_chip: null,
          picks,
          entry_history: { event: 30, bank: 0, event_transfers: 0, event_transfers_cost: 0, value: 1000 },
        }
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.querySelector('[data-testid="bgw-banner-critical"]')).not.toBeNull()
        expect(container.querySelector('[data-testid="bgw-banner-critical"]')!.textContent).toContain('fewer than 11')
        expect(container.querySelector('[data-testid="pitch"]')).toBeNull()
      })

      it('renders soft amber banner when some BGW exclusions occur but engine still returns 11 starters', () => {
        // 2 BGW players -> 13 eligible -> engine still returns a lineup; soft banner shown
        const { picks } = makeValidSquad()
        const players: MergedPlayer[] = []
        const elementTypes: (1 | 2 | 3 | 4)[] = [1, 1, 2, 2, 2, 2, 2, 3, 3, 3, 3, 3, 4, 4, 4]
        for (let i = 0; i < 15; i++) {
          const id = i + 1
          const isBgw = [14, 15].includes(id)  // 2 FWDs BGW; remaining 1 FWD + 5 MID + 5 DEF + 2 GK = 13 eligible
          players.push(makePlayer({
            id,
            element_type: elementTypes[i],
            xPts_1gw: isBgw ? 0 : 5.0,
          }))
        }
        const squadResp: SquadPicksResponse = {
          active_chip: null,
          picks,
          entry_history: { event: 30, bank: 0, event_transfers: 0, event_transfers_cost: 0, value: 1000 },
        }
        useSquadMock.mockReturnValue({ data: squadResp, isLoading: false, error: null })
        usePlayersMock.mockReturnValue({ data: players, isLoading: false })
        const { container } = render(<OptimiserPanel teamId="1234567" />)
        expect(container.querySelector('[data-testid="bgw-banner-soft"]')).not.toBeNull()
        expect(container.querySelector('[data-testid="pitch"]')).not.toBeNull()
      })
    })
  })

Save the file. Run `npx vitest run src/components/optimiser/OptimiserPanel.test.tsx` — all 11+ tests must PASS.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx vitest run src/components/optimiser/OptimiserPanel.test.tsx 2>&1 | tail -20 && grep -cE "^\s+it\(" src/components/optimiser/OptimiserPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/optimiser/OptimiserPanel.test.tsx` exists with new content
    - `grep -c "describe('Phase 43: OptimiserPanel (UI integration)'" src/components/optimiser/OptimiserPanel.test.tsx` outputs `1`
    - `grep -cE "^\s+it\(" src/components/optimiser/OptimiserPanel.test.tsx` outputs `11` or more
    - `grep -c "vi.mock('@/lib/hooks/useSquad'" src/components/optimiser/OptimiserPanel.test.tsx` outputs `1`
    - `grep -c "vi.mock('@/lib/hooks/usePlayers'" src/components/optimiser/OptimiserPanel.test.tsx` outputs `1`
    - `grep -c "OPT-01\|OPT-02\|OPT-03\|OPT-04\|OPT-05" src/components/optimiser/OptimiserPanel.test.tsx` outputs `5` or more (at least one per requirement)
    - `grep -c "bgw-banner-critical\|bgw-banner-soft" src/components/optimiser/OptimiserPanel.test.tsx` outputs `2` or more
    - `npx vitest run src/components/optimiser/OptimiserPanel.test.tsx` exits 0 (all tests GREEN)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>OptimiserPanel.test.tsx covers all OPT-01..OPT-05 UI behaviours; all tests GREEN; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live OptimiserPanel pitch with a real squad</name>
  <what-built>
    - Plan 01: pure optimiseLineup engine + OptimiserHorizon/OptimisedLineup types + RED-then-GREEN engine tests + OptimiserPanel stub
    - Plan 02: Squad sub-tab navigation (desktop + mobile) + teamId state lifted to page.tsx + TransferPanel controlled refactor + test contract updates
    - Plan 03 (this plan): full OptimiserPanel pitch UI + horizon toggle + (C)/(VC) badges + bench row + BGW handling + integration tests
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open the app at http://localhost:3000.
2. Click the "Squad" section (top nav on desktop, bottom nav on mobile).
3. Confirm the sub-tab row appears with "Transfers" and "Optimiser" buttons. Transfers is selected by default.
4. If you have not previously entered an FPL team id, enter your team id on the Transfers tab and click "Load Squad". Wait for squad data to load.
5. Click the "Optimiser" sub-tab. Verify ALL of the following:
   a. Heading reads "Optimised Lineup"
   b. A green pitch (`bg-green-950`) is visible with the FPL convention layout: GK at the bottom, defenders above, midfielders, forwards at top
   c. Formation label appears above the pitch (e.g., "Formation: 4-3-3")
   d. To the right of the formation label is a 3-button toggle: "1 GW", "3 GW", "5 GW". The "1 GW" button is highlighted by default.
   e. Exactly 11 player circles render on the pitch
   f. ONE circle has a "(C)" badge below it (amber); ONE circle has a "(VC)" badge below it (zinc)
   g. Each circle shows the player's web_name (truncated to 7 chars) and their xPts (1 decimal)
   h. Below the pitch is a "Bench" header followed by 4 player slots: a GK slot (with "GK" label) on the left, a thin vertical divider, then 3 outfield bench slots ordered by xPts descending
6. Click the "3 GW" button. The lineup should update — possibly different starters, different formation, different captain. The button highlight moves to "3 GW".
7. Click the "5 GW" button. Same — lineup updates again.
8. Switch back to the Transfers sub-tab. The squad data appears immediately (TanStack Query cache shared — no re-fetch).
9. Resize the browser to mobile width. Open Squad in MobileNav. Confirm the pill row shows "Transfers" and "Optimiser" pills. Tap "Optimiser" — the same pitch UI renders correctly.
10. (Optional, if currently in a BGW or close to one) Confirm the amber BGW banner appears when applicable.

If anything looks wrong, describe the issue and how to reproduce. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser <-> /api/squad/[teamId] | Existing API route — Phase 43 introduces no new server endpoints. teamId already validated as numeric server-side. |
| Browser <-> /api/players | Existing API route — Phase 43 reads existing data; no new field writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | Tampering | OptimiserPanel xPts rendering | accept | xPts values come from already-validated MergedPlayer data fetched via /api/players. No user-controlled inputs flow into rendered numbers. |
| T-43-08 | Information Disclosure | Player web_name truncation in circles | accept | web_name is public FPL data (player surnames). Truncation to 7 chars is a presentation choice, not a confidentiality control. |
| T-43-09 | Denial of Service | useMemo over optimiseLineup on horizon change | mitigate | optimiseLineup is bounded at 1,365 iterations (<1ms). Wrapped in useMemo with stable deps (squadData, playersData, horizon) so re-renders that don't change those inputs do not re-compute. Worst case (every horizon click triggers full re-compute) is still <1ms — well under any DoS threshold. |
| T-43-10 | Spoofing | Captain/VC badge correctness | accept | Captain and VC are derived from engine output, which is purely a function of inputs. No user-spoofable badge selection in this plan (Phase 46 chip modes will add such interactions; not in scope here). |

No high-severity threats. ASVS L1 V2 (auth), V3 (sessions), V4 (access control) covered by existing /api/squad/[teamId] route. V5 (input validation) covered by SquadPicksResponseSchema in squad-adapter.ts (unchanged). V6 (crypto) not applicable.
</threat_model>

<verification>
Run the full test suite and tsc:

  npx vitest run
  npx tsc --noEmit

All tests must pass; tsc must exit clean.

Then perform the human checkpoint (Task 3) on a live dev server.
</verification>

<success_criteria>
- src/components/optimiser/OptimiserPanel.tsx is a fully-implemented React component (not a stub) with: empty/loading/error/BGW/lineup branches; pitch with 4 rows (FWD top, GK bottom); horizon toggle; (C)/(VC) badges; bench row with isolated GK slot
- src/components/optimiser/OptimiserPanel.test.tsx contains 11+ `it` blocks covering OPT-01 through OPT-05 plus state branches; all GREEN
- `npx vitest run` exits 0 (full suite)
- `npx tsc --noEmit` exits 0
- Human checkpoint approved: live pitch renders correctly with a real squad; horizon toggle re-optimises live; mobile + desktop both work
</success_criteria>

<output>
After completion, create `.planning/phases/43-lineup-engine-navigator/43-03-SUMMARY.md` with:
- Files modified and net lines changed (Plan 01 stub replaced)
- Test pass count from `npx vitest run`
- Confirmation human checkpoint passed (or list any issues + how they were resolved before re-verifying)
- Any deviations from 43-UI-SPEC.md (none expected; if any, justify)
</output>
