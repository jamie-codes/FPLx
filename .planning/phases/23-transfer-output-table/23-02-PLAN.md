---
phase: 23-transfer-output-table
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/components/planner/PlannerTab.tsx
autonomous: false
requirements: [PLAN-05, PLAN-07]

must_haves:
  truths:
    - "PlannerTab renders TransferPlanTable when planResult is non-null"
    - "Chip toggle in a GW row updates that step's chip field in planResult state"
    - "Clicking the same chip again clears it (toggle off)"
    - "planResult state uses useImmer for safe nested mutation"
    - "User sees the full transfer table after clicking Generate Plan"
  artifacts:
    - path: "src/components/planner/PlannerTab.tsx"
      provides: "Wiring of TransferPlanTable into planner UI with useImmer chip toggle"
      contains: "useImmer"
  key_links:
    - from: "src/components/planner/PlannerTab.tsx"
      to: "src/components/planner/TransferPlanTable.tsx"
      via: "import and render <TransferPlanTable>"
      pattern: "import.*TransferPlanTable"
    - from: "src/components/planner/PlannerTab.tsx"
      to: "use-immer"
      via: "useImmer hook for planResult state"
      pattern: "import.*useImmer.*use-immer"
---

<objective>
Wire TransferPlanTable into PlannerTab by migrating planResult state from useState to useImmer and rendering the table component. Then human-verify the full flow in the browser.

Purpose: Connects the display components from Plan 01 to the live planner UI, delivering PLAN-05 and PLAN-07 end-to-end.
Output: Updated PlannerTab.tsx with TransferPlanTable rendering and chip toggle wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-transfer-output-table/23-RESEARCH.md
@.planning/phases/23-transfer-output-table/23-UI-SPEC.md
@.planning/phases/23-transfer-output-table/23-01-SUMMARY.md

@src/components/planner/PlannerTab.tsx
@src/components/planner/TransferPlanTable.tsx
@src/components/planner/ChipToggle.tsx
@src/components/planner/plan-helpers.ts
@src/lib/types.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/planner/TransferPlanTable.tsx:
```typescript
interface TransferPlanTableProps {
  planResult: PlanResult
  scoredPlayers: ScoredPlayer[]
  onChipToggle: (stepIndex: number, chip: PlannerChip) => void
}
export function TransferPlanTable({ planResult, scoredPlayers, onChipToggle }: TransferPlanTableProps): JSX.Element
```

From src/lib/types.ts:
```typescript
export type PlannerChip = 'wildcard' | 'freehit' | 'bboost' | '3xc' | null
export interface PlanResult {
  steps: PlanStep[]
  horizon: PlannerHorizon
  startingGw: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TransferPlanTable into PlannerTab with useImmer migration</name>
  <files>src/components/planner/PlannerTab.tsx</files>
  <read_first>
    - src/components/planner/PlannerTab.tsx (current state — must see exact hook calls and JSX structure)
    - src/components/planner/TransferPlanTable.tsx (props interface to match)
    - src/lib/types.ts (PlannerChip type for handleChipToggle)
    - .planning/phases/23-transfer-output-table/23-RESEARCH.md (Pitfall 3: useImmer migration steps)
  </read_first>
  <action>
    Modify `src/components/planner/PlannerTab.tsx` with these specific changes:

    **1. Update imports (line 3):**
    - Change: `import { useState, useMemo } from 'react'`
    - To: `import { useState, useMemo } from 'react'` (keep useState for teamId and horizon)
    - Add: `import { useImmer } from 'use-immer'`
    - Add: `import { TransferPlanTable } from './TransferPlanTable'`
    - Add: `import type { PlannerChip } from '@/lib/types'` (add to existing type import on line 11)

    **2. Migrate planResult state (line 15):**
    - Change: `const [planResult, setPlanResult] = useState<PlanResult | null>(null)`
    - To: `const [planResult, updatePlanResult] = useImmer<PlanResult | null>(null)`

    **3. Update handleGeneratePlan (line 66):**
    - Change: `setPlanResult(result)`
    - To: `updatePlanResult(() => result)`

    **4. Add handleChipToggle function** after handleGeneratePlan:
    ```typescript
    function handleChipToggle(stepIndex: number, chip: PlannerChip) {
      updatePlanResult(draft => {
        if (!draft) return
        const step = draft.steps[stepIndex]
        step.chip = step.chip === chip ? null : chip
      })
    }
    ```

    **5. Replace the planResult display section (lines 87-92):**
    - Remove the existing `{planResult && (<div className="text-sm ...">Plan generated: ...</div>)}` block
    - Replace with:
    ```tsx
    {planResult && (
      <TransferPlanTable
        planResult={planResult}
        scoredPlayers={scoredPlayers}
        onChipToggle={handleChipToggle}
      />
    )}
    ```

    No other changes to PlannerTab.tsx. All existing hooks, data fetching, and button logic remain as-is.
  </action>
  <verify>
    <automated>npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PlannerTab.tsx contains `import { useImmer } from 'use-immer'`
    - PlannerTab.tsx contains `import { TransferPlanTable }` from `./TransferPlanTable`
    - PlannerTab.tsx contains `const [planResult, updatePlanResult] = useImmer<PlanResult | null>(null)`
    - PlannerTab.tsx contains `updatePlanResult(() => result)` (not setPlanResult)
    - PlannerTab.tsx contains `function handleChipToggle(stepIndex: number, chip: PlannerChip)`
    - PlannerTab.tsx contains `step.chip = step.chip === chip ? null : chip`
    - PlannerTab.tsx contains `<TransferPlanTable` with planResult, scoredPlayers, onChipToggle props
    - PlannerTab.tsx does NOT contain `setPlanResult`
    - PlannerTab.tsx does NOT contain the old "Plan generated:" text
    - `npx vitest run` exits 0
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>PlannerTab renders TransferPlanTable with chip toggle support. planResult uses useImmer. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of transfer output table</name>
  <files>src/components/planner/PlannerTab.tsx</files>
  <action>
    Human visual verification of the complete transfer output table. No code changes in this task — verify the output of Tasks 1 (Plan 01) and Task 1 (Plan 02).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms table renders correctly with all columns, chip toggles work, dark mode is correct, and mobile layout is responsive.</done>
  <what-built>
    Complete transfer output table with:
    - Plan value headline above table
    - One row per GW with columns: GW (with DGW/BGW badges), Chip toggle, Out, In, Hit, Gain
    - Hold rows for GWs with no profitable transfer
    - Red hit cost styling for -4 pt hits
    - Chip toggle buttons with active/inactive states
    - Dark mode support
    - Mobile responsive layout
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Navigate to the Planner tab
    3. Ensure a team ID is set (or authenticate) so squad data loads
    4. Select a horizon (e.g., 3 GWs) and click "Generate Plan"
    5. Verify the table appears with:
       - "Plan value: +X.X pts" headline above the table
       - One row per GW with GW number, transfer details, hit cost, and gain
       - Hold rows show "Hold" with "No profitable transfer this gameweek" subtext
       - Hit cost of -4 appears in red
    6. Click a chip button (e.g., "Wildcard") on any GW row — verify it highlights
    7. Click the same chip button again — verify it clears (toggle off)
    8. Toggle dark mode and verify all text/background colours are correct
    9. Resize browser to mobile width (<640px) and verify chip toggles move below GW rows
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green
- `npx next build` — clean build
- Human visual verification of table layout, chip toggles, dark mode, mobile responsiveness
</verification>

<success_criteria>
- Clicking "Generate Plan" renders a transfer table below the button
- Table has correct columns: GW, Chip, Out, In, Hit, Gain
- Chip toggle buttons work (click to activate, click again to deactivate)
- Plan value headline shows total net gain
- Dark mode and mobile layout render correctly
- Human approves visual output
</success_criteria>

<output>
After completion, create `.planning/phases/23-transfer-output-table/23-02-SUMMARY.md`
</output>
