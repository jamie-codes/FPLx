---
phase: 20-auth-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth-expiry.ts
  - tests/lib/auth-expiry.test.ts
  - src/components/transfers/AuthModal.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "computeAuthExpiryState returns 'normal' when token has > 1hr remaining"
    - "computeAuthExpiryState returns 'expiring-soon' when token has 15min-1hr remaining"
    - "computeAuthExpiryState returns 'expired' when token has < 15min remaining or is undefined"
    - "AuthModal renders a native dialog element with numbered Chrome DevTools guide steps"
    - "AuthModal has a clipboard paste button that calls navigator.clipboard.readText()"
    - "AuthModal submits token to POST /api/auth/login and calls onSuccess on 200"
  artifacts:
    - path: "src/lib/auth-expiry.ts"
      provides: "Pure function for expiry state computation"
      exports: ["computeAuthExpiryState"]
    - path: "tests/lib/auth-expiry.test.ts"
      provides: "Unit tests for expiry thresholds"
      contains: "computeAuthExpiryState"
    - path: "src/components/transfers/AuthModal.tsx"
      provides: "Modal component with guide and token form"
      contains: "AuthModal"
  key_links:
    - from: "src/lib/auth-expiry.ts"
      to: "tests/lib/auth-expiry.test.ts"
      via: "import in test file"
      pattern: "import.*computeAuthExpiryState.*from.*auth-expiry"
    - from: "src/components/transfers/AuthModal.tsx"
      to: "/api/auth/login"
      via: "fetch POST in handleLogin"
      pattern: "fetch.*api/auth/login"
---

<objective>
Create the expiry state utility function (with TDD) and the AuthModal component that will replace the inline token form in TransferPanel.

Purpose: AUTH-03 and AUTH-04 both converge into a polished modal-based guided token entry experience (per D-01). This plan builds the two new artifacts — the next plan wires them into TransferPanel.

Output: `src/lib/auth-expiry.ts`, `tests/lib/auth-expiry.test.ts`, `src/components/transfers/AuthModal.tsx`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-auth-ux/20-CONTEXT.md
@.planning/phases/20-auth-ux/20-RESEARCH.md

<interfaces>
<!-- Existing auth hook — AuthModal and expiry function consume expiresAt from this -->
From src/lib/hooks/useAuthStatus.ts:
```typescript
export function useAuthStatus(): {
  isAuthenticated: boolean
  expiresAt: number | undefined
  isLoading: boolean
  setAuthenticated: () => void
  clearAuthenticated: () => void
}
```

From src/app/api/auth/login/route.ts:
```typescript
// POST /api/auth/login
// Request body: { token: string }
// Success response: { ok: true, expiresAt: number }
// Error response: { error: string, detail?: string }
```

From src/lib/fpl-auth.ts:
```typescript
export function extractTokenExpiry(token: string): number | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create computeAuthExpiryState pure function with TDD</name>
  <files>src/lib/auth-expiry.ts, tests/lib/auth-expiry.test.ts</files>
  <read_first>
    - src/lib/hooks/useAuthStatus.ts (expiresAt type and shape)
    - tests/lib/gem-score.test.ts (test file pattern reference)
  </read_first>
  <behavior>
    - computeAuthExpiryState(undefined, anyNumber) returns 'expired'
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now > 3600 returns 'normal'
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now === 3600 returns 'normal' (boundary: exactly 1hr is normal)
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now === 3599 returns 'expiring-soon' (just under 1hr)
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now === 900 returns 'expiring-soon' (exactly 15min)
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now === 899 returns 'expired' (just under 15min)
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now === 0 returns 'expired'
    - computeAuthExpiryState(expiresAt, now) where expiresAt - now < 0 returns 'expired'
  </behavior>
  <action>
    RED phase:
    1. Create `tests/lib/auth-expiry.test.ts` with all 8 test cases listed in behavior above.
    2. Import `computeAuthExpiryState` from `@/lib/auth-expiry` (file does not exist yet — test must fail).
    3. Each test uses a fixed `now` value (e.g., `1000000`) and computes `expiresAt` relative to it.
    4. Run `npx vitest run tests/lib/auth-expiry.test.ts` — confirm all tests fail.

    GREEN phase:
    1. Create `src/lib/auth-expiry.ts` exporting:
       ```typescript
       export type AuthExpiryState = 'normal' | 'expiring-soon' | 'expired'

       export function computeAuthExpiryState(
         expiresAt: number | undefined,
         nowSeconds: number
       ): AuthExpiryState {
         if (expiresAt === undefined) return 'expired'
         const remaining = expiresAt - nowSeconds
         if (remaining >= 3600) return 'normal'
         if (remaining >= 900) return 'expiring-soon'
         return 'expired'
       }
       ```
    2. Run tests — confirm all pass.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/auth-expiry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/lib/auth-expiry.test.ts contains `import { computeAuthExpiryState } from`
    - tests/lib/auth-expiry.test.ts contains at least 8 `test(` or `it(` calls
    - src/lib/auth-expiry.ts contains `export type AuthExpiryState = 'normal' | 'expiring-soon' | 'expired'`
    - src/lib/auth-expiry.ts contains `export function computeAuthExpiryState(`
    - src/lib/auth-expiry.ts contains `>= 3600` (1hr threshold)
    - src/lib/auth-expiry.ts contains `>= 900` (15min threshold)
    - `npx vitest run tests/lib/auth-expiry.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 8 boundary tests pass. Pure function exported from src/lib/auth-expiry.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthModal component</name>
  <files>src/components/transfers/AuthModal.tsx</files>
  <read_first>
    - src/components/transfers/TransferPanel.tsx (existing inline form pattern, button styles, dark mode classes)
    - src/app/api/auth/login/route.ts (POST body shape and response)
    - src/lib/hooks/useAuthStatus.ts (setAuthenticated call pattern)
    - .planning/phases/20-auth-ux/20-RESEARCH.md (dialog pattern, pitfalls)
  </read_first>
  <action>
    Create `src/components/transfers/AuthModal.tsx` as a `'use client'` component.

    **Props interface** (per D-03):
    ```typescript
    interface AuthModalProps {
      open: boolean
      onClose: () => void
      onSuccess: () => void
    }
    ```

    **Structure:**
    1. Native `<dialog>` element, always rendered in DOM (per Pitfall 4 in RESEARCH.md). Controlled via `ref.current.showModal()` / `ref.current.close()` in a `useEffect` keyed on `open` prop.

    2. `close` event listener on dialog element syncs React state when Escape key dismisses natively (per Pitfall 1 in RESEARCH.md). Call `onClose()` from the event handler.

    3. Backdrop click dismiss: `onClick` handler on `<dialog>` checks `e.target === dialogRef.current` and calls `onClose()` (per Pitfall 2 in RESEARCH.md, per D-03).

    4. **Modal content layout** (per D-02, D-03):
       - `<h2>` heading: "Connect FPL Account"
       - Numbered guide steps (per D-02, Chrome-focused):
         ```
         1. Open fantasy.premierleague.com and log in
         2. Press F12 to open DevTools
         3. Click the Network tab
         4. Click any request to /api/ (e.g. reload the page)
         5. In the Headers pane, find x-api-authorization
         6. Copy the value (starts with "Bearer eyJ...")
         7. Paste it below
         ```
       - Token input: `<input type="text" placeholder="Paste Bearer token here..." />` with `font-mono` class. Existing pattern from TransferPanel line 179-185.
       - Clipboard paste button (per D-04): icon button next to input, calls `navigator.clipboard.readText()` in a try/catch. Silent failure on permission denied (per Pitfall 3). Button text: "Paste" with a clipboard icon (use unicode U+1F4CB or simple text "Paste").
       - Submit button: "Save token" — same blue style as existing TransferPanel submit (`bg-blue-600 dark:bg-blue-500 text-white rounded hover:bg-blue-700 dark:hover:bg-blue-600 disabled:opacity-50 active:scale-95 transition-transform`).
       - Cancel button: "Cancel" — same muted style (`text-zinc-500 dark:text-zinc-400 hover:text-zinc-700 dark:hover:text-zinc-200`).
       - Error display: `{loginError && <span className="text-sm text-red-600">{loginError}</span>}`

    5. **Form submission logic** — replicate `handleLogin` from TransferPanel lines 67-90:
       - `fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token: tokenInput }) })`
       - On success (res.ok): call `onSuccess()`, reset `tokenInput`, reset `loginError`
       - On error: parse `body.detail ?? body.error ?? 'Invalid token'`, set `loginError`
       - On catch: set `loginError` to `'Request failed — check your connection'`
       - Loading state: disable submit button, show "Saving..." text

    6. **Tailwind styling** — dialog element classes (per RESEARCH.md Pattern 1):
       `className="rounded-lg bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-700 p-6 w-full max-w-lg shadow-lg backdrop:bg-black/50"`

       If `backdrop:bg-black/50` does not compile in Tailwind v4, add to `src/app/globals.css`:
       ```css
       dialog::backdrop {
         background: rgba(0, 0, 0, 0.5);
       }
       ```

    7. **Reset on close:** When `open` changes from true to false, clear `tokenInput` and `loginError` state.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/transfers/AuthModal.tsx 2>&1 || echo "Type check done"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/transfers/AuthModal.tsx starts with `'use client'`
    - File contains `interface AuthModalProps` with `open: boolean`, `onClose: () => void`, `onSuccess: () => void`
    - File contains `<dialog` element with `ref={dialogRef}`
    - File contains `showModal()` and `.close()` calls
    - File contains `addEventListener('close'` for Escape key sync
    - File contains `e.target === dialogRef.current` for backdrop click dismiss
    - File contains `navigator.clipboard.readText()` inside a try/catch
    - File contains `fetch('/api/auth/login'` with POST method
    - File contains the 7 numbered guide steps mentioning "fantasy.premierleague.com", "F12", "Network", "x-api-authorization"
    - File contains `dark:bg-zinc-900` (dark mode support)
    - File contains `active:scale-95` on at least one button
  </acceptance_criteria>
  <done>AuthModal component compiles, renders a native dialog with guide steps, token input with clipboard paste, and form submission to /api/auth/login.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/lib/auth-expiry.test.ts` passes (all 8 tests green)
- `src/components/transfers/AuthModal.tsx` exists and type-checks
- No modifications to existing auth Route Handlers or hooks
</verification>

<success_criteria>
- computeAuthExpiryState function exported and fully tested with 8 boundary cases
- AuthModal component created with dialog, guide steps, clipboard paste, form submission
- Both files are new additions — no existing files modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/20-auth-ux/20-01-SUMMARY.md`
</output>
