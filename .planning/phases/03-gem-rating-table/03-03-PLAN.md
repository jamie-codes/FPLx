---
phase: 03-gem-rating-table
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false
requirements: [GEM-01, GEM-02, PPS-03, UIX-01, UIX-02]
must_haves:
  truths:
    - "Table renders real player data sorted by Gem score at /"
    - "Clicking column headers toggles sort order visually"
    - "Position filter shows only selected position players"
    - "Null xG/xA shows em-dash in the rendered table"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the GemTable — confirm sorting, filtering, null handling, and layout work correctly with real data.

Purpose: Automated tests cover the scoring algorithm but cannot verify interactive table behavior (DOM sort toggling, filter button states, visual null rendering). This checkpoint confirms the full user experience.
Output: Verified GemTable ready for daily use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gem-rating-table/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify GemTable interactive behavior</name>
  <files>src/components/gem-table/GemTable.tsx, src/app/page.tsx</files>
  <action>
Present the GemTable to the user for visual verification. Run `npm run dev` to start the dev server.

What was built:
Gem Rating Table at `/` showing all FPL players scored by a 7-dimension composite algorithm, with sortable columns and position filter buttons. Built with TanStack Table v8, data from usePlayers() hook, scoring from computeAllGemScores().

How to verify:
1. Run `npm run dev` and visit http://localhost:3000
2. **Data loads:** Table shows player rows with Gem score, component scores, team, position, price
3. **Default sort:** Players should be sorted by Gem score descending (highest gem first)
4. **Sort toggle:** Click the "xG/90" column header — rows should reorder by xG per 90. Click again to toggle direction. No page reload occurs.
5. **Position filter:** Click "MID" button — only midfielders (element_type=3) should show. Row count should update. Click "All" to restore.
6. **Null handling:** Find a promoted-team player with no Understat data — their xG/90 and xA/90 columns should show em-dash, NOT "0" or empty.
7. **Component scores visible:** Each row shows FDR, Form, xG Sc, xA Sc, Own, Min, SP scores alongside the composite Gem score.
8. **Position labels:** The Pos column shows GK/DEF/MID/FWD text, not numeric codes 1/2/3/4.

Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>User has visually confirmed: table loads at /, sorting toggles work, position filter isolates positions, null xG/xA shows em-dash, component scores visible per row</done>
</task>

</tasks>

<verification>
Human visual verification of interactive table behavior.
</verification>

<success_criteria>
1. Table loads and displays player data at /
2. Sorting works by clicking column headers
3. Position filter correctly isolates GK/DEF/MID/FWD
4. Null xG/xA renders as em-dash
5. Component scores visible per row
</success_criteria>

<output>
After completion, create `.planning/phases/03-gem-rating-table/03-03-SUMMARY.md`
</output>
