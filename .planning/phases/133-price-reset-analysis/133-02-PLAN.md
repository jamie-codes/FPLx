---
phase: 133
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/app/api/price-reset/route.ts
  - src/app/api/price-reset/route.test.ts
  - src/lib/hooks/usePriceReset.ts
autonomous: true
requirements:
  - PRST-02
  - PRST-03
  - PRST-04
tags:
  - api
  - types
  - hook
  - typescript
must_haves:
  truths:
    - "GET /api/price-reset returns { published: boolean, players: PriceResetRow[], value_targets: ValueTargetRow[] } as PriceResetResponse"
    - "When price_baseline.json is absent from Blob/cache, the route returns published: false with players=[] and value_targets=[] (D-08 — no 404)"
    - "When the baseline exists and any element's now_cost differs from the baseline, published: true and players is sorted by largest absolute delta_cost first"
    - "value_targets contains only fall-rows whose merged_players xPts_1gw exceeds the position median computed across all element_type matches in merged_players.json, sorted by largest fall first (most negative delta_cost)"
    - "usePriceReset() TanStack Query hook fetches /api/price-reset with staleTime 30 minutes mirroring usePriceChanges"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "PriceResetRow, ValueTargetRow, PriceResetResponse interfaces"
      contains: "export interface PriceResetResponse"
    - path: "src/app/api/price-reset/route.ts"
      provides: "Diff-based GET handler reading baseline + current bootstrap + merged_players"
      contains: "export async function GET"
    - path: "src/app/api/price-reset/route.test.ts"
      provides: "Vitest route contract — baseline-absent, no-delta, delta-present, value-targets, malformed-input cases"
      contains: "@vitest-environment node"
    - path: "src/lib/hooks/usePriceReset.ts"
      provides: "TanStack Query hook returning PriceResetResponse"
      contains: "export function usePriceReset"
  key_links:
    - from: "src/app/api/price-reset/route.ts"
      to: "price_baseline.json + fpl_bootstrap.json + merged_players.json (Blob or local cache)"
      via: "readBlobOrLocal helper following pre-season-squad/route.ts pattern"
      pattern: "price_baseline\\.json"
    - from: "src/app/api/price-reset/route.ts"
      to: "PriceResetResponse type"
      via: "import from @/lib/types"
      pattern: "import.*PriceResetResponse.*from '@/lib/types'"
    - from: "src/lib/hooks/usePriceReset.ts"
      to: "/api/price-reset endpoint"
      via: "fetch in queryFn"
      pattern: "fetch\\(['\"]\\/api\\/price-reset"
---

<objective>
Land the Phase 133 frontend backbone: TypeScript types describing the price-reset payload, a Next.js App Router GET handler at /api/price-reset that diffs price_baseline.json against the current bootstrap and computes Value Targets from merged_players.json, a vitest contract test covering the four documented states (baseline-absent, no-deltas, deltas-present, value-targets), and a TanStack Query hook mirroring usePriceChanges.

Purpose: PRST-02/PRST-03/PRST-04 all hinge on a single API contract: the UI cannot decide between empty state and populated tables without a `published` boolean and pre-sorted player/value-target arrays. Centralising the diff and position-median maths in the route keeps the React tree pure-presentational in Plan 03.

Output: One new interface block in types.ts, one new API route + test pair, one new hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-price-reset-analysis/133-CONTEXT.md
@.planning/phases/133-price-reset-analysis/133-UI-SPEC.md

# Canonical implementations to mirror
@src/app/api/pre-season-squad/route.ts
@src/app/api/price-changes/route.ts
@src/lib/hooks/usePriceChanges.ts
</context>

<interfaces>
<!-- Key existing types and patterns the executor reuses. -->

From src/lib/types.ts (existing — already present):
```typescript
export type PositionCode = 1 | 2 | 3 | 4   // 1=GK, 2=DEF, 3=MID, 4=FWD

export interface FPLElement {
  id: number
  element_type: PositionCode
  now_cost: number                  // tenths of GBP 1m
  // ... plus team, web_name, etc.
}
```

From merged_players.json (per CONTEXT.md D-04/D-05) — each row contains at minimum:
- `id: number`, `web_name: string`, `team_short_name: string`, `element_type: 1|2|3|4`, `now_cost: number`, `xPts_1gw?: number`
The route reads merged_players.json as `Array<{ id: number; web_name: string; team_short_name: string; element_type: 1|2|3|4; now_cost: number; xPts_1gw?: number }>`.

readBlobOrLocal helper pattern (copy from src/app/api/pre-season-squad/route.ts lines 15-34):
- Reads `process.env.USE_BLOB?.toLowerCase() === 'true'`.
- Blob branch: `await list({ prefix: filename, limit: 1 })` then `await fetch(blobs[0].url)` then `await res.text()`.
- Local branch: `await readFile(join(process.cwd(), 'pipeline', 'cache', filename), 'utf-8')`.
- Returns `null` on ENOENT or empty blob list; re-throws other errors.

Route response shape this plan defines (must be exported from src/lib/types.ts):
```typescript
export interface PriceResetRow {
  player_id: number
  name: string                 // web_name
  team: string                 // team_short_name e.g. "ARS"
  element_type: PositionCode
  baseline_cost: number        // tenths of GBP 1m, from price_baseline.json
  current_cost: number         // tenths of GBP 1m, from current bootstrap
  delta_cost: number           // current_cost - baseline_cost; positive = rise
}

export interface ValueTargetRow extends PriceResetRow {
  // delta_cost is guaranteed < 0 in this collection (D-06 / UI-SPEC).
  xPts_1gw: number             // from merged_players.json; must exceed position median
  position_median_xPts: number // median xPts_1gw across all merged_players with same element_type
  position_rank: number        // 1-indexed rank within element_type, sorted by xPts_1gw desc
  position_label: 'GK' | 'DEF' | 'MID' | 'FWD'
}

export interface PriceResetResponse {
  published: boolean           // false when no baseline or all deltas zero (D-07/D-08)
  generated_at: string         // ISO 8601 — server timestamp at request time
  players: PriceResetRow[]     // sorted by Math.abs(delta_cost) DESC; empty when published=false
  value_targets: ValueTargetRow[]  // fall-only, xPts above position median; sorted by delta_cost ASC (most negative first)
}
```

ELEMENT_TYPE_LABEL constant (must match UI-SPEC #N POS format):
```typescript
const ELEMENT_TYPE_LABEL: Record<PositionCode, 'GK' | 'DEF' | 'MID' | 'FWD'> = {
  1: 'GK', 2: 'DEF', 3: 'MID', 4: 'FWD',
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing vitest contract for /api/price-reset route</name>
  <files>src/app/api/price-reset/route.test.ts, src/lib/types.ts</files>
  <read_first>
    - src/app/api/pre-season-squad/route.test.ts (canonical vitest patterns — hoisted vi.mock for fs/promises and @vercel/blob, NextRequest construction, type imports)
    - src/app/api/price-changes/route.ts (the closest sibling — same envelope and readBlobOrLocal shape)
    - src/lib/types.ts (existing PositionCode, FPLElement, PriceChanges types — extend, do not duplicate)
    - .planning/phases/133-price-reset-analysis/133-CONTEXT.md (D-02 shape `{[element_id]: now_cost}`, D-04 merged_players xPts, D-05 position median, D-06 row format, D-07 diff detection, D-08 baseline-absent fallback, D-09 empty state)
    - .planning/phases/133-price-reset-analysis/133-UI-SPEC.md (sort order specifications)
  </read_first>
  <behavior>
    - Test 1 `published_false_when_baseline_absent`: fs readFile for `price_baseline.json` throws ENOENT. Response is HTTP 200 with `{ published: false, players: [], value_targets: [], generated_at: <ISO string> }`. Confirms D-08 fallback — never 404.
    - Test 2 `published_false_when_all_deltas_zero`: baseline `{ '1': 50, '2': 65 }` and bootstrap elements with matching `now_cost` values. Response `published: false`, players empty (D-07).
    - Test 3 `published_true_with_sorted_deltas`: baseline `{ '1': 50, '2': 65, '3': 90 }`, bootstrap returns `now_cost` 45, 70, 90 respectively. Response `published: true`. `players.length === 2` (id=3 has zero delta and is excluded). `players[0].player_id === 2` (|+5| = 5) — wait: id=1 delta is -5, id=2 delta is +5; tie-break by absolute delta sorts deterministically. Assert `Math.abs(players[0].delta_cost) >= Math.abs(players[1].delta_cost)`. Assert exact shape of `players[0]`: `{ player_id, name, team, element_type, baseline_cost, current_cost, delta_cost }`.
    - Test 4 `value_targets_filters_fall_above_position_median`: 4 MIDs in merged_players with `xPts_1gw` values `[3.0, 4.0, 5.0, 8.0]` (median = 4.5). Of these, id=10 (`xPts_1gw=8.0`) has a fall delta (-3). id=11 (`xPts_1gw=4.0`, below median) has a fall delta (-2). Assert `value_targets.length === 1`, `value_targets[0].player_id === 10`, `value_targets[0].position_median_xPts === 4.5`, `value_targets[0].position_rank === 1`, `value_targets[0].position_label === 'MID'`, `value_targets[0].delta_cost < 0`.
    - Test 5 `value_targets_sorted_by_largest_fall_first`: two MIDs both qualifying (xPts above median), one with delta -3, one with delta -1. `value_targets[0].delta_cost === -3` (largest fall first, ascending numeric order on delta_cost — UI-SPEC).
    - Test 6 `malformed_merged_players_does_not_break_route`: baseline + bootstrap present, but merged_players read throws / returns invalid JSON. Response: `published: true` (delta still computable), `players` populated, `value_targets: []` (no xPts source). HTTP 200.
  </behavior>
  <action>
    First, edit src/lib/types.ts: append the four interfaces from the interfaces block above (`PriceResetRow`, `ValueTargetRow`, `PriceResetResponse`, plus reuse existing `PositionCode`). Add a `// Price reset analysis (Phase 133 PRST-02/03/04 — pipeline/cache/price_baseline.json)` comment header above the block. Place the new block immediately AFTER the existing `PriceChanges` interface (around line 641 in types.ts). Do not introduce a duplicate `PositionCode` — import via existing definition.

    Then create src/app/api/price-reset/route.test.ts with the 6 tests above. Use the hoist pattern: `vi.mock('fs/promises', () => ({ readFile: vi.fn() }))` and `vi.mock('@vercel/blob', () => ({ list: vi.fn() }))` BEFORE the import line `import { GET } from './route'`. Construct requests via `new NextRequest('http://localhost/api/price-reset')`. Use `beforeEach(() => vi.clearAllMocks())`. In each test, set the readFile mock return value per-file by inspecting the path argument: if path includes `'price_baseline.json'` return the baseline JSON string; if `'fpl_bootstrap.json'` return the bootstrap JSON string; if `'merged_players.json'` return the merged_players JSON string. Tests MUST fail because route.ts does not yet exist (the import statement throws). This is the RED phase.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && npx vitest run src/app/api/price-reset/route.test.ts 2>&1 | grep -E "Cannot find module|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types.ts contains `export interface PriceResetRow`, `export interface ValueTargetRow`, `export interface PriceResetResponse` (grep verifies all three).
    - `ValueTargetRow` extends `PriceResetRow` (grep `extends PriceResetRow`).
    - src/app/api/price-reset/route.test.ts exists with `// @vitest-environment node` directive at top.
    - File contains exactly 6 `it(...)` or `test(...)` blocks matching the names in <behavior>.
    - `npx vitest run src/app/api/price-reset/route.test.ts` exits non-zero with `Cannot find module './route'` or equivalent module-resolution error (RED phase confirmed).
    - `npx tsc --noEmit` succeeds on src/lib/types.ts (no TS errors introduced by the new interfaces).
    - Test file imports `import type { PriceResetResponse } from '@/lib/types'`.
  </acceptance_criteria>
  <done>Types compile; route.test.ts fails at module-resolution stage; vitest collects 6 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /api/price-reset route and usePriceReset hook to satisfy the contract</name>
  <files>src/app/api/price-reset/route.ts, src/lib/hooks/usePriceReset.ts</files>
  <read_first>
    - src/app/api/pre-season-squad/route.ts (copy readBlobOrLocal helper verbatim; same import set)
    - src/app/api/price-changes/route.ts (sibling route — same Cache-Control header, same error envelope style)
    - src/lib/hooks/usePriceChanges.ts (the hook template — copy structure, swap query key + path + type)
    - src/app/api/price-reset/route.test.ts (the contract being satisfied — read all 6 tests)
    - .planning/phases/133-price-reset-analysis/133-CONTEXT.md (D-02 baseline shape, D-05 position median, D-07 diff detection, D-08 fallback)
    - src/lib/types.ts (the interface block added in Task 1)
  </read_first>
  <action>
    Create src/app/api/price-reset/route.ts with:
    - Imports: `list` from `@vercel/blob`, `readFile` from `fs/promises`, `join` from `path`, type-only `PriceResetResponse, PriceResetRow, ValueTargetRow, PositionCode` from `@/lib/types`.
    - Constant `const ELEMENT_TYPE_LABEL: Record<PositionCode, 'GK' | 'DEF' | 'MID' | 'FWD'> = { 1: 'GK', 2: 'DEF', 3: 'MID', 4: 'FWD' }`.
    - `readBlobOrLocal(filename: string): Promise<string | null>` copied from pre-season-squad/route.ts (lines 15-34). Returns null on ENOENT or empty blob list.
    - `median(nums: number[]): number` helper — sorts ascending, returns `nums.length % 2 ? sorted[mid] : (sorted[mid-1] + sorted[mid]) / 2`. Returns NaN or 0 when array empty (route must guard).
    - `export async function GET(): Promise<Response>`:
      1. Read all three artifacts concurrently with `Promise.all([readBlobOrLocal('price_baseline.json'), readBlobOrLocal('fpl_bootstrap.json'), readBlobOrLocal('merged_players.json')])`.
      2. If baselineText is null OR bootstrapText is null → return `Response.json({ published: false, generated_at: new Date().toISOString(), players: [], value_targets: [] } satisfies PriceResetResponse, { status: 200 })` (D-08).
      3. Parse baseline as `Record<string, number>` and bootstrap as `{ elements: Array<{ id, web_name, team, element_type, now_cost }>, teams: Array<{ id, short_name }> }`. Wrap parse in try/catch — on JSON failure return the same empty `published: false` body.
      4. Build a `Map<number, string>` of team_id → short_name from `bootstrap.teams`.
      5. For each bootstrap element where `baseline[String(el.id)]` exists, compute `delta_cost = el.now_cost - baseline[String(el.id)]`. Collect rows with `delta_cost !== 0` into `players: PriceResetRow[]`. Sort descending by `Math.abs(delta_cost)`.
      6. `const published = players.length > 0`. If `!published`, return the empty body with `published: false` (D-07).
      7. Compute value_targets: parse merged_players inside its own try/catch — on failure set `valueTargets = []` and skip. Otherwise for each `element_type` in {1,2,3,4}, gather `xPts_1gw` values from merged_players for that element_type filtering out `undefined`/`null`/non-finite; compute `positionMedian = median(values)`. Build rank lookup: sort merged by `xPts_1gw` desc within each position, map id → rank (1-indexed). Iterate `players` filtering for `delta_cost < 0` AND merged-player exists AND `xPts_1gw > positionMedian` → push `ValueTargetRow` with `xPts_1gw`, `position_median_xPts: positionMedian`, `position_rank`, `position_label: ELEMENT_TYPE_LABEL[element_type]`. Sort value_targets ascending by `delta_cost` (most negative first — largest fall).
      8. Return `Response.json({ published: true, generated_at: new Date().toISOString(), players, value_targets } satisfies PriceResetResponse, { status: 200, headers: { 'Cache-Control': 'public, s-maxage=1800, stale-while-revalidate=86400' } })`.

    Wrap the whole GET body in a top-level try/catch that on unexpected exception returns `Response.json({ error: 'Failed to load price reset data' }, { status: 500 })` — matches price-changes/route.ts error envelope.

    Create src/lib/hooks/usePriceReset.ts following usePriceChanges.ts exactly: useQuery with queryKey `['price-reset']`, queryFn `fetch('/api/price-reset')` throwing on `!res.ok`, return type `PriceResetResponse`, staleTime `30 * 60 * 1000`.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && npx vitest run src/app/api/price-reset/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/api/price-reset/route.ts exists and exports `GET`.
    - File src/lib/hooks/usePriceReset.ts exists and exports `usePriceReset`.
    - `npx vitest run src/app/api/price-reset/route.test.ts` exits 0 with all 6 tests passing.
    - `grep -n "readBlobOrLocal\\|process.env.USE_BLOB" src/app/api/price-reset/route.ts` returns at least two matches (USE_BLOB env read inside readBlobOrLocal).
    - `grep -n "satisfies PriceResetResponse" src/app/api/price-reset/route.ts` returns at least one match (TS satisfies operator used on returned object literal).
    - `grep -n "queryKey: \\['price-reset'\\]" src/lib/hooks/usePriceReset.ts` returns one match.
    - `grep -n "staleTime: 30 \\* 60 \\* 1000" src/lib/hooks/usePriceReset.ts` returns one match.
    - `npx tsc --noEmit` succeeds with no errors introduced.
    - Sort behaviour verifiable: in Test 3 the first element of `players` has `Math.abs(delta_cost)` >= second element.
    - Value Targets sort verifiable: in Test 5 `value_targets[0].delta_cost < value_targets[1].delta_cost` (more negative first).
  </acceptance_criteria>
  <done>Route returns the documented envelope; all 6 vitest tests pass; usePriceReset hook compiles and matches the usePriceChanges signature.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/app/api/price-reset/route.test.ts` exits 0 (6 tests).
- `npx tsc --noEmit` exits 0.
- Manual curl `curl -s http://localhost:3000/api/price-reset` against a dev server (after pipeline run with empty baseline) returns `{"published":false, "players":[], "value_targets":[], "generated_at":...}` body with HTTP 200.
- `npx eslint src/app/api/price-reset src/lib/hooks/usePriceReset.ts` exits 0.
</verification>

<success_criteria>
- PRST-02: API exposes the data the UI needs to populate the Price Reset table.
- PRST-03: API computes Value Targets per D-04/D-05/D-06 (xPts above position median, fall only, sorted by largest fall).
- PRST-04: API returns `published: false` when baseline absent or all deltas zero — empty-state contract honoured.
- Plan 03 can consume PriceResetResponse + usePriceReset without further API changes.
</success_criteria>

<output>
After completion, create `.planning/phases/133-price-reset-analysis/133-02-SUMMARY.md` summarising:
- Exact field shapes added to src/lib/types.ts (line numbers).
- Route file paths and final test count (6/6 passing).
- usePriceReset query key + staleTime confirmation.
- Confirmation that D-07 (diff detection) and D-08 (baseline-absent fallback) are exercised by Tests 1-3.
</output>
