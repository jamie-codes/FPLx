---
phase: 133
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - src/components/price-reset/PriceResetTab.tsx
  - src/components/price-reset/PriceResetTab.test.tsx
  - src/app/page.tsx
autonomous: false
requirements:
  - PRST-02
  - PRST-03
  - PRST-04
tags:
  - ui
  - react
  - tab
must_haves:
  truths:
    - "PriceResetTab renders the loading state while usePriceReset is fetching and the error state on fetch failure"
    - "When published=false the tab renders the verbatim D-09 empty state copy 'FPL typically publishes new prices in mid-to-late July' under heading 'Prices not yet published'"
    - "When published=true the tab renders a 'Price Reset' section listing PlayerDeltaRow per element of response.players in API order and a 'Value Targets' section listing ValueTargetRow per response.value_targets in API order"
    - "Rise pills use bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200 and prepend '+'; fall pills use bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200 and prepend Unicode minus U+2212"
    - "ValueTargetRow appends '#N POS' xPts rank to the metadata line (e.g. '#3 MID') per D-06 / UI-SPEC"
    - "page.tsx SubTab union includes 'price-reset', the analyse subTabs array contains the entry positioned after 'window' and before 'price-changes' with label 'Price Reset' and mobileLabel 'Resets', and the conditional render block forwards activeSubTab==='price-reset' to PriceResetTab"
  artifacts:
    - path: "src/components/price-reset/PriceResetTab.tsx"
      provides: "Client-side React component for the Price Reset analyse sub-tab"
      contains: "export function PriceResetTab"
    - path: "src/components/price-reset/PriceResetTab.test.tsx"
      provides: "Vitest+RTL contract — loading, error, empty (published=false), populated, value-target row format"
      contains: "@vitest-environment jsdom"
    - path: "src/app/page.tsx"
      provides: "Sub-tab registration: union type, subTabs array entry, conditional render block"
      contains: "'price-reset'"
  key_links:
    - from: "src/components/price-reset/PriceResetTab.tsx"
      to: "src/lib/hooks/usePriceReset.ts"
      via: "named import"
      pattern: "import.*usePriceReset.*from '@/lib/hooks/usePriceReset'"
    - from: "src/components/price-reset/PriceResetTab.tsx"
      to: "PriceResetResponse, PriceResetRow, ValueTargetRow types"
      via: "type-only import"
      pattern: "import type.*PriceResetResponse.*from '@/lib/types'"
    - from: "src/app/page.tsx"
      to: "src/components/price-reset/PriceResetTab.tsx"
      via: "named import + conditional render"
      pattern: "import \\{ PriceResetTab \\}"
---

<objective>
Land the Phase 133 UI surface: a new PriceResetTab component that renders the three states (loading, error, published-false empty, published-true populated with Value Targets), a vitest+RTL test file pinning the row markup and copy, and the three-location edit in src/app/page.tsx that registers the 'price-reset' sub-tab in the Analyse section between 'window' and 'price-changes'.

Purpose: Without Plan 03 the API delivered in Plan 02 has no consumer. PRST-02/03/04 are user-facing requirements that only Plan 03 can satisfy. The UI must follow the UI-SPEC visual contract exactly — pill colours, Unicode minus, '#N POS' xPts rank format, mt-6 section spacing — so the checker can sign off the dimensions.

Output: One new component directory (PriceResetTab.tsx + test), three small edits to page.tsx (union member, subTabs entry, conditional render).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-price-reset-analysis/133-CONTEXT.md
@.planning/phases/133-price-reset-analysis/133-UI-SPEC.md

# Canonical UI patterns
@src/components/price-changes/PriceChangePanel.tsx
@src/app/page.tsx
</context>

<interfaces>
<!-- Contracts produced by Plan 02 that this plan consumes. -->

From src/lib/types.ts (added in Plan 02):
```typescript
export interface PriceResetRow {
  player_id: number
  name: string
  team: string                 // e.g. "ARS"
  element_type: PositionCode   // 1|2|3|4
  baseline_cost: number        // tenths of GBP 1m
  current_cost: number
  delta_cost: number           // positive = rise, negative = fall
}

export interface ValueTargetRow extends PriceResetRow {
  xPts_1gw: number
  position_median_xPts: number
  position_rank: number        // 1-indexed
  position_label: 'GK' | 'DEF' | 'MID' | 'FWD'
}

export interface PriceResetResponse {
  published: boolean
  generated_at: string
  players: PriceResetRow[]
  value_targets: ValueTargetRow[]
}
```

From src/lib/hooks/usePriceReset.ts (added in Plan 02):
```typescript
export function usePriceReset(): UseQueryResult<PriceResetResponse, Error>
// queryKey ['price-reset'], staleTime 30 minutes
```

From src/app/page.tsx (existing context the executor must edit):
- Line ~62: `export type SubTab = 'gems' | 'insights' | ... | 'price-changes' | ...` — INSERT `'price-reset'` immediately before `'price-changes'` to keep the visual ordering consistent with the subTabs array.
- Lines ~68-78: the `analyse` section's `subTabs` array. INSERT `{ id: 'price-reset' as SubTab, label: 'Price Reset', mobileLabel: 'Resets' },` between the `'window'` entry (line 76) and the `'price-changes'` entry (line 77).
- Lines ~287-300: conditional render block for non-squad activeSection. INSERT `{activeSection !== 'squad' && activeSubTab === 'price-reset' && <PriceResetTab />}` between the `'window'` render line (298) and the `'price-changes'` render line (299).
- Line ~29: import statement block. ADD `import { PriceResetTab } from '@/components/price-reset/PriceResetTab'` near the existing `PriceChangePanel` import.

Visual contract (verbatim from UI-SPEC):
- Container section: `<section className="mt-6 space-y-6" aria-label="Price reset analysis">`
- Value Targets section: `<section className="mt-6 space-y-3" aria-label="Value targets — price fell, xPts above median">`
- Section heading: `<h2 className="text-lg font-semibold">Price Reset</h2>` and `Value Targets`
- Row card: `rounded-md border border-zinc-200 dark:border-zinc-800 p-3`
- Row inner: `flex items-center justify-between gap-2`
- Player name: `text-sm font-semibold truncate`
- Metadata: `text-xs text-zinc-500 dark:text-zinc-400` containing `{team} · £{(current_cost/10).toFixed(1)}m`
- ValueTargetRow extra metadata appended: ` · #{position_rank} {position_label}`
- Pill: `text-[10px] font-semibold px-1.5 py-0.5 rounded` plus colour classes by direction
- Rise text: `+{(delta_cost/10).toFixed(1)}m` (regular '+' character, ASCII)
- Fall text: `−{(Math.abs(delta_cost)/10).toFixed(1)}m` (Unicode minus U+2212, NOT hyphen)
- Loading: `<p className="text-sm text-zinc-500 dark:text-zinc-400 text-center py-8">Loading price reset data…</p>`
- Error: `<p className="text-sm text-red-600 dark:text-red-400 py-4">Failed to load price reset data. Check the pipeline output and refresh.</p>`
- Empty-state heading: `Prices not yet published`
- Empty-state body: `FPL typically publishes new prices in mid-to-late July` (verbatim, D-09)
- Value Targets description (under heading, before list): `<p className="text-xs text-zinc-500 dark:text-zinc-400">Players whose price fell but xPts still rates above their position median</p>`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing vitest+RTL contract for PriceResetTab</name>
  <files>src/components/price-reset/PriceResetTab.test.tsx</files>
  <read_first>
    - src/components/price-changes/PriceChangePanel.tsx (existing visual reference — copy state branches and ARIA labels)
    - .planning/phases/133-price-reset-analysis/133-UI-SPEC.md (copywriting contract — every string asserted here must match verbatim)
    - .planning/phases/133-price-reset-analysis/133-CONTEXT.md (D-06 xPts rank format, D-09 empty state copy)
    - src/lib/hooks/usePriceChanges.ts (the hook shape being mocked — usePriceReset returns the same UseQueryResult shape)
  </read_first>
  <behavior>
    - Test 1 `renders_loading_state_while_fetching`: mock usePriceReset to return `{ data: undefined, isLoading: true, error: null }`. Assert `screen.getByText('Loading price reset data…')` matches.
    - Test 2 `renders_error_state_on_fetch_failure`: mock `{ data: undefined, isLoading: false, error: new Error('boom') }`. Assert `screen.getByText('Failed to load price reset data. Check the pipeline output and refresh.')` matches.
    - Test 3 `renders_empty_state_when_published_false`: mock data `{ published: false, generated_at: '2026-05-22T00:00:00Z', players: [], value_targets: [] }`. Assert heading `Prices not yet published` and body `FPL typically publishes new prices in mid-to-late July` present. Assert no element with text matching `Price Reset` heading or `Value Targets` heading is rendered.
    - Test 4 `renders_price_reset_section_with_rise_and_fall_pills`: mock data with 2 players: id=10 delta_cost=+5 (rise), id=11 delta_cost=-3 (fall). Assert heading `Price Reset` present. Assert text `+0.5m` (rise) and `−0.3m` (fall — Unicode minus) BOTH present. Query the fall pill element and assert it has class substring `bg-red-100`; query the rise pill and assert class substring `bg-green-100`. Assert section element has `aria-label="Price reset analysis"`.
    - Test 5 `renders_value_target_row_with_rank_label`: mock data with `value_targets` containing one row `{ player_id: 99, name: 'Salah', team: 'LIV', element_type: 3, baseline_cost: 130, current_cost: 120, delta_cost: -10, xPts_1gw: 8.2, position_median_xPts: 4.5, position_rank: 3, position_label: 'MID' }`. Assert text `#3 MID` is present in the rendered output (D-06 format). Assert metadata substring `LIV · £12.0m · #3 MID`. Assert the section element has `aria-label="Value targets — price fell, xPts above median"`. Assert heading `Value Targets` present.
    - Test 6 `value_targets_section_omitted_when_array_empty`: mock data published=true with `players` non-empty but `value_targets: []`. Assert NO element with text `Value Targets` is rendered (the entire subsection is skipped, not rendered with zero rows).
  </behavior>
  <action>
    Create src/components/price-reset/PriceResetTab.test.tsx with `// @vitest-environment jsdom` directive at top. Imports: `render, screen` from `@testing-library/react`, `describe, it, expect, vi, beforeEach` from `vitest`. Hoist mock for the hook: `vi.mock('@/lib/hooks/usePriceReset', () => ({ usePriceReset: vi.fn() }))`. Then `import { usePriceReset } from '@/lib/hooks/usePriceReset'` and cast to `vi.Mocked` to set return values per test. Then `import { PriceResetTab } from './PriceResetTab'`. Use `beforeEach(() => vi.clearAllMocks())`. Each `it()` block sets the mock return value, calls `render(<PriceResetTab />)`, then asserts via `screen.getByText` / `screen.queryByText` / `screen.getByRole`. Tests MUST fail with module-resolution error for `./PriceResetTab` because Task 2 has not run — this is the RED phase. For the pill class assertion in Test 4, use `screen.getByText('+0.5m').closest('span')?.className` or similar — assert `toContain('bg-green-100')`. For Test 5 metadata, use `screen.getByText(/LIV · £12\\.0m · #3 MID/)` (regex match against the assembled metadata string).
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && npx vitest run src/components/price-reset/PriceResetTab.test.tsx 2>&1 | grep -E "Cannot find module|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/price-reset/PriceResetTab.test.tsx exists with `// @vitest-environment jsdom` directive on line 1.
    - File contains exactly 6 test blocks matching the names in <behavior>.
    - `npx vitest run src/components/price-reset/PriceResetTab.test.tsx` exits non-zero with `Cannot find module './PriceResetTab'` or equivalent module-resolution error.
    - File hoists `vi.mock('@/lib/hooks/usePriceReset', ...)` BEFORE the `import { PriceResetTab }` statement.
    - Test 4 asserts both rise and fall pills via class-name substring `bg-green-100` and `bg-red-100`.
    - Test 4 asserts the fall pill text contains the Unicode minus character U+2212 (the source file must contain the literal character `−` or the actual character — grep for it).
    - Test 5 asserts the rank label format `#3 MID` (regex containing `#3 MID`).
    - Test 3 asserts the verbatim D-09 string `FPL typically publishes new prices in mid-to-late July`.
  </acceptance_criteria>
  <done>Vitest collects 6 tests; all fail at module-resolution stage; assertions match the UI-SPEC copywriting contract verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PriceResetTab component and register the 'price-reset' sub-tab in page.tsx</name>
  <files>src/components/price-reset/PriceResetTab.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/price-changes/PriceChangePanel.tsx (closest sibling — match loading/error branches and structural patterns)
    - src/components/price-reset/PriceResetTab.test.tsx (the contract being satisfied — every assertion must pass)
    - .planning/phases/133-price-reset-analysis/133-UI-SPEC.md (definitive copywriting + Tailwind class contract)
    - src/app/page.tsx (specifically lines 60-110 for the SubTab union and subTabs array, lines 287-310 for the conditional render block, line 29 for the imports)
    - .planning/phases/133-price-reset-analysis/133-CONTEXT.md (D-10 placement, D-11 labels)
    - node_modules/next/dist/docs/app-router/file-conventions.md if present (per AGENTS.md — confirm there are no Next.js 16 changes affecting client component patterns); if absent, fall back to existing PriceChangePanel.tsx as the canonical pattern.
  </read_first>
  <action>
    Create src/components/price-reset/PriceResetTab.tsx as a client component:
    - First line `'use client'`.
    - Imports: `usePriceReset` from `@/lib/hooks/usePriceReset`, type-only `PriceResetRow`, `ValueTargetRow` from `@/lib/types`.
    - Top-level constant `DELTA_PILL_RISE = 'bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200'` and `DELTA_PILL_FALL = 'bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200'`.
    - Top-level constant `MINUS = '−'` so the Unicode minus appears once and the rest of the file can reuse it.
    - `formatDeltaPounds(delta_cost: number): string`: returns `+{(delta_cost/10).toFixed(1)}m` when delta_cost > 0, otherwise `${MINUS}{(Math.abs(delta_cost)/10).toFixed(1)}m`.
    - `function PlayerDeltaRow({ row }: { row: PriceResetRow })`: renders the card structure per UI-SPEC. Card div `className="rounded-md border border-zinc-200 dark:border-zinc-800 p-3"`; inner flex container `className="flex items-center justify-between gap-2"`; left block `className="min-w-0"` with `<p className="text-sm font-semibold truncate">{row.name}</p>` and metadata `<p className="text-xs text-zinc-500 dark:text-zinc-400">{row.team} · £{(row.current_cost/10).toFixed(1)}m</p>`. Right block: `<span className={`text-[10px] font-semibold px-1.5 py-0.5 rounded shrink-0 ${row.delta_cost > 0 ? DELTA_PILL_RISE : DELTA_PILL_FALL}`}>{formatDeltaPounds(row.delta_cost)}</span>`.
    - `function ValueTargetRowView({ row }: { row: ValueTargetRow })`: identical to PlayerDeltaRow except the metadata `<p>` text is `{row.team} · £{(row.current_cost/10).toFixed(1)}m · #{row.position_rank} {row.position_label}`.
    - `export function PriceResetTab()`:
      1. Call `const { data, isLoading, error } = usePriceReset()`.
      2. If `isLoading` return the loading `<p>` verbatim per UI-SPEC.
      3. If `error` return the error `<p>` verbatim per UI-SPEC.
      4. If `!data || !data.published` render the empty-state section per UI-SPEC (heading `Prices not yet published`, body verbatim D-09).
      5. Otherwise render the populated structure: outer `<section className="mt-6 space-y-6" aria-label="Price reset analysis">`. Inside it, first sub-block `<div><h2 className="text-lg font-semibold mb-2">Price Reset</h2><div className="space-y-3">{data.players.map(p => <PlayerDeltaRow key={p.player_id} row={p} />)}</div></div>`. Then conditionally — only if `data.value_targets.length > 0` — render a second `<section className="mt-6 space-y-3" aria-label="Value targets — price fell, xPts above median">` containing heading `Value Targets`, description paragraph (per UI-SPEC), and the `data.value_targets.map(...)` list.
    - No client-side sort logic — render API order (Plan 02 guarantees sort).

    Then edit src/app/page.tsx in three locations:
    1. Add the import beside the existing PriceChangePanel import: `import { PriceResetTab } from '@/components/price-reset/PriceResetTab'`.
    2. In the `SubTab` union type (line ~62), insert `'price-reset' | ` immediately before `'price-changes'`.
    3. In the `analyse` section's `subTabs` array, insert the new entry between the `'window'` line and the `'price-changes'` line: `{ id: 'price-reset' as SubTab, label: 'Price Reset', mobileLabel: 'Resets' },`.
    4. In the non-squad conditional render block, insert `{activeSection !== 'squad' && activeSubTab === 'price-reset' && <PriceResetTab />}` immediately after the `window` render line and immediately before the `price-changes` render line.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && npx vitest run src/components/price-reset/PriceResetTab.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/price-reset/PriceResetTab.tsx exists, exports `PriceResetTab`, first line is `'use client'`.
    - `npx vitest run src/components/price-reset/PriceResetTab.test.tsx` exits 0 with all 6 tests passing.
    - `npx tsc --noEmit` exits 0 (no TS errors introduced).
    - `grep -n "'price-reset'" src/app/page.tsx` returns at least 3 matches (union member, subTabs array entry, conditional render).
    - `grep -n "label: 'Price Reset'" src/app/page.tsx` returns exactly one match.
    - `grep -n "mobileLabel: 'Resets'" src/app/page.tsx` returns exactly one match.
    - `grep -n "import { PriceResetTab }" src/app/page.tsx` returns exactly one match.
    - The `'price-reset'` subTabs entry appears between `'window'` and `'price-changes'`. Confirm with `grep -n "id: 'window'\\|id: 'price-reset'\\|id: 'price-changes'" src/app/page.tsx | head -5` showing line numbers in ascending order: window, price-reset, price-changes.
    - The conditional render line for `'price-reset'` appears between the `'window'` and `'price-changes'` render lines (same line-ordering check).
    - File src/components/price-reset/PriceResetTab.tsx contains the literal Unicode minus character or the escape `−` (grep verifies).
    - `npx eslint src/components/price-reset/ src/app/page.tsx` exits 0.
  </acceptance_criteria>
  <done>All 6 RTL tests pass; page.tsx three-location update verified by grep; TypeScript and ESLint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UI verification of Price Reset tab states</name>
  <what-built>
    PriceResetTab component is registered in page.tsx and renders all three states from the live API.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or whatever the project uses).
    2. Visit http://localhost:3000 and click into the Analyse section.
    3. Confirm a new tab labelled `Price Reset` appears in the sub-tab strip, positioned to the right of `Summer Window` and to the left of `Price Changes`. On a mobile viewport (≤640px) the tab pill shows `Resets`.
    4. Click the `Price Reset` tab. Without a price_baseline.json present (and no FPL price publish event), the empty state is expected: heading `Prices not yet published` and body `FPL typically publishes new prices in mid-to-late July`.
    5. To exercise the populated state in dev, manually write a `pipeline/cache/price_baseline.json` containing 2-3 known element ids with `now_cost` values that DIFFER from current bootstrap (e.g. lower the values by 5). Reload the tab and confirm the Price Reset section shows rows with green `+` pills (rises) and red `−` pills (falls). The Unicode minus character must render as a true minus glyph, not a hyphen.
    6. Confirm any Value Target rows (if present) include a `#N POS` metadata segment (e.g. `#3 MID`).
    7. Confirm the section ARIA labels: open dev tools, search for `aria-label="Price reset analysis"` and `aria-label="Value targets — price fell, xPts above median"`.
    8. Confirm there are no React console warnings/errors.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "fall pill renders as hyphen", "tab order wrong").</resume-signal>
  <acceptance_criteria>
    - User reports tab is correctly positioned (Window → Price Reset → Price Changes).
    - User confirms the verbatim D-09 empty-state copy.
    - User confirms rise pill is green with `+` and fall pill is red with Unicode minus.
    - User confirms `#N POS` rank format on Value Target rows.
    - No console errors reported.
  </acceptance_criteria>
  <read_first>
    - .planning/phases/133-price-reset-analysis/133-UI-SPEC.md (the visual contract being verified)
    - .planning/phases/133-price-reset-analysis/133-CONTEXT.md (D-09 empty state copy verbatim)
  </read_first>
  <done>User approves all six verification steps.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/price-reset/PriceResetTab.test.tsx` exits 0.
- `npx tsc --noEmit` exits 0 with the new page.tsx edits.
- `npx eslint src/components/price-reset src/app/page.tsx` exits 0.
- Manual UI checkpoint approved by the user.
</verification>

<success_criteria>
- PRST-02 satisfied: Price Reset tab is reachable in the Analyse section and surfaces the API payload.
- PRST-03 satisfied: rise/fall delta pills render with the required colour classes and Unicode minus; Value Targets subsection renders only fall-rows whose xPts beats the position median with `#N POS` rank.
- PRST-04 satisfied: empty state renders verbatim D-09 copy when API returns published=false.
- Autonomous=false (Task 3 is a human-verify checkpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/133-price-reset-analysis/133-03-SUMMARY.md` summarising:
- Component file path + export name.
- Test count (6/6 passing) + final line numbers of the three page.tsx edits.
- Confirmation that the `−` Unicode minus character is present in PriceResetTab.tsx source.
- Notes from the human-verify checkpoint (resume-signal payload).
</output>
