---
phase: 056-ft-engine-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/planner/PlannerTab.tsx
autonomous: false
requirements: [FTX-01]
must_haves:
  truths:
    - "When the user is unauthenticated OR myTeamData has not loaded, PlannerTab.initialFTState evaluates to { available: 1, banked: 0 } (safe default — same as today)"
    - "When the user is authenticated AND squadData.active_chip is 'wildcard' or 'freehit', initialFTState evaluates to { available: 1, banked: 0 } (chip GW resets visible FT count for the active GW)"
    - "When the user is authenticated AND no chip is active AND myTeamData.entry_history.event_transfers === 0, initialFTState evaluates to { available: 2, banked: 1 } (rolled FT correctly reflected)"
    - "When the user is authenticated AND no chip is active AND myTeamData.entry_history.event_transfers !== 0, initialFTState evaluates to { available: 1, banked: 0 }"
    - "initialFTState is reactive — when myTeamData transitions from undefined to a value, the memoised state updates and downstream consumers (handleGeneratePlan, ftStateAfterStepIndex calls at lines 110 and 207) observe the new state on next render"
  artifacts:
    - path: src/components/planner/PlannerTab.tsx
      provides: "Authenticated-aware reactive initialFTState replacing the hardcoded const"
      contains: "useMemo<FTState>"
  key_links:
    - from: "PlannerTab.initialFTState (useMemo)"
      to: "useAuthStatus().isAuthenticated, useMyTeam(...).data, useSquad(...).data"
      via: "memo deps array [isAuthenticated, myTeamData, squadData]"
      pattern: "useMemo.*\\[isAuthenticated, myTeamData, squadData\\]"
    - from: "PlannerTab.initialFTState"
      to: "generatePlan + ftStateAfterStepIndex"
      via: "passed as the FTState arg at lines ~68, ~110, ~207"
      pattern: "initialFTState"
---

<objective>
Replace the hardcoded `const initialFTState: FTState = { available: 1, banked: 0 }` in `src/components/planner/PlannerTab.tsx` (line 55) with a `useMemo` that derives the initial FT state from authenticated FPL data, mirroring the established pattern in `TransferPanel.tsx:87–92` (`derivedFtCount`). When authenticated and no chip is active, an `event_transfers === 0` reading must produce `{ available: 2, banked: 1 }` so the multi-GW planner reflects the user's real FT bank from the start of step 0.

Purpose: This is the consumer-facing half of FTX-01. The engine fix in plan 01 corrects how `computeNextFTState` advances the bank, but the multi-GW planner still seeds step 0 with `{ available: 1, banked: 0 }` regardless of the user's actual FT count — meaning an authenticated user who rolled an FT this week still sees the planner generate a plan as if they have only 1 FT. This plan closes that gap.

Output: A reactive `useMemo` that returns `FTState` with the correct authenticated-aware logic, used by `generatePlan` (line ~68) and the two `ftStateAfterStepIndex` call sites (lines ~110 and ~207). One human-verify checkpoint to confirm an authenticated planner GW1 step shows `freeTransfersAvailable: 2` when `event_transfers === 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/056-ft-engine-fix/056-CONTEXT.md
@.planning/phases/056-ft-engine-fix/056-PATTERNS.md

@src/components/planner/PlannerTab.tsx
@src/components/transfers/TransferPanel.tsx
@src/lib/types.ts

<interfaces>
<!-- Hooks already in scope at PlannerTab.tsx line 55. NO new imports required. -->

From PlannerTab.tsx (already imported and destructured at lines 29–35):
```typescript
const { isAuthenticated } = useAuthStatus()
const { data: playersData } = usePlayers()
const { data: squadData } = useSquad(teamId)              // type: SquadResponse | undefined
const { data: myTeamData } = useMyTeam(isAuthenticated)   // type: MyTeamResponse | undefined
```

From src/lib/types.ts (already imported at line 16):
```typescript
export interface FTState {
  available: number    // 1 or 2
  banked: number       // 0 or 1
}
```

From src/components/transfers/TransferPanel.tsx (lines 87–92 — pattern to mirror):
```typescript
const derivedFtCount: 1 | 2 = useMemo(() => {
  if (!isAuthenticated || !myTeamData) return 1
  const chip = squadData?.active_chip
  if (chip === 'wildcard' || chip === 'freehit') return 1
  return myTeamData.entry_history.event_transfers === 0 ? 2 : 1
}, [isAuthenticated, myTeamData, squadData])
```

`useMemo` is already imported on line 3 of PlannerTab.tsx: `import { useState, useMemo } from 'react'` — no import change required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert initialFTState from const to useMemo with authenticated derivation</name>
  <files>src/components/planner/PlannerTab.tsx</files>
  <read_first>
    - src/components/planner/PlannerTab.tsx (entire file — confirm hook ordering: useAuthStatus line 29, useSquad line 33, useMyTeam line 34; existing const at line 55; consumers at lines 68, 110, 207)
    - src/components/transfers/TransferPanel.tsx (lines 87–92 — derivedFtCount useMemo pattern to mirror)
    - .planning/phases/056-ft-engine-fix/056-PATTERNS.md (§"src/components/planner/PlannerTab.tsx — initialFTState fix")
    - .planning/phases/056-ft-engine-fix/056-CONTEXT.md (§D-06)
  </read_first>
  <action>
**Replace exact existing lines 54–55** of `src/components/planner/PlannerTab.tsx`:

```typescript
  // Conservative default FT state when exact count is unknown
  const initialFTState: FTState = { available: 1, banked: 0 }
```

with:

```typescript
  // Initial FT state — mirrors TransferPanel.derivedFtCount pattern (CONTEXT D-06).
  // Authenticated path: derive from event_transfers (0 → rolled FT → available: 2, banked: 1).
  // Active WC/FH chip GW: planner displays as if 1 FT for the current GW.
  // Unauthenticated or pre-load: safe default { available: 1, banked: 0 }.
  const initialFTState: FTState = useMemo(() => {
    if (!isAuthenticated || !myTeamData) return { available: 1, banked: 0 }
    const chip = squadData?.active_chip
    if (chip === 'wildcard' || chip === 'freehit') return { available: 1, banked: 0 }
    const available: 1 | 2 = myTeamData.entry_history.event_transfers === 0 ? 2 : 1
    const banked: 0 | 1 = available === 2 ? 1 : 0
    return { available, banked }
  }, [isAuthenticated, myTeamData, squadData])
```

**Do NOT change:**
- The `import { useState, useMemo } from 'react'` line (already present at line 3 — `useMemo` is already imported)
- The `import type { PlanResult, FTState, ... } from '@/lib/types'` line (already present at line 16)
- The three downstream usages of `initialFTState` at lines ~68 (`generatePlan(...)` arg), ~110 (`ftStateAfterStepIndex(...)` arg), and ~207 (`ftStateAfterStepIndex(...)` arg) — they accept `FTState`, which is what the useMemo returns
- The hooks at lines 29–35 (`useAuthStatus`, `usePlayers`, `useSquad`, `useMyTeam`, `useClubForm`) — all already in scope before line 55
- Any other lines, imports, or logic in the file

**Verify React Hook order:** `initialFTState` is defined AFTER the data hooks (`useAuthStatus`, `useSquad`, `useMyTeam`) and AFTER the existing `useMemo` for `scoredPlayers` (line 38). Adding another `useMemo` at line 55 maintains the unconditional, top-level call order — Rules of Hooks satisfied.

**Run TypeScript and tests:**
- `npx tsc --noEmit` must exit 0 (no type errors — the useMemo return type matches `FTState`)
- `npx vitest run` must exit 0 (no test regressions — there is no existing PlannerTab unit test for this const, but planning-engine and free-transfer-engine tests must still pass)

Commit message: `fix(056-02): derive PlannerTab initialFTState from authenticated state (FTX-01)`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tee /tmp/056-02-tsc.log; ! grep -E "error TS" /tmp/056-02-tsc.log; npx vitest run 2>&1 | tee /tmp/056-02-vitest.log; grep -E "Tests +[0-9]+ passed" /tmp/056-02-vitest.log && ! grep -E "[0-9]+ failed" /tmp/056-02-vitest.log; grep -c "useMemo<FTState>\\|initialFTState: FTState = useMemo" src/components/planner/PlannerTab.tsx | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - The exact string `const initialFTState: FTState = { available: 1, banked: 0 }` is GONE — `grep -F "const initialFTState: FTState = { available: 1, banked: 0 }" src/components/planner/PlannerTab.tsx` returns 0 matches
    - `grep -c "const initialFTState: FTState = useMemo" src/components/planner/PlannerTab.tsx` returns exactly 1
    - The dependency array contains exactly `[isAuthenticated, myTeamData, squadData]` — `grep -F "[isAuthenticated, myTeamData, squadData]" src/components/planner/PlannerTab.tsx` returns at least 1 match
    - `grep -c "myTeamData.entry_history.event_transfers === 0 ? 2 : 1" src/components/planner/PlannerTab.tsx` returns exactly 1
    - `grep -c "chip === 'wildcard' || chip === 'freehit'" src/components/planner/PlannerTab.tsx` returns exactly 1 (new occurrence in initialFTState; this string is unique to the new memo because the existing handleChipToggle uses a different surrounding structure)
    - `grep -c "CONTEXT D-06\\|D-06" src/components/planner/PlannerTab.tsx` returns at least 1 (decision traceability comment)
    - The three downstream consumers still pass `initialFTState` as their FTState argument — `grep -c "initialFTState" src/components/planner/PlannerTab.tsx` returns at least 4 (1 declaration + 3 consumer references at lines ~68, ~110, ~207)
    - `npx tsc --noEmit` exits 0 with no TS errors mentioning PlannerTab.tsx
    - `npx vitest run` exits 0 with all tests passing
    - `import { useState, useMemo } from 'react'` line is unchanged at line 3 (no duplicate or altered import) — `grep -c "import { useState, useMemo } from 'react'" src/components/planner/PlannerTab.tsx` returns exactly 1
  </acceptance_criteria>
  <done>The const is replaced with a `useMemo<FTState>` that returns `{ available: 2, banked: 1 }` when authenticated with `event_transfers === 0` and no active chip, otherwise the safe `{ available: 1, banked: 0 }` default. Type check passes, full vitest suite passes, no other lines in the file are altered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify authenticated FT state propagates to GW1 plan step</name>
  <read_first>
    - src/components/planner/PlannerTab.tsx (verify Task 1 changes are in place: useMemo at line ~55, dependency array contains [isAuthenticated, myTeamData, squadData])
    - src/components/transfers/TransferPlanTable.tsx (column that renders freeTransfersAvailable — confirm where the GW1 FT cell is shown so the human knows what to look at)
    - .planning/phases/056-ft-engine-fix/056-CONTEXT.md (§D-06 — authenticated derivation rule)
  </read_first>
  <acceptance_criteria>
    - Authenticated path with event_transfers === 0 and no active chip: GW1 row of TransferPlanTable shows freeTransfersAvailable = 2 (visible as the FT count cell — exact location depends on the column header)
    - Authenticated path: a 2-transfer GW1 step computes hitCost = 0 (no -4 penalty) — verify by clicking a 2-transfer scenario or inspecting React DevTools state
    - Unauthenticated path: GW1 row shows freeTransfersAvailable = 1 (safe default preserved)
    - No console errors or React Hook order warnings appear in the browser console at any point during the test sessions
    - The user explicitly types "approved" as the resume signal — anything else is treated as a failure and a follow-up plan is required
  </acceptance_criteria>
  <what-built>
    PlannerTab `initialFTState` is now reactive: when an authenticated user has rolled their FT (`event_transfers === 0`) with no active chip, the multi-GW planner seeds step 0 with `{ available: 2, banked: 1 }` instead of the previous hardcoded `{ available: 1, banked: 0 }`. This affects the FT badge on GW1 in TransferPlanTable and the hit calculation if the user takes 2 transfers.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Sign in to the app via the FPL authentication flow (the existing login button) — required so `useMyTeam` returns real data with `entry_history.event_transfers`
    3. Confirm the precondition: in the browser DevTools, open Network → find the `/api/fpl/my-team/...` response → confirm `entry_history.event_transfers === 0` (i.e. the user has not made a transfer this GW). If your test account has already used a transfer this GW, you cannot verify the rolled-FT path with this method — see fallback below.
    4. Navigate to the Planner section
    5. Click "Generate Plan" with the default 3-GW horizon
    6. Inspect the rendered TransferPlanTable — the GW1 row's "FT" cell (or whatever column displays `freeTransfersAvailable`) MUST show 2 (NOT 1)
    7. If a hit is taken at GW1 (e.g. 2 transfers), the hit cost MUST be 0 (because 2 FTs are available) — NOT -4
    8. Negative-path check: navigate to the Planner section while signed OUT (or with `useAuthStatus` returning `isAuthenticated: false` — clear cookies if needed). Generate a plan. GW1 FT cell MUST show 1 (the safe default), confirming the unauthenticated branch still works.
    9. Active-chip check (optional, requires a synthetic squad with `active_chip === 'wildcard'`): if reachable, confirm GW1 FT cell shows 1 even when authenticated.

    **Fallback if `event_transfers !== 0` for your account:** the unit-level fix is provable by a temporary local edit — open browser DevTools Sources, set a breakpoint inside the new `useMemo` callback, and observe `available` evaluate to 2 when you mock `myTeamData.entry_history.event_transfers = 0` via console; revert before continuing. This is a sanity check only — the production path requires a real `event_transfers === 0` state.
  </how-to-verify>
  <resume-signal>Type "approved" if GW1 step shows the correct FT count for both authenticated-rolled and unauthenticated states. Type a description of the issue if anything else (wrong count, broken planner UI, type/runtime error) appears.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FPL my-team API → client | `myTeamData.entry_history.event_transfers` is a number sourced from the authenticated FPL my-team endpoint via the existing `/api/fpl/[...proxy]` route. The proxy is a pre-existing trust boundary that has been hardened in earlier phases (auth cookie validation, response shape typed). This plan only consumes a single integer field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-056-07 | Tampering | myTeamData.entry_history.event_transfers | accept | TypeScript types narrow this to `number`. Worst-case malformed value (e.g. NaN, negative) flows through `=== 0 ? 2 : 1` which evaluates falsy/non-zero deterministically. The downstream `FTState.available` is then either 1 or 2 — both safe values for `computeNextFTState`. |
| T-056-08 | Spoofing | useAuthStatus().isAuthenticated | accept | Pre-existing auth cookie check from Phase 12. This plan only reads the boolean — it does not implement authentication. |
| T-056-09 | Information Disclosure | initialFTState | accept | The two-integer FTState contains no PII. It is computed and consumed entirely in client memory. |
| T-056-10 | Denial of Service | useMemo recomputation | accept | Recomputes only when `[isAuthenticated, myTeamData, squadData]` change references. All three are React-Query–cached objects with stable references between fetches. No O(n) work inside the memo body. |
| T-056-11 | Repudiation | n/a | accept | No audit logging requirement for client-side display logic; ASVS L1 V7 not applicable. |
| T-056-12 | Elevation of Privilege | n/a | accept | No authorisation surface. The memo is read-only and produces a value that flows into the existing planner pipeline; no privilege boundary crosses this code. |
</threat_model>

<verification>
**Per-task automated verification (defined in tasks above):**
- Task 1: `npx tsc --noEmit` exits 0; `npx vitest run` exits 0; required grep matches present and old const grep returns 0.
- Task 2: human checkpoint — must observe GW1 FT cell behaviour for authenticated-rolled and unauthenticated paths.

**Plan-level verification:**
- `grep -c "const initialFTState: FTState = useMemo" src/components/planner/PlannerTab.tsx` returns exactly 1
- `grep -F "const initialFTState: FTState = { available: 1, banked: 0 }" src/components/planner/PlannerTab.tsx` returns 0
- No file outside `src/components/planner/PlannerTab.tsx` is modified by this plan
- `npx vitest run` and `npx tsc --noEmit` both exit 0
</verification>

<success_criteria>
1. `initialFTState` is now a `useMemo` with the dependency array `[isAuthenticated, myTeamData, squadData]` and the four-branch logic (unauthenticated → default; chip WC/FH → 1+0; authenticated rolled → 2+1; authenticated used → 1+0).
2. No new imports are required (all hooks and types already in scope).
3. The three existing consumers (`generatePlan` arg, two `ftStateAfterStepIndex` args) accept the memoised value without modification.
4. Authenticated user with `event_transfers === 0` sees GW1 FT count = 2 in the planner (verified by human checkpoint).
5. Unauthenticated user still sees GW1 FT count = 1 (safe default preserved).
6. Type check and full test suite pass.
</success_criteria>

<output>
After completion, create `.planning/phases/056-ft-engine-fix/056-02-SUMMARY.md` documenting:
- Before/after diff snippet of lines 54–55 (const → useMemo)
- Confirmation that no new imports were required
- The three downstream consumer line numbers (~68, ~110, ~207) verified to consume the memoised value unchanged
- Human-verify outcome: authenticated rolled-FT path produces `freeTransfersAvailable: 2` at GW1; unauthenticated path produces 1
- Final tsc and vitest output
- Confirmation that FTX-01 (initial FT state correctness for the planner) is fully addressed by this plan
</output>
