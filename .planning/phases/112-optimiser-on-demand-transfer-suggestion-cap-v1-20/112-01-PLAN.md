---
phase: 112
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/cap-transfer-suggestions.ts
  - src/lib/cap-transfer-suggestions.test.ts
autonomous: true
requirements:
  - TFR-02
tags: [fpl, transfers, utility, pure-function, tdd]

must_haves:
  truths:
    - "A pure function capByPosition exists in src/lib/cap-transfer-suggestions.ts that buckets TransferSuggestion[] by element_type and keeps the top `limit` per bucket"
    - "Single-kind suggestions are bucketed by sug.buy.element_type"
    - "Combo-kind suggestions are bucketed by sug.transfers[0].buy.element_type"
    - "Input order (xPtsGain desc, cost asc) is preserved inside each bucket — slice(0, limit) is the only truncation"
    - "Return shape includes totalsByPosition Map<number, number> recording the PRE-cap count per element_type so the UI can detect which groups were truncated"
    - "Final suggestions[] is re-sorted across buckets by xPtsGain desc, cost asc (to preserve cross-bucket ordering in the rendered list)"
    - "Empty input returns empty suggestions and empty totalsByPosition"
    - "When every bucket has length ≤ limit, suggestions is identical (same length, same order) to input"
  artifacts:
    - path: "src/lib/cap-transfer-suggestions.ts"
      provides: "capByPosition pure function + CappedSuggestions type"
      contains: "export function capByPosition"
    - path: "src/lib/cap-transfer-suggestions.test.ts"
      provides: "vitest @vitest-environment node unit tests covering single, combo, mixed, empty, exact-limit, over-limit, sort-preservation"
      contains: "describe('Phase 112 (TFR-02): capByPosition'"
  key_links:
    - from: "src/lib/cap-transfer-suggestions.ts"
      to: "src/lib/types.ts (TransferSuggestion)"
      via: "import type { TransferSuggestion }"
      pattern: "import type \\{ TransferSuggestion \\} from './types'"
---

<objective>
Create the pure `capByPosition` utility (per CONTEXT.md D-04, D-05) and its unit tests. This is the foundational TFR-02 building block — both `OptimiserPanel` (Plan 02) and `TransferPanel` (Plan 03) will consume it inside their existing `useMemo` blocks. RED→GREEN TDD: write tests first, then the minimal pure function to make them pass.

Purpose: Decouple the cap logic from React, keep `suggest-transfers.ts` engine untouched (D-05), and provide a `totalsByPosition` map so consumers can render the truncation footnote (D-07) without re-counting.

Output: One new pure-function file with `capByPosition(suggestions, limit) → { suggestions, totalsByPosition }`, fully unit-tested in node environment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-CONTEXT.md
@.planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-PATTERNS.md
@.planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-UI-SPEC.md

<interfaces>
<!-- TransferSuggestion discriminated union — verified in src/lib/types.ts lines 245–265. Read-only reference. -->
From src/lib/types.ts:
```typescript
export type TransferSuggestion =
  | {
      kind: 'single'
      sell: MergedPlayer
      buy: MergedPlayer            // buy.element_type = position bucket key
      cost: 0 | 4
      xPtsGain: number             // always > 0 (filtered by engine)
      xPtsGainPerGw: number
      breakEvenGws: number | null
    }
  | {
      kind: 'combo'
      transfers: [
        { sell: MergedPlayer; buy: MergedPlayer },   // transfers[0].buy.element_type = bucket key
        { sell: MergedPlayer; buy: MergedPlayer }
      ]
      cost: 0 | 4 | 8
      xPtsGain: number
      xPtsGainPerGw: number
      breakEvenGws: number | null
    }
```

<!-- Pure-function file convention (verified in src/lib/eo-candidates.ts lines 1–10). -->
File-header convention:
- No 'use client' directive
- No React imports
- Import type from './types' using `import type`
- Phase-tagged header comment
- @vitest-environment node for the test file (mirrors src/lib/eo-candidates.test.ts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing unit tests for capByPosition in src/lib/cap-transfer-suggestions.test.ts</name>
  <files>src/lib/cap-transfer-suggestions.test.ts</files>
  <read_first>
    - src/lib/eo-candidates.test.ts (full file — header comment style, `@vitest-environment node`, factory helper `makePlayer(overrides)`, `describe`/`it` shape)
    - src/lib/types.ts lines 245–265 (TransferSuggestion discriminated union — both `single` and `combo` shapes, all required fields)
    - .planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-PATTERNS.md (section "src/lib/cap-transfer-suggestions.ts (new utility, transform)" — full utility structure including return type CappedSuggestions and key extraction logic for single vs combo)
    - .planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-CONTEXT.md (D-04 element_type buckets, D-05 cap location)
  </read_first>
  <behavior>
    Eight test cases, all initially FAILING because the source file does not yet exist (the test import resolves to a missing module).

    Test 1 — "returns empty result when given an empty array": input `[]` → returns `{ suggestions: [], totalsByPosition: new Map() }` (size 0).

    Test 2 — "buckets single-kind suggestions by buy.element_type and caps at limit": input has 4 MID singles (element_type=3) + 2 DEF singles (element_type=2), all distinct xPtsGain. Call `capByPosition(input, 3)`. Assert `suggestions.length === 5` (3 MID kept + 2 DEF kept), assert `totalsByPosition.get(3) === 4`, `totalsByPosition.get(2) === 2`, `totalsByPosition.has(1) === false`, `totalsByPosition.has(4) === false`.

    Test 3 — "buckets combo-kind suggestions by transfers[0].buy.element_type": input has 5 combo suggestions whose `transfers[0].buy.element_type === 4` (FWD). Call `capByPosition(input, 3)`. Assert `suggestions.length === 3`, `totalsByPosition.get(4) === 5`. (Engine guarantees both legs same position per Phase 111 FIX-02; only transfers[0] is read.)

    Test 4 — "preserves input order within a bucket — top-3 are the input's first 3 elements when input is already sorted desc by xPtsGain": input is 4 MID singles with `xPtsGain` values [10.0, 8.0, 6.0, 4.0]. After `capByPosition(input, 3)`, the returned `suggestions` (filtered to MID via `s.kind === 'single' && s.buy.element_type === 3`) has xPtsGain values exactly `[10.0, 8.0, 6.0]` in that order — `4.0` is dropped.

    Test 5 — "when every bucket has length ≤ limit, output suggestions length equals input length": input has 2 GK, 3 DEF, 1 MID, 2 FWD (all single, all distinct xPtsGain). Call `capByPosition(input, 3)`. Assert `suggestions.length === 8` (= input.length). Assert no bucket lost any entry: for each of element_type 1/2/3/4, count of that type in suggestions equals count in input.

    Test 6 — "mixed singles and combos in the same bucket are both counted toward the cap": input has 2 single MIDs (element_type=3) AND 2 combo MIDs (transfers[0].buy.element_type=3), each with distinct xPtsGain. With limit=3, exactly 3 MIDs are kept and `totalsByPosition.get(3) === 4`. The kept ones are the top-3 by xPtsGain regardless of kind (assuming input is presorted xPtsGain desc).

    Test 7 — "output suggestions are sorted across buckets by xPtsGain desc, tie-broken by cost asc": input is a mix where xPtsGain values are interleaved across buckets (e.g., MID=12, DEF=11, MID=10, FWD=9, DEF=8). After capByPosition(input, 3), the returned `suggestions` array is sorted by xPtsGain desc; if two entries tie, the lower-cost one comes first. Assert the returned `xPtsGain` sequence is monotonically non-increasing.

    Test 8 — "limit=0 yields zero kept suggestions but totalsByPosition still reflects pre-cap counts": input has 5 MID singles. Call `capByPosition(input, 0)`. Assert `suggestions.length === 0`, `totalsByPosition.get(3) === 5`.

    All 8 tests MUST fail with module-not-found error because src/lib/cap-transfer-suggestions.ts does not exist yet.
  </behavior>
  <action>
    1. Create `src/lib/cap-transfer-suggestions.test.ts`. Header comment mirrors `src/lib/eo-candidates.test.ts` lines 1–3 in style: `// Phase 112 (TFR-02): capByPosition — pure-function unit tests.` Add `// @vitest-environment node` on the second line. Import: `import { describe, it, expect } from 'vitest'`, `import { capByPosition } from './cap-transfer-suggestions'`, `import type { TransferSuggestion, MergedPlayer } from './types'`.
    2. Define two factory helpers at module top:
       - `makeMergedPlayer(id: number, element_type: 1 | 2 | 3 | 4): MergedPlayer` — return a minimal-but-valid MergedPlayer literal. Mirror `src/lib/eo-candidates.test.ts` lines 12–60: every required MergedPlayer field present, sensible defaults (web_name: `P${id}`, team: 1, team_short_name: 'T1', now_cost: 50, status: 'a', xPts_1gw: 5, xPts_3gw: 15, xPts_5gw: 25, etc.). The full required field list is dictated by the MergedPlayer interface in src/lib/types.ts — read that interface and copy every required field.
       - `singleSug(buyId: number, element_type: 1 | 2 | 3 | 4, xPtsGain: number, cost: 0 | 4 = 0): TransferSuggestion` — return `{ kind: 'single', sell: makeMergedPlayer(buyId * 100, element_type), buy: makeMergedPlayer(buyId, element_type), cost, xPtsGain, xPtsGainPerGw: xPtsGain, breakEvenGws: cost === 0 ? null : Math.max(1, Math.ceil(4 / xPtsGain)) }`.
       - `comboSug(buy1Id: number, element_type: 1 | 2 | 3 | 4, xPtsGain: number, cost: 0 | 4 | 8 = 0): TransferSuggestion` — return `{ kind: 'combo', transfers: [{ sell: makeMergedPlayer(buy1Id * 100, element_type), buy: makeMergedPlayer(buy1Id, element_type) }, { sell: makeMergedPlayer(buy1Id * 200, element_type), buy: makeMergedPlayer(buy1Id + 1, element_type) }], cost, xPtsGain, xPtsGainPerGw: xPtsGain, breakEvenGws: cost === 0 ? null : Math.max(1, Math.ceil(cost / xPtsGain)) }`.
    3. Write the 8 `it(...)` blocks inside a single top-level `describe('Phase 112 (TFR-02): capByPosition', () => { ... })`. Use the exact `it(...)` names and assertions described in `<behavior>` above. For each test, sort inputs by xPtsGain desc before passing to capByPosition (mirrors engine guarantee) UNLESS the test explicitly probes cross-bucket re-sort (Test 7).
    4. Run `npx vitest run src/lib/cap-transfer-suggestions.test.ts` — all 8 tests MUST fail with `Cannot find module './cap-transfer-suggestions'` (or equivalent module-resolution error).
    5. Commit with message `test(112-01): add failing tests for capByPosition utility`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cap-transfer-suggestions.test.ts 2>&1 | grep -E "FAIL|failed|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/cap-transfer-suggestions.test.ts` exists
    - File contains the literal string `@vitest-environment node`
    - File contains the literal string `describe('Phase 112 (TFR-02): capByPosition`
    - File contains exactly 8 `it(` blocks within that describe (grep `it\(` count = 8, excluding comment lines)
    - File contains both factory helpers: `singleSug` and `comboSug` named exports or const declarations
    - `npx vitest run src/lib/cap-transfer-suggestions.test.ts` exits non-zero with module-not-found / import error (RED state)
    - Git commit exists with subject line starting `test(112-01):`
  </acceptance_criteria>
  <done>Test file committed; 8 tests fail because the source module does not yet exist. RED phase complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement capByPosition in src/lib/cap-transfer-suggestions.ts</name>
  <files>src/lib/cap-transfer-suggestions.ts</files>
  <read_first>
    - src/lib/cap-transfer-suggestions.test.ts (the 8 tests written in Task 1 — implementation must satisfy each assertion exactly: shape `{ suggestions, totalsByPosition }`, key extraction `single → sug.buy.element_type` and `combo → sug.transfers[0].buy.element_type`, slice(0, limit) per bucket, totalsByPosition pre-cap counts, final cross-bucket sort by xPtsGain desc then cost asc)
    - src/lib/eo-candidates.ts (full file — verified pure-function header convention, no 'use client', `import type`, single named export with JSDoc)
    - src/lib/types.ts lines 245–265 (TransferSuggestion definition; engine invariants comment above it confirms xPtsGain > 0 always)
    - .planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-PATTERNS.md (section "Full utility structure to implement" — the verified implementation skeleton)
    - .planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    After this task all 8 tests from Task 1 PASS. The module exports:
    - `interface CappedSuggestions { suggestions: TransferSuggestion[]; totalsByPosition: Map<number, number> }`
    - `function capByPosition(suggestions: TransferSuggestion[], limit: number): CappedSuggestions`

    Implementation steps inside the function:
    1. Build a bucket map `byPosition: Map<number, TransferSuggestion[]>`. For each input suggestion, derive `pos = sug.kind === 'single' ? sug.buy.element_type : sug.transfers[0].buy.element_type`. Push into bucket. Input order is preserved within each bucket because Map iteration order matches insertion order and bucket.push appends.
    2. Build `totalsByPosition: Map<number, number>` recording each bucket's length BEFORE slicing.
    3. Build `capped: TransferSuggestion[]` by concatenating `bucket.slice(0, limit)` for each bucket.
    4. Sort `capped` in place by `(b.xPtsGain - a.xPtsGain)`; tie-break by `(a.cost - b.cost)`.
    5. Return `{ suggestions: capped, totalsByPosition }`.

    No 'use client', no React, no console output in the happy path. Single named export plus a single interface export.
  </behavior>
  <action>
    1. Create `src/lib/cap-transfer-suggestions.ts`. Header comment (3 lines): `// Phase 112 (TFR-02): capByPosition — pure post-filter for suggestTransfers output.` / `// Caps buy candidates at \`limit\` per element_type bucket (GK=1, DEF=2, MID=3, FWD=4).` / `// Pure function — no React, no side effects, testable in @vitest-environment node.`
    2. Add import: `import type { TransferSuggestion } from './types'`. No other imports.
    3. Export interface `CappedSuggestions` with two fields: `suggestions: TransferSuggestion[]` and `totalsByPosition: Map<number, number>` (with a JSDoc one-liner above totalsByPosition: `/** Maps element_type → total count BEFORE cap (for footnote rendering). */`).
    4. Add a JSDoc block above the exported function listing parameter semantics and return shape. Then export `function capByPosition(suggestions: TransferSuggestion[], limit: number): CappedSuggestions`. Implementation follows the 5-step recipe in `<behavior>` — use `for...of` over the input, derive `pos` via the discriminated-union check, populate the bucket map, then build the result. Use the established `for (const [pos, bucket] of byPosition)` Map iteration pattern (see `src/lib/eo-candidates.ts` for parallel `Map` usage if helpful).
    5. Run `npx vitest run src/lib/cap-transfer-suggestions.test.ts` — all 8 tests must pass.
    6. Run `npx vitest run` to confirm the full project suite is still green (no regression in any of the ~30 existing test files).
    7. Run `npx tsc --noEmit` to confirm no type errors.
    8. Commit with message `feat(112-01): implement capByPosition pure utility (TFR-02 D-05)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/cap-transfer-suggestions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/cap-transfer-suggestions.ts` exists
    - File contains the literal string `export function capByPosition`
    - File contains the literal string `export interface CappedSuggestions`
    - File contains the literal string `totalsByPosition`
    - File contains the literal string `sug.transfers[0].buy.element_type` (or equivalent destructured form that proves combo bucketing reads transfers[0])
    - File contains the literal string `import type { TransferSuggestion }` (verifies import-type usage, no runtime React/types import)
    - File does NOT contain the string `'use client'`
    - File does NOT contain the string `from 'react'`
    - `npx vitest run src/lib/cap-transfer-suggestions.test.ts` exits 0 with all 8 tests passing
    - `npx vitest run` exits 0 (full project suite green; no regression)
    - `npx tsc --noEmit` exits 0
    - Git commit exists with subject line starting `feat(112-01):`
  </acceptance_criteria>
  <done>capByPosition pure utility shipped with full test coverage. Plan 02 (OptimiserPanel) and Plan 03 (TransferPanel) can now import it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suggestTransfers engine output → capByPosition input | TransferSuggestion[] flows from the engine (server-side data → client memo) into a pure function. Engine is the trust authority; capByPosition does not validate types — it relies on TypeScript discriminated-union narrowing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | capByPosition assumes engine-guaranteed shape (TransferSuggestion union, xPtsGain > 0, combo legs same element_type per FIX-02 D-08) | accept | Engine is in-repo trust authority (src/lib/suggest-transfers.ts); discriminated union narrowing enforced at compile time via `sug.kind === 'single'` guard. No runtime input from network or user reaches this function. |
| T-112-02 | Denial of Service | Pathological input could be a million suggestions, hitting O(n log n) sort cost | accept | suggestTransfers caps internal pools at top-30 per position before producing candidates; realistic input is < 200 entries. No accept-from-network path for this function. |
| T-112-03 | Information Disclosure | totalsByPosition Map reveals pre-cap counts of suggestions | accept | Counts are derived from public FPL data (no PII); rendering them in the UI footnote is the stated requirement (D-07). |
</threat_model>

<verification>
- `npx vitest run src/lib/cap-transfer-suggestions.test.ts` — 8 tests green
- `npx vitest run` — full project suite green (no regression)
- `npx tsc --noEmit` — no type errors
- Two commits land in TDD RED→GREEN order: `test(112-01): ...` then `feat(112-01): ...`
</verification>

<success_criteria>
- src/lib/cap-transfer-suggestions.ts exports capByPosition and CappedSuggestions type
- 8 unit tests cover empty, single, combo, mixed, sort-preservation, limit-edge cases, and totalsByPosition correctness
- Engine file src/lib/suggest-transfers.ts is UNTOUCHED (D-05 invariant)
- Full project test suite stays green
- Plan 02 and Plan 03 can now import { capByPosition, CappedSuggestions } from '@/lib/cap-transfer-suggestions'
</success_criteria>

<output>
After completion, create `.planning/phases/112-optimiser-on-demand-transfer-suggestion-cap-v1-20/112-01-SUMMARY.md` documenting:
- Files created (cap-transfer-suggestions.ts + cap-transfer-suggestions.test.ts), line counts
- The 8 test names (one-line each) and the implementation steps in capByPosition
- Confirmation that suggest-transfers.ts engine was NOT modified (D-05)
- Note for downstream plans (02 + 03): import `{ capByPosition, CappedSuggestions } from '@/lib/cap-transfer-suggestions'`; signature is `(suggestions, limit) → { suggestions, totalsByPosition }`
</output>
