---
phase: 65
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/rejection.test.ts
  - src/components/transfers/HighOwnershipCallout.test.tsx
  - src/components/squad/ExplainPanel.test.tsx
autonomous: true
requirements: [WHY-01, WHY-02, WHY-03]
tags: [rejection-explainer, tdd-wave-0, vitest, rtl]

must_haves:
  truths:
    - "Three RED test files exist that fully describe the contracts for computeRejection (WHY-01), HighOwnershipCallout (WHY-02), and ExplainPanel.rejectionReasons (WHY-03)"
    - "All three test files exit non-zero when run, because the code-under-test does not yet exist (computeRejection unexported, HighOwnershipCallout file missing, rejectionReasons prop absent)"
    - "Test fixtures use the same makePlayer factory shape as src/lib/__tests__/sensitivity.test.ts (identical field defaults), with gem_score added so cast to ScoredPlayer is valid"
  artifacts:
    - path: "src/lib/__tests__/rejection.test.ts"
      provides: "WHY-01 RED unit-test contract for computeRejection (rank, positive framing, fragility delegation, ownership formatting, BGW guard)"
      min_lines: 120
      contains: "computeRejection"
    - path: "src/components/transfers/HighOwnershipCallout.test.tsx"
      provides: "WHY-02 RED RTL contract for HighOwnershipCallout component"
      min_lines: 60
      contains: "HighOwnershipCallout"
    - path: "src/components/squad/ExplainPanel.test.tsx"
      provides: "WHY-03 RED RTL contract for ExplainPanel.rejectionReasons prop"
      min_lines: 60
      contains: "rejectionReasons"
  key_links:
    - from: "src/lib/__tests__/rejection.test.ts"
      to: "src/lib/explain.ts (computeRejection symbol — does not exist yet)"
      via: "import { computeRejection } from '../explain'"
      pattern: "from '\\.\\./explain'"
    - from: "src/components/transfers/HighOwnershipCallout.test.tsx"
      to: "src/components/transfers/HighOwnershipCallout.tsx (file does not exist yet)"
      via: "import { HighOwnershipCallout } from './HighOwnershipCallout'"
      pattern: "from './HighOwnershipCallout'"
    - from: "src/components/squad/ExplainPanel.test.tsx"
      to: "src/components/squad/ExplainPanel.tsx (rejectionReasons prop does not exist yet)"
      via: "<ExplainPanel rejectionReasons={[...]} />"
      pattern: "rejectionReasons=\\{"
---

<objective>
Create the Wave 0 RED test stubs for Phase 65 — Rejection Explainer. These three test files codify the WHY-01, WHY-02, and WHY-03 contracts and MUST exit non-zero before any implementation begins. Wave 1 plans (02 and 03) implement code that turns each of these test files GREEN.

Purpose: Per VALIDATION.md and Nyquist requirement, every implementation task in Wave 1 must have an `<automated>` verify command that runs against an existing test file. This plan ships those test files first so executors of plans 02 and 03 have an immediate green/red signal during implementation.

Output: Three new test files committed to disk. Running `npx vitest run src/lib/__tests__/rejection.test.ts src/components/transfers/HighOwnershipCallout.test.tsx src/components/squad/ExplainPanel.test.tsx` reports failures (compile errors and/or runtime failures) — this is the expected RED state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/065-rejection-explainer/065-CONTEXT.md
@.planning/phases/065-rejection-explainer/065-RESEARCH.md
@.planning/phases/065-rejection-explainer/065-PATTERNS.md
@.planning/phases/065-rejection-explainer/065-UI-SPEC.md
@.planning/phases/065-rejection-explainer/065-VALIDATION.md
@src/lib/__tests__/sensitivity.test.ts
@src/components/shared/FragilityNote.test.tsx
@src/components/squad/ProseSummaryBlock.test.tsx
@src/components/squad/ExplainPanel.tsx
@src/lib/explain.ts
@src/lib/sensitivity.ts
@src/lib/recommend.ts
@src/lib/types.ts

<interfaces>
<!-- Symbols Wave 1 plans 02 and 03 must implement to satisfy these tests. -->

From src/lib/explain.ts (Plan 02 will add):
```typescript
export const REJECTION_START_PROB_THRESHOLD = 0.70
export const REJECTION_OWNERSHIP_THRESHOLD = 20.0

export interface RejectionResult {
  reasons: string[]   // empty => positive framing
  xPtsRank: number    // 1-based rank within position by xPts_1gw
}

export function computeRejection(
  player: ScoredPlayer,
  allPlayers: ScoredPlayer[],
): RejectionResult
```

From src/components/transfers/HighOwnershipCallout.tsx (Plan 03 will add):
```typescript
interface HighOwnershipEntry {
  player: ScoredPlayer
  inSquad: boolean
  squadRank?: number
  posCode: string
}
interface HighOwnershipCalloutProps {
  entries: HighOwnershipEntry[]
}
export function HighOwnershipCallout({ entries }: HighOwnershipCalloutProps): JSX.Element | null
```

From src/components/squad/ExplainPanel.tsx (Plan 03 will modify):
```typescript
interface ExplainPanelProps {
  reasons: string[]
  shortlist: ShortlistEntry[] | null
  rejectionReasons?: string[]   // Phase 65 addition
}
```

Existing dependencies the tests import:
- `computeFragility(player: MergedPlayer, isTransfer: boolean, xPtsGain?: number): FragilityResult` from src/lib/sensitivity.ts
- `computePositionAverages(allPlayers: ScoredPlayer[]): Map<number, number>` from src/lib/recommend.ts
- `MergedPlayer`, `ScoredPlayer`, `FixtureEntry` types from src/lib/types.ts (ScoredPlayer extends MergedPlayer adds gem_score and other fields)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create rejection.test.ts (WHY-01 unit contract)</name>
  <files>src/lib/__tests__/rejection.test.ts</files>
  <read_first>
    - src/lib/__tests__/sensitivity.test.ts (verbatim model — copy makePlayer factory and fixture objects; same `// @vitest-environment node` header)
    - src/lib/explain.ts (current state — confirms computeRejection NOT exported yet; this is the RED state)
    - src/lib/sensitivity.ts (FragilityResult shape — tests will rely on computeRejection delegating to computeFragility(player, false))
    - src/lib/recommend.ts (computePositionAverages return shape — Map<number, number>)
    - src/lib/types.ts lines 94-300 (MergedPlayer + ScoredPlayer interfaces; FixtureEntry shape used in fixtures)
    - .planning/phases/065-rejection-explainer/065-RESEARCH.md §Code Examples §computeRejection function location and exports — exact reference impl
    - .planning/phases/065-rejection-explainer/065-UI-SPEC.md §Copywriting Contract — exact strings the test asserts on
    - .planning/phases/065-rejection-explainer/065-CONTEXT.md §decisions D-04, D-05, D-06, D-07 — adaptive framing, ranking, signal order
  </read_first>
  <behavior>
    Test file MUST contain the following describe/it blocks (RED-state — computeRejection import will fail at module load):

    describe('computeRejection — Phase 65 WHY-01', () => {
      // Setup: 4 same-position MID players with distinct xPts_1gw to test ranking
      it('returns 1-based rank within position by xPts_1gw descending', ...)
      it('returns empty reasons (positive framing) when gem_score >= posAvg AND no fragility AND start_prob >= 0.70', ...)
      it('returns positive-framing rank label format compatible with UI rendering', ...)  // verifies xPtsRank field is correct integer
      it('includes "Ranked #X at MID by xPts" reason when player below average', ...)
      it('includes "Rotation risk — start probability XX%" when start_prob < 0.70', ...)
      it('includes "Difficult fixture (FDR medium)" when next fixture difficulty_tier === "medium"', ...)
      it('includes "Difficult fixture (FDR hard)" when next fixture difficulty_tier === "hard"', ...)  // per RESEARCH Open Q1 resolution
      it('delegates to computeFragility(player, false) — fragility reasons appear with "Fragile: no longer recommended if: " prefix', ...)
      it('parses selected_by_percent as float not string ("12.5" → 13 not raw string compare); ownership reason uses Math.round and reads "Owned by 13% of managers"', ...)
      it('does not throw on BGW player with empty fixtures array', ...)  // BGW guard
      it('positions reasons in order: rank, start_prob, fixture, fragility, ownership (per D-07)', ...)
      it('uses POSITION_CODES map: 1→GK, 2→DEF, 3→MID, 4→FWD', ...)
    })

    Test file MUST also import and assert constants exist:
    - REJECTION_START_PROB_THRESHOLD === 0.70
    - REJECTION_OWNERSHIP_THRESHOLD === 20.0
  </behavior>
  <action>
    Create `src/lib/__tests__/rejection.test.ts` with the following structure (this is the canonical contract):

    1. Header: `// Phase 65 (WHY-01): computeRejection — pure-function unit tests.` then `// @vitest-environment node` then `import { describe, it, expect } from 'vitest'`.

    2. Import the symbols-under-test (will fail at module-load until Plan 02 lands them):
       ```typescript
       import {
         computeRejection,
         REJECTION_START_PROB_THRESHOLD,
         REJECTION_OWNERSHIP_THRESHOLD,
         type RejectionResult,
       } from '../explain'
       ```

    3. Copy the `makePlayer` factory function VERBATIM from `src/lib/__tests__/sensitivity.test.ts` lines 7-62, then EXTEND the returned object literal to include `gem_score: 0.5` (default) so the cast to ScoredPlayer is valid. Cast the return type to `ScoredPlayer` (not MergedPlayer).
       ```typescript
       import type { MergedPlayer, ScoredPlayer, FixtureEntry } from '../types'

       type PlayerOverrides = Partial<ScoredPlayer> & {
         id: number
         element_type: 1 | 2 | 3 | 4
       }

       function makePlayer(overrides: PlayerOverrides): ScoredPlayer {
         return {
           // ...all fields from sensitivity.test.ts makePlayer...
           gem_score: 0.5,
           ...overrides,
         } as ScoredPlayer
       }
       ```

    4. Copy `easyFixture`, `mediumFixture` fixture objects VERBATIM from sensitivity.test.ts lines 64-78, plus add a `hardFixture: FixtureEntry = { opponent_team: 'LIV', is_home: false, event_id: 28, difficulty_score: 0.8, difficulty_tier: 'hard' }` for the FDR-hard branch (per RESEARCH Open Q1).

    5. Add a helper `function makePopulation(target: ScoredPlayer, opts: { sameposBetter?: number; sameposWorse?: number }): ScoredPlayer[]` that returns target plus N same-position other players with monotonically increasing/decreasing xPts_1gw, so rank-position tests are deterministic.

    6. Write each `it()` block per the behavior list. Each test:
       - Constructs target player + population via the helpers
       - Calls `computeRejection(target, population)`
       - Asserts on `result.reasons` (array contents/order) and/or `result.xPtsRank` (integer)
       - For copy-format tests, asserts the EXACT string per UI-SPEC Copywriting Contract:
         - Rank rejection reason: `expect(result.reasons[0]).toBe('Ranked #2 at MID by xPts')`
         - Rotation risk: `expect(result.reasons).toContain('Rotation risk — start probability 65%')` (em-dash U+2014)
         - Fixture medium: `expect(result.reasons).toContain('Difficult fixture (FDR medium)')`
         - Fixture hard: `expect(result.reasons).toContain('Difficult fixture (FDR hard)')`
         - Fragility prefix: `expect(result.reasons.some(r => r.startsWith('Fragile: no longer recommended if: '))).toBe(true)`
         - Ownership: `expect(result.reasons[result.reasons.length - 1]).toBe('Owned by 13% of managers')` (Math.round of 12.5 = 13)

    7. Constants assertion:
       ```typescript
       it('exports REJECTION_START_PROB_THRESHOLD === 0.70', () => {
         expect(REJECTION_START_PROB_THRESHOLD).toBe(0.70)
       })
       it('exports REJECTION_OWNERSHIP_THRESHOLD === 20.0', () => {
         expect(REJECTION_OWNERSHIP_THRESHOLD).toBe(20.0)
       })
       ```

    Total test count: 12 `it()` blocks minimum. File length: 120-180 lines. Do NOT implement any logic — only the test file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/rejection.test.ts 2>&1 | grep -E "FAIL|Failed|error|Cannot find" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/lib/__tests__/rejection.test.ts`
    - File starts with `// @vitest-environment node` directive (grep first line)
    - File imports `computeRejection`, `REJECTION_START_PROB_THRESHOLD`, `REJECTION_OWNERSHIP_THRESHOLD`, type `RejectionResult` from `'../explain'` — verify with: `grep -E "^import\\s+\\{[^}]*computeRejection" src/lib/__tests__/rejection.test.ts | grep -c "REJECTION_START_PROB_THRESHOLD"` is 1
    - File contains at least 12 `it(` blocks: `grep -v '^[[:space:]]*//' src/lib/__tests__/rejection.test.ts | grep -c '^[[:space:]]*it(' | awk '$1 >= 12'`
    - File contains the exact UI-SPEC string `'Ranked #'` AND `' at MID by xPts'` (substring assertion test): `grep -F "Ranked #" src/lib/__tests__/rejection.test.ts` returns at least one line
    - File contains the exact em-dash phrase `'Rotation risk — start probability'`: `grep -F "Rotation risk — start probability" src/lib/__tests__/rejection.test.ts` returns at least one line
    - File contains `'Difficult fixture (FDR medium)'` AND `'Difficult fixture (FDR hard)'` literal strings (UTF-8): both grep -F searches return matches
    - File contains `'Owned by '` literal: `grep -F "Owned by " src/lib/__tests__/rejection.test.ts` returns at least one line
    - Vitest run exits non-zero (RED state expected): `npx vitest run src/lib/__tests__/rejection.test.ts; test $? -ne 0`
    - Vitest output contains "Cannot find" or "computeRejection" import error message
  </acceptance_criteria>
  <done>rejection.test.ts exists with 12+ test cases, codifies all UI-SPEC copywriting strings as exact literal assertions, and fails at module load because computeRejection is not yet exported from explain.ts. RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HighOwnershipCallout.test.tsx (WHY-02 RTL contract)</name>
  <files>src/components/transfers/HighOwnershipCallout.test.tsx</files>
  <read_first>
    - src/components/shared/FragilityNote.test.tsx (verbatim model — same `// @vitest-environment jsdom` header, same `render` + `container.querySelector('[data-testid]')` pattern)
    - src/components/shared/FragilityNote.tsx (early-return-null pattern)
    - src/lib/types.ts lines 94-300 (ScoredPlayer interface to type fixtures)
    - .planning/phases/065-rejection-explainer/065-PATTERNS.md §HighOwnershipCallout component structure — full reference impl
    - .planning/phases/065-rejection-explainer/065-UI-SPEC.md §WHY-02 + §Copywriting Contract — exact strings
    - .planning/phases/065-rejection-explainer/065-CONTEXT.md D-11 D-12 D-13 D-14 — locked behaviour
  </read_first>
  <behavior>
    Test file MUST cover (RED — HighOwnershipCallout.tsx does not yet exist; module import fails):

    describe('HighOwnershipCallout — Phase 65 WHY-02', () => {
      it('renders nothing when entries is empty array', ...)  // container.firstChild === null AND data-testid not present
      it('renders root div with data-testid="high-ownership-callout" when entries provided', ...)
      it('renders header reading "ℹ️ Why aren\'t these players appearing?"', ...)
      it('renders in-squad variant copy: "[Name] (X%): Already ranked #N at POS in your squad by xPts — no upgrade needed"', ...)
      it('renders not-in-squad variant copy: "[Name] (X%): xPts gain vs your POS options is negative — not worth transferring in"', ...)
      it('renders ownership percentage as integer via Math.round(parseFloat(selected_by_percent))', ...)
      // E.g. "12.5" → 13, "23.4" → 23
      it('renders all entries provided (caller controls cap-at-3)', ...)  // pass 3 entries, assert 3 player paragraphs rendered
    })
  </behavior>
  <action>
    Create `src/components/transfers/HighOwnershipCallout.test.tsx` with the following structure:

    1. Header lines (verbatim from FragilityNote.test.tsx pattern):
       ```typescript
       // Phase 65 (WHY-02): HighOwnershipCallout — RTL component tests.
       // @vitest-environment jsdom
       import { describe, it, expect } from 'vitest'
       import { render } from '@testing-library/react'
       import { HighOwnershipCallout } from './HighOwnershipCallout'
       import type { ScoredPlayer } from '@/lib/types'
       ```

    2. Add a `makeEntry()` helper that returns a `{ player: ScoredPlayer, inSquad: boolean, squadRank?: number, posCode: string }` shape with sensible defaults overridable via Partial:
       ```typescript
       function makeEntry(overrides: Partial<{
         id: number; web_name: string; selected_by_percent: string;
         inSquad: boolean; squadRank: number; posCode: string;
       }> = {}) {
         const id = overrides.id ?? 1
         return {
           player: {
             id,
             web_name: overrides.web_name ?? 'Salah',
             selected_by_percent: overrides.selected_by_percent ?? '50.0',
             // Other fields set to minimal valid stubs — only the three fields above
             // are read by HighOwnershipCallout per the PATTERNS reference impl
           } as unknown as ScoredPlayer,
           inSquad: overrides.inSquad ?? false,
           squadRank: overrides.squadRank,
           posCode: overrides.posCode ?? 'MID',
         }
       }
       ```

    3. Write each `it()` block:

       Empty test:
       ```typescript
       it('renders nothing when entries is empty', () => {
         const { container } = render(<HighOwnershipCallout entries={[]} />)
         expect(container.firstChild).toBeNull()
         expect(container.querySelector('[data-testid="high-ownership-callout"]')).toBeNull()
       })
       ```

       Header text test (assert against exact UI-SPEC string with ℹ️):
       ```typescript
       it('renders header "ℹ️ Why aren\'t these players appearing?"', () => {
         const { getByText } = render(<HighOwnershipCallout entries={[makeEntry()]} />)
         // Note: the component uses HTML entities &#8505;&#65039; which render as ℹ️ in DOM
         expect(getByText(/Why aren['’]t these players appearing\?/)).toBeTruthy()
       })
       ```

       In-squad copy:
       ```typescript
       it('in-squad entry copy: "Already ranked #N at POS in your squad by xPts — no upgrade needed"', () => {
         const { container } = render(<HighOwnershipCallout entries={[
           makeEntry({ web_name: 'Salah', selected_by_percent: '50.0', inSquad: true, squadRank: 1, posCode: 'MID' })
         ]} />)
         // Use textContent to assert combined text including the em-dash U+2014
         expect(container.textContent).toContain('Salah')
         expect(container.textContent).toContain('(50%):')
         expect(container.textContent).toContain('Already ranked #1 at MID in your squad by xPts')
         expect(container.textContent).toContain('no upgrade needed')
         expect(container.textContent).toContain('—')  // em-dash U+2014 present
       })
       ```

       Not-in-squad copy:
       ```typescript
       it('not-in-squad entry copy: "xPts gain vs your POS options is negative — not worth transferring in"', () => {
         const { container } = render(<HighOwnershipCallout entries={[
           makeEntry({ web_name: 'Haaland', selected_by_percent: '60.0', inSquad: false, posCode: 'FWD' })
         ]} />)
         expect(container.textContent).toContain('Haaland')
         expect(container.textContent).toContain('xPts gain vs your FWD options is negative')
         expect(container.textContent).toContain('not worth transferring in')
       })
       ```

       Ownership rounding:
       ```typescript
       it('rounds ownership percentage via Math.round(parseFloat(...))', () => {
         const { container } = render(<HighOwnershipCallout entries={[
           makeEntry({ selected_by_percent: '12.5' })
         ]} />)
         expect(container.textContent).toContain('(13%)')   // Math.round(12.5) === 13
       })
       ```

       Multiple entries:
       ```typescript
       it('renders one paragraph per entry (caller responsible for cap-at-3)', () => {
         const entries = [
           makeEntry({ id: 1, web_name: 'A' }),
           makeEntry({ id: 2, web_name: 'B' }),
           makeEntry({ id: 3, web_name: 'C' }),
         ]
         const { container } = render(<HighOwnershipCallout entries={entries} />)
         const callout = container.querySelector('[data-testid="high-ownership-callout"]')
         expect(callout).not.toBeNull()
         // header <p> + 3 entry <p> = 4 paragraphs
         expect(callout!.querySelectorAll('p').length).toBe(4)
       })
       ```

       Total test count: 7 `it()` blocks minimum. Do NOT create the component file — that is Plan 03's job.
  </action>
  <verify>
    <automated>npx vitest run src/components/transfers/HighOwnershipCallout.test.tsx 2>&1 | grep -E "FAIL|Failed|Cannot find|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/components/transfers/HighOwnershipCallout.test.tsx`
    - File contains `// @vitest-environment jsdom` directive: `grep -F "@vitest-environment jsdom" src/components/transfers/HighOwnershipCallout.test.tsx` returns 1 line
    - File imports from `'./HighOwnershipCallout'` (the not-yet-created component): `grep -F "from './HighOwnershipCallout'" src/components/transfers/HighOwnershipCallout.test.tsx` returns 1 line
    - File contains at least 7 `it(` blocks: `grep -v '^[[:space:]]*//' src/components/transfers/HighOwnershipCallout.test.tsx | grep -c '^[[:space:]]*it(' | awk '$1 >= 7'`
    - File asserts the exact in-squad copy fragment `'Already ranked #'` and `'in your squad by xPts'`: both grep -F searches return at least one line
    - File asserts the exact not-in-squad copy fragment `'xPts gain vs your '` and `'not worth transferring in'`: both grep -F searches return at least one line
    - File asserts the data-testid `'high-ownership-callout'`: `grep -F "high-ownership-callout" src/components/transfers/HighOwnershipCallout.test.tsx` returns at least one line
    - Vitest run exits non-zero (RED expected because HighOwnershipCallout.tsx file does not exist): `npx vitest run src/components/transfers/HighOwnershipCallout.test.tsx; test $? -ne 0`
  </acceptance_criteria>
  <done>HighOwnershipCallout.test.tsx exists with 7+ test cases asserting exact UI-SPEC copy fragments and data-testid root. Fails at import time because component file does not exist yet. RED state confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ExplainPanel.test.tsx (WHY-03 RTL contract)</name>
  <files>src/components/squad/ExplainPanel.test.tsx</files>
  <read_first>
    - src/components/squad/ExplainPanel.tsx (current state — confirms rejectionReasons prop NOT present; this is the RED state for Plan 03)
    - src/components/squad/ProseSummaryBlock.test.tsx (closest RTL analog — `// @vitest-environment jsdom` + render + getByText pattern)
    - src/components/shared/FragilityNote.test.tsx (additional reference for `container.querySelectorAll` DOM-order assertion pattern)
    - .planning/phases/065-rejection-explainer/065-UI-SPEC.md §WHY-03 + §Copywriting Contract — exact section header "Why not recommended:"
    - .planning/phases/065-rejection-explainer/065-PATTERNS.md §ExplainPanel.tsx — modified-render-structure (rejection section between positive reasons and shortlist)
    - .planning/phases/065-rejection-explainer/065-CONTEXT.md D-08 (rejection section position: below positive, above shortlist)
  </read_first>
  <behavior>
    Test file MUST cover:

    describe('ExplainPanel — Phase 65 WHY-03', () => {
      it('renders positive reasons list always', ...)
      it('does NOT render rejection section when rejectionReasons prop is undefined (backward compat)', ...)
      it('does NOT render rejection section when rejectionReasons is empty array', ...)
      it('renders rejection section with header "Why not recommended:" when rejectionReasons is non-empty', ...)
      it('renders one <li> per rejection reason with text-xs text-zinc-600 dark:text-zinc-400 styling', ...)
      it('renders rejection section AFTER positive reasons in DOM order (D-08)', ...)
      it('renders rejection section BEFORE replacement shortlist in DOM order (D-08)', ...)
    })
  </behavior>
  <action>
    Create `src/components/squad/ExplainPanel.test.tsx` with the following structure:

    1. Header:
       ```typescript
       // Phase 65 (WHY-03): ExplainPanel rejection section — RTL component tests.
       // @vitest-environment jsdom
       import { describe, it, expect } from 'vitest'
       import { render } from '@testing-library/react'
       import { ExplainPanel } from './ExplainPanel'
       import type { ShortlistEntry } from '@/lib/replacement-shortlist'
       ```

    2. Define a minimal `ShortlistEntry` fixture:
       ```typescript
       const SAMPLE_SHORTLIST: ShortlistEntry[] = [
         {
           player: { id: 99, web_name: 'Replacement', team_short_name: 'AVL' } as never,
           pts_delta: 1.5,
           budget_sufficient: true,
         } as ShortlistEntry,
       ]
       ```
       (Use type assertions to keep the fixture minimal — ExplainPanel only reads web_name, team_short_name, pts_delta, budget_sufficient.)

    3. Tests:

       Positive reasons always render:
       ```typescript
       it('renders positive reasons list always', () => {
         const { getByText } = render(<ExplainPanel reasons={['In form — 6.0 pts/90']} shortlist={null} />)
         expect(getByText('In form — 6.0 pts/90')).toBeTruthy()
       })
       ```

       No rejection section when prop omitted:
       ```typescript
       it('does NOT render rejection section when rejectionReasons is undefined', () => {
         const { queryByText } = render(<ExplainPanel reasons={['x']} shortlist={null} />)
         expect(queryByText('Why not recommended:')).toBeNull()
       })
       ```

       No rejection section when empty array:
       ```typescript
       it('does NOT render rejection section when rejectionReasons is empty array', () => {
         const { queryByText } = render(<ExplainPanel reasons={['x']} shortlist={null} rejectionReasons={[]} />)
         expect(queryByText('Why not recommended:')).toBeNull()
       })
       ```

       Renders header + reasons:
       ```typescript
       it('renders rejection section with header "Why not recommended:" and one <li> per reason', () => {
         const { getByText, container } = render(
           <ExplainPanel
             reasons={['Projected 6.0 pts next GW']}
             shortlist={null}
             rejectionReasons={['Ranked #5 at MID by xPts', 'Rotation risk — start probability 60%']}
           />
         )
         expect(getByText('Why not recommended:')).toBeTruthy()
         expect(getByText('Ranked #5 at MID by xPts')).toBeTruthy()
         expect(getByText('Rotation risk — start probability 60%')).toBeTruthy()
       })
       ```

       Styling on rejection list items:
       ```typescript
       it('rejection reason <li> elements use text-xs text-zinc-600 dark:text-zinc-400 styling', () => {
         const { container } = render(
           <ExplainPanel
             reasons={['x']}
             shortlist={null}
             rejectionReasons={['Ranked #5 at MID by xPts']}
           />
         )
         const li = container.querySelector('li.text-xs.text-zinc-600')
         // At least one li with the expected classes (positive reasons list also matches — that's fine,
         // the test asserts the rejection reasons inherit identical styling)
         expect(li).not.toBeNull()
       })
       ```

       DOM order: rejection AFTER positive, BEFORE shortlist:
       ```typescript
       it('renders rejection section between positive reasons and shortlist (D-08 order)', () => {
         const { container } = render(
           <ExplainPanel
             reasons={['POS-REASON-A']}
             shortlist={SAMPLE_SHORTLIST}
             rejectionReasons={['REJ-REASON-A']}
           />
         )
         const html = container.innerHTML
         const posIdx = html.indexOf('POS-REASON-A')
         const rejHeaderIdx = html.indexOf('Why not recommended:')
         const rejIdx = html.indexOf('REJ-REASON-A')
         const shortlistHeaderIdx = html.indexOf('Replacement options')
         expect(posIdx).toBeGreaterThanOrEqual(0)
         expect(rejHeaderIdx).toBeGreaterThan(posIdx)
         expect(rejIdx).toBeGreaterThan(rejHeaderIdx)
         expect(shortlistHeaderIdx).toBeGreaterThan(rejIdx)
       })
       ```

    Total test count: 7 `it()` blocks minimum.
  </action>
  <verify>
    <automated>npx vitest run src/components/squad/ExplainPanel.test.tsx 2>&1 | grep -E "FAIL|Failed|error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/components/squad/ExplainPanel.test.tsx`
    - File contains `// @vitest-environment jsdom` directive: `grep -F "@vitest-environment jsdom" src/components/squad/ExplainPanel.test.tsx` returns 1 line
    - File contains at least 7 `it(` blocks: `grep -v '^[[:space:]]*//' src/components/squad/ExplainPanel.test.tsx | grep -c '^[[:space:]]*it(' | awk '$1 >= 7'`
    - File passes the `rejectionReasons` prop in JSX (will be a TypeScript compile error in current state — RED): `grep -F "rejectionReasons=" src/components/squad/ExplainPanel.test.tsx` returns at least one line
    - File asserts the exact section header literal `'Why not recommended:'`: `grep -F "Why not recommended:" src/components/squad/ExplainPanel.test.tsx` returns at least one line
    - File asserts DOM ordering using `Replacement options` as the trailing landmark: `grep -F "Replacement options" src/components/squad/ExplainPanel.test.tsx` returns at least one line
    - Vitest run exits non-zero (RED expected — TS compile error on rejectionReasons prop, since ExplainPanel.tsx doesn't accept it yet): `npx vitest run src/components/squad/ExplainPanel.test.tsx; test $? -ne 0`
  </acceptance_criteria>
  <done>ExplainPanel.test.tsx exists with 7+ test cases asserting WHY-03 rejection section behaviour, DOM order, and exact UI-SPEC strings. Fails because rejectionReasons prop is not yet declared on ExplainPanelProps. RED state confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 65 is pure client-side computation over already-fetched in-memory data. No new auth surface, no new network calls, no new persistence, no new user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering / Information Disclosure | Player name strings rendered into JSX (web_name, opponent_team) | accept | React's automatic JSX text-node escaping protects against XSS. No `dangerouslySetInnerHTML` is introduced anywhere in this plan. Names originate from FPL official API (already-trusted data source) and are interpolated only via JSX text and template strings — never as innerHTML. ASVS L1 V5.1.3 satisfied by React framework default. |

No high-severity threats identified. No new data flows, no new IO, no new dependencies.
</threat_model>

<verification>
Run all three new test files in a single command and confirm RED state:
```bash
npx vitest run src/lib/__tests__/rejection.test.ts src/components/transfers/HighOwnershipCallout.test.tsx src/components/squad/ExplainPanel.test.tsx
# Expected: non-zero exit code with module-load / import-resolution errors for plans 02 and 03 to address.
```

Confirm file count + line counts via shell:
```bash
wc -l src/lib/__tests__/rejection.test.ts \
      src/components/transfers/HighOwnershipCallout.test.tsx \
      src/components/squad/ExplainPanel.test.tsx
```

Each file must be at least 60 lines (test stubs are non-trivial fixtures + 7-12 it blocks).
</verification>

<success_criteria>
1. `src/lib/__tests__/rejection.test.ts` exists, imports `computeRejection` + 2 constants from `'../explain'`, has ≥12 `it(` blocks, asserts exact UI-SPEC copy strings, fails to compile/run.
2. `src/components/transfers/HighOwnershipCallout.test.tsx` exists, imports `HighOwnershipCallout` from `'./HighOwnershipCallout'`, has ≥7 `it(` blocks, asserts data-testid root and exact in-squad/not-in-squad copy fragments, fails to import.
3. `src/components/squad/ExplainPanel.test.tsx` exists, passes `rejectionReasons` prop to `<ExplainPanel>`, has ≥7 `it(` blocks, asserts DOM order between positive reasons and shortlist, fails to compile.
4. Combined `npx vitest run` exits non-zero across all three files (RED state) — Wave 1 plans 02 and 03 will turn them GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/065-rejection-explainer/065-01-SUMMARY.md` per the standard summary template, recording:
- File paths and line counts of the three created test stubs
- Total test count per file (count `it(` lines)
- Output of `npx vitest run` confirming RED state with the specific failure messages (compile error or import-not-found)
- The exact symbols Wave 1 plans 02 and 03 must export to turn each test file GREEN
</output>
