---
phase: 111
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/club-form.ts
  - src/lib/club-form.test.ts
  - src/app/api/club-form/route.ts
autonomous: true
requirements:
  - FIX-01
tags: [fpl, club-form, fixtures, typescript, tdd]

must_haves:
  truths:
    - "computeClubForm accepts an optional events array on RawBootstrap and derives the active GW id from is_current (with fallback to the last finished event)"
    - "Each ClubForm in the returned array carries a current_gw_played: ClubFormFixture[] field populated only with finished fixtures whose event === currentGw"
    - "Both the home and away team of a finished current-GW fixture appear in their respective current_gw_played array"
    - "current_gw_played is an empty array when events is omitted (backward compat) or when no current-GW fixture is finished"
    - "/api/club-form passes bootstrap.events through to computeClubForm so the served ClubForm[] reflects current_gw_played in production"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "ClubForm interface extended with required current_gw_played: ClubFormFixture[]"
      contains: "current_gw_played"
    - path: "src/lib/club-form.ts"
      provides: "RawEvent + RawBootstrap.events + currentGw derivation + current_gw_played builder"
      contains: "current_gw_played"
    - path: "src/lib/club-form.test.ts"
      provides: "Node-env unit tests for computeClubForm current_gw_played behaviour"
      contains: "current_gw_played"
    - path: "src/app/api/club-form/route.ts"
      provides: "Route passes bootstrap.events to computeClubForm"
      contains: "events: bootstrap.events"
  key_links:
    - from: "src/app/api/club-form/route.ts"
      to: "src/lib/club-form.ts"
      via: "computeClubForm({ teams: bootstrap.teams, events: bootstrap.events }, fixtures)"
      pattern: "events:\\s*bootstrap\\.events"
    - from: "src/lib/club-form.ts"
      to: "src/lib/types.ts"
      via: "result.push includes current_gw_played: teamPlayedCurrentGw.get(tId) ?? []"
      pattern: "current_gw_played:\\s*teamPlayedCurrentGw"
---

<objective>
Land the FIX-01 data layer: extend `ClubForm` with `current_gw_played: ClubFormFixture[]`, teach `computeClubForm` to derive the active GW from `bootstrap.events` and populate that field with finished fixtures from the active GW only, and pass `bootstrap.events` through `/api/club-form/route.ts` so the field is non-empty in production. Tests are written first (D-12 RED→GREEN) in a new `src/lib/club-form.test.ts` (which does not yet exist — confirmed in RESEARCH Open Question 1).

Purpose: This is the prerequisite data fix that Plan 02 (FixtureHeatMap rendering) consumes. Without it, the heatmap has no signal to distinguish "team played mid-week" from "true BGW" (D-01).

Output: ClubForm.current_gw_played is populated server-side at request time, served via the existing club-form route, and locked by unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md

<interfaces>
<!-- Extracted from src/lib/types.ts:483–522. Use these directly; do not invent new types. -->
From src/lib/types.ts:
```typescript
export interface ClubFormFixture {
  opponent_team: string
  is_home: boolean
  event_id: number
  difficulty_score: number
  difficulty_tier: DifficultyTier
  attacking_difficulty: number
  defensive_difficulty: number
}

export interface ClubForm {
  team_id: number
  team_name: string
  team_short_name: string
  wins: number
  draws: number
  losses: number
  goals_scored: number
  goals_conceded: number
  upcoming_fixtures: ClubFormFixture[]   // next 32
  // current_gw_played: ClubFormFixture[]  ← ADDED in Task 2 (between upcoming_fixtures and ease block)
  attacking_ease_1gw: number | null
  // ... ease/swing fields unchanged
}
```

<!-- Extracted from src/lib/club-form.ts:24–45. -->
From src/lib/club-form.ts:
```typescript
interface RawFixture {
  team_h: number
  team_a: number
  team_h_score: number | null
  team_a_score: number | null
  team_h_difficulty: number
  team_a_difficulty: number
  event: number | null
  finished: boolean
}

interface RawTeam {
  id: number
  name: string
  short_name: string
}

interface RawBootstrap {
  teams: RawTeam[]
  // events?: RawEvent[]  ← ADDED in Task 2
}

export function computeClubForm(bootstrap: RawBootstrap, fixtures: RawFixture[]): ClubForm[]
```

<!-- Extracted from src/app/api/club-form/route.ts:34–36. -->
From src/app/api/club-form/route.ts:
```typescript
const fixtures = JSON.parse(fixturesRaw)
const bootstrap = JSON.parse(bootstrapRaw)
const data = computeClubForm({ teams: bootstrap.teams }, fixtures)   // ← Task 3 changes this
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for computeClubForm current_gw_played</name>
  <files>src/lib/club-form.test.ts</files>
  <read_first>
    - src/lib/club-form.ts (full file — pin down RawFixture, RawTeam shapes, finished filter logic, fplToAttDiff, defScore, tier helper)
    - src/lib/types.ts (lines 483–522 — ClubFormFixture and ClubForm interface; note current_gw_played is not yet declared)
    - src/lib/suggest-transfers.test.ts (lines 1–10 — node-env directive pattern: `// @vitest-environment node`)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md (sections "Node-env test pattern" and "current_gw_played builder")
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <behavior>
    The new test file `src/lib/club-form.test.ts` declares `// @vitest-environment node` at the top and imports `computeClubForm` from `./club-form`. It includes a local `rawFixture()`, `rawTeam()`, and `rawEvent()` factory mirroring the shapes in `club-form.ts` (RawFixture, RawTeam) and the new RawEvent shape `{ id: number, is_current: boolean, finished: boolean }`.
    - Test 1 "populates current_gw_played with finished fixtures from the active GW only": build 3 teams (A, B, C), 3 events (gw 34/35/36 with is_current on event 35), 4 fixtures: A vs B finished gw34, A vs C finished gw35, B vs C upcoming gw35, A vs B upcoming gw36. Expect team A's current_gw_played to contain exactly 1 entry with `event_id === 35` and `opponent_team === 'C'`. Expect team C's current_gw_played to contain exactly 1 entry with `event_id === 35` and `opponent_team === 'A'`. Expect team B's current_gw_played to be `[]` (B's only finished fixture was gw34).
    - Test 2 "leaves current_gw_played empty when no fixtures are finished in current GW": same teams, events with is_current on gw 35, but only fixtures present are upcoming gw35 and upcoming gw36. Expect every team's current_gw_played to be `[]`.
    - Test 3 "does NOT include historical finished fixtures in current_gw_played": teams + events as above, fixtures = A vs B finished gw34. Expect every team's current_gw_played to be `[]` (gw34 is historical relative to current gw35).
    - Test 4 "populates current_gw_played for both home and away teams of a finished fixture": one finished fixture A(home) vs B(away) at gw35 (current). Expect team A's current_gw_played[0] has `is_home === true` and `opponent_team === B.short_name`. Expect team B's current_gw_played[0] has `is_home === false` and `opponent_team === A.short_name`.
    - Test 5 "current_gw_played is [] when events array is not provided (backward compat)": call `computeClubForm({ teams }, fixtures)` (no events key) with one finished fixture at any event. Expect every team's current_gw_played to be `[]`.
    - Test 6 "falls back to last finished event when no event has is_current": events = [{ id: 35, is_current: false, finished: true }, { id: 36, is_current: false, finished: false }]. Finished fixture at event 35. Expect team's current_gw_played to contain that fixture (event_id === 35).
    Each test asserts difficulty_score, difficulty_tier, attacking_difficulty, defensive_difficulty are present and numeric on the produced ClubFormFixture entries (mirroring upcoming_fixtures structure).
    All 6 tests MUST FAIL initially with TypeScript compile error or runtime assertion failure because `current_gw_played` does not yet exist on ClubForm.
  </behavior>
  <action>
    Create `src/lib/club-form.test.ts`. Add `// @vitest-environment node` directive on first line, then `import { describe, it, expect } from 'vitest'` and `import { computeClubForm } from './club-form'`. Define local factories `rawFixture(opts)`, `rawTeam(id, name, shortName)`, and `rawEvent(id, isCurrent, finished)` returning the inline shapes shown in the read_first files. Wrap all 6 test cases in a `describe('computeClubForm — current_gw_played (Phase 111 FIX-01)', ...)` block. Use distinct team_h_difficulty / team_a_difficulty values (e.g., 2 and 4) so the produced attacking_difficulty values are non-trivially testable. Run the test suite to confirm RED — every test must fail (the field does not exist on the returned ClubForm objects, so the property access returns undefined and `.length` / `[0]` assertions throw or return undefined). Commit as `test(111-01): add failing tests for current_gw_played field` per D-12.
  </action>
  <verify>
    <automated>npx vitest run src/lib/club-form.test.ts 2>&1 | grep -E "FAIL|6 failed|×"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/club-form.test.ts` exists
    - File contains `// @vitest-environment node` on line 1
    - File contains `describe('computeClubForm — current_gw_played (Phase 111 FIX-01)'`
    - File contains 6 `it(...)` calls with the names listed in `<behavior>`
    - `npx vitest run src/lib/club-form.test.ts` exits non-zero (RED state — implementation not yet present)
    - Git commit exists with message `test(111-01): add failing tests for current_gw_played field`
  </acceptance_criteria>
  <done>All 6 tests committed and FAILING. RED phase complete; do not proceed to Task 2 until this is confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend types + computeClubForm to populate current_gw_played</name>
  <files>src/lib/types.ts, src/lib/club-form.ts</files>
  <read_first>
    - src/lib/types.ts (lines 480–525 — see ClubFormFixture at 483, ClubForm at 494; insert site is between upcoming_fixtures line 503 and the Phase 27 ease comment block on line 504)
    - src/lib/club-form.ts (full file — note: `finished` array already exists at lines 52–54; `fplToAttDiff` at 97; `defScore` at 81; `teams` Map at 49; the upcoming_fixtures builder pattern at 87–128 is the structural template to mirror; the `result.push` block at 168–188 is where the field must be added)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md (section "src/lib/club-form.ts (service/utility, transform)" — Pattern 1 + Pattern 2 with verified code excerpts)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-01, D-02, D-03 — pipeline-layer fix; current GW only; no `finished: boolean` field)
    - src/lib/club-form.test.ts (the 6 failing tests written in Task 1 — implementation must satisfy each assertion)
  </read_first>
  <behavior>
    After this task, all 6 tests from Task 1 PASS without modification. The `ClubForm` interface declares `current_gw_played: ClubFormFixture[]` as a required field (not optional, consistent with all other fields). The `computeClubForm` function derives `currentGw` from `bootstrap.events` using `is_current` first, then last-finished fallback, then `null`; populates a `teamPlayedCurrentGw: Map<number, ClubFormFixture[]>` from the existing `finished` array filtered to `f.event === currentGw`; pushes the per-team array into the `result.push({...})` block alongside `upcoming_fixtures`. When `bootstrap.events` is undefined, `currentGw` is `null` and `current_gw_played` is `[]` for every team.
  </behavior>
  <action>
    1. In `src/lib/types.ts`, add the line `current_gw_played: ClubFormFixture[]   // Phase 111 FIX-01 — finished fixtures from active GW only` immediately after the `upcoming_fixtures: ClubFormFixture[]   // next 32` line inside the `ClubForm` interface. Keep alphabetical/logical placement: directly after upcoming_fixtures, before the Phase 27 attacking_ease block. Field is REQUIRED (no `?`), consistent with all other ClubForm fields.
    2. In `src/lib/club-form.ts`, define a new top-level interface `interface RawEvent { id: number; is_current: boolean; finished: boolean }` directly above the existing `RawBootstrap` interface (before line 41). Extend `RawBootstrap` to add `events?: RawEvent[]` as an OPTIONAL field (so existing test fixtures and any other callers without events continue to type-check).
    3. Inside `computeClubForm`, after the `teamUpcoming` loop completes (after current line 128) and before the result loop begins (current line 131), add: a `currentGw` derivation `const currentGw: number | null = bootstrap.events?.find(e => e.is_current)?.id ?? bootstrap.events?.filter(e => e.finished).slice(-1)[0]?.id ?? null`; a `teamPlayedCurrentGw = new Map<number, ClubFormFixture[]>()` initialised with empty arrays for every team; if `currentGw !== null`, iterate `finished.filter(f => f.event === currentGw)` and push entries into both home and away team arrays mirroring the exact shape used in the `upcoming` loop (use `fplToAttDiff(fix.team_h_difficulty)` for home entries, `fplToAttDiff(fix.team_a_difficulty)` for away, `defScore(fix.team_a)` / `defScore(fix.team_h)` for opponent defensive scoring, `tier(attDiff)` for difficulty_tier, `event_id: fix.event!`, `opponent_team: opp?.short_name ?? String(fix.team_*)`).
    4. In the `result.push({...})` block at current lines 168–188, insert `current_gw_played: teamPlayedCurrentGw.get(tId) ?? [],` immediately after the `upcoming_fixtures: upcomingFx,` line and before the `attacking_ease_1gw,` line.
    5. Run `npx vitest run src/lib/club-form.test.ts` — all 6 tests MUST PASS. Then run `npx vitest run src/lib/club-form.test.ts src/components/club-form/FixtureHeatMap.test.tsx` to confirm the existing FixtureHeatMap tests still pass (the test helper `team()` does not yet include `current_gw_played` but the cast to `as ClubForm` will make the tests pass; this is acceptable for now and is updated in Plan 02). Then run `npx tsc --noEmit` to ensure no callers break — note that `src/components/club-form/FixtureHeatMap.test.tsx`'s `team()` helper builds an inline `ClubForm` literal cast `as ClubForm`; that cast will silence the missing `current_gw_played`. Plan 02 updates the helper. Do NOT modify FixtureHeatMap.tsx or its test in this task.
    6. Commit as `feat(111-01): populate ClubForm.current_gw_played from finished current-GW fixtures`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/club-form.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types.ts` `ClubForm` interface contains the line declaring `current_gw_played: ClubFormFixture[]` as a required field (no `?`)
    - `src/lib/club-form.ts` defines `interface RawEvent` with fields `id: number`, `is_current: boolean`, `finished: boolean`
    - `src/lib/club-form.ts` `RawBootstrap` interface contains `events?: RawEvent[]`
    - `src/lib/club-form.ts` contains the literal token `teamPlayedCurrentGw` (the new Map)
    - `src/lib/club-form.ts` `result.push` block contains the substring `current_gw_played: teamPlayedCurrentGw.get(tId)`
    - `npx vitest run src/lib/club-form.test.ts` exits 0 with all 6 tests passing
    - `npx tsc --noEmit` exits 0 (no type errors anywhere in the project)
    - Git commit exists with message `feat(111-01): populate ClubForm.current_gw_played from finished current-GW fixtures`
  </acceptance_criteria>
  <done>All 6 tests GREEN. ClubForm.current_gw_played is populated by computeClubForm. No TypeScript errors. No regression in existing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire route — pass bootstrap.events to computeClubForm</name>
  <files>src/app/api/club-form/route.ts</files>
  <read_first>
    - src/app/api/club-form/route.ts (full 48 lines — note line 36 is the call to computeClubForm; `bootstrap` is the parsed JSON literal from `bootstrapRaw`, untyped, so `bootstrap.events` is accessible without a type cast)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md (section "Pitfall 2: Route passes only { teams: bootstrap.teams } to computeClubForm")
    - pipeline/cache/fpl_bootstrap.json (verify the file contains an `events` array — research confirmed this; just spot-check via Grep below if uncertain)
  </read_first>
  <behavior>
    The single call site `computeClubForm({ teams: bootstrap.teams }, fixtures)` becomes `computeClubForm({ teams: bootstrap.teams, events: bootstrap.events }, fixtures)`. The error handler, USE_BLOB branching, and Cache-Control header are unchanged. After this task, hitting `/api/club-form` against the local cache (current GW = 35, all 10 GW35 fixtures finished per RESEARCH) returns ClubForm objects whose `current_gw_played` arrays are non-empty for the 20 PL teams that played in GW35.
  </behavior>
  <action>
    1. Edit `src/app/api/club-form/route.ts` line 36 — change the literal from `computeClubForm({ teams: bootstrap.teams }, fixtures)` to `computeClubForm({ teams: bootstrap.teams, events: bootstrap.events }, fixtures)`. No other lines in the file change.
    2. Run `npx tsc --noEmit` to confirm no type errors (RawBootstrap.events is optional; bootstrap is untyped JSON.parse so no cast needed).
    3. Run `npx vitest run` to confirm full suite still green.
    4. Commit as `fix(111-01): pass bootstrap.events to computeClubForm so current_gw_played is populated`.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('src/app/api/club-form/route.ts','utf8'); if (!/events:\s*bootstrap\.events/.test(c)) { console.error('events not passed'); process.exit(1) } process.exit(0)" && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/club-form/route.ts` contains the substring `events: bootstrap.events`
    - File still contains the existing `Cache-Control` header and the catch block
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full suite green)
    - Git commit exists with message `fix(111-01): pass bootstrap.events to computeClubForm so current_gw_played is populated`
  </acceptance_criteria>
  <done>Route serializes ClubForm[] with non-empty current_gw_played arrays in production. Plan 02 (FixtureHeatMap rendering) can now consume the field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local cache / Vercel Blob → API route | JSON is read from disk or fetched from blob; treated as trusted pipeline output |
| API route → Browser | ClubForm[] JSON served via Response.json with Cache-Control header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | bootstrap.events array shape | accept | Source is trusted FPL pipeline cache (local file or Vercel Blob URL we control); no user input enters this path. RawEvent interface narrows the shape; optional chaining (`bootstrap.events?.find`) tolerates missing or malformed events without throwing. |
| T-111-02 | Information Disclosure | current_gw_played field | accept | Field contains only public fixture data already exposed elsewhere on /api/club-form (opponent_team short_name, is_home, event_id, difficulty values). No PII or auth tokens. |
| T-111-03 | Denial of Service | finished.filter(f => f.event === currentGw) | accept | Bounded by ~10 fixtures per current GW × 20 teams = O(200) operations per request. The route has a 1h Cache-Control + stale-while-revalidate=24h header; no per-user computation. |
</threat_model>

<verification>
- `npx vitest run src/lib/club-form.test.ts` — 6 new tests pass
- `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — existing 46 tests still pass (cast `as ClubForm` silences the missing field; helper update is in Plan 02)
- `npx vitest run` — full suite green
- `npx tsc --noEmit` — no type errors
- Manual probe (optional): start dev server, `curl http://localhost:3000/api/club-form | jq '.[0].current_gw_played'` returns the home/away entry for the team at index 0 if that team played in GW35
</verification>

<success_criteria>
- ClubForm.current_gw_played is a required field, populated server-side by computeClubForm from bootstrap.events + finished fixtures
- Route /api/club-form passes events through; production response includes current_gw_played
- 6 new node-env unit tests lock the behaviour, all GREEN
- No type errors, no test regressions
- All 3 commits land in TDD RED→GREEN order (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-01-SUMMARY.md` documenting:
- The 3 files modified, the 1 file created
- The 6 test cases added (one-line each)
- Confirmation that Plan 02 can now read `current_gw_played` from `useClubForm()` data without further data-layer work
- Any deviation from the plan (expected: none; pattern is fully spec'd)
</output>
