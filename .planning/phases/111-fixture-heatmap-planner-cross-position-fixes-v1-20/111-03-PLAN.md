---
phase: 111
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/suggest-transfers.test.ts
  - src/lib/suggest-transfers.ts
  - src/components/transfers/TransferPanel.tsx
  - src/components/optimiser/OptimiserPanel.tsx
  - src/components/squad/DecisionSummaryTab.tsx
  - src/components/rivals/RivalsTab.tsx
autonomous: true
requirements:
  - FIX-02
tags: [fpl, transfers, engine, audit, tdd, suggest-transfers]

must_haves:
  truths:
    - "An engine-level regression test in src/lib/suggest-transfers.test.ts proves that no `single` TransferSuggestion ever has sell.element_type !== buy.element_type"
    - "An equivalent invariant test exists for `combo` suggestions: each combo's transfers[] array has matching sell/buy element_type per leg"
    - "A defensive guard at the top of suggestTransfers filters players whose element_type is NOT in {1,2,3,4}, with a console.warn naming the bad ids"
    - "All 4 call sites (TransferPanel, OptimiserPanel, DecisionSummaryTab, RivalsTab) carry an explicit FIX-02 comment annotation citing the engine-level position-lock contract"
  artifacts:
    - path: "src/lib/suggest-transfers.test.ts"
      provides: "FIX-02 regression tests (single + combo position-lock invariants + guard test)"
      contains: "FIX-02"
    - path: "src/lib/suggest-transfers.ts"
      provides: "Defensive element_type guard at function entry (D-09)"
      contains: "FIX-02"
    - path: "src/components/transfers/TransferPanel.tsx"
      provides: "FIX-02 audit annotation on suggestTransfers call site"
      contains: "FIX-02"
    - path: "src/components/optimiser/OptimiserPanel.tsx"
      provides: "FIX-02 audit annotation"
      contains: "FIX-02"
    - path: "src/components/squad/DecisionSummaryTab.tsx"
      provides: "FIX-02 audit annotation"
      contains: "FIX-02"
    - path: "src/components/rivals/RivalsTab.tsx"
      provides: "FIX-02 audit annotation"
      contains: "FIX-02"
  key_links:
    - from: "src/lib/suggest-transfers.ts"
      to: "TransferSuggestion contract (sell.element_type === buy.element_type per leg)"
      via: "filter players.filter(p => p.element_type === pos ...) at line 118"
      pattern: "p\\.element_type\\s*===\\s*pos"
    - from: "src/lib/suggest-transfers.test.ts"
      to: "Position-lock invariant"
      via: "expect(sug.sell.element_type).toBe(sug.buy.element_type)"
      pattern: "sell\\.element_type.*toBe.*buy\\.element_type"
---

<objective>
Land FIX-02: prove the `suggestTransfers` engine never produces cross-position single or combo suggestions, add a defensive runtime guard at the engine entry to filter any future corrupt-element_type players (D-09), and audit all 4 known call sites — annotating each with a FIX-02 comment confirming the engine is the source-of-truth position lock. Tests are written first (D-12 RED→GREEN).

This plan deliberately runs in **Wave 1** in parallel with Plan 01 (no file overlap with Plans 01/02). It is engine-side only and does not depend on FIX-01.

Purpose: RESEARCH revealed the engine filter at `suggest-transfers.ts:118` is correct and the live `merged_players.json` cache has zero invalid `element_type` values (832 players, all in {1,2,3,4}). The bug is therefore either a transient data-corruption event OR a misreport from the user (e.g., looking at the OCS table and conflating multiple single suggestions across positions). Either way: a regression test locks the invariant, a defensive guard prevents future corruption from leaking, and call-site annotations make the position-lock contract explicit so future changes don't silently break it.

Output: Engine-level position lock provably enforced and annotated; defensive guard logs and filters any future corrupt-element_type input; all 4 call sites carry a self-documenting FIX-02 reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md

<interfaces>
TransferSuggestion contract — verified from src/lib/types.ts and src/lib/suggest-transfers.ts:

```typescript
export interface SuggestTransfersParams {
  currentPicks: SquadPick[]
  players: MergedPlayer[]
  horizon: OptimiserHorizon
  ftCount: 1 | 2
  bank: number
  sellPrices?: Map<number, number>
  targetGw?: number
}

// Engine returns TransferSuggestion[] union:
//   { kind: 'single', sell: MergedPlayer, buy: MergedPlayer, cost, xPtsGain, ... }
//   { kind: 'combo',  transfers: [{ sell, buy }, { sell, buy }], cost, xPtsGain, ... }
// Position-lock invariant: in every entry, sell.element_type === buy.element_type per leg.
```

Engine position filter — verified at line 117–122 (pre-existing, correct):

```typescript
for (const pos of POSITIONS) {                 // [1,2,3,4] = GK/DEF/MID/FWD
  const candidates = players
    .filter(p => p.element_type === pos && !ownedIds.has(p.id) && !cappedTeams.has(p.team))
    .sort((a, b) => scorePlayer(b) - scorePlayer(a))
    .slice(0, TOP_N_PER_POSITION)
  inPoolByPosition.set(pos, candidates)
}

// 1-FT enumeration (line 137-138):
for (const sell of currentPlayers) {
  const pool = inPoolByPosition.get(sell.element_type) ?? []  // position-locked
  // ...
}
```

Test factories already in suggest-transfers.test.ts — verified:

```typescript
type PlayerOverrides = Partial<MergedPlayer> & { id: number; element_type: 1 | 2 | 3 | 4 }
function makePlayer(overrides: PlayerOverrides): MergedPlayer { /* defaults */ }
function makePick(element: number, position: number): SquadPick { /* */ }
function makeValidSquad(): { picks: SquadPick[]; players: MergedPlayer[] } { /* 15-player squad */ }
```

Call site shapes (4 verified locations):
- TransferPanel.tsx line 122-133: ocsSuggestions useMemo wraps suggestTransfers({ currentPicks, players: scoredPlayers, horizon: ocsHorizon, ftCount: derivedFtCount, ... })
- OptimiserPanel.tsx line 271-281: transferSuggestions useMemo wraps suggestTransfers({ currentPicks, players: playersData, horizon, ftCount, ... })
- DecisionSummaryTab.tsx line 231-241: ocsSuggestions useMemo wraps suggestTransfers({ currentPicks, players: scoredPlayers, horizon: 1, ftCount: derivedFtCount, ... })
- RivalsTab.tsx line 82-92: transferSuggestions useMemo wraps suggestTransfers({ currentPicks, players: playersData, horizon: 1, ftCount: 1, ... })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FIX-02 position-lock characterization tests (single + combo)</name>
  <files>src/lib/suggest-transfers.test.ts</files>
  <read_first>
    - src/lib/suggest-transfers.test.ts (full file — note `makePlayer()` factory at lines 13–58, `makeValidSquad()` at lines 65–76, the existing `describe('Phase 45: suggestTransfers engine', ...)` block)
    - src/lib/suggest-transfers.ts (full file — engine pure-function shape; combo entries have `kind: 'combo'` with a `transfers: [{sell, buy}, {sell, buy}]` shape — verify by inspecting the combo enumeration around line 186+)
    - src/lib/types.ts (search for `TransferSuggestion` type — specifically the `combo` variant's `transfers` array shape)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md (section "src/lib/suggest-transfers.test.ts" — verified FIX-02 single regression snippet)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-11 engine-level coverage; D-12 TDD)
  </read_first>
  <behavior>
    Two new test cases append to the existing `describe('Phase 45: suggestTransfers engine', ...)` block under a comment banner `// Phase 111 FIX-02 — Position lock regression`:
    - "FIX-02 regression: single suggestions never produce a buy of different position than the sell" — call `makeValidSquad()`, inject 4 strong candidates one per position (id 20 GK, 21 DEF, 22 MID, 23 FWD, all xPts_1gw 7.5–9.0, distinct teams 10–13), call `suggestTransfers` with horizon=1, ftCount=1, bank=1000; filter `result.kind === 'single'`; assert `singles.length > 0`; for each single, assert `sug.sell.element_type === sug.buy.element_type`.
    - "FIX-02 regression: combo suggestions never mix positions in any leg" — same setup; filter `result.kind === 'combo'`; for each combo, iterate `combo.transfers` and assert `leg.sell.element_type === leg.buy.element_type` per leg. Skip the assertion if `combos.length === 0` (combos require ftCount≥2 or 2 affordable legs).
    Both tests MUST PASS already against the current engine — these are CHARACTERIZATION tests that lock existing correct behaviour. The TDD RED applies to Task 2's defensive guard.
  </behavior>
  <action>
    Edit `src/lib/suggest-transfers.test.ts`. After the last `it(...)` of the existing describe block but before its closing `})`, add the comment banner `Phase 111 FIX-02 — Position lock regression` and the two `it(...)` cases described in `<behavior>`. For both tests, use `makeValidSquad()` and add 4 strong candidates via `makePlayer({ id: 20+offset, element_type: pos, xPts_1gw: ..., team: 10+offset })`. The strong candidates must NOT collide with squad ids 1–15 or share teams in a way that triggers the 3-player-per-team cap. For the single test, narrow types using a type guard: `if (sug.kind === 'single') { expect(sug.sell.element_type).toBe(sug.buy.element_type) }`. For the combo test, narrow with `if (sug.kind === 'combo')`, then iterate `sug.transfers` (verify the field name by reading the engine — combo enumeration around line 186 in suggest-transfers.ts builds `transfers` array with `{sell, buy}` legs). Run the targeted test suite — both new tests SHOULD PASS immediately. Commit as `test(111-03): add FIX-02 position-lock characterization tests`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/suggest-transfers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File contains 2 new `it(` calls whose names start with `'FIX-02 regression:'`
    - File contains the comment banner string `Phase 111 FIX-02`
    - File contains the assertion pattern `expect(sug.sell.element_type).toBe(sug.buy.element_type)` (or equivalent narrowed form)
    - File contains a per-leg assertion for the combo case (e.g., `for (const leg of sug.transfers) { expect(leg.sell.element_type).toBe(leg.buy.element_type) }`)
    - `npx vitest run src/lib/suggest-transfers.test.ts` exits 0 with both new tests passing AND existing 46 still green
    - Git commit exists with message `test(111-03): add FIX-02 position-lock characterization tests`
  </acceptance_criteria>
  <done>Position-lock invariant locked by 2 regression tests; both green. Engine contract is now self-enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED then GREEN — add defensive element_type guard to suggestTransfers (D-09)</name>
  <files>src/lib/suggest-transfers.test.ts, src/lib/suggest-transfers.ts</files>
  <read_first>
    - src/lib/suggest-transfers.ts (full file — focus on lines 85–96 where `suggestTransfers` opens; the early-return guards and the `playerById` Map build at line 95)
    - src/lib/suggest-transfers.test.ts (the FIX-02 regression block written in Task 1 — append the new guard test below it)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-09 — defensive guard at point of root cause; the guard belongs in the engine because the engine is downstream of every call site and centralizes the position-lock contract)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md (Pitfall 6 — element_type cache currently clean but may drift; A1 — defensive guard as belt-and-braces against future corruption)
  </read_first>
  <behavior>
    A new test case in `suggest-transfers.test.ts` first asserts the desired guard behaviour (RED until guard is added):
    - "FIX-02 guard: invalid element_type players are filtered out before suggestion enumeration" — build a valid squad; inject one player with id=99 and `element_type` cast to an invalid value (`0 as unknown as 1`); spy on `console.warn`; call `suggestTransfers`; assert `console.warn` was called at least once with a message containing the substring `'FIX-02'` and the literal id `99`; assert that no returned suggestion (single or combo) has any `sell.id === 99` or `buy.id === 99`.
    Then in the engine: at the top of `suggestTransfers` (after the empty-array early-return on line 88, before the `field = HORIZON_FIELD[horizon]` on line 90), filter the input `players` array, log a single console.warn per invocation (not per bad player) listing the bad ids if any. The filtered array becomes the working `players` for the rest of the function.
    After the guard ships, all 3 FIX-02 tests are green AND the existing 46 are green.
  </behavior>
  <action>
    1. RED — In `src/lib/suggest-transfers.test.ts`, append a third FIX-02 test under the same banner:
       - Use `vi.spyOn(console, 'warn').mockImplementation(() => {})` in a try/finally that restores the spy.
       - Build squad and players via `makeValidSquad()`; add a corrupt player via `makePlayer({ id: 99, element_type: 0 as unknown as 1, xPts_1gw: 100, team: 15 })` (extreme xPts so it would dominate if not filtered).
       - Call `suggestTransfers({ currentPicks: picks, players: [...players, corruptPlayer], horizon: 1, ftCount: 1, bank: 1000 })`.
       - Assert `console.warn` was called with a string containing both `'FIX-02'` and `'99'`.
       - Assert no returned single or combo references id 99: iterate `result` and for each entry check `(s.kind === 'single' ? s.sell.id !== 99 && s.buy.id !== 99 : s.transfers.every(l => l.sell.id !== 99 && l.buy.id !== 99))`.
       - Run the test — it MUST FAIL because the guard does not yet exist.
       - Commit as `test(111-03): add failing test for engine element_type guard`.
    2. GREEN — In `src/lib/suggest-transfers.ts`:
       - Add a `VALID_ELEMENT_TYPES = new Set([1,2,3,4])` constant at module top (above `suggestTransfers` function).
       - Inside `suggestTransfers`, between the empty-array early-return (line 88) and the `field = HORIZON_FIELD[horizon]` line (90), compute `const invalidPlayers = players.filter(p => !VALID_ELEMENT_TYPES.has(p.element_type as number))`. If `invalidPlayers.length > 0`, call `console.warn(\`[FIX-02] suggestTransfers: dropping \${invalidPlayers.length} player(s) with invalid element_type: ids=\${invalidPlayers.map(p => p.id).join(',')}\`)`. Then introduce `const sanePlayers = invalidPlayers.length > 0 ? players.filter(p => VALID_ELEMENT_TYPES.has(p.element_type as number)) : players`. Replace subsequent references to `players` with `sanePlayers` for the `playerById` build (line 95) and the `inPoolByPosition` filter (line 118).
       - Add a code comment above the guard explaining: defensive guard per FIX-02 D-09; engine position lock at line 118 already filters by valid pos, but this guard surfaces upstream corruption explicitly.
    3. Run targeted test suite — all 3 FIX-02 tests green; existing 46 still green.
    4. Run full test suite + tsc.
    5. Commit as `feat(111-03): add defensive element_type guard to suggestTransfers (FIX-02 D-09)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/suggest-transfers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/suggest-transfers.ts` contains a `VALID_ELEMENT_TYPES` Set literal containing 1, 2, 3, and 4
    - `src/lib/suggest-transfers.ts` contains the substring `[FIX-02]` (the warn-message tag)
    - `src/lib/suggest-transfers.ts` contains a `sanePlayers` (or equivalently named) binding that filters by `VALID_ELEMENT_TYPES`
    - The `inPoolByPosition` filter (originally line 118) operates on the post-guard sanitized binding, not the raw `players` parameter
    - `src/lib/suggest-transfers.test.ts` contains 3 FIX-02 tests (single, combo, guard); the guard test contains a `console.warn` spy and asserts the warn call mentions `'FIX-02'` and `'99'`
    - `npx vitest run src/lib/suggest-transfers.test.ts` exits 0 with all 3 FIX-02 tests passing
    - `npx vitest run` exits 0 (full project suite green)
    - `npx tsc --noEmit` exits 0
    - Two git commits exist: `test(111-03): add failing test for engine element_type guard` and `feat(111-03): add defensive element_type guard to suggestTransfers (FIX-02 D-09)`
  </acceptance_criteria>
  <done>Engine guard live; corrupt-element_type players are filtered with a console.warn; 3 FIX-02 tests green; existing tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Annotate all 4 suggestTransfers call sites with FIX-02 comments</name>
  <files>src/components/transfers/TransferPanel.tsx, src/components/optimiser/OptimiserPanel.tsx, src/components/squad/DecisionSummaryTab.tsx, src/components/rivals/RivalsTab.tsx</files>
  <read_first>
    - src/components/transfers/TransferPanel.tsx (lines 115–135 — the `ocsSuggestions` useMemo wrapping `suggestTransfers`)
    - src/components/optimiser/OptimiserPanel.tsx (lines 263–282 — the `transferSuggestions` useMemo)
    - src/components/squad/DecisionSummaryTab.tsx (lines 225–242 — the `ocsSuggestions` useMemo)
    - src/components/rivals/RivalsTab.tsx (lines 75–93 — the `transferSuggestions` useMemo)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-07, D-08 — bug reported across surfaces; engine is the position-lock contract)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md (Pitfall 5 — engine returns ALL positions; the OCS table renders per-row sell/buy pairs from the engine and inherits position lock automatically)
  </read_first>
  <behavior>
    Each of the 4 call sites gains a single comment line directly above its `return suggestTransfers({...})` call (inside the useMemo body) of the form:
    `// FIX-02 (Phase 111 D-08): position lock is enforced inside suggestTransfers — engine guarantees sell.element_type === buy.element_type per leg. Do NOT pre-filter players by position; the engine builds top-30-per-position pools internally.`
    No behavioural change. No test additions needed (the engine-level tests in Tasks 1–2 cover the contract; this is documentation-as-code, making the contract self-evident at every consumption site so future edits don't accidentally pre-filter or strip the guarantee).
    Existing test suites continue to pass unchanged.
  </behavior>
  <action>
    1. In `src/components/transfers/TransferPanel.tsx`, locate the `ocsSuggestions` useMemo (around line 122) and insert the comment line directly above the `return suggestTransfers({` line (i.e., after the `if (!squadData ...)` early return). Use the exact comment text from `<behavior>`.
    2. Repeat for `src/components/optimiser/OptimiserPanel.tsx` `transferSuggestions` useMemo (around line 271) — insert comment above its `return suggestTransfers({` line.
    3. Repeat for `src/components/squad/DecisionSummaryTab.tsx` `ocsSuggestions` useMemo (around line 231).
    4. Repeat for `src/components/rivals/RivalsTab.tsx` `transferSuggestions` useMemo (around line 82).
    5. Run full test suite — must remain green (no behavioural change).
    6. Run tsc — no type errors (comments are inert).
    7. Verify all 4 files contain the FIX-02 token via grep (see verify command).
    8. Commit as `docs(111-03): annotate suggestTransfers call sites with FIX-02 position-lock contract`.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/transfers/TransferPanel.tsx` contains the substring `FIX-02` on a comment line directly above its `return suggestTransfers(`
    - `src/components/optimiser/OptimiserPanel.tsx` contains the substring `FIX-02` on a comment line directly above its `return suggestTransfers(`
    - `src/components/squad/DecisionSummaryTab.tsx` contains the substring `FIX-02` on a comment line directly above its `return suggestTransfers(`
    - `src/components/rivals/RivalsTab.tsx` contains the substring `FIX-02` on a comment line directly above its `return suggestTransfers(`
    - The annotation comment includes the literal phrase `position lock` and references that the engine enforces `element_type` matching
    - `npx vitest run` exits 0 (full suite green)
    - `npx tsc --noEmit` exits 0
    - Git commit exists with message `docs(111-03): annotate suggestTransfers call sites with FIX-02 position-lock contract`
  </acceptance_criteria>
  <done>All 4 call sites carry a self-documenting FIX-02 reference. Future engineers reading any call site see the position-lock contract inline; pre-filtering anti-pattern is explicitly forbidden in-place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/players (server) → usePlayers (client) | MergedPlayer[] JSON crosses network into React Query cache |
| MergedPlayer.element_type field → engine position filter | Untyped field at JSON-parse time becomes the position-lock key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Tampering | MergedPlayer.element_type field corruption | mitigate | Defensive guard in `suggestTransfers` (Task 2) filters any player whose `element_type` is not in {1,2,3,4} and emits a `[FIX-02]` console.warn naming the bad ids. Cross-position suggestions become structurally impossible regardless of upstream corruption. |
| T-111-08 | Repudiation | User reports cross-position suggestion but cause is unclear | mitigate | Three-pronged defence: (1) regression tests prove the engine cannot produce them under any inputs (Task 1), (2) defensive guard logs corruption with player ids when it does happen (Task 2), (3) call-site annotations document the contract inline so future code review catches violations early (Task 3). |
| T-111-09 | Denial of Service | Defensive guard adds O(N) filter per invocation | accept | Bounded by ~832 players × O(1) Set lookup per filter call = sub-millisecond overhead. The guard runs once per `suggestTransfers` invocation, which itself is a useMemo'd pure function. No measurable performance impact. |
</threat_model>

<verification>
- `npx vitest run src/lib/suggest-transfers.test.ts` — 46 existing + 3 new FIX-02 tests all green (49 total)
- `npx vitest run` — full project suite green
- `npx tsc --noEmit` — no type errors
- Manual probe (optional): start dev server, open Squad → Transfers tab; OCS table renders only same-position sell/buy pairs per row (engine contract upheld). Open browser console; if any future cache corruption introduces a bad element_type, a `[FIX-02]` warn fires on suggestTransfers invocation
</verification>

<success_criteria>
- Engine-level position-lock contract locked by 3 regression tests (single, combo, guard)
- Defensive guard at suggestTransfers entry filters invalid-element_type players with console.warn
- All 4 call sites carry FIX-02 annotations referencing the engine contract
- 4 commits land in TDD order: characterization tests, RED guard test, GREEN guard impl, audit annotations
- Existing 46 suggest-transfers tests untouched and green
- No type errors, no full-suite regressions
</success_criteria>

<output>
After completion, create `.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-03-SUMMARY.md` documenting:
- Files modified, line counts changed
- The 3 FIX-02 test cases added (one-line each)
- The defensive guard's location and trigger conditions
- The 4 call sites annotated, with the canonical comment text used
- Confirmation that no call site needed a pre-filter fix (engine already correct per RESEARCH A1)
- Note for /gsd-verify-work: FIX-02 is engine-contract-verified; user-perceptible test = open Transfers/OptimiserPanel/DecisionSummary/Rivals tabs and confirm every Sell-Buy pair in the OCS table shares element_type
</output>
