---
phase: 111
plan: 02
type: tdd
wave: 2
depends_on: [111-01]
files_modified:
  - src/components/club-form/FixtureHeatMap.tsx
  - src/components/club-form/FixtureHeatMap.test.tsx
autonomous: true
requirements:
  - FIX-01
tags: [fpl, heatmap, ui, react, tdd, fixtures]

must_haves:
  truths:
    - "When a team has played their current-GW fixture mid-week, the heatmap cell for that GW renders the difficulty color at opacity-40 with the opponent short-name label, not a blank BGW cell"
    - "The played-cell tooltip is exactly '[OPP] (H/A) — Played' (e.g., 'MCI (H) — Played')"
    - "True BGW cells (no fixture in either upcoming or played) are unchanged: blank, bg-zinc-50/dark:bg-zinc-900, title='No fixture (BGW)'"
    - "Played and BGW cells are visually distinguishable: played has a difficulty color + opponent text; BGW has neutral zinc background + no text"
    - "DGW played cells (2+ entries in current_gw_played for one team at one GW) reuse the existing split-gradient pattern wrapped with opacity-40, with tooltip joining each leg as 'OPP (H/A) — Played'"
    - "allEventIds includes event_ids drawn from current_gw_played so the current-GW column appears even when every team has played and no upcoming fixture remains for that GW"
  artifacts:
    - path: "src/components/club-form/FixtureHeatMap.tsx"
      provides: "Three-way render branch (BGW / played / normal) and grid useMemo extended with byTeamGwPlayed map"
      contains: "byTeamGwPlayed"
    - path: "src/components/club-form/FixtureHeatMap.test.tsx"
      provides: "Updated team() helper with playedFixtures param + 5 new FIX-01 test cases"
      contains: "Phase 111 FIX-01"
  key_links:
    - from: "src/components/club-form/FixtureHeatMap.tsx"
      to: "ClubForm.current_gw_played (from Plan 01)"
      via: "data.flatMap(t => (t.current_gw_played ?? []).map(...)) inside grid useMemo"
      pattern: "current_gw_played"
    - from: "FixtureHeatMap.tsx HeatMapRow"
      to: "byTeamGwPlayed map"
      via: "grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []"
      pattern: "byTeamGwPlayed\\.get"
---

<objective>
Land the FIX-01 rendering layer in `FixtureHeatMap.tsx`: extend the `grid` useMemo to build a second map `byTeamGwPlayed` from `ClubForm.current_gw_played` (delivered by Plan 01), include played event_ids in `allEventIds`, and replace the `fixtures.length === 0` BGW branch with a three-way decision (true BGW / played single / played DGW). Tests are written first (D-12 RED→GREEN) by extending the existing `FixtureHeatMap.test.tsx` (D-10 — extend, do not create new files).

Purpose: Closes the FIX-01 user story end-to-end. After this plan, a manager looking at the heatmap mid-week sees their team's played GW35 fixture as a dimmed coloured cell with the opponent short name — not a misleading blank BGW.

Output: FixtureHeatMap renders played cells distinct from BGW cells, with a verified tooltip and visual contract that matches the locked UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-RESEARCH.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-UI-SPEC.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-01-PLAN.md

<interfaces>
<!-- After Plan 01 ships, ClubForm has current_gw_played. -->
From src/lib/types.ts (post-Plan-01):
```typescript
export interface ClubForm {
  team_id: number
  team_name: string
  team_short_name: string
  // ...
  upcoming_fixtures: ClubFormFixture[]
  current_gw_played: ClubFormFixture[]   // ← required, populated by computeClubForm
  // ...
}
```

<!-- Existing FixtureHeatMap pattern excerpts (verified). -->
From src/components/club-form/FixtureHeatMap.tsx (current state):
```typescript
// Line 14–18 — TIER_CLASSES (reuse for played cell)
const TIER_CLASSES: Record<DifficultyTier, string> = {
  easy:   'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200',
  medium: 'bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200',
  hard:   'bg-red-100   text-red-800   dark:bg-red-900   dark:text-red-200',
}

// Line 32–34 — currentTier helper (reuse)
function currentTier(f: ClubFormFixture, mode: 'ATT' | 'DEF'): DifficultyTier {
  return mode === 'ATT' ? f.difficulty_tier : tier(f.defensive_difficulty)
}

// Line 42–48 — HeatMapRowProps interface (extend `grid` shape)
interface HeatMapRowProps {
  t: ClubForm
  grid: { allEventIds: number[]; byTeamGw: Map<number, Map<number, ClubFormFixture[]>> }
  mode: 'ATT' | 'DEF'
  tierMap: Record<DifficultyTier, string>
  ownedTeamIds: Set<number>
}

// Line 82–91 — current BGW branch to be split into three-way decision
{grid.allEventIds.map(gw => {
  const fixtures = grid.byTeamGw.get(t.team_id)?.get(gw) ?? []
  if (fixtures.length === 0) {
    return (<td ... title="No fixture (BGW)" />)
  }
  // ... DGW branch (fixtures.length >= 2) and single branch follow
})}

// Line 174–193 — grid useMemo (extend allEventIds + add byTeamGwPlayed)
const grid = useMemo(() => {
  if (!data || data.length === 0) return null
  const allEventIds = Array.from(
    new Set(data.flatMap(t => t.upcoming_fixtures.map(f => f.event_id)))
  ).sort((a, b) => a - b).slice(0, horizon)
  const byTeamGw = new Map<number, Map<number, ClubFormFixture[]>>()
  for (const t of data) { /* loop populates byTeamGw from upcoming_fixtures */ }
  return { allEventIds, byTeamGw, sortedTeams }
}, [data, horizon])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend FixtureHeatMap.test.tsx with played-cell test cases</name>
  <files>src/components/club-form/FixtureHeatMap.test.tsx</files>
  <read_first>
    - src/components/club-form/FixtureHeatMap.test.tsx (full 421 lines — note `fix()` helper at lines 24–34, `team()` helper at lines 36–50, beforeEach mock setup at 61–67, the existing describe block structure at 69+)
    - src/components/club-form/FixtureHeatMap.tsx (current state of HeatMapRow rendering, especially the 3 cell branches: BGW at 84–91, DGW at 93–120, single at 122–133)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md (section "src/components/club-form/FixtureHeatMap.test.tsx" — five new test snippets, verified)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-UI-SPEC.md (sections "FIX-01: Played Cell Color Contract", "Component Inventory", "Interaction Contract", "States to Implement")
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <behavior>
    The existing `team(id, short, fixtures)` helper grows a third optional param `playedFixtures: ClubFormFixture[] = []` and includes `current_gw_played: playedFixtures` in the returned literal. Five new test cases are appended at the end of the existing `describe('FixtureHeatMap', ...)` block under a comment banner `// Phase 111 FIX-01 — Played cell rendering`:
    - "FIX-01: played cell renders with difficulty color at opacity-40 and \"— Played\" tooltip" — render data with one team that played gw35 (no upcoming gw35) and one team with upcoming gw35; assert the played team's first cell has `title === 'MCI (H) — Played'`, className matches `/opacity-40/`, className matches `/bg-green-100|bg-green-900/` (difficulty color preserved).
    - "FIX-01: played cell is visually distinct from BGW cell (not bg-zinc-50, not 'No fixture (BGW)')" — same setup; assert the played cell className does NOT match `/bg-zinc-50/` and title is NOT `'No fixture (BGW)'`.
    - "FIX-01: true BGW cell unchanged — blank, bg-zinc-50, 'No fixture (BGW)'" — render a team with empty upcoming AND empty played; assert className matches `/bg-zinc-50/`, title is `'No fixture (BGW)'`, textContent trims to empty string.
    - "FIX-01: allEventIds includes played GW event_id when all teams have played (no upcoming)" — render two teams both with empty upcoming gw35 and one played gw35 entry each; assert the rendered thead column headers contain `'GW35'`.
    - "FIX-01: DGW played cell uses split-cell gradient with opacity-40" — render a team with two played gw35 entries; assert the cell className matches `/opacity-40/` AND the inline `style` attribute contains the substring `'linear-gradient'`.
    Existing tests still pass (the `team()` helper change is backward-compatible because the new param defaults to `[]`).
    All 5 new tests MUST FAIL initially (component does not yet read current_gw_played).
  </behavior>
  <action>
    1. Edit `src/components/club-form/FixtureHeatMap.test.tsx`. Modify the `team()` helper signature (currently line 36) to `function team(id: number, short: string, fixtures: ClubFormFixture[], playedFixtures: ClubFormFixture[] = []): ClubForm` and add the literal field `current_gw_played: playedFixtures,` immediately after the existing `upcoming_fixtures: fixtures,` line inside the returned object. No other change to the helper. Existing call sites (which pass only 3 args) continue to work because the new param has a default.
    2. Append 5 new `it(...)` blocks at the end of the existing `describe('FixtureHeatMap', ...)` block (just before the final closing brace of the describe). Use the names exactly as listed in `<behavior>`. Use the existing `fix()` helper to build ClubFormFixture entries — pass `tier: 'easy'` for the played-cell green-color assertion.
    3. For the cell-selection logic, recall: `allEventIds` is sorted ascending and sliced to horizon (default 8). When a team has only a played gw35 entry and the other team has only an upcoming gw35 entry, the columns are `[35, ...]` so `tbody tr:nth-child(N) td` indexes the played team's gw35 cell at index 0. Use `container.querySelectorAll('tbody tr')` to find rows; rows are sorted alphabetically by team_short_name (existing behaviour at line 189), so name teams accordingly to control row order (e.g., `team(1, 'AAA', ...)` first, `team(2, 'BBB', ...)` second).
    4. Run `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — the 5 new tests MUST FAIL (rendering of played cells is not yet implemented; the test queries get either a BGW cell or no cell where the GW column is missing).
    5. Commit as `test(111-02): add failing tests for FixtureHeatMap played cell rendering`.
  </action>
  <verify>
    <automated>npx vitest run src/components/club-form/FixtureHeatMap.test.tsx 2>&1 | grep -E "FAIL|failed|×"</automated>
  </verify>
  <acceptance_criteria>
    - `team()` helper signature in `FixtureHeatMap.test.tsx` includes `playedFixtures: ClubFormFixture[] = []` as the 4th parameter
    - Helper return literal contains the substring `current_gw_played: playedFixtures,`
    - File contains 5 new `it(` calls whose names start with `'FIX-01:'`
    - File contains the comment banner string `Phase 111 FIX-01`
    - `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` exits non-zero with 5 of the new tests failing (existing 46 still pass)
    - Git commit exists with message `test(111-02): add failing tests for FixtureHeatMap played cell rendering`
  </acceptance_criteria>
  <done>5 new tests committed and FAILING; the existing 46 still pass. RED phase complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement byTeamGwPlayed map + three-way render branch in FixtureHeatMap.tsx</name>
  <files>src/components/club-form/FixtureHeatMap.tsx</files>
  <read_first>
    - src/components/club-form/FixtureHeatMap.tsx (full file — focus on grid useMemo at 174–193, HeatMapRowProps at 42–48, BGW branch at 82–91, DGW branch at 93–120, single branch at 122–133)
    - src/components/club-form/FixtureHeatMap.test.tsx (the 5 new tests written in Task 1 — implementation must satisfy each assertion exactly: tooltip string `'MCI (H) — Played'`, className tokens `opacity-40` and `bg-green-100`/`bg-green-900`, BGW unchanged, GW35 column header present, DGW gradient with opacity-40)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-PATTERNS.md (section "src/components/club-form/FixtureHeatMap.tsx" — verified code patterns for HeatMapRowProps extension, useMemo extension, three-way branch including DGW played split-gradient)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-UI-SPEC.md (Component Inventory: cell sizing locked to h-8 single / h-10 DGW; opacity-40 on outer td; opponent label uses text-xs font-mono; BGW unchanged)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-04 opacity, D-05 tooltip, D-06 visual distinctness; Claude's Discretion: DGW played reuses split-cell pattern with opacity)
  </read_first>
  <behavior>
    After this task, all 5 new tests pass and all 46 existing tests still pass. The `HeatMapRowProps.grid` shape declares `byTeamGwPlayed: Map<number, Map<number, ClubFormFixture[]>>`. The `grid` useMemo populates `byTeamGwPlayed` by iterating `t.current_gw_played` (defaulting to `[]` for backward-compat with any cached data missing the field) and includes played event_ids in the union that becomes `allEventIds`. The cell-render `map` inside `HeatMapRow` looks up `playedFixtures = grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []` and decides:
    - `fixtures.length === 0 && playedFixtures.length === 0` → unchanged true-BGW cell (`bg-zinc-50 dark:bg-zinc-900`, `title="No fixture (BGW)"`)
    - `fixtures.length === 0 && playedFixtures.length === 1` → single played cell: className `px-2 py-1 text-center min-w-[48px] h-8 ${TIER_CLASSES[currentTier(f, mode)]} opacity-40`, title `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — Played`, content `<span className="text-xs font-mono">{f.opponent_team}</span>`
    - `fixtures.length === 0 && playedFixtures.length >= 2` → DGW played cell: same gradient pattern as the existing upcoming DGW branch (linear-gradient on `style.background`, two/three labels with absolute positioning, h-10) wrapped on a `<td>` whose className includes `opacity-40`; tooltip joins each leg's `'OPP (H/A) — Played'` strings with `' / '`
    - `fixtures.length >= 1` → existing branches (DGW upcoming or single upcoming) unchanged
  </behavior>
  <action>
    1. Update `HeatMapRowProps.grid` shape (current line 44) to add `byTeamGwPlayed: Map<number, Map<number, ClubFormFixture[]>>` after `byTeamGw`. The `mode`, `tierMap`, `ownedTeamIds` props are unchanged.
    2. Inside `HeatMapRow`'s `grid.allEventIds.map(gw => {...})` (current line 82), compute `const playedFixtures = grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []` immediately after the existing `const fixtures = ...` line. Replace the existing `if (fixtures.length === 0)` branch (lines 84–91) with the three-way decision: if both empty → existing BGW cell; if `fixtures.length === 0 && playedFixtures.length >= 2` → DGW played cell (mirror the existing DGW gradient code at lines 93–120 but wrap with `opacity-40` in className and rebuild the tooltip with each leg's `(H/A) — Played` suffix joined by ` / `); else if `fixtures.length === 0 && playedFixtures.length === 1` → single played cell using the exact className `px-2 py-1 text-center min-w-[48px] h-8 ${TIER_CLASSES[currentTier(f, mode)]} opacity-40` with title `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — Played` and content `<span className="text-xs font-mono">{f.opponent_team}</span>`. Leave the existing `if (fixtures.length >= 2)` and trailing single branches unchanged below.
    3. Extend the `grid` useMemo (current lines 174–193). Build `allEventIds` from the union of `data.flatMap(t => t.upcoming_fixtures.map(f => f.event_id))` AND `data.flatMap(t => (t.current_gw_played ?? []).map(f => f.event_id))` (use `?? []` defensively in case any cached data still lacks the field). Build a new `byTeamGwPlayed` Map of the same shape as `byTeamGw` but iterating `t.current_gw_played ?? []`. Return `{ allEventIds, byTeamGw, byTeamGwPlayed, sortedTeams }`. The existing `HeatMapRow` invocation at lines 264–272 passes `grid` by reference so no prop-call change is needed at the call site.
    4. Run `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — all 51 tests must pass (46 existing + 5 new).
    5. Run `npx vitest run` to confirm full suite green. Run `npx tsc --noEmit` to confirm no type errors.
    6. Commit as `feat(111-02): render played current-GW cells dimmed and distinct from BGW`.
  </action>
  <verify>
    <automated>npx vitest run src/components/club-form/FixtureHeatMap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `HeatMapRowProps.grid` type contains the literal `byTeamGwPlayed: Map<number, Map<number, ClubFormFixture[]>>`
    - `FixtureHeatMap.tsx` grid useMemo `allEventIds` computation includes the substring `current_gw_played` (the union with played event_ids)
    - `FixtureHeatMap.tsx` grid useMemo creates a Map literal named `byTeamGwPlayed`
    - `FixtureHeatMap.tsx` HeatMapRow contains the substring `playedFixtures = grid.byTeamGwPlayed`
    - `FixtureHeatMap.tsx` contains the substring `— Played` (the tooltip suffix from D-05)
    - `FixtureHeatMap.tsx` contains the substring `opacity-40`
    - `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` exits 0 with 51 tests passing (46 existing + 5 new)
    - `npx vitest run` exits 0 (full project suite green)
    - `npx tsc --noEmit` exits 0
    - Git commit exists with message `feat(111-02): render played current-GW cells dimmed and distinct from BGW`
  </acceptance_criteria>
  <done>FIX-01 fully shipped end-to-end: data flows from computeClubForm via Plan 01 through useClubForm into the new render branch. Manager sees dimmed coloured cell (with opponent label + "— Played" tooltip) for teams that played mid-week, while true BGW remains a blank zinc cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/club-form (server) → useClubForm (client) | ClubForm[] JSON crosses the network boundary into the React Query cache |
| ClubForm.opponent_team string → DOM attribute (title, span text) | Untyped string from JSON renders into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Tampering | ClubForm.current_gw_played array shape | accept | Source is our own /api/club-form route (Plan 01); shape locked by RawEvent + RawBootstrap interfaces. The component uses `t.current_gw_played ?? []` defensively for backward compat with any stale cache lacking the field. |
| T-111-05 | Information Disclosure | opponent_team rendered into title attribute and span text | accept | opponent_team is a 3-letter PL team short name (already exposed throughout the app). React JSX text-node and title-attribute insertion automatically escapes HTML; no XSS surface introduced. |
| T-111-06 | Repudiation | Visual signal "Played" misleading user about real fixture state | mitigate | Tooltip contains the literal "— Played" string per D-05; cell label shows opponent short name; both signals together prevent misreading. UI-SPEC checker sign-off required (Dimensions 1, 3, 4) per existing UI-SPEC.md. |
</threat_model>

<verification>
- `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — 46 existing + 5 new tests all green (51 total)
- `npx vitest run` — full project suite green
- `npx tsc --noEmit` — no type errors
- Manual probe (recommended once Plans 01 + 02 ship): start dev server, load the GW Heatmap tab. Mid-week (current GW=35), teams that have played show a dimmed-coloured cell with opponent short name and "— Played" tooltip; teams with no GW35 fixture show a blank zinc cell with "No fixture (BGW)" tooltip
</verification>

<success_criteria>
- Three-way cell render branch live in FixtureHeatMap.tsx: true BGW / played single / played DGW / normal
- byTeamGwPlayed map and extended allEventIds union live in grid useMemo
- 5 new tests committed and GREEN
- No regression in existing 46 FixtureHeatMap tests
- No type errors, no full-suite regressions
- All commits land in TDD RED→GREEN order (D-12)
</success_criteria>

<output>
After completion, create `.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-02-SUMMARY.md` documenting:
- Files modified, line counts changed
- The 5 test cases added (one-line each)
- Confirmation that FIX-01 is fully closed end-to-end (data + render)
- Any deviation from plan (expected: none if Plan 01 shipped cleanly)
- Note for /gsd-verify-work: FIX-01 user-perceptible verification = "load heatmap, current-GW played teams show dimmed coloured cell with opponent label, true BGW shows blank zinc"
</output>
