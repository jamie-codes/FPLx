---
phase: 111
plan: 04
type: tdd
wave: 3
depends_on: [111-02, 111-03]
gap_closure: true
files_modified:
  - src/components/club-form/FixtureHeatMap.tsx
  - src/components/club-form/FixtureHeatMap.test.tsx
  - src/lib/club-form.ts
  - src/lib/club-form.test.ts
autonomous: true
requirements:
  - FIX-01
tags: [fpl, heatmap, ui, react, tdd, fixtures, gap-closure, dgw]

must_haves:
  truths:
    - "When a team is in a DGW and has played one leg AND still has one upcoming leg in the same GW (fixtures.length >= 1 AND playedFixtures.length >= 1), the heatmap cell renders the upcoming fixture at full opacity AND surfaces the played leg via a tooltip suffix that contains the played opponent's short name plus the literal '— Played' marker"
    - "The mixed-state (partially-played DGW) cell tooltip joins the upcoming fixture's tooltip and each played leg's tooltip with ' / ' as the separator, so the player can read both fixtures from a single hover (e.g., 'PSG (H) — 0.28 / ARS (H) — Played')"
    - "The played fixture signal is never silently discarded — any GW where playedFixtures.length >= 1 produces at least one DOM-visible artefact (in the title attribute and/or label) regardless of whether fixtures.length is also >= 1"
    - "The CR-02 fallback-GW derivation in club-form.ts sorts events by id ascending before slicing the last finished event, so a misordered FPL bootstrap.events array can no longer cause the wrong GW to be picked"
  artifacts:
    - path: "src/components/club-form/FixtureHeatMap.tsx"
      provides: "Four-way render branch in HeatMapRow cell map: true BGW / single-played / DGW-all-played / partially-played-DGW (mixed) / upcoming-DGW / upcoming-single"
      contains: "const playedSuffix = playedFixtures"
    - path: "src/components/club-form/FixtureHeatMap.test.tsx"
      provides: "Two new partially-played DGW test cases (single upcoming + 1 played; single upcoming + 2 played) that lock the mixed-state tooltip contract"
      contains: "FIX-01 (gap): partially-played DGW"
    - path: "src/lib/club-form.ts"
      provides: "Hardened fallback that sorts events by id ascending before .slice(-1)[0]"
      contains: ".sort((a, b) => a.id - b.id)"
  key_links:
    - from: "src/components/club-form/FixtureHeatMap.tsx HeatMapRow"
      to: "byTeamGwPlayed map (built in Plan 02)"
      via: "grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []"
      pattern: "byTeamGwPlayed\\.get"
    - from: "HeatMapRow mixed-state branch"
      to: "playedFixtures + fixtures tooltip composition"
      via: "title attribute joining upcoming tooltip and played tooltips with ' / '"
      pattern: "— Played"
    - from: "src/lib/club-form.ts currentGw fallback"
      to: "bootstrap.events array"
      via: "events.filter(e => e.finished).sort((a, b) => a.id - b.id).slice(-1)[0]?.id"
      pattern: "a\\.id - b\\.id"
---

<objective>
Close the single BLOCKER gap in `.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-VERIFICATION.md`: the `FixtureHeatMap.tsx` `HeatMapRow` cell render map currently has no branch for the partially-played DGW case (`fixtures.length >= 1 && playedFixtures.length >= 1`). Mid-DGW, after one leg has been played and one upcoming leg remains in the same GW, the code falls through to the existing single-upcoming branch (current line 166) and the `playedFixtures` signal is silently dropped. This plan adds the missing branch via TDD RED→GREEN, and opportunistically hardens the CR-02 fallback-sort issue in `src/lib/club-form.ts` flagged in VERIFICATION.md as a WARNING.

Purpose: After this plan, a manager looking at the heatmap mid-DGW sees the upcoming leg rendered normally AND a clear tooltip signal that a game has already been played in the same GW. Fully closes FIX-01.

Output: Four-way render branch in `HeatMapRow`, two new locking tests for the mixed state, and a sorted fallback in `club-form.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-VERIFICATION.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-UI-SPEC.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-02-PLAN.md
@.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-02-SUMMARY.md

<interfaces>
<!-- Current state of FixtureHeatMap.tsx after Plan 02 ships (verified by direct read 2026-05-15). -->

From src/components/club-form/FixtureHeatMap.tsx (current line numbers):

```typescript
// Lines 14–18 — TIER_CLASSES (reuse for single mixed-state branch)
const TIER_CLASSES: Record<DifficultyTier, string> = {
  easy:   'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200',
  medium: 'bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200',
  hard:   'bg-red-100   text-red-800   dark:bg-red-900   dark:text-red-200',
}

// Lines 32–34 — currentTier helper (reuse)
function currentTier(f: ClubFormFixture, mode: 'ATT' | 'DEF'): DifficultyTier {
  return mode === 'ATT' ? f.difficulty_tier : tier(f.defensive_difficulty)
}

// Lines 86–88 — current cell-render entry point (the map iterating allEventIds)
{grid.allEventIds.map(gw => {
  const fixtures = grid.byTeamGw.get(t.team_id)?.get(gw) ?? []
  const playedFixtures = grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []
  // ... 5 existing branches; missing: fixtures.length >= 1 && playedFixtures.length >= 1
})}

// Lines 137–164 — existing DGW (upcoming) branch (fixtures.length >= 2)
// Lines 166–177 — existing single-upcoming branch (the fall-through that silently drops playedFixtures today)
```

From src/lib/club-form.ts (current line numbers):

```typescript
// Lines 137–141 — currentGw derivation with the CR-02 fallback-sort issue
const currentGw: number | null =
  bootstrap.events?.find(e => e.is_current)?.id ??
  bootstrap.events?.filter(e => e.finished).slice(-1)[0]?.id ??
  null
```

From src/components/club-form/FixtureHeatMap.test.tsx (current state):

```typescript
// Line 36 — team() helper signature (already extended in Plan 02)
function team(id: number, short: string, fixtures: ClubFormFixture[], playedFixtures: ClubFormFixture[] = []): ClubForm

// Line 24 — fix() helper produces ClubFormFixture
function fix(opts: { opp: string; home: boolean; gw: number; tier: DifficultyTier; ad?: number }): ClubFormFixture

// Lines 422–495 — existing FIX-01 played-cell tests (do not duplicate; append new banner)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add failing tests for partially-played DGW (mixed-state) tooltip contract</name>
  <files>src/components/club-form/FixtureHeatMap.test.tsx</files>
  <read_first>
    - src/components/club-form/FixtureHeatMap.test.tsx (full file — note `fix()` helper at lines 24–34, `team()` helper at lines 36–51 already accepts `playedFixtures` as 4th arg, the existing FIX-01 banner at line 423 and the 5 existing FIX-01 tests at lines 426–495 to ensure no duplication)
    - src/components/club-form/FixtureHeatMap.tsx (full file — confirm the existing 5 cell-render branches at lines 86–177 to understand what shape the new branch must produce, especially the single-upcoming tooltip at line 168: `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — ${(diff ?? 0).toFixed(2)}`)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-VERIFICATION.md (the gap description at lines 7–18, especially the `missing` items and the human_verification entry at lines 19–22)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-04 opacity, D-05 played-cell tooltip format `"[OPP] (H/A) — Played"`, D-06 visual distinctness; "Claude's Discretion" item: "if a team played both legs of a DGW mid-week, render each played leg in current_gw_played (same split-cell pattern as upcoming DGW, dimmed)" — the mixed state extends that pattern)
  </read_first>
  <behavior>
    Append a new banner comment `// Phase 111 FIX-01 (gap) — Partially-played DGW (mixed state) — CR-01 closure` at the bottom of the existing `describe('FixtureHeatMap', ...)` block (after the existing FIX-01 tests at line 495, before the closing `})` of the describe at line 496). Add two new `it(...)` cases that lock the mixed-state tooltip contract:

    1. **"FIX-01 (gap): partially-played DGW (1 upcoming + 1 played) tooltip contains both opponents and '— Played' marker"** — Render a team `ARS` whose only `gw=35` data is one upcoming fixture (opp `PSG`, home, easy, ad=0.28) AND one played fixture (opp `MCI`, home, easy). Assert:
       - `arsCell.getAttribute('title')` equals exactly `'PSG (H) — 0.28 / MCI (H) — Played'` (upcoming tooltip first, played leg(s) appended after ' / ')
       - `arsCell.className` matches `/bg-green-100|bg-green-900/` (full-opacity upcoming color is the dominant background)
       - `arsCell.className` does NOT match `/opacity-40/` (the cell is NOT dimmed — the upcoming leg is still actionable)
       - `arsCell.querySelector('span')?.textContent` equals `'PSG'` (the upcoming opponent label, not the played one)
       - `arsCell.className` does NOT match `/bg-zinc-50/` (the cell is NOT a BGW)

    2. **"FIX-01 (gap): partially-played DGW (1 upcoming + 2 played) tooltip lists all three opponents in order"** — Render a team `BHA` whose `gw=35` data is one upcoming fixture (opp `LIV`, away, hard, ad=0.71) AND two played fixtures (opp `MCI` home easy, opp `CHE` away hard). Assert:
       - `bhaCell.getAttribute('title')` equals exactly `'LIV (A) — 0.71 / MCI (H) — Played / CHE (A) — Played'`
       - `bhaCell.querySelector('span')?.textContent` equals `'LIV'` (the upcoming opponent label dominates)
       - `bhaCell.className` does NOT match `/opacity-40/`

    Both tests MUST FAIL initially because the partially-played DGW branch does not yet exist — the code currently falls through to the `fixtures.length >= 1` upcoming-single branch (line 166) which emits the tooltip `'PSG (H) — 0.28'` only (no `— Played` suffix) and discards `playedFixtures` entirely.
  </behavior>
  <action>
    1. Edit `src/components/club-form/FixtureHeatMap.test.tsx`. Locate the closing `})` of the outer `describe('FixtureHeatMap', ...)` block at line 496. Immediately above that closing brace (i.e., after the last existing FIX-01 test `it('FIX-01: DGW played cell uses split-cell gradient with opacity-40', ...)` which ends at line 495), insert a new comment banner block exactly matching:

       ```
         // ===========================================================================
         // Phase 111 FIX-01 (gap) — Partially-played DGW (mixed state) — CR-01 closure
         // ===========================================================================
       ```

    2. Below that banner, add two new `it(...)` blocks using the EXACT names listed in `<behavior>`:
       - Test 1 name string: `'FIX-01 (gap): partially-played DGW (1 upcoming + 1 played) tooltip contains both opponents and \'— Played\' marker'`
       - Test 2 name string: `'FIX-01 (gap): partially-played DGW (1 upcoming + 2 played) tooltip lists all three opponents in order'`

       Use the existing `fix()` helper to build `ClubFormFixture` entries — pass explicit `ad` values where asserted. Use the existing `team()` helper's 4-arg form: `team(1, 'ARS', [upcomingFix], [playedFix])`. Render via `render(<FixtureHeatMap />)` after `mockUseClubForm.mockReturnValue({ data, isLoading: false, error: null })`. Select the cell at `tbody tr:nth-child(1) td:nth-child(2)` (first data column = GW35 cell, since the row header is `th[scope="row"]` at column 1 — but matching the pattern at line 110 `arsCell = arsRow.querySelectorAll('td')[0]` is simpler and consistent with the existing tests). Use `arsRow.querySelectorAll('td')[0]` to grab the GW35 cell.

    3. For the assertions, use the existing `expect(arsCell.getAttribute('title')).toBe(...)` pattern from line 128. Assert tooltip strings as exact-match `.toBe(...)` (not `.toContain` — exact contract). Assert className via `.toMatch(/.../)` and `.not.toMatch(/.../)` (existing pattern at lines 113–118). Assert label via `cell.querySelector('span')?.textContent` (existing pattern at line 224).

    4. Run `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` from the repo root. The 2 new tests MUST FAIL — the cell currently emits the single-upcoming branch's title (no `— Played` suffix). Verify the failure message names both new tests.

    5. Commit as `test(111-04): add failing tests for partially-played DGW mixed-state cell`. Do NOT include a Co-Authored-By trailer (per project CLAUDE.md).
  </action>
  <verify>
    <automated>npx vitest run src/components/club-form/FixtureHeatMap.test.tsx 2>&1 | grep -E "FAIL|failed|×|partially-played"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/club-form/FixtureHeatMap.test.tsx` contains the substring `Phase 111 FIX-01 (gap)` exactly once
    - File contains the substring `FIX-01 (gap): partially-played DGW (1 upcoming + 1 played)`
    - File contains the substring `FIX-01 (gap): partially-played DGW (1 upcoming + 2 played)`
    - File contains the substring `'PSG (H) — 0.28 / MCI (H) — Played'` (the exact tooltip assertion for test 1)
    - File contains the substring `'LIV (A) — 0.71 / MCI (H) — Played / CHE (A) — Played'` (the exact tooltip assertion for test 2)
    - `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` exits non-zero with output containing both new test names listed as failing; the existing 51 tests (46 pre-FIX-01 + 5 FIX-01) still pass
    - Git log contains a commit with message starting with `test(111-04): add failing tests for partially-played DGW`
    - Git commit contains NO `Co-Authored-By` trailer
  </acceptance_criteria>
  <done>Two new tests committed and FAILING for the right reason: the cell currently emits the upcoming-single tooltip with no played signal. Existing 51 tests still pass. RED phase complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add fixtures.length >= 1 && playedFixtures.length >= 1 render branch to HeatMapRow</name>
  <files>src/components/club-form/FixtureHeatMap.tsx</files>
  <read_first>
    - src/components/club-form/FixtureHeatMap.tsx (full file — must read the CURRENT state of HeatMapRow's cell-render map at lines 86–177. The new branch must be inserted BEFORE the existing `if (fixtures.length >= 2)` check at line 137. Existing line numbers post-Plan-02: BGW at 89–98, DGW all-played at 99–123, single played at 124–136, DGW upcoming at 137–165, single upcoming at 166–177.)
    - src/components/club-form/FixtureHeatMap.test.tsx (the two new tests written in Task 1 — implementation MUST produce the exact tooltip strings asserted: `'PSG (H) — 0.28 / MCI (H) — Played'` and `'LIV (A) — 0.71 / MCI (H) — Played / CHE (A) — Played'`; and the className must NOT contain `opacity-40`)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-VERIFICATION.md (gap description and the `missing` list — the branch must "show upcoming fixture at full opacity with a visual indicator that confirms a game has already been played")
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (D-05 tooltip format for played legs is `"[OPP] (H/A) — Played"`; "Claude's Discretion" allows the exact mixed-state visual)
  </read_first>
  <behavior>
    After this task all 2 new tests pass and all 51 existing tests still pass (53 total). The chosen visual for the mixed state is: render the upcoming fixture exactly as the existing single-upcoming branch does (same className, same color tier, same label span, NO `opacity-40`) — and append the played leg(s) to the tooltip as `' / OPP (H/A) — Played'` segments. This keeps the upcoming fixture visually dominant and actionable (the user can still read the difficulty color and opponent at a glance) while ensuring the played leg is never silently discarded — it surfaces via the title attribute on hover.

    Rationale for choosing the tooltip-only treatment (within "Claude's Discretion" per CONTEXT.md D-04/D-05/D-06):
    - The upcoming leg is still actionable (player rotation decisions matter) — dimming the cell would imply the GW is over, which is wrong.
    - A split-cell gradient mixing upcoming-color with dimmed-played-color would clash with the existing DGW-upcoming gradient (which uses both halves for upcoming legs at different difficulties). Reusing that pattern would semantically overload the gradient.
    - The tooltip suffix is consistent with D-05 (`'— Played'` marker) and with the existing single-played branch — both signals use the same string.
    - This is the lowest-risk delta to Plan 02's contract and the only one that does not require additional UI-SPEC negotiation.

    The DGW-upcoming branch at line 137 (`fixtures.length >= 2`) is unchanged — the new branch is ONLY for the case where `fixtures.length === 1` AND `playedFixtures.length >= 1` (a partially-played DGW where exactly one upcoming leg remains). For the theoretical case `fixtures.length >= 2 && playedFixtures.length >= 1` (e.g., a triple-game-week with one already played) the existing `fixtures.length >= 2` branch handles the two upcoming legs and the played leg is appended to the tooltip there too — but that requires a tooltip change in the DGW-upcoming branch as well. **Scope decision**: handle only the `fixtures.length === 1 && playedFixtures.length >= 1` case in this gap-closure; the TGW-mixed case is out of scope (no real-world FPL precedent and not in VERIFICATION.md's gap list).
  </behavior>
  <action>
    1. Open `src/components/club-form/FixtureHeatMap.tsx`. Locate the existing single-upcoming branch (the final fall-through) at lines 166–177:

       ```
       const f = fixtures[0]
       const diff = mode === 'ATT' ? f.attacking_difficulty : f.defensive_difficulty
       const tooltip = `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — ${(diff ?? 0).toFixed(2)}`
       return (
         <td
           key={gw}
           className={`px-2 py-1 text-center min-w-[48px] h-8 ${TIER_CLASSES[currentTier(f, mode)]}`}
           title={tooltip}
         >
           <span className="text-xs font-mono">{f.opponent_team}</span>
         </td>
       )
       ```

       Modify ONLY the `tooltip` const at line 168 to conditionally append played-leg suffixes when `playedFixtures.length >= 1`. Replace the single line:

       ```
       const tooltip = `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — ${(diff ?? 0).toFixed(2)}`
       ```

       with:

       ```
       const baseTooltip = `${f.opponent_team} (${f.is_home ? 'H' : 'A'}) — ${(diff ?? 0).toFixed(2)}`
       const playedSuffix = playedFixtures
         .map(pf => `${pf.opponent_team} (${pf.is_home ? 'H' : 'A'}) — Played`)
         .join(' / ')
       const tooltip = playedSuffix.length > 0 ? `${baseTooltip} / ${playedSuffix}` : baseTooltip
       ```

       Leave the surrounding `return (<td ...>)` JSX UNCHANGED — same className, same label span, same `key={gw}`. The cell remains full opacity, the dominant color remains the upcoming-fixture difficulty tier, and the played leg(s) surface via the title attribute. This handles the `fixtures.length === 1 && playedFixtures.length >= 1` case because (a) the fall-through reaches this branch only when neither `fixtures.length >= 2` nor `fixtures.length === 0` matched, which means `fixtures.length === 1`, and (b) `playedFixtures` is already available from the existing `const playedFixtures = grid.byTeamGwPlayed.get(t.team_id)?.get(gw) ?? []` at line 88.

    2. Verify by reading the file that no other branch was disturbed (BGW at 89, DGW-all-played at 99, single-played at 124, DGW-upcoming at 137 all unchanged). The diff should be exactly 1 line removed and 4 lines added in the final single-upcoming branch.

    3. Run `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — all 53 tests MUST pass (51 existing + 2 new gap-closure tests).

    4. Run `npx vitest run` to confirm the full project suite remains green. Run `npx tsc --noEmit` to confirm no type errors.

    5. Commit as `feat(111-04): surface played leg in partially-played DGW cell tooltip`. Do NOT include a Co-Authored-By trailer.
  </action>
  <verify>
    <automated>npx vitest run src/components/club-form/FixtureHeatMap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/club-form/FixtureHeatMap.tsx` contains the substring `const playedSuffix = playedFixtures`
    - File contains the substring `(pf.is_home ? 'H' : 'A')` (the played-leg tooltip formatter)
    - File contains the substring ``(H/A)`` is NOT present as literal (sanity check — the formatter uses `pf.is_home ? 'H' : 'A'` template, not the literal string `H/A`)
    - File contains the substring `playedSuffix.length > 0 ? ` (the conditional join guard)
    - File contains the substring `' / ' ` joined onto the played suffix (the test asserts ' / ' between segments)
    - The four existing render branches' line ranges remain intact: `if (fixtures.length === 0 && playedFixtures.length === 0)` (BGW), `if (fixtures.length === 0 && playedFixtures.length >= 2)` (DGW all-played), `if (fixtures.length === 0 && playedFixtures.length === 1)` (single played), `if (fixtures.length >= 2)` (DGW upcoming) — verified by `grep -c` of each predicate returning 1
    - `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` exits 0 with 53 tests passing (51 existing + 2 new)
    - `npx vitest run` exits 0 (full project suite green)
    - `npx tsc --noEmit` exits 0
    - Git log contains a commit with message starting with `feat(111-04): surface played leg in partially-played DGW`
    - Git commit contains NO `Co-Authored-By` trailer
  </acceptance_criteria>
  <done>The partially-played DGW gap is closed. The upcoming leg renders normally at full opacity with the difficulty color and opponent label; the played leg surfaces via the tooltip suffix '— Played'. No silent data loss. VERIFICATION.md gap should re-verify GREEN on re-run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-02 hardening — sort events by id ascending before fallback slice in club-form.ts</name>
  <files>src/lib/club-form.ts, src/lib/club-form.test.ts</files>
  <read_first>
    - src/lib/club-form.ts (full file — focus on lines 137–141 where currentGw is derived; the bug is `bootstrap.events?.filter(e => e.finished).slice(-1)[0]?.id` without sorting first)
    - src/lib/club-form.test.ts (full file — the existing fallback-path test at lines 165–193 uses events in id-ascending order, so the bug is not triggered by current tests; the new test must pass events in DESCENDING order to expose the bug)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-VERIFICATION.md (lines 136–138 — the WARNING anti-pattern description, and lines 165–171 — the gap-closure recommendation)
    - .planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-CONTEXT.md (no specific decision covers CR-02; this is a defensive hardening within Claude's Discretion)
  </read_first>
  <behavior>
    After this task:
    1. `src/lib/club-form.ts` line 140 changes from `bootstrap.events?.filter(e => e.finished).slice(-1)[0]?.id` to `bootstrap.events?.filter(e => e.finished).sort((a, b) => a.id - b.id).slice(-1)[0]?.id`.
    2. A new test case in `src/lib/club-form.test.ts` exercises the fallback path with `events` in DESCENDING id order to prove the sort is required. The test asserts that when `events = [rawEvent(36, false, true), rawEvent(35, false, true)]` (both finished, NEITHER is_current, listed in descending order), the derived `currentGw` is `36` (the maximum finished id), not `35` (which would be `.slice(-1)[0]?.id` of the unsorted array). The test name is `'CR-02: fallback picks max finished event id regardless of array order'`.
    3. All existing club-form tests still pass — the sort is a stable in-place sort applied to a `.filter()` result (a new array), so it does not mutate `bootstrap.events`.
  </behavior>
  <action>
    1. Edit `src/lib/club-form.ts`. On line 140, change:

       ```
       bootstrap.events?.filter(e => e.finished).slice(-1)[0]?.id ??
       ```

       to:

       ```
       bootstrap.events?.filter(e => e.finished).sort((a, b) => a.id - b.id).slice(-1)[0]?.id ??
       ```

       Note: `.filter()` returns a fresh array, so the in-place `.sort()` does NOT mutate the caller's `bootstrap.events`. Verify by reading the file that the only diff is the single-line addition of `.sort((a, b) => a.id - b.id)` immediately before `.slice(-1)`.

    2. Edit `src/lib/club-form.test.ts`. Locate the existing fallback test `it('falls back to last finished event when no event has is_current', ...)` at line 165. Immediately after that test's closing `})` (verify the line by reading), add a new `it(...)` block with the exact name `'CR-02: fallback picks max finished event id regardless of array order'`. The test must:
       - Build `events` array in DESCENDING id order: `const events = [rawEvent(36, false, true), rawEvent(35, false, true)]` (both finished, neither is_current).
       - Build at least one finished `RawFixture` at `event: 36` so the resulting `current_gw_played` is non-empty when the sort works correctly. Use the existing `rawFixture` helper with `event: 36, finished: true`.
       - Call `computeClubForm({ teams: [teamA, teamB, teamC], events }, fixtures)`.
       - Assert: the team that played the GW36 fixture has `current_gw_played[0].event_id === 36`. This passes ONLY when the fallback correctly picks the max-id finished event (36), not the last-in-array event (35).

       Use the existing helpers (`rawEvent`, `rawFixture`, `teamA`, `teamB`, `teamC`) from the top of the test file — do not re-define them.

    3. Run `npx vitest run src/lib/club-form.test.ts` — all existing tests pass plus the new CR-02 test passes.

    4. Run `npx vitest run` to confirm the full project suite remains green. Run `npx tsc --noEmit` to confirm no type errors.

    5. Commit as `fix(111-04): sort events by id before fallback slice to harden CR-02`. Do NOT include a Co-Authored-By trailer.
  </action>
  <verify>
    <automated>npx vitest run src/lib/club-form.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/club-form.ts` contains the substring `.sort((a, b) => a.id - b.id).slice(-1)[0]?.id`
    - `src/lib/club-form.ts` no longer contains the substring `.filter(e => e.finished).slice(-1)[0]?.id` (the unsorted version is gone)
    - `src/lib/club-form.test.ts` contains the substring `CR-02: fallback picks max finished event id regardless of array order`
    - `src/lib/club-form.test.ts` contains the substring `rawEvent(36, false, true), rawEvent(35, false, true)` (the descending-order events array literal — verifies the test setup actually exercises the bug)
    - `npx vitest run src/lib/club-form.test.ts` exits 0 with the existing tests plus the new CR-02 test passing
    - `npx vitest run` exits 0 (full project suite green)
    - `npx tsc --noEmit` exits 0
    - Git log contains a commit with message starting with `fix(111-04): sort events by id before fallback slice`
    - Git commit contains NO `Co-Authored-By` trailer
  </acceptance_criteria>
  <done>CR-02 fallback-sort hardening shipped with a regression test that fails without the sort. The fallback now picks the correct max-id finished event regardless of FPL API response order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/club-form (server) → useClubForm (client) | ClubForm[] JSON crosses the network boundary into the React Query cache |
| FPL bootstrap.events array order | Untrusted ordering from external FPL API; the fallback path consumes it |
| ClubForm.opponent_team string → DOM attribute (title) | Untyped string from JSON renders into DOM title attribute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-04 | Tampering | ClubForm.current_gw_played array shape (inherited from Plan 02) | accept | Source is our own /api/club-form route (Plan 01); shape locked by RawEvent + RawBootstrap interfaces. Component uses `t.current_gw_played ?? []` defensively. No change to the threat surface in this plan. |
| T-111-05 | Information Disclosure | opponent_team rendered into title attribute (now concatenated for mixed state) | accept | opponent_team is a 3-letter PL team short name. React JSX title-attribute insertion auto-escapes HTML. Concatenating multiple short names with ' / ' separator does not introduce a new sink — same escape pathway. No XSS surface added. |
| T-111-06 | Repudiation | Visual signal misleading user about real fixture state (mixed-state case) | mitigate | The mixed-state tooltip contains the explicit '— Played' marker for each played leg, mirroring the single-played branch's contract (D-05). The upcoming fixture's '— 0.dd' difficulty score remains the first tooltip segment, so the user reads the actionable fixture first and the played-leg context second. Two new tests lock the exact tooltip format. |
| T-111-07 | Tampering (new) | bootstrap.events order from FPL API | mitigate | CR-02 hardening: `.sort((a, b) => a.id - b.id)` before `.slice(-1)[0]` ensures the fallback picks the actual max finished event id regardless of FPL API response order. The primary `is_current` derivation path is unaffected. New regression test (`CR-02:` named) locks the contract. |
</threat_model>

<verification>
- `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` — 53 tests passing (51 pre-existing + 2 new mixed-state)
- `npx vitest run src/lib/club-form.test.ts` — existing tests plus 1 new CR-02 test all green
- `npx vitest run` — full project suite green
- `npx tsc --noEmit` — no type errors
- `grep -c "fixtures.length === 0 && playedFixtures.length === 0" src/components/club-form/FixtureHeatMap.tsx` returns `1`
- `grep -c "fixtures.length === 0 && playedFixtures.length >= 2" src/components/club-form/FixtureHeatMap.tsx` returns `1`
- `grep -c "fixtures.length === 0 && playedFixtures.length === 1" src/components/club-form/FixtureHeatMap.tsx` returns `1`
- `grep -c "fixtures.length >= 2" src/components/club-form/FixtureHeatMap.tsx` returns `1`
- `grep -c "playedSuffix" src/components/club-form/FixtureHeatMap.tsx` returns at least `2` (declaration + usage in the conditional)
- `grep -c "a.id - b.id" src/lib/club-form.ts` returns `1`
- Manual probe (recommended once Plan 04 ships): start dev server during a real DGW mid-week period (e.g., GW33 or GW36 per project memory), find a team with one played leg + one upcoming leg, hover the cell — tooltip shows both legs with '— Played' marker on the finished one
</verification>

<success_criteria>
- HeatMapRow handles the partially-played DGW case (`fixtures.length === 1 && playedFixtures.length >= 1`) with a tooltip that contains both the upcoming-fixture summary and each played-leg '— Played' segment
- Played fixture data is never silently discarded — VERIFICATION.md gap is closed
- CR-02 fallback-sort hardening shipped with a regression test that fails without the sort
- 2 new tests in `FixtureHeatMap.test.tsx` + 1 new test in `club-form.test.ts` committed and GREEN
- No regression in any existing FixtureHeatMap or club-form test
- No type errors, no full-suite regressions
- All three commits land in TDD RED→GREEN→REFACTOR order with messages starting `test(111-04):`, `feat(111-04):`, `fix(111-04):`
- No `Co-Authored-By` trailers (per project CLAUDE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/111-fixture-heatmap-planner-cross-position-fixes-v1-20/111-04-SUMMARY.md` documenting:
- Files modified, exact line diffs (added/removed)
- The 3 new test cases added (one-line each)
- Confirmation that the BLOCKER gap from `111-VERIFICATION.md` is closed end-to-end
- Confirmation that CR-02 WARNING is now hardened
- Any deviation from plan (expected: none)
- Note for /gsd-verify-work re-run: the gap truth "User can view the fixture heat map mid-week ... [partially-played DGW]" is now backed by the two new tests in `FixtureHeatMap.test.tsx` AND by direct inspection of the new render branch logic in `FixtureHeatMap.tsx`. Re-run `/gsd-verify-work 111` should produce a clean PASS for FIX-01 with score 3/3.
</output>
