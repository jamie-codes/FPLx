---
phase: 114
plan: 03
type: execute
wave: 2
depends_on:
  - "114-01"
  - "114-02"
files_modified: []
autonomous: false
requirements:
  - UAT-01

must_haves:
  truths:
    - "UAT-01 checkpoint passes: all four visual dimensions confirmed by the user"
    - "BackTab dark mode renders correctly with no white flash and correct dark tokens"
    - "Delta colour polarity is correct: delta > 0 (engine better) = red, delta < 0 (user better) = green"
    - "Multi-transfer GW tooltip shows both sell/buy pairs joined with ' + ' separator without truncation"
    - "Captain regression: captain regret chart and transfer regret chart are visually distinct with no cross-section artefacts"
  artifacts: []
  key_links:
    - from: "BackTab.tsx line 57"
      to: "transferRegretFill logic"
      via: "delta > 0 → REGRET_RED (intentionally correct — D-09)"
      pattern: "delta > 0.*REGRET_RED"
    - from: "BackTab.tsx TransferRegretTooltip lines 103-108"
      to: "multi-transfer join rendering"
      via: "e.engineSell.join(' + ') / e.userSell.join(' + ')"
      pattern: "join\\(' \\+ '\\)"
---

<objective>
Human visual verification of the Transfer Regret Backtester (UAT-01). No code changes unless regressions are discovered during UAT.

Purpose: Confirms the Phase 113 BACK-02 delivery meets the four visual quality dimensions before v1.21 ships. The checkpoint is blocking — v1.21 cannot close without user sign-off on all four dimensions.

Output: User confirmation that all four UAT-01 visual checkpoints pass (or a list of regressions to fix before sign-off).
</objective>

<execution_context>
@C:/Users/jamie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jamie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/114-polish-carry-forward-fixes-v1-21/114-CONTEXT.md
@.planning/phases/114-polish-carry-forward-fixes-v1-21/114-UI-SPEC.md

<interfaces>
<!-- Reference: what BackTab renders so the human checkpoint is well-scoped. -->

From src/components/accuracy/BackTab.tsx:

transferRegretFill (lines 55-60):
```typescript
function transferRegretFill(delta: number | null): string {
  if (delta === null) return REGRET_GREY
  if (delta > 0) return REGRET_RED    // engine better → red (regret framing — D-09, intentionally correct)
  if (delta < 0) return REGRET_GREEN  // user better → green
  return REGRET_GREY
}
```

Delta cell rendering (lines 514-537):
- delta > 0: `<span className="text-red-600 dark:text-red-400">+{e.delta}pts (engine better)</span>`
- delta < 0: `<span className="text-green-600 dark:text-green-400">−{Math.abs(e.delta)}pts (good hold)</span>`
- delta === 0: `text-zinc-500 dark:text-zinc-400` "0pts (tied)"
- delta === null: em-dash in zinc-400

Multi-transfer tooltip (lines 103-108):
- engineLabel: `Sell ${e.engineSell.join(' + ')} buy ${e.engineBuy.join(' + ')}`
- userLabel: `Sell ${e.userSell.join(' + ')} buy ${e.userBuy.join(' + ')}`
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT-01 — Transfer Regret Backtester visual verification</name>
  <read_first>
    - src/components/accuracy/BackTab.tsx lines 50-60 (transferRegretFill), lines 98-126 (TransferRegretTooltip), lines 505-537 (delta cell rendering)
    - .planning/phases/114-polish-carry-forward-fixes-v1-21/114-UI-SPEC.md §UAT-01 visual contract
    - .planning/phases/114-polish-carry-forward-fixes-v1-21/114-CONTEXT.md D-09 (delta colour semantics are intentionally correct — do not invert)
  </read_first>
  <what-built>
    Plans 114-01 and 114-02 delivered:
    - TRT-01: RouteTreeTab Hits column now shows totalHits
    - TRT-02: Disabled ChipToggle stub visible in RouteTreeTab
    - SPARK-01: GemTable rank_trajectory sparkline column added

    This task is a human visual inspection of the Transfer Regret Backtester (BackTab) — no code was changed for this surface in Phase 114. The checkpoint verifies that Phase 113 BACK-02 delivery is visually correct across four dimensions before v1.21 closes.
  </what-built>
  <how-to-verify>
    Start the dev server if not already running: `npm run dev`

    Navigate to the Back / Decision History tab in the app. Select the "Transfer" pill to show the Transfer Regret view.

    Verify all four dimensions:

    **Dimension 1 — Dark mode rendering**
    Switch to dark mode (system or browser toggle). Confirm:
    - All BackTab surfaces render with dark tokens: table rows use `bg-zinc-900`/`dark:bg-zinc-800`, borders use `dark:border-zinc-700`
    - No white flash or bright surfaces visible in the Transfer Regret section
    - Tooltip popup (hover/tap a bar) renders with `bg-white dark:bg-zinc-900` background and `dark:border-zinc-700` border

    **Dimension 2 — Delta colour polarity**
    Find a gameweek where the engine recommended a different transfer to what you made:
    - If delta > 0 (engine pick scored more = you should have listened): cell text is RED (`text-red-600 dark:text-red-400`) with label "+Npts (engine better)"
    - If delta < 0 (your pick scored more = you made the better call): cell text is GREEN (`text-green-600 dark:text-green-400`) with label "−Npts (good hold)"
    - This polarity is intentionally correct (D-09). "Engine better = regret = red" is the right framing. Do NOT flag this as a bug.

    **Dimension 3 — Multi-transfer GW formatting**
    Find (or recall) a gameweek where you made 2 transfers:
    - Hover/tap the bar for that GW to open the tooltip
    - Confirm the Engine row shows both players joined with " + " separator: e.g. "Sell Salah + Haaland buy Mbappé + Kane"
    - Confirm your actual row shows both pairs joined with " + " separator
    - No truncation, no "..." cutoff, both pairs visible in full

    If no 2-transfer GW exists in your history, check the tooltip renders cleanly for a 1-transfer GW (single player names, no " + ") and note "Multi-transfer: not testable with current history" in your response.

    **Dimension 4 — Captain regression separation**
    Scroll through the BackTab. The captain regret section and the transfer regret section should be:
    - Clearly separated into distinct sections (separate headings, separate charts/tables)
    - No captain-specific data (captain names, "captain regret" label) appearing inside the Transfer Regret rows
    - No transfer-specific data appearing inside the Captain Regret rows

    **After checking all four dimensions**, type one of:
    - "approved" — all four dimensions pass, UAT-01 complete
    - Describe specific failures — which dimension failed and what you observed. Claude will fix regressions and re-verify before closing.
  </how-to-verify>
  <acceptance_criteria>
    - User has confirmed "approved" or provided a failure report
    - If approved: all four visual dimensions are confirmed passing by the user
    - If failures reported: regressions are fixed in BackTab.tsx before this checkpoint is marked done
    - D-09 is honoured: delta > 0 = red is NOT treated as a bug, not inverted
  </acceptance_criteria>
  <resume-signal>Type "approved" to close UAT-01, or describe any visual failures you observe for each dimension.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human review only | This plan contains no code changes; no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-05 | Information Disclosure | BackTab personal transfer history | accept | Data is the user's own FPL history, fetched via their authenticated session. No third-party exposure. |
</threat_model>

<verification>
UAT-01 is complete when the user types "approved" at the checkpoint prompt.

If regressions are discovered:
- Read BackTab.tsx and identify the failing rendering path
- Fix the specific regression (do NOT invert delta colour — D-09 is intentionally correct)
- Re-present the dimension for re-verification before asking the user to approve
</verification>

<success_criteria>
- User has confirmed all four UAT-01 visual dimensions pass
- No delta colour inversion performed (D-09 honoured)
- If regressions were found and fixed, BackTab.tsx changes are committed before plan is marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/114-polish-carry-forward-fixes-v1-21/114-03-SUMMARY.md`
</output>
