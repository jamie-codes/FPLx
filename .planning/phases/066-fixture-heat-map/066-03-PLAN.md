---
phase: 066-fixture-heat-map
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - src/app/page.tsx
  - src/app/page.test.tsx
autonomous: true
requirements:
  - HEAT-01
  - HEAT-02
  - HEAT-03
user_setup: []

must_haves:
  truths:
    - "Heat Map sub-tab appears as the last entry in the Analyse section sub-tab nav (D-06)"
    - "Clicking the Heat Map sub-tab mounts FixtureHeatMap component in the main content area (D-07)"
    - "page.test.tsx mocks FixtureHeatMap so all existing page tests continue to pass"
    - "A new navigation test asserts the Heat Map tab order and aria-current behaviour"
  artifacts:
    - path: src/app/page.tsx
      provides: "SubTab union extended with 'fixture-heat-map'; Analyse SECTIONS entry for Heat Map; render guard mounting FixtureHeatMap; component import"
      contains: "fixture-heat-map"
    - path: src/app/page.test.tsx
      provides: "vi.mock for FixtureHeatMap + navigation test for the new sub-tab"
      contains: "FixtureHeatMap"
  key_links:
    - from: "page.tsx render guard"
      to: "src/components/club-form/FixtureHeatMap.tsx"
      via: "conditional render activeSection !== 'squad' && activeSubTab === 'fixture-heat-map'"
      pattern: "activeSubTab === 'fixture-heat-map'"
    - from: "page.tsx SECTIONS Analyse subTabs array"
      to: "the new Heat Map sub-tab object"
      via: "{ id: 'fixture-heat-map' as SubTab, label: 'Heat Map', mobileLabel: 'Heat Map' }"
      pattern: "id: 'fixture-heat-map'"
---

<objective>
Wire the FixtureHeatMap component (built in Plan 02) into the application navigation. Four small edits to `src/app/page.tsx` (import + SubTab union extension + SECTIONS entry + render guard) and two edits to `src/app/page.test.tsx` (vi.mock for the new component + a navigation test asserting tab order and mount behaviour).

Purpose: Plan 02 produced an exported but unmounted component. This plan makes the Heat Map reachable from the user-facing UI as the last entry of the Analyse section's sub-tab strip. After this plan ships, the user-facing flow Analyse → Heat Map mounts FixtureHeatMap and renders the 20×8 grid.

Output: `src/app/page.tsx` extended with one import, one SubTab union literal, one SECTIONS subTab entry, and one render guard. `src/app/page.test.tsx` extended with one `vi.mock` block and one new `it(...)` test case. No other behavioural changes to page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/066-fixture-heat-map/066-CONTEXT.md
@.planning/phases/066-fixture-heat-map/066-RESEARCH.md
@.planning/phases/066-fixture-heat-map/066-PATTERNS.md
@.planning/phases/066-fixture-heat-map/066-UI-SPEC.md
@.planning/phases/066-fixture-heat-map/066-VALIDATION.md
@.planning/phases/066-fixture-heat-map/066-02-SUMMARY.md

<interfaces>
<!-- Current page.tsx and page.test.tsx state — extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/app/page.tsx (relevant excerpts):

Line 28 (current — import block, the price-changes import is the closest analog and the anchor for the new import):
```typescript
import { PriceChangePanel } from '@/components/price-changes/PriceChangePanel'
```

Lines 53-54 (current SubTab union — append 'fixture-heat-map' at the end):
```typescript
export type Section = 'analyse' | 'plan' | 'squad'
export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'manual-plan' | 'route-tree' | 'club-form' | 'value-gems' | 'accuracy' | 'decision' | 'transfers' | 'optimiser' | 'price-changes' | 'rivals'
```

Lines 56-69 (current SECTIONS Analyse subTabs — append after 'price-changes' entry on line 66):
```typescript
export const SECTIONS = [
  {
    id: 'analyse' as Section,
    label: 'Analyse',
    subTabs: [
      { id: 'gems' as SubTab,       label: 'Gem Ratings',     mobileLabel: 'Gems'     },
      { id: 'insights' as SubTab,   label: 'Insights',        mobileLabel: 'Insights' },
      { id: 'defcon' as SubTab,     label: 'DefCon Analysis', mobileLabel: 'DefCon'   },
      { id: 'set-pieces' as SubTab, label: 'Set Pieces',      mobileLabel: 'SP'       },
      { id: 'accuracy' as SubTab,      label: 'Accuracy',        mobileLabel: 'Acc'      },
      { id: 'price-changes' as SubTab, label: 'Price Changes',   mobileLabel: 'Prices'   },
    ],
    defaultSubTab: 'gems' as SubTab,
  },
```

Lines 234-245 (current render guard block — append after line 245):
```typescript
{activeSection !== 'squad' && activeSubTab === 'defcon' && <DefConTables />}
{activeSection !== 'squad' && activeSubTab === 'club-form' && (
  <>
    <FixtureEaseRankingPanel />
    <FixtureSwingDetector />
    <ClubFormTable />
  </>
)}
{activeSection !== 'squad' && activeSubTab === 'set-pieces' && <SetPieceTakerPanel />}
{activeSection !== 'squad' && activeSubTab === 'insights' && <InsightsTab />}
{activeSection !== 'squad' && activeSubTab === 'accuracy' && <AccuracyTab />}
{activeSection !== 'squad' && activeSubTab === 'price-changes' && <PriceChangePanel />}
```

From src/app/page.test.tsx (relevant excerpts):

Lines 36-41 (vi.mock block — the AccuracyTab/PriceChangePanel pair is the closest analog):
```typescript
vi.mock('@/components/accuracy/AccuracyTab', () => ({
  AccuracyTab: () => <div data-testid="accuracy-tab" />,
}))
vi.mock('@/components/price-changes/PriceChangePanel', () => ({
  PriceChangePanel: () => <div data-testid="price-change-panel" />,
}))
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire FixtureHeatMap into page.tsx (import + SubTab + SECTIONS + render guard)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — confirm line numbers for the four edits below; see <interfaces> excerpts)
    - src/components/club-form/FixtureHeatMap.tsx (confirm the named export `FixtureHeatMap` exists from Plan 02)
    - .planning/phases/066-fixture-heat-map/066-CONTEXT.md (D-06 placement after price-changes; D-07 render guard expression)
    - .planning/phases/066-fixture-heat-map/066-PATTERNS.md (### `src/app/page.tsx` (four edits) — exact change instructions)
    - .planning/phases/066-fixture-heat-map/066-UI-SPEC.md (Navigation Contract — locked label/mobileLabel/render guard)
  </read_first>
  <action>
Make exactly four edits to `src/app/page.tsx`. Do not refactor anything else.

**Edit 1 — Add import (immediately after the existing PriceChangePanel import on line 28):**

```typescript
import { FixtureHeatMap } from '@/components/club-form/FixtureHeatMap'
```

The import path uses the `@/components/club-form/` alias matching the analog `FixtureEaseRankingPanel` import on line 15. Place this new import directly below `import { PriceChangePanel } from '@/components/price-changes/PriceChangePanel'` (line 28).

**Edit 2 — Extend the SubTab union (line 54) by appending `'fixture-heat-map'`:**

The current line is:
```typescript
export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'manual-plan' | 'route-tree' | 'club-form' | 'value-gems' | 'accuracy' | 'decision' | 'transfers' | 'optimiser' | 'price-changes' | 'rivals'
```

Replace with:
```typescript
export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'manual-plan' | 'route-tree' | 'club-form' | 'value-gems' | 'accuracy' | 'decision' | 'transfers' | 'optimiser' | 'price-changes' | 'rivals' | 'fixture-heat-map'
```

(`'fixture-heat-map'` appended at the very end — order in the union does not affect runtime; the literal is what matters.)

**Edit 3 — Add the Heat Map subTab entry to the Analyse section's `subTabs` array (immediately after the `'price-changes'` entry on line 66):**

The current Analyse subTabs ends with:
```typescript
      { id: 'price-changes' as SubTab, label: 'Price Changes',   mobileLabel: 'Prices'   },
    ],
```

Insert one new entry between the `'price-changes'` line and the closing `],` so the array becomes:
```typescript
      { id: 'price-changes' as SubTab, label: 'Price Changes',   mobileLabel: 'Prices'   },
      { id: 'fixture-heat-map' as SubTab, label: 'Heat Map',     mobileLabel: 'Heat Map' },
    ],
```

Use the literal label `'Heat Map'` and mobileLabel `'Heat Map'` (UI-SPEC §Copywriting Contract / Navigation Contract).

**Edit 4 — Add a render guard for the Heat Map tab (immediately after the existing price-changes render guard on line 245):**

The current line 245 is:
```typescript
{activeSection !== 'squad' && activeSubTab === 'price-changes' && <PriceChangePanel />}
```

Add a new line directly below:
```typescript
{activeSection !== 'squad' && activeSubTab === 'fixture-heat-map' && <FixtureHeatMap />}
```

Do NOT modify:
- The Analyse section's `defaultSubTab: 'gems' as SubTab` (line 68) — Heat Map is NOT the default landing.
- The order of any existing SubTabs or render guards.
- The Plan or Squad section subTabs arrays.
- Any other render guard, `<main>` structure, or component prop wiring.
- The `Section` type union (the new tab is in the existing `'analyse'` section, not a new section).

Run `npx tsc --noEmit` after the four edits — it MUST exit 0. Run `npx vitest run src/app/page.test.tsx` — it WILL fail because `FixtureHeatMap` is imported but not yet mocked in page.test.tsx; that is expected and is fixed by Task 2. Do NOT proceed past Task 1 without committing the page.tsx edits first (they are atomic; the test failure is resolved in the next task).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "src/app/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { FixtureHeatMap } from '@/components/club-form/FixtureHeatMap'" src/app/page.tsx` returns 1
    - `grep -c "'fixture-heat-map'" src/app/page.tsx` returns at least 3 (SubTab union literal + SECTIONS entry id + render guard condition)
    - `grep -c "id: 'fixture-heat-map' as SubTab" src/app/page.tsx` returns 1
    - `grep -c "label: 'Heat Map'" src/app/page.tsx` returns 1
    - `grep -c "mobileLabel: 'Heat Map'" src/app/page.tsx` returns 1
    - `grep -c "activeSubTab === 'fixture-heat-map' && <FixtureHeatMap />" src/app/page.tsx` returns 1
    - `grep -c "activeSection !== 'squad' && activeSubTab === 'fixture-heat-map'" src/app/page.tsx` returns 1
    - `grep -c "id: 'price-changes' as SubTab" src/app/page.tsx` returns 1 (existing entry intact)
    - `grep -c "defaultSubTab: 'gems' as SubTab" src/app/page.tsx` returns 1 (Analyse default unchanged)
    - Heat Map entry appears AFTER price-changes — verify ordinal: `grep -n "id: 'fixture-heat-map' as SubTab" src/app/page.tsx` line number is greater than `grep -n "id: 'price-changes' as SubTab" src/app/page.tsx` line number
    - `npx tsc --noEmit 2>&1 | grep -c "src/app/page.tsx"` returns 0 (no TS errors in page.tsx)
  </acceptance_criteria>
  <done>
    Four edits applied to page.tsx exactly as specified. TypeScript clean. Heat Map sub-tab is positioned as the last entry of the Analyse section's subTabs array. Render guard mounts FixtureHeatMap when `activeSection !== 'squad' && activeSubTab === 'fixture-heat-map'`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mock FixtureHeatMap in page.test.tsx and add Heat Map navigation test</name>
  <files>src/app/page.test.tsx</files>
  <read_first>
    - src/app/page.test.tsx (current state — confirm the AccuracyTab/PriceChangePanel mocks at lines 36-41 and the Plan-section navigation tests at lines 164-191 used as the canonical pattern for a sub-tab nav test)
    - src/app/page.tsx (confirm Plan 03 Task 1 wiring is in place — without it, this task's nav test cannot find the Heat Map button)
    - .planning/phases/066-fixture-heat-map/066-PATTERNS.md (### `src/app/page.test.tsx` (two edits) — exact change instructions)
    - .planning/phases/066-fixture-heat-map/066-RESEARCH.md (Pitfall 3 — page.test.tsx must mock the new component or the entire suite breaks at import)
  </read_first>
  <action>
Make exactly two edits to `src/app/page.test.tsx`.

**Edit 1 — Add `vi.mock` block for FixtureHeatMap (immediately after the existing PriceChangePanel mock at lines 39-41):**

The current block (lines 39-41) reads:
```typescript
vi.mock('@/components/price-changes/PriceChangePanel', () => ({
  PriceChangePanel: () => <div data-testid="price-change-panel" />,
}))
```

Insert directly after it (preserving the same single-line spacing convention as the surrounding mocks):
```typescript
vi.mock('@/components/club-form/FixtureHeatMap', () => ({
  FixtureHeatMap: () => <div data-testid="fixture-heat-map" />,
}))
```

This satisfies Pitfall 3 (RESEARCH.md): without this mock, the entire page.test.tsx suite would fail at module import because `page.tsx` now imports `FixtureHeatMap`.

**Edit 2 — Add a new `it(...)` test case for Heat Map navigation:**

Place the new test inside the existing `describe('Phase 36: page.tsx state', ...)` block, immediately AFTER the existing `it('clicking "Manual Plan" sub-tab mounts ManualPlanTab and hides PlannerTab ...', ...)` test (around line 206 in the current file — append after that test's closing `})`). Use this exact test body:

```typescript
  it('Phase 66: clicking "Heat Map" sub-tab mounts FixtureHeatMap and shows aria-current="page" (HEAT-01, D-06, D-07)', () => {
    const { container } = render(<Home />)
    // Default landing is Analyse / Gems — confirm Heat Map sub-tab exists in the Analyse nav
    const analyseSubTabs = container.querySelector('nav[aria-label="Analyse sub-tabs"]')
    expect(analyseSubTabs).not.toBeNull()
    const subTabBtns = Array.from(analyseSubTabs!.querySelectorAll('button')).map(b => b.textContent)
    // Locked order per D-06: Gem Ratings | Insights | DefCon Analysis | Set Pieces | Accuracy | Price Changes | Heat Map
    expect(subTabBtns).toEqual([
      'Gem Ratings',
      'Insights',
      'DefCon Analysis',
      'Set Pieces',
      'Accuracy',
      'Price Changes',
      'Heat Map',
    ])
    // Click Heat Map
    const heatMapBtn = Array.from(analyseSubTabs!.querySelectorAll('button')).find(b => b.textContent === 'Heat Map')
    expect(heatMapBtn).toBeDefined()
    fireEvent.click(heatMapBtn!)
    // FixtureHeatMap mounted; PriceChangePanel and Gems hidden
    expect(container.querySelector('[data-testid="fixture-heat-map"]')).not.toBeNull()
    expect(container.querySelector('[data-testid="price-change-panel"]')).toBeNull()
    expect(container.querySelector('[data-testid="gem-table"]')).toBeNull()
    // aria-current is now Heat Map
    expect(
      container.querySelector('nav[aria-label="Analyse sub-tabs"] button[aria-current="page"]')?.textContent
    ).toBe('Heat Map')
  })
```

This test asserts:
- D-06 placement: Heat Map is the LAST entry of the Analyse sub-tab strip; the full ordered list matches the locked spec.
- D-07 render guard: clicking Heat Map mounts FixtureHeatMap (via the test mock) and unmounts the previously visible Gems / PriceChangePanel.
- Accessibility: the active sub-tab carries `aria-current="page"`.

**Important:** If an existing Analyse sub-tab order test exists in this file (e.g., one that asserts `subTabBtns` for the Analyse nav), update it to include `'Heat Map'` at the end. As of the read of `src/app/page.test.tsx` performed during planning, NO such pre-existing test was present — only Plan, Squad, and individual-tab tests exist. If during Task 2 execution an Analyse-strip-order test is discovered, append `'Heat Map'` to its `expect(subTabBtns).toEqual([...])` array; otherwise this concern is moot.

Do NOT modify:
- Any other vi.mock entry, test, or import in page.test.tsx.
- The `import Home from '@/app/page'` line.
- The outer `describe('Phase 36: page.tsx state', ...)` wrapper or any of the existing tests' assertions.

Run `npx vitest run src/app/page.test.tsx` after the edits — every test must pass (existing tests previously broken by Task 1's import are fixed by the new vi.mock; the new HEAT navigation test passes because Task 1 wired the SECTIONS entry and render guard).
  </action>
  <verify>
    <automated>npx vitest run src/app/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.mock('@/components/club-form/FixtureHeatMap'" src/app/page.test.tsx` returns 1
    - `grep -c "FixtureHeatMap: () => <div data-testid=\"fixture-heat-map\" />" src/app/page.test.tsx` returns 1
    - `grep -c "Phase 66: clicking \"Heat Map\" sub-tab" src/app/page.test.tsx` returns 1
    - `grep -c "data-testid=\"fixture-heat-map\"" src/app/page.test.tsx` returns at least 2 (one in mock, one in the assertion)
    - `grep -c "'Heat Map'" src/app/page.test.tsx` returns at least 4 (in the expect.toEqual array, the find lookup, the click target lookup, and the aria-current assertion)
    - `grep -c "vi.mock('@/components/price-changes/PriceChangePanel'" src/app/page.test.tsx` returns 1 (existing mock untouched)
    - `npx vitest run src/app/page.test.tsx` exits 0 with all tests (existing + new) passing
    - `npx vitest run` (full suite) exits 0
  </acceptance_criteria>
  <done>
    page.test.tsx has the new vi.mock for FixtureHeatMap and the new Phase 66 navigation test. Full vitest suite passes including the new Heat Map navigation test asserting tab order, mount behaviour, and aria-current.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js page.tsx routing | Sub-tab activation is client-side React state (`useState<SubTab>`); no server boundary crossed. |
| User click event → conditional render | The `activeSubTab === 'fixture-heat-map'` literal comparison is internal to React; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-066-05 | Tampering | SubTab union extended with new literal | accept | TypeScript-narrowed string literal; impossible to inject an unknown sub-tab id at compile time. Same disposition as every prior sub-tab addition (Phase 36/47/57/etc). |
| T-066-06 | Information disclosure | New navigation test exposes test fixture only | accept | Test mocks render data-testid placeholders; no real data flows through page.test.tsx. |

No high-severity threats. Plan 03 is pure navigation wiring with a TypeScript-checked union extension. Same security classification as every prior sub-tab addition in this app.
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. page.tsx wiring complete
grep -c "import { FixtureHeatMap }" src/app/page.tsx                                # expect: 1
grep -c "id: 'fixture-heat-map' as SubTab" src/app/page.tsx                         # expect: 1
grep -c "activeSubTab === 'fixture-heat-map' && <FixtureHeatMap />" src/app/page.tsx # expect: 1

# 2. page.test.tsx wiring complete
grep -c "vi.mock('@/components/club-form/FixtureHeatMap'" src/app/page.test.tsx     # expect: 1
grep -c "Phase 66: clicking \"Heat Map\" sub-tab" src/app/page.test.tsx             # expect: 1

# 3. TypeScript clean
npx tsc --noEmit

# 4. Vitest full suite GREEN
npx vitest run
```

Manual verification (deferred to UAT — not a blocker for this plan):
- Run `npm run dev` and confirm Analyse → Heat Map mounts the 20×8 grid in browser.
- Confirm desktop ≥1024px shows full grid without horizontal scroll (HEAT-03 visual check).
- Toggle dark mode and confirm DGW gradient cells remain visually identifiable (Pitfall 2 — light-mode hex retained per Plan 02 D-03 implementation choice).
</verification>

<success_criteria>
- page.tsx has the FixtureHeatMap import, the SubTab union literal `'fixture-heat-map'`, the SECTIONS Analyse subTab entry as the last item, and the render guard mounting `<FixtureHeatMap />`
- page.test.tsx has the matching `vi.mock` and a passing navigation test asserting Analyse strip order ends with `'Heat Map'`
- All existing page.test.tsx tests continue to pass (the new mock fixes the import-time break introduced by Task 1)
- `npx tsc --noEmit` clean
- `npx vitest run` exits 0 (full suite green)
- A user navigating Analyse → Heat Map sees the 20×8 colour-coded grid (manual UAT step; component already tested in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/066-fixture-heat-map/066-03-SUMMARY.md` documenting:
- The four edits to page.tsx (import / SubTab union / SECTIONS entry / render guard)
- The two edits to page.test.tsx (vi.mock / new HEAT navigation test)
- Confirmation TypeScript and full Vitest suite remained green
- Phase wrap-up note: HEAT-01, HEAT-02, HEAT-03 all delivered. Plan 02 component has 12 unit tests covering all three requirements; Plan 03 navigation test confirms the user-facing tab strip and mount behaviour. Manual UAT steps remain for desktop no-scroll visual confirmation (HEAT-03), DGW gradient dark-mode visual sanity-check, and end-to-end "Analyse → Heat Map" smoke test.
</output>
