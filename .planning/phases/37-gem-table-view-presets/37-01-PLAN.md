---
phase: 37-gem-table-view-presets
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/gem-table/GwToggle.tsx
  - src/components/gem-table/GwToggle.test.ts
autonomous: true
requirements:
  - GEM-01
  - GEM-02
  - GEM-03
  - GEM-04

must_haves:
  truths:
    - "getColumnVisibility(horizon) still returns the same 3-key object as before (existing tests unchanged)"
    - "getColumnVisibility(horizon, false, 'compact') hides exactly 17 columns and keeps web_name, element_type, gem_score, mins_risk, and the active xPts column visible"
    - "getColumnVisibility(horizon, false, 'default') hides fdr_score, form_score, xg_per90, xa_per90, xg_score, xa_score, ownership_score, minutes_score, set_piece_score"
    - "getColumnVisibility(horizon, false, 'analysis') hides the same 7 columns as default except xg_per90 and xa_per90 remain visible"
    - "On mobile (isMobile=true), preset parameter is ignored — MOBILE_HIDDEN_COLUMNS path is taken regardless of preset"
    - "ViewPreset type is exported from GwToggle.tsx for import by GemTable.tsx and page.tsx"
  artifacts:
    - path: "src/components/gem-table/GwToggle.tsx"
      provides: "ViewPreset type, PRESET_COLUMN_VISIBILITY maps, extended getColumnVisibility"
      exports: ["ViewPreset", "getColumnVisibility", "MOBILE_HIDDEN_COLUMNS", "GwToggle"]
    - path: "src/components/gem-table/GwToggle.test.ts"
      provides: "Test coverage for all 3 presets and mobile-ignores-preset invariant"
      contains: "describe('getColumnVisibility presets'"
  key_links:
    - from: "getColumnVisibility"
      to: "PRESET_COLUMN_VISIBILITY"
      via: "spread merge: { ...PRESET_COLUMN_VISIBILITY[preset], ...gwVisibility }"
      pattern: "PRESET_COLUMN_VISIBILITY\\[preset\\]"
    - from: "getColumnVisibility (mobile path)"
      to: "MOBILE_HIDDEN_COLUMNS"
      via: "isMobile guard bypasses preset entirely"
      pattern: "if.*isMobile.*return.*MOBILE_HIDDEN_COLUMNS"
---

<objective>
Extend `GwToggle.tsx` with the `ViewPreset` type and three `VisibilityState` maps, and extend `getColumnVisibility` to accept a third optional `preset` parameter. Add full test coverage for the new preset logic in `GwToggle.test.ts`.

Purpose: Provides the pure logic foundation all wiring tasks (Plan 02) depend on. By adding `preset` as a third optional parameter with default `'default'`, the existing 1-arg and 2-arg call sites remain unchanged and all existing tests continue to pass.

Output: Updated `GwToggle.tsx` with exported `ViewPreset` type and extended `getColumnVisibility`; updated `GwToggle.test.ts` with a new `describe('getColumnVisibility presets')` block.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-gem-table-view-presets/37-CONTEXT.md
@.planning/phases/37-gem-table-view-presets/37-PATTERNS.md
@.planning/phases/37-gem-table-view-presets/37-UI-SPEC.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add ViewPreset type, PRESET_COLUMN_VISIBILITY maps, and extend getColumnVisibility</name>
  <files>src/components/gem-table/GwToggle.tsx, src/components/gem-table/GwToggle.test.ts</files>

  <read_first>
    - src/components/gem-table/GwToggle.tsx — current implementation; MUST be read before editing (existing exports, function signatures, MOBILE_HIDDEN_COLUMNS contents)
    - src/components/gem-table/GwToggle.test.ts — current test file; MUST be read to understand existing test shapes before adding new describe block
    - .planning/phases/37-gem-table-view-presets/37-UI-SPEC.md — §Column Visibility Maps (canonical COMPACT_VISIBILITY, DEFAULT_VISIBILITY, ANALYSIS_VISIBILITY objects)
    - .planning/phases/37-gem-table-view-presets/37-PATTERNS.md — §GwToggle.tsx extension points (steps 1-4)
  </read_first>

  <behavior>
    RED phase — write failing tests first in GwToggle.test.ts, new describe block only:

    - Test: getColumnVisibility(1, false, 'compact') — xPts_1gw is true, xPts_3gw is false, team_short_name is false, now_cost is false, regression_signal is false, differential_flag is false, trend is false, fixtures is false, selected_by_percent is false, status is false. web_name, element_type, gem_score, mins_risk are undefined (not in map = TanStack treats as visible).
    - Test: getColumnVisibility(3, false, 'compact') — xPts_3gw is true (gwVisibility wins over preset), xPts_1gw is false, xPts_5gw is false.
    - Test: getColumnVisibility(1, false, 'default') — fdr_score is false, form_score is false, xg_per90 is false, xa_per90 is false, xg_score is false, xa_score is false, ownership_score is false, minutes_score is false, set_piece_score is false. xPts_1gw is true (gwVisibility spread wins).
    - Test: getColumnVisibility(1, false, 'analysis') — fdr_score is false, form_score is false, xg_score is false, xa_score is false, ownership_score is false, minutes_score is false, set_piece_score is false. xg_per90 is NOT present in analysis map (visible by default). xa_per90 is NOT present in analysis map (visible by default).
    - Test: preset ignored on mobile — getColumnVisibility(1, true, 'compact') returns result with team_short_name: false, now_cost: false, status: false (MOBILE_HIDDEN_COLUMNS path), AND xPts_1gw: true (gwVisibility spread). regression_signal should be false (from MOBILE_HIDDEN_COLUMNS).
    - Test: no third arg = default preset — getColumnVisibility(1) returns { xPts_1gw: true, xPts_3gw: false, xPts_5gw: false } (same as before — existing test shape unchanged).

    GREEN phase — implement in GwToggle.tsx:

    1. Export `ViewPreset` type immediately after the existing imports/exports:
       ```typescript
       export type ViewPreset = 'default' | 'compact' | 'analysis'
       ```

    2. Add `PRESET_COLUMN_VISIBILITY` constant after `MOBILE_HIDDEN_COLUMNS` (before `getColumnVisibility`):
       ```typescript
       export const PRESET_COLUMN_VISIBILITY: Record<ViewPreset, Record<string, boolean>> = {
         compact: {
           team_short_name: false,
           now_cost: false,
           fdr_score: false,
           form_score: false,
           xg_per90: false,
           xa_per90: false,
           xg_score: false,
           xa_score: false,
           ownership_score: false,
           minutes_score: false,
           set_piece_score: false,
           selected_by_percent: false,
           status: false,
           regression_signal: false,
           differential_flag: false,
           trend: false,
           fixtures: false,
         },
         default: {
           fdr_score: false,
           form_score: false,
           xg_per90: false,
           xa_per90: false,
           xg_score: false,
           xa_score: false,
           ownership_score: false,
           minutes_score: false,
           set_piece_score: false,
         },
         analysis: {
           fdr_score: false,
           form_score: false,
           xg_score: false,
           xa_score: false,
           ownership_score: false,
           minutes_score: false,
           set_piece_score: false,
         },
       }
       ```

    3. Extend `getColumnVisibility` signature — add third optional param with default:
       ```typescript
       export function getColumnVisibility(
         horizon: 1 | 3 | 5,
         isMobile = false,
         preset: ViewPreset = 'default'
       ): Record<string, boolean> {
         const gwVisibility = {
           xPts_1gw: horizon === 1,
           xPts_3gw: horizon === 3,
           xPts_5gw: horizon === 5,
         }

         if (isMobile) {
           return { ...MOBILE_HIDDEN_COLUMNS, ...gwVisibility }
         }

         return { ...PRESET_COLUMN_VISIBILITY[preset], ...gwVisibility }
       }
       ```

       Key invariants:
       - `isMobile` check comes before preset merge — mobile always ignores preset (D-07)
       - `gwVisibility` spreads last so active xPts column always overrides any preset map entry
       - Existing 1-arg and 2-arg calls work unchanged (preset defaults to 'default')
       - The non-mobile, no-preset path now returns `{ ...PRESET_COLUMN_VISIBILITY['default'], ...gwVisibility }` instead of just `gwVisibility`. This changes the return shape for existing desktop callers — they now receive the 9 hidden sub-score columns explicitly. This is correct and expected: GemTable currently passes the result directly to TanStack, which merges it with its own defaults. The explicit `false` values for sub-score columns are harmless to TanStack (they would have been hidden by MOBILE_HIDDEN_COLUMNS on mobile anyway).

       IMPORTANT: The existing desktop test assertions check only `toEqual({ xPts_1gw: true, xPts_3gw: false, xPts_5gw: false })` using exact equality. After the change, `getColumnVisibility(1)` returns `{ ...PRESET_COLUMN_VISIBILITY['default'], xPts_1gw: true, xPts_3gw: false, xPts_5gw: false }` which has MORE keys than the test expects. This will BREAK the existing tests.

       To fix: Update the three existing desktop-only `toEqual` assertions to use `expect.objectContaining({ xPts_1gw: true, xPts_3gw: false, xPts_5gw: false })` instead of exact `toEqual`. The `not.toHaveProperty('proj_pts_*')` guards remain unchanged. The mobile tests do not use `toEqual` for the full object — they use `.toBe(false)` on specific keys — so they are unaffected.
  </behavior>

  <action>
    Follow the RED-GREEN-REFACTOR cycle described in the behavior block.

    Files to modify in order:
    1. `src/components/gem-table/GwToggle.test.ts` — add `describe('getColumnVisibility presets', ...)` block at the end of the file. Run `npx vitest run src/components/gem-table/GwToggle.test.ts` — new tests MUST fail (RED).
    2. `src/components/gem-table/GwToggle.tsx` — add `ViewPreset` type export, `PRESET_COLUMN_VISIBILITY` constant export, update `getColumnVisibility` body as described.
    3. `src/components/gem-table/GwToggle.test.ts` — update the three existing desktop `toEqual` assertions to `expect.objectContaining(...)`.
    4. Run `npx vitest run src/components/gem-table/GwToggle.test.ts` — ALL tests MUST pass (GREEN).

    Do NOT introduce `min-h-[44px]` or mobile touch sizing in this task — that is a component concern in Plan 02.
    Do NOT create `PresetToggle.tsx` in this task.
  </action>

  <verify>
    <automated>npx vitest run src/components/gem-table/GwToggle.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - GwToggle.tsx contains `export type ViewPreset = 'default' | 'compact' | 'analysis'`
    - GwToggle.tsx contains `export const PRESET_COLUMN_VISIBILITY`
    - GwToggle.tsx getColumnVisibility signature contains `preset: ViewPreset = 'default'`
    - GwToggle.tsx getColumnVisibility body contains `PRESET_COLUMN_VISIBILITY[preset]`
    - GwToggle.test.ts contains `describe('getColumnVisibility presets'`
    - All vitest tests pass: `npx vitest run src/components/gem-table/GwToggle.test.ts` exits 0
    - No TypeScript errors: `npx tsc --noEmit` exits 0 (or with zero new errors vs baseline)
  </acceptance_criteria>

  <done>
    ViewPreset type exported, PRESET_COLUMN_VISIBILITY maps match UI-SPEC §Column Visibility Maps exactly, getColumnVisibility accepts preset as optional third param, all existing tests pass, new preset tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → React state | User interacts with toggle buttons; state is in-memory only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | PRESET_COLUMN_VISIBILITY maps | accept | Maps are compile-time constants; no user input can modify them. Values are only `true`/`false` booleans that control TanStack column visibility — no data exposure risk. |
| T-37-02 | Information Disclosure | Column visibility state | accept | This is purely a UI layer — hiding a column does not delete data from the API response. TanStack visibility only controls rendering. No sensitive data flows through this mechanism. |
| T-37-03 | Denial of Service | getColumnVisibility called on every render | accept | Pure synchronous function over a fixed small object (~24 keys). O(1) cost, no I/O, no loop over user-controlled input. No DoS vector. |
</threat_model>

<verification>
1. `npx vitest run src/components/gem-table/GwToggle.test.ts` exits 0 with all tests passing
2. `npx tsc --noEmit` exits 0 (no new TypeScript errors)
3. `grep -c "export type ViewPreset" src/components/gem-table/GwToggle.tsx` returns 1
4. `grep -c "PRESET_COLUMN_VISIBILITY" src/components/gem-table/GwToggle.tsx` returns at least 2 (declaration + usage in getColumnVisibility)
5. `grep -c "getColumnVisibility presets" src/components/gem-table/GwToggle.test.ts` returns 1
</verification>

<success_criteria>
- ViewPreset type ('default' | 'compact' | 'analysis') exported from GwToggle.tsx
- PRESET_COLUMN_VISIBILITY exported with all three preset maps matching 37-UI-SPEC.md §Column Visibility Maps exactly
- getColumnVisibility(horizon) and getColumnVisibility(horizon, isMobile) call signatures unchanged; all existing tests pass
- New describe block in GwToggle.test.ts covers compact/default/analysis presets and mobile-ignores-preset invariant
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-gem-table-view-presets/37-01-SUMMARY.md`
</output>
