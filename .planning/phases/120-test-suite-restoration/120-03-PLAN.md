---
phase: 120-test-suite-restoration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useRivals.test.ts
autonomous: true
requirements: [TH-03]
tags: [test-fix, vitest, zod-schema, fpl-bootstrap, tanstack-query]

must_haves:
  truths:
    - "All useRivals tests in src/lib/hooks/useRivals.test.ts pass (8 failing tests resolved, plus the non-failing ones remain green)"
    - "The bootstrapPayload() fixture's event object includes data_checked: false so FPLEventSchema parsing succeeds"
    - "parseFPLBootstrap returns success: true for the test fixture (the hook never throws 'bootstrap shape invalid')"
    - "waitFor(() => isSuccess) resolves without timing out for every test that exercises the bootstrap fetch"
  artifacts:
    - path: "src/lib/hooks/useRivals.test.ts"
      provides: "Passing useRivals hook test suite with corrected bootstrap fixture"
      contains: "data_checked: false"
  key_links:
    - from: "src/lib/hooks/useRivals.test.ts (bootstrapPayload)"
      to: "src/lib/fpl-adapter.ts (FPLEventSchema)"
      via: "schema validation of mocked bootstrap-static response"
      pattern: "data_checked"
---

<objective>
Fix the 8 pre-existing useRivals test failures in `src/lib/hooks/useRivals.test.ts` so the suite is fully green.

Purpose: Restore TH-03. Phase 98 added a required `data_checked: z.boolean()` field to `FPLEventSchema`. The test fixture `bootstrapPayload()` was never updated, so `parseFPLBootstrap` returns `success: false`, the queryFn throws `'bootstrap shape invalid'`, the hook's `retry: 1` setting delays re-execution by ~1000ms, and `waitFor(() => isSuccess)` times out (per D-06).

The primary fix (D-07) is one line: add `data_checked: false` to the event object inside `bootstrapPayload()`. The secondary hardening (D-08) is to change `retry: false` to `retry: 0` in `makeWrapper()` to make test intent explicit (does not affect hook-level `retry: 1`, but matches the standard the Plan 02 makeWrapper also uses).

Output: All useRivals tests pass, including the 8 that were failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-test-suite-restoration/120-CONTEXT.md

@src/lib/hooks/useRivals.test.ts
@src/lib/hooks/useRivals.ts
@src/lib/fpl-adapter.ts

<interfaces>
<!-- The schema requires data_checked. Test fixtures must include it. -->

From src/lib/fpl-adapter.ts (FPLEventSchema event shape, abbreviated):
```
{
  id: number,
  is_current: boolean,
  is_next: boolean,
  finished: boolean,
  data_checked: boolean,   // Phase 98 D-06 — REQUIRED
  deadline_time: string,
  ...
}
```

Current bootstrapPayload() in src/lib/hooks/useRivals.test.ts (lines 17-25) — missing data_checked:
```
events: [
  { id: 10, is_current: true, is_next: false, finished: false, deadline_time: deadlineISO },
]
```

Current makeWrapper() (lines 11-15) — uses `retry: false`:
```
const qc = new QueryClient({ defaultOptions: { queries: { retry: false, gcTime: 0 } } })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add data_checked to bootstrapPayload and harden makeWrapper retry</name>
  <files>src/lib/hooks/useRivals.test.ts</files>
  <read_first>
    - src/lib/hooks/useRivals.test.ts (current bootstrapPayload and makeWrapper definitions)
    - src/lib/fpl-adapter.ts (FPLEventSchema — confirms data_checked is a required z.boolean())
    - src/lib/hooks/useRivals.ts (confirms retry: 1 is set per-query; QueryClient defaults are bypassed at the hook level)
    - .planning/phases/120-test-suite-restoration/120-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <behavior>
    - All 8 previously failing useRivals tests pass (ML-01, ML-08 (×2 — large league truncation + small league), D-05 (×2 — pre-deadline + post-deadline captainPlayerId), chipsRemaining ordering, ML-02 rankGap, ML-02 fallbacks)
    - parseFPLBootstrap succeeds for the test fixture: the queryFn no longer throws 'bootstrap shape invalid'
    - waitFor(() => isSuccess) resolves within the default 1000ms timeout for every test that hits bootstrap-static
    - The "numeric guard" test (non-numeric leagueId, fetchStatus === 'idle') continues to pass — it never exercises bootstrap and is unaffected
  </behavior>
  <action>
    Per D-07 (primary fix): In `src/lib/hooks/useRivals.test.ts`, update `bootstrapPayload(deadlineISO)` (currently lines 17-25) so the single event object includes `data_checked: false`. The resulting event object must be:

    `{ id: 10, is_current: true, is_next: false, finished: false, data_checked: false, deadline_time: deadlineISO }`

    Per D-08 (secondary hardening): In `makeWrapper()` (currently lines 11-15), change `retry: false` to `retry: 0` (integer). The line becomes:

    `const qc = new QueryClient({ defaultOptions: { queries: { retry: 0, gcTime: 0 } } })`

    Do not modify any other helper (`standingsPayload`, `picksPayload`, `historyPayload`, `installFetchMock`), do not modify any `it(...)` body, and do not change the `beforeEach`/`afterEach` timer setup.
  </action>
  <verify>
    <automated>npx vitest run src/lib/hooks/useRivals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/hooks/useRivals.test.ts` exits 0
    - vitest output reports all tests in the `describe('useRivals', ...)` block pass (10 tests total — 8 previously failing + 2 already passing)
    - `grep -c "data_checked: false" src/lib/hooks/useRivals.test.ts` returns at least 1
    - `grep -nE "id: 10, is_current: true" src/lib/hooks/useRivals.test.ts` shows the event line now contains `data_checked: false`
    - `grep -c "retry: false" src/lib/hooks/useRivals.test.ts` returns 0
    - `grep -c "retry: 0" src/lib/hooks/useRivals.test.ts` returns at least 1
    - The string `"bootstrap shape invalid"` does not appear in the vitest output for this file
    - The string `"Test timed out"` does not appear in the vitest output for this file
  </acceptance_criteria>
  <done>
    `bootstrapPayload()` includes `data_checked: false`; `makeWrapper()` uses `retry: 0`; all useRivals tests pass.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/hooks/useRivals.test.ts` exits 0
- No other test file is modified by this plan
- The hook's own `retry: 1` setting in `src/lib/hooks/useRivals.ts` is NOT modified (per D-08 — the schema-fix is the real solution)
</verification>

<success_criteria>
- TH-03 satisfied: all 8 useRivals failures resolved
- bootstrapPayload event object includes `data_checked: false`
- makeWrapper uses `retry: 0` (integer)
- File `src/lib/hooks/useRivals.test.ts` is the only file modified
</success_criteria>

<output>
After completion, create `.planning/phases/120-test-suite-restoration/120-03-SUMMARY.md`.
</output>
