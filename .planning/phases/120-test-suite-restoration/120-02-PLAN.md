---
phase: 120-test-suite-restoration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/MobileNav.test.tsx
autonomous: true
requirements: [TH-02]
tags: [test-fix, vitest, react-testing-library, tanstack-query, query-client-provider]

must_haves:
  truths:
    - "All 10 MobileNav tests in src/components/nav/MobileNav.test.tsx pass"
    - "No 'No QueryClient set' error is thrown by the MobileNav suite"
    - "All renders go through a makeWrapper() helper that supplies a fresh QueryClient via QueryClientProvider"
    - "Existing NAV-04 assertion (5 Squad pills, 8 total buttons including the Lineup pill) continues to pass without test-side structural changes"
  artifacts:
    - path: "src/components/nav/MobileNav.test.tsx"
      provides: "10 passing MobileNav component tests (NAV-01 through NAV-05) wrapped in QueryClientProvider"
      contains: "QueryClientProvider"
  key_links:
    - from: "src/components/nav/MobileNav.test.tsx"
      to: "@tanstack/react-query QueryClientProvider"
      via: "makeWrapper() helper passed as render({ wrapper })"
      pattern: "render\\(.*\\{.*wrapper.*\\}\\)"
    - from: "src/components/nav/MobileNav.tsx"
      to: "src/components/LastUpdated.tsx (uses useLastUpdated → TanStack Query)"
      via: "MobileNav renders <LastUpdated /> inline; tests must supply a QueryClient"
      pattern: "QueryClient"
---

<objective>
Fix the 10 pre-existing MobileNav test failures (NAV-01 through NAV-05 in `src/components/nav/MobileNav.test.tsx`) so the suite is fully green.

Purpose: Restore TH-02. `MobileNav` renders `<LastUpdated />` inline, which calls the `useLastUpdated()` TanStack Query hook. The tests have no QueryClientProvider, so every render throws `"No QueryClient set, use QueryClientProvider to set one"`. Per D-04, the fix is to wrap every render in a QueryClientProvider via a `makeWrapper()` helper — not to mock `LastUpdated`.

Per D-05, the Phase 119 Lineup sub-tab addition is already reflected in the NAV-04 test assertion (5 Squad pills, 8 total buttons). No structural / button-count edits are needed — only the provider wrapper.

Output: Ten passing MobileNav tests, all using a shared `makeWrapper()` helper that provides a fresh QueryClient with `retry: 0, gcTime: 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-test-suite-restoration/120-CONTEXT.md

@src/components/nav/MobileNav.test.tsx
@src/components/nav/MobileNav.tsx
@src/lib/hooks/useRivals.test.ts
@src/components/LastUpdated.tsx

<interfaces>
<!-- The makeWrapper pattern below is the canonical project pattern, copied verbatim from useRivals.test.ts. -->

Reference pattern from src/lib/hooks/useRivals.test.ts (lines 11-15):
```
function makeWrapper() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false, gcTime: 0 } } })
  return ({ children }: { children: ReactNode }) =>
    createElement(QueryClientProvider, { client: qc }, children)
}
```

React Testing Library API (already imported in the file):
- `render(ui, { wrapper })` accepts a `wrapper` option to inject a provider component around the rendered tree.

Required new imports for MobileNav.test.tsx:
- `QueryClient`, `QueryClientProvider` from `@tanstack/react-query`
- `type ReactNode` from `react`
- `createElement` from `react` (so the test file can stay `.tsx` and use JSX OR createElement — either is acceptable; choose JSX since this file is .tsx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add makeWrapper() helper and wrap all 10 renders in QueryClientProvider</name>
  <files>src/components/nav/MobileNav.test.tsx</files>
  <read_first>
    - src/components/nav/MobileNav.test.tsx (current state — 10 failing tests, no provider wrapper)
    - src/lib/hooks/useRivals.test.ts (canonical makeWrapper pattern to replicate)
    - src/components/nav/MobileNav.tsx (confirms MobileNav renders <LastUpdated /> inline)
    - src/components/LastUpdated.tsx (confirms useLastUpdated is a TanStack Query hook)
    - .planning/phases/120-test-suite-restoration/120-CONTEXT.md (D-03, D-04, D-05)
  </read_first>
  <behavior>
    - All 10 existing test cases (NAV-01 through NAV-05, plus the Phase 62 / 97 D-02 assertion and the WR-04 click-coverage test) pass
    - Every `render(<MobileNav ... />)` call goes through `makeWrapper()` so `useLastUpdated()` resolves against a real QueryClient
    - No `"No QueryClient set"` error appears in the vitest output
    - Existing assertion in NAV-04 — `expect(allButtons).toHaveLength(8)` with 5 Squad pills including the Lineup pill — passes unchanged (per D-05; Phase 119 Lineup tab already expected)
  </behavior>
  <action>
    Per D-04, add a `makeWrapper()` helper at the top of `src/components/nav/MobileNav.test.tsx` (after the existing imports, before `function makeProps(...)`). Mirror the exact pattern from `src/lib/hooks/useRivals.test.ts` lines 11-15:

    - Import `QueryClient` and `QueryClientProvider` from `@tanstack/react-query`
    - Import `type ReactNode` from `react`
    - Define `function makeWrapper()` that returns a wrapper component which renders `<QueryClientProvider client={qc}>{children}</QueryClientProvider>` (since this file is `.tsx`, prefer JSX form over `createElement`)
    - The QueryClient inside `makeWrapper()` must use `defaultOptions: { queries: { retry: 0, gcTime: 0 } }` — use `retry: 0` (integer), not `retry: false`, for consistency with the planned hardening in Plan 03

    Update every `render(...)` call in the file to pass `{ wrapper: makeWrapper() }` as the second argument. There are 10 render call sites (one per `it(...)` block). Each becomes:

    `render(<MobileNav ... />, { wrapper: makeWrapper() })`

    Per D-03 / D-04, do NOT mock `LastUpdated`, do NOT mock `useLastUpdated`, do NOT mock `@tanstack/react-query`. The real component tree must execute with a real provider.

    Per D-05, do NOT change any assertion in the NAV-04 test ("Squad active: pill row shows 5 pills..."); the button counts and pill ordering are already correct for the Phase 119 Lineup addition.

    Do not modify `makeProps(...)`, fixtures, or any assertion bodies.
  </action>
  <verify>
    <automated>npx vitest run src/components/nav/MobileNav.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/nav/MobileNav.test.tsx` exits 0
    - vitest output reports 10 passed tests (NAV-01 through NAV-05, plus the Phase 62 Rank Sim assertion and the analyse-pills click-coverage test)
    - `grep -c "QueryClientProvider" src/components/nav/MobileNav.test.tsx` returns at least 2 (import + JSX usage in makeWrapper)
    - `grep -c "makeWrapper()" src/components/nav/MobileNav.test.tsx` returns at least 11 (1 definition + 10 render call sites)
    - `grep -c "wrapper: makeWrapper()" src/components/nav/MobileNav.test.tsx` returns 10
    - `grep -c "vi.mock('@/components/LastUpdated'" src/components/nav/MobileNav.test.tsx` returns 0 (no mocking — real component tree)
    - The string `"No QueryClient set"` does not appear in the vitest output for this file
    - The NAV-04 test ("Squad active: pill row shows 5 pills Decision, Transfers, Optimiser, Lineup, Review; total 8 buttons in DOM") passes with the assertions `expect(allButtons).toHaveLength(8)` and `expect(pillButtons).toHaveLength(5)` unchanged
  </acceptance_criteria>
  <done>
    All 10 MobileNav tests pass; every render uses `{ wrapper: makeWrapper() }`; no `LastUpdated` mock was introduced; the Lineup pill assertion is intact.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/nav/MobileNav.test.tsx` exits 0 with 10 passing tests
- No other test file is modified by this plan
- Real `LastUpdated` component is exercised through `QueryClientProvider` (not mocked)
</verification>

<success_criteria>
- TH-02 satisfied: all 10 MobileNav failures resolved
- `makeWrapper()` helper added, modelled on `useRivals.test.ts`
- All renders wrapped in QueryClientProvider
- File `src/components/nav/MobileNav.test.tsx` is the only file modified
</success_criteria>

<output>
After completion, create `.planning/phases/120-test-suite-restoration/120-02-SUMMARY.md`.
</output>
