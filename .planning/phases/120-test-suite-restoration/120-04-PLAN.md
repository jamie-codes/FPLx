---
phase: 120-test-suite-restoration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/club-form.test.ts
autonomous: true
requirements: [TH-04]
tags: [test-fix, vitest, fixture-data, fpl-difficulty]

must_haves:
  truths:
    - "All tests in tests/lib/club-form.test.ts pass — including the difficulty-tier classification test that was failing"
    - "The upcoming fixture event 32 (ARS home vs BUR) uses team_h_difficulty: 2 so difficulty_score = (2 - 1) / 4 = 0.25, satisfying toBeLessThan(0.5)"
    - "The test fixture changes alone resolve the failure — the production source file src/lib/club-form.ts is NOT modified"
  artifacts:
    - path: "tests/lib/club-form.test.ts"
      provides: "Passing club-form test suite with corrected difficulty fixture for event 32"
      contains: "team_h_difficulty: 2"
  key_links:
    - from: "tests/lib/club-form.test.ts (event 32 fixture)"
      to: "src/lib/club-form.ts (fplToAttDiff)"
      via: "FPL difficulty rating → difficulty_score = (fpl - 1) / 4"
      pattern: "team_h_difficulty: 2"
---

<objective>
Fix the 1 pre-existing club-form test failure in `tests/lib/club-form.test.ts` (the "assigns difficulty tier correctly — strong team is hard, weak team is easy" assertion at the bottom of the file).

Purpose: Restore TH-04. Per D-09, `difficulty_score` in `club-form.ts` was changed from xGA-based to FPL official difficulty ratings via `fplToAttDiff(fplDiff) = (fpl - 1) / 4`. All test fixtures currently hardcode FPL difficulty 3, giving every upcoming fixture `difficulty_score = (3 - 1) / 4 = 0.5`. The assertion `expect(vsBur!.difficulty_score).toBeLessThan(0.5)` fails on the boundary because 0.5 is not less than 0.5.

Per D-10, fix the test fixture (not the assertion): update the upcoming fixture `event 32` (ARS home vs BUR) inside `makeFixtures()` from `team_h_difficulty: 3` to `team_h_difficulty: 2`. This reflects realistic data (easy home game vs a weak team) and yields `difficulty_score = (2 - 1) / 4 = 0.25 < 0.5`.

Output: All tests in the file pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-test-suite-restoration/120-CONTEXT.md

@tests/lib/club-form.test.ts
@src/lib/club-form.ts

<interfaces>
<!-- difficulty_score derives directly from team_*_difficulty via fplToAttDiff. -->

From src/lib/club-form.ts:
```
fplToAttDiff(fplDiff: number): number   // returns (fplDiff - 1) / 4
```

Mapping for the upcoming fixture difficulty (home team perspective):
- team_h_difficulty: 1 → difficulty_score 0.00
- team_h_difficulty: 2 → difficulty_score 0.25   ← target value for event 32
- team_h_difficulty: 3 → difficulty_score 0.50   ← current value (causes boundary failure)
- team_h_difficulty: 4 → difficulty_score 0.75
- team_h_difficulty: 5 → difficulty_score 1.00

Current event 32 fixture line in tests/lib/club-form.test.ts (inside makeFixtures(), around line 24):
```
{ team_h: 1, team_a: 3, team_h_difficulty: 3, team_a_difficulty: 3, team_h_score: null, team_a_score: null, event: 32, finished: false },
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update event 32 upcoming fixture difficulty to 2 in makeFixtures()</name>
  <files>tests/lib/club-form.test.ts</files>
  <read_first>
    - tests/lib/club-form.test.ts (current makeFixtures() definition and the failing "assigns difficulty tier correctly" test at the bottom of the file)
    - src/lib/club-form.ts (confirms fplToAttDiff = (fpl - 1) / 4 — do NOT modify this source file)
    - .planning/phases/120-test-suite-restoration/120-CONTEXT.md (D-09 root cause, D-10 fix)
  </read_first>
  <behavior>
    - Test "assigns difficulty tier correctly — strong team is hard, weak team is easy" passes (the assertion `expect(vsBur!.difficulty_score).toBeLessThan(0.5)` now evaluates 0.25 < 0.5 → true)
    - All other already-passing tests in the file remain passing (no regression in W/D/L counts, DGW handling, FDR++ ease arrays, BGW null behaviour, defensive_difficulty window assertions, etc.)
    - The 6 finished fixtures and the 2 other upcoming fixtures (event 30 CHE-vs-ARS, event 31 BUR-vs-CHE) are NOT modified
  </behavior>
  <action>
    Per D-10: In `tests/lib/club-form.test.ts`, inside `makeFixtures()` (around line 24), locate the single fixture line for event 32:

    `{ team_h: 1, team_a: 3, team_h_difficulty: 3, team_a_difficulty: 3, team_h_score: null, team_a_score: null, event: 32, finished: false },`

    Change `team_h_difficulty: 3` to `team_h_difficulty: 2` on that line. Leave `team_a_difficulty: 3` unchanged. The resulting line is:

    `{ team_h: 1, team_a: 3, team_h_difficulty: 2, team_a_difficulty: 3, team_h_score: null, team_a_score: null, event: 32, finished: false },`

    Do not modify any other fixture line, any other `team_h_difficulty` value, the `bootstrap` object, the source file `src/lib/club-form.ts`, or any assertion text.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/club-form.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/club-form.test.ts` exits 0
    - vitest output reports all tests in the `describe('computeClubForm', ...)` block pass
    - `grep -c "event: 32, finished: false" tests/lib/club-form.test.ts` returns 1
    - `grep -n "team_h_difficulty: 2" tests/lib/club-form.test.ts` returns at least one line, and that line also contains `event: 32`
    - `src/lib/club-form.ts` is not modified (git diff shows no changes to that file)
    - The string "expected 0.5 to be less than 0.5" no longer appears in the vitest output for this file
  </acceptance_criteria>
  <done>
    Event 32 fixture uses `team_h_difficulty: 2`; all club-form tests pass; source file `src/lib/club-form.ts` is untouched.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/lib/club-form.test.ts` exits 0
- `git diff --stat src/lib/club-form.ts` shows no changes
- No other test file is modified by this plan
</verification>

<success_criteria>
- TH-04 satisfied: the 1 club-form failure resolved
- Test fixture updated (not the assertion, not the source)
- File `tests/lib/club-form.test.ts` is the only file modified
</success_criteria>

<output>
After completion, create `.planning/phases/120-test-suite-restoration/120-04-SUMMARY.md`.
</output>
