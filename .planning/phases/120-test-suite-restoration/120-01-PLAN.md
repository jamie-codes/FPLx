---
phase: 120-test-suite-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/captain-picks.test.ts
autonomous: true
requirements: [TH-01]
tags: [test-fix, vitest, react-testing-library, jsx-render]

must_haves:
  truths:
    - "All 5 CaptainPicksPanel component tests in tests/lib/captain-picks.test.ts pass"
    - "No 'Cannot read properties of null (reading useState)' errors are thrown by the captain-picks suite"
    - "Each render call uses JSX form <CaptainPicksPanel /> rather than a plain function call"
  artifacts:
    - path: "tests/lib/captain-picks.test.ts"
      provides: "5 passing CaptainPicksPanel component tests (CAP-03/CAP-04)"
      contains: "render(<CaptainPicksPanel />)"
  key_links:
    - from: "tests/lib/captain-picks.test.ts"
      to: "src/components/captaincy/CaptainPicksPanel.tsx"
      via: "JSX render call inside React Testing Library"
      pattern: "render\\(<CaptainPicksPanel \\/>\\)"
---

<objective>
Fix the 5 pre-existing captain-picks test failures (CAP-03 / CAP-04 CaptainPicksPanel rendering tests in `tests/lib/captain-picks.test.ts`) so the suite is fully green.

Purpose: Restore TH-01. The component calls `useState` and therefore must be rendered through React's reconciler. Calling the component as a plain function (`CaptainPicksPanel({})`) bypasses the renderer, so `useState` resolves to `null` and the suite throws "Cannot read properties of null (reading 'useState')".

Output: Five passing component tests inside `describe('Phase 31: CaptainPicksPanel component', ...)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-test-suite-restoration/120-CONTEXT.md

@tests/lib/captain-picks.test.ts
@src/components/captaincy/CaptainPicksPanel.tsx

<interfaces>
<!-- The component is a plain React function component. The mocked hook returns a UseQueryResult-shaped value. -->

From src/components/captaincy/CaptainPicksPanel.tsx:
```
export function CaptainPicksPanel(): JSX.Element
// Uses React.useState internally; must be rendered as JSX so React's reconciler runs.
```

From src/lib/hooks/useCaptainPicks.ts (mocked in this test file via vi.mock):
```
export function useCaptainPicks(): UseQueryResult<CaptainPicksData, Error>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert all 5 CaptainPicksPanel render calls to JSX</name>
  <files>tests/lib/captain-picks.test.ts</files>
  <read_first>
    - tests/lib/captain-picks.test.ts (current state — 5 failing tests in the component describe block)
    - src/components/captaincy/CaptainPicksPanel.tsx (confirms the component uses React.useState and must be rendered through React, not invoked as a function)
    - .planning/phases/120-test-suite-restoration/120-CONTEXT.md (D-01, D-02 root cause and fix)
  </read_first>
  <behavior>
    - Test "renders ceiling card with player name and xPts when data loaded (CAP-03)" passes
    - Test "renders EO-Adjusted card with player name and ownership (CAP-04)" passes
    - Test "shows same-player note when ceiling.id === eo_adjusted.id (CAP-03/04 edge case)" passes
    - Test "shows loading state with locked copy (CAP-03/04)" passes
    - Test "shows error state with locked copy (CAP-03/04)" passes
    - Suite no longer throws "Cannot read properties of null (reading 'useState')"
  </behavior>
  <action>
    Per D-01: Replace every occurrence of `render(CaptainPicksPanel({}))` with `render(<CaptainPicksPanel />)` inside the `describe('Phase 31: CaptainPicksPanel component', ...)` block (lines 124-217). There are 5 such call sites — one in each of the 5 component tests. No other test code (assertions, mocks, fixtures) changes.

    Per D-02: Do not add a QueryClientProvider wrapper — the file already mocks `@/lib/hooks/useCaptainPicks` via `vi.mock(...)` at the top, so the hook never makes a real query. JSX render is sufficient.

    Do not touch the `describe('Phase 31: Captain picks pipeline output', ...)` block (lines 20-122) — those tests are `it.skip` and out of scope.

    Do not touch the trailing Wave 0 stub assertion (line 219) — it must continue to pass.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/captain-picks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/captain-picks.test.ts` exits 0
    - vitest output reports 5 passed tests in the "Phase 31: CaptainPicksPanel component" describe block (plus 8 skipped pipeline tests and 1 Wave 0 stub)
    - `grep -c "render(CaptainPicksPanel({" tests/lib/captain-picks.test.ts` returns 0
    - `grep -c "render(<CaptainPicksPanel />)" tests/lib/captain-picks.test.ts` returns 5
    - No "Cannot read properties of null (reading 'useState')" string appears in the vitest output for this file
  </acceptance_criteria>
  <done>
    All 5 CaptainPicksPanel component tests pass; all 5 call sites use JSX; no `CaptainPicksPanel({})` invocation remains.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/lib/captain-picks.test.ts` exits 0 with 5 passing component tests
- No other test file is modified by this plan
- The skipped pipeline tests in the same file remain skipped (not changed)
</verification>

<success_criteria>
- TH-01 satisfied: all 5 captain-picks failures resolved
- Render calls use JSX form
- No QueryClientProvider was added (mocks remain sufficient per D-02)
- File `tests/lib/captain-picks.test.ts` is the only file modified
</success_criteria>

<output>
After completion, create `.planning/phases/120-test-suite-restoration/120-01-SUMMARY.md`.
</output>
