---
phase: 84-set-piece-threat-assisted-pipeline
verified: 2026-05-09T00:00:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 84: Set-Piece Threat Assisted Pipeline Verification Report

**Phase Goal:** Pipeline produces a per-taker measure of set-piece delivery quality — aggregate xG generated by shots they assisted from corners and direct free kicks — written as a separate artifact so a scrape failure cannot poison merged_players.json
**Verified:** 2026-05-09
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Pipeline scrapes per-team Understat shot events; filtered to `situation IN ('FromCorner', 'DirectFreekick')`, aggregated by `player_assisted` NOT shooter; 24h disk cache | VERIFIED | `_scrape_all_teams()`, `_aggregate_shots()` filter on CORNER_SITUATION/FK_SITUATION; `_is_sp_cache_fresh()` / `_write_sp_cache()` implement 24h cache at sp_shots_cache.json |
| 2 | Per-taker output in `sp_quality.json` includes `corner_danger_score` (null when n<5), `fk_danger_score` (null when n<3), `delivery_quality_rank` (EB shrinkage k=20, null when both null), `sp_sample_n` | VERIFIED | `_compute_per_taker_scores()` lines 141-207; CORNER_MIN_N=5, FK_MIN_N=3, SHRINKAGE_K=20; all 5 fields written per entry |
| 3 | Entire scrape step in run.py wrapped in try/except (mirrors prose_summary pattern); a 403 or network failure does NOT poison merged_players.json | VERIFIED | run.py lines 237-246: `sp_unmatched_count = None` before try; lazy import + call inside try; except prints to stderr; merged_players.json saved at line 227 — before and independent of the SP block |
| 4 | Unmatched Understat IDs logged; count surfaced in DH-01 `data_health.json sanity_checks[]` | VERIFIED | `_compute_per_taker_scores()` increments `unmatched_count`; run.py threads `sp_unmatched_count` to `compute_data_health()` (line 415); `_check_sp_unmatched()` in data_health.py appends 5th sanity check when int |
| 5 | pytest case feeds mixed-situation fixture (corners, FKs, open play, penalties); asserts only FromCorner and DirectFreekick counted, grouped by player_assisted | VERIFIED | `test_only_corner_and_fk_counted()` in test_set_piece_quality.py: 5 shots covering all 4 situations; asserts deliverer (10) in both buckets; shooters (99, 88, 77) absent; 12 passed |

### Plan 01 Must-Have Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `run_sp_quality(understat_data, id_map, cache_dir) -> int \| None` signature | VERIFIED | set_piece_quality.py line 282 |
| 2 | `_aggregate_shots()` filters to situation IN ('FromCorner','DirectFreekick') only | VERIFIED | Lines 122-125: if/elif on CORNER_SITUATION/FK_SITUATION; OpenPlay and Penalty fall through with no append |
| 3 | `_aggregate_shots()` groups by `player_assisted_id` (deliverer), NOT `player_id` | VERIFIED | Lines 110-111: `deliverer_id = shot.get('player_assisted_id')`; test_aggregates_by_deliverer_not_shooter confirms |
| 4 | `corner_danger_score` is null when n_corner < 5 | VERIFIED | Line 181: `corner_score = statistics.mean(c_xgs) if n_corner >= CORNER_MIN_N else None` |
| 5 | `fk_danger_score` is null when n_fk < 3 | VERIFIED | Line 182: `fk_score = statistics.mean(f_xgs) if n_fk >= FK_MIN_N else None` |
| 6 | `delivery_quality_rank` uses EB shrinkage with k=20 | VERIFIED | SHRINKAGE_K=20; `_shrink()` formula w = n/(n+k); rank_score is mean of shrunk components |
| 7 | `delivery_quality_rank` is null when both corner and FK scores are null | VERIFIED | Lines 185-193: explicit `if corner_score is None and fk_score is None: rank_score = None` |
| 8 | sp_quality.json keyed by FPL player ID string; includes `understat_id` field (D-06) | VERIFIED | Lines 200-206: key is `fpl_id` (str from reverse_id_map); dict includes `'understat_id': did` |
| 9 | On scrape failure, `run_sp_quality()` returns None; does NOT overwrite sp_quality.json (D-07) | VERIFIED | Lines 339-342: outer try/except returns None; `save('sp_quality.json', sp_quality)` at line 337 only reached on success |
| 10 | On success, `run_sp_quality()` returns unmatched-Understat-ID count as int | VERIFIED | Line 338: `return unmatched_count` (int) |
| 11 | 24h disk cache at pipeline/cache/sp_shots_cache.json with `_cached_at` ISO sentinel | VERIFIED | SP_SHOTS_CACHE_PATH resolves to .../cache/sp_shots_cache.json; `_write_sp_cache()` writes `{'shots': ..., '_cached_at': datetime.now(...).isoformat()}` |
| 12 | All open() calls pass encoding='utf-8' | VERIFIED | grep confirms 4 instances in set_piece_quality.py (read and write cache calls) |
| 13 | run.py wraps run_sp_quality() in try/except; sp_unmatched_count initialised to None BEFORE try | VERIFIED | Lines 237-246: `sp_unmatched_count = None` at line 237; try block at line 238 |
| 14 | test_set_piece_quality.py SPQ-02 case with mixed-situation fixture asserts only FromCorner/DirectFreekick counted, grouped by player_assisted | VERIFIED | test_only_corner_and_fk_counted() confirmed present and passing |
| 15 | pytest pipeline/tests/test_set_piece_quality.py exits 0 | VERIFIED | 12 passed in 0.02s (actual count 12, plan stated 11; the extra test_xg_string_coerced_to_float was intentionally included per 84-01-SUMMARY deviation note) |
| 16 | Full pytest suite exits 0 | VERIFIED | 173 passed in 0.32s |

### Plan 02 Must-Have Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `compute_data_health()` accepts `sp_unmatched_count: int \| None = None` kwarg | VERIFIED | data_health.py line 96 |
| 2 | When sp_unmatched_count=None, sanity_checks list remains exactly 4 entries | VERIFIED | `if sp_unmatched_count is not None:` guard at line 152; test_compute_data_health_shape passes (1 passed) |
| 3 | When sp_unmatched_count is int, sanity_checks has 5 entries with sp_unmatched_ids appended last | VERIFIED | test_sp_unmatched_check_appended_when_int passes with sp_unmatched_count=25 |
| 4 | `_check_sp_unmatched(0..5)` returns status='ok' | VERIFIED | Lines 81-82; test_sp_unmatched_threshold_ok (count=3) passes |
| 5 | `_check_sp_unmatched(6..20)` returns status='warn' | VERIFIED | Lines 83-84; test_sp_unmatched_threshold_warn (count=15) passes |
| 6 | `_check_sp_unmatched(21+)` returns status='error' | VERIFIED | Lines 85-86; test_sp_unmatched_threshold_error_boundary (count=21) passes |
| 7 | `_check_sp_unmatched(N)` returns dict with id='sp_unmatched_ids', value=N, threshold='<= 5' | VERIFIED | Line 88: `return {'id': 'sp_unmatched_ids', 'status': status, 'value': count, 'threshold': '<= 5'}` |
| 8 | run.py compute_data_health() call site passes `sp_unmatched_count=sp_unmatched_count` | VERIFIED | Lines 414-415 confirmed |
| 9 | All existing 11 test_data_health.py tests remain GREEN | VERIFIED | 16 passed (11 pre-existing + 5 new) |
| 10 | pytest pipeline/tests/test_data_health.py exits 0 with 16 tests | VERIFIED | 16 passed in 0.06s |
| 11 | Full pytest suite remains GREEN | VERIFIED | 173 passed |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `pipeline/set_piece_quality.py` | run_sp_quality entry point — Understat shot scrape, EB shrinkage, sp_quality.json writer | VERIFIED | 343 lines; all 7 functions present |
| `pipeline/set_piece_quality.py` | `def _aggregate_shots(` | VERIFIED | Line 89 |
| `pipeline/set_piece_quality.py` | `def _parse_shots(` | VERIFIED | Line 67 |
| `pipeline/set_piece_quality.py` | `SHRINKAGE_K = 20` | VERIFIED | Line 59 |
| `pipeline/set_piece_quality.py` | `sp_shots_cache.json` constant | VERIFIED | Line 31 |
| `pipeline/tests/test_set_piece_quality.py` | SPQ-01/SPQ-02 pytest cases | VERIFIED | 12 tests; `from set_piece_quality import` present |
| `pipeline/run.py` | try/except isolation; import run_sp_quality | VERIFIED | Lines 237-246 |
| `pipeline/data_health.py` | `def _check_sp_unmatched(` | VERIFIED | Line 74 |
| `pipeline/data_health.py` | `sp_unmatched_count: int \| None = None` in signature | VERIFIED | Line 96 |
| `pipeline/data_health.py` | `sanity_checks.append(_check_sp_unmatched(sp_unmatched_count))` | VERIFIED | Line 153 |
| `pipeline/tests/test_data_health.py` | `def test_sp_unmatched_check_appended` | VERIFIED | Line 195 |
| `pipeline/tests/test_data_health.py` | `def test_sp_unmatched_omitted_when_none` | VERIFIED | Line 182 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| pipeline/run.py | pipeline/set_piece_quality.py | `from set_piece_quality import run_sp_quality` | WIRED | Lazy import inside try block at line 239; confirmed by grep (1 match) |
| pipeline/set_piece_quality.py | pipeline/upload.py save() | `from upload import save` | WIRED | Local import inside run_sp_quality() at line 336; mirrors data_health.py pattern |
| pipeline/tests/test_set_piece_quality.py | pipeline/set_piece_quality.py | `from set_piece_quality import _aggregate_shots, _compute_per_taker_scores, _shrink, SHRINKAGE_K` | WIRED | Lines 17-22; 12 tests pass |
| pipeline/run.py | pipeline/data_health.py compute_data_health() | `sp_unmatched_count=sp_unmatched_count` kwarg | WIRED | run.py line 415; variable in scope from line 237 |
| pipeline/data_health.py compute_data_health() | pipeline/data_health.py _check_sp_unmatched() | conditional append when sp_unmatched_count is not None | WIRED | Lines 152-153 |
| pipeline/tests/test_data_health.py new cases | pipeline/data_health.py compute_data_health() | `sp_unmatched_count=` kwarg invocations | WIRED | 5 new tests; all pass |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `run_sp_quality()` | `all_shots` | `_scrape_all_teams()` (HTTP) or `_load_sp_cache()` (disk) | Yes — HTTP scrape or 24h cache | FLOWING |
| `_aggregate_shots()` | `corner_shots`, `fk_shots` | `all_shots` list filtered by situation | Yes — filters from real shot data | FLOWING |
| `_compute_per_taker_scores()` | `sp_quality` dict | `corner_shots`, `fk_shots`, `reverse_id_map` | Yes — computes from aggregated xG | FLOWING |
| `compute_data_health()` | `sanity_checks[4]` sp entry | `sp_unmatched_count` from run_sp_quality() | Yes — real count or omitted on None | FLOWING |

Note: Level 4 for the HTTP scrape path cannot be exercised without a live network call, which is appropriately guarded behind the 24h cache. The data-flow is structurally complete and verified by the unit tests using real fixture data.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 12 SPQ unit tests pass | `pytest pipeline/tests/test_set_piece_quality.py -q` | 12 passed in 0.02s | PASS |
| 16 data-health tests pass (11 old + 5 new) | `pytest pipeline/tests/test_data_health.py -q` | 16 passed in 0.06s | PASS |
| Full suite: 173 tests green | `pytest pipeline/tests/ -q` | 173 passed in 0.32s | PASS |
| run.py parses cleanly | `ast.parse(run.py)` | OK | PASS |
| set_piece_quality.py parses cleanly | `ast.parse(set_piece_quality.py)` | OK | PASS |
| data_health.py parses cleanly | `ast.parse(data_health.py)` | OK | PASS |
| SHRINKAGE_K == 20 at import | `from pipeline.set_piece_quality import SHRINKAGE_K` | 20 | PASS |
| No Phase 83 leak (lambda_opp / 3) | `grep "lambda_opp / 3" set_piece_quality.py` | 0 matches | PASS |
| encoding='utf-8' on all file opens | `grep "encoding='utf-8'" set_piece_quality.py` | 4 matches | PASS |
| player_assisted_id used (not player_id) | `grep -v "^#" set_piece_quality.py \| grep "player_assisted_id"` | 6 matches | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SPQ-01 | 84-01-PLAN.md | Pipeline scrapes Understat shots; filters FromCorner/DirectFreekick; 24h disk cache; try/except isolation in run.py | SATISFIED | set_piece_quality.py: _scrape_all_teams(), _aggregate_shots(), _is_sp_cache_fresh(), _write_sp_cache(); run.py try/except block lines 237-246 |
| SPQ-02 | 84-01-PLAN.md, 84-02-PLAN.md | Per-taker scores with sample gates; EB shrinkage k=20; unmatched count surfaced in DH-01 | SATISFIED | _compute_per_taker_scores() with CORNER_MIN_N=5/FK_MIN_N=3/SHRINKAGE_K=20; compute_data_health() _check_sp_unmatched() appended when int; 12 SPQ + 5 DH tests cover all invariants |

SPQ-03 is mapped to Phase 85 in REQUIREMENTS.md and is not in scope for Phase 84.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| None detected | — | — | — |

No TODO/FIXME/placeholder comments found. No stub return values. No hollow props. No hardcoded empty data structures in production paths.

### Human Verification Required

None. All behavioral invariants are verifiable programmatically through the pytest suite.

### Gaps Summary

No gaps. All 16 must-have truths are VERIFIED, all key links are WIRED, all artifacts are substantive, the full test suite is GREEN (173 tests), and both requirement IDs (SPQ-01, SPQ-02) are fully satisfied.

The one deviation from the plan is that test_set_piece_quality.py contains 12 tests (not 11 as the acceptance criteria mention). This is documented in 84-01-SUMMARY.md: the extra `test_xg_string_coerced_to_float` was present in the plan's provided content block and was intentionally included. It is additive and does not indicate missing coverage.

---

_Verified: 2026-05-09_
_Verifier: Claude (gsd-verifier)_
