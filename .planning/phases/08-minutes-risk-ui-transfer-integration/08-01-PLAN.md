---
phase: 08-minutes-risk-ui-transfer-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/MinsRiskBadge.tsx
  - tests/lib/mins-risk-badge.test.ts
  - src/components/squad/SquadView.tsx
  - src/components/gem-table/columns.tsx
autonomous: true
requirements: [MINS-02]

must_haves:
  truths:
    - "User can see a Nailed / Likely start / Rotation risk / Cameo risk badge on each player row in SquadView"
    - "User can see a Nailed / Likely start / Rotation risk / Cameo risk badge on each player row in GemTable"
    - "Players with injured mins_risk show no MinsRisk badge (existing StatusBadge dot suffices)"
    - "Risk column appears between Status and Trend in GemTable, and after Status in SquadView"
  artifacts:
    - path: "src/components/shared/MinsRiskBadge.tsx"
      provides: "Shared badge component mapping MinsRisk to colored span"
      exports: ["MinsRiskBadge", "getMinsRiskConfig"]
    - path: "tests/lib/mins-risk-badge.test.ts"
      provides: "Unit tests for getMinsRiskConfig pure function"
      min_lines: 20
    - path: "src/components/squad/SquadView.tsx"
      provides: "Risk column header and MinsRiskBadge cell in squad table"
      contains: "MinsRiskBadge"
    - path: "src/components/gem-table/columns.tsx"
      provides: "Risk display column in GemTable between Status and Trend"
      contains: "mins_risk"
  key_links:
    - from: "src/components/shared/MinsRiskBadge.tsx"
      to: "src/lib/types.ts"
      via: "import type { MinsRisk }"
      pattern: "import type.*MinsRisk.*from.*@/lib/types"
    - from: "src/components/squad/SquadView.tsx"
      to: "src/components/shared/MinsRiskBadge.tsx"
      via: "import { MinsRiskBadge }"
      pattern: "import.*MinsRiskBadge.*from.*@/components/shared/MinsRiskBadge"
    - from: "src/components/gem-table/columns.tsx"
      to: "src/components/shared/MinsRiskBadge.tsx"
      via: "import { MinsRiskBadge }"
      pattern: "import.*MinsRiskBadge.*from.*@/components/shared/MinsRiskBadge"
---

<objective>
Create the MinsRiskBadge shared component and add a "Risk" column to both SquadView and GemTable, satisfying MINS-02.

Purpose: Managers can see at a glance which players are nailed starters vs rotation risks in both the squad and gem rating views.
Output: MinsRiskBadge.tsx component, Risk column in SquadView and GemTable, unit tests for badge logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-minutes-risk-ui-transfer-integration/08-RESEARCH.md
@.planning/phases/08-minutes-risk-ui-transfer-integration/08-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/types.ts:
```typescript
export type MinsRisk = 'nailed' | 'likely_start' | 'rotation_risk' | 'cameo' | 'injured'
```

MinsRisk is a field on MergedPlayer (and therefore ScoredPlayer). Every player has a non-nullable mins_risk value.

From src/components/squad/SquadView.tsx:
```typescript
interface SquadViewProps {
  picks: SquadPick[]
  allPlayers: ScoredPlayer[]
  entryHistory: EntryHistory
}
```
SquadView is a hand-rolled table (not TanStack). Column headers are in a `<thead><tr>` with `<th>` elements. Each row renders `<td>` cells. The existing column order is: Player | Team | Price | Own% | Mins | Gem | Status.

From src/components/gem-table/columns.tsx:
```typescript
import { createColumnHelper } from '@tanstack/react-table'
import type { ScoredPlayer } from '@/lib/types'
const col = createColumnHelper<ScoredPlayer>()
```
GemTable uses TanStack React Table. Display columns use `col.display({ id, header, cell, enableSorting })`. The `trend` display column is currently right after the `status` accessor. The `fixtures` display column follows `trend`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MinsRiskBadge component with testable helper</name>
  <files>src/components/shared/MinsRiskBadge.tsx, tests/lib/mins-risk-badge.test.ts</files>
  <read_first>
    - src/lib/types.ts (MinsRisk type definition)
    - .planning/phases/08-minutes-risk-ui-transfer-integration/08-UI-SPEC.md (rendering contract, exact class names, tooltip text)
  </read_first>
  <behavior>
    - getMinsRiskConfig('nailed') returns { bg: 'bg-green-100', text: 'text-green-800', label: 'Nailed', title: 'Nailed: high start probability (>=85%)' }
    - getMinsRiskConfig('likely_start') returns { bg: 'bg-blue-100', text: 'text-blue-800', label: 'Likely start', title: 'Likely start: moderate start probability (65-84%)' }
    - getMinsRiskConfig('rotation_risk') returns { bg: 'bg-amber-100', text: 'text-amber-800', label: 'Rotation risk', title: 'Rotation risk: rotation risk identified' }
    - getMinsRiskConfig('cameo') returns { bg: 'bg-zinc-100', text: 'text-zinc-600', label: 'Cameo', title: 'Cameo: low minutes expected' }
    - getMinsRiskConfig('injured') returns null
    - getMinsRiskConfig(undefined as any) returns null
  </behavior>
  <action>
    1. Create directory `src/components/shared/` (does not exist yet).

    2. Create `src/components/shared/MinsRiskBadge.tsx` with:
       - Import `type { MinsRisk } from '@/lib/types'`
       - Define `Config` interface: `{ bg: string; text: string; label: string; title: string }`
       - Define `BADGE_MAP: Record<Exclude<MinsRisk, 'injured'>, Config>` with exact values:
         - nailed: bg='bg-green-100', text='text-green-800', label='Nailed', title='Nailed: high start probability (\u226585%)'
         - likely_start: bg='bg-blue-100', text='text-blue-800', label='Likely start', title='Likely start: moderate start probability (65\u201384%)'
         - rotation_risk: bg='bg-amber-100', text='text-amber-800', label='Rotation risk', title='Rotation risk: rotation risk identified'
         - cameo: bg='bg-zinc-100', text='text-zinc-600', label='Cameo', title='Cameo: low minutes expected'
       - Export `function getMinsRiskConfig(minsRisk: MinsRisk): Config | null` that returns null for falsy/injured values, otherwise returns BADGE_MAP[minsRisk] ?? null
       - Export `function MinsRiskBadge({ minsRisk }: { minsRisk: MinsRisk })` that calls getMinsRiskConfig, returns null if no config, otherwise returns:
         `<span className={\`inline-block text-xs font-normal ${config.text} ${config.bg} rounded px-2 py-1\`} title={config.title}>{config.label}</span>`

    3. Create `tests/lib/mins-risk-badge.test.ts` with:
       - Import `getMinsRiskConfig` from `@/components/shared/MinsRiskBadge`
       - Test each of the 4 non-injured values returns correct config object (check label, bg, text fields)
       - Test 'injured' returns null
       - Test undefined/null (cast to any) returns null
       - Run RED first (file does not exist yet), then GREEN after creating the component.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/mins-risk-badge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/shared/MinsRiskBadge.tsx exists and contains `export function MinsRiskBadge`
    - src/components/shared/MinsRiskBadge.tsx contains `export function getMinsRiskConfig`
    - src/components/shared/MinsRiskBadge.tsx contains `bg-green-100` and `text-green-800` (nailed colors)
    - src/components/shared/MinsRiskBadge.tsx contains `bg-amber-100` and `text-amber-800` (rotation_risk colors)
    - src/components/shared/MinsRiskBadge.tsx contains `minsRisk === 'injured'` check returning null
    - tests/lib/mins-risk-badge.test.ts exists and contains at least 5 test cases
    - `npx vitest run tests/lib/mins-risk-badge.test.ts` exits 0
  </acceptance_criteria>
  <done>getMinsRiskConfig returns correct config for all 4 active risk levels, null for injured/missing. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Risk column to SquadView and GemTable</name>
  <files>src/components/squad/SquadView.tsx, src/components/gem-table/columns.tsx</files>
  <read_first>
    - src/components/squad/SquadView.tsx (current table structure, th/td patterns)
    - src/components/gem-table/columns.tsx (current column array, position of status and trend columns)
    - src/components/shared/MinsRiskBadge.tsx (import path, component signature)
    - .planning/phases/08-minutes-risk-ui-transfer-integration/08-UI-SPEC.md (column order, header styles, cell padding)
  </read_first>
  <action>
    **SquadView.tsx changes:**

    1. Add import at top: `import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'`

    2. Add a new `<th>` AFTER the Status header (line ~97 area):
       ```
       <th className="px-3 py-2 font-medium text-zinc-500 whitespace-nowrap">Risk</th>
       ```

    3. Add a new `<td>` AFTER the Status cell (after the `<td>` containing `<StatusBadge .../>`, line ~139 area):
       ```
       <td className="px-3 py-2 whitespace-nowrap">
         <MinsRiskBadge minsRisk={player.mins_risk} />
       </td>
       ```

    Final SquadView column order: Player | Team | Price | Own% | Mins | Gem | Status | Risk

    **columns.tsx changes:**

    1. Add import at top: `import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'`

    2. Insert a new `col.display()` entry in the `columns` array BETWEEN the `status` accessor (currently at array index ~13, line ~69) and the `trend` display column (currently line ~74). The new entry:
       ```typescript
       col.display({
         id: 'mins_risk',
         header: 'Risk',
         enableSorting: false,
         cell: ({ row }) => <MinsRiskBadge minsRisk={row.original.mins_risk} />,
       }),
       ```

    Final GemTable column order: ... | Status | Risk | Trend | Next 5
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/squad/SquadView.tsx contains `import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'`
    - src/components/squad/SquadView.tsx contains `<th` with text `Risk` appearing after the Status th
    - src/components/squad/SquadView.tsx contains `<MinsRiskBadge minsRisk={player.mins_risk}`
    - src/components/gem-table/columns.tsx contains `import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'`
    - src/components/gem-table/columns.tsx contains `id: 'mins_risk'`
    - src/components/gem-table/columns.tsx contains `enableSorting: false` on the mins_risk column
    - src/components/gem-table/columns.tsx has the mins_risk col.display() entry BEFORE the trend col.display() entry
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Risk column visible in both SquadView (after Status) and GemTable (between Status and Trend). TypeScript compiles. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npx vitest run` passes (all tests including new badge tests)
- MinsRiskBadge renders correct colors for nailed/likely_start/rotation_risk/cameo
- MinsRiskBadge returns null for injured
- Risk column appears in correct position in both tables
</verification>

<success_criteria>
- MinsRiskBadge component exists at src/components/shared/MinsRiskBadge.tsx with exported getMinsRiskConfig helper
- Risk column added to SquadView after Status column
- Risk column added to GemTable between Status and Trend columns
- All unit tests pass (badge helper + existing suite)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-minutes-risk-ui-transfer-integration/08-01-SUMMARY.md`
</output>
