# Continue Here — Phase 64: Sensitivity Analysis

**Paused:** 2026-05-06  
**Stop reason:** Context exhaustion (~80%) during `/gsd-plan-phase 64`  
**Resume command:** `/gsd-plan-phase 64 --skip-research`

---

## Where We Are

Planning was started but no PLAN.md files were written. The planner agent also exhausted its context budget before producing output.

**All pre-planning artifacts are complete and ready:**

| File | Status |
|------|--------|
| `064-CONTEXT.md` | ✅ Done |
| `064-RESEARCH.md` | ✅ Done |
| `064-UI-SPEC.md` | ✅ Done |
| `064-VALIDATION.md` | ✅ Done |
| `064-DISCUSSION-LOG.md` | ✅ Done |

---

## What Needs to Happen

Run `/gsd-plan-phase 64 --skip-research` in a fresh session. The planner should produce 3 plans:

### Plan 01 — Wave 0: RED scaffolding
- `src/lib/sensitivity.ts` — stub `computeFragility()` returning `{ level, reversal }`  
- `src/lib/__tests__/sensitivity.test.ts` — 7 test cases (all RED)  
- `src/components/shared/FragilityNote.tsx` — stub component  
- `src/components/shared/FragilityNote.test.tsx` — 3 render cases

### Plan 02 — Wave 1: GREEN implementation
- Implement `computeFragility(player: MergedPlayer, isTransfer: boolean, xPtsGain?: number)`  
- Implement `FragilityNote` component (amber badge + one-line reversal explanation)  
- All 7 test cases pass

### Plan 03 — Wave 2: Wire up
- Wire `FragilityNote` into `TransferPanel.tsx`  
- Wire `FragilityNote` into `CaptainPicksPanel.tsx`  
- Smoke test: amber flag appears for fragile recommendations

---

## Also Uncommitted This Session

The visual enhancement work (team badges + player avatars) is uncommitted. It should be staged and committed before or alongside Phase 64 work:

**New files:**
- `src/components/shared/TeamBadge.tsx`
- `src/components/shared/PlayerAvatar.tsx`
- `src/lib/team-colours.ts`
- `src/lib/fpl-images.ts`

**Modified:**
- `pipeline/merge.py` — `code` field added to player dict
- `src/lib/fpl-adapter.ts` — `code` in Zod schema
- `src/lib/types.ts` — `code` on `FPLElement` + optional `code?` on `MergedPlayer`
- `src/components/gem-table/columns.tsx` — TeamBadge in team column
- `src/components/gem-table/PlayerComparisonModal.tsx` — PlayerAvatar + TeamBadge
- `src/components/captaincy/CaptaincyPanel.tsx` — PlayerAvatar + TeamBadge
- `src/components/squad/SquadView.tsx` — PlayerAvatar + TeamBadge
- `tests/fixtures/bootstrap-static-sample.json` — code fields added to all 3 elements
- `tests/lib/fpl-adapter.test.ts` — code added to baseElement

---

## Key Decisions Made

1. **`code?: number` (optional) on `MergedPlayer`** — 15+ test fixtures don't have the field; making it required breaks them all. `PlayerAvatar` already handles `undefined` via `!code || error` guard.

2. **Plain `<img>` not `next/image`** — avoids Next.js image domain config changes; ESLint suppressed per-file.

3. **Static `TEAM_BADGE_CODE` map** — no runtime API call needed for badge URL construction; fallback to coloured initials for unknown teams.
