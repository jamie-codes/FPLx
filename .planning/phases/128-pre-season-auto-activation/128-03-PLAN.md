---
phase: 128
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/app/api/pre-season-active/route.ts
autonomous: true
requirements: [AUTO-03]
tags: [api, nextjs, types, pre-season, route-handler]

must_haves:
  truths:
    - "src/lib/types.ts exports a PreSeasonActiveResponse interface with `activated_at: string` and `season_id: string`"
    - "GET /api/pre-season-active returns 404 with `{ error: 'Pre-season not yet activated' }` when pre_season_active.json is absent from Blob (USE_BLOB=true) or local cache (USE_BLOB=false)"
    - "GET /api/pre-season-active returns 200 with the parsed JSON payload when pre_season_active.json exists"
    - "Response uses Response.json() (Next.js 16 idiom matching pre-season-squad/route.ts), not NextResponse.json()"
    - "Route's readBlobOrLocal helper is functionally identical to the one in src/app/api/pre-season-squad/route.ts lines 15-33"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "PreSeasonActiveResponse interface (D-08)"
      contains: "PreSeasonActiveResponse"
    - path: "src/app/api/pre-season-active/route.ts"
      provides: "GET handler reading pre_season_active.json via Blob or local cache; 404/200/500 response shape"
      contains: "export async function GET"
  key_links:
    - from: "src/app/api/pre-season-active/route.ts"
      to: "src/lib/types.ts"
      via: "import type { PreSeasonActiveResponse }"
      pattern: "import type \\{ PreSeasonActiveResponse \\} from '@/lib/types'"
    - from: "src/app/api/pre-season-active/route.ts"
      to: "pipeline output pre_season_active.json"
      via: "readBlobOrLocal('pre_season_active.json')"
      pattern: "readBlobOrLocal\\('pre_season_active\\.json'\\)"
---

<objective>
Add the `PreSeasonActiveResponse` TypeScript type and create the `/api/pre-season-active` Next.js 16 route handler that reads the pipeline-produced `pre_season_active.json` artifact via the project's Blob/local read pattern.

Purpose: AUTO-03 requires a hook (Plan 04) that returns `null` on 404 and the parsed payload on 200. This plan delivers the type and the API endpoint that the hook consumes. The endpoint mirrors `pre-season-squad/route.ts` to maintain consistency with the rest of the project's read-only artifact APIs.

Output: One new exported interface in `src/lib/types.ts`; one new file `src/app/api/pre-season-active/route.ts` implementing GET; the existing pre-season-squad route is left untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-pre-season-auto-activation/128-CONTEXT.md
@.planning/phases/128-pre-season-auto-activation/128-RESEARCH.md
@.planning/phases/128-pre-season-auto-activation/128-PATTERNS.md
@.planning/phases/128-pre-season-auto-activation/128-UI-SPEC.md
@src/app/api/pre-season-squad/route.ts
@src/lib/types.ts
</context>

<interfaces>
<!-- Existing patterns to mirror -->

From `src/app/api/pre-season-squad/route.ts` lines 15-33 — readBlobOrLocal helper (copy verbatim):

```typescript
import { list } from '@vercel/blob'
import { readFile } from 'fs/promises'
import { join } from 'path'

const USE_BLOB = process.env.USE_BLOB?.toLowerCase() === 'true'

async function readBlobOrLocal(filename: string): Promise<string | null> {
  try {
    if (USE_BLOB) {
      const { blobs } = await list({ prefix: filename, limit: 1 })
      if (!blobs.length) return null
      const res = await fetch(blobs[0].url)
      if (!res.ok) throw new Error(`Blob fetch failed: ${res.status}`)
      return await res.text()
    } else {
      const cachePath = join(process.cwd(), 'pipeline', 'cache', filename)
      return await readFile(cachePath, 'utf-8')
    }
  } catch (err) {
    const isNotFound =
      err instanceof Error && 'code' in err && (err as NodeJS.ErrnoException).code === 'ENOENT'
    if (isNotFound) return null
    throw err
  }
}
```

From `src/lib/types.ts` lines 1138-1142 — analog interface to append after:

```typescript
export interface PreSeasonSquadResponse {
  squad: PreSeasonSquad | null
  health: SquadHealth | null
  solver: 'ilp' | 'greedy' | null
}
```

Target new interface (CONTEXT.md D-08):

```typescript
export interface PreSeasonActiveResponse {
  activated_at: string  // ISO 8601
  season_id: string     // e.g. "2526"
}
```

Target route response contract:
- 404 → `Response.json({ error: 'Pre-season not yet activated' }, { status: 404 })`
- 200 → `Response.json(payload, { status: 200, headers: { 'Cache-Control': 'public, s-maxage=300, stale-while-revalidate=60' } })`
- 500 → `Response.json({ error: 'Failed to load activation status' }, { status: 500 })`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PreSeasonActiveResponse interface to src/lib/types.ts</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts (full file is large — use Grep to find `PreSeasonSquadResponse`; append the new interface directly after that block to keep related types adjacent)
    - .planning/phases/128-pre-season-auto-activation/128-CONTEXT.md (D-08 — exact field names and types)
  </read_first>
  <behavior>
    - Importing `PreSeasonActiveResponse` from `@/lib/types` succeeds at compile time
    - Interface has exactly two fields: `activated_at: string` and `season_id: string`
    - Existing `PreSeasonSquadResponse` (and all other exports) is unchanged
  </behavior>
  <action>
    1. Locate the `PreSeasonSquadResponse` interface in `src/lib/types.ts` (around line 1138).

    2. Immediately AFTER the closing `}` of `PreSeasonSquadResponse`, append a blank line and then the new interface:

    ```typescript
    export interface PreSeasonActiveResponse {
      activated_at: string  // ISO 8601 timestamp set by pipeline at first activation
      season_id: string     // e.g. "2526" — derived from events[0].deadline_time year
    }
    ```

    3. Do NOT modify or delete any other type, interface, type alias, or export in this file.

    4. Field names MUST be exactly `activated_at` and `season_id` (snake_case) — matches the JSON schema written by `run.py` per D-05.
  </action>
  <acceptance_criteria>
    - `src/lib/types.ts` contains the exact substring `export interface PreSeasonActiveResponse {`
    - The interface has two fields named exactly `activated_at: string` and `season_id: string`
    - The existing `PreSeasonSquadResponse` interface is byte-identical to before the edit
    - `npx tsc --noEmit` exits 0 (full repo typecheck)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>New interface exported; existing types untouched; project typechecks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/app/api/pre-season-active/route.ts</name>
  <files>src/app/api/pre-season-active/route.ts</files>
  <read_first>
    - src/app/api/pre-season-squad/route.ts (lines 1-57 — direct analog; copy the readBlobOrLocal helper and the 404/200/500 response shape)
    - src/lib/types.ts (confirm PreSeasonActiveResponse from Task 1 is exported)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (Next.js 16 route handler reference — confirms Response.json() is the project idiom)
    - .planning/phases/128-pre-season-auto-activation/128-CONTEXT.md (D-07, D-08 — 404 on absent, response shape)
    - .planning/phases/128-pre-season-auto-activation/128-RESEARCH.md (Pattern 3 — full reference implementation)
  </read_first>
  <behavior>
    - GET request when artifact absent (local: file not present at pipeline/cache/pre_season_active.json; Blob: empty list) → 404 with `{ error: 'Pre-season not yet activated' }`
    - GET request when artifact present and JSON-parseable → 200 with `{ activated_at, season_id }` and `Cache-Control: public, s-maxage=300, stale-while-revalidate=60`
    - GET request when artifact present but unreadable due to non-ENOENT error (Blob fetch network error, malformed JSON parse) → 500 with `{ error: 'Failed to load activation status' }` and a `console.error` log
    - No HEAD/POST/PUT/DELETE/PATCH handlers exported (GET-only)
    - Module imports use the path-alias style `@/lib/types` consistent with the rest of the codebase
  </behavior>
  <action>
    Create `src/app/api/pre-season-active/route.ts` with the following structure (this is the canonical content, not a fence — the executor writes equivalent code):

    1. **Imports** (mirror pre-season-squad/route.ts lines 7-11):
       - `import { list } from '@vercel/blob'`
       - `import { readFile } from 'fs/promises'`
       - `import { join } from 'path'`
       - `import type { PreSeasonActiveResponse } from '@/lib/types'`

    2. **Constant**: `const USE_BLOB = process.env.USE_BLOB?.toLowerCase() === 'true'` (mirror pre-season-squad/route.ts line 13).

    3. **Helper** `readBlobOrLocal(filename: string): Promise<string | null>` — copy VERBATIM from pre-season-squad/route.ts lines 15-33. Do not refactor, extract, or shorten — direct copy to keep the two routes byte-identical in their read logic. Note: the helper deliberately treats ENOENT as `null` (returns null, no throw) and rethrows any other error.

    4. **GET handler**:
       - File-top comment block referencing Phase 128 AUTO-03 and D-07/D-08 (mirror the comment style at pre-season-squad/route.ts lines 1-6)
       - `export async function GET()` (no params — the route has no dynamic segments or query string handling)
       - Inside an outer try/catch:
         - Call `const data = await readBlobOrLocal('pre_season_active.json')`
         - If `data === null` → `return Response.json({ error: 'Pre-season not yet activated' }, { status: 404 })`
         - Otherwise → `const payload = JSON.parse(data) as PreSeasonActiveResponse` then `return Response.json(payload, { status: 200, headers: { 'Cache-Control': 'public, s-maxage=300, stale-while-revalidate=60' } })`
       - Catch block: `console.error('[pre-season-active] Unexpected error:', err)` then `return Response.json({ error: 'Failed to load activation status' }, { status: 500 })`

    5. **Cache-Control**: use `s-maxage=300, stale-while-revalidate=60` (5 minute CDN cache, 1 minute stale-while-revalidate) — NOT the 3600s value used by pre-season-squad. Rationale (RESEARCH.md Pattern 3): activation state can flip during the pre-season window and users benefit from fresher feedback; SWR ensures no user is blocked.

    6. **Do not** add a POST, PUT, DELETE, HEAD, or OPTIONS handler.

    7. **Do not** import `NextResponse` — use `Response.json()` per the project's Next.js 16 convention (matches pre-season-squad/route.ts).
  </action>
  <acceptance_criteria>
    - File `src/app/api/pre-season-active/route.ts` exists
    - File contains exactly one `export async function GET` and zero other exported HTTP method handlers
    - File contains exactly one occurrence of `readBlobOrLocal('pre_season_active.json')`
    - File contains exactly one occurrence of `s-maxage=300` and zero occurrences of `s-maxage=3600`
    - File imports `PreSeasonActiveResponse` from `@/lib/types` (path-alias form, not relative)
    - File contains `{ error: 'Pre-season not yet activated' }` (verbatim string) and `{ status: 404 }`
    - File contains `{ error: 'Failed to load activation status' }` (verbatim string) and `{ status: 500 }`
    - File does NOT import `NextResponse`
    - `npx tsc --noEmit` exits 0
    - `npx eslint src/app/api/pre-season-active/route.ts` exits 0 (or matches the project's lint exit code for unchanged files)
    - Manual curl against `npm run dev` returns 404 when `pipeline/cache/pre_season_active.json` is absent (acceptance via dev-server check is optional — the typecheck + lint gate is the primary signal)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Route file created; mirrors pre-season-squad/route.ts read logic; returns 404/200/500 per contract; typecheck clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `grep -n "PreSeasonActiveResponse" src/lib/types.ts` returns exactly one line (the interface declaration)
- `grep -n "PreSeasonActiveResponse" src/app/api/pre-season-active/route.ts` returns at least one line (the import)
- `grep -n "pre_season_active.json" src/app/api/pre-season-active/route.ts` returns exactly one line
- `grep -n "Response.json" src/app/api/pre-season-active/route.ts` returns at least 3 lines (404, 200, 500)
- `grep -c "NextResponse" src/app/api/pre-season-active/route.ts` returns 0
</verification>

<success_criteria>
- AUTO-03 type contract delivered: `PreSeasonActiveResponse` available to Plan 04 hook + test
- AUTO-03 API contract delivered: `/api/pre-season-active` returns the documented 404/200/500 shape
- No changes to pre-season-squad route or any other type in types.ts
- Project typechecks and lints clean
</success_criteria>

<output>
After completion, create `.planning/phases/128-pre-season-auto-activation/128-03-SUMMARY.md`
</output>
