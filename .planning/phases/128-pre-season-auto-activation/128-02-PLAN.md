---
phase: 128
plan: 02
type: execute
wave: 2
depends_on: [128-01]
files_modified:
  - pipeline/run.py
autonomous: true
requirements: [AUTO-01, AUTO-02]
tags: [pipeline, python, orchestration, pre-season, activation]

must_haves:
  truths:
    - "When the pipeline runs in off-season AND the activation predicate is True AND pre_season_active.json does NOT yet exist, run.py writes pre_season_active.json (with activated_at + season_id) AND calls suggest_squad(bootstrap, archive, force=True)"
    - "When pre_season_active.json already exists, the activation block is a silent no-op (idempotent)"
    - "When the activation predicate is False (any event finished, or len(events)<38, or events[0].deadline_time absent), the activation block does not fire"
    - "When the archive (season_archive_gw38.json) is unavailable on first activation, pre_season_active.json is still written and squad recompute is skipped with a non-fatal log line"
    - "Any unhandled exception inside the activation block is caught and logged via the same non-fatal try/except pattern used by IS_GW38 suggest_squad and squad_health (pipeline continues)"
  artifacts:
    - path: "pipeline/run.py"
      provides: "Phase 128 activation block nested inside IS_OFF_SEASON gate, after IS_GW38 section, before `if not IS_OFF_SEASON:`"
      contains: "pre_season_active.json"
  key_links:
    - from: "pipeline/run.py activation block"
      to: "pipeline/upload.py save()"
      via: "save('pre_season_active.json', {'activated_at': ..., 'season_id': ...})"
      pattern: "save\\('pre_season_active\\.json'"
    - from: "pipeline/run.py activation block"
      to: "pipeline/suggest_squad.py suggest_squad"
      via: "import inside try block; call with force=True"
      pattern: "suggest_squad\\(bootstrap, [^)]+, force=True\\)"
    - from: "pipeline/run.py activation block"
      to: "Vercel Blob existence check"
      via: "vercel_blob.list({'prefix': 'pre_season_active.json', 'limit': 1}) and os.path.exists local fallback"
      pattern: "vercel_blob.list"
---

<objective>
Add the Phase 128 pre-season activation block to `pipeline/run.py`, nested inside the IS_OFF_SEASON branch and positioned after the IS_GW38 block but before the `if not IS_OFF_SEASON:` block.

Purpose: AUTO-01 requires the pipeline to detect next-season bootstrap publication via the tri-state predicate locked by Plan 01's regression tests. AUTO-02 requires writing `pre_season_active.json` on first detection AND calling `suggest_squad(bootstrap, archive, force=True)` to re-run the ILP against fresh next-season prices (D-02, D-03). On subsequent runs the block is a no-op (idempotent via artifact existence check).

Output: A single self-contained activation block in `run.py` matching the structure documented in PATTERNS.md and RESEARCH.md, wrapped in a non-fatal try/except. Pipeline still runs end-to-end in dev mode (`USE_BLOB=false`) without errors when the predicate is True, False, or already activated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-pre-season-auto-activation/128-CONTEXT.md
@.planning/phases/128-pre-season-auto-activation/128-RESEARCH.md
@.planning/phases/128-pre-season-auto-activation/128-PATTERNS.md
@.planning/phases/128-pre-season-auto-activation/128-01-PLAN.md
@pipeline/run.py
@pipeline/upload.py
@pipeline/suggest_squad.py
</context>

<interfaces>
<!-- Existing run.py structure the executor must respect -->

From `pipeline/run.py` lines 141-253:

```python
bootstrap = get_bootstrap_static()
save('fpl_bootstrap.json', bootstrap)

events = bootstrap.get('events', [])
IS_OFF_SEASON = not any(e.get('is_current') for e in events)
if IS_OFF_SEASON:
    print("[pipeline] IS_OFF_SEASON detected ...")

# ... lineup_news, transfer_news (year-round) ...

current_event_entry = next((e for e in events if e.get('is_current')), None)
last_event_id = max((e['id'] for e in events), default=0)
CURRENT_GW = current_event_entry['id'] if current_event_entry else 0
IS_GW38 = (CURRENT_GW > 0) and (CURRENT_GW == last_event_id)

if IS_GW38:
    try:
        from archive_season import archive_season
        archive_season(bootstrap)
        ...
    try:
        from suggest_squad import suggest_squad
        archive_path = os.path.join(cache_dir, 'season_archive_gw38.json')
        _archive = None
        if os.path.exists(archive_path):
            with open(archive_path, 'r', encoding='utf-8') as _f:
                _archive = json.load(_f)
        elif os.getenv('USE_BLOB', '').lower() == 'true':
            import vercel_blob
            import requests as _requests
            _blob_list = vercel_blob.list({'prefix': 'season_archive_gw38.json', 'limit': 1})
            _blobs = _blob_list.get('blobs', [])
            if _blobs:
                _url = _blobs[0].get('url', '')
                if _url:
                    _archive = _requests.get(_url, timeout=30).json()
        if _archive is not None:
            suggest_squad(bootstrap, _archive)
            ...
    # squad_health block ...

# Insertion point for Phase 128 activation block goes HERE
# (after IS_GW38 closes, before `if not IS_OFF_SEASON:`)

if not IS_OFF_SEASON:
    # ... in-season steps
```

From `pipeline/upload.py` (already imported as `save` at top of run.py via existing pattern).

From Plan 01: `suggest_squad(bootstrap, archive, force=True)` bypasses idempotency and re-runs the ILP.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert Phase 128 activation block in pipeline/run.py</name>
  <files>pipeline/run.py</files>
  <read_first>
    - pipeline/run.py (full file — confirm cache_dir variable name and import scope; the existing IS_GW38 block at lines 205-245 is the structural reference)
    - pipeline/upload.py (confirm save() is already imported at top of run.py and accepts (filename, dict))
    - pipeline/suggest_squad.py (Plan 01 added force kwarg — call signature is suggest_squad(bootstrap, archive, force=True))
    - .planning/phases/128-pre-season-auto-activation/128-CONTEXT.md (D-01 through D-06 — locked predicate, schema, season_id derivation)
    - .planning/phases/128-pre-season-auto-activation/128-RESEARCH.md (Pattern 2 — full insertion-block source; Pitfalls 1, 2, 4 — placement, ordering, archive-absent handling)
    - .planning/phases/128-pre-season-auto-activation/128-PATTERNS.md (lines 68-119 — analog patterns)
  </read_first>
  <behavior>
    - Pipeline run in off-season + predicate True + pre_season_active.json absent + archive available → writes pre_season_active.json with `{activated_at: <ISO now UTC>, season_id: <yyXX form>}` AND calls suggest_squad(bootstrap, archive, force=True), printing two info lines
    - Pipeline run in off-season + predicate True + pre_season_active.json absent + archive UNAVAILABLE → writes pre_season_active.json anyway AND prints a non-fatal "archive not available — squad recompute skipped" line to stderr; pipeline continues
    - Pipeline run in off-season + predicate True + pre_season_active.json present → prints one info line ("already activated — skipping") and does nothing else
    - Pipeline run in off-season + predicate False → block does not fire; no log line from this block
    - Pipeline run in-season (IS_OFF_SEASON=False) → block does not fire at all (block is nested inside an `if IS_OFF_SEASON:` guard)
    - Any exception inside the block is caught by the outer try/except and logged to stderr; pipeline does NOT crash
  </behavior>
  <action>
    Modify `pipeline/run.py` to add the Phase 128 activation block. Concrete placement and content:

    **Placement (D-01):** After the IS_GW38 block closes (currently ends at line ~245 with the `squad_health` non-fatal try/except), and BEFORE the `if not IS_OFF_SEASON:` line (currently line ~253). The block must be guarded by `if IS_OFF_SEASON:` (the line 149 `if IS_OFF_SEASON:` is only a print, so add a NEW `if IS_OFF_SEASON:` guard for the activation block — do NOT nest inside the print's branch, that block ends after its print).

    **Block content** — structure exactly per CONTEXT.md D-02 and RESEARCH.md Pattern 2:

    1. Outer `if IS_OFF_SEASON:` guard wrapping a non-fatal `try / except Exception as _pa_exc:` block (mirror the IS_GW38 suggest_squad wrapper pattern at run.py lines 213-236).

    2. Inside the try block:
       - Comment line `# Phase 128 AUTO-01/02: Pre-season auto-activation.`
       - Evaluate the tri-state predicate into a local variable `_pre_season_predicate`. Clauses MUST be ordered: `len(events) >= 38 and not any(e.get('finished') for e in events) and bool(events[0].get('deadline_time') if events else None)`. Clause order matters (Pitfall 2 in RESEARCH.md — short-circuit safety).
       - If predicate is False, do nothing further (no else branch needed; the rest of the try block sits under `if _pre_season_predicate:`).
       - If predicate is True: check artifact existence using the dual-path pattern from IS_GW38 (lines 215-229). `_active_key = 'pre_season_active.json'`. If `USE_BLOB=true`, use `vercel_blob.list({'prefix': _active_key, 'limit': 1})` and check `len(result.get('blobs', [])) > 0`. Otherwise use `os.path.exists(os.path.join(cache_dir, _active_key))`. Bind result to `_active_exists`.
       - If `_active_exists` is True → print `"[pipeline] Pre-season already activated — skipping."` and end the block (no further work).
       - If `_active_exists` is False (first activation): derive `_year = int(events[0]['deadline_time'][:4])`, derive `_season_id = f"{_year-1}{str(_year)[2:]}"` per D-06 (e.g. year 2026 → "2526"). Build the payload `{'activated_at': datetime.now(timezone.utc).isoformat(), 'season_id': _season_id}`. Call `save(_active_key, payload)`. Print `f"[pipeline] Pre-season activation written: season_id={_season_id}"`.
       - Then attempt to load the archive from local cache, then Blob fallback (mirror IS_GW38 archive-load lines 215-229). Bind to `_arch` (None if neither found). If `_arch is not None`: `from suggest_squad import suggest_squad` (inside the try block, mirroring line 214); call `suggest_squad(bootstrap, _arch, force=True)`; print `"[pipeline] Pre-season squad force-recomputed."`. If `_arch is None`: print to stderr `"[pipeline] Pre-season activation: archive not available — squad recompute skipped."` and continue.

    3. Outer except handler: `except Exception as _pa_exc: print(f"[pipeline] Pre-season activation non-fatal error: {_pa_exc}", file=sys.stderr)` — matches the IS_GW38 wrappers.

    **Imports:** The block needs `datetime` and `timezone` from `datetime`. Use local imports inside the try block (`from datetime import datetime as _dt, timezone as _tz`) to match the existing project pattern of late-imports for non-hot paths (see IS_GW38 block `import vercel_blob` and `from suggest_squad import suggest_squad` at lines 214 and 222). Do NOT add module-level imports for these.

    **cache_dir variable:** Confirm by reading run.py that `cache_dir` exists as a local in this scope (used by IS_GW38 block at line 215). If a different name is used, substitute accordingly — do not invent a new variable.

    **Critical constraints:**
    - Do not change any line outside the inserted block. IS_GW38, lineup_news, transfer_news, and the `if not IS_OFF_SEASON:` block must remain byte-identical.
    - The `from suggest_squad import suggest_squad` import must live inside the try block (Pitfall in RESEARCH.md: "Importing suggest_squad at module level in run.py is an anti-pattern").
    - Both archive-load branches (local + blob) must be present — Pitfall 4 in RESEARCH.md is the archive-absent case.
  </action>
  <acceptance_criteria>
    - `pipeline/run.py` contains exactly one new `if IS_OFF_SEASON:` block positioned AFTER the IS_GW38 closing line and BEFORE `if not IS_OFF_SEASON:`
    - Block contains the comment `# Phase 128 AUTO-01/02: Pre-season auto-activation.`
    - Block evaluates `_pre_season_predicate` with clause ordering exactly: `len(events) >= 38 and not any(e.get('finished') for e in events) and bool(events[0].get('deadline_time') if events else None)`
    - Block uses `_active_key = 'pre_season_active.json'`
    - Block calls `save(_active_key, ...)` on first activation only (inside the `if not _active_exists:` branch)
    - Block calls `suggest_squad(bootstrap, _arch, force=True)` (force=True keyword present) only when archive is available
    - Block uses `_season_id = f"{_year-1}{str(_year)[2:]}"` derivation per D-06
    - Block prints `[pipeline] Pre-season already activated — skipping.` on the idempotent path
    - Block prints `[pipeline] Pre-season activation written: season_id={...}` on first activation
    - Block prints `[pipeline] Pre-season activation: archive not available — squad recompute skipped.` to sys.stderr when archive missing
    - Block is wrapped in `try: ... except Exception as _pa_exc: print(f"[pipeline] Pre-season activation non-fatal error: {_pa_exc}", file=sys.stderr)`
    - `from suggest_squad import suggest_squad` is inside the try block (not module-level)
    - `python -m py_compile pipeline/run.py` exits 0
    - `python -m pytest pipeline/tests/ -x` exits 0 (no regression)
    - `grep -c "pre_season_active.json" pipeline/run.py` returns at least 2 (string appears in key var + log line OR multiple references)
    - `grep -c "force=True" pipeline/run.py` returns exactly 1
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile pipeline/run.py && python -m pytest pipeline/tests/ -x</automated>
  </verify>
  <done>Activation block inserted at the correct location with the correct guard, predicate, idempotency check, save call, force-recompute call, and non-fatal wrapper; run.py compiles; full pipeline test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Local smoke test — exercise the activation block in dev mode</name>
  <files>pipeline/cache/pre_season_active.json</files>
  <read_first>
    - pipeline/run.py (verify the block reads/writes pre_season_active.json in pipeline/cache when USE_BLOB is not set)
    - pipeline/cache/ (check what artifacts exist; do NOT delete production data outside this smoke test)
  </read_first>
  <behavior>
    - Manual smoke test: with a synthetic bootstrap (38 unfinished events, deadline_time set on events[0]), running the activation block writes pipeline/cache/pre_season_active.json with both keys present (activated_at, season_id).
    - On second run with the artifact already in place, no second write occurs (file mtime unchanged) and the "already activated — skipping" log line is printed.
    - On third run with the artifact deleted and one event flipped to finished=True, the artifact is NOT recreated (predicate False).
  </behavior>
  <action>
    Write and execute a small smoke-test script (do NOT commit the script — it is a one-shot verification). Recommended approach: a single inline Python invocation via `python -c "..."` that imports the activation predicate logic (or copies it inline) and writes a synthetic JSON to test the artifact write path locally.

    Alternative: write a temporary `pipeline/_smoke_phase128.py` that:
    1. Removes any pre-existing `pipeline/cache/pre_season_active.json`
    2. Constructs a synthetic bootstrap dict with 38 events (all `finished=False`, events[0] with a 2026-08-01 deadline_time), no `is_current`
    3. Invokes the activation block logic directly (copy/paste the predicate + save call, or import `pipeline.run` carefully) — OR shells out to `python -m run` after planting the synthetic bootstrap as `pipeline/cache/fpl_bootstrap.json` (most realistic path; requires USE_BLOB unset and may require disabling network fetches — note the limitation)
    4. Asserts pipeline/cache/pre_season_active.json now exists with valid JSON containing `activated_at` (ISO 8601) and `season_id` == "2526"
    5. Re-runs and asserts the file was NOT modified (mtime stable) and stdout includes "already activated — skipping"
    6. Mutates one event to `finished=True`, deletes the artifact, re-runs, asserts the artifact is NOT recreated
    7. Cleans up: restores any production state, deletes the smoke script

    Because `pipeline/run.py` performs many side effects (network fetches for Understat, element summaries, etc.), the practical approach is to keep the smoke test scoped to the activation block in isolation: copy the predicate evaluation and the save() call into a standalone snippet rather than running the full run.py end-to-end. The goal is to confirm the new artifact appears in `pipeline/cache/` and contains the expected JSON shape — NOT to do a full pipeline run.

    If the executor cannot construct a synthetic run safely (e.g. would require mocking get_bootstrap_static), then skip the dynamic smoke test and instead validate by: (a) confirming `grep -n "pre_season_active.json" pipeline/run.py` shows the new references, (b) confirming `python -c "from pipeline.run import IS_OFF_SEASON; print('import ok')"` does not crash (or note that pipeline.run cannot be imported standalone — that is acceptable per test_run_offseason.py module docstring), and (c) running the Plan 01 + Plan 02 pytest suite as the primary regression gate.

    Do NOT commit `pipeline/cache/pre_season_active.json` if it is created by this smoke test. Delete it before finalising the task. Add a .gitignore entry only if it doesn't already exclude pipeline/cache/*.json (it should — verify).
  </action>
  <acceptance_criteria>
    - Either (a) a synthetic activation run produces a valid `pipeline/cache/pre_season_active.json` with `activated_at` (ISO 8601 string) and `season_id` ("2526" for a 2026-08-01 deadline) and a second run does NOT modify the file's mtime, OR (b) the executor documents in the summary why a dynamic smoke run was not feasible and relies on the Plan 01 + Plan 02 regression tests as the sole acceptance gate
    - No new files are committed under pipeline/cache/ (the smoke artifact is deleted; .gitignore covers the path)
    - No stray smoke-test script remains in the repo (any `_smoke_phase128.py` is deleted before commit)
    - `python -m pytest pipeline/tests/ -x` still exits 0 after smoke test cleanup
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest pipeline/tests/ -x</automated>
  </verify>
  <done>Activation block behaviour confirmed either by dynamic smoke test or by documented test-suite-only verification; no smoke artifacts left in the working tree.</done>
</task>

</tasks>

<verification>
- `python -m py_compile pipeline/run.py` exits 0
- `python -m pytest pipeline/tests/ -x` exits 0
- `grep -n "Phase 128 AUTO-01/02" pipeline/run.py` returns exactly one comment line
- `grep -n "pre_season_active.json" pipeline/run.py` returns multiple occurrences (key var + at least one log/path reference)
- `grep -n "force=True" pipeline/run.py` returns exactly one line (the suggest_squad call)
- `grep -n "Pre-season activation non-fatal error" pipeline/run.py` returns exactly one line
- The Phase 128 activation block sits between the IS_GW38 closing and the `if not IS_OFF_SEASON:` line (verify by reading line numbers)
</verification>

<success_criteria>
- AUTO-01 implemented: tri-state predicate evaluated inside IS_OFF_SEASON, idempotent via artifact existence
- AUTO-02 implemented: pre_season_active.json written via save() helper; suggest_squad called with force=True; archive-absent path handled gracefully
- Non-fatal error wrapper matches existing pipeline conventions (IS_GW38 suggest_squad wrapper)
- No regressions to existing pipeline tests
- run.py remains importable in a fresh shell (with all existing module-level side effects intact)
</success_criteria>

<output>
After completion, create `.planning/phases/128-pre-season-auto-activation/128-02-SUMMARY.md`
</output>
