---
phase: 06-club-form-value-gems-and-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/club-form.ts
  - src/components/value-gems/columns.tsx
  - src/components/gem-table/columns.tsx
  - src/components/transfers/TransferPanel.tsx
  - tests/lib/club-form.test.ts
autonomous: true
requirements: [FFA-03, VAL-03, UIX-01]
gap_closure: true

must_haves:
  truths:
    - "Man City fixtures show as hard (red), not easy (green)"
    - "Price trend column shows formatted values, never NaN"
    - "Transfer Panel price trend shows formatted values, never NaN"
  artifacts:
    - path: "src/lib/club-form.ts"
      provides: "Corrected tier() function"
      contains: "if (score >= hardThreshScore) return 'hard'"
    - path: "src/components/value-gems/columns.tsx"
      provides: "PriceTrendCell with undefined guards"
      contains: "?? 0"
    - path: "src/components/gem-table/columns.tsx"
      provides: "Inline price trend with undefined guards"
      contains: "?? 0"
    - path: "src/components/transfers/TransferPanel.tsx"
      provides: "TransferPanel cost_change with undefined guards"
      contains: "?? 0"
  key_links:
    - from: "src/lib/club-form.ts"
      to: "fixture badges UI"
      via: "difficulty_tier field"
      pattern: "return 'hard'"
    - from: "src/components/value-gems/columns.tsx"
      to: "Value Gems table"
      via: "PriceTrendCell render"
      pattern: "\\?\\? 0"
---

<objective>
Fix two UAT-reported bugs: (1) inverted fixture difficulty tiers in computeClubForm making Man City fixtures show as easy instead of hard, and (2) NaN price trend values across Value Gems, Gem Ratings, and Transfer Panel caused by undefined cost_change fields.

Purpose: Close the two remaining major UAT gaps so all 9 UAT tests pass.
Output: Corrected club-form.ts tier logic, undefined-guarded price trend renders, re-run pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-club-form-value-gems-and-polish/06-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tier inversion in computeClubForm and add regression test</name>
  <files>src/lib/club-form.ts, tests/lib/club-form.test.ts</files>
  <action>
In src/lib/club-form.ts, lines 71-73, the tier() function has inverted return values. Swap them:
- Line 71: change `return 'easy'` to `return 'hard'` (high score = strong opponent = hard fixture)
- Line 72: change `return 'hard'` to `return 'easy'` (low score = weak opponent = easy fixture)

The corrected function should read:
```
const tier = (score: number): DifficultyTier => {
  if (score >= hardThreshScore) return 'hard'
  if (score <= easyThreshScore) return 'easy'
  return 'medium'
}
```

In tests/lib/club-form.test.ts, add a new test case that verifies tier assignment is correct. The test data has 3 teams (ARS id=1, CHE id=2, BUR id=3). After computing form from the existing makeFixtures() data, look at upcoming fixtures and verify that the assignment is sensible given the xGA-based scoring. Specifically, add a test "assigns difficulty tier correctly — strong team is hard, weak team is easy" that:
1. Calls computeClubForm(bootstrap, makeFixtures())
2. Finds a team's upcoming fixture against the team with the worst defensive record (highest xGA from conceded goals)
3. Asserts that fixture's difficulty_tier is 'hard' (or at minimum not 'easy')
  </action>
  <verify>
    <automated>npx vitest run tests/lib/club-form.test.ts</automated>
  </verify>
  <done>tier() returns 'hard' for high-scoring (strong) opponents, 'easy' for low-scoring (weak) opponents. Regression test passes confirming correct tier assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Guard undefined cost_change fields and re-run pipeline</name>
  <files>src/components/value-gems/columns.tsx, src/components/gem-table/columns.tsx, src/components/transfers/TransferPanel.tsx</files>
  <action>
Three files need `?? 0` guards on cost_change_event and cost_change_start reads to prevent NaN when fields are undefined.

**src/components/value-gems/columns.tsx:**
In the PriceTrendCell component call (around line 58-60), add nullish coalescing:
```
costChangeEvent={row.original.cost_change_event ?? 0}
costChangeStart={row.original.cost_change_start ?? 0}
```

**src/components/gem-table/columns.tsx:**
In the inline price trend cell (around line 78-79), add guards:
```
const ev = row.original.cost_change_event ?? 0
const st = row.original.cost_change_start ?? 0
```

**src/components/transfers/TransferPanel.tsx:**
Every read of `s.sell.cost_change_event`, `s.sell.cost_change_start`, `s.buy.cost_change_event`, `s.buy.cost_change_start` needs `?? 0`. There are approximately 8 occurrences across two suggestion rendering blocks (sell/buy for each suggestion type). For each occurrence:
- `s.sell.cost_change_event` becomes `(s.sell.cost_change_event ?? 0)`
- `s.sell.cost_change_start` becomes `(s.sell.cost_change_start ?? 0)`
- `s.buy.cost_change_event` becomes `(s.buy.cost_change_event ?? 0)`
- `s.buy.cost_change_start` becomes `(s.buy.cost_change_start ?? 0)`

After all code fixes, re-run the pipeline to regenerate merged_players.json with the cost_change fields populated:
```
cd pipeline && python run.py
```
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>No NaN values appear in price trend columns. Value Gems, Gem Ratings, and Transfer Panel all render cost_change values correctly (showing dash for zero, arrows for non-zero). Pipeline has been re-run so merged_players.json contains cost_change_event and cost_change_start fields.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/lib/club-form.test.ts` -- all tests pass including new tier regression test
2. `npx next build` -- builds without errors
3. Visual: Club Form tab shows Man City fixtures as red (hard), not green (easy)
4. Visual: Value Gems, Gem Ratings, Transfer Panel price trend columns show formatted numbers, no NaN
</verification>

<success_criteria>
- All 4 UAT gaps from 06-UAT.md are resolved (tests 2, 6, 8, 9)
- tier() function correctly maps strong opponents to 'hard' difficulty
- No NaN values in any price trend display
- Pipeline re-run produces merged_players.json with cost_change fields
</success_criteria>

<output>
After completion, create `.planning/phases/06-club-form-value-gems-and-polish/06-04-SUMMARY.md`
</output>
