---
phase: 127
plan: 04
type: execute
wave: 3
depends_on: ["127-01", "127-02", "127-03"]
files_modified:
  - src/lib/hooks/usePreSeasonSquad.ts
  - src/components/next-season/NextSeasonPlannerTab.tsx
  - src/components/next-season/NextSeasonPlannerTab.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - GREEDY-03
  - WATCH-01
  - WATCH-04
tags:
  - hooks
  - components
  - page-wiring
  - health-indicator
must_haves:
  truths:
    - "usePreSeasonSquad returns PreSeasonSquadResponse | null (envelope shape from Plan 01)"
    - "NextSeasonPlannerTab reads data?.squad for the formation grid (no more direct data passthrough)"
    - "NextSeasonPlannerTab renders the solver badge (ILP / Greedy) in the FormationGrid headline row when data?.solver is set"
    - "NextSeasonPlannerTab renders the health indicator paragraph below the FormationGrid when data?.health is non-null"
    - "page.tsx SubTab union includes 'watchlist'"
    - "page.tsx SECTIONS Plan array includes the Watchlist sub-tab entry after Next Season"
    - "page.tsx invokes useWatchlist() and passes watchlistIds + toggleWatchlist to GemTable and WatchlistTab"
    - "page.tsx renders WatchlistTab when activeSection==='plan' && activeSubTab==='watchlist'"
  artifacts:
    - path: "src/lib/hooks/usePreSeasonSquad.ts"
      provides: "Envelope-typed hook"
      contains: "PreSeasonSquadResponse"
    - path: "src/components/next-season/NextSeasonPlannerTab.tsx"
      provides: "Updated consumer + health indicator + solver badge"
      contains: "data?.squad"
    - path: "src/app/page.tsx"
      provides: "Watchlist sub-tab wiring and useWatchlist invocation"
      contains: "useWatchlist"
  key_links:
    - from: "src/components/next-season/NextSeasonPlannerTab.tsx"
      to: "data?.squad / data?.health / data?.solver"
      via: "envelope destructure"
      pattern: "data\\?\\.(squad|health|solver)"
    - from: "src/app/page.tsx"
      to: "GemTable + WatchlistTab"
      via: "watchlistIds + toggleWatchlist props"
      pattern: "watchlistIds=\\{watchlistIds\\}"
---

<objective>
Bring it all together in Wave 3: update `usePreSeasonSquad` to the envelope type (matching the route shipped by Plan 01), fix `NextSeasonPlannerTab` to read `data?.squad` and render the health indicator + solver badge (GREEDY-03), and wire `useWatchlist` + WatchlistTab into `page.tsx` so the user can navigate to Plan → Watchlist and see pinned cards.

Purpose: This is the integration plan that activates the watchlist feature end-to-end and surfaces the GREEDY-01/02 pipeline output in the UI. It depends on Plans 01 (envelope + types), 02 (useWatchlist hook + GemTable star button props), and 03 (WatchlistTab + WatchlistPlayerCard).

Output: Four file edits stitching the existing pieces into a working feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-CONTEXT.md
@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-PATTERNS.md
@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-UI-SPEC.md

@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-01-PLAN.md
@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-02-PLAN.md
@.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-03-PLAN.md

@src/app/page.tsx
@src/lib/hooks/usePreSeasonSquad.ts
@src/components/next-season/NextSeasonPlannerTab.tsx
@src/components/next-season/NextSeasonPlannerTab.test.tsx
@src/lib/types.ts

<interfaces>
<!-- Post-Plan-01 types — the contract this plan binds to. -->

After Plan 01:
```typescript
export interface SquadHealth {
  greedy_null_rate: number
  min_feasible_budget_greedy: number | null
  greedy_optimality_gap_avg: null
  budget_sweep_min: number
  budget_sweep_max: number
  budget_sweep_step: number
  sweep_count: number
}

export interface PreSeasonSquadResponse {
  squad: PreSeasonSquad | null
  health: SquadHealth | null
  solver: 'ilp' | 'greedy' | null
}
```

After Plan 02:
```typescript
// src/lib/hooks/useWatchlist.ts
export function useWatchlist(): { watchlistIds: number[]; toggleWatchlist: (id: number) => void }
```

After Plan 03:
```typescript
// src/components/watchlist/WatchlistTab.tsx
export function WatchlistTab(props: { watchlistIds: number[]; toggleWatchlist: (id: number) => void }): JSX.Element
```

GemTable now accepts `watchlistIds` and `toggleWatchlist` as optional props (Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update usePreSeasonSquad to envelope return type</name>
  <files>src/lib/hooks/usePreSeasonSquad.ts</files>
  <read_first>
    - src/lib/hooks/usePreSeasonSquad.ts (current 18-line implementation)
    - src/lib/types.ts (PreSeasonSquadResponse interface added by Plan 01)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-CONTEXT.md (D-05, D-08)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-PATTERNS.md (usePreSeasonSquad.ts section — exact two-line diff)
  </read_first>
  <behavior>
    - Hook continues to return a `UseQueryResult<...>`.
    - The generic type parameter changes from `PreSeasonSquad | null` to `PreSeasonSquadResponse | null`.
    - The import changes from `import type { PreSeasonSquad } from '../types'` to `import type { PreSeasonSquadResponse } from '../types'`.
    - Behaviour identical otherwise: queryKey, queryFn, staleTime unchanged.
    - The `if (res.status === 404) return null` line stays — 404 means archive absent → no envelope.
  </behavior>
  <action>
    Edit `src/lib/hooks/usePreSeasonSquad.ts`:
    1. Change the import from `import type { PreSeasonSquad } from '../types'` to `import type { PreSeasonSquadResponse } from '../types'`.
    2. Change the `useQuery<PreSeasonSquad | null>` generic to `useQuery<PreSeasonSquadResponse | null>`.
    3. Do NOT touch queryKey, queryFn body, or staleTime.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/hooks/usePreSeasonSquad.ts` imports `PreSeasonSquadResponse` (and NOT `PreSeasonSquad`).
    - `src/lib/hooks/usePreSeasonSquad.ts` contains `useQuery<PreSeasonSquadResponse | null>`.
    - The file does NOT contain `PreSeasonSquad | null` (the old generic) anywhere outside comments.
    - `npx tsc --noEmit` exits 0 (this will fail until Task 2 updates NextSeasonPlannerTab to read `data?.squad`; run Task 1 and Task 2 sequentially within the executor before tsc gate).
  </acceptance_criteria>
  <done>Hook returns the envelope type; downstream consumers updated in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update NextSeasonPlannerTab consumer + add health indicator + solver badge</name>
  <files>src/components/next-season/NextSeasonPlannerTab.tsx, src/components/next-season/NextSeasonPlannerTab.test.tsx</files>
  <read_first>
    - src/components/next-season/NextSeasonPlannerTab.tsx (full 153-line file — current consumer pattern; FormationGrid component)
    - src/components/next-season/NextSeasonPlannerTab.test.tsx (existing test mocks for usePreSeasonSquad)
    - src/lib/types.ts (SquadHealth, PreSeasonSquadResponse from Plan 01)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-CONTEXT.md (D-08 consumer migration; UI-SPEC location of health indicator + badge)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-UI-SPEC.md (Component Inventory → NextSeasonPlannerTab; Copywriting Contract for the three health-indicator text variants)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-PATTERNS.md (NextSeasonPlannerTab.tsx section — solver pill classes, health indicator placement)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-RESEARCH.md (Pitfall 2 — every reference to `data.starters`/`data.bench`/`data.formation`/`data.budgetUsed` must become `data.squad.*`)
  </read_first>
  <behavior>
    - `usePreSeasonSquad` now returns `PreSeasonSquadResponse | null`. Component destructures `data`, then derives `squad = data?.squad ?? null`, `health = data?.health ?? null`, `solver = data?.solver ?? null` for clarity.
    - When `data === null || data === undefined`: render the existing "Pre-season squad not yet available" copy (D-08; the 404 → "Prices pending" path is preserved).
    - When `data !== null` but `data.squad === null`: render the same "Pre-season squad not yet available" copy (envelope arrived with health but no squad — same UX).
    - When `data.squad !== null`: render `<FormationGrid squad={data.squad} solver={solver} />`.
    - FormationGrid signature gains a second optional prop `solver?: 'ilp' | 'greedy' | null`. When `solver === 'ilp'`, render the green `ILP` pill (`text-xs font-normal text-green-800 dark:text-green-200 bg-green-100 dark:bg-green-900 rounded px-2 py-1`) inside the existing headline row. When `solver === 'greedy'`, render the zinc `Greedy` pill (`text-xs font-normal text-zinc-600 dark:text-zinc-300 bg-zinc-100 dark:bg-zinc-800 rounded px-2 py-1`). When null/undefined, no badge.
    - Health indicator (rendered AFTER `squadSection` inside Section A, NOT inside FormationGrid):
      - Hidden when `health == null`.
      - When `health.greedy_null_rate === 0`: render `Greedy success rate: 100% — all budgets feasible.`
      - When `health.min_feasible_budget_greedy === null`: render `No feasible squad found across £80m–£120m range.` in `text-red-600 dark:text-red-400`.
      - Otherwise: `Greedy success rate: {Math.round((1 - greedy_null_rate) * 100)}% across £{budget_sweep_min}m–£{budget_sweep_max}m budget sweep. Min feasible budget: £{min_feasible_budget_greedy.toFixed(1)}m.`
      - Container class: `text-sm text-zinc-500 dark:text-zinc-400 py-2` (red variant for the all-infeasible case uses `text-sm text-red-600 dark:text-red-400 py-2`).
    - Test cases added to NextSeasonPlannerTab.test.tsx:
      - mock usePreSeasonSquad returning `{ squad: <full squad>, health: null, solver: 'ilp' }` → expects `ILP` pill rendered, NO health paragraph
      - mock returning `{ squad: <full squad>, health: { greedy_null_rate: 0.1, min_feasible_budget_greedy: 83.5, budget_sweep_min: 80, budget_sweep_max: 120, budget_sweep_step: 0.5, sweep_count: 81, greedy_optimality_gap_avg: null }, solver: 'greedy' }` → expects `Greedy` pill AND health paragraph containing `90%`, `£80m`, `£120m`, `£83.5m`
      - mock returning `{ squad: <full squad>, health: { greedy_null_rate: 0, ... }, solver: 'ilp' }` → expects `100% — all budgets feasible` text
      - mock returning `{ squad: <full squad>, health: { greedy_null_rate: 1, min_feasible_budget_greedy: null, ... }, solver: 'ilp' }` → expects `No feasible squad found` text in red
      - mock returning `null` (404) → expects existing "Pre-season squad not yet available" copy, NO badge, NO health paragraph
      - existing tests continue to pass after fixture data is wrapped in the envelope shape
  </behavior>
  <action>
    Edit `src/components/next-season/NextSeasonPlannerTab.tsx`:

    1. Update the import line for types to include `SquadHealth`: `import type { PreSeasonPlayer, PreSeasonSquad, SquadHealth } from '@/lib/types'`.
    2. Extend the FormationGrid signature to accept an optional `solver?: 'ilp' | 'greedy' | null` prop: `function FormationGrid({ squad, solver }: { squad: PreSeasonSquad; solver?: 'ilp' | 'greedy' | null })`.
    3. Inside FormationGrid's headline row (the existing `<div className="text-sm text-zinc-700 dark:text-zinc-300 py-2 flex flex-wrap items-center gap-2">` around line 29), append a third `<span>` rendering the solver pill conditionally on the solver prop. ILP pill classes: `text-xs font-normal text-green-800 dark:text-green-200 bg-green-100 dark:bg-green-900 rounded px-2 py-1`. Greedy pill classes: `text-xs font-normal text-zinc-600 dark:text-zinc-300 bg-zinc-100 dark:bg-zinc-800 rounded px-2 py-1`.
    4. In the `NextSeasonPlannerTab()` body, after `const { data, isLoading, isError } = usePreSeasonSquad()`, derive `const squad = data?.squad ?? null; const health = data?.health ?? null; const solver = data?.solver ?? null`.
    5. Update the four-branch conditional that populates `squadSection`:
       - isLoading branch: unchanged.
       - isError branch: unchanged.
       - `data === null || data === undefined || squad === null` branch: render the existing "Pre-season squad not yet available" copy.
       - else branch: render `<FormationGrid squad={squad} solver={solver} />`.
    6. Inside the Section A wrapper `<div>` (line 140), AFTER `{squadSection}` and BEFORE the closing `</div>`, add a `HealthIndicator` inline render or a tiny private `<HealthIndicator />` subcomponent that emits the three text variants per <behavior>. The all-infeasible variant uses the red text class; the other two use the zinc text class.

    Edit `src/components/next-season/NextSeasonPlannerTab.test.tsx`:
    - Update every existing mock that returns a flat squad to wrap it in `{ squad: <flat>, health: null, solver: 'ilp' }`.
    - Add the five new test cases listed in <behavior>.

    Run `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` then `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx --reporter=verbose &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/next-season/NextSeasonPlannerTab.tsx` contains `data?.squad` and does NOT contain `squad={data}` anywhere (legacy passthrough is removed).
    - File contains both pill class strings (`bg-green-100` AND `bg-zinc-100` inside the FormationGrid).
    - File contains the three health-indicator copy variants: `all budgets feasible`, `No feasible squad found`, AND the `Min feasible budget: £` template.
    - File contains `SquadHealth` in its type imports from `@/lib/types`.
    - `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` exits 0 with the five new tests passing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Consumer reads envelope; health indicator and solver badge render correctly for all three health states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire useWatchlist + Watchlist sub-tab into page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (full file — SubTab union line 59, SECTIONS lines 61-104, useState patterns lines 140-163, render block patterns around lines 294-311)
    - src/lib/hooks/useWatchlist.ts (after Plan 02 lands — return shape)
    - src/components/watchlist/WatchlistTab.tsx (after Plan 03 lands — prop shape)
    - src/components/gem-table/GemTable.tsx (after Plan 02 lands — accepts new props)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-CONTEXT.md (D-10 lift state to page.tsx, D-11 hooks called inside WatchlistTab)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-UI-SPEC.md (Copywriting Contract — sub-tab labels)
    - .planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-PATTERNS.md (page.tsx section — three surgical edits)
  </read_first>
  <behavior>
    - SubTab union (line 59) gains the literal `'watchlist'` at the end of the union list.
    - SECTIONS Plan subTabs array (around lines 81-90) gains a new entry `{ id: 'watchlist' as SubTab, label: 'Watchlist', mobileLabel: 'Watchlist' }` IMMEDIATELY AFTER the existing `'next-season'` entry (line 88).
    - The Home component invokes `const { watchlistIds, toggleWatchlist } = useWatchlist()` at the same nesting level as `planHorizon` (after line 163).
    - The `<GemTable ... />` invocation in the render block gains `watchlistIds={watchlistIds}` and `toggleWatchlist={toggleWatchlist}` props.
    - A new render block is added after the existing `next-season` render block: `{activeSection === 'plan' && activeSubTab === 'watchlist' && <WatchlistTab watchlistIds={watchlistIds} toggleWatchlist={toggleWatchlist} />}`.
    - No existing behaviour changes (teamId, planHorizon, sectionMemory, settledGws auto-surface logic all unchanged).
  </behavior>
  <action>
    Edit `src/app/page.tsx`:

    1. Add the import line at the top, grouped with the other hook imports: `import { useWatchlist } from '@/lib/hooks/useWatchlist'` and `import { WatchlistTab } from '@/components/watchlist/WatchlistTab'`.
    2. SubTab union (line 59): append `| 'watchlist'` to the end of the union. Final form: `... | 'rank-sim' | 'next-season' | 'watchlist'`.
    3. SECTIONS Plan subTabs array (lines 81-89): insert `{ id: 'watchlist' as SubTab, label: 'Watchlist', mobileLabel: 'Watchlist' }` as the line AFTER the existing `next-season` entry. Trailing comma per existing style.
    4. After the `planHorizon` useState block (line ~163), add: `const { watchlistIds, toggleWatchlist } = useWatchlist()`.
    5. Find the existing `<GemTable ... />` invocation in the render block; append `watchlistIds={watchlistIds} toggleWatchlist={toggleWatchlist}` to its props list. Preserve all existing props.
    6. Find the render block for next-season (`{activeSection === 'plan' && activeSubTab === 'next-season' && (<NextSeasonPlannerTab />)}` near line 294). Insert a NEW sibling block immediately after: `{activeSection === 'plan' && activeSubTab === 'watchlist' && (<WatchlistTab watchlistIds={watchlistIds} toggleWatchlist={toggleWatchlist} />)}`.
    7. Do not touch teamId, planHorizon, sectionMemory, settledGws useEffect, or any unrelated render block.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` SubTab union literal `'watchlist'` is present: `grep -n "'watchlist'" src/app/page.tsx` returns at least 4 matches (union, SECTIONS entry, render block condition, render block prop wiring).
    - `src/app/page.tsx` SECTIONS Plan array contains `{ id: 'watchlist' as SubTab, label: 'Watchlist', mobileLabel: 'Watchlist' }` AND the entry comes after `'next-season'` (verify ordering via grep with `-A 2` on `next-season as SubTab`).
    - `src/app/page.tsx` contains `useWatchlist()` exactly once (filter: `grep -v '^//' src/app/page.tsx | grep -c 'useWatchlist()' ` returns 1) and the destructure `{ watchlistIds, toggleWatchlist }`.
    - `src/app/page.tsx` contains `<WatchlistTab` exactly once and `watchlistIds={watchlistIds}` appears at least twice (one on GemTable, one on WatchlistTab).
    - `npx tsc --noEmit` exits 0 across the whole project.
    - `npx vitest run` exits 0 (full JS suite).
    - `python -m pytest pipeline/tests/ -x` exits 0 (full Python suite).
  </acceptance_criteria>
  <done>Page renders Watchlist sub-tab; GemTable star button now triggers real watchlist toggles; all tests across both suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usePreSeasonSquad envelope → NextSeasonPlannerTab | Server JSON parsed via TanStack Query; existing trust boundary unchanged |
| useWatchlist (browser) → page.tsx → child components | localStorage IDs propagate as props to GemTable and WatchlistTab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-14 | Tampering | data?.squad reads when envelope is null | mitigate | Optional chaining (`data?.squad`) returns undefined safely; the conditional render falls into the existing "Prices pending" branch with no runtime error. |
| T-127-15 | Information Disclosure | Health indicator text reveals greedy null rate publicly | accept | Same data is computed deterministically from public FPL bootstrap; no secret. |
| T-127-16 | Denial of Service | New WatchlistTab mount triggers three hook fetches | accept | All three (usePlayers, useLineupNews, usePreSeasonSquad) are already stale-cached at the page level — no extra network round-trip on sub-tab switch (D-11). |
| T-127-17 | Spoofing | A localStorage-injected non-existent player id reaches WatchlistTab | mitigate | WatchlistTab's departed-detection set-difference (Plan 03) renders the stub card with a Departed pill; no server call is made with the spoofed id. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` exits 0 (entire JS suite — covers existing + new tests across all four plans).
- `python -m pytest pipeline/tests/ -x` exits 0 (entire Python suite).
- Manual smoke check (optional but recommended): pin a player in Gem Ratings → switch to Plan → Watchlist → confirm card renders.
- Grep: `'watchlist'` appears in page.tsx SubTab union, SECTIONS entry, render condition.
</verification>

<success_criteria>
- usePreSeasonSquad returns the envelope type and NextSeasonPlannerTab reads data?.squad.
- Health indicator and solver badge render in NextSeasonPlannerTab when the data is present.
- Plan section now contains a Watchlist sub-tab between Next Season and any other entries.
- Pinning a player in GemTable persists to localStorage and is visible immediately on the Watchlist tab.
- Departed-player IDs (manually edited in DevTools) render as a Departed pill in the watchlist grid.
- Full JS + Python test suites green.
</success_criteria>

<output>
After completion, create `.planning/phases/127-squad-health-diagnostics-transfer-watchlist/127-04-SUMMARY.md`.
</output>
