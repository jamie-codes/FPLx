---
phase: 110
plan: 04
type: tdd
wave: 1
depends_on: [110-02]
files_modified:
  - src/app/api/gw-review/route.test.ts
  - src/app/api/gw-review/route.ts
autonomous: true
gap_closure: true
requirements: [CR-01]
tags: [bugfix, api, gap-closure, fpl-live-endpoint, gw-review, captain-delta, tdd]
must_haves:
  truths:
    - "When pick.total_points is 0 for both yourCaptain and optimalCaptain (settled GW), but liveMap has non-zero actual points and yourCaptain != optimalCaptain, GET /api/gw-review returns captain_delta reflecting the liveMap-based point difference (not 0)"
    - "captainDeltaRaw at src/app/api/gw-review/route.ts no longer reads pick.total_points for either operand — both lookups go through liveMap.get(element) ?? total_points"
    - "yourCaptain.multiplier (Triple Captain handling) is preserved in the captainDeltaRaw formula — only the total_points operands change"
    - "Vitest run of src/app/api/gw-review/route.test.ts exits 0 with all existing tests green plus one new FIX-CAP-DELTA happy-path test (11 total)"
    - "Full vitest suite exits 0 — no regressions in other route or component tests"
  artifacts:
    - path: src/app/api/gw-review/route.test.ts
      provides: "One new TDD test inside the existing Phase 110 FIX-03/04 describe block (or a new FIX-CAP-DELTA describe) that uses different elements for yourCaptain and optimalCaptain with diverging pick.total_points (0) vs liveMap (14 vs 20) values"
      contains: "captain_delta"
    - path: src/app/api/gw-review/route.ts
      provides: "captainDeltaRaw rewired to use liveMap.get(element) ?? total_points for both the optimal and your captain operands; multiplier on yourCaptain preserved"
      contains: "liveMap.get(optimalCaptain.element) ?? optimalCaptain.total_points"
  key_links:
    - from: src/app/api/gw-review/route.ts (Step 5 captainDeltaRaw, ~line 221-222)
      to: liveMap (built in Step 4b)
      via: "liveMap.get(optimalCaptain.element) ?? optimalCaptain.total_points and liveMap.get(yourCaptain.element) ?? yourCaptain.total_points in the captainDeltaRaw formula"
      pattern: "liveMap\\.get\\((?:optimal|your)Captain\\.element\\)"
---

<objective>
Close gap CR-01 from 110-VERIFICATION.md: `captainDeltaRaw` at `src/app/api/gw-review/route.ts` lines 221-222 still reads `pick.total_points` for both captain operands, so the GW Review "Captain delta" stat card shows 0 for every settled gameweek where the user's captain differs from the optimal captain. `liveMap` is already built in Step 4b (Plan 02) but was deliberately not applied to `captainDeltaRaw` per a now-known-incorrect plan instruction.

Purpose: The phase goal includes "captain analytics all display correct numbers." Phase 110 fixed `top_scorer_pts` (FIX-03) and `best_bench_player_pts` (FIX-04) to use `liveMap`, but left `captain_delta` reading the stale `pick.total_points=0` source. The verification report scored 3/4 must-haves verified; this plan closes the fourth. The fix is the same liveMap lookup already used for sibling fields, with `?? total_points` (not `?? 0`) preserving the SC-5 graceful-degradation contract when the live endpoint is unavailable.

Output: One new TDD test in `route.test.ts` proving the bug (RED), followed by a two-line edit to `route.ts` swapping `optimalCaptain.total_points`/`yourCaptain.total_points` for `liveMap.get(...) ?? total_points` lookups (GREEN). `yourCaptain.multiplier` is preserved unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-CONTEXT.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-VERIFICATION.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-REVIEW.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-02-PLAN.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-02-SUMMARY.md
@src/app/api/gw-review/route.ts
@src/app/api/gw-review/route.test.ts

<interfaces>
<!-- Concrete state of route.ts after Plan 02. Executor must verify these line numbers
     against the actual file at task start (line numbers can drift). -->

Current production state — src/app/api/gw-review/route.ts (post-Plan 02):

  Step 4b (lines 171-189) — builds liveMap:
    let liveMap: Map<number, number> = new Map()
    try {
      const liveRes = await fetch(`${FPL_BASE}/event/${gw}/live/`, {
        headers: { 'User-Agent': 'fplx/1.17 (+https://fplx.app)' },
      })
      if (liveRes.ok) {
        const liveJson = (await liveRes.json()) as {
          elements?: Array<{ id: number; stats: { total_points: number } }>
        }
        if (Array.isArray(liveJson?.elements)) {
          liveMap = new Map(liveJson.elements.map((e) => [e.id, e.stats.total_points]))
        }
      }
    } catch {
      // liveMap stays empty → fields fall back
    }

  Step 5 captain delta (CURRENT — BROKEN — lines 221-222):
    const captainDeltaRaw =
      optimalCaptain.total_points * 2 - yourCaptain.total_points * yourCaptain.multiplier
    const captainDelta = Math.max(0, captainDeltaRaw)

  Step 5 captain delta (TARGET — FIXED):
    const captainDeltaRaw =
      (liveMap.get(optimalCaptain.element) ?? optimalCaptain.total_points) * 2 -
      (liveMap.get(yourCaptain.element) ?? yourCaptain.total_points) * yourCaptain.multiplier
    const captainDelta = Math.max(0, captainDeltaRaw)

Test helper signature (already extended by Plan 02, route.test.ts lines 25-60):
  mockUpstream(
    picks: Pick[],
    elements: Array<{ id: number; web_name: string }>,
    dreamTeam: ... | null = { ... },
    dreamTeamOk: boolean = true,
    live: { elements: Array<{ id: number; stats: { total_points: number } }> } | null = null,
    liveOk: boolean = true,
  )
  // When live === null, defaults to synthesising from pick.total_points (see lines 35-37).

Existing Phase 110 FIX-03/04 describe block (route.test.ts lines 347-446):
  describe('Phase 110 FIX-03/04: /api/gw-review live endpoint for settled GW points', () => {
    it('top_scorer_pts equals live total_points for the top starter (FIX-03)', ...)
    it('best_bench_player_pts equals live total_points for the best bench player (FIX-04)', ...)
    it('degrades gracefully to 0 when event/live/ returns 503 — route still returns HTTP 200 (SC-5)', ...)
  })
</interfaces>

<source_of_truth>
- 110-VERIFICATION.md gaps[0].missing — the three required edits (replace optimalCaptain operand, replace yourCaptain operand, add companion test)
- 110-VERIFICATION.md "Gap: captainDeltaRaw Not Updated to Use liveMap (CR-01)" — root cause and fix mechanism
- 110-REVIEW.md CR-01 — code review finding that originated this gap
- 110-02-PLAN.md Task 2 — pattern to mirror (liveMap.get(element) ?? fallback)
- src/app/api/gw-review/route.ts lines 221-222 — the broken formula to fix
- src/app/api/gw-review/route.test.ts lines 347-446 — existing Phase 110 FIX-03/04 describe block to extend
</source_of_truth>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add failing captain_delta liveMap test to route.test.ts</name>
  <files>src/app/api/gw-review/route.test.ts</files>
  <read_first>
    - src/app/api/gw-review/route.test.ts (ENTIRE file — confirm: (a) the `mockUpstream` helper at lines 25-60 already accepts `live` and `liveOk` parameters from Plan 02, (b) the `Phase 110 FIX-03/04` describe block at lines 347-446 and its three existing tests, (c) the `makeStarters()` helper at lines 84-93 which marks element=1 as captain with multiplier=2, (d) the `starter()`, `bench()`, `makeRequest()` helpers)
    - src/app/api/gw-review/route.ts (ENTIRE file — confirm the current captainDeltaRaw at lines 221-222 reads pick.total_points; this is the production state Task 2 will fix; the test must FAIL against this current state)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-VERIFICATION.md (the gaps[0].missing block — the new test must match its "companion test" description)
  </read_first>
  <behavior>
    - New test (FIX-CAP-DELTA RED): With yourCaptain = element 1 (multiplier=2, is_captain=true, pick.total_points=0) and optimalCaptain = element 2 (a different starter, pick.total_points=0), and liveMap returning element 1 → 14 points and element 2 → 20 points (so element 2 outscored element 1), GET /api/gw-review must return captain_delta equal to `Math.max(0, 20 * 2 - 14 * 2) = 12`. (Note: 20*2 - 14*2 = 12, because yourCaptain.multiplier is 2 — see formula).
    - The test MUST use DIFFERENT elements for yourCaptain and optimalCaptain (the existing FIX-03/04 tests used element=1 for both, which is why the gap wasn't caught).
    - The test MUST FAIL against the current production code: with the broken formula reading pick.total_points=0 for both operands, captainDeltaRaw = 0*2 - 0*2 = 0 → captain_delta = 0, which is NOT 12.
    - The 10 existing tests must continue to pass (3 PGW-01 + 4 PGW-03 + 3 FIX-03/04).
  </behavior>
  <action>
    Append a NEW test to the existing `describe('Phase 110 FIX-03/04: /api/gw-review live endpoint for settled GW points', ...)` block in src/app/api/gw-review/route.test.ts (the block currently spans lines 347-446 and ends with the SC-5 test at line 446). Insert the new `it(...)` BEFORE the closing `})` of that describe block.

    Test name: `'captain_delta reflects liveMap point difference when yourCaptain != optimalCaptain in a settled GW (CR-01)'`

    Test body — concrete construction:

    1. Build a custom starters array (do NOT use makeStarters() unchanged, because we need element=2 to be the top scorer per liveMap). The simplest path:
       - `const starters = makeStarters()` to start.
       - Then mutate `starters[0].total_points = 0` (settled-GW reality: pick.total_points=0 for everyone).
       - Then mutate `starters[1].total_points = 0` as well, and loop over all `starters[i]` setting `total_points = 0` to model a settled GW uniformly.
       - element=1 remains captain (multiplier=2, is_captain=true) from makeStarters(); element=2 remains is_vice_captain=true with multiplier=1.

    2. Build a 4-element bench at pick.total_points=0 for completeness:
       - `const benchPicks = [bench(101, 0), bench(102, 0), bench(103, 0), bench(104, 0)]`
       - `const allPicks = [...starters, ...benchPicks]`

    3. Build the elements array for bootstrap mock:
       - `const elements = [ ...starters.map(s => ({ id: s.element, web_name: \`Player${s.element}\` })), { id: 101, web_name: 'Bench1' }, { id: 102, web_name: 'Bench2' }, { id: 103, web_name: 'Bench3' }, { id: 104, web_name: 'Bench4' } ]`

    4. Build the liveData such that element=2 is the top scorer (20 pts) and element=1 is the user's captain at 14 pts:
       - element 1 → 14
       - element 2 → 20
       - elements 3..11 → some lower value, e.g. 4
       - bench (101..104) → 0
       Use this shape:
       ```
       const liveData = {
         elements: [
           { id: 1, stats: { total_points: 14 } },
           { id: 2, stats: { total_points: 20 } },
           ...Array.from({ length: 9 }, (_, i) => ({ id: i + 3, stats: { total_points: 4 } })),
           { id: 101, stats: { total_points: 0 } },
           { id: 102, stats: { total_points: 0 } },
           { id: 103, stats: { total_points: 0 } },
           { id: 104, stats: { total_points: 0 } },
         ],
       }
       ```

    5. Call `mockUpstream(allPicks, elements, null, false, liveData, true)` — same pattern used by the existing FIX-03/04 tests at lines 373, 408 (dreamTeamOk=false to keep that branch out of scope; liveOk=true so /live/ returns liveData).

    6. Invoke the route and read the body:
       ```
       const response = await GET(makeRequest())
       expect(response.status).toBe(200)
       const body = await response.json() as {
         captain_delta: number
         captain_name: string
         optimal_captain_name: string
         top_scorer_pts: number
       }
       ```

    7. Assertions:
       - `expect(body.captain_name).toBe('Player1')` — user picked element 1 as captain
       - `expect(body.optimal_captain_name).toBe('Player2')` — element 2 was optimal per liveMap (20 > 14 > 4)
       - `expect(body.top_scorer_pts).toBe(20)` — sanity check that liveMap drove top scorer (already covered by FIX-03 but good to anchor)
       - **The critical assertion**: `expect(body.captain_delta).toBe(12)` — derived from `Math.max(0, 20*2 - 14*2) = Math.max(0, 40 - 28) = 12`. yourCaptain.multiplier is 2 (from makeStarters()). This MUST fail against current production code because the broken formula yields `0*2 - 0*2 = 0`.

    DO NOT modify src/app/api/gw-review/route.ts in this task. The production code is still broken; this task only writes the failing test (TDD RED phase).

    DO NOT add an SC-5 test for the captain_delta liveMap-empty case — when liveMap is empty, both `?? total_points` fallbacks resolve to 0, captain_delta resolves to 0, which matches pre-fix behaviour and is acceptable per the verification report. The existing SC-5 test in this describe block already covers the route-still-returns-200 contract.

    Run `npx vitest run src/app/api/gw-review/route.test.ts` and confirm: 1 new failing test, 10 existing tests still pass.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/gw-review/route.test.ts 2>&amp;1 | tee /tmp/110-04-task1.log; grep -E "Tests +.* 1 failed" /tmp/110-04-task1.log &amp;&amp; grep -E "10 passed" /tmp/110-04-task1.log &amp;&amp; echo "RED CONFIRMED" || (echo "RED EXPECTED — task1 must produce exactly 1 failing test" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/gw-review/route.test.ts contains the literal string `captain_delta reflects liveMap` (the new test name)
    - src/app/api/gw-review/route.test.ts contains the literal string `CR-01` (links the test back to the gap)
    - src/app/api/gw-review/route.test.ts contains an assertion `body.captain_delta).toBe(12)` (or `.toEqual(12)`)
    - src/app/api/gw-review/route.test.ts contains an assertion `body.optimal_captain_name).toBe('Player2')` (proves yourCaptain != optimalCaptain in the fixture)
    - The new test's fixture sets `starters[i].total_points = 0` for ALL starters (settled-GW model — pick.total_points must be 0 to prove the bug)
    - The new test's `liveData` contains `{ id: 1, stats: { total_points: 14 } }` AND `{ id: 2, stats: { total_points: 20 } }` (diverging values for yourCaptain vs optimalCaptain)
    - `npx vitest run src/app/api/gw-review/route.test.ts` reports exactly 1 failed test (the new captain_delta test) and 10 passed tests
    - The 3 existing Phase 110 FIX-03/04 tests (top_scorer_pts, best_bench_player_pts, SC-5) still pass
    - src/app/api/gw-review/route.ts is UNCHANGED — `git diff src/app/api/gw-review/route.ts` returns no output
  </acceptance_criteria>
  <done>RED test committed inside the existing Phase 110 FIX-03/04 describe block; vitest reports exactly 1 new failing test (captain_delta=0 vs expected 12) and 10 passing tests; production route.ts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — rewire captainDeltaRaw to read from liveMap with ?? total_points fallback</name>
  <files>src/app/api/gw-review/route.ts</files>
  <read_first>
    - src/app/api/gw-review/route.ts (ENTIRE file — locate the current captainDeltaRaw declaration; per Plan 02 it is at lines 221-222 but the executor must verify the line numbers in the actual file and the surrounding context to ensure correct surgical replacement)
    - src/app/api/gw-review/route.test.ts (after Task 1 — confirm the failing test exists and shows the expected diff of 12 vs actual 0)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-VERIFICATION.md (gaps[0].missing — the exact two replacements specified)
    - node_modules/next/dist/docs/ (Next.js 16 route handler guide — required by AGENTS.md before editing route handlers; the edit does not change the export signature but read the guide to confirm no API surface change is needed)
  </read_first>
  <action>
    In src/app/api/gw-review/route.ts, locate the captainDeltaRaw declaration (currently lines 221-222 after Plan 02). It currently reads exactly:

      const captainDeltaRaw =
        optimalCaptain.total_points * 2 - yourCaptain.total_points * yourCaptain.multiplier

    Replace those two lines with:

      const captainDeltaRaw =
        (liveMap.get(optimalCaptain.element) ?? optimalCaptain.total_points) * 2 -
        (liveMap.get(yourCaptain.element) ?? yourCaptain.total_points) * yourCaptain.multiplier

    Rules and constraints:

    1. ONLY the two `total_points` operands in the captainDeltaRaw formula change. The `* 2` multiplier on the optimal side and the `* yourCaptain.multiplier` on the user side are preserved unchanged. `yourCaptain.multiplier` correctly handles Triple Captain (multiplier=3) and must not be touched.

    2. Use `?? total_points` (NOT `?? 0`) as the fallback. Per 110-VERIFICATION.md gaps[0] and the SC-5 contract:
       - When liveMap is empty (live endpoint failed), `liveMap.get(...)` returns undefined → fallback to `pick.total_points` (which is 0 for settled GWs anyway, acceptable degradation matching pre-fix state).
       - This is intentionally DIFFERENT from `top_scorer_pts: liveMap.get(...) ?? 0` (which uses `?? 0` because there is no meaningful fallback for the output field). For the captainDelta formula, falling back to `pick.total_points` preserves the original arithmetic shape when the live endpoint is unreachable.

    3. The `Math.max(0, captainDeltaRaw)` clamp at the following line (currently line 223) must remain unchanged. It serves D-06 defence-in-depth for Triple Captain edge cases.

    4. Update the comment block above the formula (currently lines 218-220) to reflect the new behaviour. The current comment reads:
       ```
       // D-06: captain delta uses pick.multiplier (Pitfall 3 — handles Triple Captain where multiplier=3)
       // Clamp to 0 if your captain WAS the optimal captain (or if Triple Captain on optimal makes
       // the formula go negative — defence in depth)
       ```
       Replace it with:
       ```
       // D-06: captain delta uses pick.multiplier (Pitfall 3 — handles Triple Captain where multiplier=3).
       // CR-01 (Phase 110 gap closure): both total_points operands now read from liveMap with
       // `?? total_points` fallback. pick.total_points is 0 for settled GWs (same root cause as
       // FIX-03/04); without the liveMap lookup, captain_delta would always be 0 when user
       // captain != optimal captain. The `?? total_points` fallback (not `?? 0`) preserves SC-5
       // behaviour when the live endpoint is unavailable.
       // Clamp to 0 if your captain WAS the optimal captain (or if Triple Captain on optimal
       // makes the formula go negative — defence in depth).
       ```

    5. Do NOT touch any other field. Do NOT change Step 4b (liveMap construction). Do NOT change the topScorer / optimalCaptain / bestBench reductions in Step 5 (already rewired by Plan 02). Do NOT alter `top_scorer_pts` or `best_bench_player_pts` output assignments. Do NOT change the captain_name or optimal_captain_name output fields.

    6. Do NOT change FPL_BASE, do NOT introduce a new constant, do NOT alter the input validation at lines 61-66.

    After the edit, run:
      a. `npx vitest run src/app/api/gw-review/route.test.ts` — all 11 tests must pass (the 10 baseline + 1 new captain_delta test).
      b. `npx vitest run` — full suite must pass with no regressions.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/gw-review/route.test.ts 2>&amp;1 | tee /tmp/110-04-task2.log; grep -E "11 passed" /tmp/110-04-task2.log &amp;&amp; ! grep -E "Tests +.* [1-9][0-9]* failed" /tmp/110-04-task2.log &amp;&amp; npx vitest run 2>&amp;1 | tee /tmp/110-04-task2-full.log &amp;&amp; ! grep -E "Tests +.* [1-9][0-9]* failed" /tmp/110-04-task2-full.log</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/gw-review/route.ts contains the literal string `liveMap.get(optimalCaptain.element) ?? optimalCaptain.total_points`
    - src/app/api/gw-review/route.ts contains the literal string `liveMap.get(yourCaptain.element) ?? yourCaptain.total_points`
    - src/app/api/gw-review/route.ts does NOT contain the literal substring `optimalCaptain.total_points * 2 - yourCaptain.total_points * yourCaptain.multiplier` (the broken formula must be gone)
    - src/app/api/gw-review/route.ts STILL contains `yourCaptain.multiplier` (Triple Captain handling preserved)
    - src/app/api/gw-review/route.ts STILL contains `Math.max(0, captainDeltaRaw)` (clamp preserved)
    - src/app/api/gw-review/route.ts contains a comment referencing `CR-01` near the captainDeltaRaw declaration (documents the gap-closure fix)
    - Step 4b liveMap construction (lines ~171-189) is UNCHANGED — `git diff src/app/api/gw-review/route.ts` shows only changes in the captainDeltaRaw region (lines ~218-224)
    - `top_scorer_pts: liveMap.get(topScorer.element) ?? 0` is still present (FIX-03 untouched)
    - `best_bench_player_pts: bestBench != null ? (liveMap.get(bestBench.element) ?? 0) : 0` is still present (FIX-04 untouched)
    - `npx vitest run src/app/api/gw-review/route.test.ts` reports 0 failed tests and 11 passed tests
    - `npx vitest run` (full suite) reports 0 failed tests — no regressions in route.test files, component tests, or anywhere else
  </acceptance_criteria>
  <done>captainDeltaRaw rewired to use liveMap.get(element) ?? total_points for both operands with multiplier preserved; the new RED test from Task 1 now passes (12 not 0); all 11 tests in route.test.ts green; full suite green; comment block updated to document CR-01 and the `?? total_points` (vs `?? 0`) rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/gw-review | Untrusted query params `teamId` and `gw` cross here; both validated `/^\d+$/` (unchanged) |
| /api/gw-review → fantasy.premierleague.com | Server-to-server FPL fetch; no new endpoint introduced — Step 4b live fetch already exists from Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-04-01 | Tampering | Malformed live JSON missing keys for captain elements | mitigate | `liveMap.get(element) ?? total_points` falls back to pick.total_points when liveMap is missing the key; for settled GWs that is 0, which matches pre-fix behaviour — acceptable degradation, no crash |
| T-110-04-02 | Denial of Service | Live endpoint timeout cascading into captain_delta failure | mitigate | The Step 4b standalone try/catch from Plan 02 already absorbs all live-endpoint failures into an empty liveMap; this plan only adds two more `liveMap.get(...) ?? total_points` consumers, which inherit the same graceful-degradation contract |
| T-110-04-03 | Tampering | Integer overflow in captain_delta arithmetic | accept | FPL per-player points are bounded (~30 max realistic per GW); `value * multiplier` (max multiplier=3) yields well under MAX_SAFE_INTEGER; no overflow risk |
| T-110-04-04 | Information Disclosure | New code path exposes additional data | n/a | No new data exposed — captain_delta field already exists in the API response; only its computed value changes |
</threat_model>

<verification>
- After Task 1: `npx vitest run src/app/api/gw-review/route.test.ts` reports exactly 1 failing test (new captain_delta test) and 10 passing tests
- After Task 2: `npx vitest run src/app/api/gw-review/route.test.ts` reports 0 failed and 11 passed
- After Task 2: `npx vitest run` (full suite) reports 0 failed — no regression in any other test file
- Grep verification: `grep -n "liveMap.get" src/app/api/gw-review/route.ts` must show at least 4 occurrences (Step 5 reductions x2, top_scorer_pts output, best_bench_player_pts output) PLUS the 2 new captainDeltaRaw lookups → ≥6 total occurrences
- Manual UAT (deferred): hit /api/gw-review for a settled GW where the user's captain differs from the optimal captain — `captain_delta` should be a positive number reflecting the live point gap, not 0
</verification>

<success_criteria>
- The captain_delta field on /api/gw-review reflects the live-point difference for settled GWs when user captain != optimal captain (gap CR-01 closed)
- yourCaptain.multiplier is preserved in the formula (Triple Captain semantics intact)
- The `?? total_points` fallback (NOT `?? 0`) is used for both operands — SC-5 graceful degradation when live endpoint is unavailable preserved
- One new TDD test asserts the corrected behaviour with diverging yourCaptain (element=1, live=14) and optimalCaptain (element=2, live=20) → captain_delta=12
- All 11 tests in route.test.ts pass; full vitest suite passes; no regression in other route or component tests
- Step 4b liveMap construction (Plan 02) is unchanged; top_scorer_pts and best_bench_player_pts wiring (FIX-03/04) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/110-gw-review-history-fixes-v1-20/110-04-SUMMARY.md` capturing:
- The exact lines of route.ts that were modified (captainDeltaRaw declaration and the preceding comment block)
- The exact `it(...)` name added to route.test.ts and which describe block it was inserted into
- Final vitest counts: `npx vitest run src/app/api/gw-review/route.test.ts` (expect 11 passed, 0 failed) and `npx vitest run` (full suite, expect 0 failed)
- Confirmation that `yourCaptain.multiplier` is still present in the formula (Triple Captain preserved) and that the fallback is `?? total_points` (not `?? 0`) for SC-5 reasons
- A one-line note that CR-01 from 110-REVIEW.md and the gap from 110-VERIFICATION.md are now closed
</output>
