---
phase: 110
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/squad/GwReviewTab.tsx
  - src/components/squad/GwReviewTab.test.tsx
autonomous: true
requirements: [FIX-05]
tags: [bugfix, ui, sign-flip, gw-review, tdd]
must_haves:
  truths:
    - "When dream team score (122) > user score (72), the benchmark card shows '+50 vs you' in amber sentiment class"
    - "When user score (95) > dream team score (80), the benchmark card shows '−15 vs you' (U+2212 minus, not hyphen-minus) in green sentiment class"
    - "When user score equals dream team score, the benchmark card shows 'on par' in green sentiment class"
    - "Vitest run of src/components/squad/GwReviewTab.test.tsx exits 0 with all tests green (no PGW-03 assertion regressions)"
  artifacts:
    - path: src/components/squad/GwReviewTab.tsx
      provides: "Corrected benchmarkDiff sign and swapped sentiment classes"
      contains: "review.benchmark_score - review.your_score"
    - path: src/components/squad/GwReviewTab.test.tsx
      provides: "FIX-05 TDD coverage + updated PGW-03 assertions"
      contains: "Phase 110 FIX-05"
  key_links:
    - from: src/components/squad/GwReviewTab.tsx
      to: review.benchmark_score / review.your_score
      via: "subtraction order at line 171"
      pattern: "benchmark_score\\s*-\\s*your_score"
---

<objective>
Fix FIX-05 — the dream team delta on the GW Review benchmark card currently shows the wrong sign and the wrong sentiment colour. After this fix, when the dream team beats the user, the delta is positive and amber; when the user beats the dream team, the delta is negative and green.

Purpose: The benchmark card is the user's primary "how did I do vs the optimal" signal in the GW Review surface. The current sign inversion makes the user think they outperformed the dream team when in fact they were beaten — a high-trust regression that undermines the entire review surface.

Output: Corrected `benchmarkDiff` calculation in `GwReviewTab.tsx` (line 171), swapped sentiment class assignments (lines 174-183), updated PGW-03 component tests that were written against the broken sign convention, and 2 new TDD tests covering the canonical FIX-05 examples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-CONTEXT.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-RESEARCH.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-PATTERNS.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-UI-SPEC.md
@.planning/phases/110-gw-review-history-fixes-v1-20/110-VALIDATION.md
@src/components/squad/GwReviewTab.tsx
@src/components/squad/GwReviewTab.test.tsx
@src/lib/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no further codebase exploration required. -->

GwReview type (from src/lib/types.ts — fields used by FIX-05):
  - your_score: number       // user's GW total, unsigned
  - benchmark_score: number  // dream team total OR FPL average, unsigned
  - benchmark_label: string  // 'Dream team' | 'FPL average'

Sentiment class constants (already in GwReviewTab.tsx — do NOT redefine):
  - Amber:  'text-amber-700 dark:text-amber-300'
  - Green:  'text-green-600 dark:text-green-400'

Render gate (existing condition at line 200, unchanged):
  - delta sub-label is only rendered when benchmark_label !== 'FPL average'
</interfaces>

<source_of_truth>
- 110-CONTEXT.md D-06, D-07, D-08 — locked decisions for sign + sentiment + label format
- 110-UI-SPEC.md "FIX-05: Dream Team Delta — Exact UI Contract" — verified examples table
- 110-PATTERNS.md "src/components/squad/GwReviewTab.tsx (FIX-05)" — exact line and pattern to apply
- 110-RESEARCH.md Open Question #2 — existing PGW-03 tests at lines 180-185 and 187-195 of GwReviewTab.test.tsx test the BROKEN sign and MUST be updated as part of this task
</source_of_truth>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write FIX-05 TDD tests and update broken PGW-03 assertions</name>
  <files>src/components/squad/GwReviewTab.test.tsx</files>
  <read_first>
    - src/components/squad/GwReviewTab.test.tsx (entire file — 14 baseline tests, identify the 3 PGW-03 assertions at approx lines 180-205 that test the broken sign)
    - src/components/squad/GwReviewTab.tsx (entire file — confirm benchmarkDiff lives at line 171 and the StatCard delta prop renders via gw-review-benchmark-card testid)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-PATTERNS.md (sections "src/components/squad/GwReviewTab.test.tsx" and "FIX-05" describe block pattern)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-UI-SPEC.md (verified examples table — 72/122→+50 amber, 95/80→−15 green, 88/88→on par green)
  </read_first>
  <behavior>
    - Test 1 (NEW, FIX-05 RED): With your_score=72, benchmark_score=122, benchmark_label='Dream team', the rendered benchmark card text must contain '+50 vs you' AND the sentimentClass on the StatCard must equal 'text-amber-700 dark:text-amber-300'.
    - Test 2 (NEW, FIX-05 RED): With your_score=95, benchmark_score=80, benchmark_label='Dream team', the rendered benchmark card text must contain '−15 vs you' (U+2212 minus, not hyphen-minus) AND the sentimentClass on the StatCard must equal 'text-green-600 dark:text-green-400'.
    - Test 3 (NEW, FIX-05 RED): With your_score=88, benchmark_score=88, benchmark_label='Dream team', the rendered benchmark card text must contain 'on par' AND the sentimentClass must equal 'text-green-600 dark:text-green-400'.
    - PGW-03 assertion update A (existing test at approx line 180): The test that uses your_score > benchmark_score must now assert the label is '−N vs you' and the class is green (was previously asserting '+N vs you' and amber). Update either the scenario data or the assertion to match the corrected sign convention.
    - PGW-03 assertion update B (existing test at approx line 187): The test that uses your_score < benchmark_score must now assert '+N vs you' and amber (was previously '−N vs you' and green). Update assertion to match corrected sign.
    - PGW-03 assertion update C (any other existing test that hardcodes the broken sentiment-class-to-sign mapping): identify and correct it the same way.
    - All 3 NEW tests must FAIL when run (RED) — the production code at line 171 still uses the broken `your_score - benchmark_score`. The 3 existing PGW-03 assertions, once updated, must also FAIL until Task 2 ships the production fix.
  </behavior>
  <action>
    Add a new `describe('Phase 110 FIX-05: benchmarkDiff sign correction', ...)` block to GwReviewTab.test.tsx with the three new tests above. Use the existing `withReview` helper (or define one in the same shape if absent) to override your_score and benchmark_score in the mocked GwReview. Query the rendered benchmark card via `container.querySelector('[data-testid="gw-review-benchmark-card"]')` and assert (a) `.textContent` matches the expected delta string and (b) the sentiment class is present somewhere within the card's class tree (use a class-presence check on the value or delta element — match the pattern already used by other PGW-03 tests in this file).

    Then walk the existing PGW-03 tests in this file (approx lines 180-205 per RESEARCH.md Open Question #2) and update each broken assertion to match the corrected sign mapping:
      - your_score > benchmark_score → benchmarkDiff < 0 → '−N vs you' label, green class
      - benchmark_score > your_score → benchmarkDiff > 0 → '+N vs you' label, amber class
      - equal scores → 'on par', green class

    Use U+2212 (the actual minus sign character, not '-' hyphen-minus) when writing or asserting negative deltas — copy from 110-UI-SPEC.md if needed.

    Do NOT modify GwReviewTab.tsx in this task. The component is still broken; this task only writes/updates the failing tests (TDD RED phase).

    Run `npx vitest run src/components/squad/GwReviewTab.test.tsx` — expect FAILURES on the 3 new tests and on the updated PGW-03 assertions. The 11 unrelated existing tests must still pass. Do not commit GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/squad/GwReviewTab.test.tsx 2>&amp;1 | tee /tmp/110-01-task1.log; ! grep -E "Tests +.*[0-9]+ failed" /tmp/110-01-task1.log >/dev/null &amp;&amp; echo "RED EXPECTED — task1 should fail" &amp;&amp; exit 1; echo "RED CONFIRMED"; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - GwReviewTab.test.tsx contains the literal string `Phase 110 FIX-05`
    - GwReviewTab.test.tsx contains the literal string `+50 vs you` in a test assertion
    - GwReviewTab.test.tsx contains the literal string `−15 vs you` (U+2212 minus, not hyphen-minus) in a test assertion
    - GwReviewTab.test.tsx contains the literal string `on par` in at least one assertion
    - GwReviewTab.test.tsx contains the literal string `text-amber-700` AND `text-green-600` in FIX-05 assertions
    - `npx vitest run src/components/squad/GwReviewTab.test.tsx` reports at least 5 failing tests (3 new RED + ≥2 updated PGW-03 assertions)
    - GwReviewTab.tsx is UNCHANGED (git diff src/components/squad/GwReviewTab.tsx returns no diff)
  </acceptance_criteria>
  <done>RED tests committed; vitest reports failures on the FIX-05 block and updated PGW-03 assertions; production component file untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — flip benchmarkDiff sign and swap sentiment classes in GwReviewTab.tsx</name>
  <files>src/components/squad/GwReviewTab.tsx</files>
  <read_first>
    - src/components/squad/GwReviewTab.tsx (entire file — confirm line 171 and the if/else-if/else chain at lines 174-183 still match the patterns documented in 110-PATTERNS.md)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-CONTEXT.md (D-06, D-07, D-08 — sign and class assignments)
    - .planning/phases/110-gw-review-history-fixes-v1-20/110-UI-SPEC.md ("Required (correct) logic" code block — exact target state)
    - src/components/squad/GwReviewTab.test.tsx (after Task 1 — confirm which assertions are failing so Task 2 makes them pass)
  </read_first>
  <action>
    In src/components/squad/GwReviewTab.tsx, apply exactly two semantic edits:

    1. Line 171: change the operand order of `benchmarkDiff` from `review.your_score - review.benchmark_score` to `review.benchmark_score - review.your_score`. This is the D-06 sign flip.

    2. In the if/else-if/else chain at lines 174-183, swap the sentiment class assignments so the new sign convention maps to the correct colour:
       - Branch where benchmarkDiff > 0 (dream team beat user): sentiment class becomes 'text-amber-700 dark:text-amber-300' (was green).
       - Branch where benchmarkDiff === 0 (on par): sentiment class stays 'text-green-600 dark:text-green-400' (unchanged).
       - Branch where benchmarkDiff < 0 (user beat dream team): sentiment class becomes 'text-green-600 dark:text-green-400' (was amber).

    Label format strings stay exactly as they are: `+${benchmarkDiff} vs you`, `on par`, `−${Math.abs(benchmarkDiff)} vs you` (the negative branch already uses U+2212 — keep it).

    Do NOT touch any other code in this file. Do NOT modify imports, do NOT touch the StatCard render condition at line 200 (the `benchmark_label !== 'FPL average'` gate), do NOT add new helpers, do NOT refactor the if-chain into a lookup table. The fix is the minimum change required to make the Task 1 RED tests turn GREEN.

    Run `npx vitest run src/components/squad/GwReviewTab.test.tsx` — all 14 baseline tests + 3 new FIX-05 tests + the updated PGW-03 assertions must pass (17+ green, 0 failed). Then run `npx vitest run` once to confirm no other test in the suite regressed.
  </action>
  <verify>
    <automated>npx vitest run src/components/squad/GwReviewTab.test.tsx 2>&amp;1 | tee /tmp/110-01-task2.log; grep -E "Tests +.* [0-9]+ passed" /tmp/110-01-task2.log &amp;&amp; ! grep -E "Tests +.* [0-9]+ failed" /tmp/110-01-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - src/components/squad/GwReviewTab.tsx contains the literal string `review.benchmark_score - review.your_score`
    - src/components/squad/GwReviewTab.tsx does NOT contain the literal string `review.your_score - review.benchmark_score` (the broken expression is gone)
    - In the if-branch where `benchmarkDiff > 0`, the file contains `text-amber-700 dark:text-amber-300` (verify via grep within ~3 lines of `benchmarkDiff > 0`)
    - In the if-branch where `benchmarkDiff < 0` (the `else` branch with `Math.abs`), the file contains `text-green-600 dark:text-green-400`
    - The literal string `−${Math.abs(benchmarkDiff)} vs you` (with U+2212 minus) is preserved in the negative branch
    - `npx vitest run src/components/squad/GwReviewTab.test.tsx` reports 0 failed tests
    - `npx vitest run` (full suite) reports no regressions in any other test file (overall failed count must equal the pre-Phase-110 baseline)
  </acceptance_criteria>
  <done>All FIX-05 tests green; PGW-03 assertions green with corrected sign mapping; no regressions in the full test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → React component | `review.your_score` and `review.benchmark_score` are server-provided integers serialised to JSON; React renders them via template literals into text content (no `dangerouslySetInnerHTML`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01-01 | Tampering | benchmarkDiff sign in GwReviewTab.tsx | mitigate | Two TDD tests pin the sign convention to the canonical D-06 examples (72/122→+50, 95/80→−15); regressions become RED tests, not silent UI errors |
| T-110-01-02 | Information Disclosure | benchmark card text content | accept | Numeric scores are non-sensitive; `your_score` and `benchmark_score` are public FPL data the user already sees on fantasy.premierleague.com |
| T-110-01-03 | Tampering | sentiment class string injection | accept | Class strings are static literals in the component (no user input flows into className); React escapes text content by default |
</threat_model>

<verification>
- After Task 2 completes: `npx vitest run src/components/squad/GwReviewTab.test.tsx` reports 0 failed and ≥17 passed
- After Task 2 completes: `npx vitest run` full-suite passes — no other component or route test regressed
- Manual UI check (deferred to /gsd-uat-phase, not gating execution): open GW Review for a settled GW where dream team > user → verify the benchmark card shows `+N vs you` in amber
</verification>

<success_criteria>
- benchmarkDiff in GwReviewTab.tsx subtracts your_score from benchmark_score (D-06)
- Sentiment classes match the corrected branches (D-07): amber when dream team won, green when user won or on par
- Label format unchanged (D-08): `+N vs you` / `−N vs you` (U+2212) / `on par`
- All Phase 110 FIX-05 unit tests green; PGW-03 existing tests updated and green; no full-suite regression
</success_criteria>

<output>
After completion, create `.planning/phases/110-gw-review-history-fixes-v1-20/110-01-SUMMARY.md` capturing: which lines changed in GwReviewTab.tsx, which existing PGW-03 assertions were updated and how, and the final vitest pass count for both the file and the full suite.
</output>
