---
phase: 132
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useNextDeadline.ts
  - src/lib/hooks/useNextDeadline.test.ts
autonomous: true
requirements: [DL-01]
tags: [react, tanstack-query, hook, fpl-bootstrap]

must_haves:
  truths:
    - "useNextDeadline returns { id, deadline_time } when exactly one bootstrap event has is_next === true"
    - "useNextDeadline returns null when no bootstrap event has is_next === true (off-season case)"
    - "useNextDeadline surfaces an error state (isError === true) when /api/fpl/bootstrap-static/ returns non-OK status"
    - "Hook uses TanStack Query with queryKey ['next-deadline'], staleTime 3_600_000ms, retry 1 (matches useSettledGws conventions)"
  artifacts:
    - path: "src/lib/hooks/useNextDeadline.ts"
      provides: "useNextDeadline TanStack Query hook returning { id: number; deadline_time: string } | null"
      exports: ["useNextDeadline"]
    - path: "src/lib/hooks/useNextDeadline.test.ts"
      provides: "Contract tests for is_next found, is_next absent, non-OK response"
      contains: "describe('useNextDeadline"
  key_links:
    - from: "src/lib/hooks/useNextDeadline.ts"
      to: "/api/fpl/bootstrap-static/"
      via: "fetch inside async fetchNextDeadline"
      pattern: "fetch\\('/api/fpl/bootstrap-static/'\\)"
    - from: "src/lib/hooks/useNextDeadline.ts"
      to: "src/lib/fpl-adapter.ts parseFPLBootstrap"
      via: "import + safeParse"
      pattern: "parseFPLBootstrap\\("
---

<objective>
Phase 132 Plan 01 — Create the `useNextDeadline` TanStack Query hook that fetches the FPL bootstrap, locates the `is_next` event, and exposes `{ id, deadline_time } | null` to client components. This is the data layer for DL-01.

Purpose: A single source of truth for "what is the next FPL gameweek deadline?" that every downstream banner / countdown / notification feature can consume without re-implementing fetch + parse + selection logic.

Output: `src/lib/hooks/useNextDeadline.ts` + `src/lib/hooks/useNextDeadline.test.ts` — direct structural copies of the `useSettledGws` pair adapted for `is_next` selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-deadline-day-banner/132-CONTEXT.md
@.planning/phases/132-deadline-day-banner/132-RESEARCH.md
@.planning/phases/132-deadline-day-banner/132-PATTERNS.md
@src/lib/hooks/useSettledGws.ts
@src/lib/hooks/useSettledGws.test.ts
@src/lib/fpl-adapter.ts

<interfaces>
<!-- Contracts the executor must respect. Extracted directly from codebase. -->

From src/lib/fpl-adapter.ts (DO NOT redefine — import from '@/lib/fpl-adapter'):
```typescript
export const FPLEventSchema = z.object({
  id:            z.number().int(),
  is_current:    z.boolean(),
  is_next:       z.boolean(),
  finished:      z.boolean(),
  deadline_time: z.string(),  // ISO 8601
  data_checked:  z.boolean(),
})

export function parseFPLBootstrap(raw: unknown):
  | { success: true; data: { elements: ..., teams: ..., events: FPLEvent[] } }
  | { success: false; error: ZodError }
```

From src/lib/hooks/useSettledGws.ts (template — same shape, swap fetch + select):
```typescript
async function fetchSettledGws(): Promise<number[]> {
  const res = await fetch('/api/fpl/bootstrap-static/')
  if (!res.ok) {
    const err = new Error(`bootstrap fetch failed: ${res.status}`) as Error & { status?: number }
    err.status = res.status
    throw err
  }
  const raw = await res.json()
  const parsed = parseFPLBootstrap(raw)
  if (!parsed.success) throw new Error('bootstrap parse failed')
  const settled = parsed.data.events.filter((e) => e.finished && e.data_checked).map((e) => e.id)
  return settled.slice(-3)
}

export function useSettledGws() {
  return useQuery<number[]>({
    queryKey: ['settled-gws'],
    queryFn: fetchSettledGws,
    staleTime: 60 * 60 * 1000,
    retry: 1,
  })
}
```

Target hook public type:
```typescript
export type NextDeadline = { id: number; deadline_time: string } | null
export function useNextDeadline(): UseQueryResult<NextDeadline>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write useNextDeadline contract tests (RED)</name>
  <files>src/lib/hooks/useNextDeadline.test.ts</files>
  <read_first>
    - src/lib/hooks/useSettledGws.test.ts (canonical structure — copy the wrapper factory, fetch mock pattern, beforeEach/afterEach hooks, EventInput interface idea)
    - src/lib/hooks/useSettledGws.ts (to know what the analog hook exports — your test imports `useNextDeadline` instead)
    - .planning/phases/132-deadline-day-banner/132-CONTEXT.md (D-01, D-02 — confirms behaviour under test)
    - .planning/phases/132-deadline-day-banner/132-RESEARCH.md (Validation Architecture section — Phase Requirements → Test Map for DL-01 lists exact required cases)
  </read_first>
  <behavior>
    - Test 1: When bootstrap events contain exactly one event with `is_next: true`, the hook resolves with `{ id: <that event's id>, deadline_time: <that event's deadline_time> }`.
    - Test 2: When no bootstrap event has `is_next: true`, the hook resolves with `null` (off-season / no upcoming GW).
    - Test 3: When `/api/fpl/bootstrap-static/` returns a non-OK status (e.g. 502), the hook reaches `isError === true` and `data` is `undefined`.
    - Test 4 (optional but recommended): When multiple events exist but only one has `is_next: true` (the realistic FPL shape with past + current + next events), the hook still selects the `is_next` one — not the first event, not `is_current`.
  </behavior>
  <action>
    Create `src/lib/hooks/useNextDeadline.test.ts` using the exact structure of `useSettledGws.test.ts` (the analog from .planning/phases/132-deadline-day-banner/132-PATTERNS.md "useNextDeadline.test.ts" section).

    Required elements (copy verbatim from the analog where applicable):
    - First line: `// @vitest-environment jsdom`
    - Imports from `vitest`: `describe, it, expect, vi, beforeEach, afterEach`
    - Imports from `@testing-library/react`: `renderHook, waitFor`
    - Imports from `@tanstack/react-query`: `QueryClient, QueryClientProvider`
    - `import type { ReactNode } from 'react'` and `import { createElement } from 'react'`
    - Import `useNextDeadline` from `./useNextDeadline`
    - Reuse the `makeWrapper(retries = 0)` factory exactly as in the analog (QueryClient with `retry: retries, gcTime: 0`, returns QueryClientProvider via `createElement`).
    - Reuse the `beforeEach(() => {})` + `afterEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks() })` cleanup pair.

    Bootstrap payload builder: define a `bootstrapPayload(events)` helper that returns an object with `elements: []`, `teams: []`, `events: events.map(e => ({ id, is_current: false, is_next: false, finished: false, data_checked: false, deadline_time: '2026-01-01T11:00:00Z', ...e }))`. The spread of `...e` last lets each test override only the fields it cares about (`id`, `is_next`, `deadline_time`).

    Test cases (use `describe('useNextDeadline — Phase 132 DL-01', () => { ... })`):
    1. `it('returns null when no event has is_next === true')` — payload with three events all `is_next: false`. Mock fetch via `vi.stubGlobal('fetch', fetchMock)` where `fetchMock` returns `new Response(JSON.stringify(payload), { status: 200 })`. Render hook, `await waitFor(() => expect(result.current.isSuccess).toBe(true))`, assert `expect(result.current.data).toBeNull()`.
    2. `it('returns { id, deadline_time } when exactly one event has is_next === true')` — payload with events `[{ id: 32, is_next: false, deadline_time: '2026-01-15T11:00:00Z' }, { id: 33, is_next: true, deadline_time: '2026-01-22T11:00:00Z' }]`. Assert `expect(result.current.data).toEqual({ id: 33, deadline_time: '2026-01-22T11:00:00Z' })`.
    3. `it('selects the is_next event even when surrounded by is_current and finished events')` — payload `[{ id: 30, finished: true }, { id: 31, is_current: true }, { id: 32, is_next: true, deadline_time: '2026-02-01T11:00:00Z' }, { id: 33 }]`. Assert `expect(result.current.data).toEqual({ id: 32, deadline_time: '2026-02-01T11:00:00Z' })`.
    4. `it('reaches isError when bootstrap returns non-OK status')` — fetch mock returns `new Response('boom', { status: 502 })`. Use `await waitFor(() => expect(result.current.isError).toBe(true), { timeout: 5000 })` to allow the `retry: 1` cycle. Assert `expect(result.current.data).toBeUndefined()`.

    The test file must FAIL when run before Task 2 (no implementation file yet). That is the RED state.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx vitest run src/lib/hooks/useNextDeadline.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/hooks/useNextDeadline.test.ts` exists.
    - First line of file is exactly `// @vitest-environment jsdom`.
    - File contains the string `from './useNextDeadline'`.
    - File contains at minimum 3 `it(` blocks covering: null case, is_next-found case, non-OK status case (4th `is_current` discrimination test is recommended but not strictly required for acceptance).
    - File contains the string `vi.stubGlobal('fetch'` (proves fetch mocking is in place).
    - File contains the string `queryKey` NOT — the queryKey check belongs to the implementation; tests only assert observable hook outputs.
    - Running `npx vitest run src/lib/hooks/useNextDeadline.test.ts` exits non-zero with at least one failure attributable to "Cannot find module './useNextDeadline'" or "useNextDeadline is not a function" (RED state — implementation file not yet created).
  </acceptance_criteria>
  <done>Test file created, all tests fail because implementation does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useNextDeadline hook (GREEN)</name>
  <files>src/lib/hooks/useNextDeadline.ts</files>
  <read_first>
    - src/lib/hooks/useSettledGws.ts (template — copy structure exactly, swap fetcher body + queryKey + return type)
    - src/lib/fpl-adapter.ts (confirms `parseFPLBootstrap` signature and `FPLEventSchema` shape — DO NOT redefine; import from `@/lib/fpl-adapter`)
    - src/lib/hooks/useNextDeadline.test.ts (the contract this implementation must satisfy — read it after Task 1 commits)
    - .planning/phases/132-deadline-day-banner/132-CONTEXT.md (D-01: queryKey `['next-deadline']`, staleTime `60 * 60 * 1000`; D-02: `events.find(e => e.is_next) ?? null`)
  </read_first>
  <action>
    Create `src/lib/hooks/useNextDeadline.ts`. Structure mirrors `useSettledGws.ts` line-for-line with these specific differences:

    - Public type alias: export `type NextDeadline = { id: number; deadline_time: string } | null` (or inline the type — exporting the alias is cleaner and helps the consumer in Plan 02).
    - Async fetcher: name it `fetchNextDeadline`. Return type: `Promise<NextDeadline>`.
    - Inside `fetchNextDeadline`:
      - `const res = await fetch('/api/fpl/bootstrap-static/')` — exact same path as `useSettledGws`.
      - Error shape on `!res.ok` must be identical to useSettledGws: `const err = new Error(\`bootstrap fetch failed: ${res.status}\`) as Error & { status?: number }; err.status = res.status; throw err`.
      - `const raw = await res.json(); const parsed = parseFPLBootstrap(raw); if (!parsed.success) throw new Error('bootstrap parse failed')`.
      - Selection per D-02: `const next = parsed.data.events.find((e) => e.is_next) ?? null`.
      - If `next === null` → `return null` (do NOT throw — null is a valid resolved state per D-10).
      - Otherwise: `return { id: next.id, deadline_time: next.deadline_time }`.
    - Public hook: `export function useNextDeadline()` returns `useQuery<NextDeadline>({ queryKey: ['next-deadline'], queryFn: fetchNextDeadline, staleTime: 60 * 60 * 1000, retry: 1 })`.
    - Add a JSDoc block above the fetcher referencing Phase 132 DL-01 and CONTEXT D-01/D-02 (mirror the JSDoc style on useSettledGws.ts lines 4-11).

    Do NOT add `'use client'` to this file — hooks are imported by client components but the hook file itself does not need the directive (the analog useSettledGws.ts has none).

    Do NOT add a NaN guard on `deadline_time` in the hook — the security note in RESEARCH.md §Security Domain places that guard on the consumer (the banner component). The hook returns the raw ISO string.

    After implementation, the test file from Task 1 must pass with 100% green.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx vitest run src/lib/hooks/useNextDeadline.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/hooks/useNextDeadline.ts` exists.
    - File contains exports: `export function useNextDeadline` (assert by grep).
    - File contains the literal string `queryKey: ['next-deadline']`.
    - File contains the literal string `staleTime: 60 * 60 * 1000`.
    - File contains the literal string `retry: 1`.
    - File contains the literal string `events.find((e) => e.is_next)` (D-02 selection).
    - File contains the literal string `/api/fpl/bootstrap-static/` (D-01 endpoint).
    - File imports `parseFPLBootstrap` from `@/lib/fpl-adapter` (no redefined schema).
    - File does NOT contain the string `'use client'` (matches useSettledGws.ts).
    - `npx vitest run src/lib/hooks/useNextDeadline.test.ts` exits 0 with all tests passing (GREEN state).
    - `npx tsc --noEmit` introduces no new errors attributable to `useNextDeadline.ts` (type-clean against the rest of the codebase).
  </acceptance_criteria>
  <done>Implementation file created, all useNextDeadline.test.ts tests pass green, type-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/fpl/bootstrap-static/ proxy | Same-origin fetch; the proxy in turn calls the public FPL API. Response is untrusted JSON until validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Tampering | bootstrap JSON shape | mitigate | `parseFPLBootstrap` (`FPLEventSchema`) Zod-validates `is_next: boolean` and `deadline_time: string` before selection. On parse failure the hook throws and the consumer sees `isError`. |
| T-132-02 | Tampering | malformed `deadline_time` string (e.g. empty, non-ISO) | accept (hook layer) — mitigate at banner | The hook returns the raw string per D-02 / D-10. The consumer (Plan 02) is responsible for `new Date(deadline_time)` + `isNaN(d.getTime())` guard. Documented in RESEARCH.md Security Domain. |
| T-132-03 | Denial of Service | repeated bootstrap fetches | mitigate | TanStack Query `staleTime: 60 * 60 * 1000` deduplicates and caches for 1 hour; `retry: 1` caps retry storms. |
</threat_model>

<verification>
- `npx vitest run src/lib/hooks/useNextDeadline.test.ts` exits 0 with all assertions green.
- `npx tsc --noEmit` reports no new errors involving `src/lib/hooks/useNextDeadline.ts`.
- `npm test` full suite remains green (no regressions in surrounding hooks).
</verification>

<success_criteria>
- `useNextDeadline` hook can be imported and used by Plan 02's `DeadlineBanner` with the signature `const { data, isLoading, isError } = useNextDeadline()` where `data` is `{ id: number; deadline_time: string } | null | undefined`.
- All DL-01 hook-layer truths (returns the next event, returns null off-season, surfaces errors) verified by passing unit tests.
- No new dependencies added to package.json; no new schemas defined; pattern matches `useSettledGws` for future-reader recognition.
</success_criteria>

<output>
After completion, create `.planning/phases/132-deadline-day-banner/132-01-SUMMARY.md` capturing:
- Files created (useNextDeadline.ts, useNextDeadline.test.ts) with line counts
- Test count and status (e.g. "4 passed, 0 failed")
- Confirmation that the hook signature is `() => UseQueryResult<{ id: number; deadline_time: string } | null>`
- Any deviations from the plan (expected: none)
- Patterns established for downstream consumers
</output>
