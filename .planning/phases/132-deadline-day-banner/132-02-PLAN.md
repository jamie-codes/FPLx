---
phase: 132
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - src/components/DeadlineBanner.tsx
  - src/components/DeadlineBanner.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [DL-01, DL-02, DL-03]
tags: [react, client-component, tailwind, localstorage, countdown, accessibility]

must_haves:
  truths:
    - "User sees a banner reading 'GW{id} deadline in {hours}h {minutes}m' (or '{minutes}m' when hours === 0) whenever useNextDeadline returns a future deadline"
    - "Banner uses zinc colour scheme when msRemaining >= 24h, amber when 2h <= msRemaining < 24h, red + sticky top-0 z-50 when msRemaining < 2h"
    - "Countdown re-renders every 60 seconds without a page reload (state escalates automatically when crossing 24h or 2h thresholds)"
    - "User can dismiss the banner with the × button; banner immediately renders null"
    - "Dismiss state is keyed per GW (deadline-dismissed:GW{id}); when a new GW becomes is_next the dismissed flag no longer matches and the banner reappears"
    - "Banner renders null when useNextDeadline returns null, when msRemaining <= 0, when deadline_time parses to NaN, and when localStorage shows dismissed for the current GW"
    - "All localStorage reads and writes are wrapped in try/catch so SSR and private browsing do not throw"
    - "Banner is injected immediately above the sticky nav wrapper div in src/app/page.tsx so the red sticky state visually layers above the section nav"
  artifacts:
    - path: "src/components/DeadlineBanner.tsx"
      provides: "DeadlineBanner client component with countdown, urgency states, and dismiss"
      exports: ["DeadlineBanner"]
      contains: "'use client'"
    - path: "src/components/DeadlineBanner.test.tsx"
      provides: "Vitest + Testing Library coverage for DL-01/DL-02/DL-03"
      contains: "describe('DeadlineBanner"
    - path: "src/app/page.tsx"
      provides: "Layout injection of <DeadlineBanner />"
      contains: "<DeadlineBanner />"
  key_links:
    - from: "src/components/DeadlineBanner.tsx"
      to: "src/lib/hooks/useNextDeadline.ts"
      via: "import + invocation"
      pattern: "useNextDeadline\\(\\)"
    - from: "src/components/DeadlineBanner.tsx"
      to: "browser localStorage"
      via: "try/catch-wrapped getItem/setItem with key 'deadline-dismissed:GW${id}'"
      pattern: "deadline-dismissed:GW"
    - from: "src/app/page.tsx"
      to: "src/components/DeadlineBanner.tsx"
      via: "JSX render above sticky nav wrapper div"
      pattern: "<DeadlineBanner /"
---

<objective>
Phase 132 Plan 02 — Build the `DeadlineBanner` client component that consumes `useNextDeadline` (Plan 01), runs a 60-second countdown tick, escalates colour + sticky positioning through three urgency states, supports per-GW dismiss via localStorage, and inject it above the sticky nav wrapper in `src/app/page.tsx`.

Purpose: Deliver the user-facing surface for DL-01 (countdown display), DL-02 (3-state urgency), and DL-03 (dismiss). This is the assembly plan — every primitive (hook, timer pattern, colour map, localStorage pattern, layout slot) already exists; the work is composition.

Output: `src/components/DeadlineBanner.tsx` + `src/components/DeadlineBanner.test.tsx` (new files) and one modified line range in `src/app/page.tsx` (import + one JSX element).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/132-deadline-day-banner/132-CONTEXT.md
@.planning/phases/132-deadline-day-banner/132-RESEARCH.md
@.planning/phases/132-deadline-day-banner/132-PATTERNS.md
@.planning/phases/132-deadline-day-banner/132-UI-SPEC.md
@.planning/phases/132-deadline-day-banner/132-01-SUMMARY.md
@src/lib/hooks/useNextDeadline.ts
@src/components/LastUpdated.tsx
@src/components/LastUpdated.test.tsx
@src/app/page.tsx

<interfaces>
<!-- Contracts the executor must respect. Extracted directly from codebase + Plan 01 output. -->

From src/lib/hooks/useNextDeadline.ts (created by Plan 01):
```typescript
export type NextDeadline = { id: number; deadline_time: string } | null
export function useNextDeadline(): UseQueryResult<NextDeadline>
```

From src/components/LastUpdated.tsx (canonical 'use client' + setInterval pattern):
```typescript
'use client'
import { useEffect, useState } from 'react'

useEffect(() => {
  if (!data?.last_updated) return
  setRelativeTime(formatRelativeTime(data.last_updated))
  const id = setInterval(() => {
    setRelativeTime(formatRelativeTime(data.last_updated))
  }, 30_000)
  return () => clearInterval(id)
}, [data?.last_updated])

if (!data) return null
```

From src/app/page.tsx lines 127-145 (canonical localStorage try/catch + lazy init):
```typescript
// Lazy initialiser:
const [teamId, setTeamId] = useState<string>(() => {
  try { return localStorage.getItem('fpl_team_id') ?? '' } catch { return '' }
})

// Read guard:
try {
  if (localStorage.getItem(key) !== null) return
  // ...
  localStorage.setItem(key, '1')
} catch { /* SSR / private browsing — skip silently */ }
```

From src/app/page.tsx line 200 (sticky nav wrapper — banner mounts immediately above this div):
```tsx
{/* Sticky nav wrapper — section tabs + sub-tabs (D-07, D-08) */}
<div className="sticky top-0 z-40 bg-surface/95 backdrop-blur-sm border-b border-border -mx-4 px-4">
```

Required urgency class map (from UI-SPEC.md §Color + RESEARCH.md Pattern 4):
```typescript
type UrgencyState = 'neutral' | 'amber' | 'red'

const URGENCY_CLASSES: Record<UrgencyState, string> = {
  neutral: 'bg-zinc-50 dark:bg-zinc-900 text-zinc-600 dark:text-zinc-400 border-zinc-200 dark:border-zinc-700',
  amber:   'bg-amber-50 dark:bg-amber-950 text-amber-700 dark:text-amber-300 border-amber-300 dark:border-amber-700',
  red:     'bg-red-50 dark:bg-red-950 text-red-700 dark:text-red-300 border-red-300 dark:border-red-700',
}

const STICKY_CLASSES: Record<UrgencyState, string> = {
  neutral: '',
  amber:   '',
  red:     'sticky top-0 z-50',
}
```

Threshold constants:
- 24h = 24 * 60 * 60 * 1000 = 86_400_000
- 2h  = 2 * 60 * 60 * 1000  = 7_200_000
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write DeadlineBanner test suite (RED)</name>
  <files>src/components/DeadlineBanner.test.tsx</files>
  <read_first>
    - src/components/LastUpdated.test.tsx (analog — copy the `vi.useFakeTimers()` + `vi.setSystemTime()` + `vi.advanceTimersByTime(60_000)` pattern, the `vi.mock('@/lib/hooks/...')` pattern, the `getDisplaySpan` helper, the `clears interval on unmount` test)
    - src/lib/hooks/useNextDeadline.ts (the module to mock — confirm the export name and return shape)
    - .planning/phases/132-deadline-day-banner/132-CONTEXT.md (D-03 through D-10 — full behaviour spec)
    - .planning/phases/132-deadline-day-banner/132-UI-SPEC.md (§Copywriting Contract for exact label strings; §Color for class assertions; §Accessibility for `role="status"`, `aria-live="polite"`, `aria-label="Dismiss deadline banner"`)
    - .planning/phases/132-deadline-day-banner/132-RESEARCH.md (§Validation Architecture lists the exact required test cases per requirement)
  </read_first>
  <behavior>
    DL-01 cases:
    - Test A1: Hook returns null → component renders `null` (off-season). Assert `container.firstChild === null`.
    - Test A2: Hook returns `{ id: 32, deadline_time: <14h 22m in future ISO> }` at NOW → banner text contains `GW32 deadline in 14h 22m`.
    - Test A3: Hook returns valid data but parsed Date is NaN (e.g. `deadline_time: 'not-an-iso'`) → component renders null (NaN guard from RESEARCH §Security).
    - Test A4: Hook returns deadline_time in the past (e.g. NOW − 1 hour) → component renders null (D-04: "Once deadline_time is in the past, banner hides").

    DL-02 cases (urgency classes — `>= 24h`, `>= 2h && < 24h`, `< 2h`):
    - Test B1: `msRemaining = 25h` → root element className contains `bg-zinc-50` (or `bg-zinc-100` — match whichever the implementation chose from the URGENCY_CLASSES map). Does NOT contain `bg-amber-50` or `bg-red-50`. Does NOT contain `sticky`.
    - Test B2: `msRemaining = 12h` → className contains `bg-amber-50` and `text-amber-700`. Does NOT contain `sticky top-0`.
    - Test B3: `msRemaining = 1h` → className contains `bg-red-50`, `text-red-700`, `sticky`, `top-0`, `z-50`.
    - Test B4: Set initial deadline 2h 30m in future (amber). Advance system time by 1 hour (`vi.setSystemTime(NOW + 3_600_000)` + `vi.advanceTimersByTime(60_000)` within `act()`). Re-query the banner — className should now contain `bg-red-50` (state auto-escalates on tick without remount). This proves D-06.

    DL-03 cases (dismiss):
    - Test C1: When `localStorage.getItem('deadline-dismissed:GW32')` returns `'1'` on mount, component renders null even with valid hook data.
    - Test C2: Render banner with valid data and `localStorage` empty. Banner visible. User clicks the × button (`getByRole('button', { name: /dismiss deadline banner/i })`). After click: `container.firstChild === null` AND `localStorage.setItem` was called with `'deadline-dismissed:GW32'` and `'1'`.
    - Test C3: `localStorage.getItem` throws on read → component still renders (no crash; try/catch returns `false`). Use `Object.defineProperty(window, 'localStorage', { value: { getItem: () => { throw new Error('blocked') }, setItem: vi.fn() } })` or `vi.spyOn(Storage.prototype, 'getItem').mockImplementation(() => { throw new Error('blocked') })`.
    - Test C4: `localStorage.setItem` throws on dismiss click → component still hides (sets local React state) and does not crash.

    Lifecycle / accessibility:
    - Test D1: `clearInterval` is called on unmount (mirror LastUpdated.test.tsx "clears interval on unmount").
    - Test D2: The banner root element has `role="status"` and includes `aria-live="polite"` somewhere on the visible countdown text (either on the root or on the inner span — accept either per UI-SPEC §Accessibility).
    - Test D3: The dismiss button has `aria-label="Dismiss deadline banner"`.

    Format edge case:
    - Test E1: `msRemaining = 45 minutes` (well under 1h, so hours = 0) → banner text matches `/GW\d+ deadline in 45m/` (no `0h` prefix per UI-SPEC §Copywriting Contract). Note: the alternative "0h 45m" is acceptable per CONTEXT.md D-03; the UI-SPEC has chosen "45m" — assert against UI-SPEC's chosen form.
  </behavior>
  <action>
    Create `src/components/DeadlineBanner.test.tsx`. Use the structural template from `src/components/LastUpdated.test.tsx`.

    Required scaffolding:
    - Imports: `describe, it, expect, vi, beforeEach, afterEach` from `vitest`; `render, screen, act, fireEvent` (or `userEvent` if preferred) from `@testing-library/react`; the `DeadlineBanner` from `./DeadlineBanner`.
    - Mock the hook: `vi.mock('@/lib/hooks/useNextDeadline', () => ({ useNextDeadline: vi.fn() }))`, then `import { useNextDeadline } from '@/lib/hooks/useNextDeadline'` and `const mockedUseNextDeadline = vi.mocked(useNextDeadline)`.
    - Constants: `const NOW = new Date('2026-08-15T10:00:00Z').getTime()`. Helper `const isoMsFromNow = (ms: number) => new Date(NOW + ms).toISOString()`.
    - `beforeEach`: `vi.useFakeTimers(); vi.setSystemTime(NOW); localStorage.clear()` (jsdom provides a real Storage instance in the vitest setup — see vitest.setup.ts; if not, stub with `vi.spyOn(Storage.prototype, ...)` as documented in PATTERNS).
    - `afterEach`: `vi.useRealTimers(); vi.restoreAllMocks(); vi.unstubAllGlobals()`.

    Implement every Test A1–E1 listed in `<behavior>` above. Group into `describe` blocks: `'DeadlineBanner — DL-01 display'`, `'DeadlineBanner — DL-02 urgency states'`, `'DeadlineBanner — DL-03 dismiss'`, `'DeadlineBanner — lifecycle + a11y'`.

    For each `mockedUseNextDeadline.mockReturnValue(...)` call use the cast `as any` (the analog uses the same pattern in LastUpdated.test.tsx line 81 and surrounding).

    Helper for class assertions: write a `getRoot(container: HTMLElement)` helper that returns `container.firstChild as HTMLElement` (the banner root with the className being asserted).

    File must FAIL initially (DeadlineBanner.tsx does not yet exist). That is RED.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx vitest run src/components/DeadlineBanner.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/DeadlineBanner.test.tsx` exists.
    - File contains the literal string `vi.mock('@/lib/hooks/useNextDeadline'` (hook is mocked).
    - File contains the literal string `vi.useFakeTimers()` (timer control).
    - File contains the literal string `vi.advanceTimersByTime(60_000)` at least once (60s tick assertion).
    - File contains the literal string `deadline-dismissed:GW` (dismiss key assertion).
    - File contains the literal string `aria-label` (a11y assertion on dismiss button).
    - File contains the literal string `role="status"` OR an assertion `getByRole('status'` (a11y assertion on root).
    - File contains the literal string `clearInterval` (unmount cleanup assertion).
    - File contains the literal string `bg-amber-50` AND `bg-red-50` AND `bg-zinc-` (three urgency state assertions).
    - File contains the literal string `sticky` and `z-50` (red state position assertion).
    - File contains at least 10 `it(` blocks.
    - Running `npx vitest run src/components/DeadlineBanner.test.tsx` exits non-zero with errors attributable to "Cannot find module './DeadlineBanner'" or "DeadlineBanner is not a function/component" (RED).
  </acceptance_criteria>
  <done>Test file authored; suite fails because implementation does not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DeadlineBanner component (GREEN)</name>
  <files>src/components/DeadlineBanner.tsx</files>
  <read_first>
    - src/components/LastUpdated.tsx (canonical `'use client'`, `useEffect` interval, `clearInterval` cleanup, `if (!data) return null` early-return)
    - src/lib/hooks/useNextDeadline.ts (Plan 01 output — the hook this component consumes)
    - src/app/page.tsx lines 127-145 (canonical lazy-init useState with localStorage try/catch)
    - src/components/DeadlineBanner.test.tsx (the contract this implementation must satisfy)
    - .planning/phases/132-deadline-day-banner/132-CONTEXT.md (D-03 through D-10 — full behaviour spec)
    - .planning/phases/132-deadline-day-banner/132-UI-SPEC.md (§Component Structure, §Color, §Copywriting, §Accessibility)
    - .planning/phases/132-deadline-day-banner/132-RESEARCH.md (§Common Pitfalls — stale closure on deadline_time, hydration, sticky vs fixed; §Security Domain — NaN guard)
  </read_first>
  <action>
    Create `src/components/DeadlineBanner.tsx`. The file must satisfy every test from Task 1 and every Decision D-03..D-10 + UI-SPEC contract.

    Required structural elements:

    1. **Directive + imports**
       - Line 1: `'use client'`
       - Imports: `useEffect, useState` from `'react'`; `useNextDeadline` from `'@/lib/hooks/useNextDeadline'`.

    2. **Internal types + constants** (module scope, not re-declared per render)
       - `type UrgencyState = 'neutral' | 'amber' | 'red'`
       - `const ONE_HOUR_MS = 60 * 60 * 1000`
       - `const TWENTY_FOUR_HOURS_MS = 24 * ONE_HOUR_MS` (= 86_400_000)
       - `const TWO_HOURS_MS = 2 * ONE_HOUR_MS` (= 7_200_000)
       - `const TICK_MS = 60_000`
       - `URGENCY_CLASSES` and `STICKY_CLASSES` Record maps — exact values from the `<interfaces>` block above. Do not invent new colours; the assertions in Task 1 check for these substrings.

    3. **Pure helpers** (module scope, exported NOT required but allowed for unit testing)
       - `function computeUrgency(msRemaining: number): UrgencyState` — `>= TWENTY_FOUR_HOURS_MS → 'neutral'`; `>= TWO_HOURS_MS → 'amber'`; else `'red'`. (Caller is responsible for the `<= 0` short-circuit before calling.)
       - `function formatCountdown(msRemaining: number): string` — `const totalMinutes = Math.floor(msRemaining / 60_000); const hours = Math.floor(totalMinutes / 60); const minutes = totalMinutes % 60; return hours === 0 ? \`${minutes}m\` : \`${hours}h ${minutes}m\`` (UI-SPEC chose to drop the `0h` prefix when hours === 0).

    4. **Component shell** `export function DeadlineBanner()`:
       - `const { data } = useNextDeadline()`.
       - Compute `const id = data?.id ?? null` and `const deadlineTime = data?.deadline_time ?? null`.
       - NaN guard: `const deadlineMs = deadlineTime ? new Date(deadlineTime).getTime() : NaN`. If `Number.isNaN(deadlineMs)` → set a flag (e.g. `const invalidDeadline = !deadlineTime || Number.isNaN(deadlineMs)`).
       - Dismissed state — **lazy initialiser** using the per-GW key (mirrors page.tsx lines 143-145). Use `useState<boolean>(() => { if (id === null) return false; try { return localStorage.getItem(\`deadline-dismissed:GW${id}\`) !== null } catch { return false } })`. **Important:** the lazy init runs only on first mount. If `id` changes after mount (the user keeps the tab open across GW boundary), you must also re-evaluate via the effect described next.
       - Effect to reset `dismissed` when `id` changes: `useEffect(() => { if (id === null) { setDismissed(false); return } try { setDismissed(localStorage.getItem(\`deadline-dismissed:GW${id}\`) !== null) } catch { setDismissed(false) } }, [id])`.
       - `const [msRemaining, setMsRemaining] = useState<number>(() => invalidDeadline ? 0 : deadlineMs - Date.now())`.
       - Interval effect — depend ONLY on `deadlineTime` (not on `dismissed` or `msRemaining`) per Pitfall 1 in RESEARCH:
         ```
         useEffect(() => {
           if (!deadlineTime) return
           const tick = () => {
             const ms = new Date(deadlineTime).getTime() - Date.now()
             setMsRemaining(ms)
           }
           tick()
           const intervalId = setInterval(tick, TICK_MS)
           return () => clearInterval(intervalId)
         }, [deadlineTime])
         ```

    5. **Render gates** (in order — every null path must be hit):
       - If `data === null` or `data === undefined` → return `null` (off-season / loading / error).
       - If `id === null` → return `null` (defensive — same as above).
       - If `invalidDeadline` → return `null` (NaN guard per RESEARCH Security Domain).
       - If `msRemaining <= 0` → return `null` (D-04 past deadline).
       - If `dismissed === true` → return `null` (D-03).
       - Otherwise compute `const urgency = computeUrgency(msRemaining)` and render.

    6. **JSX structure** (matches UI-SPEC §Component Structure):
       ```tsx
       <div
         role="status"
         aria-live="polite"
         className={`border-b px-4 py-2 -mx-4 flex items-center gap-2 text-sm font-medium ${URGENCY_CLASSES[urgency]} ${STICKY_CLASSES[urgency]}`}
       >
         <span>GW{id} deadline in {formatCountdown(msRemaining)}</span>
         <button
           type="button"
           onClick={handleDismiss}
           aria-label="Dismiss deadline banner"
           className="ml-auto min-h-[44px] px-2 py-1 opacity-60 hover:opacity-100 transition-opacity"
         >
           ×
         </button>
       </div>
       ```
       - The `-mx-4 px-4` full-bleed matches the sticky nav wrapper pattern at page.tsx line 200.
       - The `ml-auto` right-aligns the dismiss button per UI-SPEC §Layout.
       - The `min-h-[44px]` ensures a 44px touch target per UI-SPEC §Spacing.

    7. **Dismiss handler**:
       ```
       function handleDismiss() {
         if (id !== null) {
           try { localStorage.setItem(`deadline-dismissed:GW${id}`, '1') } catch { /* private browsing — still hide locally */ }
         }
         setDismissed(true)
       }
       ```

    Forbidden:
    - Do NOT redefine `FPLEventSchema` or any bootstrap types — the hook owns that.
    - Do NOT use `position: fixed` for the red state — must be `sticky top-0` (Pitfall 4).
    - Do NOT depend `useEffect` on `dismissed` or `msRemaining` for the interval (Pitfall 1).
    - Do NOT use arbitrary z-index values like `z-[45]` — use `z-50` per UI-SPEC.
    - Do NOT add any animation library — colour swap is instant per UI-SPEC.

    After implementation, every test in `DeadlineBanner.test.tsx` must pass.
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx vitest run src/components/DeadlineBanner.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/DeadlineBanner.tsx` exists.
    - First non-blank line is exactly `'use client'`.
    - File exports `DeadlineBanner` (assert via `grep -E "export (function|const) DeadlineBanner"`).
    - File imports `useNextDeadline` from `@/lib/hooks/useNextDeadline`.
    - File contains the literal string `setInterval(` AND `clearInterval(`.
    - File contains the literal string `deadline-dismissed:GW` (per-GW key).
    - File contains all three literal strings `bg-zinc-`, `bg-amber-50`, `bg-red-50` (three urgency states present).
    - File contains the literal string `sticky top-0 z-50` (red state position).
    - File contains the literal string `role="status"` and `aria-live="polite"`.
    - File contains the literal string `aria-label="Dismiss deadline banner"`.
    - File contains the literal string `Number.isNaN(` OR `isNaN(` applied to the parsed date (NaN guard).
    - File does NOT contain the literal string `position: fixed` or `className="fixed top-0` (no `fixed` positioning for the banner).
    - File does NOT contain the literal string `FPLEventSchema` (no schema duplication).
    - The interval useEffect's dependency array contains `deadlineTime` (or `data?.deadline_time`) — verify by reading the file; the array MUST NOT include `dismissed`, `msRemaining`, or `id`.
    - `npx vitest run src/components/DeadlineBanner.test.tsx` exits 0 with all tests passing (GREEN).
    - `npx tsc --noEmit` introduces no new errors attributable to `DeadlineBanner.tsx`.
  </acceptance_criteria>
  <done>Component implemented; DeadlineBanner.test.tsx fully green; type-check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire DeadlineBanner into page.tsx above the sticky nav wrapper</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (full file — find the exact location of the sticky nav wrapper div at approximately line 200 and the existing import block at the top)
    - src/components/DeadlineBanner.tsx (confirm the export name and that no props are required)
    - .planning/phases/132-deadline-day-banner/132-CONTEXT.md (canonical_refs section — "banner renders above the section nav … NOT a SubTab")
    - .planning/phases/132-deadline-day-banner/132-RESEARCH.md (§Architecture — "Place `<DeadlineBanner />` *immediately before* the sticky nav wrapper `<div className=\"sticky top-0 z-40 ...\">`")
    - .planning/phases/132-deadline-day-banner/132-PATTERNS.md (`src/app/page.tsx` section — exact injection point + import placement)
  </read_first>
  <action>
    Make TWO targeted edits to `src/app/page.tsx`:

    **Edit 1 — Import:** Add a new import line immediately after the existing `import { useSettledGws } from '@/lib/hooks/useSettledGws'` line (currently line 38 per the latest read). The new line:

        import { DeadlineBanner } from '@/components/DeadlineBanner'

    **Edit 2 — Render injection:** Locate the comment + div pair currently at approximately lines 199-200:

        {/* Sticky nav wrapper — section tabs + sub-tabs (D-07, D-08) */}
        <div className="sticky top-0 z-40 bg-surface/95 backdrop-blur-sm border-b border-border -mx-4 px-4">

    Insert `<DeadlineBanner />` on its own line IMMEDIATELY BEFORE the `{/* Sticky nav wrapper` comment (so the banner is a sibling above the wrapper, NOT a child inside it). The result should read:

        <DeadlineBanner />
        {/* Sticky nav wrapper — section tabs + sub-tabs (D-07, D-08) */}
        <div className="sticky top-0 z-40 bg-surface/95 backdrop-blur-sm border-b border-border -mx-4 px-4">

    Rationale (do not include in the edit): placing the banner *outside* the nav wrapper means the red state's `sticky top-0 z-50` is in the parent stacking context and naturally renders above the nav's `z-40` wrapper when both are sticky.

    Do NOT:
    - Modify any other line in page.tsx.
    - Pass props to `<DeadlineBanner />` (none required; component is self-fetching via useNextDeadline).
    - Wrap the banner in another div (the banner already manages its own width with `-mx-4 px-4`).
    - Add the banner inside the existing sticky nav wrapper div.
    - Add the banner inside the mobile-only `<MobileNav />` JSX further down.

    After editing, run a build to confirm no syntax / import errors:
  </action>
  <verify>
    <automated>cd /c/Users/jamie/fplx && npx tsc --noEmit 2>&1 | grep -E "page\.tsx|DeadlineBanner" | head -20 ; echo "---" ; grep -n "DeadlineBanner" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contains exactly one new import line: `import { DeadlineBanner } from '@/components/DeadlineBanner'` (assert with grep — exactly 1 match for `from '@/components/DeadlineBanner'`).
    - `src/app/page.tsx` contains exactly one JSX usage: `<DeadlineBanner />` (assert with grep — exactly 1 match).
    - The line containing `<DeadlineBanner />` appears BEFORE the line containing `Sticky nav wrapper — section tabs` in `src/app/page.tsx` (assert by `grep -n` and comparing line numbers — banner line number must be less than wrapper-comment line number).
    - `src/app/page.tsx` does NOT contain `<DeadlineBanner ` with any props (e.g. no `gwId=`, no `className=`).
    - `npx tsc --noEmit` reports no new errors involving `src/app/page.tsx` or `DeadlineBanner` (output of the grep in verify must be empty).
    - `npm test` full suite remains green (no regressions in any existing page.tsx-related tests).
    - `npm run build` succeeds (Next.js bundles the page with the banner included).
  </acceptance_criteria>
  <done>page.tsx imports DeadlineBanner and renders it above the sticky nav wrapper; full test suite green; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localStorage | Storage is per-origin user-controlled; values can be deleted, modified, or absent. All reads/writes must tolerate failure (private browsing throws on access). |
| Browser → useNextDeadline data | Data already validated by `parseFPLBootstrap` in Plan 01, but `deadline_time` is an opaque string; the banner is responsible for the `Number.isNaN(new Date(deadline_time).getTime())` guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-04 | Tampering | malformed `deadline_time` string surfacing from a future FPL API change | mitigate | Render gate: `if (Number.isNaN(deadlineMs)) return null`. Banner disappears silently rather than rendering `NaN` or crashing. |
| T-132-05 | Tampering | localStorage value tampered to non-`'1'` value (e.g. user sets `'true'`) | accept | Read uses `!== null`, so ANY stored value hides the banner for the GW. Worst case: user dismisses their own banner — already the intended outcome. |
| T-132-06 | Denial of Service | localStorage throws on getItem/setItem (private browsing, full quota, SecurityError) | mitigate | Every `getItem` / `setItem` wrapped in try/catch. Read failure defaults to `false` (banner visible). Write failure still updates React state so the banner hides for the current session. |
| T-132-07 | Information Disclosure | per-GW dismiss key `deadline-dismissed:GW{id}` written under same-origin storage | accept | Value is a single `'1'` flag with no PII. Visible to any same-origin script — acceptable for non-sensitive UX preference. |
| T-132-08 | Repudiation / Spoofing | none | n/a | Banner has no authentication or server-side state. |
</threat_model>

<verification>
- `npx vitest run src/components/DeadlineBanner.test.tsx` exits 0 with every test passing.
- `npx vitest run src/lib/hooks/useNextDeadline.test.ts src/components/DeadlineBanner.test.tsx` (combined Plan 01 + 02 suite) exits 0.
- `npm test` (full project suite) exits 0 with no new failures.
- `npx tsc --noEmit` reports no new TypeScript errors.
- `npm run build` succeeds.
- Manual confirmation (operator) — after deploy/dev server start: visiting the app shows the banner above the section nav with the correct GW text from FPL bootstrap; dismiss × removes it for the session and survives a refresh; refresh after a new GW becomes is_next shows the banner again.
</verification>

<success_criteria>
- DL-01: `useNextDeadline` data drives a visible "GW{id} deadline in Xh Ym" or "GW{id} deadline in Ym" label that re-renders every 60s.
- DL-02: Three distinct visual states keyed off `msRemaining` (zinc neutral ≥ 24h, amber 2–24h, red sticky < 2h) verified by unit tests asserting Tailwind class substrings, and state transitions verified via fake-timer advancement test.
- DL-03: Per-GW dismiss via `deadline-dismissed:GW{id}` localStorage key verified by unit test; banner reappears automatically when `id` changes (covered by the lazy-init + id-change effect; logically follows from the keyed read).
- No regressions in `npm test`; no new tsc errors; build succeeds.
- Banner injected at the correct DOM position (above the sticky nav wrapper, not inside it) so the red sticky state visually layers above the section nav.
</success_criteria>

<output>
After completion, create `.planning/phases/132-deadline-day-banner/132-02-SUMMARY.md` capturing:
- Files created (DeadlineBanner.tsx, DeadlineBanner.test.tsx) with line counts.
- Files modified (page.tsx) with the two specific edit locations described.
- Test counts and statuses for DL-01, DL-02, DL-03 groupings.
- Confirmation of `npm test`, `npx tsc --noEmit`, `npm run build` results.
- Note any deviation from the colour map (e.g. if the neutral background ended up as `bg-zinc-100` instead of `bg-zinc-50` for contrast — record which was chosen and why).
- Open carry-forwards: Open Question 1 from RESEARCH (`--nav-height` interaction with red banner) — re-confirm whether this surfaced as a visual issue. If yes, flag for Phase 133+ work.
</output>
