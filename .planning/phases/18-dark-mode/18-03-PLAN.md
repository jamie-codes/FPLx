---
phase: 18-dark-mode
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/shared/MinsRiskBadge.tsx
  - src/components/shared/VerdictBadge.tsx
  - src/components/fixtures/FixtureBadges.tsx
  - src/components/captaincy/CaptaincyPanel.tsx
  - src/components/defcon/DefConTables.tsx
  - src/components/club-form/ClubFormTable.tsx
  - src/components/value-gems/ValueGemsTable.tsx
  - src/components/squad/ExplainPanel.tsx
  - src/components/LastUpdated.tsx
autonomous: true
requirements: [DARK-03]
must_haves:
  truths:
    - "MinsRiskBadge and VerdictBadge are readable in dark mode — dark backgrounds with light text"
    - "FixtureBadges TIER_COLOURS use dark-aware bg/text/border for easy/medium/hard tiers"
    - "CaptaincyPanel TYPE_MAP badges and card backgrounds are dark-aware"
    - "DefConTables, ClubFormTable, and ValueGemsTable theads and rows have dark variants"
    - "ExplainPanel background and budget spans are readable in dark mode"
  artifacts:
    - path: "src/components/shared/MinsRiskBadge.tsx"
      provides: "Dark badge map colours"
      contains: "dark:bg-green-900"
    - path: "src/components/shared/VerdictBadge.tsx"
      provides: "Dark verdict map colours"
      contains: "dark:bg-green-900"
    - path: "src/components/fixtures/FixtureBadges.tsx"
      provides: "Dark TIER_COLOURS"
      contains: "dark:bg-green-900"
    - path: "src/components/captaincy/CaptaincyPanel.tsx"
      provides: "Dark TYPE_MAP and card backgrounds"
      contains: "dark:bg-zinc-800"
    - path: "src/components/defcon/DefConTables.tsx"
      provides: "Dark thead and row colours"
      contains: "dark:bg-zinc-900"
    - path: "src/components/club-form/ClubFormTable.tsx"
      provides: "Dark thead and row colours"
      contains: "dark:bg-zinc-900"
    - path: "src/components/value-gems/ValueGemsTable.tsx"
      provides: "Dark filter pills, thead, and row colours"
      contains: "dark:bg-zinc-900"
  key_links:
    - from: "src/components/shared/MinsRiskBadge.tsx"
      to: "GemTable and SquadView rows"
      via: "badge rendered inside table cells"
      pattern: "dark:bg-.*-900"
---

<objective>
Add dark: Tailwind variants to all remaining components: badge components, table components, fixture/captaincy panels, and ExplainPanel.

Purpose: Completes DARK-03 coverage. After Plan 02 themes the major views and Plan 03 themes the remaining components, all 5 tabs are fully dark-mode ready.
Output: All 9 remaining component files updated with dark: variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dark-mode/18-RESEARCH.md
@.planning/phases/18-dark-mode/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark variants on badge components and FixtureBadges/CaptaincyPanel</name>
  <files>src/components/shared/MinsRiskBadge.tsx, src/components/shared/VerdictBadge.tsx, src/components/fixtures/FixtureBadges.tsx, src/components/captaincy/CaptaincyPanel.tsx</files>
  <read_first>
    src/components/shared/MinsRiskBadge.tsx
    src/components/shared/VerdictBadge.tsx
    src/components/fixtures/FixtureBadges.tsx
    src/components/captaincy/CaptaincyPanel.tsx
    .planning/phases/18-dark-mode/18-RESEARCH.md (Pitfall 2, FixtureBadges TIER_COLOURS section, Code Examples section)
  </read_first>
  <action>
    **MinsRiskBadge.tsx — update BADGE_MAP (or equivalent) colour strings:**
    - `bg-green-100` -> `bg-green-100 dark:bg-green-900`
    - `text-green-800` -> `text-green-800 dark:text-green-200`
    - `bg-blue-100` -> `bg-blue-100 dark:bg-blue-900`
    - `text-blue-800` -> `text-blue-800 dark:text-blue-200`
    - `bg-amber-100` -> `bg-amber-100 dark:bg-amber-900`
    - `text-amber-800` -> `text-amber-800 dark:text-amber-200`
    - `bg-zinc-100` (cameo) -> `bg-zinc-100 dark:bg-zinc-700`
    - `text-zinc-600` (cameo) -> `text-zinc-600 dark:text-zinc-300`

    **VerdictBadge.tsx — update VERDICT_MAP colour strings:**
    - `bg-green-100` (buy/keep) -> `bg-green-100 dark:bg-green-900`
    - `text-green-800` -> `text-green-800 dark:text-green-200`
    - `bg-red-100` (sell) -> `bg-red-100 dark:bg-red-900`
    - `text-red-700` -> `text-red-700 dark:text-red-300`
    - `bg-zinc-100` (hold) -> `bg-zinc-100 dark:bg-zinc-700`
    - `text-zinc-600` -> `text-zinc-600 dark:text-zinc-300`

    **FixtureBadges.tsx — update TIER_COLOURS object:**
    ```
    easy:   'bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200 border-green-300 dark:border-green-700'
    medium: 'bg-amber-100 dark:bg-amber-900 text-amber-800 dark:text-amber-200 border-amber-300 dark:border-amber-700'
    hard:   'bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200 border-red-300 dark:border-red-700'
    ```
    - DGW label `text-violet-700` -> add `dark:text-violet-400`
    - Any other text-zinc-* usages -> add dark variants per standard mapping

    **CaptaincyPanel.tsx — update TYPE_MAP and card styling:**
    - TYPE_MAP badge colours: same pattern as MinsRiskBadge (bg-{colour}-100 -> dark:bg-{colour}-900, text-{colour}-800 -> dark:text-{colour}-200)
    - Card backgrounds `bg-zinc-50` -> add `dark:bg-zinc-800`
    - Text `text-zinc-700` -> add `dark:text-zinc-300`
    - Text `text-zinc-600` -> add `dark:text-zinc-400`
    - Borders `border-zinc-200` -> add `dark:border-zinc-700`

    Do NOT change component logic or structure — only add dark: class variants.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && grep -c "dark:" src/components/shared/MinsRiskBadge.tsx src/components/shared/VerdictBadge.tsx src/components/fixtures/FixtureBadges.tsx src/components/captaincy/CaptaincyPanel.tsx && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - MinsRiskBadge.tsx: BADGE_MAP entries contain `dark:bg-green-900`, `dark:bg-blue-900`, `dark:bg-amber-900`, `dark:bg-zinc-700`
    - VerdictBadge.tsx: VERDICT_MAP entries contain `dark:bg-green-900`, `dark:bg-red-900`, `dark:bg-zinc-700`
    - FixtureBadges.tsx: TIER_COLOURS contains `dark:bg-green-900`, `dark:bg-amber-900`, `dark:bg-red-900`
    - FixtureBadges.tsx: contains `dark:text-violet-400`
    - CaptaincyPanel.tsx: contains `dark:bg-zinc-800` (card backgrounds)
    - `npm test` passes
  </acceptance_criteria>
  <done>All badge components and fixture/captaincy panels have dark: variants; badges use inverted colour scheme (dark bg + light text) in dark mode</done>
</task>

<task type="auto">
  <name>Task 2: Dark variants on remaining table components and ExplainPanel</name>
  <files>src/components/defcon/DefConTables.tsx, src/components/club-form/ClubFormTable.tsx, src/components/value-gems/ValueGemsTable.tsx, src/components/squad/ExplainPanel.tsx, src/components/LastUpdated.tsx</files>
  <read_first>
    src/components/defcon/DefConTables.tsx
    src/components/club-form/ClubFormTable.tsx
    src/components/value-gems/ValueGemsTable.tsx
    src/components/squad/ExplainPanel.tsx
    src/components/LastUpdated.tsx
    .planning/phases/18-dark-mode/18-RESEARCH.md (Color Token Audit, Component Complexity Ranking)
  </read_first>
  <action>
    **DefConTables.tsx:**
    - `bg-white` (thead) -> add `dark:bg-zinc-900`
    - `even:bg-gray-50` or `even:bg-zinc-50` -> add `dark:even:bg-zinc-800`
    - `text-zinc-*` colours -> add standard dark mappings (900->100, 700->300, 600->400, 500->400)
    - `border-zinc-200` -> add `dark:border-zinc-700`
    - `text-green-600` and `text-red-600` on distance_to_threshold -> leave as-is (semantic, readable in both modes per research)
    - `hover:bg-blue-50` (if present) -> add `dark:hover:bg-zinc-700`

    **ClubFormTable.tsx:**
    - `bg-white` (thead) -> add `dark:bg-zinc-900`
    - `even:bg-gray-50` -> add `dark:even:bg-zinc-800`
    - `text-zinc-*` colours -> add standard dark mappings
    - `border-zinc-200` or `border-gray-200` -> add `dark:border-zinc-700`

    **ValueGemsTable.tsx:**
    - Filter pills: `bg-zinc-900` (active) -> add `dark:bg-white dark:text-zinc-900` (invert)
    - Filter pills: `bg-white` (inactive) -> add `dark:bg-zinc-800`
    - `bg-white` (thead) -> add `dark:bg-zinc-900`
    - `even:bg-gray-50` -> add `dark:even:bg-zinc-800`
    - `text-zinc-*` colours -> add standard dark mappings
    - `border-zinc-200` -> add `dark:border-zinc-700`

    **ExplainPanel.tsx:**
    - `bg-zinc-50` -> add `dark:bg-zinc-800`
    - `text-zinc-*` colours -> add standard dark mappings
    - Budget spans with `bg-red-50` -> add `dark:bg-red-950`
    - Any `border-zinc-*` -> add corresponding dark variant

    **LastUpdated.tsx:**
    - `text-amber-600` (stale) -> leave as-is (readable in both modes per research)
    - `text-zinc-400` (fresh) -> leave as-is (acceptable in dark per research, may add `dark:text-zinc-500` if present)
    - Only add dark variants if there are bg-white or border classes present

    Do NOT change component logic or structure — only add dark: class variants.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && grep -c "dark:" src/components/defcon/DefConTables.tsx src/components/club-form/ClubFormTable.tsx src/components/value-gems/ValueGemsTable.tsx src/components/squad/ExplainPanel.tsx && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - DefConTables.tsx: contains `dark:bg-zinc-900` (thead)
    - ClubFormTable.tsx: contains `dark:bg-zinc-900` (thead)
    - ValueGemsTable.tsx: contains `dark:bg-white` (active filter pill inversion)
    - ValueGemsTable.tsx: contains `dark:bg-zinc-900` (thead)
    - ExplainPanel.tsx: contains `dark:bg-zinc-800`
    - `npm test` passes
  </acceptance_criteria>
  <done>All remaining table components and ExplainPanel have dark: variants; every tab's content is fully themed for dark mode</done>
</task>

</tasks>

<verification>
- Every .tsx file in src/components/ that had hardcoded colour classes now contains at least one `dark:` variant
- `npm test` passes
- `grep -rL "dark:" src/components/` returns only files that genuinely have no colour classes (if any)
</verification>

<success_criteria>
- Badge components use inverted dark palette in dark mode
- TIER_COLOURS in FixtureBadges are fully dark-aware (bg, text, border)
- All table theads have dark:bg-zinc-900
- All alternating rows have dark:even:bg-zinc-800
- ExplainPanel readable in dark mode
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-dark-mode/18-03-SUMMARY.md`
</output>
