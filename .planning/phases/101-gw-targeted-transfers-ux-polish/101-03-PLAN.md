---
phase: 101
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gem-table/GwToggle.tsx
  - src/components/optimiser/OptimiserPanel.test.tsx
autonomous: true
requirements: [UX-01]
tags: [ux, label-rename, fpl-analyst]

must_haves:
  truths:
    - "GwToggle renders buttons labelled 'Next 1 GW', 'Next 3 GWs', 'Next 5 GWs' (was '1 GW', '3 GW', '5 GW')"
    - "Singular handling: gw===1 produces 'Next 1 GW' (no 's'), gw===3 and gw===5 produce 'Next 3 GWs' and 'Next 5 GWs' (with 's')"
    - "No data logic changes — only the rendered string changes"
    - "All Vitest assertions on GwToggle button text are updated; full test suite exits 0"
  artifacts:
    - path: "src/components/gem-table/GwToggle.tsx"
      provides: "Renamed button labels per D-12"
      contains: "Next {gw} GW"
    - path: "src/components/optimiser/OptimiserPanel.test.tsx"
      provides: "Updated assertion: '5 GW' → 'Next 5 GWs' (line 209)"
      contains: "Next 5 GWs"
  key_links:
    - from: "src/components/gem-table/GwToggle.tsx (line ~115)"
      to: "rendered button text 'Next {gw} GW{s}'"
      via: "JSX inline template"
      pattern: "Next \\{gw\\} GW"
    - from: "src/components/optimiser/OptimiserPanel.test.tsx line 209"
      to: "updated textContent assertion"
      via: "querySelectorAll('button').find(b => b.textContent?.trim() === 'Next 5 GWs')"
      pattern: "'Next 5 GWs'"
---

<objective>
Deliver UX-01: rename the GwToggle button labels from `{gw} GW` to `Next {gw} GW{s}` (producing "Next 1 GW", "Next 3 GWs", "Next 5 GWs"), and update the one test in `OptimiserPanel.test.tsx` that asserts the old text. This is a pure display-string change with zero data-logic impact.

**Purpose:** The current "1 GW / 3 GW / 5 GW" labels are ambiguous (could mean "match week 1" or "next week" or "this gameweek number"). "Next 1 GW / Next 3 GWs / Next 5 GWs" makes the rolling-horizon semantic explicit.

**Output:** Modified `src/components/gem-table/GwToggle.tsx` (one-line string change), modified `src/components/optimiser/OptimiserPanel.test.tsx` (one assertion update).

**Scope boundary:** `HorizonSelector.tsx` (Planner tab) is OUT OF SCOPE per 101-RESEARCH.md §UX-01 Audit — it is a different component (PlannerHorizon 1–5) and its test at `src/app/page.test.tsx` line 294 is NOT updated by this plan. `OpportunityCostTable.tsx`'s column header IS in scope but is handled by Plan 02 (already includes the "Next" prefix per D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/101-gw-targeted-transfers-ux-polish/101-CONTEXT.md
@.planning/phases/101-gw-targeted-transfers-ux-polish/101-RESEARCH.md
@.planning/phases/101-gw-targeted-transfers-ux-polish/101-PATTERNS.md
@.planning/phases/101-gw-targeted-transfers-ux-polish/101-UI-SPEC.md

@src/components/gem-table/GwToggle.tsx
@src/components/optimiser/OptimiserPanel.test.tsx

<interfaces>
<!-- The single line of code being changed in GwToggle.tsx (line 115): -->

Before:
```tsx
{gw} GW
```

After (D-12):
```tsx
Next {gw} GW{gw === 1 ? '' : 's'}
```

This produces:
- `gw === 1` → "Next 1 GW" (singular)
- `gw === 3` → "Next 3 GWs" (plural)
- `gw === 5` → "Next 5 GWs" (plural)

<!-- The assertion being updated in OptimiserPanel.test.tsx (line ~209): -->

Before:
```typescript
const fiveGwBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent?.trim() === '5 GW')
```

After:
```typescript
const fiveGwBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent?.trim() === 'Next 5 GWs')
```
</interfaces>

<scope_audit>
From 101-RESEARCH.md §UX-01 Label Audit — Exhaustive (HIGH confidence grep audit):

**IN SCOPE — handled by this plan:**
- `src/components/gem-table/GwToggle.tsx` line 115 → `{gw} GW` → `Next {gw} GW{s}`
- `src/components/optimiser/OptimiserPanel.test.tsx` line 209 → `'5 GW'` → `'Next 5 GWs'`

**IN SCOPE — handled by Plan 02 (already includes "Next" prefix in the horizon-mode template):**
- `src/components/transfers/OpportunityCostTable.tsx` column header

**OUT OF SCOPE per CONTEXT.md D-13 reading (do NOT touch in this plan):**
- `src/components/planner/HorizonSelector.tsx` line 30 (PlannerHorizon component — different feature)
- `src/components/squad/DecisionSummaryTab.tsx` lines 504, 566 (static labels, different UI context)
- `src/components/gem-table/PlayerComparisonModal.tsx` lines 115/121/127 (row labels, not toggle buttons)
- `src/lib/explain.ts` prose strings (not user-facing toggle labels)
- `src/components/gem-table/RegressionSignalBadge.tsx` tooltip strings (not toggle labels)
- `src/app/page.test.tsx` line 294 — queries `aria-label="Planning horizon"` which is `HorizonSelector`, NOT `GwToggle` (the latter has `aria-label="Projected points horizon"`). Confirmed by direct inspection of `HorizonSelector.tsx`. This assertion still passes because HorizonSelector still renders "1 GW".
</scope_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename GwToggle button label + update OptimiserPanel test assertion + verify no other label-text regressions</name>
  <files>src/components/gem-table/GwToggle.tsx, src/components/optimiser/OptimiserPanel.test.tsx</files>
  <read_first>
    - src/components/gem-table/GwToggle.tsx (entire file — confirm line 115 is the only label-render site)
    - src/components/gem-table/GwToggle.test.ts (confirm it tests getColumnVisibility only, NOT button label text — so no changes needed there)
    - src/components/optimiser/OptimiserPanel.test.tsx lines 195–220 (confirm the assertion at line 209 is the GwToggle 5-GW button, NOT a HorizonSelector button)
    - src/app/page.test.tsx lines 285–310 (confirm the `'1 GW'` button at line 294 is the HorizonSelector — `aria-label="Planning horizon"` — and therefore OUT OF SCOPE for this plan)
    - src/components/planner/HorizonSelector.tsx (confirm out-of-scope component renders the same `{gw} GW` label, unaffected by this plan)
    - .planning/phases/101-gw-targeted-transfers-ux-polish/101-RESEARCH.md §UX-01 Label Audit (full audit table)
  </read_first>
  <action>
    **Step 1 — Modify `src/components/gem-table/GwToggle.tsx` line 115.** Change ONLY the button label inside the `<button>` JSX. Do NOT change any other line. Do NOT change Tailwind classes, padding, aria-pressed, the disabled wrapper, role, aria-label, onClick, or any other prop.

    The exact diff (line 115):
    ```tsx
    // BEFORE:
            {gw} GW
    // AFTER:
            Next {gw} GW{gw === 1 ? '' : 's'}
    ```

    Confirm by reading the surrounding context (lines 104–117) — the only change should be in the JSX children of the `<button>` element inside the `.map((gw) => ...)` loop.

    **Step 2 — Modify `src/components/optimiser/OptimiserPanel.test.tsx` line 209.** Update the textContent assertion AND its comment (line ~208) for clarity:
    ```typescript
    // BEFORE (lines 208–209):
          // Click the 5GW toggle button (GwToggle renders "5 GW" — note the space)
          const fiveGwBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent?.trim() === '5 GW')

    // AFTER:
          // Click the 5GW toggle button (Phase 101 UX-01: GwToggle now renders "Next 5 GWs")
          const fiveGwBtn = Array.from(container.querySelectorAll('button')).find(b => b.textContent?.trim() === 'Next 5 GWs')
    ```

    **Step 3 — Confirm scope boundary via grep (NOT a code change, just a verification step before declaring done).** Run:
    ```
    grep -rn "'1 GW'\|'3 GW'\|'5 GW'" src --include="*.ts" --include="*.tsx"
    ```
    Expected remaining matches AFTER this plan:
    - `src/app/page.test.tsx` line 294 (HorizonSelector `'1 GW'` — out of scope, intentionally untouched)
    - `src/components/gem-table/PlayerComparisonModal.test.tsx` (modal row labels — out of scope)
    - Any other matches in OUT-OF-SCOPE files listed in `<scope_audit>` above
    
    If ANY in-scope file (GwToggle.tsx, OptimiserPanel.test.tsx) still contains `'5 GW'` or the bare `{gw} GW` pattern after this plan, the change is incomplete.

    **Step 4 — Do NOT modify these files** (they are explicitly out of scope per the scope_audit above):
    - `src/components/planner/HorizonSelector.tsx`
    - `src/components/squad/DecisionSummaryTab.tsx`
    - `src/components/gem-table/PlayerComparisonModal.tsx`
    - `src/components/gem-table/PlayerComparisonModal.test.tsx`
    - `src/app/page.test.tsx`
    - `src/lib/explain.ts`
    - `src/components/gem-table/RegressionSignalBadge.tsx`
  </action>
  <verify>
    <automated>npx vitest run src/components/gem-table/GwToggle src/components/optimiser/OptimiserPanel src/app/page</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/gem-table/GwToggle.tsx` contains the literal string `Next {gw} GW{gw === 1 ? '' : 's'}`
    - File `src/components/gem-table/GwToggle.tsx` does NOT contain a bare `{gw} GW` rendering (the old format). Verify with: `grep -v 'Next {gw}' src/components/gem-table/GwToggle.tsx | grep -c '^[^/].*{gw} GW'` returns 0 (excluding header/comment lines).
    - File `src/components/optimiser/OptimiserPanel.test.tsx` contains the literal string `'Next 5 GWs'`
    - File `src/components/optimiser/OptimiserPanel.test.tsx` does NOT contain the literal string `=== '5 GW'` (the old assertion is fully replaced)
    - File `src/components/planner/HorizonSelector.tsx` is UNCHANGED (executor must NOT touch it — verify via `git diff src/components/planner/HorizonSelector.tsx` returns empty)
    - File `src/app/page.test.tsx` is UNCHANGED (executor must NOT touch it — verify via `git diff src/app/page.test.tsx` returns empty)
    - Command `npx vitest run src/components/gem-table/GwToggle src/components/optimiser/OptimiserPanel src/app/page` exits 0
    - Command `npx vitest run` (full suite) exits 0 — no test regressions anywhere in the project from this label change
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GwToggle renders "Next 1 GW" / "Next 3 GWs" / "Next 5 GWs"; OptimiserPanel.test.tsx assertion updated; HorizonSelector and page.test.tsx UNCHANGED (out of scope); full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure display string change. No new ingress, no new logic, no new data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-06 | (none applicable) | GwToggle label | accept | Display-only change; no security surface impacted. |
</threat_model>

<verification>
1. `npx vitest run src/components/gem-table/GwToggle src/components/optimiser/OptimiserPanel src/app/page` exits 0
2. `npx vitest run` (full suite) exits 0 — no regressions
3. `npx tsc --noEmit` exits 0
4. `git diff src/components/planner/HorizonSelector.tsx src/app/page.test.tsx` returns empty (out-of-scope files untouched)
5. `grep -n "Next {gw} GW" src/components/gem-table/GwToggle.tsx` returns exactly one hit on line 115 (or wherever the JSX renders)
</verification>

<success_criteria>
- GwToggle buttons render the renamed labels with correct singular/plural handling
- OptimiserPanel.test.tsx assertion updated and passing
- HorizonSelector.tsx and page.test.tsx remain untouched (out-of-scope boundary respected)
- Full Vitest suite exits 0; TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/101-gw-targeted-transfers-ux-polish/101-03-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md. Include:
- The exact diff for both files (GwToggle.tsx line 115 + OptimiserPanel.test.tsx line 209)
- Confirmation that out-of-scope files (HorizonSelector, page.test.tsx, DecisionSummaryTab, PlayerComparisonModal) remain untouched
- Test-count snapshot (full suite passed N tests / failed 0)
- Any new label-render sites discovered post-execution that should be addressed in a follow-up phase
</output>
