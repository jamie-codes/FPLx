---
phase: 15-remaining-tables-mobile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/defcon/DefConTables.tsx
  - src/components/club-form/ClubFormTable.tsx
  - src/components/value-gems/ValueGemsTable.tsx
autonomous: true
requirements:
  - MOB-TBL-03
  - MOB-TBL-04

must_haves:
  truths:
    - "On mobile (<640px), DefConTables show exactly Player, Team, Hit Rate, and Avg DC/90 — Hits, Distance, and Easy vs Hard are hidden"
    - "On mobile (<640px), ClubFormTable shows exactly Team, W, D, L, and GD — GS, GC, and Next 5 are hidden"
    - "On mobile (<640px), ValueGemsTable shows exactly Player, Price, Gem, and Pts — Pos, Team, Own%, Trend, and Next 5 are hidden"
    - "On desktop (>=640px), all three tables show all columns unchanged"
  artifacts:
    - path: "src/components/defcon/DefConTables.tsx"
      provides: "Mobile column hiding via TanStack VisibilityState"
      contains: "isMobile"
    - path: "src/components/club-form/ClubFormTable.tsx"
      provides: "Mobile column hiding via TanStack VisibilityState"
      contains: "isMobile"
    - path: "src/components/value-gems/ValueGemsTable.tsx"
      provides: "Mobile column hiding via TanStack VisibilityState"
      contains: "isMobile"
  key_links:
    - from: "src/components/defcon/DefConTables.tsx"
      to: "window.innerWidth"
      via: "useEffect with resize listener"
      pattern: "window\\.innerWidth < 640"
    - from: "src/components/club-form/ClubFormTable.tsx"
      to: "window.innerWidth"
      via: "useEffect with resize listener"
      pattern: "window\\.innerWidth < 640"
    - from: "src/components/value-gems/ValueGemsTable.tsx"
      to: "window.innerWidth"
      via: "useEffect with resize listener"
      pattern: "window\\.innerWidth < 640"
---

<objective>
Make DefConTables, ClubFormTable, and ValueGemsTable fit on a phone by hiding non-essential columns on mobile using TanStack Table's columnVisibility state.

Purpose: These three tables have 6-9 columns each — too wide for 375px. This plan reduces each to 4-5 priority columns on mobile while leaving desktop completely unchanged.
Output: Modified DefConTables.tsx, ClubFormTable.tsx, and ValueGemsTable.tsx — each with isMobile state and TanStack columnVisibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gemtable-mobile/14-01-SUMMARY.md

<interfaces>
<!-- DefConTables column IDs (from columns.tsx): web_name, team_short_name, hit_rate, hits, avg_per90, distance_to_threshold, fixture_correlation -->
<!-- ClubFormTable column IDs (from columns.tsx): team_short_name, wins, draws, losses, goals_scored, goals_conceded, goal_diff, upcoming -->
<!-- ValueGemsTable column IDs (from columns.tsx): web_name, element_type, team_short_name, now_cost, selected_by_percent, total_points, gem_score, trend, fixtures -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile column hiding to DefConTables</name>
  <files>src/components/defcon/DefConTables.tsx</files>
  <read_first>
    - src/components/defcon/DefConTables.tsx
    - src/components/defcon/columns.tsx
    - src/components/gem-table/GemTable.tsx (reference for isMobile + columnVisibility pattern)
  </read_first>
  <action>
    DefConTables uses TanStack Table but does NOT currently use columnVisibility. It has two table instances (defTable, midFwdTable) and a shared `renderTable` helper.

    **Mobile priority columns:** Player (web_name), Team (team_short_name), Hit Rate (hit_rate), Avg DC/90 (avg_per90)
    **Hidden on mobile:** hits, distance_to_threshold, fixture_correlation

    **1. Add imports** — change `import { useState, useMemo } from 'react'` to `import { useState, useMemo, useEffect } from 'react'`. Add `type VisibilityState` to the @tanstack/react-table import.

    **2. Add isMobile state** — inside DefConTables component, after the existing `useMemo` calls but before the sorting state, add:
    ```
    const [isMobile, setIsMobile] = useState(false)
    useEffect(() => {
      const check = () => setIsMobile(window.innerWidth < 640)
      check()
      window.addEventListener('resize', check)
      return () => window.removeEventListener('resize', check)
    }, [])
    ```

    **3. Add columnVisibility** — right after isMobile state:
    ```
    const columnVisibility: VisibilityState = isMobile
      ? { hits: false, distance_to_threshold: false, fixture_correlation: false }
      : {}
    ```

    **4. Wire into both table instances** — add `columnVisibility` to both defTable and midFwdTable `state` objects:
    - defTable: change `state: { sorting: defSorting }` to `state: { sorting: defSorting, columnVisibility }`
    - midFwdTable: change `state: { sorting: midFwdSorting }` to `state: { sorting: midFwdSorting, columnVisibility }`

    **Important:** The shared `renderTable` function does NOT need changes — it already uses `row.getVisibleCells()` and `headerGroup.headers` which respect visibility state automatically.
  </action>
  <verify>
    <automated>cd /c/users/jamie/fplx && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "isMobile" src/components/defcon/DefConTables.tsx returns at least 3 matches
    - grep "window.innerWidth < 640" src/components/defcon/DefConTables.tsx returns a match
    - grep "useEffect" src/components/defcon/DefConTables.tsx returns a match
    - grep "columnVisibility" src/components/defcon/DefConTables.tsx returns at least 3 matches (declaration + 2 table state objects)
    - grep "VisibilityState" src/components/defcon/DefConTables.tsx returns a match
    - grep "hits: false" src/components/defcon/DefConTables.tsx returns a match
    - grep "distance_to_threshold: false" src/components/defcon/DefConTables.tsx returns a match
    - grep "fixture_correlation: false" src/components/defcon/DefConTables.tsx returns a match
  </acceptance_criteria>
  <done>DefConTables shows exactly Player, Team, Hit Rate, Avg DC/90 on mobile; Hits, Distance, Easy vs Hard hidden; desktop shows all 7 columns unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile column hiding to ClubFormTable and ValueGemsTable</name>
  <files>src/components/club-form/ClubFormTable.tsx, src/components/value-gems/ValueGemsTable.tsx</files>
  <read_first>
    - src/components/club-form/ClubFormTable.tsx
    - src/components/club-form/columns.tsx
    - src/components/value-gems/ValueGemsTable.tsx
    - src/components/value-gems/columns.tsx
  </read_first>
  <action>
    Both tables use TanStack Table without columnVisibility. Apply the same isMobile + VisibilityState pattern.

    **--- ClubFormTable.tsx ---**

    **Mobile priority columns:** Team (team_short_name), W (wins), D (draws), L (losses), GD (goal_diff)
    **Hidden on mobile:** goals_scored, goals_conceded, upcoming

    **1. Add useEffect import** — change `import { useState } from 'react'` to `import { useState, useEffect } from 'react'`. Add `type VisibilityState` to the @tanstack/react-table import.

    **2. Add isMobile state** — inside ClubFormTable, after `useClubForm()` and before sorting state:
    ```
    const [isMobile, setIsMobile] = useState(false)
    useEffect(() => {
      const check = () => setIsMobile(window.innerWidth < 640)
      check()
      window.addEventListener('resize', check)
      return () => window.removeEventListener('resize', check)
    }, [])
    ```

    **3. Add columnVisibility:**
    ```
    const columnVisibility: VisibilityState = isMobile
      ? { goals_scored: false, goals_conceded: false, upcoming: false }
      : {}
    ```

    **4. Wire into table** — change `state: { sorting }` to `state: { sorting, columnVisibility }`

    **--- ValueGemsTable.tsx ---**

    **Mobile priority columns:** Player (web_name), Price (now_cost), Gem (gem_score), Pts (total_points)
    **Hidden on mobile:** element_type (Pos), team_short_name (Team), selected_by_percent (Own%), trend, fixtures

    **1. Add useEffect import** — change `import { useState, useMemo } from 'react'` to `import { useState, useMemo, useEffect } from 'react'`. Add `type VisibilityState` to the @tanstack/react-table import.

    **2. Add isMobile state** — inside ValueGemsTable, after `useEffect` for filter/sorting but before `useReactTable`:
    ```
    const [isMobile, setIsMobile] = useState(false)
    useEffect(() => {
      const check = () => setIsMobile(window.innerWidth < 640)
      check()
      window.addEventListener('resize', check)
      return () => window.removeEventListener('resize', check)
    }, [])
    ```

    **3. Add columnVisibility:**
    ```
    const columnVisibility: VisibilityState = isMobile
      ? { element_type: false, team_short_name: false, selected_by_percent: false, trend: false, fixtures: false }
      : {}
    ```

    **4. Wire into table** — change `state: { sorting }` to `state: { sorting, columnVisibility }`
  </action>
  <verify>
    <automated>cd /c/users/jamie/fplx && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "isMobile" src/components/club-form/ClubFormTable.tsx returns at least 3 matches
    - grep "window.innerWidth < 640" src/components/club-form/ClubFormTable.tsx returns a match
    - grep "columnVisibility" src/components/club-form/ClubFormTable.tsx returns at least 2 matches
    - grep "goals_scored: false" src/components/club-form/ClubFormTable.tsx returns a match
    - grep "goals_conceded: false" src/components/club-form/ClubFormTable.tsx returns a match
    - grep "upcoming: false" src/components/club-form/ClubFormTable.tsx returns a match
    - grep "isMobile" src/components/value-gems/ValueGemsTable.tsx returns at least 3 matches
    - grep "window.innerWidth < 640" src/components/value-gems/ValueGemsTable.tsx returns a match
    - grep "columnVisibility" src/components/value-gems/ValueGemsTable.tsx returns at least 2 matches
    - grep "element_type: false" src/components/value-gems/ValueGemsTable.tsx returns a match
    - grep "team_short_name: false" src/components/value-gems/ValueGemsTable.tsx returns a match
    - grep "selected_by_percent: false" src/components/value-gems/ValueGemsTable.tsx returns a match
    - grep "trend: false" src/components/value-gems/ValueGemsTable.tsx returns a match
    - grep "fixtures: false" src/components/value-gems/ValueGemsTable.tsx returns a match
  </acceptance_criteria>
  <done>ClubFormTable shows Team, W, D, L, GD on mobile (5 cols); ValueGemsTable shows Player, Price, Gem, Pts on mobile (4 cols); both fit within 375px; desktop unchanged for both tables</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all existing tests pass
- DevTools at 375px: DefConTables show 4 columns (Player, Team, Hit Rate, Avg DC/90)
- DevTools at 375px: ClubFormTable shows 5 columns (Team, W, D, L, GD)
- DevTools at 375px: ValueGemsTable shows 4 columns (Player, Price, Gem, Pts)
- DevTools at 1024px: All three tables show full column sets unchanged
</verification>

<success_criteria>
- DefConTables show exactly Player, Team, Hit Rate, Avg DC/90 on mobile
- ClubFormTable shows exactly Team, W, D, L, GD on mobile
- ValueGemsTable shows exactly Player, Price, Gem, Pts on mobile
- All three tables fit comfortably within 375px
- Desktop layouts unchanged for all three tables
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-remaining-tables-mobile/15-02-SUMMARY.md`
</output>
