---
phase: 109
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pipeline/accuracy.py
  - pipeline/run.py
  - pipeline/tests/test_accuracy.py
autonomous: true
requirements: [MC-CAL-01]
tags: [calibration, monte-carlo, pipeline, accuracy-backtest, python]
must_haves:
  truths:
    - "When MC_ENABLED=True and ≥80% of players have haul_prob, accuracy_backtest.json.summary.calibration_mode == 'mc'"
    - "When MC mode is active, each calibration bucket's predicted_rate == mean(haul_prob) of the players in that decile (not bucket_mid)"
    - "When use_mc=True, players are sorted by effective_haul_prob descending (not by xpts_predicted) before decile bucketing"
    - "When mc_enabled=False OR coverage < 80%, calibration_mode == 'analytical' and predicted_rate == bucket_mid (existing behavior preserved)"
    - "Players missing from merged_haul_lookup contribute effective_haul_prob = 0.0 (never NaN, never KeyError)"
    - "_empty_backtest summary contains calibration_mode = 'analytical' (UI never sees undefined on cold-start)"
    - "All 34 existing pipeline tests continue to pass (no regression)"
  artifacts:
    - path: "pipeline/accuracy.py"
      provides: "_compute_calibration_data with use_mc + merged_haul_lookup params; compute_accuracy_backtest with merged_haul_lookup + total_merged_count params and calibration_mode in summary"
      contains: "def _compute_calibration_data(per_gw_rows: dict, use_mc: bool = False, merged_haul_lookup: dict = None)"
    - path: "pipeline/accuracy.py"
      provides: "calibration_mode written to summary alongside mc_enabled in both compute_accuracy_backtest and _empty_backtest"
      contains: "'calibration_mode':"
    - path: "pipeline/run.py"
      provides: "haul_lookup built from merged list and passed to compute_accuracy_backtest"
      contains: "haul_lookup"
    - path: "pipeline/tests/test_accuracy.py"
      provides: "7 new MC calibration tests (mode written, predicted_rate, sort order, fallback, coverage threshold, missing haul_prob, coverage below threshold)"
      contains: "def test_calibration_mode_mc_written_to_summary"
  key_links:
    - from: "pipeline/run.py"
      to: "pipeline/accuracy.py::compute_accuracy_backtest"
      via: "merged_haul_lookup + total_merged_count parameters"
      pattern: "compute_accuracy_backtest\\([^)]*merged_haul_lookup="
    - from: "pipeline/accuracy.py::compute_accuracy_backtest"
      to: "pipeline/accuracy.py::_compute_calibration_data"
      via: "use_mc bool + merged_haul_lookup dict"
      pattern: "_compute_calibration_data\\(per_gw_rows, use_mc="
    - from: "pipeline/accuracy.py output dict"
      to: "accuracy_backtest.json.summary"
      via: "'calibration_mode' key"
      pattern: "'calibration_mode': calibration_mode"
---

<objective>
Replace the analytical xPts decile-rank proxy (`predicted_rate = bucket_mid`) in `pipeline/accuracy.py` with actual MC `haul_prob` (P(pts ≥ 10) from 10k sims) as `predicted_rate` in the calibration reliability diagram. Add a `calibration_mode: 'mc' | 'analytical'` field to `accuracy_backtest.json.summary`, derived from a 80% coverage gate over `mc_enabled` × `haul_prob` availability. Wire `run.py` to build the haul lookup and pass it through.

Purpose: Phase 109 closes MC-CAL-01. Reliability diagram X-axis now reflects actual probability clusters from 10k MC sims rather than evenly-spaced decile ranks — calibration evidence becomes a genuine MC-vs-actual signal rather than a rank-vs-actual approximation.

Output:
1. `pipeline/accuracy.py` — `_compute_calibration_data` gains `use_mc` + `merged_haul_lookup` params; `compute_accuracy_backtest` gains `merged_haul_lookup` + `total_merged_count` params, derives `use_mc` from coverage, writes `calibration_mode` to summary; `_empty_backtest` includes `calibration_mode = 'analytical'`.
2. `pipeline/run.py` — builds `haul_lookup` from `merged` list, computes `total_merged_count`, passes both to `compute_accuracy_backtest`, prints coverage line.
3. `pipeline/tests/test_accuracy.py` — 7 new MC-path tests (RED gate first, GREEN after implementation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-CONTEXT.md
@.planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-RESEARCH.md
@.planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-PATTERNS.md
@.planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-VALIDATION.md

@pipeline/accuracy.py
@pipeline/run.py
@pipeline/tests/test_accuracy.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-14. -->

Current `_compute_calibration_data` signature (pipeline/accuracy.py line 496):
```python
def _compute_calibration_data(per_gw_rows: dict) -> dict:
```

New signature (per CONTEXT.md D-07):
```python
def _compute_calibration_data(
    per_gw_rows: dict,
    use_mc: bool = False,
    merged_haul_lookup: dict = None,
) -> dict:
```

Current `compute_accuracy_backtest` signature (pipeline/accuracy.py line 121):
```python
def compute_accuracy_backtest(
    summaries: dict,
    finished_gws: int,
    bootstrap: dict,
    fixtures: list,
    cache_dir: str = '',
) -> dict:
```

New signature (per CONTEXT.md D-01, D-03):
```python
def compute_accuracy_backtest(
    summaries: dict,
    finished_gws: int,
    bootstrap: dict,
    fixtures: list,
    cache_dir: str = '',
    merged_haul_lookup: dict = None,
    total_merged_count: int = 0,
) -> dict:
```

Current bucket dict shape (pipeline/accuracy.py lines 559-568):
```python
{
    'bucket_mid': bucket_mids[d],
    'predicted_rate': bucket_mids[d],    # In MC mode → mean(effective_haul_prob)
    'actual_rate': round(haul / total, 4),
    'sample_n': total,
    'predicted_mean': round(bucket_sum_predicted[pos_key][d] / total, 2),
    'actual_mean':    round(bucket_sum_actual[pos_key][d]    / total, 2),
}
```

Current call site in `compute_accuracy_backtest` (pipeline/accuracy.py line 349):
```python
calibration = _compute_calibration_data(per_gw_rows)
```

Existing `mc_enabled` read pattern (pipeline/accuracy.py line 368):
```python
mc_enabled = bool(prior_cache.get('summary', {}).get('mc_enabled', False))  # Phase 90 MC-01
```

Current `compute_accuracy_backtest` call site in run.py (line 325):
```python
backtest_data = compute_accuracy_backtest(summaries, finished_gws, bootstrap, fixtures, cache_dir=cache_dir)
```

`merged` list is in scope in run.py from line 214 onward; `MC_ENABLED = True` is set on line 194; `mc_enabled = MC_ENABLED` override is set on line 204. Every active player in production has `haul_prob` populated (100% coverage confirmed in research).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for MC calibration path in pipeline/tests/test_accuracy.py</name>
  <files>pipeline/tests/test_accuracy.py</files>
  <read_first>
    - pipeline/tests/test_accuracy.py (full file — to see `_hist`, `_build_minimal_inputs` helpers, existing calibration tests at lines 452-820, import pattern at line 21)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-CONTEXT.md (decisions D-01 through D-07)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-PATTERNS.md (test analog section at the end)
    - pipeline/accuracy.py lines 121-410 (compute_accuracy_backtest body — to see existing summary dict shape and mc_enabled handling at line 368)
    - pipeline/accuracy.py lines 496-571 (current _compute_calibration_data — to understand existing decile bucketing logic)
  </read_first>
  <behavior>
    All seven new tests MUST collect and run RED before any implementation in Task 2 lands. Each test exercises a specific MC-path behavior or fallback rule.

    - test_calibration_mode_mc_written_to_summary: Build 50 players × 5 GWs, supply merged_haul_lookup with haul_prob != None for all 50 players, total_merged_count=50; call compute_accuracy_backtest with mc_enabled flag already True in prior cache (use a tmp cache_dir with a pre-seeded accuracy_backtest.json containing summary.mc_enabled=true). Assert result['summary']['calibration_mode'] == 'mc'.
    - test_calibration_mc_path_predicted_rate: Call _compute_calibration_data directly with per_gw_rows + use_mc=True + a merged_haul_lookup that maps player_id → known haul_prob values (e.g. linearly from 0.30 down to 0.01). Assert every returned bucket's predicted_rate equals the mean of effective_haul_prob across the players assigned to that decile (within rounding tolerance 1e-4). Assert at least one bucket has predicted_rate != bucket_mid (proves the MC path is in effect).
    - test_calibration_mc_path_sort_order: Construct two players where xpts_predicted ranks them one way but haul_prob ranks them the opposite way. With use_mc=True, the higher-haul_prob player must land in the top decile (rank 0). With use_mc=False, the higher-xpts_predicted player must land in the top decile. Assert by inspecting which bucket each player's actual_pts feeds into via a single-player-per-decile setup.
    - test_calibration_analytical_fallback_when_mc_disabled: Same inputs as test_calibration_mode_mc_written_to_summary, but pre-seed prior cache with summary.mc_enabled=false. Assert result['summary']['calibration_mode'] == 'analytical' AND result['calibration']['by_position']['all'][i]['predicted_rate'] == bucket_mid for every bucket.
    - test_calibration_mc_coverage_threshold: Supply a merged_haul_lookup containing only 79% of total_merged_count (e.g. lookup with 79 entries, total_merged_count=100). With mc_enabled=true, assert calibration_mode == 'analytical' (below 80% gate). Then supply 80% coverage (80/100) and assert calibration_mode == 'mc'.
    - test_calibration_missing_haul_prob_defaults_zero: Build per_gw_rows where some player_ids are absent from merged_haul_lookup. Call _compute_calibration_data(per_gw_rows, use_mc=True, merged_haul_lookup={...subset}). Assert no NaN, no KeyError; missing players are sorted into the bottom decile (rank 9); the bottom decile's predicted_rate is finite and equals the mean of (0.0 for missing + their cohort).
    - test_calibration_mode_analytical_when_coverage_below_threshold: Edge case — mc_enabled=true but merged_haul_lookup is empty / total_merged_count=0. Assert coverage_pct evaluates to 0.0 (not divide-by-zero), use_mc=False, calibration_mode='analytical'.
    - test_calibration_empty_backtest_has_calibration_mode: Call compute_accuracy_backtest with finished_gws=0 (forces _empty_backtest path). Assert result['summary']['calibration_mode'] == 'analytical'.

    Helper additions: introduce `_seed_prior_cache(cache_dir, mc_enabled: bool)` that writes a minimal `accuracy_backtest.json` with `summary.mc_enabled` set, so tests can simulate flag state without depending on a prior real pipeline run. Use `tmp_path` fixture from pytest.

    The import at line 21 must be extended to also import `_compute_calibration_data` (it is private but available via direct import).
  </behavior>
  <action>
    Add 8 new test functions to `pipeline/tests/test_accuracy.py` at the end of the file (after the existing Phase 91/103 calibration tests, around line 820). All tests must fail initially with either AssertionError (because `calibration_mode` doesn't exist yet) or TypeError (because `_compute_calibration_data` doesn't accept `use_mc`/`merged_haul_lookup` yet).

    Extend the import line (line 21) to read: `from accuracy import compute_accuracy_backtest, build_predictions_snapshot, _compute_calibration_data, FORMULA_VERSION`.

    Add helper `_seed_prior_cache(cache_dir, mc_enabled: bool)` near the top of the file (after `_build_minimal_inputs`). It writes `{"summary": {"mc_enabled": mc_enabled, "xpts_hit_rate": 0.5}, "versions": []}` to `os.path.join(cache_dir, 'accuracy_backtest.json')`.

    Use `tmp_path` pytest fixture in tests that need cache state. Compute_accuracy_backtest takes `cache_dir=str(tmp_path)` plus the new `merged_haul_lookup` and `total_merged_count` kwargs.

    Run pytest with collect-only first to confirm tests collect, then run them and confirm ALL 8 fail with the expected error categories (TypeError on signature mismatch is acceptable RED; AssertionError on missing field is acceptable RED). Do NOT proceed to Task 2 until RED is confirmed.

    Test names exactly as specified in VALIDATION.md Wave 0 list, plus the new `test_calibration_empty_backtest_has_calibration_mode` to cover Pitfall 2 (`_empty_backtest` parity).
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && python3 -m pytest pipeline/tests/test_accuracy.py -k "calibration_mode or mc_path or analytical_fallback or missing_haul_prob or coverage_threshold or empty_backtest_has_calibration_mode" --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/tests/test_accuracy.py collects all 8 new tests (collection exit code 0)
    - Test names present in file (grep): `test_calibration_mode_mc_written_to_summary`, `test_calibration_mc_path_predicted_rate`, `test_calibration_mc_path_sort_order`, `test_calibration_analytical_fallback_when_mc_disabled`, `test_calibration_mc_coverage_threshold`, `test_calibration_missing_haul_prob_defaults_zero`, `test_calibration_mode_analytical_when_coverage_below_threshold`, `test_calibration_empty_backtest_has_calibration_mode`
    - Running the 8 new tests fails (exit code != 0) — this is the RED gate
    - Import line in test file includes `_compute_calibration_data` (grep `from accuracy import.*_compute_calibration_data`)
    - Helper `_seed_prior_cache` exists in file (grep `def _seed_prior_cache`)
    - All existing 34 calibration/accuracy tests still pass (no regression from adding new tests)
    - Tests collect with `python3 -m pytest pipeline/tests/test_accuracy.py --collect-only -q` exits 0
  </acceptance_criteria>
  <done>RED gate: 8 new test functions added with names matching VALIDATION.md, all 8 fail with TypeError or AssertionError. Existing tests still pass. Import statement extended to include `_compute_calibration_data`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MC path in pipeline/accuracy.py (_compute_calibration_data + compute_accuracy_backtest + _empty_backtest)</name>
  <files>pipeline/accuracy.py</files>
  <read_first>
    - pipeline/accuracy.py (full file — modifying _compute_calibration_data lines 496-571, compute_accuracy_backtest lines 121-411, _empty_backtest lines 437-493)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-CONTEXT.md (D-01 through D-07)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-PATTERNS.md (Pattern Assignments for accuracy.py — exact MC sort branch and bucket assembly snippets)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-RESEARCH.md (Common Pitfalls 1, 2, 5 — coverage denominator, _empty_backtest parity, bucket_total as divisor)
    - pipeline/tests/test_accuracy.py (the RED tests from Task 1 — to know the contract)
  </read_first>
  <action>
    Modify `pipeline/accuracy.py` to make the Task 1 RED tests turn GREEN. Three changes in one file:

    Change A — extend `_compute_calibration_data` signature and body (line 496):
    - New signature: `def _compute_calibration_data(per_gw_rows: dict, use_mc: bool = False, merged_haul_lookup: dict = None) -> dict:`
    - Add a new accumulator alongside `bucket_sum_predicted`/`bucket_sum_actual` (line 515 area): `bucket_haul_prob: dict = defaultdict(lambda: defaultdict(float))`.
    - In the per-GW loop, replace the unconditional `ranked = sorted(rows, key=lambda r: r['xpts_predicted'], reverse=True)` with a conditional: when `use_mc and merged_haul_lookup`, first set `row['_eff_hp'] = merged_haul_lookup.get(row['player_id'], 0.0)` for every row (D-06 missing-player rule), then sort by `_eff_hp` descending; else use the existing xpts_predicted sort. Inside the accumulation inner loop, when MC mode is active, also add `bucket_haul_prob[pk][decile] += row['_eff_hp']`.
    - In the bucket assembly (line 559-568), change the `'predicted_rate'` line so that in MC mode it computes `round(bucket_haul_prob[pos_key][d] / total, 4)` (using `bucket_total[pos_key][d]` as the divisor — Pitfall 5 resolution), otherwise keeps `bucket_mids[d]`. Keep `'bucket_mid': bucket_mids[d]` unchanged for backward compat. Preserve `bucket_sum_predicted` / `bucket_sum_actual` accumulators in MC mode (Open Question 2 resolution — keep existing semantics).

    Change B — extend `compute_accuracy_backtest` (line 121):
    - New signature appends two kwargs: `merged_haul_lookup: dict = None, total_merged_count: int = 0`.
    - After `mc_enabled = ...` is read from prior_cache (line 368), add:
      ```
      coverage_pct = (len(merged_haul_lookup) / total_merged_count) if (merged_haul_lookup and total_merged_count > 0) else 0.0
      use_mc = bool(mc_enabled and coverage_pct >= 0.80)
      calibration_mode = 'mc' if use_mc else 'analytical'
      ```
    - Change the `_compute_calibration_data` call (line 349) to pass the new kwargs: `_compute_calibration_data(per_gw_rows, use_mc=use_mc, merged_haul_lookup=merged_haul_lookup)`.
    - In the returned summary dict (line 393-406), add `'calibration_mode': calibration_mode,` immediately after the existing `'mc_enabled': mc_enabled,` line. Comment: `# Phase 109 MC-CAL-01: 'mc' when mc_enabled AND coverage >= 80%, else 'analytical'`.

    Change C — extend `_empty_backtest` (lines 437-493) to add `calibration_mode` to its summary dict:
    - Pitfall 2 fix: add `'calibration_mode': 'analytical',` immediately after the existing `'mc_enabled': mc_enabled,` line inside `_empty_backtest`'s returned summary dict. Comment: `# Phase 109 MC-CAL-01: no merged data in empty path, default analytical`.

    Do NOT rename the `merged_haul_lookup` parameter (keep the descriptive name per CONTEXT.md). Do NOT preserve `calibration_mode` across runs — it must be recomputed each call from current inputs (CONTEXT.md anti-pattern bullet 4).

    Run the new Task 1 tests after editing; they must turn GREEN. Run the full pipeline test suite — existing tests must still pass.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && python3 -m pytest pipeline/tests/test_accuracy.py -k "calibration_mode or mc_path or analytical_fallback or missing_haul_prob or coverage_threshold or empty_backtest_has_calibration_mode" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 8 new tests pass: `python3 -m pytest pipeline/tests/test_accuracy.py -k "calibration_mode or mc_path or analytical_fallback or missing_haul_prob or coverage_threshold or empty_backtest_has_calibration_mode" -x` exits 0
    - Full pipeline accuracy suite passes: `python3 -m pytest pipeline/tests/test_accuracy.py -q` exits 0 (existing 34 tests + 8 new = 42 passing)
    - Full pipeline suite passes: `python3 -m pytest pipeline/tests/ -q` exits 0 (no regressions in adjacent modules)
    - `_compute_calibration_data` signature accepts `use_mc` and `merged_haul_lookup` kwargs (grep: `def _compute_calibration_data\(per_gw_rows: dict,\s+use_mc: bool = False,\s+merged_haul_lookup`)
    - `compute_accuracy_backtest` signature accepts `merged_haul_lookup` and `total_merged_count` kwargs (grep: `merged_haul_lookup: dict = None,` AND `total_merged_count: int = 0`)
    - Summary dict in `compute_accuracy_backtest` contains `'calibration_mode': calibration_mode,` (grep)
    - `_empty_backtest` summary dict contains `'calibration_mode': 'analytical',` (grep)
    - `bucket_haul_prob` accumulator declared with `defaultdict(lambda: defaultdict(float))` (grep `bucket_haul_prob: dict = defaultdict`)
    - When `use_mc=True`, the MC sort branch sets `row['_eff_hp']` using `merged_haul_lookup.get(row['player_id'], 0.0)` (grep `merged_haul_lookup.get\(row\['player_id'\], 0\.0\)`)
    - `coverage_pct` derivation present (grep `coverage_pct = .*len\(merged_haul_lookup\)`)
    - `use_mc = bool(mc_enabled and coverage_pct >= 0.80)` present (grep `coverage_pct >= 0\.80`)
  </acceptance_criteria>
  <done>All 8 new MC tests GREEN. Existing 34 pipeline accuracy tests still GREEN. Full pipeline suite still passes. `_compute_calibration_data` and `compute_accuracy_backtest` signatures updated with new kwargs. `calibration_mode` written to summary in both `compute_accuracy_backtest` and `_empty_backtest`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire haul_lookup build and parameter pass in pipeline/run.py</name>
  <files>pipeline/run.py</files>
  <read_first>
    - pipeline/run.py lines 190-330 (MC_ENABLED constant at 194, merged construction at 214-226, compute_accuracy_backtest call at 325, print pattern at 208-212)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-CONTEXT.md (D-01 — exact build expression)
    - .planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-PATTERNS.md (pipeline/run.py section — lookup build + call site change)
    - pipeline/accuracy.py (the updated signature from Task 2 — to confirm kwargs)
  </read_first>
  <action>
    Modify `pipeline/run.py` in two places. The `merged` list is already in scope at line 214+ (post `merge_players` and post `compute_simulations`); the `compute_accuracy_backtest` call is at line 325.

    Change A — insert lookup build immediately before the call (between line 324's `print("Computing accuracy backtest...")` and line 325's call):
    ```
    haul_lookup = {p['id']: p['haul_prob'] for p in merged if p.get('haul_prob') is not None}
    total_merged_count = len(merged)
    print(f"Haul lookup coverage: {len(haul_lookup)}/{total_merged_count} players have haul_prob")
    ```

    Change B — extend the `compute_accuracy_backtest` call at line 325 to pass the new kwargs:
    ```
    backtest_data = compute_accuracy_backtest(
        summaries, finished_gws, bootstrap, fixtures,
        cache_dir=cache_dir,
        merged_haul_lookup=haul_lookup,
        total_merged_count=total_merged_count,
    )
    ```

    Do NOT change `MC_ENABLED = True` on line 194 or the `mc_enabled = MC_ENABLED` override on line 204 — those remain as-is from Phase 102.

    Run the pipeline run-time tests (`pipeline/tests/test_run.py`) to confirm no regression in run.py integration. The MC kwargs are optional (defaulted in `compute_accuracy_backtest`), so any test that calls `compute_accuracy_backtest` directly is unaffected.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx && python3 -m pytest pipeline/tests/test_run.py pipeline/tests/test_accuracy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline/tests/test_run.py passes (exit 0)
    - pipeline/tests/test_accuracy.py passes (exit 0)
    - Full pipeline suite passes: `python3 -m pytest pipeline/tests/ -q` exits 0
    - `haul_lookup` build expression present in run.py (grep: `haul_lookup = \{p\['id'\]: p\['haul_prob'\] for p in merged if p\.get\('haul_prob'\) is not None\}`)
    - `total_merged_count = len(merged)` present in run.py (grep)
    - Coverage print line present (grep: `Haul lookup coverage:`)
    - `compute_accuracy_backtest` call passes `merged_haul_lookup=haul_lookup` and `total_merged_count=total_merged_count` (grep both)
    - Python syntax check passes: `python3 -m py_compile pipeline/run.py` exits 0
    - No accidental regression to MC_ENABLED constant or mc_enabled override (grep `MC_ENABLED = True` still present; `mc_enabled = MC_ENABLED` still present at line 204)
  </acceptance_criteria>
  <done>run.py builds haul_lookup from merged list, passes it + total_merged_count to compute_accuracy_backtest, prints coverage line. test_run.py and test_accuracy.py both pass. No regression in surrounding pipeline code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Phase 109 makes no network calls, accepts no user input, introduces no auth surface. All modifications are pure transforms over in-memory dicts and a single cache file (`accuracy_backtest.json`) that is already in the trusted pipeline output tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering | accuracy_backtest.json cache file | accept | File is pipeline-owned output, written only by pipeline; no external write path. Existing FileNotFoundError/JSONDecodeError guards in `_read_existing_cache` handle malformed input by returning {} (cold-start) — same defensive pattern as Phase 90 and Phase 103. |
| T-109-02 | Information Disclosure | haul_prob values in accuracy_backtest.json | accept | haul_prob is already published in merged_players.json (Phase 102). No new exposure surface. |
| T-109-03 | Denial of Service | _compute_calibration_data with adversarial per_gw_rows | accept | Function is internal to pipeline; inputs are pipeline-built dicts from bootstrap+summaries. No untrusted input path. |

ASVS L1 categories applicable: None — no input validation surface, no authn/authz surface, no error handling surface beyond existing cache-read guards.
</threat_model>

<verification>
- All 8 new MC calibration tests pass (Task 1 RED → Task 2 GREEN)
- Existing 34 accuracy tests continue to pass (no regression)
- Full pipeline suite passes (`python3 -m pytest pipeline/tests/ -q` exits 0)
- run.py syntax-checks cleanly (`python3 -m py_compile pipeline/run.py` exits 0)
- `_compute_calibration_data` signature accepts `use_mc` and `merged_haul_lookup`
- `compute_accuracy_backtest` signature accepts `merged_haul_lookup` and `total_merged_count`
- `calibration_mode` written to both `compute_accuracy_backtest` and `_empty_backtest` summary dicts
- haul_lookup correctly constructed in run.py with the exact dict-comprehension form from CONTEXT.md D-01
</verification>

<success_criteria>
- `accuracy_backtest.json.summary.calibration_mode` field is present and equals `'mc'` when MC_ENABLED=True AND ≥80% of merged players have haul_prob (post first run after Phase 109 merges)
- Per-bucket `predicted_rate` reflects mean(haul_prob) in MC mode, NOT bucket_mid
- Players missing haul_prob are absorbed into the bottom decile with `effective_haul_prob = 0.0`; no NaN or KeyError
- Full pipeline test suite green
- Production daily pipeline run after Phase 109 ships writes `calibration_mode: 'mc'` (STATE.md Phase 109 verification item)
</success_criteria>

<output>
After completion, create `.planning/phases/109-mc-enabled-calibration-v1-19-see-planning-milestones-v1-19-r/109-01-SUMMARY.md`
</output>
