---
phase: 23-transfer-output-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/planner/TransferPlanTable.tsx
  - src/components/planner/ChipToggle.tsx
  - src/components/planner/plan-helpers.ts
  - tests/components/planner/plan-helpers.test.ts
autonomous: true
requirements: [PLAN-05, PLAN-07]

must_haves:
  truths:
    - "TransferPlanTable renders one row per PlanStep with GW, Chip, Out, In, Hit, Gain columns"
    - "Hold rows display when no transfer is suggested for a GW"
    - "DGW and BGW badges appear on rows where fixture count is >= 2 or === 0"
    - "Plan value headline shows total net gain across all steps"
    - "ChipToggle renders 4 buttons per GW row with toggle on/off behavior"
    - "Chip labels display human-readable names (Bench Boost, Triple Captain)"
    - "Unconfirmed fixture rows show asterisk and italic gain value"
  artifacts:
    - path: "src/components/planner/TransferPlanTable.tsx"
      provides: "Transfer table display component"
      min_lines: 80
    - path: "src/components/planner/ChipToggle.tsx"
      provides: "Per-GW chip toggle control"
      min_lines: 30
    - path: "src/components/planner/plan-helpers.ts"
      provides: "Pure helper functions: computePlanValue, chipLabel, formatGain"
      exports: ["computePlanValue", "CHIP_LABELS", "formatGain"]
    - path: "tests/components/planner/plan-helpers.test.ts"
      provides: "Unit tests for pure helper functions"
  key_links:
    - from: "src/components/planner/TransferPlanTable.tsx"
      to: "src/components/planner/plan-helpers.ts"
      via: "import { computePlanValue, CHIP_LABELS, formatGain }"
      pattern: "import.*plan-helpers"
    - from: "src/components/planner/TransferPlanTable.tsx"
      to: "src/components/planner/ChipToggle.tsx"
      via: "import { ChipToggle }"
      pattern: "import.*ChipToggle"
---

<objective>
Build the TransferPlanTable and ChipToggle components plus pure helper functions with tests. These components receive PlanResult and scoredPlayers as props and render the transfer-by-transfer table with chip toggle controls, DGW/BGW badges, hold rows, and plan value headline.

Purpose: Delivers PLAN-05 (transfer table) and PLAN-07 (chip toggle visibility) as self-contained display components ready to wire into PlannerTab.
Output: TransferPlanTable.tsx, ChipToggle.tsx, plan-helpers.ts, plan-helpers.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-transfer-output-table/23-RESEARCH.md
@.planning/phases/23-transfer-output-table/23-UI-SPEC.md

@src/components/planner/PlannerTab.tsx
@src/lib/types.ts
@src/lib/planning-engine.ts

<interfaces>
<!-- Key types the executor needs. Extracted from src/lib/types.ts -->

From src/lib/types.ts:
```typescript
export type PlannerChip = 'wildcard' | 'freehit' | 'bboost' | '3xc' | null

export interface ScoredTransfer {
  sellId: number
  buyId: number
  gwScore: number
  lookAheadScore: number
  totalScore: number
  hitCost: number
  netGain: number         // totalScore + hitCost
  affordable: boolean
}

export interface PlanStep extends GWStep {
  scoredTransfers: ScoredTransfer[]  // top candidates considered
  squadAfter: number[]               // player IDs in squad after this step
  unconfirmedFixtures: boolean       // true if no fixture data exists for this GW
}

export interface PlanResult {
  steps: PlanStep[]
  horizon: PlannerHorizon
  startingGw: number
}

export interface ScoredPlayer extends MergedPlayer {
  gem_score: number
  // ... inherits web_name, id, now_cost, fixtures, etc. from MergedPlayer
}
```

From src/lib/planning-engine.ts:
```typescript
export function fixtureCountForGw(player: ScoredPlayer, targetGw: number): number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helper functions with tests</name>
  <files>src/components/planner/plan-helpers.ts, tests/components/planner/plan-helpers.test.ts</files>
  <read_first>
    - src/lib/types.ts (PlanResult, PlanStep, ScoredTransfer, PlannerChip type definitions)
    - tests/lib/planning-engine.test.ts (existing test pattern for vitest in this project)
  </read_first>
  <behavior>
    - computePlanValue([step with netGain 5.2, step with netGain -1.0]) returns 4.2
    - computePlanValue([step with empty scoredTransfers]) returns 0
    - computePlanValue([]) returns 0
    - CHIP_LABELS['wildcard'] === 'Wildcard'
    - CHIP_LABELS['freehit'] === 'Free Hit'
    - CHIP_LABELS['bboost'] === 'Bench Boost'
    - CHIP_LABELS['3xc'] === 'Triple Captain'
    - formatGain(5.23) returns '+5.2 pts'
    - formatGain(-2.0) returns '\u22122.0 pts' (U+2212 minus sign)
    - formatGain(0) returns '+0.0 pts'
  </behavior>
  <action>
    Create `src/components/planner/plan-helpers.ts` with three exports:

    1. `computePlanValue(steps: PlanStep[]): number` — returns `steps.reduce((sum, step) => sum + (step.scoredTransfers[0]?.netGain ?? 0), 0)`

    2. `CHIP_LABELS: Record<string, string>` — map of chip codes to human-readable labels:
       - `wildcard` -> `'Wildcard'`
       - `freehit` -> `'Free Hit'`
       - `bboost` -> `'Bench Boost'`
       - `'3xc'` -> `'Triple Captain'`

    3. `formatGain(value: number): string` — formats a numeric gain as a signed string with 1 decimal place. Positive values get `+` prefix. Negative values use U+2212 minus sign (not hyphen). Always appends ` pts`. Examples: `+5.2 pts`, `\u22122.0 pts`, `+0.0 pts`.

    Import PlanStep from `@/lib/types`.

    Then create `tests/components/planner/plan-helpers.test.ts` with vitest tests covering all behaviors listed above. Use `import { describe, it, expect } from 'vitest'` pattern matching existing project tests. Create mock PlanStep objects with only the fields needed (scoredTransfers array with netGain).
  </action>
  <verify>
    <automated>npx vitest run tests/components/planner/plan-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - plan-helpers.ts contains `export function computePlanValue(`
    - plan-helpers.ts contains `export const CHIP_LABELS`
    - plan-helpers.ts contains `export function formatGain(`
    - plan-helpers.ts contains `import type { PlanStep }` from `@/lib/types`
    - CHIP_LABELS maps 'bboost' to 'Bench Boost' and '3xc' to 'Triple Captain'
    - formatGain uses `\u2212` for negative values (not ASCII hyphen-minus)
    - plan-helpers.test.ts contains at least 8 test cases (it() blocks)
    - `npx vitest run tests/components/planner/plan-helpers.test.ts` exits 0
  </acceptance_criteria>
  <done>All pure helper tests pass; computePlanValue, CHIP_LABELS, and formatGain are exported and tested</done>
</task>

<task type="auto">
  <name>Task 2: TransferPlanTable and ChipToggle components</name>
  <files>src/components/planner/TransferPlanTable.tsx, src/components/planner/ChipToggle.tsx</files>
  <read_first>
    - src/components/planner/plan-helpers.ts (just created — imports needed)
    - src/lib/types.ts (PlanResult, PlanStep, ScoredTransfer, PlannerChip, ScoredPlayer)
    - src/lib/planning-engine.ts (fixtureCountForGw export — lines 21-23)
    - src/components/planner/HorizonSelector.tsx (active/inactive button styling pattern to replicate)
    - src/components/fixtures/FixtureBadges.tsx (DGW badge violet colour pattern)
    - .planning/phases/23-transfer-output-table/23-UI-SPEC.md (full visual spec)
  </read_first>
  <action>
    **ChipToggle.tsx** — Create `src/components/planner/ChipToggle.tsx`:
    - `'use client'` directive at top
    - Props: `{ activeChip: PlannerChip; onToggle: (chip: PlannerChip) => void }`
    - Render 4 buttons in a `role="group"` div with `aria-label="Chip for GW {N}"` (pass gw as prop too: `gw: number`)
    - Each button shows the label from CHIP_LABELS (import from `./plan-helpers`)
    - Active button style: `bg-zinc-900 dark:bg-white text-white dark:text-zinc-900`
    - Inactive button style: `bg-white dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 hover:bg-zinc-50 dark:hover:bg-zinc-700`
    - Each button: `min-h-[44px] px-2 py-1 text-xs font-semibold rounded`
    - Each button: `aria-pressed={activeChip === chipCode}`
    - onClick: call `onToggle(chipCode)` — the parent handles the toggle-off logic (same chip clears)
    - Chip codes to iterate: `['wildcard', 'freehit', 'bboost', '3xc'] as const`

    **TransferPlanTable.tsx** — Create `src/components/planner/TransferPlanTable.tsx`:
    - `'use client'` directive at top
    - Props interface:
      ```
      interface TransferPlanTableProps {
        planResult: PlanResult
        scoredPlayers: ScoredPlayer[]
        onChipToggle: (stepIndex: number, chip: PlannerChip) => void
      }
      ```
    - Build `playerMap` via `useMemo(() => new Map(scoredPlayers.map(p => [p.id, p])), [scoredPlayers])`
    - Compute `totalNetGain` via `computePlanValue(planResult.steps)` from `./plan-helpers`
    - Import `fixtureCountForGw` from `@/lib/planning-engine`

    **Plan value headline** above table:
    - `<p aria-live="polite" className="text-xl font-semibold text-zinc-900 dark:text-zinc-100">Plan value: {formatGain(totalNetGain)}</p>`

    **Table structure** using semantic HTML:
    - `<table className="w-full text-sm">` with `<thead>` and `<tbody>`
    - `<th scope="col">` for each header: GW, Chip, Out, In, Hit, Gain
    - Header row: `text-sm font-medium text-zinc-500 dark:text-zinc-400 text-left`
    - Chip column header hidden on mobile: `className="hidden sm:table-cell"`

    **Per-row rendering** — iterate `planResult.steps.map((step, i) => ...)`:
    - **GW cell**: Show `GW{step.gw}`. Check DGW/BGW: if any player in `step.transfersIn` has `fixtureCountForGw(playerMap.get(id)!, step.gw) >= 2`, show `<span className="text-xs font-semibold text-violet-700 dark:text-violet-400" aria-label="Double gameweek">DGW</span>`. If `fixtureCountForGw` returns 0 for the buy player (or step has no transfer and `step.unconfirmedFixtures`), show `<span className="text-xs font-semibold text-amber-600 dark:text-amber-400" aria-label="Blank gameweek">BGW</span>`.
    - **Chip cell** (desktop only, hidden sm:table-cell): Render `<ChipToggle activeChip={step.chip} gw={step.gw} onToggle={(chip) => onChipToggle(i, chip)} />`
    - **Mobile chip row**: On small screens, render ChipToggle in a separate `<tr className="sm:hidden">` row spanning all columns below the main row.
    - **Transfer cells (Out / In)**:
      - If `step.transfersIn.length > 0`: Out = `playerMap.get(step.transfersOut[0])?.web_name ?? '—'`, In = `playerMap.get(step.transfersIn[0])?.web_name ?? '—'`
      - If `step.transfersIn.length === 0` (hold): Out cell spans 2 cols with text `"Hold"` in `text-zinc-400 dark:text-zinc-500`, and a `<span className="block text-xs">No profitable transfer this gameweek</span>` below
    - **Hit cell**: `step.hitCost === 0 ? '0' : step.hitCost + ' pts'`. When `step.hitCost < 0`: `className="text-red-700 dark:text-red-300"`
    - **Gain cell**: Use `formatGain(step.scoredTransfers[0]?.netGain ?? 0)`. When `step.unconfirmedFixtures === true`: wrap in italic with `text-zinc-400 dark:text-zinc-500` and append `<abbr title="Fixtures not yet confirmed for this gameweek">*</abbr>`. When no transfer (hold): show `"—"` (em dash).

    **Cell padding**: `px-2 py-2 sm:px-4` on all `<td>` and `<th>` elements.
    **Row borders**: `border-b border-zinc-200 dark:border-zinc-700` on each `<tr>`.

    All colour classes MUST include explicit `dark:` variants. No `text-black` or `bg-white` without dark counterpart.
  </action>
  <verify>
    <automated>npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TransferPlanTable.tsx starts with `'use client'`
    - TransferPlanTable.tsx contains `interface TransferPlanTableProps`
    - TransferPlanTable.tsx contains `const playerMap = useMemo`
    - TransferPlanTable.tsx contains `aria-live="polite"`
    - TransferPlanTable.tsx contains `<table` and `<thead>` and `<th scope="col"`
    - TransferPlanTable.tsx contains `text-violet-700 dark:text-violet-400` (DGW badge)
    - TransferPlanTable.tsx contains `text-amber-600 dark:text-amber-400` (BGW badge)
    - TransferPlanTable.tsx contains `text-red-700 dark:text-red-300` (hit cost)
    - TransferPlanTable.tsx contains `import { ChipToggle }` from `./ChipToggle`
    - TransferPlanTable.tsx contains `import { computePlanValue, formatGain }` from `./plan-helpers`
    - TransferPlanTable.tsx contains `import { fixtureCountForGw }` from `@/lib/planning-engine`
    - ChipToggle.tsx starts with `'use client'`
    - ChipToggle.tsx contains `role="group"`
    - ChipToggle.tsx contains `aria-pressed`
    - ChipToggle.tsx contains `min-h-[44px]`
    - ChipToggle.tsx contains `import { CHIP_LABELS }` from `./plan-helpers`
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>TransferPlanTable renders a semantic HTML table with plan value headline, per-GW rows with DGW/BGW badges, hold rows, hit cost styling, gain formatting, and ChipToggle buttons. Build passes with no type errors.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/components/planner/plan-helpers.test.ts` — all helper tests green
- `npx vitest run` — full test suite green (222+ tests)
- `npx next build` — no type errors, no build failures
</verification>

<success_criteria>
- TransferPlanTable.tsx exists and exports a component accepting PlanResult + ScoredPlayer[] + onChipToggle callback
- ChipToggle.tsx exists and exports a component with 4 chip buttons, toggle behavior, accessibility attributes
- plan-helpers.ts exports computePlanValue, CHIP_LABELS, formatGain — all tested
- All existing tests still pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/23-transfer-output-table/23-01-SUMMARY.md`
</output>
