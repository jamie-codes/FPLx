---
phase: 18-dark-mode
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/gem-table/GemTable.tsx
  - src/components/gem-table/PositionFilter.tsx
  - src/components/gem-table/GwToggle.tsx
  - src/components/transfers/TransferPanel.tsx
  - src/components/squad/SquadView.tsx
  - src/components/nav/MobileNav.tsx
  - src/app/page.tsx
autonomous: true
requirements: [DARK-03]
must_haves:
  truths:
    - "GemTable filter bar, thead, and sticky player columns have dark backgrounds in dark mode — no white bars float over the dark page"
    - "GemTable alternating rows and hover states use dark-appropriate colours"
    - "TransferPanel cards, badges, inputs, and error/success panels are readable in dark mode"
    - "SquadView sticky player column and text colours are dark-mode aware"
    - "MobileNav has a dark background in dark mode — no white bar at bottom"
    - "Desktop tab strip in page.tsx has dark-mode text and border colours"
  artifacts:
    - path: "src/components/gem-table/GemTable.tsx"
      provides: "Dark variants on filter bar, thead, sticky cols, rows, hover, back-to-top"
      contains: "dark:bg-zinc-900"
    - path: "src/components/transfers/TransferPanel.tsx"
      provides: "Dark variants on cards, badges, inputs, borders, error/success panels"
      contains: "dark:bg-zinc-900"
    - path: "src/components/squad/SquadView.tsx"
      provides: "Dark variants on sticky columns, text, rows"
      contains: "dark:bg-zinc-900"
    - path: "src/components/nav/MobileNav.tsx"
      provides: "Dark background and text on fixed bottom nav"
      contains: "dark:bg-zinc-900"
    - path: "src/app/page.tsx"
      provides: "Dark variants on desktop tab strip"
      contains: "dark:border-zinc-700"
  key_links:
    - from: "src/app/globals.css"
      to: "all components"
      via: "@custom-variant dark class selector"
      pattern: "dark:"
---

<objective>
Add dark: Tailwind variants to the high-complexity components: GemTable (with PositionFilter, GwToggle), TransferPanel, SquadView, MobileNav, and page.tsx tab strip.

Purpose: These are the most colour-dense and visually prominent components. Theming them addresses the bulk of DARK-03 (no illegible text, sufficient contrast).
Output: All 7 files updated with systematic dark: class additions per the research colour audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dark-mode/18-RESEARCH.md
@.planning/phases/18-dark-mode/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark variants on GemTable, PositionFilter, and GwToggle</name>
  <files>src/components/gem-table/GemTable.tsx, src/components/gem-table/PositionFilter.tsx, src/components/gem-table/GwToggle.tsx</files>
  <read_first>
    src/components/gem-table/GemTable.tsx
    src/components/gem-table/PositionFilter.tsx
    src/components/gem-table/GwToggle.tsx
    .planning/phases/18-dark-mode/18-RESEARCH.md (Pitfall 1, Pitfall 3, Pitfall 4, Color Token Audit section)
  </read_first>
  <action>
    **GemTable.tsx — apply these dark: variants to every matching class:**

    Sticky/fixed backgrounds (Pitfall 1 — critical):
    - Every `bg-white` on sticky/fixed elements -> add `dark:bg-zinc-900`
    - This includes: filter bar div (sticky), thead (sticky), sticky player column in thead, sticky player column in tbody rows

    Row colours (Pitfall 3):
    - `even:bg-gray-50` -> add `dark:even:bg-zinc-800`

    Hover states (Pitfall 4):
    - `hover:bg-blue-50` -> add `dark:hover:bg-zinc-700`

    Expanded row:
    - `bg-blue-50` -> add `dark:bg-blue-950`

    Text colours:
    - `text-zinc-900` -> add `dark:text-zinc-100`
    - `text-zinc-700` -> add `dark:text-zinc-300`
    - `text-zinc-600` -> add `dark:text-zinc-400`
    - `text-zinc-500` -> add `dark:text-zinc-400`
    - `text-gray-700` -> add `dark:text-zinc-300`
    - `text-gray-500` -> add `dark:text-zinc-400`

    Borders:
    - `border-zinc-200` -> add `dark:border-zinc-700`
    - `border-zinc-300` -> add `dark:border-zinc-600`
    - `border-zinc-100` -> add `dark:border-zinc-800`
    - `border-gray-200` -> add `dark:border-zinc-700`

    Back-to-top button:
    - `bg-zinc-900` -> add `dark:bg-zinc-100`
    - `text-white` on the button -> add `dark:text-zinc-900`

    **PositionFilter.tsx:**
    - Active pill `bg-blue-600` -> add `dark:bg-blue-500` (slightly lighter for dark bg)
    - Inactive pill `bg-gray-100` -> add `dark:bg-zinc-700`
    - Inactive text `text-gray-700` -> add `dark:text-zinc-300`
    - `hover:bg-gray-200` (if present) -> add `dark:hover:bg-zinc-600`

    **GwToggle.tsx:**
    - Active button `bg-zinc-900 text-white` -> add `dark:bg-white dark:text-zinc-900` (invert for dark mode)
    - Inactive button `bg-white` -> add `dark:bg-zinc-800`
    - Inactive text `text-zinc-700` or `text-zinc-600` -> add `dark:text-zinc-300`
    - Border `border-zinc-200` or `border-zinc-300` -> add corresponding `dark:border-zinc-700` or `dark:border-zinc-600`

    Do NOT change component logic, props, or structure — only add dark: class variants alongside existing classes.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && grep -c "dark:" src/components/gem-table/GemTable.tsx src/components/gem-table/PositionFilter.tsx src/components/gem-table/GwToggle.tsx && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - GemTable.tsx: every `bg-white` on a sticky/fixed element has a corresponding `dark:bg-zinc-900`
    - GemTable.tsx: `even:bg-gray-50` rows also have `dark:even:bg-zinc-800`
    - GemTable.tsx: `hover:bg-blue-50` also has `dark:hover:bg-zinc-700`
    - GemTable.tsx: grep for `bg-white` without `dark:bg-` on same element returns 0 matches on sticky elements
    - PositionFilter.tsx: contains `dark:bg-zinc-700` (inactive pill dark bg)
    - GwToggle.tsx: contains `dark:bg-white` (active toggle inversion)
    - `npm test` passes
  </acceptance_criteria>
  <done>GemTable, PositionFilter, and GwToggle all have dark: variants; no white bars float over dark backgrounds; alternating rows and hover states are dark-appropriate</done>
</task>

<task type="auto">
  <name>Task 2: Dark variants on TransferPanel, SquadView, MobileNav, and page.tsx tabs</name>
  <files>src/components/transfers/TransferPanel.tsx, src/components/squad/SquadView.tsx, src/components/nav/MobileNav.tsx, src/app/page.tsx</files>
  <read_first>
    src/components/transfers/TransferPanel.tsx
    src/components/squad/SquadView.tsx
    src/components/nav/MobileNav.tsx
    src/app/page.tsx
    .planning/phases/18-dark-mode/18-RESEARCH.md (Pitfall 1, Pitfall 2, Pitfall 5, Color Token Audit section)
  </read_first>
  <action>
    **TransferPanel.tsx — the most colour-dense component:**

    Backgrounds:
    - `bg-zinc-50` (cards) -> add `dark:bg-zinc-800`
    - `bg-white` (if any) -> add `dark:bg-zinc-900`
    - `bg-blue-50` -> add `dark:bg-blue-950`
    - `bg-green-50` -> add `dark:bg-green-950`
    - `bg-red-50` -> add `dark:bg-red-950`
    - `bg-amber-50` -> add `dark:bg-amber-950`
    - `bg-green-100` -> add `dark:bg-green-900`
    - `bg-red-100` -> add `dark:bg-red-900`
    - `bg-blue-600` (login button) -> add `dark:bg-blue-500`

    Text in badges/panels:
    - `text-green-800` -> add `dark:text-green-200`
    - `text-red-700` -> add `dark:text-red-300`
    - `text-amber-800` -> add `dark:text-amber-200`
    - `text-blue-800` -> add `dark:text-blue-200`
    - `text-zinc-900` -> add `dark:text-zinc-100`
    - `text-zinc-700` -> add `dark:text-zinc-300`
    - `text-zinc-600` -> add `dark:text-zinc-400`
    - `text-zinc-500` -> add `dark:text-zinc-400`

    Borders (Pitfall 5):
    - `border-zinc-200` -> add `dark:border-zinc-700`
    - `border-zinc-300` (inputs) -> add `dark:border-zinc-600`

    Input fields:
    - Any `bg-white` on inputs -> add `dark:bg-zinc-800`

    **SquadView.tsx:**

    Sticky player columns:
    - `bg-white` on sticky elements -> add `dark:bg-zinc-900`

    Text:
    - `text-zinc-900` -> add `dark:text-zinc-100`
    - `text-zinc-700` -> add `dark:text-zinc-300`
    - `text-zinc-600` -> add `dark:text-zinc-400`
    - `text-zinc-500` -> add `dark:text-zinc-400`

    Borders:
    - `border-zinc-200` -> add `dark:border-zinc-700`

    Row colours:
    - `even:bg-gray-50` or `even:bg-zinc-50` -> add `dark:even:bg-zinc-800`
    - `hover:bg-blue-50` -> add `dark:hover:bg-zinc-700`

    Note: StatusBadge dots (`bg-green-500`, `bg-amber-400`, `bg-red-500`) are solid semantic colours — leave unchanged per research.

    **MobileNav.tsx (Pitfall 1 — fixed element):**
    - `bg-white` -> add `dark:bg-zinc-900`
    - `border-t` with `border-zinc-200` (or similar) -> add `dark:border-zinc-700`
    - Active tab text colour (likely `text-zinc-900` or `text-blue-600`) -> add appropriate dark variant
    - Inactive tab text `text-zinc-500` or `text-zinc-400` -> add `dark:text-zinc-400` or `dark:text-zinc-500`

    **page.tsx tab strip:**
    - Tab border strip `border-zinc-200` -> add `dark:border-zinc-700`
    - Active tab `border-zinc-900 text-zinc-900` -> add `dark:border-white dark:text-white`
    - Inactive tab `text-zinc-500 hover:text-zinc-700` -> add `dark:text-zinc-400 dark:hover:text-zinc-200`

    Do NOT change component logic, props, or structure — only add dark: class variants.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && grep -c "dark:" src/components/transfers/TransferPanel.tsx src/components/squad/SquadView.tsx src/components/nav/MobileNav.tsx src/app/page.tsx && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - TransferPanel.tsx: contains `dark:bg-zinc-800` (card backgrounds)
    - TransferPanel.tsx: contains `dark:border-zinc-600` (input borders — Pitfall 5)
    - SquadView.tsx: every `bg-white` on sticky has `dark:bg-zinc-900`
    - MobileNav.tsx: `bg-white` replaced with `bg-white dark:bg-zinc-900`
    - page.tsx: desktop tab strip border has `dark:border-zinc-700`
    - page.tsx: active tab has `dark:text-white`
    - `npm test` passes
  </acceptance_criteria>
  <done>TransferPanel, SquadView, MobileNav, and page.tsx tabs all have dark: variants; no white floating bars; badges readable in dark mode; input borders visible</done>
</task>

</tasks>

<verification>
- `grep -c "dark:" src/components/gem-table/GemTable.tsx` returns >= 10
- `grep -c "dark:" src/components/transfers/TransferPanel.tsx` returns >= 15
- `grep "bg-white" src/components/nav/MobileNav.tsx` also contains `dark:bg-zinc-900` on same line
- `npm test` passes
</verification>

<success_criteria>
- All 7 high-complexity component files have systematic dark: variants
- No bg-white on sticky/fixed elements without a dark: counterpart
- Badge colours use inverted dark palette (e.g., green-100 -> dark:green-900)
- Input borders visible in dark mode (dark:border-zinc-600)
- Alternating row colours distinguish in dark mode
- Hover states use dark-appropriate colours
- Desktop tab strip readable in dark mode
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-dark-mode/18-02-SUMMARY.md`
</output>
