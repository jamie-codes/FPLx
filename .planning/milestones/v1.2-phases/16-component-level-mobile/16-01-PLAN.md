---
phase: 16-component-level-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transfers/TransferPanel.tsx
  - src/components/captaincy/CaptaincyPanel.tsx
autonomous: true
requirements:
  - MOB-COMP-01
  - MOB-COMP-02
  - MOB-COMP-03

must_haves:
  truths:
    - "Transfer suggestion cards show player names + badges on row 1 and stats on row 2 on mobile without text overflow"
    - "Login/token form inputs stack vertically with full-width on mobile"
    - "Captaincy panel renders as a 2-column card grid on mobile with all data visible without truncation"
  artifacts:
    - path: "src/components/transfers/TransferPanel.tsx"
      provides: "Mobile-responsive transfer cards and login form"
      contains: "max-sm:"
    - path: "src/components/captaincy/CaptaincyPanel.tsx"
      provides: "Mobile-responsive captaincy card grid"
      contains: "grid-cols-2"
  key_links:
    - from: "src/components/transfers/TransferPanel.tsx"
      to: "transfer card layout"
      via: "Tailwind responsive classes"
      pattern: "max-sm:flex-col"
    - from: "src/components/captaincy/CaptaincyPanel.tsx"
      to: "captaincy grid layout"
      via: "Tailwind responsive classes"
      pattern: "sm:flex"
---

<objective>
Make transfer suggestion cards, the login/token form, and the captaincy panel usable on mobile phones.

Purpose: These three components currently use horizontal flex layouts that overflow or cramp on 375px screens. This plan restructures them with responsive Tailwind classes so mobile users can read all content without horizontal scrolling or text truncation.

Output: Two modified component files with mobile-responsive layouts; desktop appearance unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/transfers/TransferPanel.tsx
@src/components/captaincy/CaptaincyPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transfer card 2-row mobile layout + login form vertical stacking</name>
  <files>src/components/transfers/TransferPanel.tsx</files>
  <read_first>src/components/transfers/TransferPanel.tsx</read_first>
  <action>
This task is CSS-only (Tailwind class changes in JSX). No Next.js APIs, hooks, or runtime features are introduced, so no Next.js docs review is required per AGENTS.md.

**Transfer suggestion cards (MOB-COMP-01):** Restructure the transfer card JSX (both the main suggestions list around line 296 and the 2-transfer combo around line 359). Currently each card has two divs: a `text-sm` line with "Sell X -> Buy Y" and a `text-xs` stats line. On mobile, the first line wraps badly because sell player, risk badge, gem score, price trends, arrow, buy player, gem score, and price trends are all inline.

Restructure each card's inner content:

Row 1 (players + badges): Create a div with classes `flex flex-wrap items-center gap-x-2 gap-y-1`. Inside:
- "Sell" label + sell player `web_name` (font-medium) + MinsRiskBadge
- Arrow separator: use `&rarr;` wrapped in a span
- "Buy" label + buy player `web_name` (font-medium)
- Move the gem scores `(X.XX)` spans to stay after each player name

Row 2 (stats): Keep the existing `text-xs text-zinc-500` div but remove the price trend spans from row 1 — they add clutter on mobile. The price trend spans (cost_change_event, cost_change_start) should move into the stats row or be hidden on mobile with `hidden sm:inline`. Use `hidden sm:inline` on the season price trend spans (`cost_change_start`) to hide them on mobile only — keep the GW price trend (`cost_change_event`) visible as it's decision-relevant.

Row 3 (budget badge): Keep the Affordable/Over budget badge div as-is.

Apply identical restructuring to both the main suggestions `.map()` block (around line 293) and the 2-transfer combo `.map()` block (around line 359). They share the same card structure.

**Login form vertical stacking (MOB-COMP-02):**

1. Team ID form (line ~111): Change `className="flex gap-2 items-end flex-wrap"` to `className="flex flex-col sm:flex-row gap-2 sm:items-end"`. Make the team ID input full-width on mobile: change `w-40` to `w-full sm:w-40`. Make the free transfers input full-width on mobile: change `w-20` to `w-full sm:w-20`. Make the "Load Squad" button full-width on mobile: add `w-full sm:w-auto`.

2. Token form (line ~177): Change the inner `div className="flex gap-2 items-start flex-wrap"` to `className="flex flex-col sm:flex-row gap-2 sm:items-start"`. The token input already has `flex-1 min-w-48` — change to `w-full sm:flex-1 sm:min-w-48` so it's full-width on mobile. The "Save token" and "Cancel" buttons: add `w-full sm:w-auto` to the Save button. Keep Cancel as inline text link.

Note: There is no email field in this form (it's a bearer token paste form). The requirement mentions `inputMode="email"` but the team ID field already correctly has `inputMode="numeric"` and the token field is a text paste field — both are correct as-is. Do NOT change any inputMode values.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && npx next build 2>&1 | tail -5 && echo "--- grep checks ---" && echo "flex-col sm:flex-row count:" && grep -c "flex-col sm:flex-row" src/components/transfers/TransferPanel.tsx && test $(grep -c "flex-col sm:flex-row" src/components/transfers/TransferPanel.tsx) -ge 2 && echo "w-full sm:w-40 count:" && grep -c "w-full sm:w-40" src/components/transfers/TransferPanel.tsx && test $(grep -c "w-full sm:w-40" src/components/transfers/TransferPanel.tsx) -ge 1 && echo "w-full sm:w-auto count:" && grep -c "w-full sm:w-auto" src/components/transfers/TransferPanel.tsx && test $(grep -c "w-full sm:w-auto" src/components/transfers/TransferPanel.tsx) -ge 1 && echo "hidden sm:inline count:" && grep -c "hidden sm:inline" src/components/transfers/TransferPanel.tsx && test $(grep -c "hidden sm:inline" src/components/transfers/TransferPanel.tsx) -ge 2 && echo "flex-wrap count:" && grep -c "flex-wrap" src/components/transfers/TransferPanel.tsx && test $(grep -c "flex-wrap" src/components/transfers/TransferPanel.tsx) -ge 2 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>Transfer suggestion cards show structured 2-row layout on mobile with player names and badges on row 1, stats on row 2, no awkward wrapping. Login/token form stacks vertically on mobile with full-width inputs. Desktop layout unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Captaincy panel 2-column card grid on mobile</name>
  <files>src/components/captaincy/CaptaincyPanel.tsx</files>
  <read_first>src/components/captaincy/CaptaincyPanel.tsx</read_first>
  <action>
**Captaincy panel mobile grid (MOB-COMP-03):** Currently renders as a vertical list of `flex items-center gap-3` rows where rank, name, team, fixture, pts, type badge, and risk badge are all inline. On a 375px screen this overflows.

1. Change the outer container from `className="space-y-2"` to `className="grid grid-cols-2 gap-2 sm:grid-cols-1 sm:gap-2"`. This gives a 2-column card grid on mobile and reverts to a single-column list on desktop.

2. Each candidate card: Change from `className="rounded border border-zinc-100 bg-zinc-50 px-3 py-2 flex items-center gap-3"` to `className="rounded border border-zinc-100 bg-zinc-50 px-3 py-2 flex flex-col gap-1 sm:flex-row sm:items-center sm:gap-3"`. This stacks content vertically inside each card on mobile, horizontal on desktop.

3. Inside each card, restructure content for mobile readability:

   - Rank + Player name row: Wrap the rank span and player name span in a `div className="flex items-center gap-1.5"`. The rank span keeps `text-sm text-zinc-400 w-4 shrink-0`. The player name keeps `text-sm font-medium text-zinc-900` but remove `flex-1` (it's now in a flex-col context on mobile).

   - Team + Fixture row: Wrap the team short name span and fixture span in a `div className="flex items-center gap-2 text-xs text-zinc-500"`. Move the team `text-sm` to `text-xs` and the fixture keeps `text-xs text-zinc-400 whitespace-nowrap`.

   - Projected pts: Keep `text-sm text-zinc-700 whitespace-nowrap` on the pts span.

   - Badges row: Wrap CaptainTypeBadge and MinsRiskBadge in a `div className="flex items-center gap-1.5"`.

   On desktop (`sm:`), the parent is `sm:flex-row sm:items-center` so all these inner divs flow horizontally as before. On mobile, they stack vertically within each card.

4. For the desktop single-column list to look identical to current: add `sm:flex-1` to the player name span so it pushes remaining items right on desktop. Add `sm:ml-auto` or keep the natural flex-row spacing via gap-3.
  </action>
  <verify>
    <automated>cd C:/users/jamie/fplx && npx next build 2>&1 | tail -5 && echo "--- grep checks ---" && echo "grid-cols-2 count:" && grep -c "grid-cols-2" src/components/captaincy/CaptaincyPanel.tsx && test $(grep -c "grid-cols-2" src/components/captaincy/CaptaincyPanel.tsx) -ge 1 && echo "sm:grid-cols-1 count:" && grep -c "sm:grid-cols-1" src/components/captaincy/CaptaincyPanel.tsx && test $(grep -c "sm:grid-cols-1" src/components/captaincy/CaptaincyPanel.tsx) -ge 1 && echo "flex-col count:" && grep -c "flex-col" src/components/captaincy/CaptaincyPanel.tsx && test $(grep -c "flex-col" src/components/captaincy/CaptaincyPanel.tsx) -ge 1 && echo "sm:flex-row count:" && grep -c "sm:flex-row" src/components/captaincy/CaptaincyPanel.tsx && test $(grep -c "sm:flex-row" src/components/captaincy/CaptaincyPanel.tsx) -ge 1 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>Captaincy panel renders as 2-column card grid on mobile. Each card shows rank, player name, safe/upside badge, and projected captain pts without truncation. Desktop layout is a single-column horizontal list identical to current.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. On a 375px viewport: transfer suggestion cards show 2-row structured layout, login form stacks vertically with full-width inputs, captaincy panel shows 2-column card grid
3. On desktop (>=640px): all three components render identically to their current appearance
</verification>

<success_criteria>
- Transfer cards: player names + badges on row 1, gem delta + cost + proj pts on row 2, no text overflow at 375px
- Login form: inputs stack vertically on mobile, full-width; desktop unchanged
- Captaincy panel: 2-column card grid on mobile, each card has rank + name + badge + pts visible; desktop unchanged as single-column list
</success_criteria>

<output>
After completion, create `.planning/phases/16-component-level-mobile/16-01-SUMMARY.md`
</output>
