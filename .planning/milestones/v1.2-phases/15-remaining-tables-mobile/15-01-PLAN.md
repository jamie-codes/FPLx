---
phase: 15-remaining-tables-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/squad/SquadView.tsx
autonomous: true
requirements:
  - MOB-TBL-02
  - MOB-TBL-05

must_haves:
  truths:
    - "On mobile (<640px), SquadView shows exactly Player (with C/VC indicator), Price, Risk, and Rec columns — Team, Own%, Mins, Gem, Status are hidden"
    - "On desktop (>=640px), SquadView shows all 9 columns unchanged"
    - "When scrolling SquadView horizontally on mobile, the Player column remains fixed to the left edge"
    - "Bench rows and expanded ExplainPanel still work correctly on both mobile and desktop"
  artifacts:
    - path: "src/components/squad/SquadView.tsx"
      provides: "Mobile column hiding, sticky Player column, isMobile state"
      contains: "isMobile"
  key_links:
    - from: "src/components/squad/SquadView.tsx"
      to: "window.innerWidth"
      via: "useEffect with resize listener"
      pattern: "window\\.innerWidth < 640"
---

<objective>
Make SquadView readable on mobile by hiding non-essential columns and pinning the Player column to the left edge when scrolling horizontally.

Purpose: SquadView has 9 columns — too wide for 375px. This plan reduces to 4 priority columns (Player, Price, Risk, Rec) on mobile, with sticky Player column for context while scrolling.
Output: Modified SquadView.tsx with isMobile state, conditional column hiding via CSS classes, and sticky Player column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gemtable-mobile/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isMobile state and mobile column hiding to SquadView</name>
  <files>src/components/squad/SquadView.tsx</files>
  <read_first>
    - src/components/squad/SquadView.tsx
    - src/components/gem-table/GemTable.tsx (reference for isMobile pattern)
  </read_first>
  <action>
    SquadView uses a manual HTML table (NOT TanStack Table), so column hiding is done via conditional CSS classes on each th/td, not via VisibilityState.

    **1. Add useEffect import** — change `import React, { useState } from 'react'` to `import React, { useState, useEffect } from 'react'`

    **2. Add isMobile state** — inside the SquadView component, after the existing `useState` for `expandedIds`, add:
    ```
    const [isMobile, setIsMobile] = useState(false)
    useEffect(() => {
      const check = () => setIsMobile(window.innerWidth < 640)
      check()
      window.addEventListener('resize', check)
      return () => window.removeEventListener('resize', check)
    }, [])
    ```

    **3. Define a CSS helper** — add right after the isMobile state:
    ```
    const hideOnMobile = isMobile ? 'hidden' : ''
    ```

    **4. Hide columns on mobile** — the table has 9 columns. On mobile, hide: Team (col 2), Own% (col 4), Mins (col 5), Gem (col 6), Status (col 7). Keep: Player (col 1), Price (col 3), Risk (col 8), Rec (col 9).

    For each hidden column's `<th>`, add `${hideOnMobile}` to the className. Specifically:
    - Team th (line ~122): change className to `{px-3 py-2 font-medium text-zinc-500 whitespace-nowrap ${hideOnMobile}}`
    - Own% th (line ~124): same pattern
    - Mins th (line ~125): same pattern
    - Gem th (line ~126): same pattern
    - Status th (line ~127): same pattern

    For each hidden column's `<td>`, add `${hideOnMobile}` to the className. Specifically:
    - Team td (the one rendering `player.team_short_name`, line ~161-163): add `${hideOnMobile}` to className
    - Own% td (the one rendering `player.selected_by_percent`, line ~179-181): add `${hideOnMobile}`
    - Mins td (the one rendering `player.minutes`, line ~182-184): add `${hideOnMobile}`
    - Gem td (the one rendering `player.gem_score`, line ~185-187): add `${hideOnMobile}`
    - Status td (the one rendering `StatusBadge`, line ~188-190): add `${hideOnMobile}`

    **5. Sticky Player column** — apply sticky positioning to the Player th and td:
    - Player th (line ~121): change className to `"px-3 py-2 font-medium text-zinc-500 whitespace-nowrap sticky left-0 z-30 bg-white"`
    - Player td (line ~140): change className to `"px-3 py-2 whitespace-nowrap font-medium text-zinc-900 sticky left-0 z-10 bg-white"` — but also handle the even row background: add `${!isBench && expandedIds.has(pick.element) ? '' : ''}` is not needed, just use bg-white as the opaque base (same as Phase 14 GemTable approach — bg-white prevents bleed-through).

    **6. Update colSpan on ExplainPanel** — the expanded ExplainPanel row currently has `colSpan={9}`. Change to dynamic: `colSpan={isMobile ? 4 : 9}` so it spans only the visible columns.

    **7. Other header z-index** — add `z-20` to all other (non-Player) `<th>` elements to maintain proper layering with the sticky top thead. This means the non-hidden th elements (Price, Risk, Rec) need z-20, and the hidden ones get it too (harmless when hidden).

    Update ALL `<th>` className strings (except Player which gets z-30) to include `z-20`.

    **Important:** Do NOT change the `overflow-x-auto` wrapper — it already exists on line 117.
  </action>
  <verify>
    <automated>cd /c/users/jamie/fplx && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "isMobile" src/components/squad/SquadView.tsx returns at least 3 matches (state, useEffect, hideOnMobile)
    - grep "window.innerWidth < 640" src/components/squad/SquadView.tsx returns a match
    - grep "useEffect" src/components/squad/SquadView.tsx returns a match
    - grep "hideOnMobile" src/components/squad/SquadView.tsx returns at least 10 matches (5 th + 5 td usages + definition)
    - grep "sticky left-0 z-30" src/components/squad/SquadView.tsx returns a match (Player th)
    - grep "sticky left-0 z-10" src/components/squad/SquadView.tsx returns a match (Player td)
    - grep "hidden" src/components/squad/SquadView.tsx returns matches (hideOnMobile produces "hidden" class)
    - grep "z-20" src/components/squad/SquadView.tsx returns at least 1 match (non-Player th elements)
    - grep "colSpan={isMobile ? 4 : 9}" src/components/squad/SquadView.tsx returns a match
  </acceptance_criteria>
  <done>SquadView shows exactly Player (with C/VC), Price, Risk, Rec on mobile; Team, Own%, Mins, Gem, Status hidden; Player column sticky left with z-30/z-10 layering; ExplainPanel colSpan adjusts for visible column count; desktop unchanged with all 9 columns</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all existing tests pass
- DevTools at 375px: SquadView shows 4 columns (Player, Price, Risk, Rec), Player sticky when scrolling right
- DevTools at 1024px: SquadView shows all 9 columns unchanged
- Captain (C) and Vice-Captain (VC) indicators still visible in Player column on mobile
- ExplainPanel expand/collapse still works on both mobile and desktop
</verification>

<success_criteria>
- SquadView shows exactly Player (with C/VC indicator), Price, Risk badge, Rec verdict on mobile
- Player column sticks to left edge when scrolling horizontally on mobile
- Desktop SquadView is identical to pre-phase behavior with all 9 columns
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-remaining-tables-mobile/15-01-SUMMARY.md`
</output>
