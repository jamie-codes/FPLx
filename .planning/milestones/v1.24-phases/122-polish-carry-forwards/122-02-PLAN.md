---
phase: 122-polish-carry-forwards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transfers/OpportunityCostTable.tsx
  - src/components/transfers/OpportunityCostTable.test.tsx
  - .planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md
autonomous: true
requirements: [POL-03, POL-04, POL-05, POL-06]
tags: [mins-risk-badge, opportunity-cost, verify-only, ui-wiring]

must_haves:
  truths:
    - "MinsRiskBadge renders in the OpportunityCostTable buy-player cluster, after StatusLabelBadge and before NewsBanner, for every transfer leg of every non-roll row"
    - "MinsRiskBadge with a non-injured, defined minsRisk value (e.g. 'rotation_risk') is visually present in the rendered OCS table"
    - "MinsRiskBadge with minsRisk='injured' or undefined renders nothing (existing component contract preserved)"
    - "POL-03 (SquadView), POL-05 (GemTable column), and POL-06 (PlayerComparisonModal) are verified as already-implemented by source-pinned assertions plus a written verification record"
  artifacts:
    - path: "src/components/transfers/OpportunityCostTable.tsx"
      provides: "Buy-player badge cluster with MinsRiskBadge slotted between StatusLabelBadge and NewsBanner"
      contains: "MinsRiskBadge"
    - path: "src/components/transfers/OpportunityCostTable.test.tsx"
      provides: "At least one new assertion proving MinsRiskBadge renders in the buy cluster with the expected minsRisk value"
      contains: "MinsRiskBadge"
    - path: ".planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md"
      provides: "Source-pinned verification record for POL-03/POL-05/POL-06 (line numbers, grep evidence, decision: PASS or FAIL)"
      contains: "POL-03"
  key_links:
    - from: "src/components/transfers/OpportunityCostTable.tsx (PlayerMoveCell)"
      to: "src/components/shared/MinsRiskBadge.tsx"
      via: "<MinsRiskBadge minsRisk={t.buy.mins_risk} /> inserted between StatusLabelBadge and NewsBanner"
      pattern: "MinsRiskBadge[\\s\\S]*?t\\.buy\\.mins_risk"
    - from: "src/components/transfers/OpportunityCostTable.tsx (imports)"
      to: "@/components/shared/MinsRiskBadge"
      via: "import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'"
      pattern: "import \\{ MinsRiskBadge \\} from '@/components/shared/MinsRiskBadge'"
---

<objective>
Slot `MinsRiskBadge` into the buy-player cluster of `OpportunityCostTable.PlayerMoveCell` (POL-04), and produce a written verification record for POL-03, POL-05, POL-06 — three requirements that CONTEXT.md D-06/D-07/D-08 already confirm are implemented but which the phase requires evidence for.

Purpose: Bring the OCS buy cluster in line with other surfaces that already expose MinsRiskBadge (SquadView, GemTable column, PlayerComparisonModal), and document the verification status of the already-implemented requirements so the phase has a single auditable record.

Output:
- `src/components/transfers/OpportunityCostTable.tsx` with `MinsRiskBadge` import and JSX in the buy cluster.
- `src/components/transfers/OpportunityCostTable.test.tsx` with a new test asserting MinsRiskBadge renders in the buy cluster.
- `.planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md` — source-pinned verification record for POL-03/05/06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-polish-carry-forwards/122-CONTEXT.md
@.planning/phases/122-polish-carry-forwards/122-PATTERNS.md
@.planning/phases/122-polish-carry-forwards/122-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From `src/lib/types.ts`:
- `MinsRisk = 'nailed' | 'likely_start' | 'rotation_risk' | 'cameo' | 'injured'`
- `ScoredPlayer.mins_risk: MinsRisk` (always present on `ScoredPlayer`; `t.buy` in OCSRow is `ScoredPlayer`).

From `src/components/shared/MinsRiskBadge.tsx`:
- `export function MinsRiskBadge({ minsRisk, mins60Prob }: { minsRisk: MinsRisk | undefined; mins60Prob?: number })`
- Returns `null` when `minsRisk` is `undefined` or `'injured'` — safe to render unconditionally.
- Renders an inline `<span>` with label text. For `rotation_risk` the label is `Rotation risk` (used in tests for assertion).

From `src/components/transfers/OpportunityCostTable.tsx`:
- `PlayerMoveCell` component (starts line 107). Buy cluster JSX is inside `row.transfers.map((t, i) => ...)` between lines 134–149.
- Current cluster order (top-to-bottom in JSX, left-to-right visually): "Sell" label → sell name → arrow → "Buy" label → buy name → `<RotationRiskBadge>` → `<StatusLabelBadge>` → `<NewsBanner>`.
- Target cluster order after this plan: ... → `<RotationRiskBadge>` → `<StatusLabelBadge>` → `<MinsRiskBadge>` → `<NewsBanner>` (per CONTEXT.md D-05).

From `src/components/squad/SquadView.tsx` line 224 (already correct, verify only):
- `<MinsRiskBadge minsRisk={player.mins_risk} />`

From `src/components/gem-table/columns.tsx` lines 271–276 (already correct, verify only):
- `col.display({ id: 'mins_risk', header: H('Risk', ...), enableSorting: false, cell: ({ row }) => <MinsRiskBadge minsRisk={row.original.mins_risk} /> })`

From `src/components/gem-table/PlayerComparisonModal.tsx` line 172 (already correct, verify only):
- `<MinsRiskBadge minsRisk={p.mins_risk} />` inside `renderSignalsColumn`, rendered for both compared players via the function's two invocations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MinsRiskBadge to OpportunityCostTable buy cluster (POL-04)</name>
  <files>src/components/transfers/OpportunityCostTable.tsx, src/components/transfers/OpportunityCostTable.test.tsx</files>
  <read_first>
    - src/components/transfers/OpportunityCostTable.tsx (full file — import block lines 1–18, PlayerMoveCell starts line 107, buy cluster JSX lines 134–149)
    - src/components/transfers/OpportunityCostTable.test.tsx (lines 1–60+ — understand existing fixture pattern, makeScoredPlayer helper, withQueryClient wrapper)
    - src/components/shared/MinsRiskBadge.tsx (full file — confirm `minsRisk` prop, null-safety, exact rendered label text for `rotation_risk`)
    - src/components/squad/SquadView.tsx line 224 (canonical analog — `<MinsRiskBadge minsRisk={player.mins_risk} />`)
    - .planning/phases/122-polish-carry-forwards/122-CONTEXT.md (D-05 — buy player only, between StatusLabelBadge and NewsBanner)
    - .planning/phases/122-polish-carry-forwards/122-PATTERNS.md (exact JSX replacement and shared pattern for badge cluster ordering)
    - .planning/phases/122-polish-carry-forwards/122-UI-SPEC.md (Component Inventory — badge cluster order; States to Implement — badge palette)
  </read_first>
  <behavior>
    - When `OpportunityCostTable` is rendered with a non-roll OCS row whose first transfer leg has `t.buy.mins_risk = 'rotation_risk'`, the rendered DOM inside the buy cluster contains a `<span>` with text `Rotation risk`.
    - The badge sits between the existing `StatusLabelBadge` slot and the `NewsBanner` slot in JSX order (visually: after the buy name and RotationRiskBadge/StatusLabelBadge, before the news banner area).
    - When `t.buy.mins_risk = 'injured'` (or undefined), the MinsRiskBadge renders nothing — no extra `<span>` for the badge. This relies on the existing `MinsRiskBadge` null-return contract (no new guard required).
    - The sell side of the cluster is unchanged — no MinsRiskBadge on the sell player.
  </behavior>
  <action>
    Make these concrete edits to `src/components/transfers/OpportunityCostTable.tsx`:

    1. Add a new import line in the import block (after line 17 `import { StatusLabelBadge } from '@/components/shared/StatusLabelBadge'`):
       - INSERT: `import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'`

    2. Inside `PlayerMoveCell`'s buy cluster — currently lines 140–143:
       - LOCATE the JSX block beginning with `<RotationRiskBadge rotationRisk={t.buy.rotation_risk ?? false} />` and ending with `<StatusLabelBadge statusLabel={lineupNewsMap?.get(t.buy.id)?.status_label} />`.
       - IMMEDIATELY AFTER `<StatusLabelBadge ... />` and BEFORE the `{/* Phase 88 SCRAPER-01: news banner for buy candidate (D-07) */}` comment + `<NewsBanner ... />`, INSERT this block on its own lines:
         ```
         {/* Phase 122 POL-04: MinsRiskBadge for buy candidate — minutes confidence signal */}
         <MinsRiskBadge minsRisk={t.buy.mins_risk} />
         ```
       - Pass ONLY `minsRisk={t.buy.mins_risk}`. Do NOT pass `mins60Prob` — per UI-SPEC.md it is optional and OCS does not have it on transfer legs.
       - Do NOT add a wrapping conditional (`t.buy.mins_risk && ...`). The component already returns `null` for `undefined` and `'injured'`.

    3. Do NOT modify the sell side of the cluster (`{t.sell.web_name}` span and surrounding markup). POL-04 is buy-only per D-05.

    Make these concrete edits to `src/components/transfers/OpportunityCostTable.test.tsx`:

    4. Add a new test that proves the badge renders in the buy cluster. Place it inside an existing `describe` block (whichever covers PlayerMoveCell rendering — if none exists, create a new `describe('PlayerMoveCell — POL-04 MinsRiskBadge', () => { ... })` block at the end of the file). The test must:
       - Construct an `OCSRow` with `kind: 'single-free'` and a single transfer leg.
       - Use `makeScoredPlayer({ id: 200, mins_risk: 'rotation_risk' })` for `t.buy` (define a sell player with any mins_risk; sell is not asserted).
       - Render via the existing `withQueryClient(...)` wrapper and the existing `OpportunityCostTable` props pattern (mirror the OCSRow construction style from `makeRollRow` and the surrounding fixtures).
       - Assert: `expect(container.textContent).toContain('Rotation risk')` (the label MinsRiskBadge renders for `rotation_risk`).
       - Optionally assert order: the position of the `Rotation risk` text comes AFTER the buy player's `web_name` text in `container.textContent` (use `.indexOf` comparison). This guards the cluster ordering invariant.

    Do not introduce new mocks — the existing `vi.mock` for `useNewsFlagEnabled`, `usePlayerInsight`, and `PlayerInsightSection` cover all dependencies. `MinsRiskBadge` is pure and does not need mocking.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx; npx vitest run src/components/transfers/OpportunityCostTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { MinsRiskBadge } from '@/components/shared/MinsRiskBadge'" src/components/transfers/OpportunityCostTable.tsx` returns 1.
    - `grep -c "<MinsRiskBadge minsRisk={t.buy.mins_risk} />" src/components/transfers/OpportunityCostTable.tsx` returns 1.
    - In `src/components/transfers/OpportunityCostTable.tsx`, the line containing `<MinsRiskBadge minsRisk={t.buy.mins_risk} />` appears AFTER the line containing `<StatusLabelBadge statusLabel={lineupNewsMap?.get(t.buy.id)?.status_label} />` AND BEFORE the line containing `<NewsBanner` for the buy candidate (verified by line-number ordering, e.g. `grep -n "MinsRiskBadge\|StatusLabelBadge\|NewsBanner" src/components/transfers/OpportunityCostTable.tsx`).
    - The sell-side JSX (around `{t.sell.web_name}`) does NOT contain a `MinsRiskBadge` — `grep -c "MinsRiskBadge" src/components/transfers/OpportunityCostTable.tsx` returns exactly 2 (the import + the buy cluster usage), confirming buy-only insertion.
    - `src/components/transfers/OpportunityCostTable.test.tsx` contains at least one new assertion containing both the literal `MinsRiskBadge` (in a comment or test name acceptable) AND the literal `Rotation risk` (the asserted label).
    - `npx vitest run src/components/transfers/OpportunityCostTable.test.tsx` exits 0 with all tests passing including the new one.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>MinsRiskBadge renders in the OCS buy-player cluster between StatusLabelBadge and NewsBanner; existing tests pass; a new test asserts the badge renders for `rotation_risk`.</done>
</task>

<task type="auto">
  <name>Task 2: Produce source-pinned verification record for POL-03, POL-05, POL-06</name>
  <files>.planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md</files>
  <read_first>
    - src/components/squad/SquadView.tsx (lines 215–235 — confirm `<MinsRiskBadge minsRisk={player.mins_risk} />` at line 224 inside the Transfers tab player rows table)
    - src/components/gem-table/columns.tsx (lines 268–280 — confirm `col.display({ id: 'mins_risk', ... cell: ({ row }) => <MinsRiskBadge minsRisk={row.original.mins_risk} /> })` at lines 271–276)
    - src/components/gem-table/PlayerComparisonModal.tsx (lines 160–180 — confirm `<MinsRiskBadge minsRisk={p.mins_risk} />` at line 172 inside `renderSignalsColumn`, and that `renderSignalsColumn` is invoked for both compared players)
    - src/components/gem-table/columns.test.tsx (lines 85–100 — confirm `it('renders MinsRiskBadge inside card when minsRisk is rotation_risk (D-02)')` exists and currently passes)
    - src/components/gem-table/PlayerComparisonModal.test.tsx (search for `mins_risk` usages — confirm the modal test fixtures set `mins_risk` on compared players)
    - .planning/phases/122-polish-carry-forwards/122-CONTEXT.md (D-06, D-07, D-08 — the three already-done items)
  </read_first>
  <action>
    Create the file `.planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md` with the structure below. Fill in the exact line numbers, grep outputs, and pass/fail decisions from the actual source files (do NOT rely solely on the CONTEXT.md hints — re-confirm against the live source).

    Use this template (replace `[...]` placeholders with real findings):

    ```
    # Phase 122 — Verification of Already-Implemented Requirements

    **Verified:** {ISO date}
    **Scope:** POL-03, POL-05, POL-06 (D-06, D-07, D-08 from 122-CONTEXT.md)

    ## POL-03 — SquadView Transfers tab MinsRiskBadge

    - File: `src/components/squad/SquadView.tsx`
    - Line: 224
    - Code: `<MinsRiskBadge minsRisk={player.mins_risk} />`
    - Context: [Confirm the badge is inside the player rows table for the Transfers tab — name the surrounding `<td>` and tab condition]
    - Test coverage: [Run `grep -n "MinsRiskBadge\|mins_risk" src/components/squad/*.test.tsx` — record results; if no direct test exists, note it as "no dedicated test; visual + integration only"]
    - **Decision:** PASS / FAIL — [reasoning]

    ## POL-05 — GemTable mins_risk column

    - File: `src/components/gem-table/columns.tsx`
    - Lines: 271–276
    - Code: `col.display({ id: 'mins_risk', header: H('Risk', ...), enableSorting: false, cell: ({ row }) => <MinsRiskBadge minsRisk={row.original.mins_risk} /> })`
    - Default-preset visibility: [Confirm `mins_risk` is NOT listed in any hidden-columns preset in this file or in PresetToggle. Record evidence.]
    - Test coverage: `src/components/gem-table/columns.test.tsx` line 87 — `it('renders MinsRiskBadge inside card when minsRisk is rotation_risk (D-02)')`
    - **Decision:** PASS / FAIL — [reasoning]

    ## POL-06 — PlayerComparisonModal MinsRiskBadge

    - File: `src/components/gem-table/PlayerComparisonModal.tsx`
    - Line: 172
    - Code: `<MinsRiskBadge minsRisk={p.mins_risk} />`
    - Both players coverage: [Confirm `renderSignalsColumn` is called for both compared players — name the two call sites with line numbers]
    - Test coverage: `src/components/gem-table/PlayerComparisonModal.test.tsx` — fixtures set `mins_risk` on both compared players (lines 66, 137)
    - **Decision:** PASS / FAIL — [reasoning]

    ## Summary

    | REQ-ID | Status | Evidence |
    |--------|--------|----------|
    | POL-03 | PASS/FAIL | SquadView.tsx:224 |
    | POL-05 | PASS/FAIL | columns.tsx:271–276 + columns.test.tsx:87 |
    | POL-06 | PASS/FAIL | PlayerComparisonModal.tsx:172 + PlayerComparisonModal.test.tsx |

    If any decision is FAIL, the phase must spawn a remediation plan (122-03) before closing.
    ```

    If any of the three sources fails verification (line number drift, file moved, code regressed, or test removed), record `FAIL` with the discovered state and STOP — do not silently mark PASS. The phase orchestrator handles remediation routing.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx; test -f .planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md && grep -E "POL-03|POL-05|POL-06" .planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md | grep -E "PASS|FAIL" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md` exists.
    - File contains three sections with headings `POL-03`, `POL-05`, `POL-06`.
    - Each section contains a `**Decision:** PASS` or `**Decision:** FAIL` line (exact string match for one of those two).
    - Each section cites the source file path and the line number for the relevant MinsRiskBadge usage (verified against the live file — line numbers must match the current source, not stale references).
    - Summary table at the bottom lists all three REQ-IDs with a status and an evidence reference.
    - If any FAIL is recorded, the file's "Summary" closing line clearly states remediation is required.
  </acceptance_criteria>
  <done>Verification record exists with PASS/FAIL decisions for POL-03/05/06, each pinned to live source line numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure frontend component wiring. No new trust boundaries are introduced. `OpportunityCostTable` already consumes existing typed data (`OCSRow`, `ScoredPlayer`) from upstream hooks whose trust boundaries are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04 | Information Disclosure | `t.buy.mins_risk` rendered in MinsRiskBadge label/tooltip | accept | `mins_risk` is a public derived classification of FPL data already visible across multiple surfaces (SquadView, GemTable, PlayerComparisonModal). No new exposure. |
| T-122-05 | Tampering | `t.buy.mins_risk` client-rendered value | accept | Value comes from server-computed `ScoredPlayer` data already used by other UI surfaces. Component is presentational; no mutation or persistence introduced. |
| T-122-06 | Denial of Service | Additional badge render per transfer leg | accept | Negligible — at most a few `<span>` nodes per row; `MinsRiskBadge` is a constant-time function with no async work. |

Pure frontend cluster-insertion work plus a documentation file. No new routes, auth, API, or data mutation — threat surface unchanged from the existing OCS table.
</threat_model>

<verification>
- Run `npx vitest run src/components/transfers/OpportunityCostTable.test.tsx` — all tests pass, including the new MinsRiskBadge buy-cluster assertion.
- Run `npx tsc --noEmit` — no TypeScript errors introduced.
- Run `npx eslint src/components/transfers/OpportunityCostTable.tsx` — no new lint errors.
- Open `.planning/phases/122-polish-carry-forwards/122-VERIFY-EXISTING.md` — file exists with POL-03/POL-05/POL-06 sections each having a PASS/FAIL decision.
- Visual smoke check (recorded for execute-phase UAT): in dev mode, navigate to Transfers tab → trigger a transfer suggestion → confirm a "Rotation risk" / "Nailed" / "Likely start" badge appears next to the buy player name between the existing StatusLabelBadge and NewsBanner.
</verification>

<success_criteria>
- POL-04: MinsRiskBadge is imported and rendered in `OpportunityCostTable.PlayerMoveCell` buy cluster between `StatusLabelBadge` and `NewsBanner`, with `minsRisk={t.buy.mins_risk}` and no `mins60Prob`.
- POL-03, POL-05, POL-06: Each has a PASS or FAIL decision recorded in `122-VERIFY-EXISTING.md` pinned to live source line numbers.
- All existing OpportunityCostTable tests pass, plus one new test asserting the new badge renders for `rotation_risk`.
- No new ESLint or TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/122-polish-carry-forwards/122-02-SUMMARY.md` using the standard summary template. Reference `122-VERIFY-EXISTING.md` as a phase artifact in the summary.
</output>
