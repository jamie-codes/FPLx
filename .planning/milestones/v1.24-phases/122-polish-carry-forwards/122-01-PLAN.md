---
phase: 122-polish-carry-forwards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/planner/RouteTreeTab.tsx
  - src/components/planner/RouteTreeTab.test.tsx
autonomous: true
requirements: [POL-01, POL-02]
tags: [chip-toggle, route-tree, ui-wiring]

must_haves:
  truths:
    - "User can click any of the 4 chip buttons (Wildcard / Freehit / Bench Boost / Triple Captain) in RouteTreeTab and the active chip visually reflects the selected state"
    - "Clicking the active chip again deselects it (chipMode returns to null)"
    - "Selected chipMode is passed into buildTransferRouteTree and changes the rendered route tree output"
    - "The RouteTreeTab summary table column header for hits reads 'Transfer Hits' (not 'Hits')"
    - "The existing RouteTreeTab test that asserts column header order still passes against the new label"
  artifacts:
    - path: "src/components/planner/RouteTreeTab.tsx"
      provides: "RouteTreeTab with useState-backed chipMode, enabled ChipToggle, corrected column label"
      contains: "useState<PlannerChip>"
    - path: "src/components/planner/RouteTreeTab.test.tsx"
      provides: "Updated header-order assertion using 'Transfer Hits' and at least one assertion that ChipToggle is no longer rendered as disabled"
      contains: "Transfer Hits"
  key_links:
    - from: "src/components/planner/RouteTreeTab.tsx"
      to: "src/components/planner/ChipToggle.tsx"
      via: "<ChipToggle activeChip={chipMode} onToggle={(chip) => setChipMode(prev => prev === chip ? null : chip)} />"
      pattern: "ChipToggle[\\s\\S]*?activeChip=\\{chipMode\\}"
    - from: "src/components/planner/RouteTreeTab.tsx"
      to: "src/lib/transfer-route-tree (buildTransferRouteTree)"
      via: "chipMode passed inside buildTransferRouteTree options object (useMemo)"
      pattern: "buildTransferRouteTree\\([\\s\\S]*?chipMode"
---

<objective>
Wire `chipMode` in `RouteTreeTab` from a hardcoded `null` constant to a reactive `useState<PlannerChip>` value driven by `ChipToggle`, and correct the summary table column header from "Hits" to "Transfer Hits". Both changes live in a single component file.

Purpose: Resolve two long-standing carry-forwards (TRT-06 and TRT-02 from Phase 60) so that the Route Tree planner respects chip mode selection (matching ManualPlanTab behaviour) and the column label is correct.

Output:
- `src/components/planner/RouteTreeTab.tsx` with reactive chipMode and corrected column label.
- `src/components/planner/RouteTreeTab.test.tsx` updated to match the new header label and to assert ChipToggle is now interactive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-polish-carry-forwards/122-CONTEXT.md
@.planning/phases/122-polish-carry-forwards/122-PATTERNS.md
@.planning/phases/122-polish-carry-forwards/122-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From `src/lib/types.ts`:
- `PlannerChip = 'wildcard' | 'freehit' | 'bboost' | '3xc' | null` (already imported in RouteTreeTab line 16)

From `src/components/planner/ChipToggle.tsx`:
- `interface ChipToggleProps { gw: number; activeChip: PlannerChip; onToggle: (chip: PlannerChip) => void; disabled?: boolean }`
- Omit `disabled` to enable; `disabled={true}` adds `pointer-events-none opacity-50`.

From `src/lib/transfer-route-tree`:
- `buildTransferRouteTree(opts: { picks, players, horizon, initialFT, initialBank, sellPrices, chipMode, startingGw })` — already wired in RouteTreeTab line 96–108 with `chipMode` inside the options object and inside the useMemo deps array. No engine change required.

From `src/components/planner/ManualPlanTab.tsx` (canonical analog, lines 191–199):
- Toggle-deselect idiom: `cur === chip ? null : chip`.

From `react`:
- `useState` already imported in RouteTreeTab line 7.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded chipMode with useState and wire ChipToggle (POL-01)</name>
  <files>src/components/planner/RouteTreeTab.tsx</files>
  <read_first>
    - src/components/planner/RouteTreeTab.tsx (full file — see current state of chipMode at line 90, ChipToggle render at lines 234–239, useMemo at lines 96–108)
    - src/components/planner/ChipToggle.tsx (component contract: activeChip, onToggle, disabled props)
    - src/components/planner/ManualPlanTab.tsx (lines 191–199 — canonical toggle-deselect handler pattern)
    - src/components/planner/RouteTreeTab.test.tsx (existing tests to verify nothing else breaks)
    - .planning/phases/122-polish-carry-forwards/122-CONTEXT.md (D-01, D-02, D-03 — wiring decisions)
    - .planning/phases/122-polish-carry-forwards/122-PATTERNS.md (exact replacement blocks for line 90 and lines 234–239)
  </read_first>
  <behavior>
    - Render test: ChipToggle is rendered without the `disabled` prop (or with `disabled={false}` not set to true), so chip buttons are interactive.
    - State test: clicking the Wildcard chip sets `activeChip` to `wildcard` on the rendered ChipToggle.
    - Toggle-deselect: clicking the currently-active chip again sets `activeChip` back to `null`.
    - Engine reactivity: when `chipMode` changes, the `tree` useMemo recomputes (already covered by existing dependency array containing `chipMode`; no new test required for the dep array, just confirm rerender does not throw).
  </behavior>
  <action>
    Make the following three concrete edits inside `src/components/planner/RouteTreeTab.tsx`:

    1. Replace the hardcoded constant at line 90:
       - DELETE: `const chipMode: PlannerChip = null`
       - INSERT: `const [chipMode, setChipMode] = useState<PlannerChip>(null)`
       - `useState` is already imported at line 7. `PlannerChip` is already imported at line 16. Do NOT add new imports.

    2. Replace the ChipToggle render at lines 234–239:
       - DELETE the existing block with `activeChip={null}`, `onToggle={() => {}}`, `disabled={true}`.
       - INSERT a ChipToggle render that passes:
         - `gw={startingGw ?? 1}` (unchanged)
         - `activeChip={chipMode}` (sourced from the new useState)
         - `onToggle={(chip) => setChipMode(prev => prev === chip ? null : chip)}` (inline toggle-deselect matching ManualPlanTab D-02 pattern)
         - OMIT the `disabled` prop entirely — do not pass `disabled={false}` (D-03 specifies removal).

    3. Do NOT touch the `tree` useMemo at lines 96–108. The `chipMode` reference inside `buildTransferRouteTree({ ..., chipMode, ... })` and inside the dependency array already wires correctly because `chipMode` is now reactive state.

    All four chips (`wildcard`, `freehit`, `bboost`, `3xc`) must be selectable per D-01. ChipToggle already enumerates them; no per-chip filtering is required here.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx; npx vitest run src/components/planner/RouteTreeTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/planner/RouteTreeTab.tsx` contains the literal substring `const [chipMode, setChipMode] = useState<PlannerChip>(null)`.
    - `src/components/planner/RouteTreeTab.tsx` does NOT contain `const chipMode: PlannerChip = null` anywhere.
    - `src/components/planner/RouteTreeTab.tsx` ChipToggle render contains `activeChip={chipMode}` (not `activeChip={null}`).
    - `src/components/planner/RouteTreeTab.tsx` ChipToggle render contains `setChipMode(prev => prev === chip ? null : chip)`.
    - `src/components/planner/RouteTreeTab.tsx` ChipToggle render does NOT contain `disabled={true}` anywhere inside the `<ChipToggle ... />` element JSX (`grep -n "disabled" src/components/planner/RouteTreeTab.tsx` returns no line inside the ChipToggle element).
    - `npx vitest run src/components/planner/RouteTreeTab.test.tsx` exits 0 (no new test regressions from the state change — header-label test is updated in Task 2).
    - TypeScript still typechecks: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>RouteTreeTab uses reactive `chipMode` state; ChipToggle is wired and interactive; toggle-deselect works; engine recomputes routes on chip change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update column header label "Hits" → "Transfer Hits" and adjust the existing header-order test (POL-02)</name>
  <files>src/components/planner/RouteTreeTab.tsx, src/components/planner/RouteTreeTab.test.tsx</files>
  <read_first>
    - src/components/planner/RouteTreeTab.tsx (line 269 — the `<th>` with text "Hits")
    - src/components/planner/RouteTreeTab.test.tsx (lines 268–272 — the test `'column headers are in order: Path, Hits, Hit cost, Net xPts, Chips, Action'` that currently asserts `['Path', 'Hits', 'Hit cost', 'Net xPts', 'Chips', 'Action']`)
    - .planning/phases/122-polish-carry-forwards/122-CONTEXT.md (D-04 — exact replacement)
    - .planning/phases/122-polish-carry-forwards/122-UI-SPEC.md (Copywriting Contract — "Transfer Hits" is the canonical label)
  </read_first>
  <behavior>
    - The rendered `<thead>` contains a column header with text exactly `Transfer Hits` (not `Hits`).
    - The test that asserts the header order is updated to expect `Transfer Hits` in slot 2 (zero-indexed slot 1).
    - The updated test name is `column headers are in order: Path, Transfer Hits, Hit cost, Net xPts, Chips, Action`.
  </behavior>
  <action>
    Two concrete edits in this task — one in the component, one in the test (the test currently asserts `'Hits'` literally, so failing to update it will break CI):

    1. In `src/components/planner/RouteTreeTab.tsx` at line 269:
       - DELETE the inner text `Hits` inside the `<th scope="col" className="...">Hits</th>` element.
       - INSERT the inner text `Transfer Hits` in its place. Do not change className, scope, or surrounding `<th>`s.

    2. In `src/components/planner/RouteTreeTab.test.tsx` lines 268–272:
       - Update the `it(...)` description from `'column headers are in order: Path, Hits, Hit cost, Net xPts, Chips, Action'` to `'column headers are in order: Path, Transfer Hits, Hit cost, Net xPts, Chips, Action'`.
       - Update the assertion from `expect(headers).toEqual(['Path', 'Hits', 'Hit cost', 'Net xPts', 'Chips', 'Action'])` to `expect(headers).toEqual(['Path', 'Transfer Hits', 'Hit cost', 'Net xPts', 'Chips', 'Action'])`.

    Do not touch the other 5 column headers (`Path`, `Hit cost`, `Net xPts`, `Chips`, `Action`) — none of those are in scope. No new tests are required for this task; the existing one becomes the regression guard.
  </action>
  <verify>
    <automated>cd C:/Users/jamie/fplx; npx vitest run src/components/planner/RouteTreeTab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Transfer Hits" src/components/planner/RouteTreeTab.tsx` returns 1.
    - `grep -n ">Hits<" src/components/planner/RouteTreeTab.tsx` returns nothing (the old label is fully replaced inside this `<th>` — note other tokens like "totalHits" in JS expressions are not `>Hits<`).
    - `grep -c "Transfer Hits" src/components/planner/RouteTreeTab.test.tsx` returns at least 2 (one in the `it` description, one in the `toEqual` assertion).
    - `npx vitest run src/components/planner/RouteTreeTab.test.tsx` exits 0 with all tests in the file passing.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Column header now reads "Transfer Hits"; existing header-order test updated to match and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure client-side UI wiring. No new trust boundaries are introduced. The component already reads from existing hooks (`usePlayers`, `useSquad`, `useMyTeam`, `useAuthStatus`) whose trust boundaries are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | `chipMode` client state in RouteTreeTab | accept | Local React state. No server impact; `chipMode` is read by `buildTransferRouteTree` which is a pure client function. Worst case: user sees a route tree for a chip they cannot actually play — already covered by existing FPL API enforcement on actual chip use. |
| T-122-02 | Information Disclosure | Column header text "Transfer Hits" | accept | Static UI string; no PII or secret material. |
| T-122-03 | Denial of Service | `setChipMode` rapidly causing useMemo recompute thrash | accept | `buildTransferRouteTree` is bounded by squad size (15) and horizon (≤5 GWs) — recompute cost is negligible on user-typical input. No debounce needed. |

Pure frontend component wiring; no new routes, auth, API, or data mutation. Threat surface is minimal as expected for a polish phase.
</threat_model>

<verification>
- Run `npx vitest run src/components/planner/RouteTreeTab.test.tsx` — all tests pass.
- Run `npx tsc --noEmit` — no TypeScript errors introduced.
- Run `npx eslint src/components/planner/RouteTreeTab.tsx` — no new lint errors.
- Visual smoke check (manual, recorded for execute-phase end-of-phase UAT): in dev mode, navigate to the Plan section → Route Tree subtab, click each of the 4 chips, confirm visual active state toggles and column header reads "Transfer Hits".
</verification>

<success_criteria>
- POL-01: `chipMode` is reactive state, ChipToggle is interactive, all 4 chips selectable, toggle-deselect works, `buildTransferRouteTree` receives the live `chipMode`.
- POL-02: Summary table column 2 header text is exactly "Transfer Hits".
- All existing RouteTreeTab tests pass with the updated header-order assertion.
- No new ESLint or TypeScript errors.
</success_criteria>

<output>
After completion, create `.planning/phases/122-polish-carry-forwards/122-01-SUMMARY.md` using the standard summary template.
</output>
