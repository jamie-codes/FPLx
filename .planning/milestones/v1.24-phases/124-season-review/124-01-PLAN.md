---
phase: 124-season-review
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/season-review.ts
  - src/lib/season-review.test.ts
  - src/app/api/season-review/route.ts
  - src/app/api/season-review/route.test.ts
autonomous: true
requirements:
  - REV-01
  - REV-02
tags:
  - season-review
  - fpl-api
  - grade
must_haves:
  truths:
    - "GET /api/season-review?teamId={id} returns aggregated SeasonReview JSON for valid numeric teamId"
    - "GET /api/season-review with missing/non-numeric teamId returns 400"
    - "computeDecisionGrade returns A/B/C/D matching the D-05/D-06 thresholds"
    - "computeDecisionGrade with chipCount=0 renormalizes captainEV+hitBE weights and never divides by zero"
  artifacts:
    - path: src/lib/types.ts
      provides: "SeasonGwEntry and SeasonReview interfaces"
      contains: "export interface SeasonReview"
    - path: src/lib/season-review.ts
      provides: "computeDecisionGrade pure function + GradeLabel type"
      exports: ["computeDecisionGrade", "GradeLabel"]
    - path: src/lib/season-review.test.ts
      provides: "Boundary tests for grade thresholds and D-06 renormalization"
    - path: src/app/api/season-review/route.ts
      provides: "GET /api/season-review handler with SSRF guard + parallel FPL fetches"
      exports: ["GET"]
    - path: src/app/api/season-review/route.test.ts
      provides: "Route handler validation + aggregation tests"
  key_links:
    - from: src/app/api/season-review/route.ts
      to: https://fantasy.premierleague.com/api/entry/{teamId}/history/
      via: fetch with FPL_UA header
      pattern: "fantasy.premierleague.com/api/entry"
    - from: src/app/api/season-review/route.ts
      to: https://fantasy.premierleague.com/api/bootstrap-static/
      via: fetch with FPL_UA header for events[].average_entry_score
      pattern: "bootstrap-static"
    - from: src/app/api/season-review/route.ts
      to: src/lib/types.ts
      via: "import type { SeasonReview, SeasonGwEntry }"
      pattern: "from '@/lib/types'"
---

<objective>
Establish the Phase 124 backend foundation: the `SeasonReview` / `SeasonGwEntry` type contract, the pure `computeDecisionGrade` function implementing D-05/D-06 grade logic (REV-02), and the `/api/season-review` Route Handler returning aggregated per-GW data + summary stats (REV-01) from FPL `/entry/{id}/history/` + `/bootstrap-static/`.

Purpose: Provide the typed data and grade primitive that the Wave 2 hook and Wave 3 component consume. Locks the SeasonReview JSON shape so Plan 02 and Plan 03 can be built against a concrete contract.
Output: Two type exports in `src/lib/types.ts`, a tested pure grade function in `src/lib/season-review.ts`, and a tested Route Handler at `/api/season-review`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/124-season-review/124-CONTEXT.md
@.planning/phases/124-season-review/124-RESEARCH.md
@.planning/phases/124-season-review/124-PATTERNS.md
@.planning/phases/124-season-review/124-UI-SPEC.md
@.planning/phases/124-season-review/124-VALIDATION.md
@src/app/api/season-analytics/route.ts
@src/lib/regret.ts

<interfaces>
<!-- Existing exports the new code must reuse, NOT redefine. -->

From src/lib/types.ts:
```typescript
export interface ChipRoiEntry {
  chipName: 'bboost' | '3xc' | 'freehit'
  event: number
  gwPoints: number
  seasonAvgPoints: number
  delta: number
}
export interface HitTrackingEntry {
  event: number
  brokeEven: boolean | null
  // ...
}
export interface SeasonAnalytics {
  chipRoi: ChipRoiEntry[]
  hitTracking: HitTrackingEntry[]
}
```

From src/lib/regret.ts:
```typescript
export interface SeasonSummary {
  captainHitRate: number | null
  captainHits: number
  gwsWithData: number
  // ...
}
export function computeSeasonSummary(entries: RegretEntry[]): SeasonSummary
```

From src/app/api/season-analytics/route.ts (canonical route pattern — copy and EXTEND):
```typescript
const FPL_BASE = 'https://fantasy.premierleague.com/api'
const FPL_UA = 'fplx/1.17 (+https://fplx.app)'
interface FPLHistoryChip    { name: string; event: number; time: string }
// NOTE: existing FPLHistoryCurrent omits overall_rank — new route MUST add it (Pitfall 1)
interface FPLHistoryCurrent { event: number; points: number; event_transfers_cost: number }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SeasonGwEntry and SeasonReview types to src/lib/types.ts</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts (so executor sees the existing SeasonAnalytics block at lines ~745-781 and the Insights block immediately after — insertion point is between them)
    - .planning/phases/124-season-review/124-PATTERNS.md (canonical type field list)
    - .planning/phases/124-season-review/124-CONTEXT.md (D-04: route returns RAW history stats only — no captainHits / captainGwsWithData fields)
  </read_first>
  <behavior>
    - Test 1: `SeasonGwEntry` interface exists with fields `gw: number`, `points: number`, `avgManagerScore: number`, `overallRank: number`, `chipPlayed: string | null`
    - Test 2: `SeasonReview` interface exists with fields `totalPoints: number`, `finalRank: number`, `bestGw: { gw: number; points: number }`, `worstGw: { gw: number; points: number }`, `transferNetPoints: number`, `gwData: SeasonGwEntry[]`
    - Test 3: Types compile under `tsc --noEmit`
  </behavior>
  <action>
    Add two interface exports to `src/lib/types.ts` immediately after the `SeasonAnalytics` interface (currently the last block before the `Insights` section comment). Both interfaces are additive — do not modify any existing exports.

    Field contracts:
    - `SeasonGwEntry`: fields `gw: number`, `points: number`, `avgManagerScore: number`, `overallRank: number`, `chipPlayed: string | null` — chip slugs are `'bboost' | '3xc' | 'freehit' | 'wildcard'` but the field type is `string | null` so unknown future chip slugs do not break the type.
    - `SeasonReview`: fields `totalPoints: number`, `finalRank: number`, `bestGw: { gw: number; points: number }`, `worstGw: { gw: number; points: number }`, `transferNetPoints: number` (sum of `-(event_transfers_cost)` across GWs; negative = hits taken), `gwData: SeasonGwEntry[]` (ordered by `gw` ascending; only GWs present in `history.current[]`).

    Do NOT include `captainHits` or `captainGwsWithData` on `SeasonReview` — per CONTEXT.md D-04 the route returns RAW history stats only; captain hit rate is derived client-side via `computeSeasonSummary` on `useDecisionHistory` data.

    Add JSDoc referencing `Phase 124 REV-01 / REV-03` and the CONTEXT.md decisions D-01..D-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types.ts` contains `export interface SeasonGwEntry {` with exactly these fields: `gw`, `points`, `avgManagerScore`, `overallRank`, `chipPlayed`
    - `src/lib/types.ts` contains `export interface SeasonReview {` with exactly these fields: `totalPoints`, `finalRank`, `bestGw`, `worstGw`, `transferNetPoints`, `gwData`
    - `SeasonReview` does NOT contain `captainHits` or `captainGwsWithData` fields
    - `SeasonReview.gwData` typed as `SeasonGwEntry[]`
    - `SeasonReview.bestGw` and `worstGw` typed as `{ gw: number; points: number }`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both interfaces exported from `src/lib/types.ts`; tsc passes; no other type exports modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create computeDecisionGrade pure function + RED tests + GREEN implementation (REV-02 D-05/D-06)</name>
  <files>src/lib/season-review.ts, src/lib/season-review.test.ts</files>
  <read_first>
    - src/lib/regret.ts (file header + computeSeasonSummary pattern at lines 1-70 — same conventions: pure module, no React, no fetch, JSDoc with `Sources of truth` block citing CONTEXT.md decisions)
    - src/lib/regret.test.ts (mirror this test file's structure for boundary tests)
    - .planning/phases/124-season-review/124-CONTEXT.md (D-05 grade thresholds; D-06 chip=0 renormalization)
    - .planning/phases/124-season-review/124-RESEARCH.md (Code Examples section: exact computeDecisionGrade signature and renormalization math `40/75`, `35/75`)
  </read_first>
  <behavior>
    - Test 1: `computeDecisionGrade(1.0, 1.0, 1.0, 2)` returns `'A'` (score = 1.0 >= 0.75)
    - Test 2: `computeDecisionGrade(0.75, 0.75, 0.75, 2)` returns `'A'` (boundary; score = 0.75 >= 0.75)
    - Test 3: `computeDecisionGrade(0.5, 0.5, 0.5, 2)` returns `'B'` (boundary; score = 0.5 >= 0.5 < 0.75)
    - Test 4: `computeDecisionGrade(0.25, 0.25, 0.25, 2)` returns `'C'` (boundary; score = 0.25 >= 0.25 < 0.5)
    - Test 5: `computeDecisionGrade(0.0, 0.0, 0.0, 2)` returns `'D'` (score = 0 < 0.25)
    - Test 6: D-06 renormalization with chipCount=0: `computeDecisionGrade(1.0, 1.0, 0, 0)` returns `'A'` (renormalized score = 1.0 * 40/75 + 1.0 * 35/75 = 1.0). Confirms chip ROI component is excluded when no chips played.
    - Test 7: D-06 NaN guard: `computeDecisionGrade(0.5, 0.5, Number.NaN, 0)` returns `'B'` — the `chipROIPositiveRate` argument is IGNORED when `chipCount === 0`, so NaN must not poison the score.
    - Test 8: D-06 partial: `computeDecisionGrade(0.6, 0.4, 0, 0)` returns `'B'` (score = 0.6 * (40/75) + 0.4 * (35/75) = 0.32 + 0.187 = 0.507 → B)
    - Test 9: Standard 3-component path: `computeDecisionGrade(0.8, 0.6, 0.4, 3)` returns `'B'` (score = 0.8*0.4 + 0.6*0.35 + 0.4*0.25 = 0.32+0.21+0.10 = 0.63 → B)
    - Test 10: `GradeLabel` exported as a discriminated string literal type — `const g: GradeLabel = 'A'` compiles.
  </behavior>
  <action>
    Create `src/lib/season-review.ts` as a pure module (no React, no fetch). Mirror the `src/lib/regret.ts` header block conventions:
    - File header comment with `// Phase 124 REV-02: season review grade computation.` and a `Sources of truth:` block citing `.planning/phases/124-season-review/124-CONTEXT.md §D-05, D-06`.

    Exports:
    - `export type GradeLabel = 'A' | 'B' | 'C' | 'D'`
    - `export function computeDecisionGrade(captainEVRate: number, hitBreakEvenRate: number, chipROIPositiveRate: number, chipCount: number): GradeLabel`

    Implementation per CONTEXT.md D-05/D-06:
    - If `chipCount === 0`: `score = captainEVRate * (40 / 75) + hitBreakEvenRate * (35 / 75)` — the `chipROIPositiveRate` argument is NOT referenced in this branch (so NaN inputs are safely ignored).
    - Else: `score = captainEVRate * 0.40 + hitBreakEvenRate * 0.35 + chipROIPositiveRate * 0.25`
    - Thresholds: `score >= 0.75 → 'A'`; `>= 0.50 → 'B'`; `>= 0.25 → 'C'`; else `'D'`.

    JSDoc must explain D-06 explicitly: "When chipCount === 0, the chip ROI component is excluded and the remaining two weights are renormalized to 100% (captainEV = 40/75 ≈ 53.3%, hitBE = 35/75 ≈ 46.7%). This avoids penalizing chip-saving as a strategy and prevents NaN propagation when chipRoi is empty."

    Create `src/lib/season-review.test.ts` with `describe('computeDecisionGrade — D-05/D-06 grade thresholds', () => { ... })` containing all 10 behavior tests above. Use `import { describe, it, expect } from 'vitest'`. The test file is the RED step — write it first, run `npx vitest run src/lib/season-review` to confirm failure (file does not exist or function undefined), then implement to GREEN.

    Commit sequence per `<tdd_integration>`:
    1. `test(124-01): add failing tests for computeDecisionGrade D-05/D-06`
    2. `feat(124-01): implement computeDecisionGrade per CONTEXT.md D-05/D-06`
  </action>
  <verify>
    <automated>npx vitest run src/lib/season-review</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/season-review.ts` contains `export function computeDecisionGrade(`
    - `src/lib/season-review.ts` contains `export type GradeLabel`
    - `src/lib/season-review.ts` contains the literal `40 / 75` and `35 / 75` (D-06 renormalization constants)
    - `src/lib/season-review.test.ts` contains `describe('computeDecisionGrade` and at least 10 `it(` cases covering grade boundaries A/B/C/D, D-06 chip=0 renormalization, NaN guard, partial D-06, standard 3-component, and GradeLabel type compile-check
    - `npx vitest run src/lib/season-review` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RED→GREEN→commit sequence complete; `computeDecisionGrade` exported from `src/lib/season-review.ts` and all 10 behavior tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create /api/season-review Route Handler with parallel FPL fetches + aggregation</name>
  <files>src/app/api/season-review/route.ts, src/app/api/season-review/route.test.ts</files>
  <read_first>
    - src/app/api/season-analytics/route.ts (full file — canonical pattern: FPL_BASE/FPL_UA constants, non-fatal fetch helpers, GET handler with `/^\d+$/.test()` SSRF guard, Promise.all parallel, `current.length === 0` empty guard, Cache-Control header `public, s-maxage=1800, stale-while-revalidate=86400`)
    - src/app/api/season-analytics/route.test.ts (if exists; otherwise look at any *.route.test.ts under src/app/api/ for vi.stubGlobal('fetch') pattern)
    - .planning/phases/124-season-review/124-PATTERNS.md §`src/app/api/season-review/route.ts` (exact interface blocks + fetch helpers + aggregation steps)
    - .planning/phases/124-season-review/124-RESEARCH.md Pitfalls 1, 3, 4, 6 (overall_rank field, NaN guards, average_entry_score exact field name)
    - src/lib/types.ts (verify SeasonReview / SeasonGwEntry shape from Task 1)
  </read_first>
  <behavior>
    - Test 1: GET with missing `teamId` query param returns 400 with JSON body `{ error: 'Invalid teamId parameter' }`
    - Test 2: GET with non-numeric `teamId` (e.g. `?teamId=abc`) returns 400 (SSRF guard before URL construction)
    - Test 3: GET with `teamId=99999` and stubbed FPL history fetch returning null returns 502 with JSON body containing `error`
    - Test 4: GET with valid teamId + stubbed history response (containing `current[]` with 3 GWs and `chips[]` with one chip) + stubbed bootstrap response (containing `events[]` with `average_entry_score` per GW) returns 200 with JSON body matching `SeasonReview` shape: `totalPoints` = sum of points, `finalRank` = last entry's `overall_rank`, `bestGw` / `worstGw` reflect max/min points entries, `transferNetPoints` = `-(sum of event_transfers_cost)`, `gwData[]` has length 3 with `chipPlayed` matching the stubbed chip event and `null` elsewhere
    - Test 5: GET response includes `Cache-Control: public, s-maxage=1800, stale-while-revalidate=86400` header
    - Test 6: GET with empty `current[]` returns 200 with `totalPoints=0`, `finalRank=0`, `bestGw={gw:0,points:0}`, `worstGw={gw:0,points:0}`, `transferNetPoints=0`, `gwData=[]` (Pitfall 6 — no division by zero / no crash on empty season)
    - Test 7: `avgManagerScore` in `gwData[i]` is sourced from bootstrap `events[].average_entry_score` (NOT `average_score`) — verify by stubbing bootstrap with distinguishable per-GW values and asserting they appear in the response
    - Test 8: When bootstrap fetch fails (returns empty events array), `avgManagerScore` falls back to 0 for all GWs (non-fatal — history fetch is the only blocking dependency)
  </behavior>
  <action>
    Create `src/app/api/season-review/route.ts` mirroring `src/app/api/season-analytics/route.ts` structure. Required elements (copy patterns verbatim from PATTERNS.md §`src/app/api/season-review/route.ts`):

    1. **File header comment** with `// Phase 124 REV-01: /api/season-review route.` + `Sources of truth:` block citing CONTEXT.md D-01..D-04, PATTERNS.md, RESEARCH.md Pitfalls 1/3/4/6.

    2. **Imports**: `import type { NextRequest } from 'next/server'` and `import type { SeasonReview, SeasonGwEntry } from '@/lib/types'`.

    3. **Constants**:
       - `const FPL_BASE = 'https://fantasy.premierleague.com/api'`
       - `const FPL_UA = 'fplx/1.24 (+https://fplx.app)'` (bump version to match phase milestone)

    4. **Local interface block** — copy from PATTERNS.md exactly, with `overall_rank: number` ADDED to `FPLHistoryCurrent` (Pitfall 1):
       - `interface FPLHistoryChip { name: string; event: number; time: string }`
       - `interface FPLHistoryCurrent { event: number; points: number; event_transfers_cost: number; overall_rank: number }`
       - `interface FPLHistoryResponse { chips?: FPLHistoryChip[]; current?: FPLHistoryCurrent[] }`
       - `interface FPLBootstrapEvent { id: number; average_entry_score: number; finished: boolean }` — field name `average_entry_score` per CONTEXT.md D-02 and RESEARCH Pitfall 6.

    5. **Non-fatal fetch helpers** `fetchHistory(teamId)` and `fetchBootstrapEvents()` per the PATTERNS.md template. Both wrap fetch in try/catch and return null/[] on failure. Use `'User-Agent': FPL_UA` header on both.

    6. **GET handler**:
       - SSRF guard: `if (!teamIdParam || !/^\d+$/.test(teamIdParam)) return Response.json({ error: 'Invalid teamId parameter' }, { status: 400 })`
       - `Promise.all([fetchHistory(teamId), fetchBootstrapEvents()])`
       - If `history === null` return 502 with `{ error: 'FPL history fetch failed' }`
       - Extract `current = Array.isArray(history.current) ? history.current : []` and `chips = Array.isArray(history.chips) ? history.chips : []`
       - Build `eventToAvgScore: Map<number, number>` from `bootstrapEvents` using `event.id → event.average_entry_score`; fall back to `0` for events missing from the bootstrap response (Test 8).
       - Build `eventToChip: Map<number, string>` from `chips` using `chip.event → chip.name`.
       - Aggregate `gwData: SeasonGwEntry[]` by iterating `current`, mapping each entry to `{ gw: entry.event, points: entry.points, avgManagerScore: eventToAvgScore.get(entry.event) ?? 0, overallRank: entry.overall_rank, chipPlayed: eventToChip.get(entry.event) ?? null }`. Order by `gw` ascending (already in API order, but `current.slice().sort((a,b) => a.event - b.event)` to be defensive).
       - Compute summary fields:
         - `totalPoints` = sum of `entry.points` across current
         - `finalRank` = `current[current.length - 1]?.overall_rank ?? 0`
         - `bestGw` = entry with max `points` (`{ gw, points }`); when `current.length === 0`, return `{ gw: 0, points: 0 }`
         - `worstGw` = entry with min `points`; same empty-fallback
         - `transferNetPoints` = `-(sum of event_transfers_cost)` — i.e. multiply by -1 so a value of `-8` means two hits taken (UI-SPEC §Transfer net contract).
       - Return `Response.json(payload, { status: 200, headers: { 'Cache-Control': 'public, s-maxage=1800, stale-while-revalidate=86400' } })`.

    7. **Empty-current guard** (Pitfall 6): if `current.length === 0`, return immediately with the zero/empty payload above (do NOT compute bestGw/worstGw via reduce on empty array).

    Create `src/app/api/season-review/route.test.ts` with the 8 behavior tests above. Stub fetch via `vi.stubGlobal('fetch', vi.fn())` and assert response body + headers. Construct `Request` objects via `new Request('http://localhost/api/season-review?teamId=...')` cast to `NextRequest` (matching the season-analytics test pattern if it exists; otherwise use the simpler `new NextRequest(new URL(...))` form — read an existing route test first to pick the correct shape).

    Commit sequence:
    1. `test(124-01): add failing tests for /api/season-review route`
    2. `feat(124-01): implement /api/season-review per REV-01 + D-02/D-04`
  </action>
  <verify>
    <automated>npx vitest run src/app/api/season-review src/lib/season-review</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/season-review/route.ts` contains `export async function GET(request: NextRequest)`
    - Route file contains the literal string `'Invalid teamId parameter'` and the literal SSRF guard `/^\d+$/.test(teamIdParam)`
    - Route file contains `overall_rank: number` inside the local `FPLHistoryCurrent` interface (Pitfall 1 fix)
    - Route file contains the literal string `average_entry_score` (Pitfall 6 — exact field name)
    - Route file contains `Cache-Control` header value `public, s-maxage=1800, stale-while-revalidate=86400`
    - Route file imports `SeasonReview` and `SeasonGwEntry` from `@/lib/types`
    - Route file uses `Promise.all([fetchHistory(teamId), fetchBootstrapEvents()])`
    - `src/app/api/season-review/route.test.ts` contains 8 `it(` cases covering all behaviors listed above
    - `npx vitest run src/app/api/season-review` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Route handler returns aggregated `SeasonReview` JSON for valid teamId, 400 for invalid teamId, 502 on upstream history failure; all 8 route tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API route | `teamId` query param is untrusted user input crossing into URL construction for upstream FPL API call |
| API route → FPL upstream | Untrusted JSON response body crosses into typed deserialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering (SSRF) | `src/app/api/season-review/route.ts` GET handler | mitigate | Validate `teamId` against `/^\d+$/.test()` BEFORE constructing FPL URL; reject with 400 on non-match. Established pattern from season-analytics/route.ts:100-101. |
| T-124-02 | Tampering (response injection) | `src/app/api/season-review/route.ts` fetch helpers | mitigate | TypeScript interface cast on FPL response; `try/catch` around `fetch + res.json()` returns null/[] on parse failure. Malformed upstream JSON cannot propagate to client. |
| T-124-03 | Denial of Service (upstream latency cascade) | `src/app/api/season-review/route.ts` GET handler | accept | FPL upstream is the same external dependency the rest of the app already relies on; no additional surface added. Cache-Control `s-maxage=1800` reduces hot-path pressure. |
| T-124-04 | Information Disclosure | `src/app/api/season-review/route.ts` GET handler | accept | All returned data is public FPL data (already accessible via fantasy.premierleague.com directly). No PII, no session tokens, no internal state exposed. |
</threat_model>

<verification>
- `npx vitest run src/lib/season-review src/app/api/season-review` — all tests green
- `npx tsc --noEmit` — no type errors
- Manual probe (informational, not blocking): `curl 'http://localhost:3000/api/season-review?teamId=1' | head -c 500` returns either 200 + SeasonReview JSON or 502 (depending on FPL upstream availability during dev)
- Grep gate: `grep -v '^//' src/app/api/season-review/route.ts | grep -c "average_entry_score"` returns at least 1 (Pitfall 6 protection)
- Grep gate: `grep -v '^//' src/app/api/season-review/route.ts | grep -c "overall_rank"` returns at least 2 (interface declaration + usage in mapping; Pitfall 1 protection)
</verification>

<success_criteria>
- `SeasonGwEntry` and `SeasonReview` types exported from `src/lib/types.ts`
- `computeDecisionGrade` + `GradeLabel` exported from `src/lib/season-review.ts` with full D-05/D-06 logic
- `/api/season-review` Route Handler returns `SeasonReview` JSON for valid numeric `teamId` and 400/502 on invalid input / upstream failure
- All Wave 1 tests pass: `npx vitest run src/lib/season-review src/app/api/season-review`
- TypeScript compiles: `npx tsc --noEmit` exits 0
- Three atomic commits per task (test → feat)
</success_criteria>

<output>
After completion, create `.planning/phases/124-season-review/124-01-SUMMARY.md` documenting:
- Final shape of `SeasonReview` and `SeasonGwEntry` types (verbatim TypeScript)
- Exact `computeDecisionGrade` signature and branching behavior
- `/api/season-review` request/response contract (200/400/502 paths)
- Any deviations from PATTERNS.md (e.g. additional defensive guards)
- Test command and pass count
</output>
