---
phase: 123
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/requirements.txt
  - pipeline/player_matching.py
  - pipeline/transfer_news.py
  - pipeline/lineup_news.py
  - pipeline/tests/test_player_matching.py
  - pipeline/tests/test_transfer_news.py
  - pipeline/tests/conftest.py
autonomous: true
requirements: [SCR-01, SCR-02, SCR-03, SCR-05]
tags: [scraper, rss, rapidfuzz, transfer-news, python-pipeline]

must_haves:
  truths:
    - "rapidfuzz>=3.0.0 is declared in pipeline/requirements.txt and importable in the pipeline runtime"
    - "lineup_news.py imports build_name_lookup and match_player from player_matching.py and does NOT import difflib for player-name matching (SCR-02 shared utility)"
    - "player_matching.build_name_lookup(elements) returns dict[str, int] of lowercased web_name/second_name → element_id"
    - "player_matching.match_player(text, lookup) returns int element_id when rapidfuzz token_sort_ratio >= 85, else None"
    - "transfer_news.scrape(bootstrap) early-returns and logs '[transfer_news] TRANSFER_NEWS_ENABLED not set — skipping' when env var is unset or not 'true'"
    - "transfer_news.scrape() fetches Sky Sports RSS and BBC Sport RSS, isolated by per-source try/except so one source failure does not bail the other"
    - "Each article in the output artifact has a classification field equal to one of: confirmed_signing, rumour, injury_return, rotation_signal, general"
    - "transfer_news.scrape() writes via upload.save('transfer_news.json', payload); never imports vercel_blob directly"
    - "transfer_news.scrape() never writes save() when articles list is empty (SCRP-05 empty guard)"
  artifacts:
    - path: "pipeline/requirements.txt"
      provides: "rapidfuzz dependency declaration"
      contains: "rapidfuzz>=3.0.0"
    - path: "pipeline/player_matching.py"
      provides: "Fuzzy player-name → element_id utility (SCR-02)"
      exports: ["build_name_lookup", "match_player", "FUZZY_CUTOFF"]
    - path: "pipeline/transfer_news.py"
      provides: "RSS scraper, classifier, and artifact writer (SCR-01, SCR-03, SCR-05)"
      exports: ["scrape", "classify_article", "CLASSIFICATION_KEYWORDS"]
    - path: "pipeline/tests/test_player_matching.py"
      provides: "Unit tests covering exact match, fuzzy >=85 match, sub-85 reject, short-word skip"
    - path: "pipeline/tests/test_transfer_news.py"
      provides: "Unit tests for TRANSFER_NEWS_ENABLED gate, classifier, non-fatal isolation, empty-guard"
  key_links:
    - from: "pipeline/transfer_news.py"
      to: "pipeline/player_matching.py"
      via: "from player_matching import build_name_lookup, match_player"
      pattern: "from player_matching import"
    - from: "pipeline/lineup_news.py"
      to: "pipeline/player_matching.py"
      via: "from player_matching import build_name_lookup, match_player"
      pattern: "from player_matching import"
    - from: "pipeline/transfer_news.py"
      to: "pipeline/upload.py"
      via: "from upload import save"
      pattern: "from upload import save"
    - from: "pipeline/player_matching.py"
      to: "rapidfuzz"
      via: "from rapidfuzz import fuzz"
      pattern: "from rapidfuzz import fuzz"
---

<objective>
Build the Python pipeline foundation for transfer news: install rapidfuzz, create the player_matching utility, create the transfer_news scraper with classifier, env gate and non-fatal RSS isolation, and ship pytest coverage for all of it.

Purpose: Satisfies SCR-01, SCR-02, SCR-03, SCR-05. Wave 1 establishes the public scrape() contract that Plan 03 (run.py wiring) and the artifact shape that Plan 02 (TS hook/route) depend on.

Output: rapidfuzz declared in requirements.txt, two new pipeline modules (player_matching.py, transfer_news.py), and three new test files exercising the gate, classifier, fuzzy matcher, and isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-scraper-02-pipeline/123-CONTEXT.md
@.planning/phases/123-scraper-02-pipeline/123-RESEARCH.md
@.planning/phases/123-scraper-02-pipeline/123-PATTERNS.md
@.planning/phases/123-scraper-02-pipeline/123-VALIDATION.md

<interfaces>
<!-- Existing reusable APIs that transfer_news.py and player_matching.py must use. -->
<!-- Do not re-explore the codebase — these are the contracts. -->

From pipeline/upload.py (only Blob write path):
```python
def save(key: str, data: dict | list) -> None: ...
```

From pipeline/lineup_news.py (canonical analog — copy structure, NOT difflib):
```python
SKY_RSS_URL = 'https://www.skysports.com/rss/11095'
BBC_RSS_URL = 'https://feeds.bbci.co.uk/sport/football/rss.xml'
HEADLINE_MAX_LEN = 280
ERROR_MAX_LEN = 200

def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()

# source_health dict shape (init):
source_health = {
    'skysports': {'ok': False, 'last_success': None, 'last_error': None},
    'bbc':       {'ok': False, 'last_success': None, 'last_error': None},
}

# Per-source isolation pattern (each in its own try/except):
try:
    _scrape_rss_sky(...)
    source_health['skysports']['ok'] = True
    source_health['skysports']['last_success'] = scraped_at
except Exception as exc:
    source_health['skysports']['last_error'] = str(exc)[:ERROR_MAX_LEN]
    print(f"[transfer_news/skysports] non-fatal: {exc}", file=sys.stderr)

# Empty-guard (SCRP-05 mirror):
if not articles:
    print("[transfer_news] articles list empty — skipping save, preserving previous run")
    return
save('transfer_news.json', payload)
```

From rapidfuzz (D-01 — 0-100 scale, NOT 0.0-1.0):
```python
from rapidfuzz import fuzz
score: int = fuzz.token_sort_ratio(text_a, text_b)   # 0-100
# Threshold: score >= 85
```

Public API contract that Plan 03 and Plan 02 depend on:
```python
# pipeline/player_matching.py
FUZZY_CUTOFF: int = 85

def build_name_lookup(elements: list) -> dict[str, int]:
    """Lowercased web_name/second_name → element_id (int, not full element dict)."""

def match_player(text: str, name_lookup: dict, cutoff: int = FUZZY_CUTOFF) -> int | None:
    """Return element_id or None if best rapidfuzz token_sort_ratio < cutoff."""

# pipeline/transfer_news.py
CLASSIFICATION_KEYWORDS: dict[str, list[str]] = {...}

def classify_article(title: str, summary: str | None) -> str:
    """Return one of: confirmed_signing|rumour|injury_return|rotation_signal|general."""

def scrape(bootstrap: dict) -> None:
    """Env-gated; isolated per RSS source; writes transfer_news.json via upload.save; empty-guard."""
```

Artifact JSON shape (must match TypeScript TransferNewsArticle / TransferNewsFeed in Plan 02):
```json
{
  "scraped_at": "2026-05-18T12:00:00+00:00",
  "articles": [
    {
      "title": "...",
      "summary": "..." | null,
      "url": "https://...",
      "published": "2026-05-18T10:30:00Z" | null,
      "source": "skysports" | "bbc",
      "classification": "confirmed_signing" | "rumour" | "injury_return" | "rotation_signal" | "general",
      "element_id": 42 | null,
      "scraped_at": "2026-05-18T12:00:00+00:00"
    }
  ],
  "source_health": {
    "skysports": {"ok": true, "last_success": "...", "last_error": null},
    "bbc":       {"ok": true, "last_success": "...", "last_error": null}
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01: Add rapidfuzz dependency and create test scaffolding</name>
  <files>pipeline/requirements.txt, pipeline/tests/conftest.py, pipeline/tests/test_player_matching.py, pipeline/tests/test_transfer_news.py</files>
  <read_first>
    - pipeline/requirements.txt (current dependency list — confirm rapidfuzz absent per CF-02)
    - pipeline/tests/conftest.py (existing fixtures; understand the conftest pattern before modifying)
    - pipeline/tests/test_lineup_news.py if present, otherwise pipeline/tests/test_merge.py (pytest style and import path setup used in this repo)
    - pipeline/lineup_news.py (constants, _build_name_lookup, classification logic to mirror in tests)
  </read_first>
  <action>
    Append a single line `rapidfuzz>=3.0.0` to `pipeline/requirements.txt` (CF-02 / RESEARCH.md). Do not reorder existing lines.

    Install the dependency locally so subsequent pytest runs succeed: `pip install rapidfuzz>=3.0.0` (executor command, not a code change). If `conftest.py` does not already add `pipeline/` to `sys.path`, ensure existing fixture pattern is preserved — do NOT rewrite conftest.py from scratch; only append additional fixtures if needed.

    Create RED-stage test stubs (Nyquist Wave 0). Tests MUST initially fail with `ModuleNotFoundError: No module named 'player_matching'` / `transfer_news` — that is correct for RED.

    `pipeline/tests/test_player_matching.py` — minimum test cases:
      - test_build_name_lookup_lowercases_web_and_second_name: given elements [{'id': 1, 'web_name': 'Salah', 'second_name': 'Salah Mohamed'}], the returned dict has 'salah' → 1 and 'salah mohamed' → 1
      - test_build_name_lookup_skips_duplicate_second_name: when web_name and second_name are equal (case-insensitive), only one entry is created
      - test_match_player_exact_match_returns_element_id: match_player('Salah', lookup) returns the id
      - test_match_player_fuzzy_above_cutoff_returns_element_id: match_player('Mohammed Salah', lookup_with_'mohamed salah') returns the id (token_sort_ratio >= 85)
      - test_match_player_below_cutoff_returns_none: match_player('Completely Different Name', lookup) returns None
      - test_match_player_short_word_skip_avoids_false_positive: text containing only 3-char tokens (Pitfall 8 from RESEARCH.md) does not match
      - test_fuzzy_cutoff_is_85_not_point_85: assert FUZZY_CUTOFF == 85 (Pitfall 1 from RESEARCH.md — scale guard)

    `pipeline/tests/test_transfer_news.py` — minimum test cases:
      - test_scrape_early_returns_when_env_var_unset: monkeypatch os.environ removing TRANSFER_NEWS_ENABLED, call scrape({}), capture stdout, assert '[transfer_news] TRANSFER_NEWS_ENABLED not set — skipping' is in output and upload.save was NOT called
      - test_scrape_early_returns_when_env_var_false: env var = 'false', same assertion
      - test_classify_confirmed_signing: classify_article('Arsenal sign striker', None) == 'confirmed_signing'
      - test_classify_rumour: classify_article('Spurs linked with midfielder', None) == 'rumour'
      - test_classify_injury_return: classify_article('Saka returns to fitness', None) == 'injury_return'
      - test_classify_rotation_signal: classify_article('Player rotation expected', None) == 'rotation_signal'
      - test_classify_general_fallback: classify_article('Pre-match interview', None) == 'general'
      - test_classify_case_insensitive: classify_article('Arsenal SIGNS striker', None) == 'confirmed_signing' (Pitfall 6 from RESEARCH.md)
      - test_classify_uses_summary_text: classify_article('Title text', 'transfer rumour bid') == 'rumour'
      - test_non_fatal_isolation_sky_failure: monkeypatch feedparser.parse for SKY url to raise, BBC returns 1 valid entry; scrape() does not raise, source_health['skysports']['ok'] is False and ['bbc']['ok'] is True
      - test_empty_articles_guard: monkeypatch both feeds to return zero entries; verify upload.save is NEVER called (mock via patch); stdout contains 'articles list empty — skipping save'
      - test_writes_artifact_with_correct_shape: feedparser returns one entry per feed; bootstrap has matching player; assert upload.save called once with key 'transfer_news.json' and payload has scraped_at, articles[] (len >= 1), source_health.skysports, source_health.bbc; each article has title, summary, url, published, source, classification, element_id, scraped_at

    All transfer_news tests must monkeypatch TRANSFER_NEWS_ENABLED='true' (use a pytest fixture in conftest.py if shared) and monkeypatch `upload.save` to a MagicMock so no real I/O occurs.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_player_matching.py tests/test_transfer_news.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^rapidfuzz' pipeline/requirements.txt` returns 1
    - `python -c "import rapidfuzz; from rapidfuzz import fuzz; print(fuzz.token_sort_ratio('Salah', 'Mohamed Salah'))"` exits 0 and prints an integer 0-100
    - `cd pipeline && python -m pytest tests/test_player_matching.py --collect-only -q` lists at least 6 test cases (build_name_lookup_lowercases, skips_duplicate_second_name, exact_match, fuzzy_above_cutoff, below_cutoff_returns_none, short_word_skip, cutoff_is_85)
    - `cd pipeline && python -m pytest tests/test_transfer_news.py --collect-only -q` lists at least 11 test cases (env_unset, env_false, 5 classifier cases, case_insensitive, summary_text, non_fatal_isolation, empty_articles_guard, writes_artifact)
    - At this RED stage, tests can fail with ModuleNotFoundError — but they MUST be collected successfully (no SyntaxError, no import error in the test file itself; the import target may still be missing)
  </acceptance_criteria>
  <done>
    rapidfuzz declared in requirements.txt and installable; player_matching and transfer_news test stubs are pytest-collectible; tests fail at this point only because the implementation modules do not yet exist (RED state, expected).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 02: Implement player_matching.py with rapidfuzz token_sort_ratio</name>
  <files>pipeline/player_matching.py</files>
  <read_first>
    - pipeline/lineup_news.py lines 122-176 (canonical _build_name_lookup and _match_player using difflib — pattern to MIRROR but with rapidfuzz substitution)
    - .planning/phases/123-scraper-02-pipeline/123-PATTERNS.md "pipeline/player_matching.py" section (lines 140-208)
    - .planning/phases/123-scraper-02-pipeline/123-RESEARCH.md "Pattern 2: rapidfuzz token_sort_ratio Player Matching" and "Pitfall 1" (scale confusion) and "Pitfall 8" (short-name false positives)
    - pipeline/tests/test_player_matching.py (the contract tests just written in Task 01)
  </read_first>
  <behavior>
    - build_name_lookup(elements) builds dict from elements list using web_name and second_name keys, lowercased, deduped when both names match case-insensitively; values are int element_id, NOT full element dict (key difference from lineup_news.py)
    - match_player('Salah', lookup) returns the element_id by exact lowercased lookup
    - match_player with text whose best token_sort_ratio >= 85 against any lookup key returns that element_id
    - match_player skips word tokens with len < 4 (Pitfall 8) before fuzzy scoring
    - match_player returns None when best score < cutoff
    - match_player returns None when text is empty string or None
    - FUZZY_CUTOFF is an int of value 85 (Pitfall 1 documentation)
  </behavior>
  <action>
    Implement `pipeline/player_matching.py` with module docstring referencing SCR-02 and locking D-01 decision rationale. Constants: `FUZZY_CUTOFF: int = 85` (include comment "rapidfuzz token_sort_ratio scale is 0-100; threshold is >=85, NOT >=0.85 — see Pitfall 1").

    Imports: `from rapidfuzz import fuzz`.

    `build_name_lookup(elements: list) -> dict[str, int]`: iterate elements; for each, read `web_name` and `second_name`; insert lowercased non-empty keys mapping to `element['id']` (int); skip second_name if equal to web_name case-insensitively. Match lineup_news.py `_build_name_lookup` structure verbatim, only change the value type from `element` dict to `element['id']`.

    `match_player(text: str, name_lookup: dict, cutoff: int = FUZZY_CUTOFF) -> int | None`:
      1. If not text: return None
      2. `query = text.lower().strip()`
      3. Direct exact-match: `if query in name_lookup: return name_lookup[query]`
      4. Per-word loop: `for word in query.split(): if len(word) < 4: continue` (mirrors lineup_news.py line 159 short-token skip, but raised from <3 to <4 per RESEARCH.md Pitfall 8)
      5. For each candidate (word, full name) pair, compute `score = fuzz.token_sort_ratio(word, name)`; track best_score and best_id
      6. After full-string fallback also try `fuzz.token_sort_ratio(query, name)` for each lookup name; update best if higher
      7. Return `best_id if best_score >= cutoff else None`

    Do NOT use `difflib`. Do NOT import from `lineup_news`. Public API is exactly `build_name_lookup`, `match_player`, `FUZZY_CUTOFF`.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_player_matching.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd pipeline && python -m pytest tests/test_player_matching.py -x -q` exits 0 with all test cases passing (GREEN state)
    - `grep -c "from rapidfuzz import fuzz" pipeline/player_matching.py` returns 1
    - `grep -c "import difflib" pipeline/player_matching.py` returns 0 (must not use difflib)
    - `grep -c "FUZZY_CUTOFF = 85" pipeline/player_matching.py` returns 1 (integer literal, not 0.85)
    - `python -c "from pipeline.player_matching import build_name_lookup, match_player, FUZZY_CUTOFF; assert FUZZY_CUTOFF == 85; lookup = build_name_lookup([{'id': 1, 'web_name': 'Salah', 'second_name': 'Mohamed Salah'}]); assert lookup.get('salah') == 1; assert match_player('Salah', lookup) == 1; assert match_player('Mohammed Salah', lookup) == 1; assert match_player('Random Person', lookup) is None"` exits 0
  </acceptance_criteria>
  <done>
    player_matching.py is GREEN: all unit tests pass; rapidfuzz used exclusively for fuzzy scoring; FUZZY_CUTOFF correctly an integer 85; short-word skip threshold raised to <4 per Pitfall 8.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03: Implement transfer_news.py scraper, classifier, env gate, and isolation</name>
  <files>pipeline/transfer_news.py</files>
  <read_first>
    - pipeline/lineup_news.py (FULL file — canonical RSS scraper structure, constants, source_health dict, per-source try/except, _now_iso helper, feedparser usage, empty-guard, save() call)
    - pipeline/upload.py (save() API — confirm signature `save(key: str, data: dict)`)
    - .planning/phases/123-scraper-02-pipeline/123-PATTERNS.md "pipeline/transfer_news.py" section (lines 26-138)
    - .planning/phases/123-scraper-02-pipeline/123-RESEARCH.md "Patterns 1, 6, 7" (non-fatal isolation, env gate, empty-guard) and "Critical Constraints (Anti-Patterns)" 1-8
    - pipeline/tests/test_transfer_news.py (the contract tests written in Task 01)
  </read_first>
  <behavior>
    - scrape(bootstrap) early-returns when TRANSFER_NEWS_ENABLED env var unset or not lowercase 'true'; prints '[transfer_news] TRANSFER_NEWS_ENABLED not set — skipping' (Pattern 6); does NOT call upload.save
    - When gate is enabled, scrape() reads bootstrap.elements, builds name_lookup via player_matching.build_name_lookup, then attempts Sky Sports RSS in try/except and BBC Sport RSS in try/except (per-source isolation; one failure leaves the other source running)
    - For each feed entry: read title, summary, url, published; classify via classify_article; resolve element_id via player_matching.match_player(title + ' ' + (summary or ''), name_lookup); append article dict to articles list
    - classify_article(title, summary) lowercases concatenated title + ' ' + (summary or ''); returns first matching class in order: confirmed_signing → rumour → injury_return → rotation_signal; else 'general'
    - When articles list is empty after both sources, scrape() logs '[transfer_news] articles list empty — skipping save, preserving previous run' and returns WITHOUT calling upload.save (Pattern 7)
    - When articles list is non-empty, scrape() calls upload.save('transfer_news.json', payload) exactly once with payload shape: {scraped_at, articles, source_health}
    - feedparser.parse() must NEVER bail on feed.bozo — always iterate feed.entries (Pitfall 3)
    - title is truncated to HEADLINE_MAX_LEN = 280 chars; summary truncated to SUMMARY_MAX_LEN = 500 chars (RESEARCH.md security domain V5 input validation)
    - error strings in source_health.last_error are truncated to ERROR_MAX_LEN = 200 chars
  </behavior>
  <action>
    Implement `pipeline/transfer_news.py` with module docstring referencing SCR-01, SCR-03, SCR-05 and the locked D-03, D-04, D-05, D-06 decisions.

    Constants block (mirror lineup_news.py lines 34-44, ADD SUMMARY_MAX_LEN):
      - SKY_RSS_URL = 'https://www.skysports.com/rss/11095'
      - BBC_RSS_URL = 'https://feeds.bbci.co.uk/sport/football/rss.xml'
      - HEADLINE_MAX_LEN = 280
      - SUMMARY_MAX_LEN = 500
      - ERROR_MAX_LEN = 200
      - CLASSIFICATION_KEYWORDS dict per D-03 keyword sets exactly as locked in CONTEXT.md (lines 22-26): keys in iteration order confirmed_signing, rumour, injury_return, rotation_signal; values are the lowercased keyword lists from CONTEXT.md verbatim

    Imports: os, sys, datetime/timezone, feedparser, `from upload import save`, `from player_matching import build_name_lookup, match_player`.

    Helpers:
      - `_now_iso() -> str`: copy verbatim from lineup_news.py lines 51-53
      - `classify_article(title: str, summary: str | None) -> str`: concatenate title + ' ' + (summary or ''), lowercase, iterate CLASSIFICATION_KEYWORDS in insertion order, return first class whose any() keyword is in text; default 'general'
      - `_scrape_rss_sky(articles: list, name_lookup: dict) -> None`: feedparser.parse(SKY_RSS_URL); iterate feed.entries WITHOUT bailing on bozo (Pitfall 3); for each: skip if no title; append article dict with source='skysports', truncated title and summary, classification, element_id from match_player(title + ' ' + summary, name_lookup), scraped_at
      - `_scrape_rss_bbc(articles: list, name_lookup: dict) -> None`: same as Sky but source='bbc'

    Public `scrape(bootstrap: dict) -> None`:
      1. Env gate (Pattern 6): `if os.getenv('TRANSFER_NEWS_ENABLED', '').lower() != 'true': print(...); return`
      2. `scraped_at = _now_iso()`
      3. `name_lookup = build_name_lookup(bootstrap.get('elements', []))`
      4. `articles: list = []`
      5. `source_health = {'skysports': {...}, 'bbc': {...}}` (initial all ok=False, last_success=None, last_error=None)
      6. Sky source isolation block: try _scrape_rss_sky(articles, name_lookup); on success set ok=True, last_success=scraped_at; on Exception capture last_error=str(exc)[:ERROR_MAX_LEN] and print to stderr `[transfer_news/skysports] non-fatal: {exc}`
      7. BBC source isolation block: same pattern with bbc key
      8. Empty-guard (Pattern 7 / Pitfall 4): `if not articles: print('[transfer_news] articles list empty — skipping save, preserving previous run'); return`
      9. payload = {'scraped_at': scraped_at, 'articles': articles, 'source_health': source_health}
      10. save('transfer_news.json', payload)

    Article dict shape MUST match the artifact JSON in <interfaces> exactly — JSON-serialisable, no datetime objects (use ISO strings), no None vs missing key inconsistencies.

    Deduplication and age-cutoff (Claude's Discretion per CONTEXT.md): for v1, deduplicate by exact URL match across both sources (keep first occurrence). No age cutoff in v1 (RSS feeds naturally return recent items only). Document the choice in the module docstring.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_transfer_news.py tests/test_player_matching.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd pipeline && python -m pytest tests/test_transfer_news.py -x -q` exits 0 with all test cases passing (GREEN state)
    - `cd pipeline && python -m pytest tests/test_transfer_news.py tests/test_player_matching.py -x -q` exits 0 (both modules green together — no regression in Plan 01 Task 02)
    - `grep -c "from upload import save" pipeline/transfer_news.py` returns 1
    - `grep -c "import vercel_blob\|from vercel_blob" pipeline/transfer_news.py` returns 0 (Anti-Pattern: never call Blob SDK directly)
    - `grep -c "from player_matching import" pipeline/transfer_news.py` returns 1
    - `grep -v '^#' pipeline/transfer_news.py | grep -c "TRANSFER_NEWS_ENABLED"` returns >= 1 (env gate present; '-v ^#' excludes comment hits to avoid self-invalidating grep gate)
    - `grep -v '^#' pipeline/transfer_news.py | grep -c "feed.bozo"` returns 0 (must never bail on bozo — Pitfall 3)
    - `grep -c "CLASSIFICATION_KEYWORDS" pipeline/transfer_news.py` returns >= 2 (definition + use)
    - `python -c "from pipeline.transfer_news import classify_article; assert classify_article('Arsenal sign striker', None) == 'confirmed_signing'; assert classify_article('Spurs linked with midfielder', None) == 'rumour'; assert classify_article('Player rotation expected', None) == 'rotation_signal'; assert classify_article('Pre-match interview', None) == 'general'"` exits 0
    - `python -c "import os; os.environ.pop('TRANSFER_NEWS_ENABLED', None); from pipeline.transfer_news import scrape; scrape({})"` exits 0 and prints the skip message (env gate works)
  </acceptance_criteria>
  <done>
    transfer_news.py implements the full SCR-01/SCR-03/SCR-05 contract: env-gated, per-source isolated, classifier-correct, empty-guarded, writes via upload.save with the locked artifact shape. All Plan 01 tests are green.
  </done>
</task>


<task type="auto" tdd="true">
  <name>Task 04: Refactor lineup_news.py to use player_matching.py (SCR-02 shared utility)</name>
  <files>pipeline/lineup_news.py</files>
  <read_first>
    - pipeline/lineup_news.py FULL FILE (all lines — understand the existing _build_name_lookup and _match_player using difflib before replacing them)
    - pipeline/player_matching.py (Task 02 output — confirm build_name_lookup(elements) and match_player(text, lookup) public API exists and matches expected signatures)
    - pipeline/tests/test_lineup_news.py (existing lineup_news tests — must all remain GREEN after refactor)
    - .planning/phases/123-scraper-02-pipeline/123-RESEARCH.md "Open Question 2" and "Pitfall 1" (rapidfuzz 0-100 scale vs difflib 0.0-1.0; verify the existing cutoff threshold is preserved semantically)
  </read_first>
  <behavior>
    - lineup_news.py no longer defines _build_name_lookup or _match_player internally
    - lineup_news.py imports `from player_matching import build_name_lookup, match_player` and delegates to those functions
    - All existing tests in test_lineup_news.py continue to pass (no regression in lineup news behavior)
    - difflib is no longer imported in lineup_news.py for player-name matching
    - The output of the name-matching logic in lineup_news.py produces results equivalent to the prior difflib-based implementation for typical inputs (regression guard: test_lineup_news.py tests use fixed player fixtures — these must still pass)
  </behavior>
  <action>
    In `pipeline/lineup_news.py`:

    1. Add `from player_matching import build_name_lookup, match_player` to the imports block at the top of the file. Place immediately after any existing local pipeline imports (e.g. after `from upload import save`).

    2. Remove (or comment out with a `# removed SCR-02: replaced by player_matching.py` note) the internal `_build_name_lookup(elements)` function definition and the `_match_player(text, lookup)` function definition. These use `difflib.get_close_matches` — they are being replaced by the shared `player_matching` module.

    3. Replace every call site of `_build_name_lookup(...)` with `build_name_lookup(...)`. Replace every call site of `_match_player(...)` with `match_player(...)`. Argument lists are API-compatible: `build_name_lookup(elements: list) -> dict[str, int]` and `match_player(text: str, name_lookup: dict) -> int | None`.

    4. Remove the `import difflib` line from lineup_news.py if it is only used by `_match_player`/`_build_name_lookup`. If difflib is used elsewhere in the file, leave it.

    5. Do NOT modify any other function logic in lineup_news.py — this is purely a dependency substitution, not a behavior change.

    **Algorithm note:** rapidfuzz `token_sort_ratio >= 85` (the new threshold from player_matching.py) is semantically similar to difflib `get_close_matches(n=1, cutoff=0.7)` for surname-based matching. The existing test_lineup_news.py tests use fixture player names that are well above or below both thresholds — they are expected to stay GREEN. If a test fails due to a borderline case (score between difflib 0.7 equivalent and rapidfuzz 85), record the test name and failing player fixture in the SUMMARY.md deviation log; do not lower the rapidfuzz threshold.
  </action>
  <verify>
    <automated>cd pipeline && python -m pytest tests/test_lineup_news.py tests/test_player_matching.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd pipeline && python -m pytest tests/test_lineup_news.py -x -q` exits 0 (no regression in lineup news tests)
    - `cd pipeline && python -m pytest tests/test_player_matching.py -x -q` exits 0 (player_matching tests also green — no side effects)
    - `grep -c "from player_matching import build_name_lookup, match_player" pipeline/lineup_news.py` returns 1 (shared import added)
    - `grep -c "import difflib" pipeline/lineup_news.py` returns 0 (difflib removed from player-matching usage — unless difflib is used elsewhere in the file for non-matching purposes; check with `grep -v "match_player\|close_matches" pipeline/lineup_news.py | grep -c "difflib"`)
    - `grep -c "def _build_name_lookup\|def _match_player" pipeline/lineup_news.py` returns 0 (internal implementations removed)
    - `python -c "import pipeline.lineup_news"` exits 0 (module still importable with no errors)
  </acceptance_criteria>
  <done>
    lineup_news.py delegates player-name matching to player_matching.py; difflib removed from matching path; all existing lineup_news tests remain GREEN; SCR-02 shared-utility requirement fully satisfied (player_matching.py is used by both transfer_news.py and lineup_news.py).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External RSS feeds → pipeline | Sky Sports and BBC Sport RSS feed content is untrusted: title/summary may be oversized, contain non-UTF8 bytes, or be manipulated via feed poisoning |
| Pipeline → Vercel Blob | Pipeline writes JSON to Blob; downstream UI trusts artifact shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | RSS feed entry with oversized title | mitigate | Truncate title to HEADLINE_MAX_LEN=280 chars and summary to SUMMARY_MAX_LEN=500 chars before appending to articles list (V5 input validation; mirrors HEADLINE_MAX_LEN in lineup_news.py) |
| T-123-02 | Tampering | RSS feed with malformed XML / non-UTF8 chars | mitigate | feedparser sanitises input; never bail on feed.bozo (Pitfall 3); json.dumps with ensure_ascii=False handles encoding in upload.save |
| T-123-03 | Denial of Service | One RSS source hanging or 5xx-ing blocks whole pipeline | mitigate | Per-source try/except isolation (Pattern 1); feedparser uses default socket timeout; one source failure logs and continues |
| T-123-04 | Denial of Service | Empty articles wipes out previous valid Blob artifact | mitigate | Empty-guard (Pattern 7): never call upload.save when articles list is empty |
| T-123-05 | Information Disclosure | Error message in source_health.last_error leaks secrets | accept | Errors are pipeline internals only (logged to GitHub Actions); truncated to 200 chars; no auth secrets transit transfer_news.py |
</threat_model>

<verification>
- Both new test files exist under `pipeline/tests/` and are pytest-collectible
- `cd pipeline && python -m pytest tests/test_player_matching.py tests/test_transfer_news.py tests/test_lineup_news.py -x -q` exits 0 (all GREEN)
- No regression in existing pipeline tests: `cd pipeline && python -m pytest tests/ -x -q --ignore=tests/test_transfer_news.py --ignore=tests/test_player_matching.py` still green
- rapidfuzz importable: `python -c "from rapidfuzz import fuzz"` exits 0
- player_matching public API functions importable and behave per contract
- transfer_news.scrape() honours env gate, never writes empty artifact, never calls Blob SDK directly
- lineup_news.py imports from player_matching, not difflib; test_lineup_news.py is GREEN
</verification>

<success_criteria>
- SCR-01 (Sky + BBC RSS scrape, write transfer_news.json): satisfied by transfer_news.scrape() with per-source isolation and upload.save call — Transfermarkt RSS omitted per CF-01 (no official feed; CONTEXT.md locks only Sky + BBC; documented in module docstring)
- SCR-02 (player_matching.py shared utility used by both transfer_news.py and lineup_news.py): satisfied by Task 02 (player_matching.py) + Task 03 (transfer_news.py imports it) + Task 04 (lineup_news.py refactored to import from player_matching.py, replacing difflib-based _build_name_lookup/_match_player)
- SCR-03 (5-class classification per article): satisfied by classify_article with D-03 keyword sets, persisted in artifact's articles[].classification field
- SCR-05 (TRANSFER_NEWS_ENABLED env gate; non-fatal pipeline continuation): satisfied by env-gate early-return and per-source try/except isolation; run.py wiring in Plan 03 will provide the outer non-fatal wrap
- All decisions D-01, D-02 (name normalization via lowercased web/second_name), D-03, D-04 implemented exactly as locked
</success_criteria>

<output>
After completion, create `.planning/phases/123-scraper-02-pipeline/123-01-SUMMARY.md`
</output>
