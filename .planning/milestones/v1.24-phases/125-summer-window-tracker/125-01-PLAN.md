---
phase: 125
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/ConfirmedSigningBadge.tsx
  - src/components/shared/ConfirmedSigningBadge.test.tsx
autonomous: true
requirements:
  - WIN-02
tags:
  - badge
  - shared-component
  - transfer-news
must_haves:
  truths:
    - "ConfirmedSigningBadge renders a green pill labeled 'Confirmed Signing' when given any tooltipText"
    - "The badge's title attribute equals the tooltipText prop verbatim"
    - "Badge follows the shared shape contract: inline-block text-xs font-normal rounded px-2 py-1"
    - "Badge has full dark-mode classes alongside light-mode classes"
  artifacts:
    - path: "src/components/shared/ConfirmedSigningBadge.tsx"
      provides: "Confirmed Signing badge React component"
      exports: ["ConfirmedSigningBadge"]
      contains: "Confirmed Signing"
    - path: "src/components/shared/ConfirmedSigningBadge.test.tsx"
      provides: "Vitest contract test for ConfirmedSigningBadge"
      contains: "describe('ConfirmedSigningBadge'"
  key_links:
    - from: "src/components/shared/ConfirmedSigningBadge.tsx"
      to: "Tailwind green token (bg-green-100 / dark:bg-green-900)"
      via: "className string"
      pattern: "bg-green-100"
    - from: "src/components/shared/ConfirmedSigningBadge.test.tsx"
      to: "ConfirmedSigningBadge"
      via: "import"
      pattern: "from './ConfirmedSigningBadge'"
---

<objective>
Create the ConfirmedSigningBadge shared component and its Vitest contract test. The badge is a stateless presentational pill rendered in GemTable expanded rows (D-10) and the OpportunityCostTable PlayerMoveCell buy cluster (D-14). It must exactly follow the existing badge shape contract used by StatusLabelBadge / MinsRiskBadge / VerdictBadge.

Purpose: Provide the shared primitive that Plan 03 imports into GemTable and OpportunityCostTable. Decoupling the primitive from its integration sites keeps Plan 03 focused on wiring and allows the badge to be unit-tested in isolation.

Output:
- src/components/shared/ConfirmedSigningBadge.tsx — green pill component accepting tooltipText prop
- src/components/shared/ConfirmedSigningBadge.test.tsx — Vitest contract test asserting label, classes, title attribute
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-summer-window-tracker/125-CONTEXT.md
@.planning/phases/125-summer-window-tracker/125-RESEARCH.md
@.planning/phases/125-summer-window-tracker/125-PATTERNS.md
@.planning/phases/125-summer-window-tracker/125-UI-SPEC.md

<interfaces>
<!-- Existing badge shape contract (extracted verbatim from src/components/shared/StatusLabelBadge.tsx) -->

From src/components/shared/StatusLabelBadge.tsx:
```tsx
export function StatusLabelBadge({
  statusLabel,
}: {
  statusLabel: StatusLabel | undefined
}) {
  const config = statusLabel ? BADGE_MAP[statusLabel] : undefined
  if (!config) return null
  return (
    <span
      className={`inline-block text-xs font-normal ${config.text} ${config.bg} rounded px-2 py-1`}
      title={config.title}
    >
      {config.label}
    </span>
  )
}
```

Green token reference (from src/components/shared/MinsRiskBadge.tsx "nailed" config):
- bg: `bg-green-100 dark:bg-green-900`
- text: `text-green-800 dark:text-green-200`

Target interface for ConfirmedSigningBadge:
```ts
export function ConfirmedSigningBadge(props: { tooltipText: string }): JSX.Element
```
Always renders (no conditional null branch — caller is responsible for only rendering when a match exists, per D-13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConfirmedSigningBadge component and its Vitest contract test</name>
  <files>src/components/shared/ConfirmedSigningBadge.tsx, src/components/shared/ConfirmedSigningBadge.test.tsx</files>
  <read_first>
    - src/components/shared/StatusLabelBadge.tsx (exact shape contract to mirror)
    - src/components/shared/StatusLabelBadge.test.tsx (exact test structure to mirror)
    - src/components/shared/MinsRiskBadge.tsx (green "nailed" token reference)
    - .planning/phases/125-summer-window-tracker/125-UI-SPEC.md (WIN-02 ConfirmedSigningBadge section, lines 296-322)
    - .planning/phases/125-summer-window-tracker/125-PATTERNS.md (ConfirmedSigningBadge analog and test analog sections)
  </read_first>
  <behavior>
    Test 1 — Renders single span: render(<ConfirmedSigningBadge tooltipText="X" />) yields exactly one span element in container.
    Test 2 — Label text: span textContent equals "Confirmed Signing" exactly.
    Test 3 — Green light-mode classes: span className contains bg-green-100 and text-green-800.
    Test 4 — Green dark-mode classes: span className contains dark:bg-green-900 and dark:text-green-200.
    Test 5 — Shape contract classes: span className contains all of inline-block, text-xs, font-normal, rounded, px-2, py-1.
    Test 6 — Tooltip passthrough: title attribute equals the tooltipText prop verbatim. Use tooltipText="Salah signs new deal · Sky Sports" as the input and assert span?.getAttribute('title') === 'Salah signs new deal · Sky Sports'.
  </behavior>
  <action>
    Create src/components/shared/ConfirmedSigningBadge.tsx exporting a named function ConfirmedSigningBadge that accepts a single prop tooltipText: string and returns a single span element. The span's className must be the literal string `inline-block text-xs font-normal text-green-800 bg-green-100 dark:bg-green-900 dark:text-green-200 rounded px-2 py-1` (per D-11 and UI-SPEC §WIN-02). The span's title attribute must equal tooltipText. The span's children must be the literal text Confirmed Signing.

    Top of file must contain a Phase comment header matching the StatusLabelBadge style — exactly:
    // Phase 125 WIN-02: ConfirmedSigningBadge — confirmed transfer news pill.
    // Sources of truth:
    //   .planning/phases/125-summer-window-tracker/125-CONTEXT.md §D-11, D-15
    //   .planning/phases/125-summer-window-tracker/125-UI-SPEC.md §WIN-02

    The component has no BADGE_MAP and no conditional null branch — it always renders (callers gate with D-13).

    Create src/components/shared/ConfirmedSigningBadge.test.tsx mirroring StatusLabelBadge.test.tsx structure:
    - First line: `// @vitest-environment jsdom`
    - Second line: `// Phase 125 WIN-02 / D-11 / D-15 — ConfirmedSigningBadge contract tests`
    - Imports: `import { describe, it, expect } from 'vitest'`, `import { render } from '@testing-library/react'`, `import { ConfirmedSigningBadge } from './ConfirmedSigningBadge'`
    - One describe('ConfirmedSigningBadge', ...) wrapping the six tests defined in <behavior>.
    - Test 6 must use the exact tooltip string "Salah signs new deal · Sky Sports" (the centred dot U+00B7 between title and source, mirroring the D-15 example).

    Do NOT add any prop other than tooltipText. Do NOT introduce a default tooltipText. Do NOT add a className override prop. Do NOT use BADGE_MAP — the badge is single-state.
  </action>
  <verify>
    <automated>npx vitest run src/components/shared/ConfirmedSigningBadge.test.tsx --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/shared/ConfirmedSigningBadge.tsx exists and exports a named function `ConfirmedSigningBadge`.
    - File src/components/shared/ConfirmedSigningBadge.tsx contains the literal substring `text-green-800 bg-green-100 dark:bg-green-900 dark:text-green-200`.
    - File src/components/shared/ConfirmedSigningBadge.tsx contains the literal substring `inline-block text-xs font-normal`.
    - File src/components/shared/ConfirmedSigningBadge.tsx contains the literal substring `rounded px-2 py-1`.
    - File src/components/shared/ConfirmedSigningBadge.tsx contains the literal text `Confirmed Signing` (the span children).
    - File src/components/shared/ConfirmedSigningBadge.tsx contains the literal substring `title={tooltipText}`.
    - File src/components/shared/ConfirmedSigningBadge.test.tsx contains `describe('ConfirmedSigningBadge'` and the test assertion `Salah signs new deal · Sky Sports`.
    - Command `npx vitest run src/components/shared/ConfirmedSigningBadge.test.tsx --reporter=basic` exits with code 0 and reports 6 passing tests.
    - Command `npx tsc --noEmit` exits 0 with no errors referencing ConfirmedSigningBadge.tsx (typecheck passes).
  </acceptance_criteria>
  <done>
    ConfirmedSigningBadge.tsx exports a single named function that always renders a green pill matching the badge shape contract with the prop tooltipText routed to title. The test file exercises 6 assertions and all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useTransferNews data → ConfirmedSigningBadge tooltipText | Article title and source name flow from the Vercel Blob feed (server-controlled artifact) into the badge's title attribute via the caller. The badge itself never renders user input — it receives a string and routes it to title. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-02 | Tampering (XSS) | ConfirmedSigningBadge title attribute | mitigate | React auto-escapes string interpolation into the `title` DOM attribute; the badge uses `title={tooltipText}` with no `dangerouslySetInnerHTML` anywhere. The shape contract is identical to StatusLabelBadge which already ships this mitigation in production. Test 6 asserts the title attribute round-trips verbatim, which transitively verifies React's escaping (any script tag in input would be escaped, not executed). |
</threat_model>

<verification>
- `npx vitest run src/components/shared/ConfirmedSigningBadge.test.tsx --reporter=basic` exits 0 with 6 passing tests
- `npx tsc --noEmit` exits 0
- `npm run lint -- src/components/shared/ConfirmedSigningBadge.tsx src/components/shared/ConfirmedSigningBadge.test.tsx` exits 0
</verification>

<success_criteria>
- ConfirmedSigningBadge.tsx exports a function matching `({ tooltipText: string }) => JSX.Element`
- All six contract tests pass under Vitest jsdom environment
- File contains no `dangerouslySetInnerHTML`, no `eval`, no `new Function`
- Badge shape contract literal classes are present (verified by string grep in acceptance criteria)
- TypeScript compile is clean
</success_criteria>

<output>
After completion, create `.planning/phases/125-summer-window-tracker/125-01-SUMMARY.md` documenting the badge contract, the file paths created, the test count, and a note that this primitive is consumed by Plan 03 (page.tsx integration).
</output>
