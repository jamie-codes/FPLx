---
phase: 125
plan: 03
type: execute
wave: 2
depends_on:
  - 125-01
  - 125-02
files_modified:
  - src/app/page.tsx
  - src/components/gem-table/GemTable.tsx
  - src/components/transfers/OpportunityCostTable.tsx
autonomous: true
requirements:
  - WIN-01
  - WIN-02
tags:
  - integration
  - sub-tab-registration
  - badge-injection
must_haves:
  truths:
    - "Summer Window sub-tab is visible in the Analyse section nav (desktop label 'Summer Window', mobile label 'Window'), positioned immediately after the Season sub-tab"
    - "Clicking the Summer Window sub-tab renders the SummerWindowTab component (the feed)"
    - "When a confirmed_signing article matches a player.id, a Confirmed Signing badge appears in BOTH the mobile and desktop expanded rows of GemTable for that player, after the FragilityBadge and before the ComparisonSearch"
    - "When a confirmed_signing article matches the buy-side player.id in an OpportunityCostTable row, a Confirmed Signing badge appears in the PlayerMoveCell buy cluster between MinsRiskBadge and NewsBanner"
    - "The badge is absent on player rows with no matching confirmed_signing article (D-13)"
    - "Sell-side player rows in OpportunityCostTable do NOT show the badge (D-16, buy-side only)"
    - "If multiple confirmed_signing articles match the same player, the most recent one (by published ?? scraped_at) supplies the tooltip"
    - "Badge tooltip text equals `${article.title} · ${SOURCE_NAME[article.source]}` (centred dot, full source name 'Sky Sports' or 'BBC Sport')"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Updated SubTab union + SECTIONS array + render condition + import"
      contains: "'window'"
    - path: "src/components/gem-table/GemTable.tsx"
      provides: "ConfirmedSigningBadge injected into both expanded-row blocks"
      contains: "ConfirmedSigningBadge"
    - path: "src/components/transfers/OpportunityCostTable.tsx"
      provides: "ConfirmedSigningBadge injected into PlayerMoveCell buy cluster"
      contains: "ConfirmedSigningBadge"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/news/SummerWindowTab.tsx"
      via: "import + render condition for activeSubTab === 'window'"
      pattern: "from '@/components/news/SummerWindowTab'"
    - from: "src/components/gem-table/GemTable.tsx"
      to: "src/lib/hooks/useTransferNews.ts"
      via: "hook call at component top + per-row article lookup"
      pattern: "useTransferNews\\(\\)"
    - from: "src/components/gem-table/GemTable.tsx"
      to: "src/components/shared/ConfirmedSigningBadge.tsx"
      via: "import + conditional render in both mobile and desktop expanded rows"
      pattern: "from '@/components/shared/ConfirmedSigningBadge'"
    - from: "src/components/transfers/OpportunityCostTable.tsx"
      to: "src/lib/hooks/useTransferNews.ts"
      via: "hook call at top of PlayerMoveCell"
      pattern: "useTransferNews\\(\\)"
    - from: "src/components/transfers/OpportunityCostTable.tsx"
      to: "src/components/shared/ConfirmedSigningBadge.tsx"
      via: "import + conditional render in PlayerMoveCell buy cluster"
      pattern: "from '@/components/shared/ConfirmedSigningBadge'"
---

<objective>
Wire Plan 01 (ConfirmedSigningBadge) and Plan 02 (SummerWindowTab) into the application:

1. Register the Summer Window sub-tab in `src/app/page.tsx`: extend the SubTab union, add the SECTIONS entry in the Analyse section after `'season'`, import SummerWindowTab, and add the render condition.
2. Inject ConfirmedSigningBadge into both expanded-row blocks of `src/components/gem-table/GemTable.tsx`, gated on a per-row confirmed_signing article match.
3. Inject ConfirmedSigningBadge into the `PlayerMoveCell` buy cluster of `src/components/transfers/OpportunityCostTable.tsx`, between MinsRiskBadge and NewsBanner, buy-side only.

Purpose: Plans 01 and 02 deliver isolated primitives. This plan wires them into the surfaces users interact with — the Analyse nav, the gem table expansion, and the transfers opportunity-cost view.

Output:
- src/app/page.tsx — Summer Window registered in nav and routed to SummerWindowTab
- src/components/gem-table/GemTable.tsx — Confirmed Signing badge visible in both expanded rows when matched
- src/components/transfers/OpportunityCostTable.tsx — Confirmed Signing badge visible in buy cluster when matched
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-summer-window-tracker/125-CONTEXT.md
@.planning/phases/125-summer-window-tracker/125-RESEARCH.md
@.planning/phases/125-summer-window-tracker/125-PATTERNS.md
@.planning/phases/125-summer-window-tracker/125-UI-SPEC.md
@.planning/phases/125-summer-window-tracker/125-01-SUMMARY.md
@.planning/phases/125-summer-window-tracker/125-02-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 and Plan 02 -->

From src/components/shared/ConfirmedSigningBadge.tsx (Plan 01):
```ts
export function ConfirmedSigningBadge(props: { tooltipText: string }): JSX.Element
```

From src/components/news/SummerWindowTab.tsx (Plan 02):
```ts
export function SummerWindowTab(): JSX.Element  // 'use client', no props
```

<!-- Existing integration sites (extracted verbatim from current source) -->

From src/app/page.tsx line 57 — SubTab union (must be extended):
```ts
export type SubTab = 'gems' | 'insights' | 'defcon' | 'set-pieces' | 'planner' | 'manual-plan' | 'route-tree' | 'club-form' | 'value-gems' | 'accuracy' | 'season' | 'decision' | 'transfers' | 'optimiser' | 'price-changes' | 'rivals' | 'lineup' | 'review' | 'rank-sim'
```

From src/app/page.tsx lines 70-71 — Analyse subTabs array (insertion point between season and price-changes):
```ts
{ id: 'season' as SubTab,        label: 'Season',          mobileLabel: 'Season'   },
{ id: 'price-changes' as SubTab, label: 'Price Changes',   mobileLabel: 'Prices'   },
```

From src/components/gem-table/GemTable.tsx lines 372-373 (mobile expanded row) and 401-402 (desktop expanded row) — FragilityBadge slot:
```tsx
{/* Phase 93 SENS-01 (D-10): FragilityBadge after RowExpandNewsSection — viewing surface, isTransfer=false */}
{fragility.tier !== 'robust' ? <FragilityBadge tier={fragility.tier} reasons={fragility.reasons} /> : null}
```

From src/components/transfers/OpportunityCostTable.tsx lines 141-151 — PlayerMoveCell buy cluster (insertion between MinsRiskBadge and NewsBanner):
```tsx
<RotationRiskBadge rotationRisk={t.buy.rotation_risk ?? false} />
<StatusLabelBadge statusLabel={lineupNewsMap?.get(t.buy.id)?.status_label} />
<MinsRiskBadge minsRisk={t.buy.mins_risk} />
<NewsBanner ... />
```

From src/lib/types.ts:
```ts
export interface TransferNewsArticle {
  title: string
  url: string
  published: string | null
  source: 'skysports' | 'bbc'
  classification: TransferClass
  element_id: number | null
  scraped_at: string
  // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Summer Window sub-tab in page.tsx (SubTab union + SECTIONS entry + import + render condition)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — read full file before editing; do not assume offsets are stable)
    - src/components/news/SummerWindowTab.tsx (verify the named export exists, created by Plan 02)
    - .planning/phases/125-summer-window-tracker/125-CONTEXT.md §D-17 (sub-tab id, label, mobileLabel, position)
    - .planning/phases/125-summer-window-tracker/125-RESEARCH.md §Pattern 1 (Sub-tab Registration)
    - .planning/phases/125-summer-window-tracker/125-PATTERNS.md (page.tsx self-analog section)
  </read_first>
  <action>
    Make four targeted edits to src/app/page.tsx:

    Edit 1 — Extend the SubTab union (current line ~57): add `| 'window'` to the union. The final union must include `'window'` exactly once.

    Edit 2 — Add the SECTIONS entry: in the Analyse section's `subTabs` array, insert the following entry IMMEDIATELY AFTER the `'season'` entry and BEFORE the `'price-changes'` entry (per D-17):
    `{ id: 'window' as SubTab,        label: 'Summer Window',   mobileLabel: 'Window'   },`
    Match the indentation and alignment of the surrounding entries.

    Edit 3 — Add the import: in the existing import block (near where SeasonReviewTab and PriceChangePanel are imported, around lines 27-28), add a new line:
    `import { SummerWindowTab } from '@/components/news/SummerWindowTab'`

    Edit 4 — Add the render condition: locate the existing block that renders the season tab (`{activeSection !== 'squad' && activeSubTab === 'season' && <SeasonReviewTab teamId={submittedId} />}`) and add the following line IMMEDIATELY AFTER it:
    `{activeSection !== 'squad' && activeSubTab === 'window' && <SummerWindowTab />}`

    Do NOT add `'window'` to the `sectionMemory` initialiser — it maps Section → SubTab, not the reverse (Anti-pattern in RESEARCH.md). Do NOT touch the MobileNav file — it derives from SECTIONS automatically (verified in RESEARCH.md). Do NOT change the `defaultSubTab` for the Analyse section.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx SubTab union contains the literal string `'window'`. Verify with `grep -c "| 'window'" src/app/page.tsx` returns >= 1.
    - src/app/page.tsx contains the literal substring `{ id: 'window' as SubTab,        label: 'Summer Window',   mobileLabel: 'Window'   },` (or a structurally identical line — verify by grepping `label: 'Summer Window'` returns >= 1 hit AND `mobileLabel: 'Window'` returns >= 1 hit).
    - src/app/page.tsx contains the literal import line `import { SummerWindowTab } from '@/components/news/SummerWindowTab'`.
    - src/app/page.tsx contains the literal render line `{activeSection !== 'squad' && activeSubTab === 'window' && <SummerWindowTab />}`.
    - The 'window' SECTIONS entry appears AFTER the 'season' entry and BEFORE the 'price-changes' entry. Verify by reading the Analyse section's subTabs array and confirming order: gems, insights, defcon, set-pieces, club-form, accuracy, season, window, price-changes.
    - Command `npx tsc --noEmit` exits 0 with no errors referencing page.tsx.
    - Command `npm run lint -- src/app/page.tsx` exits 0.
    - `grep -c "'window'" src/app/page.tsx` returns >= 3 (union, SECTIONS, render condition).
  </acceptance_criteria>
  <done>
    page.tsx exposes the Summer Window sub-tab in the Analyse section nav with desktop label "Summer Window" and mobile label "Window", positioned between Season and Price Changes, and clicking it renders the SummerWindowTab component from `@/components/news/SummerWindowTab`. TypeScript compile is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject ConfirmedSigningBadge into both GemTable expanded rows (mobile + desktop), gated on per-player confirmed_signing article match</name>
  <files>src/components/gem-table/GemTable.tsx</files>
  <read_first>
    - src/components/gem-table/GemTable.tsx (current state — read full file; both expanded-row blocks live around lines 340-415)
    - src/components/shared/ConfirmedSigningBadge.tsx (the primitive, created by Plan 01)
    - src/lib/hooks/useTransferNews.ts (hook signature; verify return type)
    - src/lib/types.ts lines 1066-1091 (TransferNewsArticle shape)
    - .planning/phases/125-summer-window-tracker/125-CONTEXT.md §D-10, D-12, D-13, D-15 (placement, match logic, absence, tooltip)
    - .planning/phases/125-summer-window-tracker/125-RESEARCH.md §Pattern 4 (GemTable injection), §Pitfall 1 (both blocks), §Pitfall 2 (rules-of-hooks), §Pitfall 4 (spread-before-sort)
    - .planning/phases/125-summer-window-tracker/125-PATTERNS.md (GemTable self-analog section)
  </read_first>
  <action>
    Make four edits to src/components/gem-table/GemTable.tsx:

    Edit 1 — Add imports near the existing component imports:
    - `import { useTransferNews } from '@/lib/hooks/useTransferNews'`
    - `import { ConfirmedSigningBadge } from '@/components/shared/ConfirmedSigningBadge'`
    - `import type { TransferNewsArticle } from '@/lib/types'`

    Edit 2 — Add a module-level constant SOURCE_NAME near the top of the file (outside the component, after imports):
    `const SOURCE_NAME: Record<'skysports' | 'bbc', string> = { skysports: 'Sky Sports', bbc: 'BBC Sport' }`

    Edit 3 — Add a hook call inside the GemTable function body, alongside the existing hook calls (e.g., next to the existing `usePlayers()` call if present, otherwise at the first hook-call position at the top of the component body):
    `const { data: transferNews } = useTransferNews()`
    This call must be unconditional (not inside any if/map/callback). Rules-of-hooks requires top-level placement (Pitfall 2 — never inside the scoredPlayers.map row callback).

    Edit 4 — Inject the badge into BOTH expanded-row blocks. Locate the mobile expanded row (the `<tr>` that does NOT have `hidden sm:table-row`; the FragilityBadge line appears around line 372-373) and the desktop expanded row (the `<tr className="... hidden sm:table-row">` block around lines 401-402). In each block, IMMEDIATELY AFTER the existing `{fragility.tier !== 'robust' ? <FragilityBadge ... /> : null}` line and BEFORE the `<ComparisonSearch ... />` element, derive the matching article inline and conditionally render the badge:

    Pseudocode (do not paste fenced code into the file; reproduce as inline expression with proper formatting):
    - Compute `confirmedArticle` as the first element of: spread the transferNews articles (use the spread operator before .sort to satisfy Pitfall 4), filter by `a.classification === 'confirmed_signing' && a.element_id === row.original.id`, sort descending by `new Date(a.published ?? a.scraped_at).getTime()`, take index [0]. If `transferNews` is undefined, `confirmedArticle` must resolve to undefined (use optional chaining: `transferNews?.articles`).
    - Render: `{confirmedArticle && <ConfirmedSigningBadge tooltipText={`${confirmedArticle.title} · ${SOURCE_NAME[confirmedArticle.source]}`} />}` — use the centred dot character U+00B7 between title and source name (matches D-15).

    Both blocks must receive identical injections. Add a Phase 125 comment above each badge line: `{/* Phase 125 WIN-02 (D-10, D-12): ConfirmedSigningBadge after FragilityBadge — confirmed transfer news for this player */}`

    Implementation choice: you may extract a small inline IIFE `(() => { const art = ...; return art ? <ConfirmedSigningBadge ... /> : null })()` per block, OR compute `confirmedArticle` once at the row level (e.g., inside the existing `(() => { ... })()` row-render block that wraps the expanded rows) and reference it in both <tr> blocks. The single-derivation approach is preferred because it avoids duplicating the filter/sort logic.

    Do NOT call `useTransferNews()` inside `scoredPlayers.map(...)` or any other callback — only at the top of the GemTable function body (Pitfall 2). Do NOT mutate `transferNews.articles` — always spread before sort (Pitfall 4). Do NOT add a badge to non-expanded row cells — D-10 says expanded row only. Do NOT match on `element_id === null` — `player.id` is always a positive integer, and `null === positiveInt` is always false (Pitfall 5 confirms this is safe).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gem-table/GemTable.tsx contains `import { useTransferNews } from '@/lib/hooks/useTransferNews'`.
    - src/components/gem-table/GemTable.tsx contains `import { ConfirmedSigningBadge } from '@/components/shared/ConfirmedSigningBadge'`.
    - src/components/gem-table/GemTable.tsx contains `const SOURCE_NAME` with both `skysports: 'Sky Sports'` and `bbc: 'BBC Sport'` values.
    - File contains exactly one `useTransferNews()` call (top-level in the component, not inside a callback). Verify with `grep -c "useTransferNews()" src/components/gem-table/GemTable.tsx` returns 1.
    - File contains the literal substring `'confirmed_signing'` (the classification filter constant).
    - File contains the literal substring `element_id === row.original.id`.
    - File contains `[...` (spread) immediately before any new `.sort(` call introduced by this task (Pitfall 4 guard).
    - File contains the substring `<ConfirmedSigningBadge tooltipText={` at least 2 times — once for mobile expanded row, once for desktop expanded row (Pitfall 1 guard). Verify with `grep -c '<ConfirmedSigningBadge' src/components/gem-table/GemTable.tsx` returns >= 2 (if using a shared derivation that renders the badge twice in JSX). If using a shared variable, both `<tr>` blocks must reference the badge expression — verify by reading both blocks.
    - File contains the centred dot character ` · ` (U+00B7) in the tooltipText template literal.
    - Command `npx tsc --noEmit` exits 0 with no errors referencing GemTable.tsx.
    - Command `npm run lint -- src/components/gem-table/GemTable.tsx` exits 0.
    - Command `npm test -- src/components/gem-table` exits 0 (existing GemTable tests still pass).
  </acceptance_criteria>
  <done>
    Both mobile and desktop expanded-row blocks of GemTable render ConfirmedSigningBadge with a tooltip of the form `${title} · ${SOURCE_NAME[source]}` when a confirmed_signing article matches the player.id; the badge is absent when no match exists; useTransferNews is called once at the top of the component (no rules-of-hooks violation); existing GemTable tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Inject ConfirmedSigningBadge into OpportunityCostTable PlayerMoveCell buy cluster (between MinsRiskBadge and NewsBanner, buy-side only)</name>
  <files>src/components/transfers/OpportunityCostTable.tsx</files>
  <read_first>
    - src/components/transfers/OpportunityCostTable.tsx (current state — read full file; PlayerMoveCell is around lines 108-176)
    - src/components/shared/ConfirmedSigningBadge.tsx (the primitive, created by Plan 01)
    - src/lib/hooks/useTransferNews.ts (hook signature)
    - .planning/phases/125-summer-window-tracker/125-CONTEXT.md §D-12, D-14, D-15, D-16 (match logic, placement, tooltip, buy-side only)
    - .planning/phases/125-summer-window-tracker/125-RESEARCH.md §Pattern 5 (PlayerMoveCell injection; Option 1 — hook inside PlayerMoveCell is the recommendation)
    - .planning/phases/125-summer-window-tracker/125-PATTERNS.md (OpportunityCostTable self-analog section)
  </read_first>
  <action>
    Make three edits to src/components/transfers/OpportunityCostTable.tsx:

    Edit 1 — Add imports near the existing component imports:
    - `import { useTransferNews } from '@/lib/hooks/useTransferNews'`
    - `import { ConfirmedSigningBadge } from '@/components/shared/ConfirmedSigningBadge'`

    Edit 2 — Add a module-level constant SOURCE_NAME near the top of the file (after imports, near any existing module-level constants):
    `const SOURCE_NAME: Record<'skysports' | 'bbc', string> = { skysports: 'Sky Sports', bbc: 'BBC Sport' }`

    Edit 3 — Inject the badge in the `PlayerMoveCell` function (around lines 108-176):
    1. Add `const { data: transferNews } = useTransferNews()` as the FIRST line inside the PlayerMoveCell function body, before the existing `if (row.kind === 'roll' || ...) { return ... }` early return. Hook calls must be unconditional (rules-of-hooks).
    2. Inside the `row.transfers.map((t, i) => { ... })` callback, IMMEDIATELY AFTER the `<MinsRiskBadge minsRisk={t.buy.mins_risk} />` line and IMMEDIATELY BEFORE the `<NewsBanner ... />` element, insert the badge with this exact pattern (per RESEARCH.md Pattern 5 IIFE recommendation):

    Pseudocode (write the actual JSX inline; do not paste fenced code blocks into the file):
    - Add a comment: `{/* Phase 125 WIN-02 (D-14, D-16): ConfirmedSigningBadge for buy candidate — buy-side only, between MinsRiskBadge and NewsBanner */}`
    - Then an IIFE that filters transferNews?.articles by `classification === 'confirmed_signing' && element_id === t.buy.id`, spreads-then-sorts descending by `new Date(a.published ?? a.scraped_at).getTime()`, takes [0], and renders `<ConfirmedSigningBadge tooltipText={`${art.title} · ${SOURCE_NAME[art.source]}`} />` when truthy, else null.

    The IIFE pattern is required because the badge needs both a derivation AND a conditional render inside JSX — Pattern 5 shows the exact shape.

    Do NOT match on `t.sell.id` — D-16 is explicit: buy-side only. Do NOT widen `OpportunityCostTableProps` to accept transferNews as a prop — the RESEARCH.md recommendation (Pattern 5, Option 1) is to call the hook directly inside PlayerMoveCell, which is already a 'use client' component. Do NOT mutate `transferNews.articles` — spread before sort (Pitfall 4). Do NOT call `useTransferNews()` inside the `row.transfers.map` callback — only at the top of PlayerMoveCell.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/transfers/OpportunityCostTable.tsx contains `import { useTransferNews } from '@/lib/hooks/useTransferNews'`.
    - src/components/transfers/OpportunityCostTable.tsx contains `import { ConfirmedSigningBadge } from '@/components/shared/ConfirmedSigningBadge'`.
    - File contains `const SOURCE_NAME` with both `skysports: 'Sky Sports'` and `bbc: 'BBC Sport'` values.
    - File contains exactly one `useTransferNews()` call (top of PlayerMoveCell function body). Verify with `grep -c "useTransferNews()" src/components/transfers/OpportunityCostTable.tsx` returns 1.
    - File contains the literal substring `element_id === t.buy.id` (buy-side match, D-16).
    - File does NOT contain the literal substring `element_id === t.sell.id` (sell-side must NOT be matched).
    - File contains `[...` (spread) before the new `.sort(` call introduced by this task.
    - File contains `<ConfirmedSigningBadge tooltipText={` exactly once.
    - File contains the centred dot character ` · ` (U+00B7) in the tooltipText template literal.
    - The badge JSX appears AFTER the line containing `<MinsRiskBadge minsRisk={t.buy.mins_risk}` and BEFORE the line containing `<NewsBanner` in the PlayerMoveCell buy cluster. Verify by reading the file and confirming order.
    - Command `npx tsc --noEmit` exits 0 with no errors referencing OpportunityCostTable.tsx.
    - Command `npm run lint -- src/components/transfers/OpportunityCostTable.tsx` exits 0.
    - Command `npm test -- src/components/transfers` exits 0 (existing OpportunityCostTable tests still pass).
  </acceptance_criteria>
  <done>
    PlayerMoveCell renders ConfirmedSigningBadge in the buy cluster between MinsRiskBadge and NewsBanner when a confirmed_signing article matches t.buy.id; sell-side rows do not show the badge; useTransferNews is called once at the top of PlayerMoveCell; existing OpportunityCostTable tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useTransferNews → ConfirmedSigningBadge tooltipText (GemTable + PlayerMoveCell) | Article title and source name flow from the Vercel Blob feed through GemTable's per-row derivation and PlayerMoveCell's IIFE derivation into the badge's title attribute. |
| useTransferNews → SummerWindowTab article rendering (delegated to Plan 02) | Already mitigated in Plan 02's threat model (T-125-01 link safety + T-125-02 React escaping). This plan's render condition (`<SummerWindowTab />`) does not re-introduce the surface — it is delegated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering (open redirect / tab-napping) | SummerWindowTab (rendered via this plan's route condition) | mitigate | Mitigated entirely in Plan 02 — every `<a>` in SummerWindowTab carries `target="_blank" rel="noopener noreferrer"`. This plan's `<SummerWindowTab />` render condition does not introduce additional links and does not wrap SummerWindowTab's anchors. |
| T-125-02 | Tampering (XSS) | ConfirmedSigningBadge tooltipText interpolated from `article.title` and SOURCE_NAME in GemTable and PlayerMoveCell | mitigate | The tooltipText template `${article.title} · ${SOURCE_NAME[article.source]}` produces a plain JS string. React escapes string interpolation into the `title` DOM attribute (the badge renders `title={tooltipText}` per Plan 01 contract). No `dangerouslySetInnerHTML` is introduced. Acceptance criteria grep explicitly forbids the badge JSX from containing `dangerouslySetInnerHTML`. SOURCE_NAME is a closed-set Record lookup — its values are hard-coded strings, not user input. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 across all three modified files
- `npm run lint -- src/app/page.tsx src/components/gem-table/GemTable.tsx src/components/transfers/OpportunityCostTable.tsx` exits 0
- `npm test` (full suite) exits 0 — verifies no regression in existing GemTable, OpportunityCostTable, or page tests
- Static grep checks (run sequentially via Bash):
  - `grep -c "ConfirmedSigningBadge" src/app/page.tsx` returns 0 (page.tsx does NOT render the badge directly — only routes to SummerWindowTab)
  - `grep -c "ConfirmedSigningBadge" src/components/gem-table/GemTable.tsx` returns >= 2 (import + at least one JSX usage; or import + 2 JSX usages if duplicated per <tr> block)
  - `grep -c "ConfirmedSigningBadge" src/components/transfers/OpportunityCostTable.tsx` returns >= 2 (import + JSX usage)
  - `grep -c "t.sell.id" src/components/transfers/OpportunityCostTable.tsx` after this plan should be unchanged from baseline (no new sell-side badge match) — verify by comparing to the file's pre-edit count
- Manual UI smoke (post-merge, recorded for Phase 125 verifier): Navigate to /, click the Analyse tab, click Summer Window — feed renders. Switch to Gem Ratings, expand a row that has a confirmed signing — badge appears after FragilityBadge. Switch to Squad → Transfers — a row whose buy player has a confirmed signing shows the badge in the buy cluster between MinsRiskBadge and NewsBanner.
</verification>

<success_criteria>
- Summer Window sub-tab appears in the Analyse nav (desktop label "Summer Window", mobile label "Window"), positioned between Season and Price Changes
- Clicking Summer Window renders the SummerWindowTab feed component
- GemTable: ConfirmedSigningBadge appears in BOTH the mobile and desktop expanded-row blocks for any player with a confirmed_signing article match, immediately after the FragilityBadge and before the ComparisonSearch
- OpportunityCostTable: ConfirmedSigningBadge appears in the PlayerMoveCell buy cluster between MinsRiskBadge and NewsBanner for any buy-side player with a confirmed_signing match; sell-side rows never show the badge
- When multiple confirmed_signing articles match a player, the most recent (by `published ?? scraped_at`) supplies the tooltip
- Tooltip format is `${article.title} · ${SOURCE_NAME[article.source]}` with U+00B7 centred dot
- TypeScript compile, lint, and full test suite are clean
- No `useTransferNews()` call inside any callback or conditional (rules-of-hooks)
- No mutation of `transferNews.articles` (spread-before-sort in both injection sites)
- No `dangerouslySetInnerHTML` anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/125-summer-window-tracker/125-03-SUMMARY.md` documenting the four page.tsx edits, the two GemTable expanded-row injections (mobile + desktop), the single PlayerMoveCell buy-cluster injection, the tooltip format, and a confirmation that the full test suite passes.
</output>
