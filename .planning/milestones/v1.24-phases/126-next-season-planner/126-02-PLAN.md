---
phase: 126
plan: 02
type: execute
wave: 1
depends_on:
  - "126-01"
files_modified:
  - pipeline/archive_season.py
  - pipeline/suggest_squad.py
  - pipeline/run.py
autonomous: true
requirements:
  - NSP-01
  - NSP-02
tags:
  - pipeline
  - python
  - blob-storage
must_haves:
  truths:
    - "archive_season(bootstrap) writes season_archive_gw38.json to Vercel Blob during GW38 with >=50% player coverage"
    - "archive_season is idempotent — a re-run while the artifact exists is a no-op"
    - "archive_season fetches element summaries concurrently (~10 workers) instead of sequentially"
    - "suggest_squad.py computes an ILP-optimal 15-player squad via PuLP from archive + bootstrap and writes pre_season_squad.json"
    - "run.py invokes archive_season only when CURRENT_GW == last event id (GW38 gate), positioned BEFORE the IS_OFF_SEASON block"
  artifacts:
    - path: "pipeline/archive_season.py"
      provides: "Per-player element-summary archive (NSP-01)"
      exports: ["archive_season", "_blob_exists", "_fetch_one", "_fetch_all_summaries"]
    - path: "pipeline/suggest_squad.py"
      provides: "PuLP ILP fallback that pre-computes a 15-player pre-season squad"
      exports: ["suggest_squad", "_solve_ilp"]
    - path: "pipeline/run.py"
      provides: "GW38 gate invoking archive_season"
      contains: "IS_GW38"
  key_links:
    - from: "pipeline/archive_season.py"
      to: "pipeline/upload.py save()"
      via: "from upload import save"
      pattern: "from upload import save"
    - from: "pipeline/run.py"
      to: "archive_season"
      via: "from archive_season import archive_season"
      pattern: "from archive_season import archive_season"
    - from: "pipeline/suggest_squad.py"
      to: "pipeline/upload.py save()"
      via: "save('pre_season_squad.json', ...)"
      pattern: "pre_season_squad\\.json"
---

<objective>
Ship the Python pipeline half of Phase 126. Two new modules — `archive_season.py` (NSP-01) and `suggest_squad.py` (NSP-02 fallback) — wired into `run.py` behind a GW38 gate. Concurrent element-summary fetch, idempotent Blob write, non-fatal failure isolation, and PuLP ILP pre-compute.

Purpose: D-08, D-09, D-10 lock the pipeline contract — one-time-per-season artifact, partial-write guard at 50%, concurrent fetch (~10 workers). The TypeScript greedy `buildPreSeasonSquad()` in Plan 03 has a Python ILP fallback that runs in the pipeline, not at request time. Both Blob artifacts (`season_archive_gw38.json`, `pre_season_squad.json`) MUST exist for the UI to display anything.

Output: Three Python files modified, three pytest-green tests in `pipeline/test_archive_season.py` (transition the Wave 0 RED scaffold to GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-next-season-planner/126-CONTEXT.md
@.planning/phases/126-next-season-planner/126-RESEARCH.md
@.planning/phases/126-next-season-planner/126-PATTERNS.md
@.planning/phases/126-next-season-planner/126-01-SUMMARY.md

<interfaces>
<!-- Interfaces this plan depends on. Read source files for full context. -->

From `pipeline/upload.py` (verified):
```python
def save(pathname: str, data) -> None
    # Routes to Vercel Blob if USE_BLOB=true, else writes to pipeline/cache/{pathname}
```

From `pipeline/fpl_client.py` (existing — used in run.py line 190):
```python
def get_element_summary(player_id: int) -> dict
    # GET /element-summary/{id}/ — returns dict with history[] and history_past[]
```

From `pipeline/run.py` lines 144-151 (current state — to be extended):
```python
events = bootstrap.get('events', [])
IS_OFF_SEASON = not any(e.get('is_current') for e in events)   # line 148
```
The GW38 gate MUST be inserted AFTER the `events`/`IS_OFF_SEASON` assignment and BEFORE the IS_OFF_SEASON block that wraps GW-dependent steps (Pitfall 1 in RESEARCH.md).

From Phase 123 transfer_news block (run.py lines 163-168) — exact non-fatal pattern to mirror:
```python
try:
    from <module> import <func>
    <func>(bootstrap)
    print("...")
except Exception as exc:
    print(f"[<module>] non-fatal error: {exc}", file=sys.stderr)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pipeline/archive_season.py (NSP-01)</name>
  <files>pipeline/archive_season.py</files>
  <read_first>
    - pipeline/test_archive_season.py (from Plan 01 Task 3 — the RED test contract this implementation MUST satisfy)
    - pipeline/upload.py (the only legal Blob write path; `save()` is the public API)
    - pipeline/lineup_news.py (non-fatal scraper isolation pattern, per-source try/except)
    - pipeline/fpl_client.py (look for `get_element_summary` function signature and the FPL UA header convention `fplx/1.X (+https://fplx.app)`)
    - pipeline/run.py (read lines 140-200 — confirm IS_OFF_SEASON block position; archive_season is called from here in Task 3)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`pipeline/archive_season.py` analog block — exact ordering of idempotency check, concurrent fetch, partial-write guard)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-08, D-09, D-10 — the locked decisions this implementation honours)
  </read_first>
  <action>
    Create `pipeline/archive_season.py`. The module exposes a public `archive_season(bootstrap: dict) -> None` and three helpers: `_blob_exists`, `_fetch_one`, `_fetch_all_summaries`.

    Constants and constraints (all values are non-negotiable — they are locked by CONTEXT.md):
    - Target Blob key: `'season_archive_gw38.json'` (string is the canonical artifact name — used by `_blob_exists` AND `save()`)
    - `MAX_WORKERS = 10` (per D-10)
    - Partial-write threshold: `len(results) >= 0.5 * len(elements)` triggers `save()`; otherwise log to stderr and return WITHOUT writing
    - Import `save` from `upload` (NOT `vercel_blob.put` directly — D-08; Anti-pattern in RESEARCH.md)

    Execution ordering (this order matters — see Pitfall 2):
    1. FIRST statement of `archive_season(bootstrap)`: call `_blob_exists('season_archive_gw38.json')`. If True, print "[archive_season] already exists — skipping." and return.
    2. SECOND: extract `elements = bootstrap['elements']`.
    3. THIRD: call `_fetch_all_summaries(elements)` which wraps `concurrent.futures.ThreadPoolExecutor(max_workers=MAX_WORKERS)`. The submit/as_completed pattern is in the PATTERNS.md analog block — copy it.
    4. FOURTH: apply the >=50% guard. Below threshold → stderr log "<50% players fetched ({len(results)}/{total}) — skipping Blob write." and return.
    5. FIFTH: at or above threshold → `save('season_archive_gw38.json', results)` and print success line with `{len(results)}/{total}`.

    `_fetch_one(player_id)` MUST return a tuple `(player_id, data_or_None)`. On exception it logs to stderr and returns `(player_id, None)`. This is what makes per-player failures non-fatal.

    `_blob_exists` uses `vercel_blob.list({'prefix': pathname, 'limit': 1})` and checks `len(result.get('blobs', [])) > 0`. Wrap the call in try/except — on any exception, return False (treat as "does not exist" → proceed to fetch; the partial-write guard still protects against overwriting good data with bad).

    Results dict structure: `{ player_id (int): element_summary_dict, ... }`. Each value is the raw `get_element_summary(id)` response (a dict containing `history`, `history_past`, etc.). Do NOT transform — store verbatim so the API route can compute its own score map (D-02).

    Imports MUST include `import sys`, `import concurrent.futures`, `from upload import save`, and `from fpl_client import get_element_summary`. Use `import vercel_blob` lazily inside `_blob_exists` so unit tests can monkeypatch `archive_season._blob_exists` without importing vercel_blob.
  </action>
  <verify>
    <automated>python -m pytest pipeline/test_archive_season.py -x --tb=short 2>&1 | tee /tmp/126-02-1.log; grep -E "passed|failed" /tmp/126-02-1.log | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/archive_season.py` exists
    - File contains `def archive_season(bootstrap: dict) -> None`
    - File contains `def _blob_exists(pathname: str) -> bool`
    - File contains `def _fetch_one(player_id: int)`
    - File contains `def _fetch_all_summaries(`
    - File uses `concurrent.futures.ThreadPoolExecutor` with `max_workers=10`
    - File imports `from upload import save`
    - File does NOT contain a literal `vercel_blob.put(` call (must go through `save()`)
    - First non-comment statement in `archive_season()` body is the idempotency check (`_blob_exists` call)
    - `python -m pytest pipeline/test_archive_season.py` exits 0 with 4/4 tests passing
  </acceptance_criteria>
  <done>pytest GREEN; archive_season idempotent, concurrent, partial-write-guarded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement pipeline/suggest_squad.py (NSP-02 ILP fallback)</name>
  <files>pipeline/suggest_squad.py</files>
  <read_first>
    - pipeline/archive_season.py (just-shipped Task 1 — to understand the archive shape that suggest_squad reads)
    - pipeline/lineup_news.py (module docstring + public-function structure)
    - pipeline/upload.py (`save()` signature; suggest_squad calls `save('pre_season_squad.json', squad_dict)`)
    - pipeline/requirements.txt (verify `pulp>=2.7.0` was added in Plan 01 Task 1)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`pipeline/suggest_squad.py` analog block — PuLP ILP constraint set is verbatim)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-02 — `points_per_minute = total_points / total_minutes`, exclude < 500 minutes)
  </read_first>
  <action>
    Create `pipeline/suggest_squad.py`. Public API: `suggest_squad(bootstrap: dict, archive: dict) -> None`.

    Constants (locked):
    - Target Blob key: `'pre_season_squad.json'` (Pitfall 8 — must match the key used by the API route in Plan 04)
    - Budget: `1000` (tenths of GBP 1m = GBP 100m)
    - Team cap: `3`
    - Min-minutes eligibility: `500` total minutes from archive (D-02). Players below this threshold are NOT included in the ILP candidate set.
    - Position quotas: `MIN_SLOTS = {1: 2, 2: 3, 3: 2, 4: 1}`, `MAX_SLOTS = {1: 2, 2: 5, 3: 5, 4: 3}` — match `buildOptimalSquad` and the buildPreSeasonSquad spec (D-05).

    Algorithm:
    1. Build the candidate list by joining `bootstrap['elements']` with `archive[str(id)]` (archive keys may be string IDs depending on Blob serialisation — handle both `str(id)` and `int(id)` lookups). For each element compute `total_points` = sum of `history[].total_points`; `total_minutes` = sum of `history[].minutes`. Exclude where `total_minutes < 500`.
    2. Compute `ppm = total_points / total_minutes` for each eligible player.
    3. Call `_solve_ilp(players, score_map, budget=1000, team_cap=3)` — implementation per the PATTERNS.md analog block. Use `pulp.LpProblem("PreSeasonSquad", pulp.LpMaximize)` with a binary variable per player, objective sum of ppm*x, constraints: budget (sum cost*x <= 1000), squad size (sum x == 15), position MIN/MAX, team cap. Solve with `pulp.PULP_CBC_CMD(msg=0)`.
    4. If `pulp.LpStatus[prob.status] != 'Optimal'`, log to stderr and return without writing — non-fatal.
    5. Convert the 15 selected players to a dict matching `PreSeasonSquad` shape: split into starters (11) and bench (4). Derive starters by picking the top 11 by ppm within position quotas for a valid formation; bench is the remaining 4 (GK first, then field by ppm desc).
    6. Call `save('pre_season_squad.json', squad_dict)` with keys `starters`, `bench`, `formation` (string e.g. `"4-3-3"`), `budgetUsed` (int tenths).

    Wrap the entire body of `suggest_squad()` in a try/except that logs to stderr and re-raises (the outer caller in `run.py` will catch and continue — D-10 non-fatal isolation).

    Test scaffold note: there is no dedicated pytest file for suggest_squad in this phase — the function is verified end-to-end in CI when archive_season produces real input. Internal helpers (`_solve_ilp`, `_compute_score_map`) should be pure and testable; if you split them out, add a docstring explaining their input/output for downstream debuggability.
  </action>
  <verify>
    <automated>python -c "import suggest_squad; assert callable(suggest_squad.suggest_squad)" 2>&1; echo "exit=$?"</automated>
    Plus: `python -m pyflakes pipeline/suggest_squad.py` (or equivalent linter the project uses) exits 0.
  </verify>
  <acceptance_criteria>
    - `pipeline/suggest_squad.py` exists
    - File contains `def suggest_squad(bootstrap: dict, archive: dict) -> None`
    - File contains `def _solve_ilp(` with PuLP constraints (objective, budget <= 1000, sum == 15, MIN/MAX per position, team cap <= 3)
    - File imports `pulp` and `from upload import save`
    - Call `save('pre_season_squad.json', ...)` appears exactly once
    - Players with total_minutes < 500 (computed from archive history[]) MUST NOT appear in the ILP candidate set (source assertion: a filter expression keyed on 500 / min_minutes exists)
    - `python -c "import suggest_squad"` exits 0 after `pip install pulp` runs
  </acceptance_criteria>
  <done>suggest_squad imports cleanly; PuLP constraint set covers budget, size, position, team cap.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add GW38 gate in pipeline/run.py</name>
  <files>pipeline/run.py</files>
  <read_first>
    - pipeline/run.py lines 140-210 (the IS_OFF_SEASON block — the insertion point is immediately AFTER line 148 `IS_OFF_SEASON = ...` and BEFORE the IS_OFF_SEASON-wrapped block that begins at line 202)
    - pipeline/archive_season.py (just shipped — confirm import path is `from archive_season import archive_season`)
    - pipeline/suggest_squad.py (just shipped — Task 2)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`pipeline/run.py (modify)` block — exact insertion snippet)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Pitfall 1 — position the GW38 block BEFORE the IS_OFF_SEASON-wrapped block; using `is_current` not the off-season flag)
  </read_first>
  <action>
    Edit `pipeline/run.py`. Insert a new block AFTER the existing line `IS_OFF_SEASON = not any(e.get('is_current') for e in events)` (line 148) and BEFORE the `if not IS_OFF_SEASON:` block (currently at line 202).

    Insert block content:
    1. Derive `current_event_entry`, `last_event_id`, `CURRENT_GW`, and `IS_GW38` per the PATTERNS.md snippet. `IS_GW38 = (CURRENT_GW > 0) and (CURRENT_GW == last_event_id)`.
    2. Add a non-fatal try/except matching the Phase 123 transfer_news pattern (run.py lines 163-168):
       - `from archive_season import archive_season; archive_season(bootstrap)` inside try
       - `print("Season archive written.")` after success
       - except Exception → `print(f"[archive_season] non-fatal error: {exc}", file=sys.stderr)`
    3. Add a SECOND non-fatal block under the same `if IS_GW38:` gate that invokes `suggest_squad`:
       - Read the archive back via `pipeline/cache/season_archive_gw38.json` (USE_BLOB=false dev path) or accept that suggest_squad takes `bootstrap` + `archive_dict` — pass it via local read if cache exists. Simplest: invoke `suggest_squad(bootstrap, archive_dict)` where `archive_dict` is loaded from disk after archive_season ran. If `season_archive_gw38.json` is not on disk yet (USE_BLOB=true path), the call is skipped with a stderr log.
       - Non-fatal: `except Exception as exc: print(f"[suggest_squad] non-fatal error: {exc}", file=sys.stderr)`.

    The two new try/except blocks MUST be at the same indentation as the existing IS_OFF_SEASON assignment (top-level of the `try` block that surrounds the run() body in run.py).

    Do NOT modify the existing IS_OFF_SEASON-wrapped block (line 202 onward) — it stays exactly as is. Do NOT remove or reorder the existing transfer_news / lineup_news blocks.
  </action>
  <verify>
    <automated>python -m py_compile pipeline/run.py 2>&1; echo "exit=$?"; grep -c "IS_GW38" pipeline/run.py; grep -c "from archive_season import archive_season" pipeline/run.py</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/run.py` contains the literal token `IS_GW38` at least once
    - `pipeline/run.py` contains `from archive_season import archive_season` exactly once
    - `pipeline/run.py` contains `from suggest_squad import suggest_squad` exactly once
    - The line containing `IS_GW38 =` appears BEFORE the line `if not IS_OFF_SEASON:` (verifiable via `grep -n` line numbers — IS_GW38 line < IS_OFF_SEASON-block line)
    - `python -m py_compile pipeline/run.py` exits 0
    - Both new try/except blocks log to stderr (`file=sys.stderr`) on failure — confirms non-fatal isolation
  </acceptance_criteria>
  <done>run.py compiles, GW38 gate present, archive_season and suggest_squad invoked non-fatally before IS_OFF_SEASON wrap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FPL API → pipeline | 700+ untrusted FPL API responses parsed into archive dict. JSON shape may shift between seasons. |
| pipeline → Vercel Blob | `save('season_archive_gw38.json', dict)` write — public CDN URL, but write-only via authenticated Blob token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-02-01 | Tampering | Blob write overwrite | mitigate | `_blob_exists()` guard runs FIRST inside `archive_season()` — prevents partial re-run from overwriting a complete archive (Pitfall 2). Source assertion: idempotency check is the first non-comment statement. |
| T-126-02-02 | D (Denial of Service) | concurrent fetch storm against FPL API | mitigate | `MAX_WORKERS=10` cap on ThreadPoolExecutor (D-10) — conservative starting point. FPL UA header `fplx/1.X (+https://fplx.app)` retained from existing fpl_client.py to satisfy attribution policy. |
| T-126-02-03 | I (Information Disclosure) | `season_archive_gw38.json` Blob URL is public | accept | Archive contains only public FPL data (player names, points, minutes). No PII or auth secrets. Acceptable per RESEARCH.md security domain. |
| T-126-02-04 | Tampering | `pre_season_squad.json` Blob artifact | mitigate | Blob write is pipeline-only (server env with auth token); UI is read-only consumer via `/api/pre-season-squad`. No user-supplied write path. |
| T-126-02-05 | D (DoS) | GW38 gate fails open if `events` is empty | accept | `last_event_id = max((e['id'] for e in events), default=0)` returns 0 if events is empty; `IS_GW38 = (CURRENT_GW > 0) and ...` short-circuits to False. Verified in code path. |
</threat_model>

<verification>
- `python -m pytest pipeline/test_archive_season.py` exits 0 with 4/4 tests passing
- `python -c "import archive_season; import suggest_squad"` exits 0
- `python -m py_compile pipeline/run.py` exits 0
- `grep -c "from archive_season import" pipeline/run.py` returns 1
- `grep -c "from suggest_squad import" pipeline/run.py` returns 1
- `grep -c "pre_season_squad.json" pipeline/suggest_squad.py` returns >=1
- `grep -c "season_archive_gw38.json" pipeline/archive_season.py` returns >=1
- `grep -c "vercel_blob.put" pipeline/archive_season.py pipeline/suggest_squad.py` returns 0 (must go via save())
</verification>

<success_criteria>
- NSP-01 fulfilled: `archive_season.py` writes `season_archive_gw38.json` to Blob during GW38, is idempotent, and applies the 50% partial-write guard
- NSP-02 (Python side) fulfilled: `suggest_squad.py` runs PuLP ILP and writes `pre_season_squad.json` to Blob
- `run.py` GW38 gate is positioned BEFORE the IS_OFF_SEASON block — the locked correctness condition
- All four Wave 0 `pipeline/test_archive_season.py` tests transition RED → GREEN
- No direct `vercel_blob.put()` calls — `upload.save()` remains the sole write path
</success_criteria>

<output>
After completion, create `.planning/phases/126-next-season-planner/126-02-SUMMARY.md`
</output>
