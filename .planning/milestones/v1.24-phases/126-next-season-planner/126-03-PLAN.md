---
phase: 126
plan: 03
type: execute
wave: 1
depends_on:
  - "126-01"
files_modified:
  - src/lib/pre-season-squad.ts
  - src/lib/hooks/usePreSeasonSquad.ts
  - src/app/api/pre-season-squad/route.ts
  - src/components/club-form/FixtureHeatMap.tsx
autonomous: true
requirements:
  - NSP-02
  - NSP-03
tags:
  - typescript
  - react-query
  - api-route
must_haves:
  truths:
    - "buildPreSeasonSquad(players, scoreMap) returns a valid 15-player squad or null per the chip-modes.ts greedy contract"
    - "Players not present in scoreMap are excluded from selection (no status === 'a' filter)"
    - "/api/pre-season-squad reads season_archive_gw38.json from Blob (or pre_season_squad.json fallback) and returns a PreSeasonSquad JSON, 404 if absent"
    - "usePreSeasonSquad() returns null on 404 (not throw) — distinguishes 'archive absent' from 'fetch failure'"
    - "HeatMapRow and HeatMapRowProps are exported from FixtureHeatMap.tsx so NextSeasonPlannerTab can import them"
  artifacts:
    - path: "src/lib/pre-season-squad.ts"
      provides: "Pure greedy 15-player squad builder (NSP-02)"
      exports: ["buildPreSeasonSquad"]
    - path: "src/lib/hooks/usePreSeasonSquad.ts"
      provides: "TanStack Query hook with 6h staleTime; null on 404"
      exports: ["usePreSeasonSquad"]
    - path: "src/app/api/pre-season-squad/route.ts"
      provides: "Blob-backed JSON endpoint for the pre-season squad"
      exports: ["GET"]
    - path: "src/components/club-form/FixtureHeatMap.tsx"
      provides: "Exported HeatMapRow for reuse by NextSeasonPlannerTab"
      contains: "export function HeatMapRow"
  key_links:
    - from: "src/app/api/pre-season-squad/route.ts"
      to: "src/lib/pre-season-squad.ts"
      via: "import { buildPreSeasonSquad } from '@/lib/pre-season-squad'"
      pattern: "buildPreSeasonSquad"
    - from: "src/lib/hooks/usePreSeasonSquad.ts"
      to: "/api/pre-season-squad"
      via: "fetch('/api/pre-season-squad')"
      pattern: "/api/pre-season-squad"
    - from: "src/lib/pre-season-squad.ts"
      to: "src/lib/types.ts"
      via: "import type { PreSeasonPlayer, PreSeasonSquad }"
      pattern: "PreSeasonPlayer"
---

<objective>
Ship the TypeScript / API half of Phase 126 except the tab UI: the pure greedy `buildPreSeasonSquad()` function, the `/api/pre-season-squad` Route Handler, the `usePreSeasonSquad()` hook, and the `HeatMapRow` export gate. After this plan, Plan 04 has every dependency it needs to drop in `NextSeasonPlannerTab` and the sub-tab wiring.

Purpose: D-01..D-03 lock the data contract (score map = points_per_minute, exclude < 500 minutes, 404 = "Prices pending"). D-11 locks the HeatMapRow reuse strategy (export, do NOT copy). The hook MUST return `null` on 404 (not throw) so the UI can distinguish "archive absent" (graceful state) from "network error" (red error state).

Output: Three new files, one modified file, three NSP-02 unit tests transition Wave 0 RED → GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-next-season-planner/126-CONTEXT.md
@.planning/phases/126-next-season-planner/126-RESEARCH.md
@.planning/phases/126-next-season-planner/126-PATTERNS.md
@.planning/phases/126-next-season-planner/126-01-SUMMARY.md

<interfaces>
<!-- Interfaces this plan depends on or produces. -->

From `src/lib/types.ts` (shipped in Plan 01 Task 1):
```typescript
export interface PreSeasonPlayer {
  id: number; web_name: string; element_type: PositionCode;
  team: number; team_short_name: string; now_cost: number;
  total_points: number; ppm: number
}
export interface PreSeasonSquad {
  starters: PreSeasonPlayer[]; bench: PreSeasonPlayer[];
  formation: string; budgetUsed: number
}
export interface SeasonArchiveEntry { history: Array<{element: number; total_points: number; minutes: number; [k: string]: unknown}>; ... }
```

From `src/lib/chip-modes.ts` lines 14-16 (the slot constants — redeclared locally per the D-07 pattern, do NOT import):
```typescript
const MIN_SLOTS: Record<number, number> = { 1: 2, 2: 3, 3: 2, 4: 1 }
const MAX_SLOTS: Record<number, number> = { 1: 2, 2: 5, 3: 5, 4: 3 }
```

From `src/lib/hooks/useTransferNews.ts` lines 1-14 (verified analog — same 6h staleTime, but THIS hook MUST return null on 404 instead of throwing):
```typescript
staleTime: 6 * 60 * 60 * 1000  // 6h
```

From `src/app/api/transfer-news/route.ts` (verified Blob read pattern — same USE_BLOB env check, same 404 / 502 / 500 mapping). This plan's route extends it: after the Blob read, parse the archive, compute ppm scoreMap, call `buildPreSeasonSquad`, return the squad JSON (NOT the raw archive).

From `src/components/club-form/FixtureHeatMap.tsx` lines 40-54 (current state — both `interface HeatMapRowProps` and `function HeatMapRow` are UNEXPORTED):
```typescript
interface HeatMapRowProps { ... }                          // line 42
function HeatMapRow({ ... }: HeatMapRowProps) { ... }      // line 54
```
Both MUST be made `export` in Task 4. Do NOT move the component to a separate file (RESEARCH.md Pattern 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement buildPreSeasonSquad() (NSP-02)</name>
  <files>src/lib/pre-season-squad.ts</files>
  <read_first>
    - src/lib/pre-season-squad.test.ts (RED scaffold from Plan 01 — this implementation MUST satisfy all four test cases)
    - src/lib/chip-modes.ts (lines 1-110 — the `buildOptimalSquad` greedy pattern that this function adapts)
    - src/lib/types.ts (PreSeasonPlayer, PreSeasonSquad shape — from Plan 01)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`src/lib/pre-season-squad.ts` analog block — exact greedy ordering)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-02 — scoreMap eligibility replaces status === 'a' check; D-05 — formation grid mirror)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Pitfall 3 — eligibility is scoreMap.has(p.id) only, NOT status check)
  </read_first>
  <action>
    Create `src/lib/pre-season-squad.ts`. Export `buildPreSeasonSquad(players: PreSeasonPlayer[], scoreMap: Map&lt;number, number&gt;, budget = 1000, teamCap = 3): PreSeasonSquad | null`.

    Mirror `buildOptimalSquad` from chip-modes.ts (lines 37-108) but apply the three locked deviations:
    1. Eligibility: `players.filter(p => scoreMap.has(p.id))`. Do NOT filter on `status === 'a'` or `xPts_1gw !== 0` — off-season values are unreliable (Pitfall 3, locked by D-02).
    2. Sort key: `scoreMap.get(p.id) ?? 0` descending; tie-break on `now_cost` ascending (cheaper wins).
    3. Return type is `PreSeasonSquad | null` (not `ChipSquadResult`). On null return, the caller is expected to fall back to the Python-side ILP artifact `pre_season_squad.json`.

    Slot constants redeclared locally:
    ```
    MIN_SLOTS = { 1: 2, 2: 3, 3: 2, 4: 1 }
    MAX_SLOTS = { 1: 2, 2: 5, 3: 5, 4: 3 }
    ```

    Greedy loop: iterate sorted candidates, skip if MAX_SLOTS for position is full, skip if teamCap reached for player.team, skip if running_cost + p.now_cost > budget. Stop at 15.

    Post-loop: return null if `squad.length < 15` OR any position has `< MIN_SLOTS[pos]`.

    Derive `starters` and `bench` for the 15 squad:
    - Starters = 11 players forming a legal formation (1 GK + 3-5 DEF + 2-5 MID + 1-3 FWD, with totals = 11). Simplest legal derivation that the chip-modes pattern uses: pick the GK with higher ppm as starter; then fill 10 outfield slots greedily by ppm desc, respecting the formation rules (min 3 DEF, min 2 MID, min 1 FWD).
    - Formation string: derive as `"{DEF_starters}-{MID_starters}-{FWD_starters}"` (e.g. `"4-3-3"`).
    - Bench = the remaining 4 (1 GK + 3 others). Sort bench: GK first, then by ppm desc (matches ChipSquadView convention).

    Pure function: no React, no `'use client'`, no side effects. File header line 1 must be a comment indicating Phase 126 / NSP-02 origin, just like chip-modes.ts line 1.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pre-season-squad.test.ts 2>&1 | tee /tmp/126-03-1.log; grep -E "Tests +[0-9]+ passed" /tmp/126-03-1.log | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pre-season-squad.ts` exists
    - File exports `function buildPreSeasonSquad(`
    - File contains `MIN_SLOTS` and `MAX_SLOTS` records matching `{1:2,2:3,3:2,4:1}` and `{1:2,2:5,3:5,4:3}`
    - File contains a `scoreMap.has(` expression (eligibility filter)
    - File does NOT contain a `status === 'a'` or `xPts_1gw` filter on the eligible set (source assertion: `grep -E "status\\s*===\\s*'a'|xPts_1gw" src/lib/pre-season-squad.ts` returns 0 matches)
    - All 4 tests in `src/lib/pre-season-squad.test.ts` PASS (`npx vitest run` exits 0 with 4/4 green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>buildPreSeasonSquad GREEN; vitest 4/4 passing; no off-season status check.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement /api/pre-season-squad route + usePreSeasonSquad hook</name>
  <files>src/app/api/pre-season-squad/route.ts, src/lib/hooks/usePreSeasonSquad.ts</files>
  <read_first>
    - src/app/api/transfer-news/route.ts (verified Blob read pattern; USE_BLOB env flag; 404 / 502 / 500 mapping)
    - src/lib/hooks/useTransferNews.ts (6h staleTime pattern — but THIS hook deliberately differs in the 404 handling: return null instead of throwing)
    - src/lib/pre-season-squad.ts (just shipped — the `buildPreSeasonSquad` function this route invokes)
    - src/lib/types.ts (PreSeasonPlayer, PreSeasonSquad, SeasonArchiveEntry shapes — to construct the player[] payload from archive entries)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (route.ts + hook analog blocks — exact structure)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-02 score formula; D-03 "Prices pending" trigger on archive absence)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Pitfall 8 — canonical Blob key is `pre_season_squad.json`; route MAY prefer this pre-computed artifact when it exists)
  </read_first>
  <action>
    A. Create `src/app/api/pre-season-squad/route.ts`. Export a single `GET()` handler.

    Resolution order (in order, first that succeeds wins; the rest are fallbacks):
    1. Prefer the pre-computed ILP result. Attempt to load `pre_season_squad.json` first (Blob if USE_BLOB=true, else `pipeline/cache/pre_season_squad.json`). If present, parse and return as 200 directly — it is already a `PreSeasonSquad`.
    2. Fall back to `season_archive_gw38.json`. If present: parse the archive, also load `fpl_bootstrap.json` (from the same Blob/local source — the same `list({ prefix: 'fpl_bootstrap.json', limit: 1 })` pattern). Construct the `PreSeasonPlayer[]` by joining bootstrap elements with archive entries. Compute `total_points`, `total_minutes` from `history[]`; compute `ppm = total_points / total_minutes`. Exclude players with `total_minutes < 500`. Build the `scoreMap: Map<number, number>` keyed on player id. Call `buildPreSeasonSquad(players, scoreMap)`. If the greedy returns null, log to stderr and respond `{ error: 'Squad infeasible — ILP fallback pending' }` with status 503.
    3. If neither artifact exists, return `{ error: 'Archive not available' }` with status 404 (the "Prices pending" trigger — D-03).

    Use the same Cache-Control header as `transfer-news/route.ts`: `public, s-maxage=3600, stale-while-revalidate=86400`.

    Wrap the entire handler in try/catch. ENOENT (local file) → 404; other failures → 500. Match the error JSON shapes in transfer-news/route.ts.

    B. Create `src/lib/hooks/usePreSeasonSquad.ts`. Use `useQuery&lt;PreSeasonSquad | null&gt;` with `queryKey: ['pre-season-squad']`, `staleTime: 6 * 60 * 60 * 1000` (6h).

    Critical difference from `useTransferNews`: the queryFn MUST handle 404 specially:
    ```
    if (res.status === 404) return null    // archive absent → "Prices pending"
    if (!res.ok) throw new Error('Failed to fetch pre-season squad')
    return res.json()
    ```

    The `null` return is what the UI uses to render the "Prices pending" graceful state without surfacing the red error state. Pitfall: do NOT just `if (!res.ok) throw` — that would conflate 404 with 5xx.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^#" | grep -E "error TS" | head -3; echo "tsc-exit=$?"; npx vitest run src/lib/pre-season-squad.test.ts --reporter=verbose 2>&1 | grep -E "Tests +[0-9]+ passed|Tests +[0-9]+ failed" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/pre-season-squad/route.ts` exists and exports `async function GET()`
    - Route contains `list({ prefix: 'pre_season_squad.json', limit: 1 })` OR a local-cache equivalent
    - Route contains `list({ prefix: 'season_archive_gw38.json', limit: 1 })` OR a local-cache equivalent
    - Route returns `Response.json(..., { status: 404 })` when both artifacts are absent
    - Route imports `buildPreSeasonSquad` from `@/lib/pre-season-squad`
    - Route applies a `< 500` minutes exclusion before calling `buildPreSeasonSquad` (source assertion: `grep -E "500" src/app/api/pre-season-squad/route.ts` matches the threshold)
    - `src/lib/hooks/usePreSeasonSquad.ts` exists and exports `usePreSeasonSquad`
    - Hook contains `if (res.status === 404) return null` (exact 404 → null mapping)
    - Hook contains `staleTime: 6 * 60 * 60 * 1000`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Route + hook ship; tsc clean; 404 → null contract verified by source assertion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Export HeatMapRow and HeatMapRowProps from FixtureHeatMap.tsx</name>
  <files>src/components/club-form/FixtureHeatMap.tsx</files>
  <read_first>
    - src/components/club-form/FixtureHeatMap.tsx lines 40-60 (the HeatMapRowProps interface and HeatMapRow function — currently unexported)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`FixtureHeatMap.tsx (modify)` block — the change is two `export` keyword additions)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-11 — locked: export in place, do NOT move to a shared module)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Pitfall 5 — both interface AND function must be exported)
  </read_first>
  <action>
    Edit `src/components/club-form/FixtureHeatMap.tsx`:
    - Line 42 currently reads `interface HeatMapRowProps {`. Prepend `export ` so it reads `export interface HeatMapRowProps {`.
    - Line 54 currently reads `function HeatMapRow({...}: HeatMapRowProps) {`. Prepend `export ` so it reads `export function HeatMapRow({...}: HeatMapRowProps) {`.

    Do NOT change the function body, the props type, any sibling components, any imports, or any other line. The diff for this task should be exactly two added `export ` tokens.

    Do NOT move the component to `src/components/shared/HeatMapRow.tsx` or any other file — D-11 explicitly locks the "export in place" strategy.
  </action>
  <verify>
    <automated>grep -E "^export (function HeatMapRow|interface HeatMapRowProps)" src/components/club-form/FixtureHeatMap.tsx | wc -l; npx tsc --noEmit 2>&1 | grep -E "error TS" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function HeatMapRow" src/components/club-form/FixtureHeatMap.tsx` returns 1
    - `grep -c "^export interface HeatMapRowProps" src/components/club-form/FixtureHeatMap.tsx` returns 1
    - `npx tsc --noEmit` exits 0
    - `npx vitest run src/components/club-form/FixtureHeatMap.test.tsx` (if the test file exists) exits 0 — confirms no regression
    - No new files were created in `src/components/shared/` or elsewhere (source assertion: this task's commit touches only the one file)
  </acceptance_criteria>
  <done>HeatMapRow + HeatMapRowProps exported; tsc clean; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/pre-season-squad | Public read-only endpoint; no user-supplied parameters. |
| API route → Vercel Blob | Read-only via `list()` + `fetch(blobs[0].url)`. No write path in this surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-03-01 | I (Info Disclosure) | `/api/pre-season-squad` response body | accept | Returns only public FPL data (player names, last-season points, derived ppm). No PII. Same risk profile as `/api/transfer-news`. |
| T-126-03-02 | T (Tampering) | Blob URL fetch in route handler | accept | `blobs[0].url` is a signed Vercel Blob CDN URL — already trusted within the deployed origin. Same as transfer-news/route.ts. |
| T-126-03-03 | D (DoS) | ILP runtime in API route | mitigate | Greedy `buildPreSeasonSquad` is O(n log n) for 700 players — safe at request time. The ILP path NEVER runs at request time (it is the Python `suggest_squad.py` pre-compute from Plan 02). Route only calls greedy. |
| T-126-03-04 | V5 (Input Validation) | archive JSON parse | mitigate | `JSON.parse(data)` is wrapped in try/catch via the outer handler try/catch. Malformed Blob payload → 500. No injection surface (no user input passes through the parse). |
</threat_model>

<verification>
- `npx vitest run src/lib/pre-season-squad.test.ts` exits 0 with 4/4 passing
- `npx tsc --noEmit` exits 0
- `src/lib/pre-season-squad.ts` does not contain `status === 'a'` or `xPts_1gw` (source assertion)
- `src/lib/hooks/usePreSeasonSquad.ts` contains `if (res.status === 404) return null`
- `src/app/api/pre-season-squad/route.ts` contains both `season_archive_gw38.json` and `pre_season_squad.json` literals
- `src/components/club-form/FixtureHeatMap.tsx` has both `export function HeatMapRow` and `export interface HeatMapRowProps`
- No new files outside `src/lib/`, `src/lib/hooks/`, `src/app/api/`
</verification>

<success_criteria>
- NSP-02 (TypeScript greedy) shipped: `buildPreSeasonSquad` passes all four Wave 0 tests
- NSP-02 (API surface) shipped: route handler resolves both Blob artifacts and returns a `PreSeasonSquad` or 404
- NSP-03 (heatmap reuse precondition) shipped: HeatMapRow is exported so Plan 04's NextSeasonPlannerTab can consume it
- 404 → null hook contract confirmed: UI in Plan 04 can use the null sentinel for "Prices pending"
</success_criteria>

<output>
After completion, create `.planning/phases/126-next-season-planner/126-03-SUMMARY.md`
</output>
