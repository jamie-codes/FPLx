---
phase: 126
plan: 04
type: execute
wave: 2
depends_on:
  - "126-01"
  - "126-03"
files_modified:
  - src/components/next-season/NextSeasonPlannerTab.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - NSP-03
  - NSP-04
tags:
  - frontend
  - react
  - ui
must_haves:
  truths:
    - "User can navigate to Plan → Next Season (desktop) / Pre-Season (mobile)"
    - "When season archive is absent, user sees 'Pre-season squad not yet available' graceful state instead of an error"
    - "When archive is present, user sees a 15-player formation grid (GK row / DEF row / MID row / FWD row + 4 bench) with name, team, £X.Xm cost, and last-season points per card"
    - "Points-per-minute appears as a native title-attribute tooltip on the total-points span (not a visible column)"
    - "When next-season fixtures are not yet published, user sees 'Fixtures not yet published' empty state in the FDR heatmap section"
    - "Sub-tab 'next-season' is registered AFTER 'rivals' in the Plan SECTIONS array"
  artifacts:
    - path: "src/components/next-season/NextSeasonPlannerTab.tsx"
      provides: "Read-only pre-season squad display + GW1-8 FDR heatmap (NSP-04)"
      exports: ["NextSeasonPlannerTab"]
    - path: "src/app/page.tsx"
      provides: "Sub-tab registration for the new tab"
      contains: "next-season"
  key_links:
    - from: "src/components/next-season/NextSeasonPlannerTab.tsx"
      to: "src/lib/hooks/usePreSeasonSquad.ts"
      via: "usePreSeasonSquad()"
      pattern: "usePreSeasonSquad"
    - from: "src/components/next-season/NextSeasonPlannerTab.tsx"
      to: "src/components/club-form/FixtureHeatMap.tsx HeatMapRow"
      via: "import { HeatMapRow, HeatMapRowProps } from '@/components/club-form/FixtureHeatMap'"
      pattern: "HeatMapRow"
    - from: "src/app/page.tsx"
      to: "src/components/next-season/NextSeasonPlannerTab.tsx"
      via: "import NextSeasonPlannerTab + render condition activeSection === 'plan' && activeSubTab === 'next-season'"
      pattern: "next-season"
---

<objective>
Ship the user-facing Plan section sub-tab: a read-only formation grid showing the 15 recommended players plus a GW1-8 FDR heatmap, with graceful states for "Prices pending" (archive absent) and "Fixtures not yet published" (no next-season fixture data). Wire it into the global SubTab union, the Plan SECTIONS array, and the render switch in `page.tsx`.

Purpose: D-04 (read-only), D-05 (GK/DEF/MID/FWD + bench formation grid), D-06 (exact card fields with ppm as tooltip only), D-07 (sub-tab id `'next-season'`, label "Next Season", mobileLabel "Pre-Season", placed after `'rivals'`). UI-SPEC pins every Tailwind class and copy string — no creative latitude on visual design.

Output: One new component, one modified page.tsx, four RTL tests in `NextSeasonPlannerTab.test.tsx` transition Wave 0 RED → GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-next-season-planner/126-CONTEXT.md
@.planning/phases/126-next-season-planner/126-UI-SPEC.md
@.planning/phases/126-next-season-planner/126-PATTERNS.md
@.planning/phases/126-next-season-planner/126-03-SUMMARY.md

<interfaces>
<!-- All consumed contracts are now landed. -->

From `src/lib/hooks/usePreSeasonSquad.ts` (Plan 03 Task 2):
```typescript
export function usePreSeasonSquad(): UseQueryResult<PreSeasonSquad | null, Error>
// data === null  → archive absent → render "Prices pending"
// data === PreSeasonSquad → render formation grid
// isError === true → render red error copy
```

From `src/lib/types.ts` (Plan 01 Task 1): `PreSeasonSquad`, `PreSeasonPlayer`.

From `src/components/club-form/FixtureHeatMap.tsx` (Plan 03 Task 3):
```typescript
export interface HeatMapRowProps { ... }
export function HeatMapRow({ t, grid, mode, tierMap, ownedTeamIds }: HeatMapRowProps): JSX.Element
```

From `src/app/page.tsx` line 58 — current `SubTab` union (Phase 125 added `'window'`; this phase appends `'next-season'`):
```typescript
export type SubTab = 'gems' | 'insights' | ... | 'rank-sim'
```
Modification target: append `'next-season'` at the end of the union.

Plan SECTIONS Plan subTabs array currently ends with `{ id: 'rivals' as SubTab, label: 'Rivals', mobileLabel: 'Rivals' }` at page.tsx line 86. Append `{ id: 'next-season' as SubTab, label: 'Next Season', mobileLabel: 'Pre-Season' }` after it.

Render switch currently has the `'rivals'` block at page.tsx lines 290-292:
```tsx
{activeSection === 'plan' && activeSubTab === 'rivals' && (
  <RivalsTab submittedId={submittedId} />
)}
```
Add an immediately-following block for `'next-season'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement NextSeasonPlannerTab component (NSP-03, NSP-04)</name>
  <files>src/components/next-season/NextSeasonPlannerTab.tsx</files>
  <read_first>
    - src/components/next-season/NextSeasonPlannerTab.test.tsx (RED scaffold from Plan 01 Task 2 — the contract this component MUST satisfy)
    - src/components/optimiser/ChipSquadView.tsx (lines 1-110 — formation grid visual template; copy classnames verbatim per UI-SPEC)
    - src/components/club-form/FixtureHeatMap.tsx (lines 1-300 — to see how `HeatMapRow` is invoked, what `grid` and `tierMap` props expect)
    - src/lib/hooks/usePreSeasonSquad.ts (Plan 03 — the hook returning `PreSeasonSquad | null`)
    - src/lib/types.ts (PreSeasonPlayer, PreSeasonSquad shapes)
    - .planning/phases/126-next-season-planner/126-UI-SPEC.md (the binding visual contract — every Tailwind class, every copy string is fixed)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-04, D-05, D-06 — read-only, formation rows, exact card fields)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`NextSeasonPlannerTab.tsx` analog block — render structure)
  </read_first>
  <action>
    Create `src/components/next-season/NextSeasonPlannerTab.tsx`. First line: `'use client'`. Export the component as a named export: `export function NextSeasonPlannerTab()`.

    Imports: `useState` / `Fragment` from React; `usePreSeasonSquad` from `@/lib/hooks/usePreSeasonSquad`; `HeatMapRow`, `HeatMapRowProps` types from `@/components/club-form/FixtureHeatMap`; `PreSeasonSquad`, `PreSeasonPlayer` from `@/lib/types`.

    Component body has TWO sections inside one root `<div className="space-y-4">`:

    SECTION A — Pre-Season Squad (NSP-04):
    1. Heading: `<h3 className="text-xl font-semibold">Pre-Season Squad</h3>`
    2. State machine from `usePreSeasonSquad()`:
       - `isLoading` → `<p className="text-sm text-zinc-500 dark:text-zinc-400 py-4">Loading pre-season squad...</p>`
       - `isError` → `<p className="text-sm text-red-600 dark:text-red-400 py-4">Failed to load pre-season squad. Check the pipeline output and refresh.</p>`
       - `data === null` → graceful "Prices pending" state. Render a heading and body per UI-SPEC Copywriting Contract: `<p className="text-sm text-zinc-500 dark:text-zinc-400 py-4">Pre-season squad not yet available. The squad builder becomes available once the season archive is ready. Check back after GW38.</p>` (heading + body may be a single paragraph; if you prefer two paragraphs, both use the same muted classes)
       - `data` populated → render the formation grid (see below)
    3. Formation grid render — mirror `ChipSquadView.tsx` lines 38-105 exactly. Differences from the chip view:
       - Headline row contains only `Formation: {data.formation}` and `Budget used: £{(data.budgetUsed/10).toFixed(1)}m`. No chip label, no FH reversion notice.
       - Each XI player row class: `flex items-center justify-between py-1.5 border-b border-zinc-100 dark:border-zinc-800 border-l-2 border-green-500 pl-2 text-sm`
       - Each bench player row class: `flex items-center justify-between py-1.5 border-b border-zinc-100 dark:border-zinc-800 opacity-60 pl-2 text-sm`
       - Card LEFT span: `<span className="font-semibold text-zinc-700 dark:text-zinc-300">{p.web_name}</span>`
       - Card RIGHT span: `<span className="text-xs text-zinc-500 dark:text-zinc-400"><span>{p.team_short_name}</span><span className="ml-2">£{(p.now_cost/10).toFixed(1)}m</span><span className="ml-2" title={`${p.ppm.toFixed(2)}pts/min (last season)`}>{p.total_points}pts</span></span>`
       - The `title` attribute on the total-points span IS the ppm tooltip surface (D-06; do NOT render ppm as a visible column).
       - Position section labels (GK / DEF / MID / FWD / Bench): `<div className="text-[10px] font-semibold uppercase text-zinc-500 dark:text-zinc-400 pt-2 pb-0.5 bg-zinc-50 dark:bg-zinc-800/40 px-1">{label}</div>`
       - Group starters by element_type: GK row first, then DEF, MID, FWD. Render only positions with members.
       - Bench section: header label "Bench", then `data.bench` in order (the bench is already GK-first per buildPreSeasonSquad contract).

    SECTION B — GW1-8 FDR Heatmap (NSP-03):
    1. Heading: `<h3 className="text-xl font-semibold">GW1–8 Fixture Difficulty</h3>`
    2. For this phase, the heatmap source is the same data path that `FixtureHeatMap` consumes. The simplest correct approach: render a deliberate "Fixtures not yet published" state in the FIRST shipping version (the next-season fixture data will not exist on disk until FPL publishes — verified in CONTEXT.md GW1-8-FIXTURES deferred item). When that data arrives in production, the empty-state check switches automatically.
    3. Implementation: import the existing `useClubForm` (or whatever hook ClubFormTab uses to fetch fixtures) OR a new lightweight fetch to `/api/fixtures` if that already exists; from the response, filter to GW1-8 of the next season (`event >= 1 && event <= 8` AND NOT finished). If the filtered array is empty → render `<p className="text-sm text-zinc-500 dark:text-zinc-400 py-2">Fixtures not yet published for next season.</p>`. If populated → render `HeatMapRow` rows inside a `<table>` (one row per team). For now, since GW1-8-FIXTURES is a known deferred condition, the empty branch is the EXPECTED render path at ship time; the populated branch is the future-ready code path.
    4. If you cannot import a fixture hook cleanly without expanding scope, hard-code the empty-state render with a TODO comment referencing GW1-8-FIXTURES; this is acceptable per CONTEXT.md.

    The four `it()` cases in `NextSeasonPlannerTab.test.tsx` (Plan 01 Task 2) MUST all pass:
    - Test 1: null data → "Pre-season squad not yet available"
    - Test 2: populated data → formation grid with 15 player rows
    - Test 3: empty fixtures → "Fixtures not yet published"
    - Test 4: isError → red error copy

    Read-only contract: no `onClick`, no `<button>` elements that mutate state, no drag handles, no edit affordances anywhere in the squad grid (D-04).
  </action>
  <verify>
    <automated>npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx 2>&1 | tee /tmp/126-04-1.log; grep -E "Tests +[0-9]+ passed" /tmp/126-04-1.log | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/next-season/NextSeasonPlannerTab.tsx` exists with `'use client'` on line 1
    - File exports `function NextSeasonPlannerTab` (named export, NOT default)
    - File contains the literal heading string `"Pre-Season Squad"` and `"GW1–8 Fixture Difficulty"`
    - File contains the literal graceful copy `"Pre-season squad not yet available"` (or `"squad builder becomes available once the season archive is ready"`)
    - File contains the literal graceful copy `"Fixtures not yet published"`
    - File imports `HeatMapRow` from `@/components/club-form/FixtureHeatMap` (source assertion via grep)
    - File imports `usePreSeasonSquad` from `@/lib/hooks/usePreSeasonSquad`
    - File contains a `title=` attribute on a span rendering `total_points` (ppm tooltip — source assertion)
    - File contains class `border-l-2 border-green-500` (XI accent border per UI-SPEC)
    - File contains class `opacity-60` (bench dimming per UI-SPEC)
    - File does NOT contain `<button` elements that mutate squad state (read-only D-04 — source assertion: `grep -E "&lt;button[^&gt;]*onClick" src/components/next-season/NextSeasonPlannerTab.tsx` returns 0)
    - `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` exits 0 with 4/4 green
  </acceptance_criteria>
  <done>NextSeasonPlannerTab GREEN; all four RTL tests pass; read-only contract enforced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register 'next-season' sub-tab in src/app/page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx lines 55-105 (SubTab union, SECTIONS array — Phase 125 added 'window'; this task appends 'next-season')
    - src/app/page.tsx lines 280-310 (render switch — the `'rivals'` block at lines 290-292 is the insertion neighbour)
    - src/components/next-season/NextSeasonPlannerTab.tsx (just shipped — confirm the named export name `NextSeasonPlannerTab`)
    - .planning/phases/126-next-season-planner/126-CONTEXT.md (D-07 — exact ID, label, mobileLabel, and placement order)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Pitfall 6 — SubTab union MUST be extended or TypeScript errors)
  </read_first>
  <action>
    Three precise edits to `src/app/page.tsx`:

    EDIT 1 (line 58, the `SubTab` union):
    Append `| 'next-season'` to the END of the union literal. The line currently ends with `... | 'rank-sim'`. After this edit it ends with `... | 'rank-sim' | 'next-season'`.

    EDIT 2 (the Plan SECTIONS subTabs array, ending at line 86 with the `'rivals'` entry):
    Append a new entry immediately AFTER the `'rivals'` line and BEFORE the closing `],` of the subTabs array:
    `      { id: 'next-season' as SubTab, label: 'Next Season', mobileLabel: 'Pre-Season' },`
    Indentation must match the surrounding entries (6 spaces — verify by reading the file).

    EDIT 3 (the render switch, after the `'rivals'` block at lines 290-292):
    Add an import at the top of page.tsx (in the existing component imports block): `import { NextSeasonPlannerTab } from '@/components/next-season/NextSeasonPlannerTab'`.
    Then append a new render conditional immediately AFTER the `'rivals'` block:
    `        {activeSection === 'plan' && activeSubTab === 'next-season' && (\n          <NextSeasonPlannerTab />\n        )}`
    Indentation must match the surrounding render blocks.

    Do NOT modify the `defaultSubTab: 'planner'` for the Plan section — the new tab is appended, not made default.
    Do NOT touch the `Analyse` or `Squad` SECTIONS entries.
    Do NOT remove or reorder existing sub-tabs.
  </action>
  <verify>
    <automated>grep -c "'next-season'" src/app/page.tsx; grep -c "NextSeasonPlannerTab" src/app/page.tsx; npx tsc --noEmit 2>&1 | grep -E "error TS" | head -3; echo "tsc-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'next-season'" src/app/page.tsx` returns >= 3 (union literal + SECTIONS entry + render condition)
    - `grep -c "NextSeasonPlannerTab" src/app/page.tsx` returns 2 (import statement + JSX usage)
    - `grep -c "from '@/components/next-season/NextSeasonPlannerTab'" src/app/page.tsx` returns 1
    - The line containing `'rivals' as SubTab` appears BEFORE the line containing `'next-season' as SubTab` in the SECTIONS array (verifiable via `grep -n`)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (full suite green — no regression in any other tab)
    - `defaultSubTab: 'planner' as SubTab` is unchanged in the Plan section
  </acceptance_criteria>
  <done>page.tsx registers next-season tab; tsc clean; full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → React state | All inputs to NextSeasonPlannerTab come from typed hooks; no user-supplied strings reach `dangerouslySetInnerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-04-01 | I (Info Disclosure) | tooltip on total-points span | accept | `title={\`${p.ppm.toFixed(2)}pts/min (last season)\`}` — string interpolation of a Number. No XSS risk; React escapes the title attribute. |
| T-126-04-02 | T (Tampering) | client-side render of PreSeasonSquad | accept | Read-only display (D-04 — no mutation paths). No `<button>` with onClick changing state. Source assertion in acceptance_criteria. |
| T-126-04-03 | V5 (Input Validation) | hook data is typed `PreSeasonSquad | null` | accept | Type guard ensures `data.starters` / `data.bench` are arrays; null branch handled explicitly. No untyped JSON.parse in the component. |
| T-126-04-04 | D (DoS) | Heatmap render with 20 teams * 8 GWs | accept | 160 cells — negligible render cost. HeatMapRow is already proven in production via ClubFormTab. |
</threat_model>

<verification>
- `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` exits 0 with 4/4 green
- `npx vitest run` (full suite) exits 0
- `npx tsc --noEmit` exits 0
- `grep -c "'next-season'" src/app/page.tsx` returns >= 3
- `grep -c "NextSeasonPlannerTab" src/app/page.tsx` returns 2
- No `dangerouslySetInnerHTML` in NextSeasonPlannerTab.tsx (source assertion)
- No `<button` with `onClick` mutating squad state in NextSeasonPlannerTab.tsx (source assertion — read-only D-04)
- `defaultSubTab: 'planner'` for the Plan section is unchanged
</verification>

<success_criteria>
- NSP-03 fulfilled: GW1-8 FDR heatmap section reuses exported `HeatMapRow` and renders the "Fixtures not yet published" empty state during the deferred GW1-8-FIXTURES window
- NSP-04 fulfilled: Next Season Planner tab surfaces in Plan section with sub-tab id `'next-season'`, label "Next Season", mobileLabel "Pre-Season", placed after `'rivals'`. "Prices pending" graceful state renders when `usePreSeasonSquad()` returns null.
- All four Wave 0 `NextSeasonPlannerTab.test.tsx` tests transition RED → GREEN
- Full vitest suite stays green — no regression in any existing tab
- Phase 126 complete: NSP-01..NSP-04 all shipped
</success_criteria>

<output>
After completion, create `.planning/phases/126-next-season-planner/126-04-SUMMARY.md`
</output>
