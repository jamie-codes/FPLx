---
phase: 126
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pipeline/requirements.txt
  - src/lib/pre-season-squad.test.ts
  - src/components/next-season/NextSeasonPlannerTab.test.tsx
  - pipeline/test_archive_season.py
  - src/lib/types.ts
autonomous: true
requirements:
  - NSP-01
  - NSP-02
  - NSP-03
  - NSP-04
tags:
  - testing
  - tdd
  - fixtures
  - pipeline
must_haves:
  truths:
    - "Test files exist for every Wave 1+ deliverable"
    - "Tests are RED at start of Wave 1 (no implementation present yet)"
    - "pulp>=2.7.0 is declared in pipeline/requirements.txt"
    - "PreSeasonPlayer and PreSeasonSquad types exist in src/lib/types.ts"
  artifacts:
    - path: "pipeline/requirements.txt"
      provides: "pulp dependency declaration"
      contains: "pulp>=2.7.0"
    - path: "src/lib/pre-season-squad.test.ts"
      provides: "NSP-02 unit test scaffold (RED)"
    - path: "src/components/next-season/NextSeasonPlannerTab.test.tsx"
      provides: "NSP-03 + NSP-04 RTL test scaffold (RED)"
    - path: "pipeline/test_archive_season.py"
      provides: "NSP-01 pytest scaffold (RED)"
    - path: "src/lib/types.ts"
      provides: "PreSeasonPlayer and PreSeasonSquad type exports"
      contains: "export interface PreSeasonPlayer"
  key_links:
    - from: "src/lib/pre-season-squad.test.ts"
      to: "src/lib/types.ts"
      via: "import type { PreSeasonPlayer, PreSeasonSquad }"
      pattern: "import.*pre.*season|PreSeasonPlayer"
---

<objective>
Wave 0 scaffold: lay down failing tests (RED) for every Wave 1+ deliverable, declare the missing Python dependency (`pulp`), and publish the shared TypeScript types that downstream waves consume. After this plan completes, every subsequent task can pull a typed contract and a failing test from main without exploration.

Purpose: Nyquist gate. RESEARCH.md (Validation Architecture) lists four missing test files plus the missing pulp dependency — all four MUST exist before Wave 1 starts or the per-task `<verify>` commands cannot run.

Output: Three test scaffolds (vitest + pytest), one requirements.txt line, and one types-only block in src/lib/types.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-next-season-planner/126-CONTEXT.md
@.planning/phases/126-next-season-planner/126-RESEARCH.md
@.planning/phases/126-next-season-planner/126-PATTERNS.md
@.planning/phases/126-next-season-planner/126-UI-SPEC.md
@.planning/phases/126-next-season-planner/126-VALIDATION.md

<interfaces>
<!-- Types this plan introduces. Wave 1+ plans will import these. -->
Target additions to src/lib/types.ts (full implementations belong in Wave 1; here we publish the type contracts only):

```typescript
export interface PreSeasonPlayer {
  id: number
  web_name: string
  element_type: PositionCode   // 1=GK, 2=DEF, 3=MID, 4=FWD (existing union in types.ts)
  team: number
  team_short_name: string
  now_cost: number             // tenths of GBP 1m (GW38 archive price)
  total_points: number         // last-season total (display field)
  ppm: number                  // points_per_minute (tooltip signal)
}

export interface PreSeasonSquad {
  starters: PreSeasonPlayer[]  // 11 in best XI
  bench: PreSeasonPlayer[]     // 4
  formation: string            // e.g. "4-3-3"
  budgetUsed: number           // tenths of GBP 1m
}

export interface SeasonArchiveEntry {
  // Per-player element-summary archive shape (from /element-summary/{id}/)
  history: Array<{
    element: number
    total_points: number
    minutes: number
    [k: string]: unknown      // other fields preserved verbatim
  }>
  history_past?: unknown[]
  // summary_season field is captured but not strictly typed here
}
```

Existing reference (read before editing types.ts to avoid duplicate definitions):
```
src/lib/types.ts lines 1-3: PositionCode is already exported as `1 | 2 | 3 | 4`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Declare pulp dependency and create PreSeason* types</name>
  <files>pipeline/requirements.txt, src/lib/types.ts</files>
  <read_first>
    - pipeline/requirements.txt (read entire file to see existing dependency format — alphabetical? pinned? comment style)
    - src/lib/types.ts (read first 100 lines to confirm PositionCode export and find an idiomatic insertion point near other player-shape interfaces; do NOT scroll past line 200 — Edit at the chosen location)
    - .planning/phases/126-next-season-planner/126-RESEARCH.md (Standard Stack section — pulp version reasoning)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`src/lib/pre-season-squad.ts` analog block — exact field list for PreSeasonPlayer / PreSeasonSquad)
  </read_first>
  <action>
    Add `pulp>=2.7.0` to `pipeline/requirements.txt`. Place it in the file using the same alphabetical / grouped convention found there (read_first first).

    In `src/lib/types.ts`, append (or place near the other player/squad interfaces) three exported interfaces: `PreSeasonPlayer`, `PreSeasonSquad`, `SeasonArchiveEntry`. Field shapes are listed in the `<interfaces>` block above — copy them verbatim. `PreSeasonPlayer.element_type` MUST reuse the existing `PositionCode` union (do NOT redeclare `1 | 2 | 3 | 4` inline; import or reference the existing alias). `SeasonArchiveEntry.history[]` MUST include at minimum `element: number`, `total_points: number`, `minutes: number`, plus an index signature `[k: string]: unknown` to preserve extra fields.

    Do NOT implement `buildPreSeasonSquad` in this task — that is Wave 1 (Plan 02). This task only publishes the type contracts.
  </action>
  <verify>
    <automated>npx vitest run --typecheck src/lib/types.ts 2>&1 | grep -v "^#" | grep -E "(error|fail)" ; echo "exit=$?"</automated>
    Additionally: `grep -v '^#' pipeline/requirements.txt | grep -c '^pulp' ` must equal 1.
  </verify>
  <acceptance_criteria>
    - `pipeline/requirements.txt` contains a non-comment line starting with `pulp` and version specifier `>=2.7.0`
    - `src/lib/types.ts` contains `export interface PreSeasonPlayer` with all 8 listed fields
    - `src/lib/types.ts` contains `export interface PreSeasonSquad` with `starters`, `bench`, `formation`, `budgetUsed`
    - `src/lib/types.ts` contains `export interface SeasonArchiveEntry` with `history` array
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
    - `PreSeasonPlayer.element_type` references the existing `PositionCode` alias (not an inline `1 | 2 | 3 | 4`)
  </acceptance_criteria>
  <done>Types exported, pulp declared, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED vitest scaffolds for NSP-02, NSP-03, NSP-04</name>
  <files>src/lib/pre-season-squad.test.ts, src/components/next-season/NextSeasonPlannerTab.test.tsx</files>
  <read_first>
    - src/lib/chip-modes.test.ts (test pattern + makeChipSquadPlayer factory — the analog for pre-season-squad.test.ts)
    - src/components/optimiser/OptimiserPanel.test.tsx (vi.mock hook pattern + RTL render pattern for NextSeasonPlannerTab.test.tsx)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`src/lib/pre-season-squad.test.ts` + `NextSeasonPlannerTab.test.tsx` analog blocks — gives the exact test case list)
    - src/lib/types.ts (after Task 1 edits — to import PreSeasonPlayer / PreSeasonSquad)
  </read_first>
  <behavior>
    `src/lib/pre-season-squad.test.ts`:
    - Test 1: `buildPreSeasonSquad()` returns a valid 15-player squad at budget=1000 given a 30-player pool with sufficient cheap eligible players (asserts starters.length===11, bench.length===4, budgetUsed<=1000)
    - Test 2: `buildPreSeasonSquad()` returns `null` when budget is too low (budget=200 with all players >=50 cost)
    - Test 3: `buildPreSeasonSquad()` excludes players whose id is not in scoreMap (asserts no excluded id appears in `starters` or `bench`)
    - Test 4: `buildPreSeasonSquad()` respects team cap (no team appears more than 3 times across starters+bench)

    `src/components/next-season/NextSeasonPlannerTab.test.tsx`:
    - Test 1: renders "Pre-season squad not yet available" heading when `usePreSeasonSquad()` returns `{ data: null, isLoading: false, isError: false }`
    - Test 2: renders formation grid (a node with the formation string and at least 15 player rows) when `usePreSeasonSquad()` returns a populated `PreSeasonSquad`
    - Test 3: renders "Fixtures not yet published" copy when next-season fixtures hook returns empty / all-finished
    - Test 4: renders error copy "Failed to load pre-season squad" when `isError===true`
  </behavior>
  <action>
    Create `src/lib/pre-season-squad.test.ts` with the `@vitest-environment node` directive on line 1, import `buildPreSeasonSquad` from `./pre-season-squad` (file does not exist yet — import will fail and that is the RED state), and import `PreSeasonPlayer`, `PreSeasonSquad` from `./types`. Implement a `makePreSeasonPlayer(overrides)` factory mirroring the analog in `src/lib/chip-modes.test.ts`. Write the four `it()` cases from `<behavior>`. Use a `Map<number, number>` for scoreMap with explicit entries.

    Create `src/components/next-season/NextSeasonPlannerTab.test.tsx` with the `@vitest-environment jsdom` directive on line 1. Use `vi.mock('@/lib/hooks/usePreSeasonSquad', () => ({ usePreSeasonSquad: () => usePreSeasonSquadMock() }))` exactly as in `OptimiserPanel.test.tsx`. Import `render` from `@testing-library/react`. The component import (`from './NextSeasonPlannerTab'`) will fail — that is the RED state. Write the four `it()` cases from `<behavior>`.

    Both files MUST fail at this point (import resolution error). Do NOT skip them with `it.skip`; the executor of Wave 1 needs them RED so the test runner exits non-zero until implementation lands.
  </action>
  <verify>
    <automated>npx vitest run src/lib/pre-season-squad.test.ts src/components/next-season/NextSeasonPlannerTab.test.tsx 2>&1 | tee /tmp/126-w0-vitest.log; grep -E "FAIL|Cannot find module" /tmp/126-w0-vitest.log | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/pre-season-squad.test.ts` exists and starts with `// @vitest-environment node`
    - File contains exactly four `it(` invocations
    - File imports from `./pre-season-squad` and `./types`
    - `src/components/next-season/NextSeasonPlannerTab.test.tsx` exists and starts with `// @vitest-environment jsdom`
    - File contains a `vi.mock('@/lib/hooks/usePreSeasonSquad', ...)` block
    - File contains exactly four `it(` invocations
    - `npx vitest run src/lib/pre-season-squad.test.ts` exits NON-ZERO (RED state, expected)
    - `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` exits NON-ZERO (RED state, expected)
  </acceptance_criteria>
  <done>Both vitest scaffolds committed in RED state; failure mode is import resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED pytest scaffold for NSP-01 archive_season</name>
  <files>pipeline/test_archive_season.py</files>
  <read_first>
    - pipeline/test_xmins_stats.py (or any existing `pipeline/test_*.py`) for the pytest test-file convention used in this codebase (import path, monkeypatch usage, fixture style)
    - pipeline/lineup_news.py (non-fatal scraper isolation pattern that archive_season will mirror)
    - pipeline/upload.py (the `save()` function signature that archive_season will call; tests must monkeypatch this entry point)
    - .planning/phases/126-next-season-planner/126-PATTERNS.md (`pipeline/archive_season.py` analog block — gives the >=50% threshold and idempotency check shape)
  </read_first>
  <behavior>
    - Test 1 — idempotency: when `_blob_exists('season_archive_gw38.json')` returns True, `archive_season(bootstrap)` returns early and never calls `save()`
    - Test 2 — partial-write guard, success: when 70% of element_summary fetches succeed, `save('season_archive_gw38.json', ...)` is called exactly once with a dict whose length equals the successful fetches
    - Test 3 — partial-write guard, failure: when <50% of element_summary fetches succeed, `save(...)` is NEVER called and a warning is logged to stderr
    - Test 4 — non-fatal player failures: an exception inside `_fetch_one(player_id)` does not abort the loop; surviving players still appear in the saved dict
  </behavior>
  <action>
    Create `pipeline/test_archive_season.py`. Mirror the pytest layout of an existing `pipeline/test_*.py` file (read_first one of them first).

    Import target symbols (`archive_season`, `_blob_exists`, `_fetch_one`, `_fetch_all_summaries`) from `archive_season`. The module does not exist yet — that is the intended RED state.

    Use `monkeypatch` (or `unittest.mock.patch`) to stub:
    - `archive_season._blob_exists` (idempotency test)
    - `archive_season.save` (capture call args; spy)
    - `archive_season.get_element_summary` (return canned dict or raise for partial-failure tests)

    Build a small fake `bootstrap` dict literal: `{'elements': [{'id': i} for i in range(1, 11)]}`. Use 10 players to make the 50% threshold trivial to demonstrate (5 succeed = boundary case).

    All four tests will fail at collection time (`ModuleNotFoundError: archive_season`). That is the RED state — do NOT add `pytest.skip`.
  </action>
  <verify>
    <automated>python -m pytest pipeline/test_archive_season.py -x --tb=short 2>&1 | tee /tmp/126-w0-pytest.log; grep -E "ModuleNotFoundError|FAILED|ERROR" /tmp/126-w0-pytest.log | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/test_archive_season.py` exists
    - File contains four `def test_` functions covering idempotency, >=50% success, <50% failure, and non-fatal per-player exception
    - File monkeypatches/mocks `save`, `_blob_exists`, and `get_element_summary` (or equivalent shim)
    - `python -m pytest pipeline/test_archive_season.py` exits NON-ZERO with ModuleNotFoundError or ImportError (RED state, expected)
  </acceptance_criteria>
  <done>Pytest scaffold committed RED; failure is ImportError on `archive_season` module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Wave 0 is test scaffolding and type declarations only — no runtime path, no I/O, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-W0-01 | Tampering | `pipeline/requirements.txt` | accept | Standard requirements file; dependency pinning to `>=2.7.0` is intentional. Supply-chain risk is inherited from existing pip workflow; no new attack surface in this task. |
| T-126-W0-02 | I (Info Disclosure) | test fixtures | accept | Tests use synthetic 10-player bootstrap; no PII or real FPL data leaks into committed test files. |
</threat_model>

<verification>
- `pulp>=2.7.0` present in pipeline/requirements.txt
- `PreSeasonPlayer`, `PreSeasonSquad`, `SeasonArchiveEntry` exported from src/lib/types.ts
- `npx tsc --noEmit` exits 0
- `npx vitest run src/lib/pre-season-squad.test.ts` exits non-zero (RED)
- `npx vitest run src/components/next-season/NextSeasonPlannerTab.test.tsx` exits non-zero (RED)
- `python -m pytest pipeline/test_archive_season.py` exits non-zero (RED, ImportError)
- Wave 1 plans can `import { PreSeasonPlayer } from '@/lib/types'` without errors
</verification>

<success_criteria>
- All four MISSING references from VALIDATION.md Wave 0 Requirements satisfied
- All Wave 1 plans have a failing test target that will transition to GREEN once they ship
- No production code added in this plan — only types, tests, and a dependency declaration
</success_criteria>

<output>
After completion, create `.planning/phases/126-next-season-planner/126-01-SUMMARY.md`
</output>
