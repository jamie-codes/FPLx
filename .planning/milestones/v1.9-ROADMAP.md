# Milestone v1.9: Competitive Intelligence

**Status:** ✅ SHIPPED 2026-05-04
**Phases:** 56–60
**Total Plans:** 13
**Timeline:** 2026-05-03 → 2026-05-04 (2 days)
**Files changed:** 121 files, +27,865 / −1,388 lines

---

## Phases

### Phase 56: FT Engine Fix

**Goal:** Fix Wildcard free-transfer reset bug and surface correct FT state at start of next GW.

**Requirements:** FTX-01, FTX-02

**Status:** ✅ Complete (2026-05-03)

**Plans:**
- [x] 056-01: `computeNextFTState` Wildcard branch — `Math.min(1, currentAvailable - 1)` formula; `initialFTState` memo in PlannerTab

**Key files:**
- `src/lib/free-transfer-engine.ts` — `computeNextFTState` Wildcard branch fix
- `src/components/planner/PlannerTab.tsx` — `initialFTState` useMemo replaces stale constant

**Key decisions:**
- `Math.min(1, currentAvailable - 1)`: Wildcard bank must preserve 1 FT (not 0) when current available ≥ 2; clamps to 1 when starting from 1
- `initialFTState` memo: derive from `myTeam.transfers` at render time, not at module load

**Issues resolved:**
- Wildcard activation was resetting available FTs to 0 instead of 1 (FTX-01)
- PlannerTab was reading stale initialFTState from a hardcoded constant (FTX-02)

---

### Phase 57: Effective Ownership Mode

**Goal:** Surface EO% context alongside captain picks — ranked top-5, dangerous-to-fade badge, 4-mode EO toggle.

**Requirements:** EO-01, EO-02, EO-03, EO-04

**Status:** ✅ Complete (2026-05-03)

**Plans:**
- [x] 057-01: `computeEOCandidates` engine — ranked top-5 by EO%, badge thresholds, captain vs EO divergence score
- [x] 057-02: `CaptainPicksPanel` rewrite — EOModeToggle (4 modes), Dangerous-to-fade badge, 14 RTL tests

**Key files:**
- `src/lib/effective-ownership.ts` — `computeEOCandidates`, `EOCandidate` type, badge thresholds
- `src/components/accuracy/CaptainPicksPanel.tsx` — 4-mode EOModeToggle, ranked list, badge render
- `src/components/accuracy/EOModeToggle.tsx` — new component, 4 toggle modes
- `src/components/accuracy/CaptainPicksPanel.test.tsx` — 14 RTL tests

**Key decisions:**
- 4 EO modes: `own` (ownership %), `eo` (effective ownership %), `diff` (divergence), `badge` (dangerous-to-fade)
- Dangerous-to-fade badge fires when captain EO% > 30% and manager's captain pick diverges
- Top-5 ranked by EO% descending; ties broken by player name

**Issues resolved:**
- CaptainPicksPanel previously showed 2-card layout with no EO context (EO-01)
- No "dangerous to fade" signal existed anywhere in the UI (EO-03)

---

### Phase 58: Mini-League Rival Tracker

**Goal:** Fetch and display rivals' squads + live rank within the manager's mini-league.

**Requirements:** ML-01, ML-02, ML-03, ML-04, ML-05, ML-06, ML-07, ML-08

**Status:** ✅ Complete (2026-05-04)

**Plans:**
- [x] 058-01: `rival-intel.ts` engine — league fetch, entry parse, rival squad hydration
- [x] 058-02: `useRivals` hook — `p-limit(3)` 3-concurrent-request batching, error boundary
- [x] 058-03: `RivalsTab` UI — league table, rival squad card, diff indicators
- [x] 058-04: Integration + E2E tests — ML-01..ML-08 covered, 8/8 verified

**Key files:**
- `src/lib/rival-intel.ts` — league fetch pipeline, `RivalEntry`, `RivalSquad` types
- `src/hooks/useRivals.ts` — `p-limit(3)` batched squad fetching, caching
- `src/components/rivals/RivalsTab.tsx` — league table + rival squad cards
- `src/components/rivals/RivalsTab.test.tsx` — 8 RTL tests

**Key decisions:**
- `p-limit ^6.1.0` for concurrent rival fetch batching — 3 concurrent max (FPL API rate limit)
- Rivals limited to top-10 in mini-league standings (manageable fetch volume)
- Diff indicators: green (rival doesn't own), red (rival does own but manager doesn't)

**Issues resolved:**
- No rival visibility existed — managers had to manually check rival team pages (ML-01)
- FPL API has no bulk squad endpoint — `p-limit` batching required for safe concurrent fetches (ML-08)

**Issues deferred:**
- ML-09: pagination for leagues > 50 members — deferred to v1.12 (requires UI state work)

---

### Phase 59: Manual Transfer Planner

**Goal:** GW-by-GW manual transfer planning with bank/FT simulation and localStorage persistence.

**Requirements:** MTP-01, MTP-02, MTP-03, MTP-04, MTP-05, MTP-06, MTP-07, MTP-08

**Status:** ✅ Complete (2026-05-04)

**Plans:**
- [x] 059-01: `manual-plan.ts` engine — `ManualPlan` type, `truncateOrExtendSteps`, `simulatePlan`, Phase 56 FT engine reused verbatim
- [x] 059-02: `ManualPlanTab` base UI — step cards, player picker, bank/FT display
- [x] 059-03: Persistence + chip integration — localStorage via `persistManualPlan`/`loadManualPlan`, chip selector

**Key files:**
- `src/lib/manual-plan.ts` — `ManualPlan`, `PlanStep`, `simulatePlan`, `truncateOrExtendSteps`
- `src/components/planner/ManualPlanTab.tsx` — 429-line UI component
- `src/components/planner/ManualPlanTab.test.tsx` — 21 RTL tests

**Key decisions:**
- Reused Phase 56 `free-transfer-engine.ts` verbatim — no duplication
- `truncateOrExtendSteps`: horizon reduction drops tail steps; extension pads with empty steps
- MTP-07 sell price caveat: shown only when unauthenticated; exact `selling_price` used when authenticated
- `window.location.reload()` hack from Plan 02 no-squad submit remains — not ideal but stable
- `activeSection === 'plan'` render guard (not `!== 'squad'`) — locks tab strictly to Plan section

**Issues resolved:**
- No multi-GW transfer planning tool existed — managers planned in spreadsheets (MTP-01)
- FT carryover simulation was error-prone manually (MTP-03)

---

### Phase 60: Transfer Route Tree

**Goal:** Visualise the top-3 "sell root" transfer paths as an expandable tree; bridge selected path into Manual Planner.

**Requirements:** TRT-01, TRT-02, TRT-03, TRT-04, TRT-05, TRT-06, TRT-07

**Status:** ✅ Complete (2026-05-04)

**Plans:**
- [x] 060-01: `buildTransferRouteTree` engine — pure TS, greedy continuation per branch, 430 lines, 31 tests
- [x] 060-02: `RouteTreeTab` UI + D-07 horizon lift — 429-line component, 21 RTL tests; planHorizon lifted to `page.tsx`

**Key files:**
- `src/lib/transfer-route-tree.ts` — `buildTransferRouteTree`, `RouteNode`, `RoutePath` types
- `src/components/planner/RouteTreeTab.tsx` — expandable tree table, bridge button, confirm dialog
- `src/components/planner/RouteTreeTab.test.tsx` — 21 RTL tests
- `src/app/page.tsx` — `[planHorizon, setPlanHorizon]` state, section-level `<HorizonSelector>`
- `src/components/planner/PlannerTab.tsx` — accepts `horizon` prop (no local state)
- `src/components/planner/ManualPlanTab.tsx` — accepts `horizon` prop, D-07 sync effect

**Key decisions:**
- TRT-01: pure TypeScript — no LLM; top-3 sell roots + greedy continuation per branch
- D-07 horizon lift: `page.tsx` owns `[planHorizon, setPlanHorizon]`; single `HorizonSelector` drives all Plan sub-tabs
- TRT bridge chip always `null` (`chipMode: null`) — ChipToggle UI deferred per RESEARCH.md Open Question 2
- Inline confirm fires only when existing plan has any `step.transfers.length > 0`; silent overwrite when empty

**Issues resolved:**
- No visual representation of multi-step transfer paths existed (TRT-01)
- Each Plan sub-tab had its own horizon state — they fell out of sync (D-07)
- No path to load a planned route into Manual Planner without re-entering manually (TRT-05)

**Issues deferred:**
- TRT-06: ChipToggle UI in RouteTreeTab — `chipMode` hardcoded to `null`; deferred to v1.12
- TRT-02: "Hits" column shows `path.totalTransfers` label; `path.totalHits` always 0 per D-01 (cosmetic, deferred)
- Phase 60 VERIFICATION.md not created — documented gap; 14/14 UAT verified manually in STATE.md

---

## Tech Debt Carried Forward

| ID | Description | Phase | Target |
|----|-------------|-------|--------|
| TRT-06 | ChipToggle UI in RouteTreeTab — chipMode hardcoded null | 60 | v1.12 |
| TRT-02 | "Hits" column label cosmetic mismatch | 60 | v1.12 |
| WR-02 | decision-severity.ts captain MEDIUM when candidates < 2 | pre-v1.9 | backlog |
| WR-01 | DecisionSummaryTab Load Squad duplicate transition classes | pre-v1.9 | backlog |
| WR-03/04 | MobileNav.test.tsx pill description + Acc pill untested | pre-v1.9 | backlog |
| — | captain-picks.test.ts 5 pre-existing failures (Phase 57 rewrite) | 57 | backlog |
| — | club-form.test.ts 1 pre-existing failure (unrelated) | pre-v1.9 | backlog |

---

## Cross-Phase Integration

- **Phase 56 → 59:** `free-transfer-engine.ts` reused verbatim in `manual-plan.ts` `simulatePlan`
- **Phase 59 → 60:** `persistManualPlan`/`loadManualPlan` bridge wires TRT-05 path load into Manual Planner
- **Phase 60 → 57/59:** D-07 horizon lift — `page.tsx` `planHorizon` drives PlannerTab, ManualPlanTab, RouteTreeTab simultaneously

---

_Archived: 2026-05-04_
