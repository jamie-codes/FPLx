# Milestone v1.20: Fixes & Decision Quality

**Status:** ✅ SHIPPED 2026-05-16
**Phases:** 110-113
**Total Plans:** 15

## Overview

Bug-sweep and UX polish milestone. Fixed 6 data accuracy bugs across GW Review, fixture heatmap, planner, and decision history; shipped the Transfer Regret Backtester; made the Optimiser on-demand; capped transfer suggestions to top 3 per position.

## Phases

### Phase 110: GW Review & History Fixes

**Goal:** Fix four data accuracy bugs in GW Review and decision history captain delta
**Depends on:** Phase 109 (v1.19 complete)
**Requirements:** FIX-03, FIX-04, FIX-05, FIX-06
**Plans:** 4 plans

Plans:
- [x] 110-01: FIX-03 — GW Review top scorer points display fix
- [x] 110-02: FIX-04 + FIX-05 — Best bench points (0→actual) and dream team delta sign inversion
- [x] 110-03: FIX-06 — Decision history captain delta (dashes→actual pts diff via liveMap)
- [x] 110-04: CR-01 gap closure — captainDeltaRaw liveMap fix for settled GWs

**Details:**
GW Review route extended to call the FPL live endpoint to source actual settled-GW points. `liveMap.get(element)` replaces `pick.total_points` as the authoritative source for captain delta, top scorer, and bench pts across `/api/gw-review`. The dream team delta sign was inverted (dream_team_score − user_team_total, not reversed). All 4 FIX requirements resolved in 4 plans.

---

### Phase 111: Fixture Heatmap & Planner Cross-Position Fixes

**Goal:** Fix heatmap BGW false-positive for already-played GW leg; enforce position-lock in transfer planner at all call sites
**Depends on:** Phase 110
**Requirements:** FIX-01, FIX-02
**Plans:** 4 plans

Plans:
- [x] 111-01: FIX-02 — Types + position-lock guard design
- [x] 111-02: FIX-02 — Position-lock enforced at all 4 `suggestTransfers` call sites with `inPoolByPosition.get(sell.element_type)` + `VALID_ELEMENT_TYPES` guard
- [x] 111-03: FIX-01 — HeatMapRow partially-played DGW cell (played-leg tooltip suffix branch)
- [x] 111-04: FIX-01 gap closure — CR-02 fallback-GW sort hardening in `club-form.ts`

**Details:**
Position-lock wired at all 4 `suggestTransfers` call sites using `inPoolByPosition.get(sell.element_type)`. HeatMapRow gained a mixed-state branch (`fixtures.length ≥ 1 && playedFixtures.length ≥ 1`) surfacing the played leg via tooltip suffix `'— Played'` without breaking the dominant upcoming-fixture cell. CR-02 fallback `.sort((a,b) => a.id-b.id)` before `.slice(-1)` in `club-form.ts`. 37/37 plan-specific tests GREEN.

---

### Phase 112: Optimiser On-Demand & Transfer Suggestion Cap

**Goal:** Optimiser tab shows empty state with explicit trigger button; transfer suggestions capped at top 3 per position
**Depends on:** Phase 111
**Requirements:** OPT-01, TFR-02
**Plans:** 3 plans

Plans:
- [x] 112-01: OPT-01 — `hasRun` gate, empty state component, "Optimise Lineup" button in OptimiserPanel
- [x] 112-02: OPT-01 gap closure — `hasRun` gate wired correctly; controls interactive pre-click; post-click control changes re-trigger silently
- [x] 112-03: TFR-02 — `capByPosition(raw, 3)` pure utility; cap wired into both OptimiserPanel and TransferPanel/OpportunityCostTable; per-position `cap-footnote-{POS}` rendered when bucket exceeds limit

**Details:**
`hasRun` boolean state gate prevents auto-compute on OptimiserPanel mount. `capByPosition(raw, n)` pure function caps `suggestTransfers` output at top-n per `element_type` bucket; `ocsTotalsByPosition` forwards pre-cap counts for footnote rendering. 67/67 Phase 112 tests GREEN.

---

### Phase 113: Transfer Regret Backtester

**Goal:** Manager can see what the engine recommended each GW vs what was actually done, with hindsight xPts delta
**Depends on:** Phase 112
**Requirements:** BACK-02
**Plans:** 4 plans

Plans:
- [x] 113-01: Data layer — `pipeline/transfer_snapshots.py`, SLIM_FIELDS tuple, `write_transfer_slim_snapshot()`, wired into `run.py`; `SlimPlayer` + `TransferRegretEntry` types in `types.ts`
- [x] 113-02: Math primitives — `computeTransferDelta` + `computeTransferSeasonSummary` in `src/lib/regret.ts`; 14 TDD tests GREEN
- [x] 113-03: API extension — `readTransferSlimSnapshot` + `fetchTransfers` + `reconstructPreTransferSquad` helpers; `GET /api/decision-history` extended to return `transferEntries[]`
- [x] 113-04: UI toggle — Captain|Transfer pill in BackTab; `TransferRegretView` + `TransferSeasonSummaryHeader` + `TransferRegretChart`; 25 tests GREEN

**Details:**
Pipeline writes `merged_players_slim_gw{N}.json` (9 fields) to Vercel Blob as a durable recommendation snapshot trail. `/api/decision-history` joins snapshots with FPL `event_transfers` to compute per-GW `computeTransferDelta` (engine gain − user gain, signed). `BackTab` gains a Captain|Transfer pill toggle; Transfer view shows season summary header + Recharts bar chart (red=engine better, green=good hold) + per-GW detail table. 9 ASVS L1 guards in place (teamId regex, blob exact-match, element-summary SSRF, JSON parse try/catch). Human UAT gate pending (dark mode rendering, multi-transfer GW format, colour correctness).

---

## Milestone Summary

**Decimal Phases:** None

**Key Decisions:**
- liveMap as authoritative source for settled-GW points (not pick.total_points which is 0 for settled GWs) — Phase 110
- Position-lock enforced at call sites, not at engine level — cleaner than engine-level change given multiple entry points after v1.6/v1.9/v1.17 expansion — Phase 111
- `capByPosition` as pure utility shared across OptimiserPanel and TransferPanel — Phase 112
- Slim snapshot pattern (9-field subset) mirrors Phase 96 captain snapshot exactly — Phase 113
- BACK-02 hindsight scoring uses actual realised FPL points, not predicted xPts — Phase 113

**Issues Resolved:**
- FIX-01: Heatmap partially-played DGW false BGW cell
- FIX-02: Cross-position transfer suggestions (MID→GK etc.)
- FIX-03: GW Review top scorer showing 0 pts instead of actual
- FIX-04: GW Review best bench showing 0 instead of actual bench pts
- FIX-05: Dream team delta sign inverted
- FIX-06: Decision history captain delta showing dashes instead of pts diff

**Issues Deferred:**
- Phase 113 human UAT checkpoint: dark mode rendering, multi-transfer GW compression, delta colour verification — acknowledged as known gap at milestone close
- RANK-SPARK: rank_trajectory sparkline in GemTable
- TRT-06: ChipToggle UI in RouteTreeTab (chip mode hardcoded null)
- TRT-02: "Hits" column label cosmetic mismatch in RouteTreeTab
- TEST-57: captain-picks.test.ts 5 pre-existing failures from Phase 57

**Technical Debt Incurred:**
- Phase 113 human UAT not completed before milestone close — visual dark mode and multi-transfer GW row require live browser test

---

_For current project status, see .planning/ROADMAP.md_
