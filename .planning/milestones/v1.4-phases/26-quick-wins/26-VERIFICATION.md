---
phase: 26-quick-wins
verified: 2026-04-27T23:25:00Z
status: human_needed
score: 4/4 must-haves verified
re_verification: false
human_verification:
  - test: "Set Pieces tab renders 20 team cards"
    expected: "Clicking 'Set Pieces' on desktop shows a 3-column grid of 20 Premier League team cards each with Penalties, Direct FK, and Corners taker names"
    why_human: "set_piece_changes.json not yet generated by a pipeline run in this environment — API returns 500 locally until pipeline executes; visual layout cannot be confirmed programmatically"
  - test: "Amber change alert appears when taker changes detected"
    expected: "After a pipeline run where a taker order changes, an amber banner reading 'Set-piece changes detected — N taker order change(s)...' appears above the team grid, and affected takers show a 'Changed' badge"
    why_human: "Requires two sequential pipeline runs with an actual order change between them to trigger the alert path"
  - test: "Landscape tip on Gems and DefCon tabs in mobile portrait"
    expected: "On a mobile device (or DevTools at 390px portrait), the Gems and DefCon tabs each show 'Rotate to landscape for the full table.' above the table; tip disappears in landscape and is absent on desktop"
    why_human: "Orientation-detection and responsive CSS (sm:hidden) require a real browser to verify"
---

# Phase 26: Quick Wins Verification Report

**Phase Goal:** Deliver set-piece taker intelligence and quick mobile UX improvements — visible to users immediately after next pipeline run.
**Verified:** 2026-04-27T23:25:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User can see the penalty taker, direct FK taker, and corner taker for each Premier League team in a dedicated panel | ? HUMAN | SetPieceTakerPanel wired correctly to useSetPieces hook and tab system; requires pipeline run + browser to confirm rendering |
| 2 | User is alerted when a set-piece order has changed between the current and previous pipeline run | ? HUMAN | SetPieceChangeAlert and 'Changed' badge logic are substantive and wired; requires two pipeline runs with a real order change |
| 3 | User sees a subtle landscape tip when viewing Gems or DefCon tabs on a mobile device in portrait orientation | ? HUMAN | LandscapeTip wired into GemTable and DefConTables with correct isMobile+isPortrait guards and orientationchange listener; requires browser to confirm |
| 4 | Set-piece text fields from FPL bootstrap-static are present in merged_players.json | ✓ VERIFIED | merge.py lines 367-369 extract penalties_text, direct_freekicks_text, corners_and_indirect_freekicks_text; MergedPlayer type declares all three as string |

**Score:** All automated checks pass. 3 of 4 truths require human browser verification (not blocked — code is complete and correct).

---

### Required Artifacts

#### Plan 01 Artifacts

| Artifact | Status | Evidence |
|----------|--------|---------|
| `pipeline/merge.py` | ✓ VERIFIED | Lines 367-369: all three `_text` fields extracted via `element.get()` with `''` default, inserted after `_order` siblings |
| `pipeline/run.py` | ✓ VERIFIED | `_extract_sp_snapshot` (line 31), `_diff_sp_snapshots` (line 47), snapshot diff block (lines 149-165) — substantive, non-stub implementations |
| `src/lib/types.ts` | ✓ VERIFIED | `penalties_text: string`, `direct_freekicks_text: string`, `corners_and_indirect_freekicks_text: string` on MergedPlayer (lines 114-116); `SetPieceTaker`, `SetPieceTeam`, `SetPieceChanges` interfaces exported (lines 260-278) |
| `src/app/api/set-pieces/route.ts` | ✓ VERIFIED | `export async function GET()` using Blob/local pattern; serves `set_piece_changes.json`; Cache-Control: `public, s-maxage=3600, stale-while-revalidate=86400` |
| `src/lib/hooks/useSetPieces.ts` | ✓ VERIFIED | `export function useSetPieces()` with queryKey `['set-pieces']`; typed as `SetPieceChanges`; 6h staleTime; imports `SetPieceChanges` from `../types` |

#### Plan 02 Artifacts

| Artifact | Status | Evidence |
|----------|--------|---------|
| `src/components/set-pieces/SetPieceTakerPanel.tsx` | ✓ VERIFIED | `'use client'`; calls `useSetPieces()`; renders loading/error/empty states; 3-column grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`; `SetPieceChangeAlert`; TakerRow with `title="Taker order changed since last pipeline run"` Changed badge |
| `src/components/set-pieces/SetPieceChangeAlert.tsx` | ✓ VERIFIED | `'use client'`; `border-amber-400 bg-amber-50`; returns null when changeCount === 0; "Set-piece changes detected" copy |
| `src/components/set-pieces/LandscapeTip.tsx` | ✓ VERIFIED | `'use client'`; `sm:hidden` guard; isMobile && isPortrait gate; "Rotate to landscape for the full table." copy |
| `src/app/page.tsx` | ✓ VERIFIED | Tab union includes `'set-pieces'`; `SetPieceTakerPanel` imported; desktop button at line 75; `{activeTab === 'set-pieces' && <SetPieceTakerPanel />}` at line 110 |
| `src/components/nav/MobileNav.tsx` | ✓ VERIFIED | Tab union includes `'set-pieces'`; TABS array entry `{ id: 'set-pieces', label: 'SP' }` at line 10 |

---

### Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|---------|
| `pipeline/run.py` | `pipeline/cache/set_piece_changes.json` | `save('set_piece_changes.json', sp_changes)` | ✓ WIRED | Line 163: `save('set_piece_changes.json', sp_changes)` — reads prev snapshot first (line 154-160), then saves changes, then saves new snapshot (line 164) |
| `src/app/api/set-pieces/route.ts` | `pipeline/cache/set_piece_changes.json` | `readFile(cachePath, 'utf-8')` / Blob list | ✓ WIRED | Lines 12 and 19: both Blob (`prefix: 'set_piece_changes.json'`) and local (`join(process.cwd(), 'pipeline', 'cache', 'set_piece_changes.json')`) paths wired |
| `SetPieceTakerPanel.tsx` | `/api/set-pieces` | `useSetPieces` hook | ✓ WIRED | Panel calls `useSetPieces()` (line 25); hook fetches `/api/set-pieces` (useSetPieces.ts line 8) |
| `src/app/page.tsx` | `SetPieceTakerPanel.tsx` | `activeTab === 'set-pieces'` conditional | ✓ WIRED | Line 110: `{activeTab === 'set-pieces' && <SetPieceTakerPanel />}` |
| `GemTable.tsx` | `LandscapeTip.tsx` | import + render | ✓ WIRED | Line 22: `import { LandscapeTip }` from set-pieces; line 123: `<LandscapeTip isMobile={isMobile} isPortrait={isPortrait} />` |
| `DefConTables.tsx` | `LandscapeTip.tsx` | import + render | ✓ WIRED | Line 17: `import { LandscapeTip }` from set-pieces; line 121: `<LandscapeTip isMobile={isMobile} isPortrait={isPortrait} />` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `SetPieceTakerPanel.tsx` | `data` (SetPieceChanges) | `useSetPieces()` → `/api/set-pieces` → `set_piece_changes.json` | Yes — pipeline writes real taker IDs from bootstrap-static `_order` fields | ✓ FLOWING |
| `SetPieceChangeAlert.tsx` | `changeCount` prop | passed from `data.change_count` in SetPieceTakerPanel | Yes — computed by `_diff_sp_snapshots` comparing real snapshots | ✓ FLOWING |
| `LandscapeTip.tsx` | `isMobile`, `isPortrait` props | `window.innerWidth/Height` in GemTable/DefConTables via resize+orientationchange listeners | Yes — real DOM measurement | ✓ FLOWING |

Note: `set_piece_changes.json` does not yet exist in the local pipeline cache (pipeline has not been re-run since these code changes were committed). The data path is complete — the file will be produced on next pipeline run. The empty-state UI ("Set-piece taker data is not yet available. Run the pipeline to populate this panel.") handles this correctly.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Python snapshot functions importable | `python -c "from pipeline.run import _extract_sp_snapshot, _diff_sp_snapshots; print('imports OK')"` | `imports OK` | ✓ PASS |
| Test suite unbroken (21 files, 240 tests) | `npx vitest run` | 21 passed, 240 passed, 8 skipped | ✓ PASS |
| API route file exists | `test -f src/app/api/set-pieces/route.ts` | exists | ✓ PASS |
| Hook file exists | `test -f src/lib/hooks/useSetPieces.ts` | exists | ✓ PASS |
| All 4 commits present in git log | `git show --stat dddbc6e 27d9422 4d1e585 0860d44` | All 4 commits verified | ✓ PASS |
| Set Pieces tab rendered on tab click | Browser required | — | ? SKIP (needs browser) |
| Landscape tip on mobile portrait | Browser + DevTools | — | ? SKIP (needs browser) |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| DATA-04 | 26-01 | Extract penalties_text, direct_freekicks_text, corners_and_indirect_freekicks_text into merged_players.json | ✓ SATISFIED | merge.py lines 367-369; MergedPlayer type lines 114-116 |
| SP-02 | 26-01 (pipeline), 26-02 (UI) | Alert user when set-piece order changes between pipeline runs | ✓ SATISFIED (code) / ? HUMAN (runtime) | _diff_sp_snapshots + save in run.py; SetPieceChangeAlert + Changed badge in SetPieceTakerPanel; requires browser confirmation |
| SP-01 | 26-02 | User can see penalty, FK, and corner takers per team in dedicated panel | ✓ SATISFIED (code) / ? HUMAN (runtime) | SetPieceTakerPanel with team card grid wired to useSetPieces; requires browser confirmation |
| MOB-LS-01 | 26-02 | Landscape mode tip on data-heavy tabs when mobile + portrait | ✓ SATISFIED (code) / ? HUMAN (runtime) | LandscapeTip in GemTable (line 123) and DefConTables (line 121) with isMobile+isPortrait+orientationchange; requires browser confirmation |

All 4 requirement IDs claimed in PLAN frontmatter are accounted for. No orphaned requirements for Phase 26 found in REQUIREMENTS.md (traceability table maps DATA-04, SP-01, SP-02, MOB-LS-01 to Phase 26 — all four covered).

---

### Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| SetPieceTakerPanel.tsx | Empty state `data.teams.length === 0` returns "not yet available" copy | ℹ️ Info | NOT a stub — this is correct handling for pre-pipeline-run state; real data flows when pipeline executes |
| SetPieceChangeAlert.tsx | `if (changeCount === 0) return null` | ℹ️ Info | NOT a stub — correct conditional render; alert only shows when there are actual changes |

No blockers or warnings found.

---

### Human Verification Required

#### 1. Set Pieces Tab — Team Card Grid

**Test:** Run `npm run dev`, open http://localhost:3000, run `python pipeline/run.py` first to generate set_piece_changes.json, then click the "Set Pieces" tab on desktop.
**Expected:** A 3-column grid of 20 Premier League team cards, each showing Penalties / Direct FK / Corners taker names. Heading reads "Set-Piece Takers" with the subtitle about FPL bootstrap-static sourcing.
**Why human:** set_piece_changes.json does not yet exist in local cache — must be generated by a pipeline run. Visual layout requires browser.

#### 2. Amber Alert Banner + Changed Badge (SP-02 runtime path)

**Test:** Manually edit `pipeline/cache/set_pieces_snapshot.json` to change one taker ID, then re-run the pipeline. Reload the Set Pieces tab.
**Expected:** Amber banner appears reading "Set-piece changes detected — 1 taker order change(s) since the last pipeline run. Updated rows are marked below." The changed taker row shows an amber "Changed" badge.
**Why human:** Requires deliberate snapshot manipulation to trigger the changed path; cannot simulate without running the full pipeline.

#### 3. Landscape Tip on Gems and DefCon (MOB-LS-01)

**Test:** Open DevTools responsive mode at 390x844 (portrait). Switch to Gems tab — verify tip appears above the table. Switch to DefCon tab — verify tip appears. Rotate to landscape (844x390) — verify tip disappears. Return to desktop width — verify tip absent.
**Expected:** "Rotate to landscape for the full table." appears only on mobile portrait on both Gems and DefCon tabs.
**Why human:** orientationchange events and CSS sm:hidden breakpoint require a real browser environment.

---

### Gaps Summary

No gaps found. All artifacts are substantive (not stubs), all key links are wired, all requirement IDs are covered, and the test suite passes clean. The three human verification items are UX confirmation tasks, not code defects — the implementation is complete and correct. The missing `set_piece_changes.json` cache file is expected (pipeline not run in this environment) and is handled gracefully by the empty-state UI.

---

_Verified: 2026-04-27T23:25:00Z_
_Verifier: Claude (gsd-verifier)_
