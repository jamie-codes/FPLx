---
phase: 41-accuracy-ui-model-rationalisation
plan: 03
type: execute
wave: 2
depends_on: [41-02]
files_modified:
  - src/lib/types.ts
  - src/components/accuracy/AccuracyTab.tsx
  - src/components/accuracy/AccuracyTab.test.tsx
  - pipeline/merge.py
  - src/components/gem-table/columns.tsx
  - src/components/gem-table/GwToggle.tsx
  - src/components/gem-table/GemTable.tsx
  - src/app/page.tsx
autonomous: false
requirements: [ACC-06]
must_haves:
  truths:
    - "User confirms which projection model to remove via terminal prompt — selection backed by live hit-rate data from accuracy_backtest.json"
    - "After removal, the loser model leaves NO references in pipeline/merge.py, src/lib/types.ts, src/components/accuracy/AccuracyTab.tsx, src/components/gem-table/columns.tsx, src/components/gem-table/GwToggle.tsx, or src/components/gem-table/GemTable.tsx"
    - "After removal, npx tsc --noEmit exits 0"
    - "After removal, the full vitest suite exits 0"
    - "After removal, the surviving model's primary 1GW prediction column is positioned immediately to the LEFT of the last_gw_actual_pts column in GemTable"
  artifacts:
    - path: ".planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md"
      provides: "Audit trail recording which model was removed, the live hit rates that informed the decision, and the per-file deletion log"
      contains: "## Removal Decision"
  key_links:
    - from: "human checkpoint"
      to: "accuracy_backtest.json data"
      via: "pre-prompt summary print"
      pattern: "xPts hit rate.*proj_pts hit rate"
    - from: "post-removal codebase"
      to: "loser model"
      via: "absence (grep returns 0 hits)"
      pattern: "{loser_model_pattern}"
---

<objective>
Decide which projection model is the loser based on live hit-rate data, then fully remove it from the codebase. This is the only `autonomous: false` plan in the phase — it has a `checkpoint:human-verify` gate that pauses for user input.

Purpose: Honour D-06 (human-in-the-loop decision), D-07 (full cut, no dead code), and D-08 (winner determined at runtime, not pre-decided).

Output:
- Terminal-only checkpoint that displays live hit rates from `accuracy_backtest.json` and prompts for `xpts` or `proj_pts`
- Full per-file removal of the loser's code, types, columns, and merge.py blocks
- A decision audit log committed alongside the removal
- Repositioned `last_gw_actual_pts` column adjacent to the surviving model's 1GW column

Critical: The removal scope is ASYMMETRIC. proj_pts removal does NOT touch `columns.tsx` (proj_pts has no GemTable column) or `GwToggle.tsx`. xPts removal touches both heavily plus four pipeline helper functions. The two action blocks below cover each scenario explicitly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-accuracy-ui-model-rationalisation/41-CONTEXT.md
@.planning/phases/41-accuracy-ui-model-rationalisation/41-RESEARCH.md
@.planning/phases/41-accuracy-ui-model-rationalisation/41-PATTERNS.md
@.planning/phases/41-accuracy-ui-model-rationalisation/41-UI-SPEC.md
@.planning/phases/41-accuracy-ui-model-rationalisation/41-02-SUMMARY.md
@.planning/phases/40-accuracy-pipeline/40-CONTEXT.md

# Source-of-truth files (read before editing)
@pipeline/merge.py
@src/lib/types.ts
@src/components/accuracy/AccuracyTab.tsx
@src/components/accuracy/AccuracyTab.test.tsx
@src/components/gem-table/columns.tsx
@src/components/gem-table/GwToggle.tsx
@src/components/gem-table/GemTable.tsx
@src/app/page.tsx

<interfaces>
<!-- Pre-existing locations of loser-model code (verified line numbers). Use these to bound your edits. -->

```text
proj_pts removal (5 sites):
  pipeline/merge.py:115            def _proj_pts_ngw(...)        — DELETE entire function
  pipeline/merge.py:245            comment "Mirrors _proj_pts_ngw() loop structure (lines 104-133)" — UPDATE the comment to drop reference
  pipeline/merge.py:804            proj_pts_1gw = round(ep_next * availability, 2)   — DELETE
  pipeline/merge.py:815-816        proj_pts_3gw / proj_pts_5gw   — DELETE
  pipeline/merge.py:829-831        player['proj_pts_*gw'] = ...  — DELETE
  src/lib/types.ts:138-140         proj_pts_1gw/3gw/5gw on MergedPlayer — DELETE
  src/lib/types.ts (Phase 41 add)  proj_pts_predicted/proj_pts_rank/proj_pts_flagged on AccuracyHaulter; proj_pts_predicted/proj_pts_delta on AccuracyPlayerGw — DELETE
  src/components/accuracy/AccuracyTab.tsx  All proj_pts_* column pairs in HaulterList + PlayerDeltaTable + GwSummaryTable — DELETE
  src/components/accuracy/AccuracyTab.test.tsx  proj_pts_* fixture fields + assertions — DELETE
```

```text
xPts removal (8+ sites):
  pipeline/merge.py:147            def _cs_prob(...)             — DELETE
  pipeline/merge.py:166            def _compute_xpts_fixture(...) — DELETE
  pipeline/merge.py:228            def _xpts_ngw(...)            — DELETE
  pipeline/merge.py:281            def _compute_xpts_sigma(...)  — DELETE
  pipeline/merge.py:448            'xPts_1gw': p.get('xPts_1gw', 0.0),   — DELETE (or fix calling block)
  pipeline/merge.py:837-852        xpts_1gw / 3gw / 5gw + xPts_components_1gw assignments — DELETE
  pipeline/merge.py:869-877        _sigma_1gw / 3gw / 5gw assignments — DELETE
  pipeline/merge.py:891-902        BGW guards using xPts_1gw — REWRITE (differential block now uses proj_pts_1gw or remove differential block entirely if it depended only on xPts)
  pipeline/merge.py:932-943        xPts_90th_1gw + _sigma cleanup — DELETE
  src/lib/types.ts:147-158, 173    xPts_*gw, xPts_ceiling_*gw, xPts_components_1gw, xPts_90th_1gw on MergedPlayer — DELETE
  src/lib/types.ts (Phase 41 add)  xpts_predicted/xpts_rank/xpts_flagged on AccuracyHaulter; xpts_predicted/xpts_delta on AccuracyPlayerGw; xpts_hit_rate on AccuracyGwSummary + AccuracySummary — DELETE
  src/components/gem-table/columns.tsx  xPts_1gw/3gw/5gw col.accessor entries (lines 142-177) + XPtsCell helper (lines 23-56) + VarianceBadge import (line 5) — DELETE
  src/components/gem-table/GwToggle.tsx  gwVisibility object xPts_1gw/3gw/5gw entries (lines 73-75); GwToggle component is no longer needed if no horizon-toggled column remains — REWRITE getColumnVisibility to drop gwVisibility, simplify GwToggle to a no-op or delete the component (and remove its callers in GemTable.tsx)
  src/components/accuracy/AccuracyTab.tsx  All xpts_* column pairs in HaulterList + PlayerDeltaTable + GwSummaryTable; default sort key changes to 'proj_pts_delta' — REWRITE relevant sections
  src/components/accuracy/AccuracyTab.test.tsx  xpts_* fixture fields + assertions, default-sort assertion uses proj_pts_delta — REWRITE assertions
  src/components/gem-table/GemTable.tsx  GwToggle import + render call (if GwToggle removed) — DELETE
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live hit-rate review + model selection prompt</name>
  <read_first>
    pipeline/cache/accuracy_backtest.json,
    .planning/phases/41-accuracy-ui-model-rationalisation/41-CONTEXT.md
  </read_first>
  <what-built>
    Plan 02 wired AccuracyTab into the app and added last_gw_actual_pts to GemTable. Both projection models (xPts and proj_pts) are still rendered side-by-side in the AccuracyTab delta/haulter sections. Before this checkpoint, no removal has happened.
  </what-built>
  <how-to-verify>
    The executor will:

    1. Read `pipeline/cache/accuracy_backtest.json`. If the file does not exist, abort the checkpoint with a clear error: `accuracy_backtest.json not found — Phase 40 pipeline must run before model rationalisation`. Surface to user as a blocker, do not proceed with deletion.
    2. Print to the terminal exactly this format (substituting live numbers — values shown with `.toFixed(1)` for percentages, integer for counts):
       ```
       ============================================================
       Phase 41 ACC-06 — Model Rationalisation Checkpoint
       ============================================================

       Overall hit rates (across all GWs covered):
         xPts      hit rate:  {xpts_hit_rate*100:.1f}%
         proj_pts  hit rate:  {proj_pts_hit_rate*100:.1f}%

       Per-GW breakdown:
         GW    Haulters   xPts %      proj_pts %
         ----  --------   --------    -----------
         {gw}  {count}    {xrate:.1f}%   {prate:.1f}%
         ...

       Removal scope (asymmetric — read carefully):

       If you remove [proj_pts]:
         - pipeline/merge.py: _proj_pts_ngw function + 3 player-dict assignments
         - src/lib/types.ts: 3 fields on MergedPlayer; 3 fields on AccuracyHaulter; 2 on AccuracyPlayerGw
         - src/components/accuracy/AccuracyTab.tsx: 3 proj_pts column pairs
         - GemTable columns: NO CHANGE (proj_pts has no GemTable column)
         - GwToggle: NO CHANGE

       If you remove [xpts]:
         - pipeline/merge.py: 4 helper functions (_cs_prob, _compute_xpts_fixture,
           _xpts_ngw, _compute_xpts_sigma) + xPts/sigma/90th_1gw assignments + BGW
           guards + differential block (uses xPts_1gw)
         - src/lib/types.ts: 8+ fields on MergedPlayer; 3 on AccuracyHaulter;
           2 on AccuracyPlayerGw; 2 on AccuracyGwSummary; 2 on AccuracySummary
         - src/components/accuracy/AccuracyTab.tsx: rewrite default sort key
           to 'proj_pts_delta'; remove all xpts_* column pairs
         - src/components/gem-table/columns.tsx: 3 xPts_*gw column entries,
           XPtsCell helper, VarianceBadge import
         - src/components/gem-table/GwToggle.tsx: gwVisibility object simplified or removed;
           GwToggle component may become a no-op
         - src/components/gem-table/GemTable.tsx: GwToggle import/render removed if applicable

       Recommendation: choose the model with the LOWER overall hit rate.

       ============================================================
       Which model do you want to REMOVE? [xpts/proj_pts]:
       ```
    3. Read user input. Acceptable values (case-insensitive, trimmed): `xpts`, `proj_pts`. If invalid, re-prompt up to 3 times then abort.
    4. Echo back the selection with a final confirmation: `You selected to REMOVE: {choice}. This is irreversible. Proceed? [yes/no]:`
    5. On `yes`, write `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md` with this content:
       ```
       # Phase 41 ACC-06 Model Removal Decision

       **Date:** {ISO timestamp}
       **Source data:** pipeline/cache/accuracy_backtest.json (generated_at: {bt.generated_at})

       ## Live Hit Rates (at decision time)

       | Model    | Overall hit rate |
       |----------|------------------|
       | xPts     | {xpts_hit_rate*100:.1f}% |
       | proj_pts | {proj_pts_hit_rate*100:.1f}% |

       Per-GW:
       (table from print output above)

       ## Removal Decision

       Removed model: **{choice}**
       Surviving model: **{survivor}**

       ## Next Step

       Plan 03 Task 2 ({choice}-removal) executes the file-by-file deletion.
       ```
    6. Set an environment-style marker the executor can read in Task 2 — store the choice in a file the next task reads: write the literal string `xpts` or `proj_pts` (lowercase, no newline) to `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target` (a temporary file; commit it alongside the decision log so the audit trail is reproducible — it is NOT a runtime artifact).
    7. PAUSE the plan and wait for the user to confirm. Resume signal: user types `approved` or `proceed`. The executor MUST NOT begin deletion until this checkpoint resumes.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md` exists with literal heading `## Removal Decision` and contains either `Removed model: **xpts**` or `Removed model: **proj_pts**`
    - File `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target` exists with content matching exactly `xpts` or `proj_pts` (lowercase, no trailing newline if possible — `printf %s` style)
    - Terminal output included BOTH overall hit rates and per-GW breakdown
    - Terminal output included BOTH removal-scope summaries (proj_pts vs xpts) so user could compare
    - User explicitly confirmed `yes` (re-confirmation gate after initial selection)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the .removal-target file is written and you've inspected the decision log.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the loser-model removal (asymmetric — branch on .removal-target)</name>
  <files>
    pipeline/merge.py,
    src/lib/types.ts,
    src/components/accuracy/AccuracyTab.tsx,
    src/components/accuracy/AccuracyTab.test.tsx,
    src/components/gem-table/columns.tsx,
    src/components/gem-table/GwToggle.tsx,
    src/components/gem-table/GemTable.tsx
  </files>
  <read_first>
    .planning/phases/41-accuracy-ui-model-rationalisation/.removal-target,
    .planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md,
    pipeline/merge.py,
    src/lib/types.ts,
    src/components/accuracy/AccuracyTab.tsx,
    src/components/accuracy/AccuracyTab.test.tsx,
    src/components/gem-table/columns.tsx,
    src/components/gem-table/GwToggle.tsx,
    src/components/gem-table/GemTable.tsx,
    .planning/phases/41-accuracy-ui-model-rationalisation/41-RESEARCH.md
  </read_first>
  <action>
    Read `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target` to determine the removal target. Branch on the value.

    ════════════════════════════════════════════════════════════════
    BRANCH A: target == "proj_pts" (less invasive)
    ════════════════════════════════════════════════════════════════

    A1. **pipeline/merge.py**
        - Delete the entire function `_proj_pts_ngw` starting at line 115. The function spans from `def _proj_pts_ngw(` to the line BEFORE the next top-level `def` (likely `def _cs_prob` at line 147). Verify by checking that the line immediately following deletion is a blank line followed by `def _cs_prob` (or whichever def is next).
        - Update the comment at line 245 (`Mirrors _proj_pts_ngw() loop structure (lines 104-133).`) to remove the function reference. New comment: `Mirrors the per-fixture loop pattern.` (no line numbers, no dead reference).
        - Delete lines 804, 815, 816 in the player loop:
          ```python
          proj_pts_1gw = round(ep_next * availability, 2)
          ```
          and
          ```python
          proj_pts_3gw = _proj_pts_ngw(ppg, sp, player_fixtures, 3)
          proj_pts_5gw = _proj_pts_ngw(ppg, sp, player_fixtures, 5)
          ```
          Note: `ep_next` and `availability` may still be needed by surviving xPts code — DO NOT remove their assignments unless they become unreferenced. Use grep to confirm: `grep -n "ep_next\|availability" pipeline/merge.py` — if any other lines reference them, keep their assignments. Likewise `ppg`, `sp`, `player_fixtures` — likely still needed by xPts. Verify before deleting.
        - Delete lines 829-831:
          ```python
          player['proj_pts_1gw'] = proj_pts_1gw
          player['proj_pts_3gw'] = proj_pts_3gw
          player['proj_pts_5gw'] = proj_pts_5gw
          ```

    A2. **src/lib/types.ts**
        - In `MergedPlayer` (lines ~138-140), delete:
          ```typescript
          proj_pts_1gw: number
          proj_pts_3gw: number
          proj_pts_5gw: number
          ```
          (Including their leading comment block on lines 137-138 about "Phase 7 — PROJ-01/02/03". The comment block specifically about proj_pts is dead; remove it.)
        - In `AccuracyHaulter` (added by Plan 01), delete:
          ```typescript
          proj_pts_predicted: number
          proj_pts_rank: number
          proj_pts_flagged: boolean
          ```
        - In `AccuracyPlayerGw` (added by Plan 01), delete:
          ```typescript
          proj_pts_predicted: number
          proj_pts_delta: number
          ```
        - Leave `AccuracyGwSummary.proj_pts_flagged` and `proj_pts_hit_rate` UNTOUCHED — these reflect what the pipeline writes; the pipeline itself still writes both models' summary stats from Phase 40. (D-07 says full cut of UI/types for the loser, but the JSON schema written by the Phase 40 pipeline is independent of this UI removal. The UI simply stops rendering those fields.)

          Wait — re-read D-07: "Removal = full cut: Delete the loser model's GemTable column from columns.tsx, remove its merged_players.json field from merge.py and related type definitions in types.ts, and remove its display from all UI components". This means the type field is removed too. So:
          - DELETE `proj_pts_flagged: number` and `proj_pts_hit_rate: number` from `AccuracyGwSummary`
          - DELETE `proj_pts_hit_rate: number` from `AccuracySummary`
          - The Phase 40 pipeline still writes those values to the JSON file, but the TS interface no longer declares them (the runtime values are simply ignored — JSON parses don't care about extra fields). Acceptable per D-07 since the UI never reads them.

    A3. **src/components/accuracy/AccuracyTab.tsx**
        - **GwSummaryTable**: Remove the `proj_pts Flagged` and `proj_pts Hit Rate` columns. Update the colspan-equivalent of the Overall row by removing the corresponding cells. Header row goes from 6 columns to 4: GW, Haulters, xPts Flagged, xPts Hit Rate. Body rows match.
        - **HaulterList**: Remove the three columns `proj_pts Pred`, `proj_pts Rank`, `proj_pts` (flagged). Header row goes from 9 columns to 6. Remove their cells from the body row template.
        - **PlayerDeltaTable**: Remove the two columns `proj_pts Pred` and `proj_pts Δ`. Update `SortKey` to drop `'proj_pts_predicted' | 'proj_pts_delta'`. Update the `DeltaRow` type to drop those fields (and the corresponding push in `flat`). Header row goes from 8 columns to 6.
        - All other behaviour (default sort, sort toggle, tier classes) is UNCHANGED.

    A4. **src/components/accuracy/AccuracyTab.test.tsx**
        - Remove the `proj_pts_*` fields from `fixtureBacktest` haulters and players entries.
        - Remove `proj_pts_flagged`, `proj_pts_hit_rate` from each `gws[]` entry, and `proj_pts_hit_rate` from the top-level summary. (Tests parse against the now-narrower TS interface; JSON would still be valid but test fixtures should match the type.)
        - The 5 existing tests should remain conceptually identical; their assertions referencing xPts columns continue to pass.

    A5. **No edits to `columns.tsx`, `GwToggle.tsx`, or `GemTable.tsx`** — proj_pts has no GemTable presence (verified in 41-RESEARCH.md Pitfall 5). Skip these files.

    A6. **src/app/page.tsx** — No edits.

    A7. Run the full test suite. Fix any TypeScript errors revealed (most likely: a stray `proj_pts_*` reference in a file outside the listed scope). Use `grep -rn "proj_pts" src/` and `grep -rn "proj_pts" pipeline/` to verify zero remaining references in source files (excluding test fixtures already pruned and excluding D-* / pipeline source for accuracy.py / 40-* artifacts which are read-only here).

    ════════════════════════════════════════════════════════════════
    BRANCH B: target == "xpts" (extensive)
    ════════════════════════════════════════════════════════════════

    B1. **pipeline/merge.py**
        - Delete the four helper functions:
          - `def _cs_prob(...)` starting line 147 — through the line before the next top-level def
          - `def _compute_xpts_fixture(...)` starting line 166
          - `def _xpts_ngw(...)` starting line 228
          - `def _compute_xpts_sigma(...)` starting line 281
          (Each `def` block ends at the blank line preceding the next top-level `def` or `class`.)
        - Delete the post-loop xPts block beginning at line 837 through line 877 (xpts_1gw computation, xPts_components_1gw assignment, _sigma_1gw/3gw/5gw assignments).
        - Delete lines 891-902 (BGW guards for xPts) and lines 932-943 (xPts_90th_1gw + _sigma cleanup). VERIFY: lines 891-902 are guards inside differential/regression blocks. The differential block uses `xPts_1gw`. With xPts removed, the differential block must be REWRITTEN to use `proj_pts_1gw` instead, or the differential block must be removed entirely.
        - Differential block decision: rewrite to use `proj_pts_1gw` so DIFF/TRAP flags continue to function. Replace `p.get('xPts_1gw')` with `p.get('proj_pts_1gw')` and `p['xPts_1gw']` with `p['proj_pts_1gw']` inside that block (lines ~891-925).
        - Captain-picks block (line 448 area): if it depends on `xPts_1gw`, replace with `proj_pts_1gw`. If the captain-picks code is a separate function, follow its structure to swap the source field consistently.
        - Confirm with `grep -n "xPts_\|_sigma_\|xpts_" pipeline/merge.py` that no references remain post-edit.

    B2. **src/lib/types.ts**
        - From `MergedPlayer`, delete:
          ```typescript
          xPts_1gw?: number
          xPts_3gw?: number
          xPts_5gw?: number
          xPts_ceiling_1gw?: boolean
          xPts_ceiling_3gw?: boolean
          xPts_ceiling_5gw?: boolean
          xPts_components_1gw?: { ... } | null
          xPts_90th_1gw?: number
          ```
          plus their leading comment blocks describing Phase 28/31 xPts engine.
        - From `AccuracyHaulter`, delete `xpts_predicted`, `xpts_rank`, `xpts_flagged`.
        - From `AccuracyPlayerGw`, delete `xpts_predicted`, `xpts_delta`.
        - From `AccuracyGwSummary`, delete `xpts_flagged`, `xpts_hit_rate`.
        - From `AccuracySummary`, delete `xpts_hit_rate`.

    B3. **src/components/gem-table/columns.tsx**
        - Delete the import line for `VarianceBadge`.
        - Delete the entire `XPtsCell` function (lines 23-56).
        - Delete the three `col.accessor('xPts_*gw', ...)` entries (currently between the `mins_risk` display column and the `last_gw_actual_pts` column added by Plan 01). The `last_gw_actual_pts` column entry MUST remain.
        - Reposition: After deletion, `last_gw_actual_pts` should be positioned RIGHT AFTER the `mins_risk` display column (since xPts_1gw used to sit between them). Verify by reading the resulting accessor order.
        - The `gwN` parameter on `createColumns` remains — Plan 02 added it for the `last_gw_actual_pts` header.

    B4. **src/components/gem-table/GwToggle.tsx**
        - Remove `xPts_1gw`, `xPts_3gw`, `xPts_5gw` from the `gwVisibility` object (lines 73-75). With no horizon-gated columns remaining, the `gwVisibility` object is empty and can be removed entirely. The `getColumnVisibility` function simplifies to:
          ```typescript
          export function getColumnVisibility(
            horizon: 1 | 3 | 5,
            isMobile = false,
            preset: ViewPreset = 'default'
          ): Record<string, boolean> {
            // horizon parameter retained for backward compatibility, no longer used
            void horizon
            if (isMobile) return MOBILE_HIDDEN_COLUMNS
            return PRESET_COLUMN_VISIBILITY[preset]
          }
          ```
        - Decide on the GwToggle component:
          - If keeping the function for backward compatibility: leave the `GwToggle` Props/component definition but mark it as no-op-effectively (no horizon-gated columns left, so the toggle has no effect on visibility — but it may still be wired in `GemTable.tsx`). Recommended: REMOVE the `GwToggle` component entirely and remove the `horizon` parameter from `getColumnVisibility`.
        - Cleanest path: change `getColumnVisibility` signature to drop `horizon`:
          ```typescript
          export function getColumnVisibility(
            isMobile = false,
            preset: ViewPreset = 'default'
          ): Record<string, boolean> {
            if (isMobile) return MOBILE_HIDDEN_COLUMNS
            return PRESET_COLUMN_VISIBILITY[preset]
          }
          ```
          Remove the `Props` interface and `GwToggle` component export. Update all call sites (next step).

    B5. **src/components/gem-table/GemTable.tsx**
        - Remove the `import { GwToggle, ... }` import — keep the `getColumnVisibility, type ViewPreset` portion.
        - Remove the state `const [gwHorizon, setGwHorizon] = useState<1 | 3 | 5>(1)`.
        - Update the `getColumnVisibility(gwHorizon, isMobile, ...)` call site to drop the first arg: `getColumnVisibility(isMobile, isMobile ? 'default' : preset)`.
        - Remove any JSX `<GwToggle value={gwHorizon} onChange={setGwHorizon} />` element.
        - Remove the `useAccuracy` call added by Plan 02 IF the only purpose was the GW header — wait, `useAccuracy` is needed by the `last_gw_actual_pts` column header. KEEP it. The `lastGwActualGwN` derivation and `createColumns(handleCompare, lastGwActualGwN)` invocation stay as-is.

    B6. **src/components/accuracy/AccuracyTab.tsx**
        - **GwSummaryTable**: Remove `xPts Flagged` and `xPts Hit Rate` columns. Header from 6 → 4 columns. Remove the `xpts_hit_rate` `<HitRateBadge>` from the Overall row.
        - **HaulterList**: Remove `xPts Pred`, `xPts Rank`, `xPts` (flagged) columns. Header from 9 → 6 columns. Update header tooltips.
        - **PlayerDeltaTable**:
          - Default sort key changes from `'xpts_delta'` → `'proj_pts_delta'`.
          - Header label "xPts Δ" removed; "proj_pts Δ" becomes the default sort.
          - Description text changes to: `Sorted by biggest proj_pts over-prediction (most negative delta first). Click any column header to re-sort.`
          - `SortKey` type loses `'xpts_predicted' | 'xpts_delta'`.
          - `DeltaRow` loses `xpts_predicted` and `xpts_delta` fields.
          - Header from 8 → 6 columns.

    B7. **src/components/accuracy/AccuracyTab.test.tsx**
        - Update fixture: remove `xpts_*` fields from haulters, players[].gws[], summary.gws[], and summary.
        - Update test "ACC-04: default sort is xPts Δ ascending" to "ACC-04: default sort is proj_pts Δ ascending" — assertion uses the most-negative `proj_pts_delta` row in the fixture.
        - Update test "ACC-04: clicking xPts Δ header" to "ACC-04: clicking proj_pts Δ header".
        - Update fixture data so the most-negative `proj_pts_delta` produces a deterministic first-row assertion.

    B8. **src/app/page.tsx** — No edits required (the accuracy sub-tab still mounts AccuracyTab; the SubTab union is unchanged).

    B9. Run the full test suite. Fix any TypeScript errors revealed by removing xPts. Likely sites needing updates: `src/lib/gem-score.ts`, `src/components/gem-table/PlayerComparisonModal.tsx`, `src/components/gem-table/RegressionSignalBadge.tsx`, `src/components/captaincy/CaptainPicksPanel.tsx`. For each file showing TS errors, replace `xPts_1gw` with `proj_pts_1gw` (and equivalent windows) — or remove the xPts references if the surviving model is exposed through a different field on the player. Verify with `grep -rn "xPts\|xpts_\|_sigma_\|VarianceBadge\|XPtsCell\|xPts_components\|xPts_ceiling\|xPts_90th" src/` and `grep -rn "xPts\|_sigma_" pipeline/` returning zero matches in source files.

    ════════════════════════════════════════════════════════════════
    BOTH BRANCHES — POST-REMOVAL CLEANUP
    ════════════════════════════════════════════════════════════════

    C1. Run `npx tsc --noEmit`. Fix all TypeScript errors before proceeding. Each error site should be either:
        - A stray reference to the loser model (DELETE the reference)
        - A computation that depended on the loser model (REPLACE with the survivor's equivalent field)

    C2. Run `npx vitest run`. Fix all test failures. Common sites: PlayerComparisonModal.test.tsx (xPts breakdown assertions), gem-score.test.ts (if a score weight referenced xPts), captaincy tests (CaptainPicksPanel uses xPts ceiling).

    C3. Delete the temporary marker file: `rm .planning/phases/41-accuracy-ui-model-rationalisation/.removal-target`. The decision log (41-03-DECISION.md) remains as the audit trail.

    C4. Update `41-03-DECISION.md` with a "Removal Log" appendix listing every file modified and its diff line count. Format:
        ```
        ## Removal Log

        | File | Lines deleted | Lines added |
        |------|---------------|-------------|
        | pipeline/merge.py | -78 | +2 |
        | src/lib/types.ts | -22 | 0 |
        | ...

        ## Verification

        - npx tsc --noEmit: PASS
        - npx vitest run: PASS ({N} tests)
        - grep -rn "{loser_model_pattern}" src/ pipeline/: 0 matches
        ```

    C5. Commit per CLAUDE.md (no Co-Authored-By trailer): `chore(41): remove {loser_model} per ACC-06 model rationalisation`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md` exists with `## Removal Log` section listing all modified files
    - The `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target` file has been DELETED post-removal
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
    - If target was `proj_pts`:
      - `grep -rn "proj_pts" src/` returns 0 matches
      - `grep -rn "proj_pts\|_proj_pts_ngw" pipeline/merge.py` returns 0 matches (other pipeline files like accuracy.py may still reference proj_pts since they consume it from cached merged_players.json — but merge.py specifically writes 0 references)
      - `grep -c "proj_pts" pipeline/cache/merged_players.json` may still be > 0 from a stale cache; ignore.
    - If target was `xpts`:
      - `grep -rn "xPts\|xpts_\|VarianceBadge\|XPtsCell\|_sigma_" src/` returns 0 matches in source files (test fixtures may use the lowercase `xpts_` from accuracy_backtest.json keys — but only inside files that read accuracy_backtest.json, NOT inside removed UI/type files)
      - Actually given AccuracyBacktest types are pruned, even test fixtures should be free of `xpts_*` references. So: `grep -rn "xPts\|xpts_\|VarianceBadge\|XPtsCell\|_sigma_" src/ pipeline/merge.py` returns 0 matches.
      - The `last_gw_actual_pts` column is positioned immediately after the `mins_risk` display column (verify: read columns.tsx and check accessor ordering)
    - All `<verify>` automated commands pass
  </acceptance_criteria>
  <done>
    The loser model has zero references in the codebase (per the grep gates above). The full test suite passes. The decision log records what was removed and why.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal user input → checkpoint prompt | User selects which model to remove via stdin. Input is constrained to `xpts`/`proj_pts`/`yes`/`no`. |
| Filesystem → `.removal-target` marker | Internal handoff between Task 1 and Task 2; not a runtime artifact. Deleted after use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-13 | T (Tampering) | Removal target marker file | mitigate | Task 2 reads `.removal-target` and validates content matches exactly `xpts` or `proj_pts`. Any other value → abort with error. Treat as untrusted internal input. |
| T-41-14 | E (Elevation of privilege) | merge.py block deletion | mitigate | Removal modifies pipeline source files only. No runtime privilege boundary crossed. Captured in audit trail (`41-03-DECISION.md`). |
| T-41-15 | I (Information disclosure) | Decision log content | accept | Audit log records hit-rate decimals + removal scope. No PII or secrets. |
| T-41-16 | D (Denial of service) | Pipeline broken by partial removal | mitigate | Task 2 acceptance criteria require `npx tsc --noEmit` and `npx vitest run` exit 0 before completion. Partial removal that breaks the build fails the gate. |
| T-41-17 | R (Repudiation) | Irreversible removal | mitigate | Decision log + git commit form a complete audit trail; the original code is recoverable from `git log` even after deletion. |

No high-severity threats remain unmitigated.
</threat_model>

<verification>
- Branch A (proj_pts): `grep -rn "proj_pts" src/ pipeline/merge.py` → 0 matches
- Branch B (xpts): `grep -rn "xPts\|xpts_\|VarianceBadge\|XPtsCell\|_sigma_" src/ pipeline/merge.py` → 0 matches
- `npx tsc --noEmit` → exit 0
- `npx vitest run` → exit 0
- `41-03-DECISION.md` exists with `## Removal Decision` and `## Removal Log` sections
- `.removal-target` file has been deleted
</verification>

<success_criteria>
1. Human checkpoint paused execution and presented live hit-rate data sourced from `accuracy_backtest.json`
2. User explicitly confirmed model choice (two-step: select + confirm `yes`)
3. Decision audit trail (`41-03-DECISION.md`) committed alongside the removal
4. Loser model has zero references in source files (verified by grep gates)
5. Full TS compile + vitest suite green post-removal
6. `last_gw_actual_pts` column repositioned adjacent to surviving model's primary column
</success_criteria>

<output>
After completion, create `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-SUMMARY.md` covering:
- Which model was removed and why (overall hit rates at decision time)
- Per-file removal log (lines deleted/added)
- Confirmation that `npx tsc --noEmit` and `npx vitest run` both exit 0
- Confirmation that `.removal-target` was cleaned up
- Confirmation that `41-03-DECISION.md` is the permanent audit trail
- Note any unexpected ripple effects (e.g. files outside the planned scope that needed edits)
</output>
