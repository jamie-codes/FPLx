---
context: phase
phase: 41-accuracy-ui-model-rationalisation
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-04-30T08:59:53Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| proj_pts_1gw cascade | Removing proj_pts_1gw from MergedPlayer breaks 9+ files across planning/transfer/captaincy engines. The partial removal was committed to avoid shipping broken code mid-session. | advisory | Fix all TS errors (npx tsc --noEmit) before committing the MergedPlayer field removal. Run vitest after. |

<current_state>
Phase 41, Plan 03, Task 2 — partially complete.

The proj_pts removal (Branch A) is ~70% done:
- ✅ pipeline/merge.py: _proj_pts_ngw deleted, proj_pts_* player-dict assignments deleted
- ✅ AccuracyTab.tsx: all proj_pts columns removed from GwSummaryTable, HaulterList, PlayerDeltaTable
- ✅ AccuracyHaulter, AccuracyPlayerGw, AccuracyGwSummary, AccuracySummary: proj_pts fields removed from types.ts
- ✅ AccuracyTab.test.tsx: fixture stripped of proj_pts fields
- ✅ 41-03-DECISION.md written (xPts 16.7% vs proj_pts 9.0% — proj_pts removed)
- ❌ proj_pts_1gw/3gw/5gw STILL ON MergedPlayer — planning engine dependency not yet swept

Last commit: `8baf8dc chore(41): remove proj_pts per ACC-06 model rationalisation`
</current_state>

<completed_work>
- Plan 41-01 COMPLETE: AccuracyBacktest types, /api/accuracy route, useAccuracy hook, /api/players graceful join, GemTable last_gw_actual_pts column, 5 RED test stubs
- Plan 41-02 COMPLETE: AccuracyTab.tsx (276 lines), Accuracy sub-tab in page.tsx, GemTable dynamic GW column header. All 5 RED tests GREEN.
- Plan 41-03 Task 1 COMPLETE: Pipeline ran, hit rates displayed, user selected proj_pts for removal, decision logged in 41-03-DECISION.md
- Plan 41-03 Task 2 PARTIAL: proj_pts removed from merge.py + AccuracyTab + accuracy interfaces. MergedPlayer proj_pts_1gw/3gw/5gw retained pending sweep.
</completed_work>

<remaining_work>
Plan 41-03 Task 2 — finish the proj_pts sweep:

1. Remove `proj_pts_1gw`, `proj_pts_3gw`, `proj_pts_5gw` from `MergedPlayer` in `src/lib/types.ts`
2. Fix all TS errors by replacing `proj_pts_1gw` → `xPts_1gw` (and 3gw/5gw equivalents) in:
   - src/lib/planning-engine.ts
   - src/lib/captaincy-engine.ts
   - src/lib/chip-strategy-engine.ts
   - src/lib/explain.ts
   - src/lib/replacement-shortlist.ts
   - src/components/planner/PlannerTab.tsx
   - src/components/planner/PlayerPickerModal.tsx
   - src/components/transfers/TransferPanel.tsx
   - src/lib/__tests__/planning-engine-rescore.test.ts (test fixtures)
   - src/lib/chip-strategy-engine.test.ts (test fixtures)
3. Check ScoredTransfer comment in types.ts (references proj_pts_1gw delta — update to xPts)
4. Run `npx tsc --noEmit` → must exit 0
5. Run `npx vitest run` → must exit 0
6. Delete `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target`
7. grep verify: `grep -rn "proj_pts" src/ pipeline/merge.py` → 0 matches
8. Update 41-03-DECISION.md with Removal Log section (lines deleted/added per file)
9. Commit: `chore(41): complete proj_pts sweep — remove from MergedPlayer and planning engine`
10. Create 41-03-SUMMARY.md
11. Run phase verification (gsd-verifier)
</remaining_work>

<decisions_made>
- **Remove proj_pts (not xPts)**: xPts overall hit rate 16.7% vs proj_pts 9.0% across GWs 30-34. User confirmed.
- **Retained proj_pts_1gw/3gw/5gw on MergedPlayer temporarily**: Context exhausted before planning engine sweep could be completed safely.
- **41-03-DECISION.md committed**: Audit trail of hit rates and removal choice is in git.
</decisions_made>

<blockers>
None — all blockers resolved. Pipeline ran, accuracy_backtest.json exists at pipeline/cache/.
</blockers>

## Required Reading (in order)
1. `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-PLAN.md` — Task 2 Branch A + post-removal cleanup steps C1-C5
2. `.planning/phases/41-accuracy-ui-model-rationalisation/41-03-DECISION.md` — which model was removed and why

## Infrastructure State
- `pipeline/cache/accuracy_backtest.json` — exists, generated 2026-04-30T08:41:19Z (5 GWs: 30-34)
- All tests passing except pre-existing failures in `tests/lib/captain-picks.test.ts` (5 errors, unrelated to this phase)
- `.removal-target` file exists at `.planning/phases/41-accuracy-ui-model-rationalisation/.removal-target` — delete after sweep

<context>
Plan 03 has two tasks: (1) human checkpoint showing hit rates and getting model selection, (2) executing the removal. Task 1 is fully done. Task 2 is partially done — the accuracy-specific code is cleaned up, but proj_pts_1gw is woven through the entire planning engine and needs a dedicated sweep replacing every reference with xPts_1gw. This is straightforward mechanical work — grep for proj_pts, replace with xPts equivalents, verify compile + tests.
</context>

<next_action>
Start with: Remove proj_pts_1gw/3gw/5gw from MergedPlayer in src/lib/types.ts, then run `npx tsc --noEmit 2>&1 | grep "error TS"` to get the full error list, then fix each file systematically. Commit when tsc and vitest both exit 0.
</next_action>
