# Pipeline runtime dependencies.
#
# soccerdata was removed 2026-09-04: it pinned seleniumbase, which pins
# beautifulsoup4==4.13.x against this file's own >=4.14.3, so pip could not
# resolve the file at all. It was safe to drop rather than pin around — the only
# mentions left in the tree are two comments, and pipeline.yml installs an
# explicit list that has never included it, so production has run without it all
# along. The failure only surfaced now because nothing installed this file.
requests>=2.32.0
pandas>=2.2.0
vercel-blob>=0.4.0
pulp>=2.7.0
python-dotenv>=1.0.0
anthropic>=0.98.1
numpy>=1.26.0
feedparser>=6.0.12
beautifulsoup4>=4.14.3
lxml>=6.1.0
rapidfuzz>=3.0.0
xgboost>=2.1.0
scikit-learn>=1.5.0
